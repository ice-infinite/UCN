[package]
name = "ucn-security"
description = "Fixed-capacity security session, crypto provider and replay owner for UCN v6 simplified"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ucn-owner = { path = "../ucn-owner" }
ucn-persistence = { path = "../ucn-persistence" }
ucn-types = { path = "../ucn-types" }
ucn-wire = { path = "../ucn-wire" }

[lints]
workspace = true
