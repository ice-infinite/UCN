# UCN Core 实现任务表

> 创建日期：2026-08-04  
> 实施原则：MCU-first、Core 无 Linux 运行依赖、C99、小核心/静态配置、每阶段先单测再模拟，最终必须通过集成测试。  
> 架构依据：[UCN 整体架构设计](UCN_整体架构设计.md) · [UCN v4 协议核心说明](UCN_v4_协议核心说明.md) · [UCN v5 Adaptive Wire Profile 设计方案](UCN_v5_Adaptive_Wire_Profile设计方案.md) · [V5-31～V5-33 PATH_INSTALL/API 修复报告](UCN_V5_31_PATH_INSTALL兼容与API符号修复报告.md) · [UCN v5 审计遗留问题实际修复方案](UCN_V5_审计遗留问题实际修复方案.md) · [UCN v5 最新审计交叉问题修复建议](UCN_V5_最新审计交叉问题修复建议.md) · [UCN v5 代码缺陷审计报告](UCN_V5_代码缺陷审计报告.md) · [UCN 更新后设计方案](UCN_更新后设计方案.md) · [节点内任务通信建议](UCN_节点内任务通信建议.md) · [UCN × ROS2 协同整体架构](UCN_ROS2协同整体架构.md) · [路由缓存与自动选路建议](UCN_路由缓存与自动选路建议.md) · [节点入离网与网络稳定性建议](UCN_节点入离网与网络稳定性建议.md) · [策略路由与可选负载均衡建议](UCN_策略路由与可选负载均衡建议.md) · [路由策略与负载均衡执行建议](UCN_路由策略与负载均衡执行建议.md) · [稳定化第二阶段建议](UCN_稳定化第二阶段问题与执行建议.md) · [T21 目标板资源报告](UCN_T21_目标板资源报告.md)

## 验收门禁

每个可交付阶段都必须按以下顺序推进，任一项失败不得标记完成：

```text
实现 → 单元测试 → 模拟测试 → 记录结果 → 进入下一阶段
```

最终再执行完整集成测试。测试证明当前实现符合已定义的协议范围，不等同于真实无线、电磁兼容、密码实现或飞行安全认证。

## 当前执行状态（2026-08-14）

| 优先级 | 当前有效任务 | 状态与边界 |
| --- | --- | --- |
| P0 | S22 高并发重复抑制与 RREQ 状态拆分 | 代码/软件测试完成；S21 M1/M2/M4 重放均无重复业务，真实重复链路和目标 MCU 资源仍归 S06。 |
| P0 发布门禁 | S02 生产安全闭环、S26 生产安全激活门禁 | S26 的失败关闭激活门禁已完成；真实身份、审计 AEAD、密钥和逐跳认证仍归 S02，未完成前不得宣称生产安全。 |
| P1 | S23 Feature-aware Profile 行为测试 | 已完成；Nano/Lite/Full 直接运行各自启用能力，远端 Actions 仍需推送后观察。 |
| P2 | S24 Service 配置与 Binding 生命周期、S25 Hop 可配置门禁 | 已完成；Binding 保持借用式静态契约，Hop 默认仍为已验证的 16。 |
| 实机 | S06、S07、V5-38、V5-49 | v5 三板 ESP-NOW+UART 首轮已完成：两跳、动态 Cost、热断/恢复和中间节点长离网均有实机证据；真实零丢帧、路由快速刷新、长期压力、功耗及其他 RTOS/Bearer 仍未完成。 |
| P1/P2 修复 | V5-27～V5-30 | 代码/软件闭环：异构 Bearer/Path 使用共同 Profile/MTU，能力失败确定性 RERR；Full/Nano 动态 MTU统一；Policy/Balance 跟随逻辑 Bearer。真实多板仍归 S06/S07。 |
| P1 发布修复 | V5-31～V5-33 | 已完成：v5 `PATH_INSTALL` 基础格式恢复兼容，扩展格式由 capability API 显式选择；Lite/Nano capability API 符号与跨 Profile 链接门禁已闭环。 |
| v5 | V5-00～V5-15、V5-17～V5-20、V5-22～V5-36、V5-44～V5-60 已闭环或完成当前软件门禁；V5-16 完成设计冻结；V5-21 阻塞于 S02 | LC-1、本地排序和 V5-51 每 Link 存活档/UART FAST 已实现；V5-52～57 已在 N16R8→N16R8→N8R8 的 UART 两跳上完成九档功能、115200～5M、窗口 1/4/8 和事件 Owner。V5-58～60 已把事件 Runtime、Stream Source、CAN/CAN-FD Frame Source 与经典 CAN Carrier 收回主库；真实 CAN 控制器/BSP、各 RTOS SDK、多源实机、Q0 严格延迟和 CPU/功耗仍归 V5-61。 |
| Extended | EXT-01～EXT-07 消息大小等级与有界分片/重组 | 已实现九档 Class、固定 TX/RX、CRC32、累计 ACK、默认窗口 1/显式窗口 2～8、有界 Go-Back-N、静态 Peer Capability、MTU 自适应和两跳透明中继；Core-only 不构建该 Archive。窗口 8 与事件 Owner 已完成一组 UART 两跳三板门禁；自动能力协商、完整 Path MTU/Endpoint 独立安全映射、并发压力、ESP-NOW-only/CAN-FD/切换中 Transfer 仍待完成。 |

## v5 Adaptive Wire Profile 执行表

> 执行顺序固定为：先固化并备份 v4，再创建并远端同步 v5 分支，之后才允许修改线协议。每项继续遵守“实现 → 单元测试 → 模拟测试 → 记录结果”。

| ID | 阶段 | 实现内容 | 验证与完成判据 | 状态 |
| --- | --- | --- | --- | --- |
| V5-00 | 基线与版本 | 冻结当前 v4 修复、全量回归、提交并推送 `main`；建立 `v4.0.0-final-before-v5` 标签和独立本地备份；从该标签建立 `codex/v5-adaptive-wire` 并推送 GitHub。 | v4 三 Profile/Service 组合、Sanitizer 和规模重放通过；标签、备份目录、分支和远端引用均可核对。 | 已完成：`main@25bcd22`、标签已推送、249 文件备份已校验、v5 分支已远端同步。 |
| V5-01 | 官方 Wire Class | 增加 W0/W1/W2/W3 固定描述符、Profile/Version 编码、1/2/3/4 B 字段读写和可变基础/Route/Path 帧头；未显式配置时兼容落到 W3。 | 四档 Golden Vector、边界值、坏 Profile、字段溢出、CRC/AAD、旧 v4 显式拒绝；三 Profile CTest 与 Sanitizer 全绿。 | 已完成（Codec/软件测试）；Node 固定域配置和控制帧压缩仍归 V5-02/V5-03。 |
| V5-02 | Node 固定域档位 | 为 Node 配置固定本地域 Wire Profile 与允许接收上限；发送、转发、HELLO/准入和编译期容量均按档位失败关闭，不引入动态内存。 | W0～W3 双节点/多跳模拟、越界 Node/Network/Session/Hop 拒绝、W0 广播保留值和静态资源报告。 | 代码/软件测试完成：显式 Node 域 API、默认 W3、TX/RX/MTU/Session/Hop/Route Epoch 门禁与 W0 三节点 AODV 已通过；目标 MCU 资源仍归 V5-07/S06。 |
| V5-03 | 控制帧压缩 | 删除 HELLO 中重复 Source，并评估/删除 RREQ 中重复 Origin；对控制帧 Payload 建立独立版本与长度校验。 | HELLO/RREQ 正反向向量、跨档路由发现、坏长度/旧格式拒绝、控制开销对比。 | 已完成历史阶段：HELLO 不再重复 Source，V5-10 后以 1 B 发布 RX Ceiling；RREQ 当时为 `AddrWidth+8` B。累计 Cost 与当前 RREQ/RREP 格式已由 V5-14 继续升级。 |
| V5-04 | 安全绑定 | 将 Wire Profile 和规范化地址域纳入 CRC/AAD；冻结“逐跳可读外层 + 端到端规范内层”边界，禁止 Gateway 改写受认证字段后复用旧 Tag。 | Profile/地址/长度篡改失败、透明密文中继不解密、Provider 失败关闭；真实 AEAD 仍归 S02。 | 代码/软件测试完成：W0 透明密文三节点、Profile/地址/长度旧 Tag 失败和密文篡改失败通过；生产 AEAD/身份/密钥仍归 S02。 |
| V5-05 | 路由感知自动选档 | 根据目标地址、路径能力、MTU、Hop 和 Security 要求选择满足条件的最小档位；保留固定档位和产品策略覆盖。 | 多 Bearer/多路径能力传播、无共同档位拒绝、路径切换时不静默截断、负载与帧效率模拟。 | 代码/软件测试完成：默认固定、显式开启自动最小档、HELLO/Link Ceiling、Route Hop、MTU/Tag/Path 字段和无共同档失败关闭已覆盖。 |
| V5-06 | Extended Gateway | 独立设计跨域目录、短地址 Alias/租约、Session Slot、外层/内层 E2E 信封和大规模域互联；不得混入第一阶段数字截断实现。 | 单独 RFC/威胁模型/容量预算/迁移测试后再决定是否实现。 | 设计评审完成：批准未来独立 `ucn_gateway_ext`；因依赖 S02 且资源/故障面显著，不进入当前小 Core。 |
| V5-07 | 发布门禁 | 更新架构、调用树、快速手册、知识库和资源报告；完成 Host 软件回归与后续多板/多介质实机表。 | 软件证据与硬件证据分栏；未完成 S02/S06/S07 前不宣称生产安全或实机性能。 | 软件范围已完成：三 Profile、Release、WSL Sanitizer、256 Node 固定/自动对照、资源与文档/知识库已闭环；实机按 S06/S07 待执行。 |
| V5-08 | 全档接收互操作 | 冻结“Build Profile 与 Wire Profile 正交、所有 Node 使用统一 W0～W3 Decoder、发送使用最低够用档、接收默认开放至 W3”的规则；保留产品按 MTU/资源/安全主动收窄接收上限。 | Nano/Lite/Full 都必须证明 W0 TX/W3 RX Node 能接收并分发 W3 静态 Endpoint 指令；W0/W0 仍拒绝 W1～W3；同步 API 注释、设计、手册、任务和知识库，不把“可解析”误写为“具备所有高级功能”。 | 已完成：W0 TX/W3 RX Node 依次接收并分发 W0～W3 静态命令 Endpoint，三 Build Profile 共用测试；窄 RX 拒绝、Feature/MTU/Security 边界保留。 |
| V5-09 | 全局公共配置 | 新增一个公开全局配置入口，集中列出全部可调编译期默认值；编译器/产品全局定义优先，全局未定义的项继续由原头文件 `#ifndef` 默认值兜底，不删除现有分散默认值。运行期 Node ID、Network ID、密钥和板级引脚仍由产品启动配置/Flash 管理。 | 默认值一致性、代表性全局覆盖、关闭全局默认后的原文件回退、非法组合编译拒绝、Nano/Lite/Full 和 Sanitizer 回归；文档说明配置优先级与所有翻译单元必须一致的 ABI 边界。 | 已完成：`ucn_config.h` 覆盖现有分散 `#ifndef` 默认项 103/103，产品配置头/CMake 入口可部分覆盖；默认/回退/覆盖 `3/3`、Full 产品头构建、当前 33/46/64 B、255 Hop 拒绝和 Full ASan+UBSan 通过。 |
| V5-10 | 单档与混档极限模拟 | 扩展 Host 规模模拟器，使每个 Node 可固定为 W0/W1/W2/W3，并支持同网混合档位；所有节点保持 W3 RX，分别验证单档上限、混档互操作、地址/Hop 门禁、高负载与故障。逐节点 CSV 增加实际 Wire Profile，混档额外输出逐档汇总，不能把 Build Profile 与 Wire Profile 混为一谈。 | W0 单档和包含 W0 的四档 mixed 均在单域 254 Node 真实地址上限测试，并显式拒绝 255；W1/W2/W3 在 Harness 4096 Node 上限测试。至少覆盖 local 高负载、two-hop 动态路由、丢包/重复/延迟/断链，检查非法返回、重复业务、Route Loop、事件堆背压、固定表/队列峰值、逐档交付率/线效率/Host 工作量。Lite/Full Smoke、Release 和 Full Sanitizer 通过；跨 W0 域扩展仍归 V5-06，真实 MCU CPU/RAM/介质容量仍归 S06/S07。 | 已完成：19/19 Release 场景和 Full ASan+UBSan 12/12 通过；本地高负载 100%，大规模 two-hop 暴露默认固定表工作集边界；HELLO 已分离 TX 档与 RX Ceiling。 |
| V5-11 | 审计硬门禁 | 增加 Protocol Version 6 bit 编译断言；Path 本地/远端安装在写表前校验当前 Wire 域可表达性；RREP 旧格式先补 Header/Payload 一致性失败关闭。 | Version=64 负向构建；W0 Path ID 256、越界目标/下一跳拒绝且不污染表；RREP Origin 与 Header Destination 不一致拒绝。 | 已完成（Full 9/9；Version=64 预期编译失败；本地与远端越界 Path、畸形 RREP 已覆盖）。 |
| V5-12 | 自动控制面选档 | HELLO/Heartbeat 在 Auto 模式按 Header、Hop、MTU、Peer RX Ceiling 选择最小档；路由发现为未知目标选择可表达完整搜索域的最小档并保持转发 Profile；RREP 继承请求域。其余含 Profile-sized Payload 的控制帧留在 V5-15 显式迁移。 | W3 TX Maximum + Auto 到 W0 RX，HELLO→RREQ→RREP→Data 全链通过；固定模式不变、无共同档失败关闭。 | 已完成（Full 9/9；W3 Auto→W0 两跳全链、固定四档和 W1 请求遇 W0 Peer Ceiling 失败关闭均已覆盖）。 |
| V5-13 | per-Link 本地 RX Ceiling | Link 可选择继承 Node RX 上限或显式收窄；注册和 Ingress 取 Node/Link/MTU共同上限，`peer_wire_profile` 继续只表示对端 RX Ceiling。 | 同 Node 的 W3/W0 两 Link 分别接受/拒绝对应帧；旧 Link 零配置兼容；小 MTU只在可承载声明档时注册。 | 已完成（Full 9/9、Nano 1/1；W0/W3 双 Link、HELLO 广告与窄 MTU 注册已覆盖，Host `ucn_link_t` 仍为 40 B）。 |
| V5-14 | 长距离 Cost 与 RREQ/RREP | 累计 Route/Candidate Cost 升级为 32 bit；历史阶段采用 1/2/3/4 B Cost，V5-23 已进一步修正为当前 3/3/3/4 B；Request ID 保持固定 4 B；RREP 删除重复 Origin/Target，并按 Profile 编码 Cost 与 Epoch。 | 四档边界/坏长度、Cost 溢出失败关闭、100～200 Hop Host 基线、双路径高 Cost 可比较、AODV 与 Candidate 全回归。 | 已完成：累计 Cost 为 `uint32_t`；V5-23 后各档合法 Hop×单跳最大 Cost 均可表示，旧 W0/W1 长度失败关闭；80,000 对 100,000 双路径和 200 边链路均已覆盖。 |
| V5-15 | 其余控制 Payload Codec | 逐类评估并迁移 RERR、PATH_INSTALL/REVOKE、Path Trace、Node Snapshot 等载荷中的 Node/Path 字段，统一复用 Profile 宽度读写；每类保留旧格式拒绝和资源报告。 | 四档 Golden/坏长度/越界、诊断截断、Path 安全授权、最小 MTU 和调用树同步。 | 已完成：四类控制面均按收到/选择的 Wire Profile 编解码；四档长度、坏长度、精确 MTU、W3 Auto→W0 与授权顺序已覆盖。Candidate Probe/Activate 与 Policy Diagnostic 的独立固定语义 Schema 经评估保持不变。 |
| V5-16 | Authorized Class 与预算设计 | Wire Profile 继续只表示编码；在认证身份/Neighbor 授权状态上定义最大允许 Class、控制预算和 Fanout。任何权限不得由帧自报 Profile 提升。 | 威胁模型、默认拒绝、身份绑定、降级/升级、重启/轮换和表满；依赖 S02 生产身份/密钥。 | 设计已冻结：定义 C0～C3、身份/Session Generation 绑定、ACL/预算/Fanout 与升降级流程；因 S02 尚未提供生产身份与撤销基础，本轮不写伪安全实现。 |
| V5-17 | 路由可用性与延迟门禁 | 区分 Wire 可表达与业务可用；增加 Node/业务 `max_hops`、`max_route_cost`、可选已验证 RTT 门禁。候选切换、负载均衡和指定 Path 只能使用满足当前业务约束的路线；保持默认 16 Hop 产品上限，不把 W2/W3 的 64/254 当默认搜索范围。 | Hop/Cost/RTT 边界、默认继承和非法组合；低 Cost 超 Hop、低 Hop 超 Cost、实时业务拒绝未验证路线；200 Edge 只作协议极限而被默认产品策略拒绝。 | 代码/软件测试完成：新增 Node 默认与每 Policy 约束、路由质量查询、直接/动态/指定 Path 门禁；Candidate Probe ACK 生成端到端 RTT EWMA，未知有限 Cost 和必需但未验证 RTT 均失败关闭。Nano 对高级路由 API 返回 `UCN_ERR_CONFIG`。真实 RTT/Cost 标定归 S06。 |
| V5-18 | Pinned Path Remaining Hops | 为 Path 配置/表项和 PATH_INSTALL 增加 `remaining_hops`；源节点使用真实 Path Hop Limit，中继校验并递减，目标只接受终端状态。PATH_INSTALL 四档长度更新为 8/11/14/17 B。 | 四档新格式/坏长度/边界、旧格式拒绝；A→B→C 的 2→1→0 状态、错误 Scope 拒绝、W3 地址但 2 Hop 仍保持 Hop=2。 | 代码/软件测试完成：公开安装 API、固定 Path 表与 Wire Payload 均携带 Remaining Hops；旧长度拒绝，源/中继/终端逐跳范围校验并精确递减。Node Storage Layout 版本由 2 升至 3。 |
| V5-19 | 有界 Expanding Ring | Auto 寻路按 2→4→8→16 的产品半径逐级扩大，每轮再选择可表达当前字段的最小 Wire Profile；Profile 与 Hop 解耦。每轮新 Request ID，受 Token/Fanout/固定表和 Node 最大 Hop 约束，固定档模式不变。 | Ring 状态/超时/升级/耗尽、迟到 RREP、Token 和旧 Pending；W0 窄链局部成功、5 Hop 升级、16 Hop 截止、重复/丢包/断链/高负载。 | 代码/软件测试完成：每 Ring 250 ms、总预算 1000 ms，每轮新 Request ID/重新选档；完整 2→4→8→16 和 W0 2→4 截止均已回归。自动发送不再反复重启活动发现，修复规模模拟中的 Pending 饥饿。 |
| V5-20 | Ingress Profile 早期预过滤 | 新增 3 B Magic/Version/Profile Peek；在完整 Decode/CRC 前按 Node/Link 本地 RX Ceiling 早拒绝，Full/Lite/Nano 保持同一错误语义。Peek 不承担认证。 | 短帧、坏 Magic/Version/Profile、超 Ceiling、允许档坏 CRC 的错误优先级；W0/W3 Link 独立注入、早拒绝工作量对照、Sanitizer。 | 代码/软件测试完成：公开 3 B Prefix Peek，Full/Lite/Nano 均先解析 Link 本地 Ceiling 再完整 Decode；窄 W0 Link 上损坏 W3 帧返回 `UCN_ERR_UNSUPPORTED`，宽 W3 Link 对同帧返回 `UCN_ERR_CRC`。只减少无效解码 CPU，不代表介质接收免费。 |
| V5-21 | Authorized Class 执行层 | 在 S02 已认证 Principal/Session Generation 上实现 C0～C3、消息 ACL、按身份/控制类型 Token、Fanout 和撤销清理；不得从 Wire Profile、Node ID 或 Bearer 自报值提权。 | 未认证、W3 伪报、错误 Key、旧/吊销 Session、轮换/重启/表满失败关闭；C0/C2 跨档请求证明 Class 与 Wire 正交。 | 阻塞于 S02；在生产身份、逐跳认证、密钥/Session Generation 和持久 Replay 完成前不得开始伪实现。 |
| V5-22 | Pending Q1 绝对 Deadline | 区分应用新 Latest 与 Protocol Task 内部重试；内部重试保持原 Deadline、禁止重新排队，成功/过期/永久错误后才清槽。 | 路线持续不合格超过超时不续命；新值覆盖刷新；恢复只发最新值一次；临时背压不提前清槽。 | 已完成：内部发送入口禁止 Retry 重新排队；新增覆盖/重试/成功/永久失败统计，专项测试证明旧值不续命且只发送最新值。 |
| V5-23 | 累计 Cost Wire Domain | 将 RREQ/RREP Cost Width 固定为 W0/W1/W2/W3=`3/3/3/4 B`，覆盖合法单跳 Cost 在各档最大 Hop 下的累计值；普通业务 Header 不变。 | W0 Cost=300、W0 四跳/W1 十六跳边界、Known/Unknown/越界、四档新长度与旧长度拒绝；最小 MTU与资源重算。 | 已完成：RREQ=`10/11/12/14 B`、RREP=`10/11/11/12 B`；最大合法累计值、旧 W0/W1 格式拒绝和 Full/Lite 动态寻路回归通过。 |
| V5-24 | Candidate Profile 连续性 | Candidate 保存发现 Profile；Probe/Activate 使用 Candidate Profile，ACK 继承并核对请求 Profile，Epoch 按 Candidate 域分配。 | W3 Auto→W0-only 两跳完整 Candidate 切换；混 Profile 拒绝；W0 Epoch 回绕不产生 0；固定模式回归。 | 已完成：Storage Layout Version=4；W3 Auto→W0-only Probe→ACK→Activate→ACK→Data、混 Profile ACK 拒绝和 W0 Epoch 回绕通过。 |
| V5-25 | 产品 Hop Scope 硬门禁 | Full/Lite/Nano 在 Network 后、Security/状态前按运行期 Hop 上限拒绝；RREQ 原始 Ring、Route/Candidate 学习和 PATH_INSTALL 同步门禁。 | W3 Hop=64 注入 16 Hop Node 不改变状态；Config 8 收窄；RREQ/RREP/Candidate/Path/Nano 正负场景。 | 已完成：统一使用 `default_hop_limit<=UCN_MAX_HOPS`；W3 Hop=64 Data/RREQ、原始 Ring Scope、Route/Candidate 学习和 Path Remaining Hops 均失败关闭并有独立统计。 |
| V5-26 | 交叉测试与全量回归 | 将 V5-22～V5-25 的跨 Feature 场景纳入正式 CTest，更新 Wire Golden/最小 MTU/Scale/资源，并运行 Debug/Release/Sanitizer。 | Full/Lite/Nano、配置门禁、单档/混档、端到端 Scale、WSL ASan+UBSan 全绿；软件与实机证据分栏。 | 已完成：Windows Full/Lite Debug/Release/Service OFF 均 10/10，Nano 均 1/1；WSL Full ASan+UBSan+配置契约 13/13；Host 资源已重测，未访问硬件。 |
| V5-27 | 动态 MTU 公共契约 | 统一 Full/Nano 有效 MTU：静态/运行时同时存在取最小值，任一为零时使用另一方，两者均为零时 Link 暂不可发送；允许动态 MTU Link 注册，所有发送与自动选档只使用统一有效值。 | Full/Nano 覆盖静态、仅运行时、两者同时、两者均零、运行期缩小/恢复；Profile 选择与实际发送结果一致。 | 已完成：公共 `ucn_link_effective_mtu()` 成为唯一语义；动态 Link 可注册，零有效 MTU 返回 `UCN_ERR_LINK_DOWN`，恢复后无需重注册。 |
| V5-28 | Path/Bearer 能力与确定性失效 | 对逻辑邻居的合格 Bearer 汇总共同 Profile/MTU；Path 保存端到端可承载能力，业务封装不只看首跳。任何中继 `UNSUPPORTED/TOO_LARGE` 必须撤销 Path、返回 Path RERR 和诊断计数，禁止静默保留黑洞。 | 主 W3/备 W0、主备 MTU 不同、首跳宽/后跳窄；源与中继、明文与 E2E 场景要么无缝送达，要么源端明确失效并可重建。 | 已完成：Path 能力进入固定表和扩展 11/14/17/20 B PATH_INSTALL；V5-31 后旧 API 继续发送基础 8/11/14/17 B。本地 Bearer 求交，控制器可传端到端瓶颈；后续跳不匹配会撤销并回送 Path-RERR。 |
| V5-29 | Policy 逻辑 Bearer 活性与质量 | Node 层解析 Policy Path 的当前健康 Bearer；任一候选 Bearer 可用时保持 `VERIFIED`，全部不可用才 `DOWN`；负载均衡使用当前 Bearer 质量，Wire Path 仍存在时定义恢复迁移。 | `PINNED_STRICT` 跨质量刷新后继续走备链；全断才 DOWN；恢复与 Path 已撤销边界；AUTO_BALANCE 不读取失效主链指标。 | 已完成：周期采样与 AUTO_BALANCE 均解析当前 Bearer；健康备链可保持/恢复 `VERIFIED`，Wire Path 缺失或全 Bearer 不可用才置 `DOWN`。 |
| V5-30 | 审计专项与发布回归 | 将 V5-27～V5-29 的跨 Feature 场景加入正式测试，反向核对审计→任务→代码→测试；同步 API、架构、Adapter 契约、调用树、操作记录与知识库。 | Windows Full/Lite/Nano Debug/Release、配置契约、WSL ASan+UBSan 和 `git diff --check`；实机边界继续归 S06/S07。 | 已完成（软件）：Windows 六矩阵全绿；WSL Full ASan+UBSan/配置契约 13/13、GCC `-fanalyzer` 13/13；动态 MTU、异构主备、后续跳 RERR、透明中继和 Policy 恢复有专项用例。 |
| V5-31 | PATH_INSTALL v5 Schema 兼容 | 保留协议版本 5；旧 `ucn_node_send_path_install()` 发送基础 `{Path,Destination,NextHop,Lease,RemainingHops}`，新 capability API 才发送扩展 `{MaximumWireProfile,MinimumMTU}`；新接收端按精确合法长度双格式解码。 | W0～W3 基础 `8/11/14/17 B` 与扩展 `11/14/17/20 B` 均接收；基础格式能力为空并本跳推导；坏中间长度拒绝；旧 API 不向旧 v5 节点发送扩展字段。 | 已完成：旧 API 与扩展 API 分离发送；接收端只接受两组精确长度，基础格式继续派生本跳 Bearer 能力。旧 v5 节点可接收基础格式，但不能接收调用方主动选择的扩展格式。 |
| V5-32 | 非 Full capability API 符号完整性 | 为 Lite/Nano 补齐 `ucn_node_install_local_path_capable()`、`ucn_node_send_path_install_capable()` Stub，保持 NULL Node=`ARGUMENT`、有效 Node=`CONFIG`。 | Full/Lite/Nano 测试二进制直接引用两个新 API；静态库符号表三档均导出；公共头链接测试防止新增 API 只声明不实现。 | 已完成：两个 Stub 均返回既定错误；公共头与非 Full 运行测试直接调用，三档 `libucn_core.a` 均核实导出符号。 |
| V5-33 | 发布阻塞回归与同步 | 执行三 Profile Debug/Release/Service OFF、配置契约、旧/新 Path 控制兼容、Sanitizer、`-fanalyzer`；同步任务、报告、调用树、操作记录与知识库。 | 所有门禁通过；明确 v5 基础/扩展格式边界、旧节点能力限制和实机未验证项；`git diff --check`、项目链接和知识库内链通过。 | 已完成（软件）：Windows Debug/Release+Service OFF 的 Full/Lite 均 10/10、Nano 均 1/1；配置契约、WSL ASan+UBSan、GCC `-fanalyzer` 均 13/13；项目与知识库静态检查通过。 |
| V5-34 | 标准 Port/Adapter 与默认 Cost 基线 | 冻结裸机/FreeRTOS/Zephyr/NuttX 的 Port 边界、UART/RS-485/CAN/CAN-FD/Wi-Fi/USB CDC 的标准 Adapter 配置、基础 Cost Preset 和动态有效选择分合同。 | 对照当前 `route_cost` 基础语义、RTT/失败/队列独立指标、现有 500 ms/25% EWMA、滞回/Probe 与固定资源约束；明确经典 CAN Carrier、USB 点对点、Wi-Fi 实际吞吐和实机标定边界。 | 已完成（设计文档）；不改 Core/驱动，不把初始 Cost 表宣称为目标板标定值。 |
| V5-35 | 标准配置类型与 Preset Resolver | 新增不依赖 MCU SDK 的 `ucn_standard_adapter.h`，提供 Bearer/Preset、共同 Link 配置、产品覆盖和逻辑 MTU Resolver；冻结 UART/RS-485、CAN/CAN-FD、ESP-NOW/Wi-Fi、USB CDC 及 LC-1 扩展 Bearer 的静态元数据。 | 逐 Preset 数值/速率/MTU/RTT、非法/未知 Preset、地址/MTU/偏置/Carrier 拒绝、覆盖优先级；Full/Lite/Nano 构建与产品全局配置链接测试；无动态内存。 | 已完成（软件）：`ucn_standard_preset_resolve()`/`ucn_standard_link_config_resolve()` 不含 SDK、驱动或 Node 常驻表；MSVC Full 配置契约 4/4、Lite 1/1、Nano 1/1，128 B 产品配置专用测试 1/1，WSL GCC Full 配置契约 4/4 均通过。扩展 Preset 仅为 `CONDITIONAL` 元数据，不等于已有 Adapter。 |
| V5-36 | 动态有效选择分接入 | 在 V5-44 提供的 LC-1 Resolver 基础上，由 Full Core 将 `effective_select_cost` 接入 Bearer/Candidate/Q1 Flow 的本节点排序；Lite/Nano 保持静态基础 Cost与既有表边界；线上 `route_cost`/RREQ/RREP Cost 不变。 | LC-1 的状态门、饱和、Unknown、20% 滞回、3 样本、2 次 Probe ACK、3000 ms 保持期、Q0/Pinned/AUTO_BALANCE 回归及各 Profile 资源差异。 | 已完成（Host 软件）：Full 的同 Neighbor Bearer、Candidate 本地出口分和 Q1 Flow 加权分均使用 LC-1；软切换需 20%/3 样本/2 ACK/3000 ms，硬失效立即排除，Q0/Pinned 与 Flow 租约语义不变。Lite/Nano 不增加动态质量表。 |
| V5-37 | 四类标准 Port 外壳 | 建立裸机、FreeRTOS、Zephyr、NuttX 的 Protocol Owner、RX 通知、静态队列/对象和 Host Fake HAL 编译模板。 | 不同 Port 的单一 Node Owner、step_at 同一时钟、ISR 有界入队、Service 可选编译与无实际驱动依赖。 | 已完成（历史软件阶段）：其集中式 mode API 未发布，已由 V5-46 的分目录独立 Port 架构替代；DOC-042/043 保留为阶段记录。 |
| V5-46 | 平台 Port 分目录解耦 | 删除集中式运行模型枚举，建立 SDK 无关 Protocol Owner 公共契约，以及裸机、FreeRTOS、Zephyr、NuttX、RT-Thread、Host Fake 各自独立的头、源和 API。 | 每种 Port 都可单独初始化、RX 入队、通知/等待、Step 与查询统计；RT-Thread 不依附其它 RTOS；公共 Owner 不出现平台枚举/SDK 类型；Full/Lite/Nano/Service OFF/产品头/GCC 回归。 | 已完成（软件）：include/ucn/ports 与 src/ports 已交付；六 Port API 及公共 Owner 均由 test_protocol_owner.c 和公共头链接测试覆盖。真实 SDK 对象、驱动、实机仍待 V5-38～V5-42。 |
| V5-47 | 工程架构治理与内部源码分层 | 将仓库明确分为 Foundation/Core、Node、Transport、Routing、Service、Ports 六个内部实现模块；新增系统边界、代码依赖、迁移兼容和构建测试地图。 | 公开 `include/ucn/...` 路径和公共 API 不变；CMake 按模块列出源；Core 不依赖 Port/SDK/OS，Port 只经公共 API 依赖 Core；现有测试按逻辑分类而不强行物理迁移；Full/Lite/Nano/Service OFF/产品头/GCC 回归及目录/调用树检查通过。 | 已完成（软件/工程结构）：内部源码已迁移到六个目录，CMake、README、调用树、测试地图和知识库已同步；Windows Full 4/4、Lite 1/1、Nano 1/1、Service OFF 1/1、128 B 产品配置 2/2、WSL GCC 4/4 均通过，Core-only Lite 未生成 Platform Port。 |
| V5-48 | 审计复审：ISR Queue 合同、Scale 容量门禁与 Path 配置兼容 | 修复 V5-REAUD-02/03/01：Task/ISR RX Queue 使用独立且可恢复 token 的临界区合同；低 Link 容量 Scale Smoke 自动选择可满足的拓扑；保持 `ucn_path_forward_config_t` 精确历史八字段，把 capability 移入独立扩展 API。 | Host Fake 可观察 ISR 锁/token 而非任务锁；无 ISR 锁配置失败关闭；128 B/3-Link 产品头默认 CTest 不因无效 tree 失败，并明确实际拓扑；旧式八字段位置初始化在严格 GCC 编译且语义正确。 | Full/Lite/Nano/Service OFF/128 B 产品头 CTest，Host Scale 的默认/显式不支持拓扑，严格 WSL GCC；不把 Host Fake 推定为真实 RTOS ISR。 | 已完成（Host 软件）：Task/ISR Queue 双入口、五个 Port ISR 分流、自动 line/tree 和容量合同、八字段结构/API 扩展均已实现；MSVC Full Scale 14/14、产品头 15/15、WSL GCC 严格 14/14。真实 SDK/BSP/ISR 与实机仍待 V5-38～V5-42。 |
| V5-38 | ESP32-S3 v5 UART/ESP-NOW 标准 Adapter | 将外部 ESP32 测试工程迁移到当前 v5 分层源码，接入 UART 与 ESP-NOW Adapter、默认 Cost、质量计数和三节点测试；A/B 为 S3、C 为 WROOM。 | v5 三目标构建/Hash 烧录、双 Bearer 同 Neighbor、默认/动态 Cost、队列/失败统计、实际 UART/Wi-Fi 收发和切换日志。 | 首轮实机完成：A/C 各 2 Link、B 4 Link；正常 A↔C=`2 hops/cost 68`，A—B UART 断开回退 Wi-Fi+UART/cost 79，B 长离网后 Link 清零并可重新准入。长期压力、功耗和产品化标准 Adapter 仍待后续。 |
| V5-49 | 三节点实测正确性修复与报告 | 修复 Adapter Metrics 采样时间戳晚于 Node 本轮时间造成的 `METRICS_STALE`；RREQ Origin 在首次 Flood 前写入去重缓存，阻止多 Bearer 回流形成自路由；归档三板正常、热断/恢复和节点离网证据。 | RREQ Origin Loop 专项回归、Full CTest `14/14`、三目标构建/烧录；实机不得出现自路由、Metrics Stale 或 Route Loop。 | 已完成首轮：上述两项缺陷未再出现，详细数据见 [V5-49 三节点实测报告](UCN_V5_49_ESP32三节点多Bearer实测报告.md)。 |
| V5-50 | 实机故障快速收敛与 UART 空闲硬化 | 当 Route 的下一跳 Bearer 已撤销准入或硬失败时，立即使该 Route 不可发送并触发有界 RERR/重发现；同 Neighbor 的已验证 Backup 可直接接管，不等待旧 Route 缓存刷新。ESP32 UART Adapter 对启用 RX 配置空闲上拉，对未接端口允许显式关闭并限制噪声错误预算。 | 单元/虚拟：Route 不得继续引用未准入 Bearer，Pinned/Q0/Q1 语义不退化；实机：重复热断/恢复至少 10 轮，统计切换时延、连续丢帧、乱序、Route Cost 刷新和悬空 RX 错误。 | 已完成当前门禁：软件矩阵全绿，三板最终同源并完成 10 轮热断/恢复；每轮均 `68→79→68`，Route/Primary 同快照同步，发送拒绝 0、重复/乱序 0，悬空 RX 无错误风暴。9 个逐序号轮次 A→C 有 26 个缺口，说明 2.5 s 物理静默检测窗仍非零丢帧；后续转 V5-51，不宣称无缝切换。详见 [V5-50 报告](UCN_V5_50_快速故障收敛与UART空闲硬化报告.md)。 |
| V5-51 | 物理静默检测窗与关键流丢失控制 | 复用现有每 Bearer Heartbeat，不新增 UART 私有 Probe Wire；增加零初始化兼容的 Link 存活档位，UART FAST=`250/1250/2000 ms`，Wi-Fi 保持 DEFAULT=`1000/3000/4000 ms`。硬件 DCD/CTS/GPIO 继续允许 Adapter 更早报告 Down。Suspect 双发因不能覆盖判疑前窗口而不作为本轮默认修复。 | Host/虚拟覆盖混合档、静默、延迟恢复、通用控制 Token 耗尽、回绕、非法档位和固定资源；实机 10 轮保存动作/最后有效 RX/Primary/Route 毫秒事件、逐序号缺口、重复/乱序、假阳性、控制开销和资源。 | 已完成当前软件与三板故障功能门禁。软件矩阵保持 Full+配置 `14/14`、Lite `11/11`、Nano `1/1`、Service OFF `11/11`、128 B/3-Link `12/12`，WSL Sanitizer/Analyzer 各 `14/14`。三板 10/10 轮 UART→Wi-Fi→UART；A/B 各 10 次 SUSPECT/DOWN，无额外假阳性；9/10 轮 Route 事件 0～16 ms，第 10 轮约 1 s；A/C 总缺序 11/13，最大连续缺口 1，重复/倒序/提交拒绝/UART 错误/重启均为 0，Heap 稳定、Heartbeat 最大延迟 142 ms。CPU% 未装仪表，保留给目标资源标定；仍不承诺可靠交付或零丢帧。见 [方案](UCN_V5_51_物理静默检测窗与关键流保护方案.md)与[十轮实测](UCN_V5_51_三节点十轮实测报告.md)。 |
| V5-52 | ESP32-S3-N8R8 三节点九档 Transfer 实测 | 外部 PlatformIO Bench 增加 N8R8/COM38 目标与可选 Transfer 源；A/COM5 发送，B/COM34 只做 Core 透明中继，C/COM38 重组；T32～T8K 使用确定性 Payload 和完整 CRC32 校验。 | 三目标构建/完整烧录/Hash；同一日志从 BOOT 记录到 COMPLETE。验收 9/9 等级、两跳 UART Cost 68、85 Fragment/ACK、无重试/失败/CRC 错/Queue Drop，并记录耗时、Heap/栈与未覆盖边界。 | 当前 UART 两跳功能门禁已完成：32/64 B 直接到达，128 B～8 KiB 分别 `1/2/3/6/11/21/41` 片；9/9 `valid=1`，总 85 片、0 重试、0 失败，C 重组 7 次，8 KiB 确认 2528 ms。ESP-NOW-only、切换中 Transfer、并发/Q0 干扰、CAN-FD、CPU/功耗仍待 EXT-07。见 [实测报告](UCN_V5_52_N8R8三节点Transfer实测报告.md)。 |
| V5-53 | ESP32-S3 三节点九档等载荷压力 | 外部 Bench 每档固定发送 8 KiB，从 `T32×256` 到 `T8K×1`；C 校验消息序号/长度/Payload，A 通过可重发的统计请求以远端交付量计算端到端有效吞吐。 | 三目标构建/显式端口烧录/Hash；两轮正式日志与 18 行 CSV。T128～T8K 要求全收、CRC/无效/重复为 0；Direct 小包必须如实记录 Queue Drop 和完成率。 | 当前 115200 UART 两跳已完成：T128～T8K 两轮 `14/14` 阶段全收，可靠吞吐 `2.48～3.17 KiB/s`，T4K 平均约 `3.13 KiB/s`、比 T128 提升约 21.8%；T32/T64 峰值更高但无端到端确认，压力完成率不稳定。见 [压力报告](UCN_V5_53_三节点Transfer压力测试报告.md)。 |
| V5-54 | 921600 UART 三节点九档压力对比 | 外部 Bench 新增独立 921600 A/B/C 环境，默认 115200 不变；Link Cost、两跳拓扑和每档 8 KiB 规则不变，三轮比较可靠交付、耗时、吞吐、重传、Queue 与后台 Ping。 | 三目标构建、显式端口完整烧录/Hash；三轮日志、27 行汇总和 115200/921600 对比 CSV。T128～T8K 要求全收且无 Wire/CRC 错；背景业务退化必须单列。 | 已完成：T128～T8K 三轮 `21/21`，平均 `2.825→10.296 KiB/s`、3.63×，耗时下降 72.3%，T8K 11.86 KiB/s。T32/T64 仍受 Direct Queue Drop；一轮 A/C Ping 55/70，后续须做 Transfer/Q0/Q1 并发调度门禁。见 [报告](UCN_V5_54_921600波特率三节点Transfer压力测试报告.md)。 |
| V5-55 | UART 1.5M～5M 极限扫描 | 外部 Bench 新增 `1.5M/2M/3M/4M/5M × A/B/C` 15 个环境；保持相同两跳 Cost 68、每档 8 KiB 和后台控制流。3M～5M 各三轮，用于区分单次可用与重复稳定。 | 15/15 构建；逐档显式烧录/Hash；11 轮原始日志、99 行阶段 CSV、11 行逐轮和 5 行逐速率 CSV。可靠交付、重试、Wire 错、Queue、Direct、Ping、Route 必须同时记录。 | 已完成：可靠阶段 `77/77`；3M 三轮 0 重试/0 Wire 错、平均 `12.523 KiB/s`，当前短线推荐档；5M 三轮均由协议完成，但一轮出现 1 次 COBS 错及 1 次重传，只作为 SDK 功能极限。三板最终保留 3M 固件。见 [报告](UCN_V5_55_UART极限波特率扫描报告.md)。 |
| V5-56 | Transfer 有界固定窗口与吞吐闭环 | 保留默认窗口 1 和既有 8 B ACK Wire，显式配置本机/Peer 最大窗口；`next_expected_offset` 作为累计 ACK，接收端严格顺序零乱序缓存，缺口/超时执行受重试上限约束的 Go-Back-N。编译默认最大窗口 4，不复制每个在途 Fragment 的 Payload。 | 软件覆盖旧 Peer 退化窗口 1、丢 START、丢中间片、活动 TX 改窗拒绝、位置初始化/API 链接；Full/Lite/Nano、低资源产品头、ASan/UBSan、`-fanalyzer`。实机在 3M 同台架各跑窗口 1/4 三轮并记录可靠吞吐、Direct、Queue、Ping、Heap/栈和资源增量。 | 已完成：最终窗口 4 固件 T128～T8K 三轮 `21/21`、0 重试/0 Wire 错，可靠等级平均 `12.523→19.121 KiB/s`（+52.7%）；T8K `14.041→24.750 KiB/s`，耗时下降 43.3%。Transfer 对象 +28 B，A/C 静态 RAM +32 B；Wire/协议版本不变。三板当前保留 3M/窗口 4 固件。见 [报告](UCN_V5_56_固定窗口Transfer优化与三节点对比报告.md)。 |
| V5-57 | 外设事件驱动 Owner 与窗口 8 | UART ISR/框架 RX Task、ESP-NOW RX/TX completion 只向唯一 Protocol Owner 发通知；Owner 被立即唤醒后按固定轮数/时间预算批量 Pump/Step，轮询仅保留 10 ms 最大维护/漏通知/无中断平台兜底。Transfer 编译最大窗口允许 8，运行默认仍为 1，Peer 未声明仍退化到 1。 | 框架中断/回调上下文核对；三目标构建/完整写入；3M UART 两跳九档三轮；记录事件/定时/兜底唤醒、Queue/Wire/Transfer、吞吐和同步日志干扰；Full/Lite/Nano、低资源产品头、ASan/UBSan、`-fanalyzer`。 | 已完成当前门禁：最终事件固件 27/27 阶段通过、0 Transfer 重试、B UART Queue Drop 0；九档等权平均 `15.712→17.283 KiB/s`（较窗口 4 +10.0%），T8K `24.750→29.480 KiB/s`。T512 因三板 `V5STAT` 同步阻塞调试串口产生相位干扰，仍 16/16 全收。三板当前运行 3M/事件 Owner/窗口 8；其他 RTOS/介质/Q0/CPU/功耗仍待。见 [报告](UCN_V5_57_事件驱动Owner与窗口8三节点报告.md)。 |
| V5-58 | 标准事件 Runtime 与多 Bearer Source | 在主库增加 SDK 无关固定 Source 注册、Task/ISR 事件通知、事件合并、Carrier Drain、完整帧直接 Submit、唯一 Owner 预算运行、等待裁剪和 FALLBACK Scan；裸机把 Owner 当函数，RTOS 只映射 notify/wait/yield。现有 Port API 保持兼容，Wire 不变。 | 两 Source 同时/并发通知、Task/ISR、完整帧、Queue 满、Source/Round Budget、Yield、Wait/Fallback、裸机无 Hook和错误门禁；Full/Lite/Nano、产品头、Sanitizer/Analyzer。 | 已完成 Host 软件门禁：新增 `ucn_event_runtime_t`/固定 Source API、`test_event_runtime` 和跨 Profile 公共头/配置契约；Windows Full/Lite/Nano、产品头、Full Service OFF、WSL ASan/UBSan 与 `-fanalyzer` 全通过。默认 8 Source 的 Host x64 对象 Full/Lite 为 432 B、Nano 为 408 B；3 Source 产品配置为 304 B（均不含 Node、公共 RX Queue和各 Driver Ring）。真实 UART/CAN/USB Ring、各 RTOS SDK 与目标板实机分别转 V5-59～61。详见 [V5-58 方案/实现](UCN_V5_58_标准事件Runtime与多Bearer接入方案.md)。 |
| V5-59 | 标准 Carrier Ring 与 UART/USB Stream Source | 提供可选调用者存储 Byte Ring/Carrier Decoder Source 模板；UART、RS-485、USB CDC 只配置驱动/DMA、引脚/端点、MTU和默认 Cost。 | Wrap/满/拆包/粘包/坏帧/多实例/ISR/DMA、背压、流控和不同 MTU；Host Fake 后再做目标驱动。 | 已完成 Host 软件门禁：公共 `ucn_stream_source_t`/COBS+0 TX、调用者 Ring/Frame Storage、Task/ISR 整块写入、有序缺口重同步、三重预算、Queue 背压保留与统计均已实现；Windows Full 14/14、Lite 11/11、Nano 1/1、Service OFF 11/11、128 B 产品头 5/5、WSL Sanitizer/Analyzer 通过。默认/128 B 产品便利 Storage 为 771/258 B，Host x64 Source 对象 240 B。真实 DMA/USB/RS-485/RTOS/流控/资源仍归 V5-61。详见 [V5-59 方案/实现](UCN_V5_59_标准Stream_Carrier与UART_USB_Source方案.md)。 |
| V5-60 | CAN/CAN-FD Frame Source 与经典 CAN Carrier | 提供固定 Frame Ring；CAN-FD 和经典 CAN 分别完成直接/Carrier 分段重组、Bus-Off/恢复、过滤和 Cost 指标。 | 多控制器、乱序/丢段/槽满/超时/Bus-Off、64 B MTU与 8 B Carrier；Host 虚拟总线后实机。 | 已完成 Host 软件门禁：公共 `ucn_can_source_t`、CAN ID→Link Resolver、CAN-FD 合法 DLC 零填充/真实长度探测、经典 CAN `C1/C2` 8 B Carrier、固定重组槽/回绕安全超时、Bus-Off/显式恢复和 Health 指标均已实现。Windows Full 11/11、Lite 11/11、Nano 1/1、Service OFF 11/11、128 B/4 Ring/1 Slot 产品头 5/5、WSL ASan/UBSan 与 `-fanalyzer` 1/1 通过。默认/产品 Host x64 Convenience Storage=`1184/464 B`，Source 对象=`256 B`。真实 CAN 控制器、仲裁、过滤、Bus-Off、Cost 和 Q0/Q1 时延仍归 V5-61/V5-41。详见 [V5-60 设计/实现](UCN_V5_60_CAN_CANFD_Frame_Source与经典CAN_Carrier设计.md)。 |
| V5-61 | 平台 SDK 与多中断实机门禁 | 将统一 Runtime Hook 映射到 FreeRTOS、Zephyr、NuttX、RT-Thread 和裸机 WFI；同一节点同时接 UART/CAN/USB/Wi-Fi 多 Source。 | 真实 ISR 优先级、FromISR Yield、栈/CPU/功耗、事件风暴、公平性、断链和长稳；不得以 Host Fake 替代。 | 待对应硬件/RTOS 条件。 |
| EXT-01 | Transfer 等级、资源与兼容契约 | 冻结独立 `Transfer Class`：T32/T64/T128/T256/T512/T1K/T2K/T4K/T8K；明确它与 W0～W3、Q0/Q1 正交。定义 Endpoint 最大等级、分片许可、E2E、固定 Peer Capability 和 Transfer OFF 默认零资源；预留 `0x22/0x23`，不改既有 v5 Header/DATA 语义。 | 文档/头文件设计评审；现有 v5 Golden Vector、公共 ABI、Transfer OFF 的 RAM/Flash/Wire 均不变；旧对端绝不被主动发送未知 Transfer 帧。 | 已完成（软件）：独立 `ucn_transfer` Target/Header、九档/静态 Peer/Endpoint/资源合同；Core-only 构建不生成 Transfer Archive。 |
| EXT-02 | Transfer Codec 与边界门禁 | 实现 Fragment 14 B 信封、ACK 8 B 信封、九档长度/偏移/保留位/CRC32 校验及精确错误语义。 | T32～T8K 的最小/最大/超限、格式错、CRC 错、未启用/未授权拒绝；不得分配动态内存。 | 已完成（软件）：Codec、IEEE CRC32、坏 Flag/Class/Reserved/长度和能力拒绝均进入专项测试。 |
| EXT-03 | 固定 TX 与有界可靠发送 | 实现固定 TX Slot、默认窗口 1/显式窗口 2～8、累计 ACK、有界 Go-Back-N、最多 3 次恢复轮、绝对 Deadline、Q0/维护优先与一次一步片段发送。 | ACK 丢失、首片/中间片丢失、断链、队列满、超时、回绕、持续 Q0 压力和 512 片上界；不得动态分配、无限重发或无能力声明流水发送。 | 当前软件完成：借用式 TX、旧 Peer 窗口 1 退化、丢 START/中间片恢复、最终 ACK 持续丢失后重试耗尽、最小 MTU 失败关闭；窗口 8 已做 UART 两跳三板门禁，持续 Q0 压力/有损无线仍待 EXT-06/07 补强。 |
| EXT-04 | 固定 RX 重组与任务交付 | 实现固定 Reassembly Slot、严格顺序、重复 ACK、Recent Completion、Slot Index 投递和显式 release；中继零重组。 | 乱序/重复/丢片/Slot 满/任务不 release/超时/一次执行；T8K 不复制到默认 Service Inbox。 | 首版软件完成：固定 RX Buffer、CRC 错、Slot 满/双 Slot、超时、Recent Completion、代际 Handle 和显式 release 已覆盖；中继两跳不创建 Transfer。 |
| EXT-05 | 安全、MTU、路径与旧节点边界 | 分片/ACK 复用 Provider/E2E，按完整 Path 最小 MTU 预检查，接入 Endpoint ACL、静态 Peer Class 与窗口 Capability。 | 明文降级、透明中继、Path/Bearer 切换、64/250/256 B MTU、窄路径拒绝、未声明窗口退化到 1 和旧节点隔离。 | 部分完成：静态 Peer Class/窗口、Binding 最大等级/E2E 明文拒绝、64/128/250/256 B MTU 和两跳透明中继已测；HELLO 自动能力协商、完整 Path MTU 主动协商、Endpoint 独立 TX 安全映射、切换与旧固件实机仍待继续。 |
| EXT-06 | 软件/规模/资源门禁 | 运行三 Profile、Transfer OFF、Sanitizer、固定表极限和每等级压力模拟；报告 RAM/Flash/CPU 模型。 | T32～T8K、窗口 1/4、丢首片/中间片、32/64/250/256 B MTU、重试/回收/无重复执行、`git diff --check` 全绿。 | 当前软件门禁完成：Full/Lite/Nano、T32～T8K、窗口兼容/缺口恢复、512 B/双 RX 槽/最大窗口 2 产品裁剪、Core-only、WSL ASan/UBSan 和 `-fanalyzer` 通过；更大并发规模/目标 CPU 留待继续。 |
| EXT-07 | 多 Bearer 实机验收 | 在 ESP-NOW、UART、具备条件后的 CAN-FD 上验证单跳/两跳各等级、窗口选择，以及 Q0 不受大包影响。 | 记录完成率、耗时、重传、丢失、队列、Heap、栈、CPU、功耗与 Q0 延迟；不以 Host 结果替代。 | 部分完成：当前三板已完成 115200～5M UART 两跳九档压力、3M 窗口 1/4 和事件 Owner/窗口 8；最终事件固件三轮 27/27、0 重试，九档平均较窗口 4 再提高 10.0%。Direct 小包语义未改变；ESP-NOW-only、单跳、Bearer 切换中消息、多源并发/Q0、CAN-FD、CPU/功耗/长稳仍待执行。 |
| V5-39 | CAN-FD 与 USB CDC 标准 Adapter | 实现 CAN-FD/USB CDC 配置与 Adapter，完成 Host/目标构建、MTU/状态/指标边界和点对点测试。 | CAN-FD 64 B、USB CDC FS/HS、Down/恢复、固定队列、成本 Preset 与无 Node ID 混用。 | SDK 无关公共层已分别由 V5-59 Stream Source 和 V5-60 CAN-FD Frame Source 完成；真实 USB Endpoint/CAN 控制器驱动、目标构建和点对点实测转 V5-61，不能写作整项实机完成。 |
| V5-40 | 经典 CAN 有界 Carrier | 实现经典 CAN 的 Adapter 内 Carrier 分段/重组、超时、错误/Bus-Off 和固定重组槽；不改为 Core 业务分片。 | 分段/乱序/丢段/槽满/超时/Bus-Off、125K～1M Preset、Host 虚拟总线和目标实测。 | 软件部分已并入 V5-60 完成；125K～1M 真实仲裁、Bus-Off、拥塞和 Q0/Q1 时延继续 V5-61/V5-41。 |
| V5-41 | 默认 Cost 真实标定 | 在目标板/真实拓扑采集各 Preset 的 RTT、失败率、队列压力、P50/P95、吞吐、功耗和切换结果，以产品覆盖值替换不适合现场的初始值。 | 两板、三板、多 Bearer、拥塞、断链恢复、不同速率与摆位；不以 Host 或单次无线可见替代。 | 待硬件条件。 |
| V5-42 | 扩展 Bearer 评审与实现 | 在 UART/ESP-NOW/CAN-FD/USB 基础后，按 P0/P1/P2 评审并实现以太网、RS-485、BLE LE、802.15.4、NRF24/私有 2.4G、LoRa P2P/FSK、UWB、SPI/I²C 与受控 IP Tunnel；每类须先满足 MTU、固定队列、指标归一、单一网络层责任和目标板资源门槛。 | 每个 Adapter 独立 Host Fake HAL、单元/模拟、目标构建和实机验收；原生 Wi-Fi/BLE/Zigbee/Thread Mesh 与 LoRaWAN 不与 UCN 路由层双重叠加。 | 待开始（按实际产品硬件优先级选择）。 |
| V5-43 | 冻结 LC-1 Link Cost 计算规范 | 将动态 Cost 的指标口径、1000 ms 窗口、500 ms 采样、25% 整数 EWMA、固定加减表、陈旧/Invalid/Unknown、状态门、同分、20%/3 样本/2 Probe/3000 ms 保持期、Q1 重绑和测试向量固定为单一规范。 | 与 `ucn_link.h`/Policy 当前事实交叉核对；不把尚未实现的 RX failure、介质占用/质量或 Resolver 写成现状；项目文档和知识库均链接到同一 LC-1。 | 已完成（设计/任务）；不改 Core、公共头、Adapter、驱动或 Wire 协议。 |
| V5-44 | 实现 LC-1 Metrics 与 Cost Resolver | 在不改变线上 `route_cost`/控制帧格式的前提下，扩展可选 RX failure、介质占用、介质质量、时间戳/bad-metric 诊断；实现定点 EWMA、状态门、固定 Resolver 和 LC-1 测试向量，并保持 Profile 裁剪。 | Full/Lite/Nano API/链接/资源测试；所有 C01～C10 向量、占用/质量不重复扣分、溢出、Invalid、陈旧、Unknown、硬失效、无动态内存和现有 Cost/Path 回归。 | 已完成（Host 软件）：新增 `ucn_link_cost.h/.c`、扩展 Metrics/Full Snapshot，C01～C10 与额外非法/重复来源/EWMA 用例通过；Full/Lite/Nano 为 11/11、11/11、1/1，产品头 15/15，Service OFF 11/11，WSL ASan/UBSan 与 `-fanalyzer` 均 11/11。真实介质数值仍待 V5-41。 |
| V5-45 | 产品 Frame 覆盖全量回归 | 清除旧单元测试对默认 `UCN_MAX_FRAME_BYTES=256` 与默认固定工作集的隐式假设，使 `tests/config/ucn_test_user_config.h` 的 128 B/8 Hop/3 Link/低 Service 容量产品配置能够运行完整 `ucn_tests`。 | Frame Payload 从帧头/Tag/配置推导；Wire/AODV 从 `UCN_MAX_HOPS` 推导；Service 按可用 Binding/Validator 容量选择子集；32×4 压力明确检查前提后跳过，同时保留固定资源边界。 | 已完成（软件）：Windows 默认 Full CTest `4/4`、Lite `1/1`、Nano `1/1`，Full + 产品头完整 CTest `2/2`（含 Resolver 专用目标），WSL GCC Full CTest `4/4` 均通过。未修改 Core/MTU/Preset/Wire 行为；产品配置因仅 3 Link 不执行 32×4 Host 压力，不能将其写作该配置下已验证。 |

## 任务清单

| ID | 阶段 | 实现内容 | 单元测试 | 模拟测试 | 集成验收 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| T00 | 项目台账 | 建立本任务表、操作记录和测试门禁。 | 文档结构检查。 | 不适用。 | 不适用。 | 已完成 |
| T01 | 工程骨架 | 建立 C99/CMake 工程、公共头文件、错误码、版本和静态配置模型。 | 编译配置、常量和配置校验。 | 创建空节点。 | Debug/Release 均可构建并执行测试入口。 | 已完成 |
| T02 | 基础帧 | 实现固定帧头、长度/版本/Network ID/TTL/CRC 校验、编解码。 | 正常帧、短帧、坏版本、坏长度、坏 CRC、TTL 边界。 | 虚拟 Link 收发单帧。 | 两个节点完成可验证的单跳收发。 | 已完成 |
| T03 | Port 与 Link | 定义 Port（时间、随机数、临界区、持久计数器）和 Link（send/poll/status）接口，实现内存虚拟 Link。 | Link 注册、MTU 拒绝、收发错误、状态变化。 | 两节点/多 Link 虚拟拓扑。 | 业务 API 不直接引用具体 Link。 | 已完成 |
| T04 | 节点与 QoS | 实现 Node、收包分发、Q0/Q1 固定队列、截止时间与 `latest_value` 覆盖。 | 队列优先级、满队列、过期丢弃、最新值覆盖。 | Q1 高负载下 Q0 抢占。 | 应用只使用 Node ID、消息类型和 QoS 发送。 | 已完成 |
| T05 | 路由与转发 | 实现固定 Route Cache、静态路由辅助、TTL 递减、转发与去重缓存。 | 命中/未命中、TTL 耗尽、重复包、路由失效。 | A→B→C 两跳数据转发。 | 多跳通信不需要 Linux 或 Host。 | 已完成 |
| T06 | 按需寻路 | 实现资源受限 AODV-Lite：`ROUTE_REQ`、`ROUTE_REPLY`、`ROUTE_ERROR`、Hop Limit、限速和老化。 | RREQ 去重、RREP 建表、RERR 清表、超时。 | 三节点未知路由发现、断链后修复。 | 三 MCU 逻辑节点可无 Linux 两跳通信并恢复。 | 已完成 |
| T07 | 安全边界 | 实现身份/会话/重放保护接口、Counter 持久化契约、ACL 钩子；测试使用明确标识的非生产测试 Provider。 | Counter 回退、认证拒绝、ACL 拒绝、Provider 失败。 | 未授权节点和旧包注入。 | 安全接口覆盖完整数据路径；生产 AEAD Provider 单列待接入。 | 已完成 |
| T08 | Host 边界 | 实现 Host 侧同一 C API 的虚拟接入示例，不依赖 ROS2。 | Host 节点收发与 ACL。 | Host 加入/退出不影响 MCU 路径。 | 验证“Host 是普通节点”边界。 | 已完成 |
| T09 | 完整验证 | 汇总测试、协议测试向量、模拟拓扑和端到端场景。 | 全部单测。 | 单跳、两跳、路由修复、QoS、异常包、Host 断线。 | CMake/CTest 全绿并记录命令、结果和已知限制。 | 已完成 |
| T10 | 链路质量选路 | 增加可选的通用 Link Cost；低层将 RSSI/丢包/总线错误等映射为代价，Core 不绑定任一介质；AODV-Lite 优先保留较低代价路径。 | 保守未知 Cost、低代价直连选择、重复 RREQ 的低代价替换、路由表更新。 | A→B→C 与 A→D→C 双路径模拟，验证选中较低总代价路径。 | v4 对未上报指标的 Link 使用保守未知 Cost，启用指标后不需要修改应用 API。 | 已完成 |
| T11 | 邻居生命周期 | 实现固定容量的 `Candidate → Admitted → Expired/Rejected` 邻居状态；保留人工准入，新增显式开放与 Provider 准入策略。 | 参数、重复候选、容量、手工准入、拒绝与过期。 | 虚拟 Link 上发现候选但不自动加入。 | 未启用开放/Provider 策略时，既有静态 Link 注册行为保持不变。 | 已完成 |
| T12 | HELLO 与自动准入 | 定义最小 `HELLO`，完成 Node ID 与物理 Link 绑定、双向 HELLO、按策略自动注册 Link 与候选老化。 | HELLO 格式/身份不匹配/Network ID 拒绝/未准入 Link 数据拒绝。 | 两个未知逻辑 MCU 自动加入、双向 HELLO 与单跳业务收发；多候选容量/超时复用 T11。 | 已准入节点无需人工 `ucn_node_register_link()` 即可建立单跳并触发多跳寻路。 | 已完成 |
| T13 | 多介质 Adapter 契约 | 为 ESP-NOW、CAN、RS485/UART、BLE/LoRa 明确“物理发现 → Candidate Link → UCN 收包”的 Adapter 契约和示例骨架。 | Link MTU、回调入队、Node ID/MAC 映射、默认 Cost 回退。 | 各 Adapter 的内存虚拟驱动。 | 业务层不感知 MAC、CAN ID、串口号或无线介质。 | 已完成 |
| T14 | ESP32 实际接入 | 实现 ESP-NOW Adapter、广播 HELLO、MAC/Node ID 表、FreeRTOS 收包队列与 `ucn_node_step()` 任务。 | 编译期 MTU（v1 250 字节）与队列边界。 | ESP-IDF 多实例/虚拟链路。 | 至少 3 块指定 ESP32 实机完成上电自动入网、两跳、断链重建和指标采样。 | 待开始（需确定 ESP 芯片型号、ESP-IDF 工程与实机接入范围） |
| T15 | 生产入网与规模验证 | 按[按需加密与透明转发建议](UCN_按需加密与透明转发建议.md)接入真实身份/AEAD Provider、Node 默认与 Endpoint 覆盖安全策略、透明密文中继、ACL、候选淘汰策略、压力/故障注入与容量报告；可选逐跳 Link AEAD 不替代端到端业务保护。 | 明文/密文共存、`ENCRYPTED_ONLY` 拒绝明文、坏 Tag、改头、重放/计数器回退、无 Key 透明中继、ACL 拒绝、表满。 | A→B→C 透明转发、明文温度与加密电机命令并发、节点上下线、候选洪泛、链路波动。 | 真实硬件测量 AEAD 的 RAM、Flash、栈、CPU、最大帧、节点规模、时延、丢包与恢复时间。 | 进行中：v4 Core 已有 Tag 线格式、`seal/open` Provider、策略和透明转发；真实身份、经审计 AEAD、ACL 产品表、密钥轮换与实机验证待完成。 |
| T16 | 小 MTU Carrier | 为需要经典 CAN 或小于 UCN 固定帧头 MTU 的介质，实现 Adapter 内部有界分段/重组、超时、错误统计和总线故障恢复；不把它伪装为 Core 业务分片。 | 段序号、乱序/丢段、重组槽满、超时、坏 CRC、Bus-Off。 | CAN/CAN-FD 虚拟总线多节点与拥塞。 | 指定总线实机长时间运行，验证 Q0/Q1 时延和重组内存上界。 | 软件完成：V5-60 已实现经典 CAN 固定 Carrier/Slot/超时/Bus State 并通过 Host 故障测试；真实总线长稳、拥塞和时延待 V5-61。 |
| T17 | 路由缓存续租与自动选路 | 按[路由缓存与自动选路建议](UCN_路由缓存与自动选路建议.md)实现受限后台刷新、活动/候选路径、`PATH_PROBE/ACK` 先测后切、路径标识、防抖滞回、明确断链快速恢复与切换统计；保持固定内存和跨介质 `route_cost`。 | 路由到期/刷新、未知 Cost、候选路径标识、探测 ACK/安全、防抖、候选替换、Q0/Q1 切换期语义。 | 双路径质量变化下旧路径持续业务、候选探测、候选失败回退、验证后切换、断链/恢复、乱序与队列压力。 | 在 T14 指定实机上测量收敛时间、探测开销、丢包、切换次数、RAM/Flash 与介质干扰表现。 | Core 完成：v4 `route_epoch`/1 s grace、Candidate/Probe/Activate、保守未知 Cost、固定计数和虚拟拓扑；Cost 抖动窗口与实机标定待 T14。 |
| T18 | 节点入离网与网络稳定 | 按[节点入离网与网络稳定性建议](UCN_节点入离网与网络稳定性建议.md)实现一跳 `HEARTBEAT`、已准入邻居存活状态、离网回收、Adapter/Link 槽复用、随机退避与 HELLO/RREQ 控制配额；借鉴 Reticulum 的控制面预算与 ESP-WIFI-MESH 的邻居恢复，但保持非树形、跨介质 UCN；与 T17 联动恢复路径。 | 心跳漏包、状态转换、认证拒绝、表项/绑定回收、退避和限流。 | 静默离网、多节点同时接入、重连复用、持续 Q1 与 Q0 本地安全动作。 | 在 T14 指定实机测入网 P50/P95、离线发现时间、控制流量、既有业务丢包、RAM/Flash 与大规模并发接入表现。 | 进行中：Heartbeat/回收、RREQ/Probe/Activate 预算和 S17 Adapter HELLO Jitter/Token/退避的软件闭环已完成；真实 Adapter 地址池、三板并发入网参数和实机 Profile 仍待 S06。 |
| T19 | 业务 Endpoint 与服务目录 | 先按[更新后设计方案](UCN_更新后设计方案.md)冻结 `0x40～0xBF` 静态 Endpoint、产品 ABI、Q0/Q1 与 Endpoint ACL，直接复用当前 1 B `message_type`，不增加正常业务帧开销；仅在需要动态能力查询/订阅时，再实现固定容量的 Extended 目录、租约和 4 B Endpoint 信封。 | 控制/业务编号隔离、同目的地多 Endpoint 的 `latest_value`、静态 ABI、ACL；Extended 的查询/订阅/租约/表满。 | 同节点 IMU/气压计/温度同路径并发、重启后 ABI 一致、动态目录受限查询与超时回收。 | 与 T14 实机联测不同 Endpoint 的 Q1 时延/丢包/乱序、静态 RAM/Flash；Extended 仅在产品确有动态节点需求时验收。 | 进行中：R1.4 已通过静态命名空间/API/固定分发；具体产品 ABI、Endpoint ACL、动态目录和实机联测后续冻结。 |
| T20 | 首包自动寻路与有界等待 | 让应用按 Endpoint 发送 Q1 时，在未知多跳路径上受限触发 RREQ 并保留固定数量待发项；Q0 仍只走直连/预建路由或本地失效安全，绝不因寻路等待。 | 未知路由、并发同目标合并、等待超时、队列满、RERR、Q0 不等待。 | 新节点加入后首次 Q1 自动寻路并发送；RREQ 风暴和断链时不挤占 Q0。 | T14 实机测首包时延、控制开销、RAM 与异常恢复。 | Core 完成：`ucn_node_send_endpoint()` 的 Q1 触发 RREQ、按 `(destination, Endpoint)` latest-value 合并、默认 4 槽/1 s；旧 `ucn_node_send()` 保持显式寻路兼容，Q0 不等待。 |
| T21 | 同对端多介质主备 Link（父任务） | 按[多介质同对端主备 Link 建议](UCN_多介质同对端主备_Link_建议.md)将一个 Node ID 的固定 Bearer 集与 Neighbor 身份分离；保持应用无介质感知、无动态内存和不改业务帧。所有具体结论以 T21.1～T21.6 为准。 | 子任务全部通过。 | 子任务全部通过。 | 子任务全部通过。 | 进行中：T21.1～T21.4 C99/虚拟闭环、T21.5 静态目标板资源已完成；真实双 Adapter、多板切换与运行时峰值仍待 T21.6。 |
| T21.1 | 固定 Bearer 集与准入 | 新增默认 `UCN_MAX_BEARERS_PER_NEIGHBOR=2` 的固定 Bearer 数组；一个 Node ID 只占一个 Neighbor 槽；第二 Link 逐 Bearer 经过 Join Provider/ACL，超限拒绝且不挤掉现有链路。`=1` 保留单 Link Profile。 | 两 Link 自动准入、第三 Link 拒绝、Provider 第二 Bearer 拒绝、旧邻居生命周期回归。 | `test_neighbor_bearer.c` 虚拟双 Link。 | Debug/Release/64 B/`Bearer=1` CTest 全绿。 | 已完成（C99；无动态内存；未做真实介质）。 |
| T21.2 | Bearer 保活与全失效移除 | 每 Bearer 独立 `last_seen`/Heartbeat/状态；单 Bearer Driver Down 时保留 Neighbor 和另一 Bearer；所有 Bearer Down 后才关闭 Link、回收 Neighbor 并清理相关动态路由。 | 单路 Down 不离网、全 Down 回收、单 Link SUSPECT/恢复/移除旧回归。 | 虚拟双 Link 断开与 Heartbeat。 | 四个 CMake Profile CTest 全绿。 | 已完成（C99；真实断链时间待实测）。 |
| T21.3 | Primary/Backup 与质量切换 | 首次准入按可用 Cost 选 Primary，Primary 明确 Down 后下一帧选健康 Backup；正常期间保持 Primary。候选需低至少 20%、连续 3 个 500 ms 窗口成立，并在候选 Bearer 上取得 2 次一跳 `HEARTBEAT` ACK（至多 3 次尝试）才切换；所有值均可编译期覆盖。 | 初选/硬 Down、5% 抖动不切、明显更优但 ACK 丢失不切、恢复后 2 ACK 切换和统计。 | `test_neighbor_bearer.c` 虚拟双 Link；Probe 复用 8 B Heartbeat，不新增线格式。 | Debug/Release/64 B/`Bearer=1` CTest 全绿；ESP-NOW+UART/CAN-FD 实测待补。 | 已完成（C99/虚拟；不承诺真实无缝）。 |
| T21.4 | Route/Candidate/RERR 联动 | Active Route、Previous Route 与 Candidate 的 egress Link 均在使用时解析为同一 Neighbor 当前 Primary；同一下一跳泛洪只允许当前 Primary 参与。单 Bearer Down 不清逻辑 Route/Candidate；全 Bearer Down 才清动态状态。 | A→B→C 动态寻路、Primary Down 后业务/Probe/Activate 走 Backup、下游 Down 的 RERR 经 Backup 清理 A 的动态 Route/Candidate、全 Down 回收；静态 Route 保留给产品策略。 | `test_neighbor_bearer.c` 虚拟 A→B→C 双 Bearer。 | Debug/Release/64 B/`Bearer=1` CTest 全绿；多板断链修复待实测。 | 已完成（C99/虚拟；静态 Route 不自动清除）。 |
| T21.5 | 完整构建与资源报告 | 保持原有单 Link、路由、安全与 Endpoint 回归；形成 Profile 构建证据，并使测试固件可报告运行时堆/`loopTask` 栈高水位。 | 全部 CTest。 | 虚拟双 Bearer 的硬 Down、抖动、Probe/Activate、RERR 与全 Down 回收。 | Debug/Release/64 B/`Bearer=1` 已通过；S3 目标构建证明双 Bearer 相对单 Bearer 增加 256 B 静态 RAM、496 B Flash；WROOM 也已构建。实机 `RESOURCE` 日志（峰值堆/栈）待采集。 | 进行中（静态报告见 [T21 目标板资源报告](UCN_T21_目标板资源报告.md)，运行时实测待板卡）。 |
| T21.6 | 实际 Adapter 与多板测试 | 将 ESP-NOW+UART 或 CAN-FD 的两个真实 Adapter 绑定同一 Node ID，测切换时延、控制量、丢失/乱序、资源和功耗。 | Adapter 地址/身份、队列、Down 事件。 | 先完成两板单跳，再做 A→B→C。 | 多板实测报告。 | 进行中：T21.6.1 双 S3 真实独立 Bearer 接入已完成；物理断链、性能和三板仍待 T21.6.2～T21.6.4。 |
| T21.6.1 | 双 Link 接入 | 将 ESP-NOW Peer Link 与 UART Link 分别作为独立 `ucn_link_t` 接入同一 Node；保留旧 `DualMediaLink` 为回归对照，不改 Core 帧格式。 | 两 Link 各自 `send/get_status/get_metrics/RX queue`；同 Node ID 的第二 HELLO 合并为第二 Bearer；旧对照 Profile 构建。 | Core `test_neighbor_bearer` 回归；S3 Node A/B 与 legacy 对照 Profile 构建。 | 两板启动后应显示一个已准入 Neighbor、两个 Bearer、独立 Wi-Fi/UART 收发计数。 | 已完成：两块 S3 实机同步启动均为 `links=2/admitted=1/count=2`，UART `0x70/Cost=5` 为 Primary，ESP-NOW `0x01/Cost=10` 为 Backup。 |
| T21.6.2 | 观测与基线 | 输出每介质的队列、TX/RX、Cost、Bearer 数、Primary、质量切换/Heartbeat/回收统计以及 `RESOURCE` 堆/栈水位。 | 日志字段/单位、无 UART Profile、legacy Profile、无 Peer 边界。 | 虚拟 Core 质量切换/全 Down 既有回归。 | 空闲、持续业务、UART 拔线、复接、Wi-Fi 失效/恢复均保存 A/B 日志。 | 进行中：空闲双板 `STAT/ADAPTER/BEARER/RESOURCE` 已实测；持续负载、物理拔线与 Wi-Fi 故障日志待补。 |
| T21.6.3 | 两板独立 Bearer 切换 | 用两块 S3 验证 UART Primary、ESP-NOW Backup、物理 UART 断开后的 Heartbeat/SUSPECT→Backup、复接后的 Probe/ACK 防抖；反向验证 Wi-Fi 故障。 | 发送失败/队列满不误判 Down；单 Bearer Down 不移除 Neighbor。 | 使用可控 Link 状态的 `test_neighbor_bearer`。 | 记录检测/切换/恢复时延、丢失/乱序、控制量、RAM/Flash/Heap/Stack；不得以旧逻辑回退日志替代。 | 进行中：B 临时刷同 Node ID 的 Wi-Fi-only 镜像后，A 保留 `admitted=1` 并以 ESP-NOW 通信；恢复 B 独立镜像后 A 自动回到 `count=2`。物理拔线、定时和反向 Wi-Fi 故障仍待实测。 |
| T21.6.4 | 三板多跳与 RERR | 物理隔离为 A↔B↔C，验证真实 RREQ/RREP、经 B 转发、B-C 断开后的 RERR、恢复后再寻路；按需执行 Trace。 | 路由/候选/RERR 回归。 | A→B→C 虚拟 Bearer 回归。 | 三板日志证明非 A↔C 直连；记录多跳首包、恢复、资源与干扰边界。 | 待开始（依赖 T21.6.3 和第三板拓扑）。 |
| T22 | 策略路由与可选负载均衡（父任务） | 按[路由策略与负载均衡执行建议](UCN_路由策略与负载均衡执行建议.md)在 T21 后实现多 Path 策略、固定主备与可关闭的 Q1 流级均衡；所有具体实现/验收必须落在 T22.0～T22.7。 | T22.0～T22.6 的单元/虚拟验收通过；T22.7.1 已固定为可重复全量回归。 | T22.7.2 才可给出真实多板证据。 | 未以构建代替实机性能。 | 进行中：T22.0～T22.6、T22.7.1 已完成；真实多板验收仍待 T22.7.2。 |
| T22.0 | 边界与资源冻结 | 冻结 Policy/Path/Flow 的 Profile 容量、硬/软故障定义、Adapter Cost 输入、Q0/Q1 规则、旧 API 兼容和不逐帧均衡边界。 | 配置边界、文档交叉核对、旧 API 回归计划。 | 不适用；本项不改协议/代码。 | 形成可审查的执行建议和依赖图。 | 已完成（文档；未改代码） |
| T22.1 | 策略数据模型与质量输入 | 新增 `ucn_policy.h/.c`：固定 `Policy(8)/本地 Path(8)/Q1 Flow(8)/Link 快照(4)` 表（均可编译期覆盖）；`ucn_link_metrics_t` 扩展可选 RTT、失败率、队列压力，Core 每 500 ms 以 25% EWMA 记录快照。ESP-NOW 使用 RSSI EWMA、失败率和 RX 队列惩罚，UART 使用失败/队列惩罚；两者不伪造 RTT。 | `test_policy.c` 覆盖默认无策略、精确/通配匹配、重复更新、表满、非法 Q0/通配 `AUTO_BALANCE`、质量 EWMA、Path/Flow 容量/租约与旧发送 API；既有未知 Cost 回归保留。 | 虚拟 Link 注入 Cost/RTT/失败率/队列压力，确认 500 ms 采样窗口内不更新、下一窗口按 EWMA 平滑；普通 `send_endpoint()` 仍走既有 AUTO_BEST。 | S3 A 构建为 RAM `43,788 B`、Flash `584,015 B`；WROOM 为 RAM `45,928 B`、Flash `610,419 B`。未烧录本轮固件；RTT、阈值与真实 Cost 标定留待实机。 | 已完成（C99/虚拟/双目标构建；本地 Path Handle 不上线，策略不改变业务转发）。 |
| T22.2 | Path ID 与安全控制面 | 新增固定 `ucn_path` 表（默认每节点 8 项）：以 `(源 Node、源 Session、Path ID、目标 Node)` 唯一标识，保存本跳下一跳/egress、终端标志和租约。冻结 40 B Path Header、`PATH_INSTALL/PATH_REVOKE`、受认证远程控制回调、逐跳透明转发及 Path 范围 RERR；Path ID 纳入受保护业务 AAD。 | `test_path_control.c` 覆盖 40 B 编解码/AAD、默认拒绝与显式授权、重复安装更新、过期/撤销、未知 Path 拒绝、P1 断链只清 P1。 | 虚拟 A→B→D(P1) 与 A→C→D(P2)：中继不调用 `open()`，目标才解密；P1 RERR 后 P2 继续传输。 | Debug、Release、64 B、`Bearer=1` CTest 全绿；ESP32-S3/WROOM 只编译通过。真实多板 Path 控制、透明中继和失效时延留待 T22.7。 | 已完成（C99/虚拟/双目标构建；T22.3 已将已验证线上 Path 接入普通 Endpoint）。 |
| T22.3 | 固定路径与主备切换 | `ucn_route_policy_config_t` 新增 Primary/Backup 本地句柄，`ucn_policy_path_config_t` 显式绑定已安装的线上 `wire_path_id`；`ucn_node_send_endpoint()` 对 `PINNED_STRICT` 固定走 Primary，对 `PINNED_FAILOVER` 仅在 `LINK_DOWN`/Path 不存在时切已验证 Backup。无 Backup 且配置允许时，只有 Q1 可受限 RREQ/等待；Q0 永不寻路。 | `test_path_control.c` 覆盖严格模式不允许发现、不回退；P1 正常固定、P1 RERR/Down 后 Q1/Q0 切 P2；无 Backup 的 Q1 经 A→C→D RREQ 回退，Q0 返回本地失败；AUTO_BEST 回归保留。 | 虚拟 A→B→D(P1) / A→C→D(P2)，中继 E2E 透明；Cost 未触发 Primary 切换，只有硬故障切换。 | Debug、Release、64 B、`Bearer=1` CTest 全绿；ESP32-S3/WROOM 仅构建通过。真实多板切换时延、丢失、乱序、Q0 本地安全和恢复留待 T22.7。 | 已完成（C99/虚拟/双目标构建）。 |
| T22.4 | Q1 流亲和负载均衡 | 已实现默认关闭的 `AUTO_BALANCE`：Policy Primary/Backup 是最多两条已验证 Path 成员；仅精确 Q1 Policy 可用，Primary 必填、禁止 Discovery。每个 `(destination, Endpoint, Q1)` 在固定 Flow 表中租约绑定一条 Path（默认 2 s）；V5-44/V5-36 后首次/到期选择按 Full 本地 LC-1 `effective_select_cost ×（该 Path 活动 Flow 数 + 1）` 评分，Known 基础 Cost优先于 Unknown；持续 Adapter Queue Pressure 还可独立触发未到期 Flow 的拥塞重绑。 | `test_policy.c` 覆盖全部 Metrics/EWMA/LC-1 快照；`test_path_control.c` 覆盖动态分、同流固定、两流分散、3 个 500 ms 高队列压力样本后的重绑、硬 Down 后单次改发、租约过期、Q0 排除和无复制。 | 虚拟 A→B→D(P1)/A→C→D(P2)：按有效分×Flow 分担；P1 持续队列压力时 Flow 改绑 P2；P1 Down 后仅受影响 Flow 改绑 P2。 | V5-44/V5-36 Host 回归已闭环；早期 S3/WROOM 资源记录仅属旧实现构建证据。真实负载比例、P50/P95、丢失、乱序、功耗和多板结果留 T22.7/V5-41。 | 已完成（C99/Host 软件；不做逐帧轮询、帧复制、带宽聚合或真实性能宣称）。 |
| T22.5 | Path 与 Bearer 联动 | 已让端到端 Path egress 在每次发送/转发时解析同 Neighbor 当前健康 Bearer；单 Bearer Down 保持同一 Path ID，全部 Bearer Down 才撤销受影响 Path 并把本机对应 Policy Path 标为 Down。 | `test_path_control.c` 覆盖源/中继两跳的单 Bearer 切 Backup、全 Bearer Down、Path 范围 RERR、E2E 透明转发和无关 P2 保留；Debug/Release/64 B/Bearer=1 全绿。 | 虚拟 A⇄B(双 Bearer)⇄D(双 Bearer) + 独立 P2；真实 WiFi+UART/CAN 多板仍待 T22.7。 | S3 A：RAM `44,308 B`、Flash `588,047 B`；WROOM：RAM `46,448 B`、Flash `614,615 B`，均只构建未烧录。 | 已完成（C99/虚拟/双目标构建；不承诺实际切换时延、丢失、乱序或功耗）。 |
| T22.6 | 策略与路径诊断 | 已新增 `POLICY_DIAGNOSTIC_REQ/REPLY (0x1E/0x1F)`：管理 Node 选择三页 Summary 或固定 Policy/Path/Flow/Link-quality 槽位；请求 8 B、回复 32 B，64 B Profile 可用。普通业务帧零额外字段。 | `test_policy_diagnostic.c` 覆盖显式授权/默认拒绝、独立令牌桶、固定 Pending 表满、空槽、Q0 优先、Path/Bearer、Flow、质量、Summary 和超时。 | 虚拟 A 管理 → B 响应：B 只在授权后排队 Q1 诊断 Reply；请求/Reply 均在正常 Q0/Q1、保活和既有诊断之后发送。 | 单 Node 按需观察，不泛洪、不保存常驻全网拓扑；生产 ACL/Security Provider 和实机时延/开销留 T22.7。 | 已完成（C99/虚拟/双目标构建；S3 RAM `44,540 B`/Flash `591,371 B`，WROOM RAM `46,680 B`/Flash `617,807 B`，均未烧录）。 |
| T22.7 | 全量回归与实机验收（父任务） | 分为 T22.7.1 自动 Core/固件回归和 T22.7.2 分阶段多板实机门禁；记录资源与实际性能边界。 | 不把任一 C99 或构建结果写成空口性能。 | T22.7.1 后才进入实机；实机从两板、三板、四/五板逐级进行。 | P50/P95、丢失、乱序、收敛、控制面开销、RAM/Flash/栈、功耗与干扰必须由实际日志得出。 | 进行中（T22.7.1 已完成；等待 T22.7.2 实板）。 |
| T22.7.1 | 自动全量回归与虚拟故障矩阵 | 新增 `tools/run_t22_7_core_regression.ps1`：重新配置 Debug、Release、64 B MTU、Bearer=1 四个 Profile，分别构建/CTest；随后仅构建 S3 Node A/B 与 WROOM 固件，绝不上传、监听、复位或打开串口。 | 主测试程序集包含 T21 Bearer、T22 Path/P1-P2 RERR、Strict/Failover、Q1 Flow 重绑、策略诊断等既有虚拟故障覆盖。 | 四个 Profile 的 CMake Flags 独立重配，`git diff --check` 通过。 | 四次 CTest 均 `1/1` 通过；S3 A/B 均 RAM `44,540 B`/Flash `591,371 B`，WROOM RAM `46,680 B`/Flash `617,807 B`；不产生实机结论。 | 已完成（仅构建/CTest）。 |
| T22.7.2 | 分阶段多板实机验收 | 在 T22.7.1 通过后，按两板双 Bearer → 三板 A→B→C/RERR → 四/五板双 Path/策略/均衡的顺序执行，所有测试使用明确 Node ID、板号、介质与摆位。 | 保留原始 A/B/C/D/E 串口日志和统计快照；生产 ACL/安全另设条件。 | P1 断链、拥塞、恢复、指定 Path、Q1 分流、Q0 安全，以及诊断控制面开销。 | 记录 P50/P95、丢失、乱序、收敛、控制帧、RAM/Flash/最小 Heap/任务栈和功耗；不能以“无线可见”代替路径证明。 | 待开始（需要实际拓扑、供电/摆位与日志条件）。 |
| T23 | 按需路径追踪诊断 | 按[路径追踪诊断](UCN_路径追踪诊断建议.md)增加 `PATH_TRACE_REQ/REPLY`、诊断 Flag、逐跳 Node ID 记录、固定 Reverse/Pending 表、显式回调与状态；普通业务/Route Cache 零额外字段。 | Trace 编解码、未知 Flag 拒绝、表满/超时、A→B→C 完整路径、无路由/TTL/截断、反向 Reply、E2E/未准入拒绝、Q0 回归。 | 虚拟三节点/四节点、多路径切换后查询、64 B Profile；后续三板实测 Trace 时延、断链和重组网。 | Debug、Release、64 B CTest 全绿；实机 T23.5 前不宣称真实介质路径查询已验证。 | Core 已完成：C99 固定表、独立诊断 Token、`CACHE_ONLY` 查询与虚拟回归已通过；真实介质 T23.5 待测试。 |
| T24 | 按需节点快照诊断 | 按[v4 节点快照诊断](UCN_v4_节点快照诊断.md)增加 `NODE_SNAPSHOT_REQ/REPLY`、受限广播、固定结果表、短期 Reverse、Reply 抖动、独立限流与默认拒绝授权；不保存常驻全网拓扑。 | 版本 4/拒绝 v3、Request/Reply 格式、默认 ACL 拒绝、去重、Reverse 多回包、结果表截断、Token 与 Q0/Q1 回归。 | 虚拟 A→B→{C,D}、环路/重复包、网络分区和表满；后续五板 ESP32。 | Debug、Release、64 B CTest 全绿；五板 T24.5 前不宣称真实空口全网节点收集率。 | Core 已完成：固定资源/授权/虚拟四节点回归已通过；ESP32 实机与产品 ACL 表待完成。 |
| T25 | 节点内任务通信 Adapter | 按[节点内任务通信建议](UCN_节点内任务通信建议.md)、[详细执行方案](UCN_T25_节点内任务通信详细执行方案.md)与[T25.0 契约](UCN_T25_首版Endpoint与Service契约.md)在 Core 外实现固定 Service 表、Endpoint→任务 Inbox、统一 `ucn_service_send()`、本机直投与固定远端 TX Request Queue；一个 MCU 只保留一个 Node ID 和一个协议任务。 | Endpoint/表重复与容量、长度/QoS、任务未就绪、队列满、Q1 Latest Value、Q0 FIFO、本机直投不调用 Link、协议任务独占 Core。 | C99 Router + Bridge 模拟 A→B→C；ESP32 静态 Port 以 Router Inbox、Endpoint 事件 Queue 与静态业务 Task 接入；两块 S3 后续并发 IMU Q1、本机 Control/Servo 与远端 Q0。 | 不改 v4 帧/路由；测静态 RAM、任务栈、Q0/Q1 时延、队列丢弃和断链下本地安全动作。 | 进行中：T25.0～T25.3 代码/构建闭环已完成；下一项为 T25.4 实机任务通信验收。 |
| T25.0 | 首版 Endpoint / Service 契约 | 冻结 R1 的静态 Endpoint 分区、Service 身份、单消费者绑定、Payload 编码/单位/QoS、执行器安全字段和 T25.1 默认固定资源 Profile；隔离 ESP 测试 Endpoint。 | 文档交叉核对：Core 控制范围、现有静态 Endpoint 设计、长度、Q0/Q1 与测试冲突。 | 不适用；本项不改协议或固件。 | 每个后续 Router 测试都有唯一的 Endpoint、长度、QoS 与消费者输入；ABI 改动必须新增 Endpoint。 | 已完成（R1 文档契约；测试固件 `0x40/0x41` 的迁移留 T25.3）。 |
| T25.1 | 纯 C Service Router | 新增 Core 外 `ucn_service` 固定 Binding、Local Fast Path、Remote TX Q0/Q1、入站投递和统计；无 FreeRTOS 依赖、无动态内存、无 Core 并发访问。 | 配置拒绝、本机不调用 Link、Q0 FIFO/Q1 Latest、未就绪/满队列/长度/QoS、Remote Request 边界和统计。 | 三个 Router 的 A→B→C：B 不可投入 C 目标帧，C 才投递 Endpoint 到 Service Inbox。 | Debug/Release/64 B/Bearer=1 CTest 均 `1/1`；S3 A/B、WROOM 均构建且编译 `ucn_service.c`。 | 已完成（纯 C/虚拟/目标构建；未实例化 Router、未接 Core/FreeRTOS、未烧录）。 |
| T25.2 | Protocol Task 桥接 | 新增独立 `ucn_service_bridge`：在唯一 Protocol Task 的有界循环中读取 Remote TX 并调用既有 `ucn_node_send_endpoint()`；目标 Endpoint callback 接到 `ucn_service_deliver_remote()`。 | Node ID 一致、handler 所有权/容量、未安装拒绝、出队上限、Q0 优先、Core 返回值、无路由 Q0 失败、回调只投递、不重入 Core。 | 真正虚拟 A→B→C：A Router→Bridge→Core→B 透明转发→C Core callback→C Router Inbox；B 通用回调为 0。 | 无新增帧字段/Task ID；Protocol Task 独占 Core；四个 CTest Profile 全绿。 | 已完成（C99/虚拟；目标 Service 拒绝只记本机统计，不是端到端 ACK；未接 FreeRTOS/实板）。 |
| T25.3 | ESP32 静态 FreeRTOS Port | 在测试工程静态实例化 R1 6 Binding、Router、Bridge、两个 1 B Endpoint 事件 Queue 与一个业务分发 Task；`loopTask` 仍独占 Core。Port 使用短临界区保护 Router，Bridge hook 在入站投递后通知消费者；测试 Ping/吞吐迁至 `0xB0/0xB1`，不占 R1 ABI。 | 无动态 Queue/Task；Router Inbox 是唯一 Payload 队列；绑定 Task 才能读 Service Inbox；Bridge 每轮预算 2；hook 不把 RTOS 类型写进 C99 Core。 | `test_service_bridge` 增加 lock/unlock/observer hook 覆盖；四 CTest Profile；S3 A/B 与 WROOM 编译 `ucn_service_freertos_port.cpp`。 | 烧录后读取 `SERVICE`/`RESOURCE`，验证静态 Task、Q0 FIFO、Q1 Latest、事件 Queue 满、Task Stack、Heap、断链本地安全与端到端时延。 | 已完成（代码/构建；未 upload/monitor，所有板级运行时结论仍待 T25.4）。 |

### 稳定化阶段（审查新增，暂不扩展新的 Extended 功能）

> 依据 2026-08-10 两轮架构/源码复核。目标是先收束调度、Task 生命周期、安全、指标、控制面预算、长期计时、资源和真实硬件证据；在 P0/P1 未闭合前，不增加动态目录、文件传输、OTA、GPS 预测路由或新的 QoS 类别。第二轮复核确认 `AUTO_BALANCE` 的实际评分位于 `ucn_node.c`，此前只核对 `ucn_policy.c` 导致 S03 被错误标记为完成；本表从本轮起以实际调用路径为准重新打开 S03。

| ID | 任务 | 实现范围 | 单元测试 | 模拟/实机验收 | 完成证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| S01（P0） | 消除业务队列对维护控制面的饥饿 | 已加入 `UCN_BUSINESS_TX_BURST_BEFORE_MAINTENANCE`（默认 4）：只有在 Heartbeat、Bearer/Path Probe 或 Route Refresh 实际到期时才会占用一个业务槽位；Q0 仍 FIFO 且仅被这类必要维护延后一轮，Snapshot/Policy Diagnostic 永不抢占 Q0/Q1。连续计数饱和后，维护一旦到期即在当前 `ucn_node_step()` 取得机会；维护发送/尝试返回的错误沿用原 `ucn_result_t`，并记入 `maintenance_preemptions`。 | 已覆盖持续 Q0、持续 Q1、Q0+Q1 的 30 s 虚拟时间压力：Heartbeat 保持发送、邻居不误入 `SUSPECT/REMOVED`；Candidate Route 覆盖连续四个 Q1 后 Route Refresh 抢到维护槽。Q0 满载时固定队列的 `UCN_ERR_NO_SPACE` 是预期背压，测试确认随后调度仍继续。 | 软件虚拟验证完成；待两块 S3 在 WiFi/串口压力镜像，以及三板多跳下确认真实调度周期、时延和丢包。 | Debug、Release、64 B MTU、Bearer=1 四个 CTest Profile 均 `1/1`；ASan/UBSan 因当前 MinGW 工具链缺 `libasan/libubsan` 无法配置，记录为环境限制，不视为通过。 | 代码/软件测试完成；实机待 S06。 |
| S02（P0） | 生产安全闭环：身份、控制面逐跳认证与业务 E2E AEAD | 冻结威胁模型、设备身份/Join 引导、密钥生命周期、单调计数器持久化、Replay Window、Endpoint ACL、密钥轮换和失联降级策略；实现经审计 AEAD Provider。控制帧继续禁止作为 E2E 业务密文，但必须由生产 `authorize_rx/tx` 或 Adapter Link Security 提供逐跳来源认证；E2E AAD 有意不含 Hop Limit/Route Epoch，因此这两个可变字段同样必须由逐跳链路认证保护。 | 明文/密文策略、坏 Tag、篡改 AAD、重放/计数器回退、未认证 HELLO/Heartbeat/RREQ/RREP/RERR、可变路由字段篡改、ACL、Key/Session Rotation、无 Key 中继。 | A→B→C：B 不解密业务密文；无线伪造控制帧被拒绝；断电重启后序号不回退；接近序号阈值时由 Provider 原子轮换 Session/Key；记录安全开销/资源。 | 安全设计文档、受审计库与密钥来源说明、真实 Provider 测试向量、四 Profile CTest 和实机注入日志。 | 进行中：Core 已补 `rotate_session`、按 `(source, session, sequence)` 去重和失败关闭测试；生产身份、AEAD、Replay Window、逐跳 Link 认证与真实密钥仍是产品化门禁。 |
| S03（P1） | 修正并冻结跨介质 Link Metrics、Cost 与 AUTO_BALANCE | 保留 [Link Metrics 与 Cost 契约](UCN_Link_Metrics与Cost契约.md) 的独立量纲；禁止 Cost、RTT(ms)、失败率(‰)、队列压力(‰)裸相加。V5-44 以 LC-1 固定表归一动态项，V5-36 用本地有效分接入 Full Bearer/Candidate/Q1；Unknown 与线上基础 Cost 语义不变。 | 覆盖 C01～C10、Known/Unknown、有效 Cost 上限、全部指标 EWMA/陈旧/Invalid、活动 Flow 分担、持续拥塞重绑和旧 AODV/Bearer 回归。 | 用真实 UART/ESP-NOW/CAN/LoRa 夹具记录基础 Cost、选路/重绑理由并完成 V5-41 标定；Host 初值不是通用硬件结论。 | DOC-039/047、源码、三 Profile、产品头、Sanitizer/Analyzer 和后续真实介质标定表。 | 代码/Host 软件完成：LC-1 Resolver 与动态选路已闭环；真实介质默认值标定待 V5-41/S06。 |
| S04（P1） | 建立可裁剪 Feature Profile：Nano / Lite / Full | 已新增 `ucn_profile.h` 与 CMake `UCN_PROFILE`；Nano 使用独立静态 Node，Lite 从对象/源码移除 Candidate/Path/Policy/Diagnostic，Full 保持默认兼容；Service 独立 ON/OFF。 | 三档功能/负向 API、公开 Node/Path/Policy 符号完整性、三节点静态/动态交付、Full 全量回归、当前最小帧 33/46/64 B 与低一字节拒绝；MSVC/GCC 严格构建。 | S04 原始 Host 基线 Node `2640/5456/8136 B`、`.text` `13904/50308/106704 B`；当前资源以本轮发布报告为准。 | [S04 Feature Profile 与资源报告](UCN_S04_Feature_Profile与资源报告.md)、CI Profile 矩阵；S08 已完成头/API 边界，目标绝对资源和实机行为留 S06/S07。 | 代码/软件验证完成；V5-14 将 Lite 最小帧从旧 50 B 收紧到 46 B。 |
| S05（P1） | 长时间虚拟压力与随机故障测试 | 新增固定 Seed、可复现的多节点/多 Link 压力模拟：随机 Link Down、Cost 波动、丢包、持续 Q1、间歇 Q0、Route/Path/Neighbor/Service 资源耗尽；先以 CI 可承受步数运行，夜间/手动可扩展到长跑。 | 表边界、无死循环、无无界增长、Seen/Route/Candidate/Path/Flow 回收、Q0/维护公平、失败后恢复。 | 逐步扩大到 32 Node、每 Node 最多 4 Link；断言无 Route Loop、误 REMOVE、状态泄漏或控制队列失控。 | 固定 Seed 报告、失败复现命令、ASan/UBSan（可用平台）与四 Profile CTest 结果。 | 代码/软件测试完成：保留 32 Node 静态环网，新增长度固定的 32 Node×4 Link 动态事件网，覆盖 HELLO、AODV、RERR、Cost、Down/恢复、丢失/重复/延迟/乱序、持续 Q1/间歇 Q0、固定表耗尽/回收、同 Epoch 无环和故障后重入网；默认 2000 轮、可配 Seed/最多 1,000,000 轮。本轮 5 Seed×1000、固定 Seed×100,000、四 Profile及 WSL ASan/UBSan 均通过；百万轮入口已提供但本轮未执行。详见 [S05 动态综合压力测试](UCN_S05_动态综合压力测试.md)。 |
| S06（P1） | 三板真实多跳、故障与策略验收 | 收束既有 T21.6.2～T21.6.4、T22.7.2：搭建物理 A↔B↔C，证明 A 与 C 不直连；测试 B 掉电/远离、B-C 断开、RERR、恢复再寻路、物理 UART 拔插、Wi-Fi 恶化/恢复和实际 Path/Flow 策略。 | 复用邻居、Route、Candidate、Path、Policy 虚拟回归作为前置门禁。 | 保存 A/B/C 原始串口日志、路径 Trace、P50/P95/最大时延、丢失/乱序、收敛、控制开销、Heap/栈/功耗。 | 受控摆位、Node ID、介质、故障时间线和原始日志齐全；不得以“无线可见”替代多跳路径证明。 | 进行中（复用并细化 T21.6/T22.7.2） |
| S07（P1） | 完成 Task/Service 实机压力与失联安全验收 | 收束 T25.4：测本机 Q0 P50/P95/Max、Q1 1 kHz Latest Value、多个 Producer、Router/事件 Queue 满、远端 Q0/Q1 同时工作、Bridge 预算、任务栈/Heap 与断链时执行器本地安全。 | Router/Bridge Q0 FIFO、Q1 覆盖、错误路径、Task 所有权和 Hook 回归。 | 两块 S3 先烧录已构建压力镜像，保存 `T25PRESS/SERVICEQ/SERVICE/STAT/RESOURCE` 双端原始日志。 | 栈高水位、Heap 最小值、队列统计、时延分位、Q0 失联安全和恢复结论分开记录。 | 进行中（复用 T32；压力镜像尚未实测） |
| S08（P2） | 公开 API 与固定资源/小 MTU 边界收口 | `ucn_node.h` 只保留不完整 Node 类型、配置/结果/统计和公开函数；`ucn_node_storage.h` 只由唯一 Protocol Task owner、Core 与白盒测试显式包含，继续支持无堆静态分配。已提供按 Flags 计算实际最大 Payload 的 helper，修复 Path Trace 小 MTU 下溢并增加 Feature-specific 最小帧断言；网络去重、生产 Security Replay Window 和业务命令 Replay 表三者语义分离。 | API-only 头独立编译、storage-only 静态分配、v5 可变头与 E2E Tag Payload 边界、当前 33/46/64 B 正向和 32/45/63 B 负向门禁、Nano/Lite/Full 符号完整性。 | 三个 Feature Profile 的最小 MTU 与 API/静态 owner 示例；ESP32 owner/Adapter 包含边界编译；硬件性能不由本项替代。 | [S08 公共 API 与静态存储边界](UCN_S08_公共API与静态存储边界.md)；GCC/MSVC/Profile 对象尺寸核对。 | 代码/软件验证完成；V5-14 更新 Lite 门禁，公开不完整类型边界不变。目标 ELF/栈/Heap/CPU/功耗和真实介质继续归 S06/S07。 |
| S09（P2） | 发布工程化验证资产 | 在仓库中提供可公开复现的 `examples/esp32`（不含本机构建产物/凭据）；增加 GitHub Actions 运行 Debug、Release、64 B、Bearer=1 与 Nano/Lite/Full Feature Profile 矩阵；把长压力测试作为可选工作流。 | CI 本身验证 CMake/CTest 和文档/配置检查。 | Fork/干净工作区可构建 ESP32 示例；CI 产物只包含日志/报告，不包含密钥或固件私密配置。 | Actions 成功记录、示例构建说明、发布前敏感信息扫描。 | 进行中：`core-ci.yml` 已包含既有 Core/ASan 门禁和 Nano/OFF、Lite/ON、Full/ON 矩阵，本地对应组合全绿；远端 Actions 尚未运行，公开 `examples/esp32` 仍待补。 |
| S10（P0） | Task/Service 重启生命周期安全 | `ucn_service_set_ready(..., false)` 必须原子地阻止新投递并清空该 Binding 的旧 Q0 FIFO/Q1 Latest；未 ready 时消费者不能取出旧消息。重新 ready 后只能处理新命令；增加显式 purge 统计，保持固定内存。 | 分别覆盖 Q0/Q1 入队后置 `ready=false`、停机期间投递拒绝、未 ready 读取拒绝、重新 ready 后无旧消息、新消息正常。 | ESP32 T25 压力镜像模拟 Motor/Actuator Task 重启，确认旧命令不恢复执行。 | C99 单测、四 Profile CTest、Service 文档和实机日志。 | 代码/软件测试完成：Q0/Q1 清理、未 ready 取件拒绝、`binding_purges` 与重启后新消息已覆盖；实机待 S07。 |
| S11（P1） | 入站控制面预算与诊断授权加固 | 在已准入 Link 和 Security Provider 之后、分配路由/Reverse/回复资源之前，对 RREQ Request（含中继/目标处理）、Heartbeat Request 和 Path Trace Request 使用固定内存入站预算；源端既有 Token 保留。Path Trace 增加显式产品 authorizer，默认拒绝远端请求；避免合法但失控节点用唯一 Sequence/Request ID 拖垮控制面。 | 唯一 RREQ 洪泛、Heartbeat Request 洪泛、Trace Relay/目标洪泛、预算恢复、不同控制类型隔离、授权拒绝不分配状态、正常保活/寻路/Trace 回归。 | 多板故障注入记录控制丢弃计数、正常收敛与业务时延。 | 固定 Token/时间状态、统计、CTest 和实机原始日志。 | 代码/软件测试完成：按直连对端+控制类型固定预算、离网回收、Trace 默认拒绝/显式授权、洪泛/恢复/邻居隔离已覆盖；参数与实机待 S06。 |
| S12（P1） | 长期运行 Deadline 与 Sequence/Session Rotation | 全 Core 使用统一 wrap-safe Deadline helper：所有有效 duration 小于 `2^31 ms`，生成的有效 Deadline 永不为 0，禁止裸写 `now + timeout`；补 `0xFFFFFFFF` 附近回绕测试。Sequence 接近保留阈值时调用可选 Security Provider Rotation 回调，Provider 必须原子持久化新 Session/Key/next_sequence；没有回调时明确返回安全错误。 | Deadline 生成回绕到 0、跨回绕超时、邻居/Route/Path/Flow/诊断回收；Rotation 成功、重复 Session/非法 Sequence、Provider 失败和无 Provider 边界。 | 长跑/断电恢复验证序号不回退，轮换期间无旧 Session 误接受。 | Core helper/接口、单测、S05 长跑和生产 Provider 证据。 | Core/软件测试完成：统一 `ucn_time`、Duration 编译边界、Neighbor/Route/Path/Flow/Trace 跨回绕及 Rotation 成功/失败关闭已覆盖；真实 Key/断电轮换留 S02/S06。 |
| S13（P1） | 冻结 Link `send()` 实时性与背压契约 | `send()` 在生产 Adapter 中只允许进行有界复制/入固定 TX Queue 后返回，不得等待物理发送完成；队列满返回 `UCN_ERR_NO_SPACE`，Driver Down 返回 `UCN_ERR_LINK_DOWN`。每个产品 Adapter 必须声明/测量最坏执行时间和队列容量；Core 不在 `send()` 内自旋或重试。 | 队列满/恢复、Driver Down、同步虚拟 Link 仅限测试声明、慢驱动不会由 Core 重试或吞掉错误。 | UART/WiFi/后续 CAN/LoRa Adapter 记录 send WCET、队列压力和背压统计。 | 公开头/Adapter 契约、虚拟测试和实机时序。 | 接口/软件测试完成：公开头与 Adapter 文档冻结 enqueue 语义，`NO_SPACE/LINK_DOWN` 原样返回且单次调用不重试；真实 Adapter WCET/队列待 S06。 |
| S14（P2） | Service 异步接受与高风险消息新鲜度 | 保持 `UCN_OK` 的兼容含义为“本机 Router 已拥有副本”，明确不表示远端送达/执行；关键 Q0 使用业务 `command_id + valid_for_ms + result Endpoint`，而不是给所有静态消息强制增加时间字段。后续如产品需要通用完成通知，再新增不破坏现有 API 的异步 receipt。 | Router accepted、Bridge/Core 后续失败、重复 command_id、过期拒绝、结果 Endpoint 关联；不把可靠 ACK 伪装成既有能力。 | 两板远端命令/结果往返与断链超时。 | 使用手册、示例 Payload 契约、虚拟/实机日志。 | 代码/软件测试完成：兼容 `send()` 保留，`send_ex()` 显式区分本机投递/远端入队；可选 12 B Command Guard 覆盖重复、过期、回绕与 Result Endpoint，不增加普通消息 RAM；实机结果往返待 S07。 |
| S15（P1） | Q0 有界背压与 Service Bridge 所有权闭环 | 按[稳定化第二阶段建议](UCN_稳定化第二阶段问题与执行建议.md)实现默认兼容的 `BEST_EFFORT` 和仅 Q0 可选的 `RETRY_ON_BACKPRESSURE`；只对 `UCN_ERR_NO_SPACE` 按固定次数、短间隔、绝对截止时间重试。Core Q0 保留原槽，Bridge 增加一个固定 Pending Q0 槽；终止错误立即失败，并修正一次失败重复累计统计。 | 前 N 次 `NO_SPACE` 后恢复、一直背压至耗尽、终止错误零重试、Q0 FIFO、Q1 Latest、不重复发送/计数、维护公平。 | 虚拟 Adapter Queue 满/恢复和 Router→Bridge→Core 组合；S06/S07 实机测 Queue 压力、截止时间与时延。 | 新统计、专项单测/模拟、四 Profile CTest；实机结果独立记录。 | 代码/软件测试完成：Core Q0 保留槽、Bridge 单 Pending、最终 Observer、互斥统计和维护公平已回归；Debug/Release/64 B/Bearer=1 全绿，ESP32-S3/WROOM 构建通过；真实 Queue 压力、WCET 和时延待 S06/S07。 |
| S16（P1） | Protocol Task 最大 Step 间隔与维护时延契约 | 增加产品 `UCN_MAX_STEP_INTERVAL_MS` Profile、`last_step/max_step_gap/violations` 运行时观测，并按业务 Burst、Neighbor、Bearer 上限给出保守维护服务上界；非法调度配置失败关闭。Port 文档冻结 Task 优先级、最大 Block、Adapter Pump/Bridge 预算和 Link `send()` WCET。 | 首次 Step、正常/超限 Gap、跨回绕、合法/非法配置、多 Neighbor/Bearer 持续业务下维护最大间隔。 | 虚拟时间压力；ESP32 记录真实 Step 最大 Gap、Heartbeat/Probe 时延与违规计数。 | 配置断言、运行时统计、文档、四 Profile CTest 和实机日志。 | 代码/软件测试完成：默认 10 ms、保守维护上界 800 ms；首次/边界/超限/回绕和持续业务下多 Neighbor/Bearer 维护已覆盖，50 ms 非法档案编译失败关闭；四 Profile 与 S3 A/B、WROOM 构建通过。真实 Step/Heartbeat/Probe/WCET 日志待 S06/S07。 |
| S17（P1） | Adapter HELLO 启动风暴控制 | Core 保持介质无关；在 Adapter 契约/参考 helper 实现 Initial Jitter、每 Adapter Token、有限 Fast Retry、指数 Backoff 和准入后 Stop/Slow。静态 CAN/UART 点对点 Profile 可关闭主动 HELLO。 | 固定 Seed 的状态转换、Token/退避/最大尝试、准入停止、掉线重启和静态禁用。 | 100 节点同时启动，比较 10/100 ms 窗口 HELLO 峰值、总量和入网 P50/P95；失败可按 Seed 重放。 | 平台无关测试、Adapter 文档、ESP 参考接入；实机规模参数待 S06。 | 代码/软件测试完成：固定 Scheduler、Adapter Token、Slow/Stop、静态禁用与跨回绕已覆盖；100 节点固定 Seed 模拟峰值 10 ms `100→9`、100 ms `175→52`、总量 250、准入 P50/P95 `608/1177 ms`；六个 ESP 参考 Profile 构建通过，实机规模参数待 S06。 |
| S18（P1） | 高风险远端 Q0 强制 Validator | Bridge 按 Endpoint 注册产品 Validator；高风险 Binding 标记“远端 Q0 必须校验”，缺 Validator 时初始化/安装失败。Validator 在 Router 入队前接收完整 Frame/Source/Session/Endpoint/Payload/Now；可选固定 Replay 表按 `(source, session, endpoint)` 去重。普通 Payload 不强制 12 B Guard，执行 Task 仍二次检查。 | 缺 Validator、重复/过期/旧 Session、坏长度、表满、合法 Session 轮换、本机 Fast Path、普通遥测不受影响。 | 两节点远端电机/舵机命令与结果 Endpoint；拒绝项不得进入 Inbox。 | C99 API/单测/虚拟往返、四 Profile CTest；产品 ABI 与实机待 S07。 | 代码/软件测试完成：Binding 强制标记、安装期失败关闭、每 Endpoint Validator、固定 Replay/显式认证 Session 轮换和入队前拒绝已覆盖；普通 Q1、本机 Fast Path及自定义 1 B Q0 格式不强制 Guard。ESP R1 以原 16 B ABI 接入并在消费 Task 二次校验；五镜像构建通过，实机待 S07。 |
| S19（P2） | 已认证 Path Install/Revoke 管理预算 | 在 Security 与 `path_control_authorize` 成功后、修改 Path 状态前，按认证管理源 `(source Node, source Session)` 使用独立固定 Token Bucket；INSTALL/REVOKE、授权拒绝、预算拒绝和表满分别统计，同源换 Bearer 不能绕过预算。 | 洪泛/恢复、不同管理源隔离、同源换 Bearer、Session 轮换、类型统计、拒绝时 Path 表不变。 | 多节点虚拟管理链；S06 实机故障注入确认不影响正常 Heartbeat/RREQ/业务。 | 固定资源、专项测试、四 Profile CTest 和实机控制统计。 | 代码/软件测试完成：默认 4 个认证管理源槽，每槽 INSTALL/REVOKE 各 4 Token、1 s 补 1 Token，60 s 不活跃后惰性回收；同源认证 Session 更新复用槽。专项测试和既有四节点 Path 回归在四 Profile CTest 全绿；ESP `PATHCTL` 统计与六环境构建通过，实机故障注入待 S06。 |
| S20（P2） | Service 异步阶段和本机 Outbound Observer | 统一 `LOCAL_INBOXED / REMOTE_ROUTER_QUEUED / LINK_QUEUE_ACCEPTED / REMOTE_INBOXED / REMOTE_EXECUTED` 术语；Bridge 增加最终本机 outbound observer，报告提交接受、背压耗尽、终止失败和过期。`REMOTE_INBOXED/EXECUTED` 继续由业务结果 Endpoint 确认，本阶段不增加通用远端 ACK/Q2。 | 每种本机最终状态只回调一次；S15 重试中间态不冒充完成；命令 ID 与结果 Endpoint 关联。 | 两节点命令/结果往返、断链/超时/目标拒绝，明确区分本机接受与远端执行。 | API/使用手册/虚拟测试；实机结果待 S07。 | 代码/软件测试完成：统一 Stage 枚举与 Acceptance 映射；兼容旧 Observer，新增结构化本机最终事件区分 Link 接受、立即背压拒绝、重试耗尽、过期和终止失败；可选 8 B Result Header 以 Command ID + 实际 Result Endpoint 关联远端 Inbox/Executed。两节点模拟覆盖成功往返、Link Down、目标 Validator 拒绝后无伪 ACK及产品超时；四 CTest Profile 与 S3 Service A/B、WROOM 构建通过，实机结果待 S07。 |
| S21（P1） | 极限节点规模与全节点高负载模拟 | 新建独立 Host 规模模拟器，真实实例化多个 `ucn_node_t`，使用可配置 Tree/Ring4 虚拟链路、二叉事件堆和固定 Seed；区分直连规模、两跳路由工作集、远端配对、汇聚、全对全轮转和 Q0/Q1 混合，支持丢包、重复、延迟与链路抖动。Host Harness 可动态分配批量实例，但不改变 MCU Core 的无堆/固定表设计。 | 8 节点 CTest Smoke；校验无崩溃、无非法结果、无可执行 Route Loop、无重复业务交付、无 Harness 事件队列背压；Lite/Full 均必须构建。 | 按 8→16→32→64→128→256→512→1024 阶梯运行，直连规模继续探测 2048/4096；每节点高负载与故障组合单独报告。输出每节点资源/表峰值、受理/交付、Wire 效率、P50/P95/P99、转发、控制量、背压和 Host 工作时间 CSV。 | [S21 极限规模模拟测试方案](UCN_S21_极限规模模拟测试方案.md)、[S21 测试结果](UCN_S21_极限规模模拟测试结果.md)、[逐节点与汇总 CSV](results/S21/)、可复现命令、Debug/Release/Lite/Full 与 Sanitizer 结果；PC 数字不得冒充 MCU CPU/栈/功耗或真实 Wi-Fi/CAN/UART 性能。 | 软件测试完成：直连阶梯到 Harness 上限 4096、1024 Node M4/M8、远端工作集、Q0/Q1、故障和负向门禁均有 CSV；Full/Lite CTest `2/2`、Nano `1/1`、WSL ASan/UBSan `2/2`。80‰ 重复+M4 暴露业务重复投递，已转入 S22；实机性能仍待 S06/S07。 |
| S22（P0） | 高并发乱序重复抑制窗口与 RREQ 状态重构 | 已将普通帧改为固定 `(Source, Session)+highest_sequence+bitmap`；Nano/Lite/Full 默认 4×32/16×32/32×64 bit。RREQ 使用独立 8/16 槽 Request/Best Cost 表，并按 Peek→Token→Commit；活跃来源表满明确失败，不静默淘汰。`Session=0` 保持兼容但重启复用 Sequence 在窗口过期前按 Replay 处理，产品应设置非零 Boot Session。 | 专项覆盖乱序/重复/旧帧、多 Source/Session、零 Session、Sequence 回绕、来源槽满/过期；RREQ 重复不耗 Token、Token 耗尽不提交 Better Cost、恢复后可处理。 | Full/Lite/Nano 回归及 256 Node 重复场景通过。 | [S22 修复报告](UCN_S22_重复抑制与稳定化修复报告.md)及 `results/S22` CSV；V5-44/V5-36 后 Host Node 当前 2648/6024/10080 B。目标 MCU 资源与真实重复链路继续留 S06。 | 代码/软件测试完成；实机门禁待 S06。 |
| S23（P1） | Feature-aware Nano/Lite/Full 行为测试 | CMake 测试按 Common、Dynamic Mesh/Security、Full Extended、Service 装配；每档直接运行启用能力。 | Nano 直接运行 Frame/QoS/静态 Route/Endpoint/Adapter/去重；Lite 增加 AODV/RERR/Neighbor/Heartbeat/Security/Control Budget/Stress；Full 再增加 Candidate/Path/Policy/Diagnostic/Dynamic Stress。 | Debug 与 Release 三档分别配置、构建、CTest；Lite/Full 继续执行 Scale Smoke。Nano 专项测试暴露并修复错误码、Q1 Queue 与 Endpoint 双回调差异。 | 本地测试集合已可见；远端 Actions 只能在推送后记录。 | 代码/软件测试完成；远端 CI 待推送。 |
| S24（P2） | Service Router 配置与 Binding 生命周期收口 | `local_node_id=0` 与广播均拒绝；公共头冻结借用式只读 Binding：数组在 Router 全生命周期保持有效且不可修改，推荐 `static const`，避免所有小 MCU 强制复制。 | Node ID 0/广播/合法边界已覆盖；现有测试和使用示例采用静态 Binding。 | Service Router/Bridge、Validator、结果关联与三 Profile Service 回归通过。 | 使用手册和公共头一致；临时配置若确有需求再另设拥有式存储。 | 已完成（C99/软件测试）。 |
| S25（P2） | Hop 上限可配置与长路径门禁 | `UCN_MAX_HOPS` 支持编译期覆盖并静态限制 `1..254`，默认仍为 16。只解除硬编码，不宣称长路径已验证。 | 默认 16 和覆盖 32 构建/CTest通过；0/255 均在公共头编译期失败；配置校验仍拒绝超过上限。 | 32 Hop 配置下 Full 路由/Trace/压力 CTest `2/2`。真正超过16跳前仍需设计 Cost 位宽、RREQ超时/预算和规模策略。 | 对外继续声明当前已验证默认为16；长路径产品验收另立任务。 | 已完成（配置/软件门禁）。 |
| S26（P1，S02子任务） | 生产安全激活失败关闭门禁 | 新增运行时 `ucn_node_set_security_required()`/`ucn_node_security_ready()` 与编译期 `UCN_SECURITY_REQUIRED_BY_DEFAULT=1`。Required 时只有完整 Provider、非零 Session、持久 Sequence、authorize/seal/open 和禁止明文的 Node/Endpoint 策略全部就绪，`step/send/receive` 才运行；Nano 生产安全构建直接失败。 | 开发默认兼容；无/不完整 Provider、默认明文和明文 Endpoint 覆盖失败关闭；完整测试 Provider/安全策略通过；清除 Provider 后再次关闭。 | 现有透明 E2E 中继/安全回归与三 Profile CTest通过；测试 Provider 仍不代表生产密码。 | Core API/配置文档/CTest；真实身份、审计 AEAD、密钥持久化/轮换、逐跳认证仍归 S02。 | 代码/软件门禁完成；S02 仍未完成。 |

### 稳定化第二阶段建议—任务覆盖核对（2026-08-10）

| 建议 ID | 主任务 | 配套任务 | 核对结果 |
| --- | --- | --- | --- |
| ST2-01 | S15 | S13、S20 | 已覆盖：瞬时背压、Bridge Pending、统计唯一性。 |
| ST2-02 | S16 | S01、S06 | 已覆盖：Step 上限、维护时延和实机观测。 |
| ST2-03 | S17 | S05、T18 | 已覆盖：Adapter HELLO 抖动、Token、退避和规模模拟。 |
| ST2-04 | S18 | S14、S02、S07 | 已实现软件闭环：远端 Q0 入队前强制验证、固定 Replay、显式认证 Session 轮换；生产 Session/实机留 S02/S07。 |
| ST2-05 | S19 | S11、T22.1 | 已覆盖：认证 Path 管理源的独立预算。 |
| ST2-06 | S20 | S15、S18、S07 | 软件闭环：阶段枚举、结构化本机最终观察和 8 B 业务结果关联已覆盖；通用远端 ACK 明确不做，实机待 S07。 |
| ST2-07 | S02 | S06 | 已覆盖：生产安全继续保留 P0 发布门禁。 |
| ST2-08 | S04 | S08、S09 | 已覆盖：Nano/Lite/Full 对象和源码真裁剪。 |
| ST2-09 | S05 | S09 | 已覆盖：动态发现/路由/故障/资源耗尽压力引擎。 |
| ST2-10 | DOC-019 | T22.4 与知识库专题 | 已覆盖：本轮修正文档漂移。 |

> 核对结论：建议文档 ST2-01～ST2-10 共 10 项，任务表主任务/文档任务覆盖 10/10；S15、S16、S17、S18、S19、S20 与 S05 的代码/软件测试均已完成并完成反向核对，实机证据仍归 S06/S07。下一项按“先完成软件能力、后补硬件证据”的约束进入 S04：Nano/Lite/Full 真 Feature Profile，再按资源优先级推进 S08/S09。每个任务实现后必须按建议文档第 15 节反向核对“问题 → 任务 → 代码 → 测试”，再更新状态。

## 本轮 V3：协议破坏性升级实施清单

> 升级决策：从本轮起，Core 线格式统一升级为 **UCN v3**；v2 帧必须显式拒绝，不能与 v3 节点在同一 Network ID 内静默互通。保持 MCU-first、无动态内存和固定表上限。生产密钥、真实 ESP32 Adapter 和实机性能仍需独立硬件门禁，不能由虚拟测试替代。

| ID | 顺序 | 实现内容 | 单元测试 | 模拟/集成验收 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V3.0 | 冻结 | 将 `UCN_PROTOCOL_VERSION` 升至 3；冻结 32 B 基础头、36 B Route Extension、CRC 覆盖范围、Flag 和 v2 拒绝规则。 | 32/36 B 编解码、边界长度、未知 Flag、v2 拒绝、CRC 覆盖 Payload/Tag。 | v3 节点单跳；v2 注入不得被接收。 | 已完成（C99/虚拟 Link） |
| V3.1 | 路由 Epoch | 在 Active 路由中保存当前/上一 Epoch 及固定 grace 路径；业务按 `(destination, route_epoch)` 转发。 | Epoch 命中/未命中、旧 Epoch 仅在 grace 内有效、路由/Link 失效清理。 | A-B-C 旧路径和 A-D-C 新路径并存。 | 已完成（默认 1 s grace） |
| V3.2 | 切换控制面 | 扩展 `PATH_ACTIVATE/ACK` 携带 Candidate ID + Epoch；中继先保留旧路径，源在 ACK 后切换，Candidate 失败不影响 Active。 | Activate/ACK 格式、重复/错 Epoch、ACK 丢失、grace 回收。 | 持续 Q1 数据下先测后切；旧/新在途帧均到达或按 Epoch 可识别。 | 已完成（虚拟四节点） |
| V3.3 | 按需安全帧 | 实现 `E2E_PROTECTED` 的 16 B Tag 帧长、固定缓冲 `seal/open` Provider、不可变 AAD 契约和错误拒绝；Core 不内置未审计密码算法。 | 明文/密文长度、Tag 坏、头字段篡改、无 Provider、控制帧误标安全。 | A-B-C 中 B 无 Key 透明转发，C 解密分发。 | 已完成（Core；测试 Provider 非生产） |
| V3.4 | 安全策略 | 增加 Node 默认与固定 Endpoint 覆盖策略：发送明文/加密/自动、接收要求、透明转发模式；保留 ACL/会话/Counter 接口。 | `ENCRYPTED_ONLY`、`OPAQUE_E2E_ONLY`、Endpoint 覆盖、拒绝降级与表满。 | 明文遥测与加密命令并发，中继不解密。 | 已完成（固定策略表） |
| V3.5 | 首包与稳定性收口 | 实现 Q1 未知路由的固定待发槽/合并 RREQ/有界超时；Q0 永不等待；补未知 Cost 保守值、控制预算和统计。 | 等待/超时/表满、同目的合并、Q0 不等待、未知 Cost 不能压过实测路径。 | 新节点入网后首个 Q1 自动发出；故障/控制风暴不饿死 Q0。 | 已完成（Endpoint Q1；源端令牌桶） |
| V3.6 | 全量验证 | 新增 v3 协议向量、路由/安全/首包单测、四节点模拟；Debug、Release、64 B Profile 构建与文档链接检查。 | 所有测试入口。 | 路径切换、透明密文、入离网、Q0/Q1 组合场景。 | 已完成：三套构建 CTest 全绿，文档链接通过。 |
| V3.7 | 文档与复核 | 同步整体架构、协议档案、更新方案、任务表和操作记录；逐项将“设计”改为“已实现/待硬件验证”。 | 文档本地链接检查。 | 代码、任务表、操作记录三方交叉核对。 | 已完成 |

## 本轮 V4：节点快照诊断升级实施清单

> 升级决策：v3 先冻结到外部源码快照 `E:\File\MESH\UCN_Backups\ucn-v3.0.0-before-v4-20260808`，主工程再统一升级为 **UCN v4**。Snapshot 升级时帧头为 32/36 B；后续 T22.2 已在 v4 内增加 40 B Path Header。v3/v4 不能在同一 Network ID 内混用。Snapshot 只在显式调用时运行，默认拒绝远端请求，不引入永久全网拓扑表或动态内存。

| ID | 顺序 | 实现内容 | 单元测试 | 模拟/集成验收 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V4.0 | 备份与版本冻结 | 建立 v3.0.0 源码/文档/测试快照；将 CMake/库/线协议升至 4。 | `ucn_version()`、v3 帧拒绝。 | 既有业务/路由/安全回归。 | 已完成（快照 52 文件；C99） |
| V4.1 | Snapshot 协议与资源 | 定义 `0x1A/0x1B`、8/12 B Payload、固定 Pending/Reverse/Reply/Result 表与统计。 | 长度、Node ID 一致性、表满、版本/Flag/E2E 拒绝。 | 固定容量不随节点数动态增长。 | 已完成（C99） |
| V4.2 | 泛洪、回包与授权 | 首包去重泛洪、短期 Reverse、多 Reply 回源、确定性短抖动、独立慢 Token、默认 ACL 拒绝。 | ACL、重复、TTL、Token、回程、超时/截断。 | A→B→{C,D} 节点收集。 | 已完成（虚拟 Link） |
| V4.3 | 全量验证与同步 | Debug、Release、64 B、ESP32 双目标编译，以及项目/知识库文档同步。 | 全部测试入口与文档链接。 | 五块板实机留作 T24.5。 | 已完成（Core/构建/文档；实机未开始） |

### 文档同步补注（2026-08-08）

| ID | 内容 | 核对与验收 | 状态 |
| --- | --- | --- | --- |
| DOC-001 | 将 v3 的 Heartbeat 一跳范围、HELLO/RREQ 区别、路径逻辑并行与共享节点/共享空口的实际串行边界写入[整体架构](UCN_整体架构设计.md)和[节点入离网与网络稳定性建议](UCN_节点入离网与网络稳定性建议.md)。 | 对照 `src/ucn_node.c` 的 `send_due_heartbeat()`、`handle_heartbeat()`、`ucn_node_step()` 和业务转发路径；确认本次仅更新文档，不改线格式、Core 或既有测试。 | 已完成 |
| DOC-002 | 更正离网后的当前行为：直连邻居先检测和本地回收，业务命中失效出口才沿上游发送 RERR；不做全网离网广播。 | 对照 `maintain_neighbor_liveness()`、`remove_neighbor_entry()`、`invalidate_routes_by_link()`、`send_route_error()` 与 `handle_route_error()`；确认动态路由自动清理、静态路由需产品层处理。 | 已完成 |
| DOC-003 | 新增同一 Node ID 的多介质 Bearer 主备建议，区分当前跨介质路由能力与尚未实现的自动同对端冗余，并建立 T21 门禁。 | 对照 `find_direct_link()`、`forward_route_request()`、`ucn_node_observe_neighbor()`、Candidate/Probe/Activate 和 Adapter 契约；不修改协议源码或线格式。 | 已完成 |
| DOC-004 | 新增指定路径、固定主备路径与可关闭自动负载均衡建议，明确它们是 T22 后续能力，不与 T21 Bearer 主备或 v3 `route_epoch` 混淆。 | 对照 `find_direct_link()`、`find_link()`、`ucn_node_add_route()`、`ucn_node_send_endpoint()`、RERR 与 Active/Candidate 表；确认本次仅新增设计/任务台账，不改 Core 或线格式。 | 已完成 |
| DOC-005 | 建立 UCN Obsidian 知识库，并约定后续协议、源码、测试、架构或建议文档的实际更新均同步到知识库任务表与操作记录。 | 知识库目录 `C:\Users\y2389_4rq4ld9\Documents\Obsidian Vault\知识库\UCN` 必须具备入口、任务表、操作记录、阅读路线、模块笔记和全局索引；检查 Obsidian 内链、绝对源码路径和当前 CTest。 | 已完成（首版）；后续持续同步 |
| DOC-006 | 冻结 v3 按需路径追踪的 Flag、Payload、反向临时表、状态、安全/调度边界和 T23 验收；同步知识库。 | 对照 `ucn_frame` Flag 拒绝、`ucn_node` 控制/转发与 64 B Profile；已复核实现/API、三档 CTest 与知识库。 | 已完成 |
| DOC-007 | 冻结 v4 节点快照的版本升级、受限广播、短期 Reverse、多 Reply 回源、固定结果表、独立限流、默认授权拒绝与 T24.5 实机门禁；同步知识库。 | 对照 `ucn_types.h`、`ucn_endpoint.c`、`ucn_node.h/.c`、`test_node_snapshot.c`、三档 CTest、ESP32 双目标构建及知识库；项目文档 46 条、ESP 文档 11 条、知识库 171 条链接均无缺失。 | 已完成 |
| DOC-008 | 新增面向使用者的独立 V4 核心说明，解释协议要解决的问题、MCU 无 Linux 闭环、Core/Adapter/Host 分层、路由/QoS/安全/诊断机制及已验证边界；同步知识库入口与台账。 | 对照 `ucn_types.h`、`ucn_frame.c`、`ucn_node.c`、`ucn_endpoint.c`、`ucn_adapter.c` 和现有 V4 测试/架构文档；检查项目 Markdown 与知识库 Obsidian 链接，不以本次文档更新替代实机验证。 | 已完成 |
| DOC-009 | 将完整 `docs/` 文档集公开到 GitHub，并让公开 README 提供 V4 核心说明入口；同步知识库中的仓库发布状态。 | 清点 16 份 Markdown 与 1 份 DOCX（共 478,995 B），扫描常见私钥/Token/API Key/密码模式；检查暂存差异并确认 `origin/main` 推送成功。 | 已完成（`39e8147`） |
| DOC-010 | 冻结“一个 MCU 为一个 Node、任务为静态 Endpoint/Service”的节点内通信建议和 T25 门禁；本机直投不走 UCN Link，远端仍复用 Endpoint 路由。 | 对照 `ucn_node_set_endpoint_handler()`、固定 Endpoint 表与 `ucn_node_receive()` 的当前协议任务回调行为；同步整体架构、协议档案、更新方案、V4 核心说明和 Obsidian 知识库。 | 已完成（设计/文档；未改 Core 或 FreeRTOS 代码） |
| DOC-011 | 新增路由策略与负载均衡执行建议，明确 T21/T22 关系、Policy/Path/Flow 固定容量、Path ID 安全控制面、Q0/Q1 分流边界和 T22.0～T22.7 验收顺序；同步知识库路线图。 | 对照 `ucn_link.h` 的通用 Cost 契约、`ucn_node.h/.c` 的 Active/Candidate/Previous/`route_epoch`/RERR 与 ESP32 `DualMediaLink` 的 UART 优先回退；检查项目 Markdown 与 Obsidian 内链。本次不修改 Core、线格式、测试或实机结论。 | 已完成（设计/文档；未改 Core） |
| DOC-012 | 实现并归档 T21 固定 Bearer 邻居模型与第一阶段主备行为。 | 对照 `ucn_neighbor.h` 的固定数组、`ucn_node.c` 的自动准入/保活/egress 解析、`test_neighbor_bearer.c` 的虚拟故障注入；执行 Debug、Release、64 B 与 `Bearer=1` CTest。 | 已完成（Core/虚拟验证；真实多板待 T21.6） |
| DOC-013 | 完成 T21.3/T21.4 的 Bearer 质量切换与 Route/Candidate/RERR 联动，并同步项目和知识库边界。 | 对照 Candidate 出口必须在 Probe/Activate/转发时解析当前 Primary；虚拟 A→B→C 覆盖单 Bearer 切备、Probe/Activate、下游 RERR 回传、全 Down 动态清理及静态 Route 保留边界；执行四个 CMake Profile。 | 已完成（Core/虚拟验证；真实多介质待 T21.5/T21.6） |
| DOC-014 | 归档 T21.5 ESP32-S3 单/双 Bearer 静态资源对比、WROOM 编译和运行时资源观测口。 | 对照权威 Core 直接编译脚本、目标 ELF 的 `g_node`/段大小、PlatformIO RAM/Flash 输出与测试固件 `RESOURCE` 行；不把单逻辑 `DualMediaLink` 写成已实测双 Bearer。 | 已完成（静态构建/仪表已验证；实机峰值待 T21.6）。 |
| DOC-015 | 新增面向产品开发者的使用与调用手册，明确 Node/Link/Adapter/Service Router/Bridge/Policy/Path/Security/诊断的职责、初始化顺序和公开 API 调用方式。 | 对照当前 `include/ucn/` 公开头文件、`tests/test_policy.c`、`tests/test_path_control.c` 与 ESP32 FreeRTOS Port；检查 README、项目文档和知识库入口链接；本次不修改协议/源码/构建配置，也不把文档当作新的实机证据。 | 已完成（文档/API 核对）。 |
| DOC-016 | 发布前清理本地构建产物边界：忽略命名 CMake Profile 目录 `build_*`，仅提交可复现的源码、测试、工具和文档。 | 检查 Git 未跟踪项、现有 `.gitignore` 规则与敏感信息模式扫描；确认不删除任何本地构建目录，不把构建缓存、ELF/BIN 或凭据提交到 GitHub。 | 已完成（发布卫生检查）。 |
| DOC-017 | 参考 F405 项目的 `docs/calltree`，建立 UCN 的 YAML 调用关系树：模块索引、模板、接收/发送/调度主链和 Core/Adapter/Neighbor/Route/Policy/Path/Security/Service/Diagnostic 分模块节点。 | 对照当前 `src/` 的函数调用、`include/ucn/` 的公开 API 与 ESP32 FreeRTOS Port；检查 YAML 可解析、节点 ID 唯一及内部 `calls.target` 闭环；本次不改协议/源码/测试结论。 | 已完成（文档/API 调用链核对）。 |
| DOC-018 | 建立裸机、通用 RTOS、FreeRTOS、Zephyr、NuttX、RT-Thread 六份独立快速使用文档，并提供总览入口。 | 对照 `ucn_adapter.h`、`ucn_link.h`、`ucn_node.h`、`ucn_service.h`、`ucn_service_bridge.h` 与 ESP32 `UcnServiceFreeRtosPort`；明确 FreeRTOS 仅有 ESP32 参考，其他系统为接入模板；检查入口和相对链接。 | 已完成（文档/API 核对；未新增 RTOS Port、驱动或实机结论）。 |
| DOC-019 | 归档稳定化第二阶段 ST2-01～ST2-10，建立 S15～S20 并将生产安全、Feature Profile、动态压力等建议映射到既有 S02/S04/S05/S08/S09；同步修正 T22.4/Unknown Cost 在现行架构、路由建议、调用树与 UCN 知识库中的描述。 | 对照 Core Q0 出队、Service Bridge、Step 调度、ESP HELLO、Command Guard、Path Control、Feature/压力测试现状；执行 10/10 建议—任务覆盖检查、项目/知识库链接检查、YAML 调用目标闭环和 `git diff --check`。本次只建立执行基线，不把待开始任务写成已实现。 | 已完成（问题/建议/任务归档；代码从 S15 开始）。 |
| DOC-020 | 完成 S08 公共 API/静态存储拆分并归档 Seen/Replay、小 MTU 和历史版本边界。 | 对照 API-only/storage-only 编译单元、Nano/Lite/Full + Service 组合、最小 MTU 正负门禁、公共符号、ESP32 owner 包含点与文档/知识库；不把 Host 构建写成目标板实机资源。 | 已完成（代码/软件测试与文档同步；硬件性能归 S06/S07）。 |
| DOC-021 | 新增网络容量与关键参数总览，集中说明地址空间、直连Link/Neighbor/Bearer、Route工作集、16跳、AODV成本、帧效率、RAM/Flash、保活/切换参数、S21规模证据和S22限制。 | 对照当前`ucn_types/node/neighbor/link/policy/path/service`头文件、`ucn_frame.c`、S04/S08/S21报告与CSV；区分地址上限、Host模拟、两板实机和未验证多板规格，同步README、操作记录和知识库。 | 已完成（文档/源码与测试证据核对；未修改Core或实机状态）。 |
| DOC-022 | 完成 S22～S26 稳定化修复报告与当前资源/安全/去重/长路径边界同步。 | 对照 Source/Session 位图窗口、RREQ Peek→Token→Commit、Feature-aware CMake、Service 生命周期、Hop 正负构建、生产安全 Ready 门禁、三 Profile CTest、S21 M1/M2/M4 重放和 WSL Sanitizer；同步调用树、使用/架构/资源文档和知识库。 | 已完成（代码/软件测试；S02 生产密码与 S06/S07 实机仍待完成）。 |
| DOC-023 | 冻结 v5 Adaptive Wire Profile 方案和执行顺序，区分 Build Profile、Wire Profile、Traffic Class 与 Security Policy，并登记 V5-00～V5-07。 | 核对 v4 实际帧布局、W0～W3 可表示范围、W3 Hop 上限 254、CRC/AAD 和透明 Gateway 安全矛盾；先文档/任务，后 v4 固化、备份、分支与代码。 | 已完成（设计与任务；代码从 V5-01 开始）。 |
| DOC-024 | 归档 V5-00/V5-01 的 v4 基线、备份、远端分支、官方 Wire Profile Codec、Golden Vector、资源与软件回归证据。 | 对照 `ucn_types.h`/`ucn_frame.h`/`ucn_frame.c`、专项测试、三 Profile、WSL Sanitizer、256 Node 重放和远端引用；明确 V5-02 尚未完成。 | 已完成（软件证据；无实机）。 |
| DOC-025 | 完成 V5-02～V5-07 的固定域、控制帧压缩、安全绑定、自动最小档、Gateway 评审和发布门禁同步。 | 对照公开 API、W0 受保护三节点、三 Profile/Release/Sanitizer、最小 MTU 正负编译、256 Node fixed/auto CSV、Host 资源、架构/使用/六平台快速手册/调用树/知识库；生产密码和实机保持待办。 | 已完成（软件与文档证据；S02/S06/S07 待硬件/产品实现）。 |
| DOC-026 | 冻结 V5-08 全档接收互操作：最低够用 TX/W3 RX、三 Build Profile 统一 Decoder、普通 Endpoint 与高级 Feature 边界。 | 对照 Node 收包顺序、Nano/Full 分发、W0～W3 编解码和窄 RX 拒绝；三 Profile Debug/Release 与 Sanitizer。 | 已完成（软件证据；真实 MCU 解码耗时/功耗待 S06/S07）。 |
| DOC-027 | 建立 V5-09 全局公共配置说明与双保险，集中 103 项原有分散 `#ifndef` 默认并保留原文件兜底；同步 CMake、手册和知识库。 | 自动比对全局/分散宏，默认/回退/产品覆盖 3/3，产品头构建、最小 MTU、非法 Hop 和 Sanitizer；核对运行期身份/密钥/引脚不进入公共头。 | 已完成（代码/软件测试/文档；无实机修改）。 |
| DOC-028 | 归档 V5-10 W0～W3 单档/四档混合极限模拟、逐节点/逐档 CSV、W0 单域 254 边界和 HELLO RX Ceiling 修正。 | 对照模拟器真实 Node 实例、19 组 Release CSV、Lite/Full CTest、Full ASan+UBSan、255 地址负向、HELLO W0 TX/W3 RX 双向互操作、任务/操作记录/架构/知识库；不把 Host 4096 或虚拟时延外推为实机。 | 已完成（代码/软件模拟/文档；实机仍归 S06/S07）。 |
| DOC-029 | 归档 v5 分支审计问题的源码确认、优先级、实施顺序与明确不修改项。 | 对照 `fe3cb0d` 的 Frame/Node/Link/Storage/Path/AODV 测试；每项区分真实 Bug、能力缺口、已知延期和不采用建议，并映射 V5-11～V5-16/S02/S06/S07。 | 已完成（审计确认与任务拆分；代码从 V5-11 开始）。 |
| DOC-030 | 归档 V5-14 的 32 bit 累计 Cost、Profile-aware RREQ/RREP、长链与模拟器交付门禁。 | 对照四档 Cost 哨兵/边界、80,000/100,000 双路径、201 Node/200 Edge 线形拓扑以及 Hop=200/201 正负结果；不把 Host 虚拟时间外推为实机性能。 | 已完成（代码/软件测试；真实 MCU 长链仍归 S06/S07）。 |
| DOC-031 | 归档 V5-15 的 RERR、Path Control、Path Trace、Node Snapshot Profile-aware Payload 和调用树。 | 对照四档长度、坏长度、最小精确 MTU、Auto W3→W0、诊断截断和 Path authorizer/token/写表顺序；明确未含 Node/Path 字段的固定 Schema 不为压缩而重写。 | 已完成（代码/软件测试；生产授权仍归 S02）。 |
| DOC-032 | 冻结 V5-16 Authorized Class 与控制预算方案，严格分离编码能力、接收上限与管理权限。 | 对照 C0～C3、认证身份/Session Generation、ACL、Token、Fanout、升级/降级、轮换/重启/多 Bearer 与表满；S02 前不得宣称实现。 | 已完成（设计；代码依赖 S02）。 |
| DOC-033 | 归档最新审计四项交叉问题、V5-22～V5-26 实施与反向核对；同步 Cost 契约、Wire 长度、Candidate Profile、Hop Scope、任务/操作记录和 UCN 知识库。 | 对照源码、专项交叉测试、三 Build Profile、配置门禁、Scale、Sanitizer 与资源报告；明确 S02/V5-21 保留不改、S06/S07 仍待实机。 | 已完成：建议、任务、源码、测试、调用树、当前文档与知识库已同步；历史操作记录保留原阶段数据。 |
| DOC-034 | 全面核对全部 16 份原有 v5 专题文档及 README/架构/分层/容量/使用/配置/Cost/调用树，使历史阶段证据与当前 `f941ae9` 代码事实分栏。 | 对照 Descriptor、公共头、控制 Payload helper、Feature 裁剪、Q1/Candidate/Hop 状态机、正式测试和当前 Host 资源；检查项目链接、调用树、知识库内链与 `git diff --check`。 | 已完成：新增一致性审计，修正全部确认差异并同步知识库；Windows Full/Lite 10/10、Nano 1/1，WSL 13/13，配置契约 4/4；标识符、链接、调用树与内链无意外缺失。 |
| DOC-035 | 归档 V5-31～V5-33 的 PATH_INSTALL v5 双格式兼容、非 Full capability API 符号与发布门禁。 | 对照基础/扩展四档长度、坏中间长度、公共头直接调用、Lite/Nano Stub、三档静态库符号、Windows 六矩阵、配置契约、WSL Sanitizer/Analyzer、调用树与知识库。 | 已完成（代码/软件/文档）；旧 v5 只保证基础格式，扩展格式需产品确认目标支持；实机仍归 S06/S07。 |
| DOC-036 | 按当前 v5 公共接口重新审计并更新裸机、通用 RTOS、FreeRTOS、Zephyr、NuttX、RT-Thread 快速使用手册及详细调用手册。 | 核对 Build Profile 源文件矩阵、W1 字段范围、Link 注册/open 生命周期、统一 `step_at` 时钟、动态 MTU、Boot Session/安全、远端 Q0 Validator、Zephyr/RT-Thread 模板和 FreeRTOS 外部工程版本边界；执行链接、代码块、标识符、旧模式与三 Profile CTest。 | 已完成（文档/软件验证）；Full 13/13、Lite 13/13、Nano 4/4；ESP32 外部测试工程仍是 v4 历史参考，未在本任务中迁移或修改。 |
| DOC-037 | 冻结标准 Port/Adapter 封装接口和默认 Link Cost 初始基线，登记从设计到实现、实机标定的 V5-34～V5-41 路线。 | 核对当前 Link Metrics 基础/动态指标分离、Policy 500 ms/25% EWMA、Q0/Q1/Path 边界、CAN/USB/Wi-Fi 现实速率约束及文档/知识库同步。 | 已完成（设计/任务）；未修改 Core 或驱动，默认 Cost 仅是待标定的初始 Preset。 |
| DOC-038 | 补充标准 Adapter 的扩展 Bearer 路线和“不得双重 Mesh”边界。 | 对照以太网、RS-485、BLE LE、802.15.4、私有 2.4G、LoRa P2P/FSK、UWB、SPI/I²C、IP Tunnel 与 Wi-Fi/BLE/Zigbee/Thread/LoRaWAN 的职责差异。 | 已完成（设计/任务）；未新增驱动、Preset 或硬件结论。 |
| DOC-039 | 将 Link Cost 从建议值冻结为 LC-1 可执行计算规范。 | 明确基础/动态分离、窗口、EWMA 取整、每项惩罚、唯一减项、状态门、陈旧/Unknown、确定性同分、滞回/硬失效、Q1 重绑、测试向量和不纳入自动计算的因素；与现有代码事实和知识库同步。 | 已完成（设计并已由 V5-44/V5-36 实现）；Wire 格式未改变。 |
| DOC-040 | 归档 V5-35 标准 Preset Resolver、产品 Link 覆盖和产品配置专用链接门禁。 | 对照公开静态 Resolver、Preset 元数据、非法配置失败关闭、Full/Lite/Nano 与 128 B 产品头。 | 已完成（代码/软件/文档）；该阶段本身不代表真实 Adapter、后续 V5-44/V5-36 动态 Cost 或实机结果。 |
| DOC-041 | 归档 V5-45 代表性产品全量回归，明确测试配置化、压力拓扑前提与未改变的生产协议行为。 | 对照 Frame/Wire/AODV/动态压力/Service 测试，运行默认三 Profile 与 Full 128 B 产品头完整 CTest。 | 已完成（软件测试）；128 B 配置的 3 Link 不覆盖 32×4 Host 压力，真实硬件仍归 S06/S07。 |
| DOC-042 | 冻结 V5-37 标准 Port 外壳的唯一 Owner、ISR RX 入队、固定预算、同一时钟、等待上限与 SDK 边界。 | 对照既有 Adapter RX Queue、Port Ops、可选 Service Bridge 与四类平台映射。 | 已完成（设计并随实现核对）；不创建真实 RTOS 对象、BSP 或介质 Adapter。 |
| DOC-043 | 归档 V5-37 标准 Port 外壳的软件实现与 Host Fake 回归。 | 对照 `ucn_standard_port.h/.c`、公开头、Host Fake、Full/Lite/Nano/Service OFF/产品配置和 WSL GCC。 | 已完成（软件测试）；实际中断/RTOS/驱动/目标资源仍须后续实测。 |
| DOC-044 | 归档 V5-46 的 Platform Port 分目录解耦：公共 Owner 与裸机/FreeRTOS/Zephyr/NuttX/RT-Thread/Host Fake 独立实现。 | 对照目录、无平台枚举、公共 API 链接、各 Port 回调/等待边界、Service 和多 Profile 回归。 | 已完成（软件测试）；不代替真实 SDK Task/Thread、BSP 或物理 Adapter 验收。 |
| DOC-045 | 归档 V5-47 工程架构治理：系统边界、模块依赖、目录迁移兼容、构建/测试地图和内部源码分层。 | 对照公开头路径不变、Core/Node/Transport/Routing/Service/Ports 源目录、CMake 源清单、调用树、测试分类和多 Profile 回归。 | 已完成（软件/文档）：模块目录、CMake、调用树、项目/知识库导航与多 Profile 回归均已核对；不宣称真实 SDK、Bearer Adapter 或实机验收。 |
| DOC-046 | 归档 V5-48 的 ISR Queue token 合同、容量自适应 Scale Smoke 和 Path 配置源兼容修复。 | 对照 `ucn_port_ops_t`、Adapter/Owner、全部独立 Port、Scale 工具、128 B/3-Link 配置、公开位置初始化测试及多 Profile 回归。 | 已完成（文档/软件）：新增 V5-48 修复报告，严格 GCC 覆盖旧八字段位置初始化；真实 FreeRTOS/Zephyr/NuttX/RT-Thread ISR/BSP 仍须目标实测。 |
| DOC-047 | 归档 V5-44/V5-36 的 LC-1 Resolver、Metrics 扩展、动态 Bearer/Candidate/Q1 Flow 接入和资源/回归边界。 | 对照 C01～C10、20%/3 样本/2 ACK/3000 ms、Unknown/陈旧/硬失效、Wire 不变、三 Profile/产品头/Sanitizer/Analyzer 与 Host 资源。 | 已完成（Host 软件/文档）；真实 Adapter、Cost 标定、MCU ELF/栈/CPU/功耗仍待 V5-38～V5-42。 |
| DOC-048 | 归档 V5-52 N8R8 三节点九档 Transfer 实测与 EXT-07 当前边界。 | 对照 COM5/COM34/COM38 完整 BOOT→COMPLETE 日志、三目标 PlatformIO 构建/Hash、两跳 Route/Cost、九档 CRC/片数/耗时、Carrier 错误与 Heap/栈；不把 UART 台架外推为 ESP-NOW/CAN/CPU/功耗结果。 | 已完成（UART 两跳实机功能门禁）；其余 EXT-07 场景继续待测。 |
| DOC-049 | 归档 V5-53 三节点九档等载荷压力、吞吐对比和 Direct 帧边界。 | 对照两轮正式日志/CSV、远端实际交付、Fragment/重传、Queue Drop、Heap/栈与 Route；区分无 ACK 快速帧峰值和可靠 Transfer 吞吐。 | 已完成（115200 UART 两跳压力门禁）；ESP-NOW-only、切换、多源并发/Q0、CAN-FD、CPU/功耗仍待 EXT-07。 |
| DOC-050 | 归档 V5-54 921600 UART 三节点重复压力、115200 对比和背景业务边界。 | 对照三轮日志、27 行汇总/对比 CSV、可靠加速比、Direct Queue Drop、Ping 波动、Wire 错误和资源；不把短线台架结论外推为所有产品默认。 | 已完成（921600 两跳性能门禁）；下一优先项为 Transfer 与 Q0/Q1 并发调度。 |
| DOC-051 | 归档 V5-55 UART 1.5M～5M 极限扫描、重复稳定性和推荐工作档。 | 对照 15 个构建目标、11 轮日志、99/11/5 行 CSV、可靠交付、重试、Wire/Queue/Ping/Route 与最终固件；区分 SDK 上限、功能可恢复上限和工程推荐档。 | 已完成（当前短线两跳台架）；3M 为重复零错误推荐档，5M 仅实验极限，产品仍须按线长/温漂/EMC 标定。 |
| DOC-052 | 归档 V5-56 默认兼容固定窗口、累计 ACK、Go-Back-N 软件边界和三节点窗口 1/4 对比。 | 对照公开 API/配置、旧 Peer 退化、丢首片/中间片、三 Profile/产品头/Sanitizer/Analyzer、最终六轮日志、54 行汇总、九档/逐轮对比、PlatformIO 资源和当前固件；明确 Wire 不变与未自动协商。 | 已完成（软件与 3M UART 两跳实机）；可靠平均吞吐 +52.7%，其他 Bearer/Q0/多源/切换/CPU/功耗仍归 EXT-07。 |
| DOC-053 | 归档 V5-57 外设事件唤醒 Owner、ISR/Task 边界、定时兜底、窗口 8 与三节点实测。 | 对照 Arduino-ESP32 UART ISR→Event Task、ESP-NOW 回调、Owner Notify/Drain/Timer Deadline、队列背压、三轮 27 阶段、T512 调试日志相位干扰和三 Profile软件门禁；明确轮询只作保底。 | 已完成（软件与 3M UART 两跳实机）；其他 RTOS/BSP ISR、介质、Q0 严格延迟、CPU/功耗仍待验证。 |
| DOC-054 | 归档 V5-58 公共 Event Runtime、固定多 Source、Scheduler Hook、Task/ISR 边界、预算/兜底与平台映射。 | 同步方案、Adapter 契约、架构边界、使用手册、六个平台快速手册、全局配置和 Call Tree；明确公共 Runtime 已软件验证，Driver Ring/Carrier/SDK/实机仍未完成。 | 已完成。 |
| DOC-055 | 归档 V5-59 公共 Stream Ring/COBS Carrier、缺口顺序、Queue 背压、资源与调用方式。 | 同步方案、Adapter 契约、架构/目录、使用手册、全局配置、Call Tree、任务与操作记录；区分 Host 软件证据和真实 UART/USB/RS-485 驱动门禁。 | 已完成。 |

### V3 Core 完成边界（2026-08-07）

| 项目 | 已完成的代码边界 | 未用“已完成”表述的边界 |
| --- | --- | --- |
| 线协议 | `UCN_PROTOCOL_VERSION=3`；32 B 基础头、36 B Route Extension、受保护业务额外 16 B Tag，CRC 覆盖头/业务/Tag；v2 显式拒绝。 | v2↔v3 Gateway 和双网迁移工具。 |
| 路由 | RREP 额外携带 Route Epoch；动态业务按 Epoch 走 Current 或 Previous；`PATH_ACTIVATE/ACK` 为 6 B（Candidate ID + Epoch），Previous 默认保留 1 s。 | 真实空口上的零丢包/零乱序承诺；上层 Q1 仍应带业务序号/时间戳。 |
| 安全 Core | 固定 `seal/open` Provider、26 B 不可变 AAD、Node/Endpoint 策略、`E2E_PROTECTED`、透明密文中继、坏 Tag/明文降级拒绝。 | 真实密钥供应、生产 AEAD/身份库、JOIN 状态机、密钥轮换、撤销和产品 ACL 表。 |
| 首包与预算 | Endpoint Q1 自动发 RREQ，固定 4 槽/1 s Latest-Value 等待；Q0 和旧原始发送 API不等待；未知 Cost 默认 1000。源端 RREQ/Probe/Activate 受 4 Token/100 ms 限制；周期 Heartbeat 由固定 Link/Bearer 上限和各自存活档位节拍独立约束，入站 Heartbeat Request 仍受每 Peer 固定预算。 | 每 Adapter/每 Link 的真实空口占用和规模参数仍需实机标定。 |
| 验证范围 | C99 单测与虚拟 2/3/4 节点路径、透明密文、Tag 篡改、等待/超时/表满、候选 Epoch 切换。 | ESP32/ESP-NOW、CAN、BLE、LoRa、Linux Host 的实际驱动/吞吐/RAM/Flash/功耗/EMC 测量。 |

## 本轮 R1：最新 MCU 局域网 Core 实施清单

本轮以现有 C99 虚拟 Link 为验证载体，实现不依赖具体 ESP32、Linux、密码库或动态内存的最新 Core 能力。每一小项遵守“实现 → 单测 → 模拟 → 记录”的门禁；R1 结束后再重新核对任务、代码与测试。

| ID | 顺序 | 本轮实现内容 | 完成判据 | 状态 |
| --- | --- | --- | --- | --- |
| R1.0 | 核对 | 对照 v2 源码、全部测试和最新方案，登记已实现、差距及外部前置条件。 | 基线 Debug 构建与 CTest 全绿；差距写入本表和操作记录。 | 已完成 |
| R1.1 | T18.1 | 扩展邻居 `ADMITTED → SUSPECT → REMOVED`，实现认证后的一跳 `HEARTBEAT` 收发、业务帧刷新 `last_seen` 与固定超时。 | 正常保活不误删；漏包进入 SUSPECT；恢复回 ADMITTED；最终 REMOVED。 | 已完成（虚拟 Link） |
| R1.2 | T18.2 | 移除已自动准入邻居时清动态路径、解除本地 Link 注册、清 Node ID，并复用固定 Neighbor/Link 槽。 | 断开仅影响相关路径；同一槽可绑定新 Node；重复移除安全。 | 已完成（虚拟 Link） |
| R1.3 | T17.1～T17.3（Core 子集） | 固定大小 `ACTIVE + CANDIDATE` 路由、活跃路径后台刷新、受限 `PATH_PROBE/ACK`、三次验证和 Cost 滞回切换；普通业务持续走 ACTIVE。 | 双路径中候选不覆盖旧路径；候选失败回退；达到门限后只切一次；控制面不抢占 Q0/Q1。 | 已完成（v2 控制面、虚拟 4 节点） |
| R1.4 | T19.1 | 建立 `0x40～0xBF` 静态 Endpoint 公共命名空间与发送 API；验证同目的地不同 Endpoint 的 `latest_value` 不互相覆盖。 | 无每帧额外字节；控制类型与 Endpoint 显式分离；兼容 v2 旧发送 API。 | 已完成（虚拟 Link） |
| R1.5 | 验证 | 新增单元测试、三/四节点虚拟拓扑模拟、全量 Debug/Release/64 B Profile 构建测试，并复核所有 R1 项。 | 所有构建/测试实际通过；任务和操作记录只标记实际完成的能力。 | 已完成 |

### R1 更新前功能核对（2026-08-07）

| 功能 | 更新前结论 | 本轮处理 |
| --- | --- | --- |
| MCU 无 Linux 自组网 | 已有虚拟 Core：HELLO 准入、AODV-Lite、转发、Host 断线不影响 MCU 路径。 | 保持边界；不引入 Host 依赖。 |
| 物理链路统一 | 已有 Link/Adapter 契约和通用 `route_cost`；没有真实 ESP/BLE/CAN 驱动。 | Core 只消费通用状态/代价；实机仍归 T14。 |
| 一跳入网 | 已有 Candidate、HELLO、Manual/Open/Provider 准入；Candidate 仅会过期。 | 扩展为已准入邻居保活、SUSPECT/REMOVED 和槽复用。 |
| 静默离网 | 未实现；只有发送时 Link Down 或 30 s 路由过期。 | R1.1/R1.2 实现固定 Heartbeat 状态机。 |
| 最优多跳路径 | 仅 RREQ 阶段以 `route_cost` 选低成本路径；每目标只有一条动态路径，无后台切换。 | R1.3 实现固定 Active/Candidate 与先测后切的 Core 闭环。 |
| 同节点多类数据 | 原始 `message_type` 可用，但业务编号范围、Endpoint API/ABI 未冻结。 | R1.4 固定静态命名空间和 API；产品具体编号仍由后续 ABI 表冻结。 |
| 按需端到端加密 | 仅 Session/Counter/ACL Provider 接口和测试 Provider；无 AEAD、密钥模型或透明密文格式。 | 不伪造实现；T15 等算法、密钥供应和安全帧格式冻结。 |
| v3 路径 Epoch / 加密帧 | 未实现，当前 v2 仅接受 32 B 帧头。 | R1 不改业务线格式；v3 36 B Epoch、PATH_ACTIVATE 与 E2E Tag 归后续破坏性升级任务。 |
| 真实 ESP32 与小 MTU | 未实现，且缺芯片/ESP-IDF/硬件条件；经典 CAN 需 Carrier 分段。 | 分别保留 T14/T16 的实机门禁。 |

### R1 更新后复核（2026-08-07）

| 项目 | 实际结果 | 未覆盖边界 |
| --- | --- | --- |
| 自动入离网 Core | 已实现 Heartbeat 请求/ACK、`ADMITTED → SUSPECT → REMOVED`、业务帧恢复存活、Driver Down/超时清路由、注销 Link、清 Node ID 和槽复用。 | 无 HELLO 随机退避/令牌桶，真实 Adapter 地址池与实机时间待 T14/T18。 |
| 自动选路 Core | 已实现固定 Candidate 表、活跃路径刷新窗口、20% Cost 滞回、3 次 Probe/ACK、`PATH_ACTIVATE/ACK` 和中继 Candidate 转发表；候选失败不覆盖 Active。 | v2 普通业务帧仍无 `route_epoch`/grace；未知 Cost 保守值、切换乱序统计和实机参数待 T17 后续。 |
| Endpoint | 已实现 `0x40～0xBF` 判断、专用发送、固定 Endpoint RX 分发表、控制报文禁止走业务 API、旧 `DATA_Q0/Q1` 兼容。 | 产品 Endpoint ABI、Endpoint ACL、动态目录/订阅待 T19/T15。 |
| 首次业务发送 | 现有 `ucn_node_send()` 仍要求已有直连/Route Cache；未知路径需调用 `ucn_node_discover_route()`。 | T20 负责 Q1 自动寻路/有界等待；Q0 不等待是固定约束。 |
| 验证 | Debug、Release、`UCN_MAX_FRAME_BYTES=64` 三个配置均构建并通过 CTest；新虚拟测试覆盖 2 节点保活/回收、Endpoint、4 节点候选失败/成功切换。 | 未接实机、未测 RAM/Flash/栈/CPU/电磁干扰/吞吐，也未接生产 AEAD。 |

## 本轮实现范围

本轮优先完成 `UCN-Core` 的可运行、可测试最小闭环：固定帧、统一 Link、Q0/Q1、固定路由缓存、受限 AODV-Lite、可选链路质量选路、邻居 Candidate/Admitted 生命周期、最小 HELLO 自动准入、虚拟多节点模拟和 Host 非中心化验证。

以下内容不在当前结果中伪造为“已完成”：真实 BLE/CAN 驱动、ESP-NOW-only 大消息性能、ROS2 Bridge、DroneCAN Gateway、生产级 AEAD/证书实现、OTA、Q2/Q3、服务目录与小 MTU Carrier 分段。业务消息已有独立 `ucn_transfer`，支持 T32～T8K、固定重组、CRC32、默认窗口 1/显式窗口 2～8、累计 ACK、有界 Go-Back-N 和固定资源；V5-52～57 只证明一组 ESP32-S3 的 115200～5M 短线 UART 两跳九档功能/压力、3M 窗口与事件 Owner，其中 T32/T64 Direct 帧不可靠，窗口能力尚未自动协商，5M 发生过可恢复 COBS 错。完整 Path MTU 主动协商、Q0/Q1 并发隔离、多源/切换压力及其他目标/介质实机验证仍分别保留在 EXT-05～EXT-07，不能扩大表述为生产级可靠文件传输。

## 完成定义

一个任务只有同时满足以下条件才能标记“完成”：

1. 源码和公共接口已写入工程。
2. 相关单元测试已通过。
3. 对应模拟场景已通过。
4. 修改和验证结果已追加到 [项目操作记录](01-项目操作记录.md)。
5. 没有把测试 Provider、虚拟 Link 或模拟性能表述成真实硬件验证。
