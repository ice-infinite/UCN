# CLV2-14-10 文档一致性自审报告

## 结论

14-10 软件文档范围完成并自审通过。旧 Current FSM 的“Role + bool 是第二状态源”和 Cluster calltree 的“28 B / format v1”描述已删除，文档现在明确区分 Current v3 生产路径、隔离 v4/Target 组件与尚未解除的 M05/实机阻断。

## 更新内容

- 重写 `UCN_V5_Cluster_CURRENT_FSM.md`：22 个 Phase、Phase-only、opaque storage、Record v4 与 production v3/v4 边界；
- 更新 `UCN_V5_Cluster_FSM_Design_v2.md`：增加当前实现对齐说明，不把 Target 规格冒充默认产品；
- 更新 `UCN_Cluster_Wire_v4.md`：RFC4 已冻结且隔离 codec 完成，但 encoder/default RX/TX/FSM 仍关闭；
- 新增 Persistence v4、Config v2、Timer Algebra、API/storage v2 迁移文档；
- 重写 `docs/calltree/cluster.calltree.yaml`；
- 新增 `UCN_Cluster_Code_Doc_Contract.md` 与自动检查脚本。

## 自动核对

`tools/check_cluster_docs_contract.py` 直接解析公共头并核对：

- `ucn_cluster_phase_t` 22 个值与 Current/冻结表；
- Wire v4 Type 1..33 与 RFC/冻结表；
- API=2、storage=2、production format=3、recommended format=4；
- v3=32 B、v4=40 B、writer schema=4、record=388 B；
- M14 14-01..12 任务 ID 和 M05 `AUDIT HOLD` 边界。

定向 CTest 的 Phase、storage、Wire module、resource、docs 五项 gate 全部通过。

## 限制

自动检查锁定的是公开编号、版本和发布边界，不会证明所有自然语言都不存在语义遗漏。14-11 仍需以 Safety/Liveness checklist 做跨文档与跨模块人工自审。
