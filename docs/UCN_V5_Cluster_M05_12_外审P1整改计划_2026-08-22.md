# CLV2-05-12：外审 P1 整改计划

日期：2026-08-22

## 外审发现

默认生产配置的 `test_cluster_wire_v4_encoder_closed()` 正确验证了 `ucn_cluster_wire_v4_encode()` 返回 `UCN_ERR_CONFIG`，但只检查 40 B output 的首、尾字节。该覆盖不足以证明 fail-closed 分支不写 caller buffer 的完整合同。

## 整改

1. 初始化 output 为固定哨兵后，复制完整 40 B expected buffer。
2. 调用默认关闭的 encoder，断言返回 `UCN_ERR_CONFIG`。
3. 对全部 40 B 使用精确 `memcmp`，禁止任何首尾或中间字节被写入。

## 范围与验收

- 只修改默认 encoder-closed 回归和台账；不改 encoder 实现、RFC4、v3 Wire、Cluster runtime/FSM、Adapter、资格或 Authority。
- 重跑 Windows GCC Full/Lite/Nano、WSL ASan/UBSan、WSL `-fanalyzer`；复扫 production v4/Adapter/runtime 引用和 encoder default，执行 `git diff --check`。
- 通过后仍为 `CODE COMPLETE / AUDIT HOLD（待外部复审）`，不构成 M05 整体放行。

## 实施与自审结果

- 测试已改为完整 `expected_output[40]` 哨兵副本与 `memcmp`，原首尾断言已删除。
- Windows GCC Full/Lite/Nano 分别 `29/29`、`29/29`、`19/19`；WSL ASan/UBSan `19/19`；WSL `-fanalyzer -Wall -Wextra -Werror` `6/6`。
- production v4 RX/TX/FSM refs、Adapter Cluster refs、Host runtime calls 均为 0；encoder default 仍为 0；`git diff --check` 通过（仅既有 CRLF 提示）。
- 外部复审：P1 已确认完整关闭，05-12 获 **DONE / 外部复审 GO（受限范围）**；外审独立复跑 Windows Full `3/3`、WSL ASan/UBSan `26/26` 通过。
- 限制：M05 顶层仍为 **AUDIT HOLD**。本整改/签字不授权 production v4 RX/TX/FSM、Authority、资格或实机兼容性声明。
