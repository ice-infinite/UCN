# UCN 项目操作记录

> 记录规则：每次新增、修改、删除源码/文档/测试，或执行关键验证后，均在本文件追加记录。  
> 记录字段：时间、操作、修改内容、验证、结论/限制。

## 2026-09-05

### OP-481｜完成 V6-13 软件范围：统一 Adapter、FreeRTOS 与 ESP32-S3 参考边界

- 新增独立 `ucn_v6_adapter`，以固定 RX/TX 槽管理完整帧、Event Key、Buffer Token、物理
  completion、取消、readiness、quiesce/reopen 与禁止回绕的 Link Generation。
- 任务锁与 ISR try-lock 分离，Driver 回调只发布完整 RX/completion；单一 Protocol Owner
  由 FreeRTOS task notification 立即唤醒，timer 只作有界保底。
- UART/RS-485、ESP-NOW、Classic CAN/CAN-FD、USB 默认能力拆分到独立文件；ESP32-S3
  reference builder 只验证 UART 号、GPIO、DMA、信道和 Peer 容量，不写死开发板引脚，
  不引入 ESP-IDF 头或在协议内创建任务。
- Adapter 容量进入 v6 Manifest/Layout Hash；默认静态预算为 48,128 B，FreeRTOS Port 为
  2,048 B；最小 `1 Link + 1 RX + 1 TX + 64 B` 配置完成 MinSizeRel 编译。
- 分项自审关闭了事件键结构 padding 比较、完成结果非法枚举、输出失败写回、任务重取锁、
  cancel/completion 竞争、非 canonical timestamp、Link 未就绪收发和 reopen 迟到事件问题。
- 验证：Windows GCC Full `72/72`；WSL ASan/UBSan Adapter `2/2`；GCC
  `-fanalyzer -Werror` Adapter `1/1`；双线程双 Link pthread 回归通过；Adapter/Port 最大
  静态栈帧分别 112/128 B。
- 限制：GCC TSan 在当前 WSL 启动时报 `unexpected memory mapping`，Clang 缺 TSan
  runtime，故不虚报 TSan。当前仅有虚拟/蓝牙 COM，没有 ESP32 串口；固件、DMA/ISR、
  真实吞吐/timestamp/功耗和 24 h 长稳仍为 V6-13/14 硬件 HOLD。

### OP-480｜完成 V6-12：统一 Cluster Target、持久化 Authority 与跨簇状态

- 新增独立 `ucn_v6_cluster`，只依赖 v6 Config/Wire/Security/Capability/Route/Transfer，
  不链接 Realtime，也不调用 v5 Cluster v3/v4 双栈、Mixed Version 或 Legacy bridge。
- Cluster Record v1 固定 4096 B，使用显式网络序与 CRC32C；启动只接受唯一 v6 Schema，
  每次启动先持久递增 boot incarnation，所有状态提交执行 `submit → load → 原始字节精确
  比较`，Provider 回调受 V6-05 共享 Gate 保护。
- Authority 同时要求本机为持久 Head、未 Fence、当前 Stable quorum 成立；Joint 阶段还必须
  满足新旧双 quorum。成员必须来自 V6-07 的 Hop+E2E+ACL 认证结果，并与 V6-06 Cluster
  Capability、Principal、Binding、Session Generation 精确绑定。
- Joint Config 实现 Prepare、指定 Backup 的精确 ACK、双 quorum Commit 和绑定 C_new 的
  Abort；Takeover/Handover/Recovery/Rekey 均持久化完整旧/目标 Epoch、完整目标 Config、
  事务 high-water、Vote bitmap 和 lineage Tombstone，避免重启后丢失安全证明。
- Directory 与 Tunnel 使用固定槽且表满不驱逐；Tunnel 精确绑定 v6 Route Domain、Path
  Capability 与 Session，可向 V6-10 复制跨簇 Transfer 路径，过期由 Owner `step()` 清理。
- MCU 栈自审把所有约 2 KB 的事务快照移入 Owner 固定 staging；Record decoder 改为调用方
  提供 scratch，失败不改 output。GCC `-fstack-usage` 下最大公共函数静态栈帧为 208 B，
  最大内部 helper 为 624 B；默认 Cluster Owner 编译期预算为 22272 B。
- 验证：Windows GCC Full `71/71`；32 Voter/32 Member 上限配置定向 `1/1`；WSL
  ASan/UBSan `2/2`；WSL `-fanalyzer -Werror` Cluster `1/1`；default-OFF Core v6 符号
  为 0，Cluster archive 的 Realtime 未解析符号为 0；`git diff --check` 无空白错误。
- 限制：当前没有真实 Flash 撕裂写、MCU 复位、网络分区或跨簇硬件 Transfer 证明；本机
  当前未安装 MSVC，因此本小节没有虚报 MSVC 结果，交叉编译器总门禁归 V6-14。

### OP-479｜完成 V6-11：Realtime、固定 Path 与双 Deadline 门禁

- 新增独立 `ucn_v6_realtime`，不链接 v5 Realtime 或 Cluster。普通 Endpoint 的 `NONE`
  仍为零额外字节；LOCAL/SYNCED/DEADLINE 使用固定 16 B 网络序 Envelope，失败不写回。
- Capability Record 从 64 B 调整为 68 B，显式认证 Realtime Mode、Clock Domain ID 和
  Domain Generation；Path Capability 增加 `immutable_for_realtime`，并进入 Route
  Proposal Digest，防止固定 Path 证明在激活事务中被替换。
- Time Domain 精确绑定 Master Principal/Binding/Session、Route/Path Generation 与认证
  Capability；必须具有双向硬件 timestamp 能力和非零误差。Domain Generation 先写持久
  high-water、再回读精确证明，且 Provider 使用共享 callback gate 防递归/跨 Owner 重入。
- uncertainty 同时要求 timer、link capture、filter residual、rounding、sample capture 和
  asymmetry 六项非零已知上界，checked 累加后才允许进入 LOCKED；旧采样、跳变、整数溢出、
  同 generation 时间倒退均失败关闭。
- Endpoint 门禁复用 V6-07 的 `hop_authenticated + endpoint_authorized + E2E_CONTEXT`；
  REQUIRED Timed Command 无 ACL/E2E 不可接收。接收和执行各做一次 `age + source/local
  uncertainty < max_age` 半开检查，远未来、远端 HOLDOVER 和过期消息均拒绝。
- 分项自审补充了 Provider 假成功回读、共享 callback gate、固定 Path 位进入 Route Digest、
  Capability 精确 Wire 字段、signed difference/oscillator growth 溢出、HOLDOVER 统计和
  Payload overlap 合同。
- 验证：Windows GCC Full `70/70`；MSVC Release Realtime/Capability/Route `3/3`；
  WSL ASan/UBSan `3/3`；WSL `-fanalyzer -Werror` `3/3`；default-OFF `ucn_core`
  的 v6 符号为 0；`git diff --check` 无空白错误。
- 限制：本阶段为 v6 软件集成与 Host 证明；ESP32/UART/RS-485/ESP-NOW/CAN-FD/USB 的
  硬件 timestamp、真实 asymmetry、ISR/DMA 与最坏误差仍必须在 V6-13/14 实测。

### OP-478｜完成 V6-10：Selective Repeat、SACK、Credit 与 Transfer Pipeline

- 新增 default-OFF `ucn_v6_transfer`，覆盖 T32～T8K 九档能力桶；固定 24 B
  Fragment Header，Message ID 与持久 Operation ID 合一，实际数据预算只能使用 V6-06
  已归约的 Path Fragment Budget。
- TX 使用固定窗口 Selective Repeat：Fragment `peek→physical submit`，累计 Base+32-bit
  位图 SACK，只重发缺片；定向回归在首个 ACK 前提交四片，并在乱序/单片丢失后验证仅缺片
  重传，不形成 Stop-and-Wait 的固定 `1/N` 退化。
- RX 使用固定重组槽、逐片身份冻结、重复内容一致性和整消息 CRC32C；应用退休后进入包含完整
  Message 身份的 Recent Completion，最终 SACK 丢失时可重放 ACK 而不重复业务交付。
- Q2/Q3 Credit 精确绑定 Ingress Peer Session、Link/Class/Generation/Sequence，exact replay
  不续租，所有代际 checked-next；reservation 在物理提交前可退回，存在未结算 reservation
  时拒绝绝对 Credit 更新。
- Path 切换保持已 ACK 片，只重发未确认片；新 Path 必须同 Session 且能力不降低。Session
  撤销先核对调用方 Token 容量，再原子清 TX/RX/Recent/Credit/Reservation 并返还 Buffer。
- 分项自审修复了 Operation/Message ID 分裂、Path 并发未门禁、完成槽永久占用、Recent
  身份不足、Credit padding 比较/回绕/未结算覆盖、Deadline 后不可退休和 Session 资源
  遗漏等问题。
- 验证：Windows GCC Full `69/69`；MSVC Release Transfer 与依赖定向通过；WSL
  ASan/UBSan Transfer `1/1`；WSL `-fanalyzer -Werror` Transfer `1/1`；
  `git diff --check` 无空白错误。
- 限制：仍不接生产 Node/Adapter；真实多跳吞吐、Q0/Q1 压力、硬件 Credit、DMA Buffer
  和多 Bearer Pipeline 留待 V6-13/14。

### OP-477｜完成 V6-09：Metric、四级 QoS 与逐跳 Budget

- Metric：实现固定 32 Path 槽、8 项显式 Known Mask、1 us/ppm/permille/bps
  单位、同时间戳幂等/冲突重放、保守 EWMA 与 1 s 半开陈旧门。延迟、抖动、
  丢包和成本向上舍入；带宽与稳定性向下舍入，避免把未知或乐观估计写成低 Cost。
- Cost：冻结默认 Algorithm ID `0x00060001` 和 Q0～Q3 独立权重；缺少任何有
  非零权重的输入即拒绝。跨跳只允许同 Algorithm ID 累加，并检查 Hop 与 64-bit
  Cost 溢出。
- QoS：固定 Queue/Flow/Inflight 表；Security open result 显式携带真实 Ingress Peer
  Session，使 per-peer/session/flow 配额不把多跳 Origin 冒充成邻居。采用每 Flow Token
  Bucket、12 槽 `6:3:2:1` Class 调度、Q0 Priority+EDF、Q1 流公平，以及 Q2/Q3
  Deficit Round Robin。
- 正交交付：`LATEST` 替换由 Delivery Guarantee 决定，不再与 Q1 强耦合；Reliable Q1
  保持 FIFO，Latest Q2 仍可只保留同 Flow 最新值。选择使用 `peek → completion`，Link
  提交后进入固定 Inflight，并分别统计 Physical、Remote ACK 与 Application Result。
- Hop Budget：只接受 Hop-authenticated Frame，严格要求
  `0 < remaining <= initial <= policy_max`；每跳 checked 扣除驻留与发送上界，边界
  耗尽唯一返回 `TIMEOUT/DROP_EXPIRED`，不能重试或退回无 Budget 调度。Class 固定，
  Hardware Priority 只在原 Q0～Q3 内映射。
- 失效：V6-07 Session 撤销/换代可按 Ingress Peer Session 原子预检容量后返还所有
  Queue 与 Inflight Buffer Token，并清除该 Session Flow；容量不足时零写拒绝。
- 分项自审：关闭了 Metric 重复原始样本与滤波值混淆、32-bit 归一化加法溢出、带宽/
  稳定性 EWMA 乐观舍入、Delivery 与 Q1 再耦合、过期 Budget 可 RETRY，以及 QoS 把
  多跳 Source 地址错误拼接到 Hop Principal 等边界。
- 验证：Windows GCC Full `68/68`；MSVC Release Config/Security/QoS `3/3`；WSL
  ASan/UBSan QoS `1/1`；WSL `-fanalyzer -Werror` QoS `1/1`；default-OFF
  `ucn_core` v6 符号为 0；`git diff --check` 无空白错误。
- 限制：当前仍是 default-OFF 隔离模型，不接生产 Adapter/Node。真实 Link Queue
  深度、硬件优先级、动态 Metric 采样精度、最坏调度延迟和多跳吞吐由 V6-13/14 实测。

### OP-476｜完成 V6-08：RouteSet、冻结 Candidate 与多路径选择

- 身份主键：Route Domain 精确绑定双 Principal、Realm/Source/Destination Binding 与
  Origin Session；Route Generation 只能从 1 开始并按 checked-next 前进。相同地址换
  Binding、换 Principal 或换 Session 都不能命中旧 RouteSet/Candidate/Flow Pin。
- Candidate 事务：固定 16 槽 Candidate 与 16 槽 Domain Transaction high-water；RREP
  Path 只能在 Probe 前加入，首次 Probe 后整份 Proposal 永久冻结并形成覆盖完整 Path、
  Capability、Next Hop、Link、Hop Count 和 Session 的 16 B Digest。发送失败、迟到 ACK、
  错 Epoch/Digest、交叉 Origin 和同 ID 改路均不能提交或污染状态。
- 原子激活：只有所有 Path 均完成 Probe 且至少一次 Activate 已实际提交，精确 ACK 才进入
  Commit；Commit 前重新从 V6-06 Capability Owner 读取每条 Path，确认未过期、未换代，
  然后一次性切换 Current/Previous RouteSet。任何容量或验证失败均保留 Candidate 和旧
  Active Route，不形成半提交。
- 多路径：实现 Pinned、Active/Standby、Per-flow Hash 与显式允许乱序的 Weighted
  Multipath。Flow Pin 固定 32 槽、半开租期、表满不驱逐；RERR 精确绑定 Domain、Route
  Generation 和 Path Generation，只撤销命中路径并清理对应 Pin。
- 发送前失效：每次选路都重新查询 V6-06 Capability Owner，过期或换代 Path 不可发送；
  V6-07 撤销/替换 Session 时可调用精确 invalidation，一次性回收关联 Route、Candidate、
  Flow Pin，并仅在 Origin Session 被替换时释放对应 Transaction high-water 域。
- 分项自审：覆盖 Candidate freeze、物理发送失败后改路、错 ACK、ACK 重试、事务 ID 复用、
  跨 Origin/Binding ABA、Route Generation Grace、Pinned/主备/Per-flow/Weighted、
  RERR、固定容量和失败 output 不写回。自审补充了 Proposal Digest 中遗漏的目标 Session
  与 Hop Count，并加入同 Domain Candidate ID high-water。
- 验证：Windows GCC Full（Scale/Cluster 模拟关闭）`39/39`；MSVC Release Route/Config `2/2`；WSL
  ASan/UBSan Route `1/1`；WSL `-fanalyzer -Werror` Route `1/1`；default-OFF
  `ucn_core` 的 v6 符号为 0；`git diff --check` 无空白错误。
- 限制：当前是 default-OFF 隔离模型，没有接生产 RREQ/RREP/RERR RX/TX；Route 状态按
  Origin Session 隔离，重启必须先由 V6-07 REAUTH 推进 Session Generation。动态 Metric、
  Queue 与 Hop Scheduling Budget 属于 V6-09，Transfer 流水属于 V6-10。

### OP-475｜完成 V6-06：认证 HELLO、Capability 与 Path Budget

- 范围：在 V6-07 唯一 JOIN/Security 之后实现隔离的 V6-06；不接入 v5 生产
  Node/Adapter，不建立第二套 JOIN/ADMITTED。
- Security 边界：新增仅允许 `PEER_HELLO/CAPABILITY_QUERY/CAPABILITY_ADVERTISE`
  的 Hop-authenticated Peer Discovery 通道。接收结果显式区分
  `hop_authenticated`、Endpoint ACL 和 Group discovery；Peer Discovery 不能借此获得
  Endpoint 权限。
- 能力模型：新增固定 64 B Capability Record、24 B HELLO Summary 与 20 B Query，
  分离 Carrier、Link Frame、Processing Frame、Peer、Suite、Transfer 和 Timestamp
  能力；保留字段非零即拒绝，非法输入和失败输出均不写回。
- 缓存与失效：Capability Owner 使用编译期固定 Peer/Path/Group Hint 表，表满不驱逐。
  Capability Generation 必须同域 checked-next；Binding、Session、Link Generation、
  Digest 或能力变化会立即清除关联 Path。
- Group HELLO：共享 Group Key 只形成 Reauth hint，不写 Peer Capability/ACL/Session；
  同时受 per-Link 与 per-Group Token Bucket、全局固定槽和半开 1 s Deadline 约束，
  不匹配/超时输入不清理或续期旧槽。
- Path Budget：逐跳归约 `link_frame_mtu` 与 `processing_frame_mtu`，与产品 Path limit
  取最小；Feature/Suite 取交集，Message Class/Window/Concurrency 取最小，Timestamp
  能力取交集并保守累加 uncertainty；最终复用 Wire Codec 计算精确零载荷开销，再以
  checked subtraction 得到 Payload 与 Fragment Data Budget。
- 小节自审：记录编解码、Peer Discovery、能力缓存、Group Hint、Path Budget 五节均完成
  正负向回归；另外关闭 public struct padding `memcmp`、统计回绕、表满驱逐、旧 Path
  占槽和失败前消耗 Group budget 等自审问题。
- 验证：Windows GCC Full `66/66`、MSVC Release v6 `8/8`、WSL ASan/UBSan 与
  `-fanalyzer -Werror` 的 Security/Capability 均 `2/2`；default-OFF `ucn_core`
  的 `ucn_v6_` 符号为 0，`git diff --check` 无空白错误。
- 限制：当前仍是 `UCN_BUILD_V6_EXPERIMENTAL=ON` 下的隔离 archive；真实驱动
  `carrier_mtu`、Adapter 重组上限、硬件 timestamp 误差和生产消息接线属于后续任务。

### OP-474｜完成 V6-07：Security、唯一 JOIN/REAUTH 与持久 Replay

- 新增 default-OFF `ucn_v6_security`：固定容量 Snapshot 同时保存本机 Binding、Peer Session、
  精确 ACL、Static Group Policy/Key、TX 预留高水位和 RX Replay；更新严格使用独立 Generation
  Witness 后写 Snapshot、双 reload 和字段级一致性验证，任何撕裂/回退/重入均 fault-closed。
- 唯一 `ADMITTED` 入口要求 opaque Bootstrap Owner 中精确 Key/transcript 已到
  `FINAL_DURABLE`，再验证 Authority/Device 双 Proof、durable Fence/quorum/Lease、Binding
  Certificate、显式 Hop/E2E selector 和保守本地 Lease Deadline。重启后 Session 必须 REAUTH，
  exact durable replay 零写幂等，冲突 replay 和已撤销 Session 不得复活。
- 实现 HMAC-SHA256-128 Hop/Group/Auth-only、AES-GCM-128 与 ChaCha20-Poly1305 AEAD 的编译期
  Suite registry 和 Provider 合同；完整 Hop/E2E/Group Tag、64-bit Replay window、Key grace、
  TX Sequence 持久区间预留、轮换与撤销均失败关闭。测试 Crypto 仅为确定性 Fake，不是生产算法实现。
- ACL 精确绑定 Principal、双 Binding、Session、Endpoint/Protocol Opcode、Traffic、Delivery、
  Interaction、Operation ID 与方向；同 Frame Type 的不同 Opcode 不能互相提权。Group HELLO
  固定为 discovery-only，不能建立 Principal、Session、Capability、ACL 或续租。
- 分项自审额外发现并修复：持久预留块内后续 Sequence 误在栈副本递增导致复用；发送容量不足时
  过早消耗 Sequence；Security commit 可绕过 Bootstrap 最终状态；Control ACL 语义字段来源不统一。
- 验证：Windows GCC Full `65/65`；MSVC 19.29 Release v6 `7/7`；WSL ASan/UBSan 与
  `-fanalyzer -Werror` 的 Identity/Security 各 `2/2`；`git diff --check` 无空白错误。
  本项仍未接生产 Node/Adapter，真实密码、Flash/掉电、MCU 安全存储和硬件并发留待 V6-13/14。

### OP-473｜完成 V6-05：C99 Opaque Storage、Manifest 与唯一 Protocol Owner

- 新增唯一 `ucn_v6_config.h`：所有容量均为编译期常量，storage bytes 随相关容量生成；
  Feature Manifest 固定 API/Storage Layout、Feature bits、十项容量及 Layout Hash。
- Identity Authority、Bootstrap Owner、Operation-ID Allocator 与 Durable Journal 的结构体正文移入
  `src/v6/internal/`；公共头只暴露不完整类型、C99 对齐 union、`init_in_place()`、操作 API 和只读 view。
- 所有 `init_in_place()` 在写 storage 或调用 Provider 前先核验 Manifest、容量和 8 B 对齐；失败保持
  storage/output/Provider 次数不变。运行入口检查 magic、Schema、Layout Hash、canary，损坏后失败关闭。
- 新增唯一 v6 Protocol Owner：驱动/ISR 只能投递 RX/TX/Completion/Timer/Provider 事件并通知；
  `run(budget)` 才能推进协议，五类事件 round-robin，处理失败保留事件，递归 run 与容量溢出拒绝。
- 分项自审覆盖跨 TU 配置错配、未对齐/容量不足、损坏 storage、Provider 重入、ISR 通知、Owner
  budget/fairness/retry 和默认产品物理隔离。未来 V6-08～12 新增的 Node/Transfer/Realtime/Cluster
  对象必须使用同一 opaque/Owner 合同，不能重新公开内部布局。
- 验证：Windows GCC Full `64/64`；MSVC 19.29 Release 五项 `5/5`；WSL ASan/UBSan 与
  `-fanalyzer -Werror` 各五项 `5/5`；默认 OFF `libucn_core.a` 的 `ucn_v6_*` 符号为 0；
  `git diff --check` 无空白错误。均为 Host 软件证据，未声称真实 RTOS 锁、ISR 或 Flash 已验证。

## 2026-09-04

### OP-472｜完成 V6-04：正交 Message 合同与 Durable Operation Journal

- 新增 default-OFF `ucn_v6_message`：Traffic、Delivery、Interaction 三轴独立校验，16-bit
  Endpoint Contract 与五级 Completion 枚举不再由优先级推导可靠性或业务角色。
- 实现持久区间预留的 64-bit Operation ID 分配器；掉电跳过未用 ID，达到阈值不回绕，
  Provider submit/load 不一致或失败时 Fence。
- 实现 8 槽固定 Journal 和 8 槽 Principal retired-high-water：PREPARED、EXECUTING、
  COMMITTED_RESULT、IN_DOUBT、TOMBSTONED 单向推进；启动把遗留 EXECUTING 持久化为
  IN_DOUBT，未认证对账不得关闭或重试。
- Result GC 分两步：认证 ACK+最小保留期后先 Tombstone，发起方 durable high-water 越过且
  最大重放期结束后才把本地 high-water 与槽清除原子提交；表满不驱逐未决或历史证明。
- 分项自审覆盖负枚举、冲突 Digest、重复执行、Provider 重入、全零/损坏 Record、submit
  failure、固定容量和所有失败不写回；报告见
  `UCN_V6_04_Message与DurableOperation实现报告.md`。本项仍不接生产 RX/TX、真实 Flash 或
  外部执行器。
- 验证：Windows GCC Full `61/61`；V6-02/03/04 定向 `3/3`；MSVC 19.29 Release
  `/W4 /WX`、WSL ASan/UBSan、WSL `-fanalyzer -Werror` 各 `1/1`；`git diff --check`
  无空白错误。默认产品仍不链接 v6 archive。

### OP-471｜完成 V6-03：Core Wire v6 隔离 Codec 与精确格式冻结

- 新增 `ucn_v6_wire` default-OFF 静态库，冻结 A0～A3、8 B 前缀、双 Binding、Session/
  Sequence、八类固定顺序扩展、16 B Tag、CRC32C 和 80 B canonical AAD；精确格式写入
  `UCN_v6_Core_Wire_精确格式_RFC.md`。
- 正向门禁：四档完整 Golden、Peer+E2E+Message+Path+Budget、Peer Control+Route、Group
  HELLO 和独立 AAD 向量；逐跳可变字段不改变 E2E AAD，initial budget 必须改变。
- 负向门禁：旧 Version、坏 Magic/CRC、截断/追加、保留 Traffic bit、Peer/Group 或 Route/Path
  双存、零 Key selector、非法 Operation/Packet serial、反向 Budget、缺失扩展的脏字段和保留
  地址误用均不写回。fixed seed 4096 次 raw fuzz 若接受则必须逐字节 canonical 往返。
- 分项自审关闭基础长度、AAD 长度、Decoder bounds、最大 Frame 扫描和 absent Tag canonical
  等内部发现。Windows GCC Full 全量 `60/60`；Nano/Lite/Release、MSVC、WSL ASan/UBSan 与
  Analyzer 定向均 `1/1`。
- 边界：V6-03 不接生产 RX/TX，不验证真实 Tag/Key/Replay/ACL。旧 v4/v5 资产在最终切换前
  仍服务 v5 基线回归，Compatibility Manifest 保持 PENDING，V6-15 必须统一删除并执行 denylist。

### OP-470｜完成 V6-02：Identity、Bootstrap、Binding 与 Generation 隔离模型

- 新增 `include/ucn/v6/ucn_v6_identity.h`、`ucn_v6_bootstrap.h` 以及对应实现，建立
  128-bit Principal、Realm/Address/Binding/Session Key、Address Authority Epoch、保守
  Lease Deadline、固定 Binding/Group 表、一跳 JOIN Bootstrap 和已绑定节点 REAUTH。
- 持久化顺序：Authority Epoch、Binding slot 和动态 Group 高水位都先经 Provider 成功提交，
  再发布 RAM 状态；Provider 失败将 Authority 围栏为 faulted。调用方持有的共享 callback
  gate 在 Provider 回调前建立，可阻断递归初始化和跨 Authority 并发进入，不使用未同步全局变量。
- 资源整改：JOIN/REAUTH 虽分开保存，但全局 8 槽、per-Link 2 槽和 per-peer 1 槽统一跨表
  计算；Cookie 前零 pending、认证前无放大、Token Bucket 饱和补充、绝对 timeout 和显式
  timer 清理均失败关闭。动态 Group 只推进持久化高水位，退休不复用 ID。
- 自审矩阵：Windows GCC Full `59/59`，MSVC 19.29 定向 `1/1`，WSL ASan/UBSan 定向
  `1/1`，WSL `-fanalyzer -Wall -Wextra -Wpedantic -Werror` 定向 `1/1`；默认关闭的 Release
  `ucn_core` 中 `ucn_v6_*` 导出符号为 0。
- 边界：这仍是 default-OFF 隔离模型，没有真实密码、生产 Wire、Flash/reload、掉电或 MCU
  证据；V6-02 只记为分项自审完成，按用户要求延后至全部任务完成后的统一外审。

### OP-469｜完成 V6-01：v5 自审快照、兼容删除清单与 v6 分支

- 用户授权：明确要求暂不逐项外审，先整改并连续执行全部任务；每个小项完成后自审，最终
  再做多轮全体自审并统一外审。因此本项不伪造 V5-64 A06 的外审签字，只建立带限制的
  “自审开发快照”。
- 快照：提交 `c554239f0940d77a0a0a3baef81c6742be1a0c90` 已推送到 `origin/main`，并建立
  远端分支 `v5-final-experimental` 与带注释 Tag `v5.0.0-experimental-final`；两者 peeled
  commit 都必须解析到 `c554239`。Tag 注释明确为非生产发布。
- v6 基线：从同一提交创建 `v6-development`，后续破坏性重构只在该分支推进；v5 历史源码
  只由 Git 对象保存，不复制到 v6 的 archive/legacy 源码目录。
- 删除清单：新增 `05-V6兼容删除清单.md`，以 28 行逐项登记 Wire/API/ABI/Storage/CMake/
  test/doc/fallback 资产、唯一删除阶段、v6 替代物、负向门禁、验证方式和提交状态。
- 状态边界：V6-01 实施与自审完成、最终外审延期；不表示 V5-64、v6 Wire、安全、实机或
  掉电能力已放行。V6-02 可以开始设计与隔离实现。

### OP-468｜整改 V564-A06-B：冻结每一跳 Candidate 路径证明

- 问题核实：A06 的发起端 Candidate 冻结仍不足以覆盖共享中继。中继转发路径 A 的
  `PATH_PROBE` 后，若同 Candidate ID 的更优 RREP 把槽位改成路径 B，旧 Probe/ACK 可以
  错误证明从未经过 Probe 的路径 B；该反例属于真实 P1 路由语义缺陷。
- 代码整改：`ucn_candidate_route_t` 增加显式 `path_snapshot_frozen`。发起端、中继和目标在
  首次 `PATH_PROBE` 或 `PATH_PROBE_ACK` 进入外部 Link 回调前冻结本节点持有的 Candidate；
  `PATH_ACTIVATE` 预检要求双向 Candidate 均已冻结。冻结后只允许精确重复 RREP 幂等返回，
  任何出口、Cost 或 Hop 变化都必须使用新的 Candidate ID 重新发现和证明。
- 回归：新增中继 A→B 路径迁移、目标未 Probe 直接 Activate、回调前冻结可见性等生产路径
  断言。外部定向探针现为
  `forward_frozen=1, rrep=-16, candidate_moved=0, reverse_frozen=1, old_activate=1, new_activate=0`。
- 矩阵：Windows GCC Full/Lite/Nano/Release/Service-OFF 分别 `58/58、58/58、48/48、58/58、58/58`；
  MSVC Full `58/58`；Product128 注册 59 项、43 项通过、16 项容量跳过、0 失败；WSL
  ASan/UBSan 与 `-fanalyzer -Wall -Wextra -Wpedantic -Werror` 均 `60/60`。
- ABI：Node Storage Layout 升为 11。新增布尔量落入现有 Candidate 对齐空隙，Host
  Nano/Lite/Full 对象大小仍为 `3496/6952/11152 B`；语义布局已经改变，库和应用仍必须
  全量重编，不能因为 `sizeof` 未变而复用 Layout 10 对象。
- 自审结论：A06-B 软件整改与分项自审 PASS。按用户要求暂不单独外审，V5-64 保持
  `AUDIT HOLD / FINAL EXTERNAL REVIEW DEFERRED`；允许后续建立带清晰限制的 v5 自审开发
  快照，不能称为发布签字。最终 `src/node/ucn_node.c` SHA256 为
  `5E3CD594313799DA0A3DAE0A7D4897667CCD34333AAD604055B6CA91DFFE5592`。

### OP-467｜同步 V6-00 最终架构 RFC 外部终审 GO

- 外审结论：外部复审以 RFC 1632 行、SHA256
  `F79FC9B310CE6FFD5E662F3A7A986A1E61C54F1CB46B8AD7EA5A4405D04E1680` 为规范内容基线，
  确认 V6A-25 通过，V6A-24/V6A-25 均为 `EXTERNAL GO`，并签署
  `V6-00 = DONE / EXTERNAL FINAL REVIEW GO`（最终架构 RFC/纯文档范围），P0/P1/P2 均为 0。
- 台账同步：任务表、路线图、建议导航、RFC 顶端状态和 V6-00 自审报告统一改为最终 GO；
  V6-01 不再受 V6-00 阻塞，但仍等待 V5-64 A06 可追溯独立外审，以及用户对提交、建立
  v5 快照/Tag 和 v6 基线的明确授权。
- 基线区分：终审后只修改 RFC 的状态元数据和末尾阶段说明，未改变受审规范字段、算法、
  失败关闭合同或任务依赖。状态同步后的 RFC 为 1633 行、SHA256
  `14E505A06E22A11F52DF7E037A408877586F3E8328FA23272EBD4074282163C7`；自审报告同时保留
  外部受审内容 SHA 与状态同步后 SHA，避免把行政更新误写成新的规范签字基线。
- 边界：本轮没有修改 v6 源码、Wire/API/ABI、CMake 或测试，没有建立 Tag/分支、提交或
  推送，也没有运行 CTest。V6-00 的文档签字不表示安全、实机、掉电或产品能力完成。
- 架构复核：只读核对 Obsidian 的 UCN MCU-first 说明；固定内存、MCU 自主运行和 Host 无
  隐式协议特权仍保持，未修改知识库。

### OP-466｜整改 V6-00 第五轮交叉审计 V6A-25

- 审计核实：以 RFC
  `4D8BDE93FAC781EFC5318BCF3C70CC53DFB6A72B9CD73E5A87441FAAE9DF372E`、1588 行为基线，
  确认 V6A-25 P1 成立。V6A-23 更新为外部 GO；V6A-24 的 Owner、父域、持久化、换主连续性
  和防 ABA 语义保持通过，本轮只删除无法在固定内存中判定的无界“已用 ID 集合”合同。
- 动态 Group：每个 Realm 只持久化一个 `dynamic_group_id_high_water`，新 ID 必须是其
  checked-next。先持久化预留后发布；删除只释放固定 Active Group 运行槽，不降低高水位，
  不查找稀疏空洞。活动表满零写拒绝，ID 到达冻结阈值进入 Fault。
- 静态 Group：全部生命周期 ID 由签名 Manifest 的固定槽预置；槽只允许
  `NEVER_ACTIVATED → ACTIVE → RETIRED`，退休永久占位，重启、重刷配置、恢复旧 Manifest
  或垃圾回收均不得复活。槽耗尽后拒绝创建。
- Group Key：每 Group 使用编译期固定 Key 槽和不可变 Key ID；常规轮换保持 Key ID 并只
  checked-next Key Generation。新 Key ID 只能来自从未激活的 Manifest 槽，退休槽不驱逐、
  不复用；需要全新槽集合时必须先推进 Group Policy Generation，形成新的完整 selector 父域。
- 固定容量：RFC 冻结 `UCN_V6_MAX_ACTIVE_GROUPS`、`UCN_V6_MAX_STATIC_GROUP_SLOTS` 和
  `UCN_V6_MAX_GROUP_KEY_SLOTS_PER_GROUP` 三类编译期容量语义；摘要只校验固定记录完整性，
  不能回答“某 ID 是否用过”。V6-02/V6-05/V6-07 验收同步表满、撕裂、回退和耗尽反例。
- 台账同步：V6-00 更新为第五轮整改/自审通过/等待外审，V6-01 继续阻塞；任务表新增
  V6A-25，路线图、建议导航与 V6-00 自审报告同步。Obsidian UCN MCU-first 说明用于核对
  静态分配、固定容量和 Host 无隐式协议特权，最终合同以当前 RFC/源码事实为准；Vault 未修改。
- 文档自审：RFC 为 1632 行、一级章节 1～23 连续，SHA256
  `F79FC9B310CE6FFD5E662F3A7A986A1E61C54F1CB46B8AD7EA5A4405D04E1680`；V6-00～15 共
  16 项、五轮 V6A-01～25 共 25 项，V6A-25 仅登记一次；509 个 Markdown 的 1562 条本地
  链接为 0 坏链；Cluster 文档合同 `phases=22/wire_types=33/tasks=12` 通过。
- 结论/限制：`V6-00 = FIFTH AUDIT REMEDIATED / RE-SELF-REVIEW PASS / EXTERNAL RE-REVIEW
  PENDING / AUDIT HOLD`；`V6-01` 继续阻塞。本轮只有文档合同变化，不修改协议源码、Wire/
  API/ABI、CMake 或测试，因此未运行 CTest；没有提交或推送。`git diff --check` 和包含未跟踪
  RFC/自审报告的隔离临时索引检查均通过，真实 Git 暂存区保持不变。

### OP-465｜整改 V6-00 第四轮交叉审计 V6A-23～V6A-24

- 审计核实：以当前 RFC
  `849C3695C623343F861C1489FB64D41A9CB34E3354835052841C747DF8C71BE8`、1552 行为基线，
  确认 V6A-23 P1 与 V6A-24 P2 均成立。V6A-20/V6A-22 保持外部 GO；V6A-19 主问题和
  V6A-21 原问题不重复整改，只补本轮发现的验证端量化误差与 Group Generation 所有权缺口。
- V6A-23：Lease verifier 公式除慢钟裕量外，固定扣除一整个本地 Timer Resolution 和
  Challenge/验证两次读取误差；Resolution 必须已知且大于零，read uncertainty 必须已知，
  仅 Port 证明无额外读数误差时允许后者为零。所有乘/加/减均 checked，安全时长为零、下溢、
  溢出、非整 tick 真实过期和 `now==deadline` 均失败关闭。区分性反例固定为 10 ms Resolution、
  15 ms proof、真实经过 19 ms 而 raw now 只前进 10 ms，结果必须拒绝。
- V6A-24：统一 Generation 表增加 `Group Policy Generation` 与 `Group Key Generation`。
  Realm Manifest 互斥选择签名静态 Owner 或动态逻辑 Realm Address Authority/quorum；冻结
  `{realm,group_id}` 与 `{realm,group_id,group_generation,group_key_id}` 父域、persist-before-
  publish/use、已发布 high-water、已用 Group/Key ID 集合、跨 Authority/Key Owner 换主
  连续性、合法重置和耗尽 Fault。重启、配置恢复、分区合并或换主都不得把代际重置为 1。
- 台账同步：V6-00 更新为第四轮整改/自审通过/等待外审，V6-01 继续阻塞；任务表新增
  V6A-23/V6A-24，V6-02/V6-07 验收、路线图、建议导航和 V6-00 自审报告同步。Obsidian UCN
  MCU-first 说明用于核对固定内存、MCU 自主和 Host 无隐式特权边界，最终合同以当前 RFC
  和源码事实为准；本轮未修改 Vault。
- 文档自审：RFC 为 1588 行、一级章节 1～23 连续，SHA256
  `4D8BDE93FAC781EFC5318BCF3C70CC53DFB6A72B9CD73E5A87441FAAE9DF372E`；V6-00～15 共
  16 项、四轮 V6A-01～24 共 24 项，V6A-23/V6A-24 各有唯一任务；509 个 Markdown 的
  1562 条本地链接为 0 坏链；Cluster 文档合同 `phases=22/wire_types=33/tasks=12` 通过。
- 结论/限制：`V6-00 = FOURTH AUDIT REMEDIATED / RE-SELF-REVIEW PASS / EXTERNAL RE-REVIEW
  PENDING / AUDIT HOLD`；`V6-01` 继续阻塞。本轮只有文档合同变化，不修改协议源码、Wire/
  API/ABI、CMake 或测试，因此未运行 CTest；没有提交或推送。`git diff --check` 和包含未跟踪
  RFC/自审报告的隔离临时索引检查均通过，真实 Git 暂存区保持不变。

### OP-464｜整改 V6-00 第三轮交叉审计 V6A-19～V6A-22

- 基线纠正：确认外部审计读取的是当时当前 RFC `BA008149...`、1362 行；V6A-11～18
  的整改已由当前版复核确认，不重复修改。本轮只处理新发现的 V6A-19～22，V6-00 全程
  保持 `AUDIT HOLD`，V6-01 继续阻塞。
- V6A-19：租约不再从“收到 Final Commit 的本机时刻”重新获得完整时长。验证者先锁存
  新鲜 Challenge 的本地起点，proof 认证 Authority/Fence/quorum、nonce/txid、Lease Sequence
  和最大剩余时长；各 quorum signer 先扣自身计时误差，聚合器取最小上界，接收端再扣本地
  慢钟裕量并建立半开 Deadline。延迟只缩短租期，相同 proof 重放不续时；重启、Unknown
  慢钟、`now==deadline` 和 checked arithmetic 失败均关闭。
- V6A-20：Bootstrap 与无 Session Reauth 在分配 pending、执行 Identity/证书密码验证、访问
  Provider 或放大响应前，必须先经过 boot-scoped 无状态 Cookie 和认证前预算。冻结默认
  8 个全局槽、每 Link 2 槽、每 discriminator 1 槽、3 s 绝对 timeout、2 s Cookie bucket、
  per-Link burst 4/每秒恢复 2、无驱逐/无续时及每次 Owner run 的昂贵操作上限。
- V6A-21：增加与 Peer Hop Context 互斥的 Group Security Context，固定 GroupKeySelector、
  Group Tag/Replay、`CONTROL/GROUP_HELLO`、`LINK_LOCAL_GROUP` 全 1 保留地址和 hop=1/禁止
  转发合同。共享 Group Key 只证明组 Key 持有，不能证明独立 Principal；合法帧最多产生受
  固定 hint 槽、Token Bucket 和 cooldown 约束的 Peer Reauth 提示，不能续租或写
  Capability/ACL/ADMITTED。
- V6A-22：Endpoint/Control ACL Key 增加 canonical `protocol_opcode`；非 DATA 精确匹配，
  DATA 固定为 0。Bootstrap 与 Group discovery 使用独立准入策略；同 Frame Type 下替换
  Opcode、未知 Opcode 或外层/内层解释不一致均在副作用前拒绝。
- 台账同步：任务表增加 V6A-19～22 和实现 Owner/反例，V6-02/03/06/07 验收同步；路线图、
  建议导航和 V6-00 自审报告统一改为第三轮状态。V6A-11～18 标记为当前版复核确认，未补造
  V5-64 A06 签字。
- 文档自审：RFC 为 1552 行、一级章节 1～23 连续，SHA256
  `849C3695C623343F861C1489FB64D41A9CB34E3354835052841C747DF8C71BE8`；V6-00～15 共
  16 项、三轮 V6A-01～22 共 22 项；509 个 Markdown 的 1562 条本地链接为 0 坏链；Cluster
  文档合同 `phases=22/wire_types=33/tasks=12` 通过。
- 结论/限制：`V6-00 = THIRD AUDIT REMEDIATED / RE-SELF-REVIEW PASS / EXTERNAL RE-REVIEW
  PENDING / AUDIT HOLD`；`V6-01` 继续阻塞。本轮没有修改协议源码、Wire/API/ABI、CMake
  或测试，没有运行 CTest，也没有提交或推送。RFC 与自审报告仍为未跟踪文件，提交前必须
  暂存全部目标文件并执行 `git diff --cached --check`，不能只依赖 `git diff --check`。

### OP-463｜整改 V6-00 第二轮架构外审 V6A-11～V6A-18

- 审计核实：确认 7 项 P1、1 项 P2 均成立。V6A-01～10 的主方向保留，但 Bootstrap/
  JOIN、Address Authority、Wire Security Selector、Durable Operation 掉电窗口、Hop Budget、
  生产 Codec 门禁、V5-64 台账和 Opaque 威胁模型仍需补齐。
- V6A-11：Bootstrap 改为 JOINING_DEVICE 与 REALM_ADDRESS_AUTHORITY 双向认证，固定双方
  Principal、Authority Lease Certificate、双 nonce、txid、地址/Binding/Lease、Suite/Key
  的 canonical transcript；增加 `ADDRESS_BOUND_NO_PEER` 新 Link 重认证，并区分认证单播
  HELLO 与只提供发现提示的 Group HELLO。
- V6A-12：冻结单一逻辑 Realm Address Authority。只有持有 durable Generation/Lease/Fence、
  allocation high-water 和配置 quorum 才能分配/续期；少数分区零新增、零续期。换主必须
  先失效旧 Fence、checked-next Authority Generation并继承完整 Binding 高水位。
- V6A-13：固定 Hop/E2E Security Context 的 Suite、Key ID/Generation 和 Protocol Context
  的 Opcode 位置；明确 Key selector、轮换 grace、禁止试 Key/隐式默认、Hop Tag/E2E AAD/
  CRC 各自覆盖范围。
- V6A-14：Durable Journal 增加 `PREPARED/EXECUTING/COMMITTED_RESULT/IN_DOUBT/
  TOMBSTONED`。外部副作用不能与 Journal 原子提交或认证查询对账时，掉电后一律 IN_DOUBT、
  不重试、不伪造结果；同时冻结固定表满、结果 ACK/retention、Tombstone 和 GC 顺序。
- V6A-15：Hop Budget 只能在原 Traffic Class 和已认证 per-peer/source/flow 固定配额内排序；
  严格验证 `0<remaining<=initial<=policy_max`。交叉自审发现改变 Class 会破坏 E2E AAD，
  因此耗尽行为收敛为唯一 `DROP_EXPIRED`，不得删除扩展后回退普通调度继续传播。
- V6A-16：V6-03 只允许隔离、default-OFF Codec/Golden/Negative，不得接生产 Node/Adapter；
  生产 v6 RX/TX/Encoder 必须等待 V6-07 Security/JOIN 外审 GO。任务表和路线图同步。
- V6A-17：任务表曾写 V5-64/A06 已外审 GO，但现有 OP-460 明确仍等待复审，仓库没有独立
  A06 签字记录；因此恢复 V5-64 `AUDIT HOLD`，并把 A06 可追溯外审设为 V6-01 前置，不补造
  历史签字。
- V6A-18：收窄 Opaque 保证。它阻止合法代码依赖私有布局，但不能物理阻止同地址空间
  `memset`/越界写；入口以 magic/Schema/Layout/Fence/canary 检测并失败关闭，强隔离需
  MPU/TrustZone/进程。
- 文档自审：RFC 为 1362 行、一级章节 1～23 连续，SHA256
  `BA008149B5163D384BB75C3CC9410D8E3B8ED36359265A5A3EFACFE03C3F4C3C`；V6-00～15 共
  16 项、V6A-11～18 共 8 项；509 个 Markdown 的 1562 条本地链接为 0 坏链；Cluster
  文档合同 `phases=22/wire_types=33/tasks=12` 通过。
- 结论/限制：`V6-00 = SECOND AUDIT REMEDIATED / RE-SELF-REVIEW PASS / EXTERNAL FINAL
  REVIEW PENDING`；`V6-01` 继续阻塞。本轮只改 RFC、任务、自审、路线图、导航和操作记录，
  没有修改协议源码、Wire/API/ABI、CMake 或测试，也没有运行 CTest、提交或推送。

### OP-462｜整改 V6-00 首轮架构外审 V6A-01～V6A-10

- 审计核实：确认 8 项 P1、2 项 P2 均成立；问题集中在交付/交互双语义、未绑定地址
  Bootstrap、地址复用 ABA、E2E Principal 认证、C99 静态 Storage、重复 JOIN Owner、
  MTU/Payload Budget、Realtime Deadline 可见性、兼容删除销账和 Optional Feature 依赖。
- V6A-01：将 `delivery_guarantee=BEST_EFFORT/LATEST/RELIABLE` 与
  `interaction_role=ONE_WAY/REQUEST/RESULT/ERROR` 分离；增加发起 Principal 持有的固定
  64-bit Operation ID、Payload 冲突拒绝和 durable at-most-once 边界。
- V6A-02/A03：增加禁止转发的一跳 Bootstrap Frame、128-bit Identity Digest、64-bit
  Bootstrap txid 和本地 Peer 区分；普通 Frame 增加 Source/Destination Address Binding
  Generation，并冻结各 Generation 的 Owner、父域、持久化和合法重置条件。
- V6A-04：canonical AAD 纳入 Version/Class、不可变 Flags、Header Contract、双 Binding、
  Guarantee/Interaction、Operation、Route/Path 和初始 Hop Budget；凡按原始 Device
  Principal 授权的消息必须使用 E2E Auth-only 或 AEAD，Hop Auth 不能替代端到端身份。
- V6A-05：C99 Opaque Storage 改为产品生成的编译期 `*_STORAGE_BYTES`、Alignment、
  storage union 和声明宏；`storage_required()` 只作运行期核对，init 写入前检查容量、
  对齐、Schema 和 Manifest/Layout Hash。
- V6A-06/A07：V6-07 成为唯一 JOIN/ADMITTED Owner并先于 V6-06；V6-06 只做认证
  Session 后的 HELLO/Capability。区分 Carrier MTU、Link/Path Frame MTU，并按精确
  Contract、Security Suite、Address Class、扩展和 Fragment Header 计算实际 Payload Budget。
- V6A-08：E2E Deadline 默认只在源/目标检查；中继不解析 Realtime Envelope，只能使用
  可选 Hop-authenticated `HOP_SCHEDULING_BUDGET`，remaining 只减不增且不能放宽目标端
  E2E Deadline。
- V6A-09/A10：V6-01 增加逐文件/符号 Compatibility Removal Manifest，V6-15 增加
  `rg/nm/CMake` denylist；历史源码只留 Git Branch/Tag。V6-11 Realtime 与 V6-12 Cluster
  改为共同基座后的并行 Optional Feature，互不链接、互不依赖。
- 一致性：普通候选基础长度因双 Binding Generation 从 28/30/32/34 B 修正为
  36/38/40/42 B；路线图和 V6-00～15 任务依赖已同步。RFC 当前 1079 行，SHA256 为
  `6516A0E3B5AB63A0F4FEBE82920F22DDD32D9A4D968E815D7BE87543E2465CA5`。
- 结论/限制：`V6-00 = AUDIT REMEDIATED / RE-SELF-REVIEW PASS / EXTERNAL RE-REVIEW
  PENDING`。本轮只改 RFC、任务、自审、路线图、导航和操作记录；没有修改协议源码、
  CMake、Wire、API、ABI 或测试，也没有运行 CTest、提交或推送。V6-01 继续阻塞。

### OP-461｜完成 V6-00 最终架构 RFC、任务拆分与全体自审

- 用户决策：UCN 尚未正式发布，后续不承担 v4/v5 测试固件、Wire、API、ABI、Storage
  或 Cluster Record 兼容；目标是先把协议收敛为最合理的单一版本，再发布 UCN 1.0。
- RFC：新增《UCN v6 最终协议架构与破坏性重构 RFC》，以 MCU-first 为基础，统一
  Device Identity/Realm/Address/Generation、A0～A3 地址档、Core Wire、Message/
  Endpoint/Traffic/Delivery、Security、Capability/Path MTU、RouteSet/Multipath、Metric/
  QoS、Transfer v2、Owner、Opaque Storage、Realtime、Cluster 与发布门禁。
- 删除边界：明确删除 v4/v5 Runtime 双解码、Cluster v3/v4 双栈、legacy bridge、
  W0～W3 对安全 serial 的联动缩窄、DATA_Q0/Q1 重复类型、透明公开大状态和多个配置
  事实源；旧成果仅由 Git 分支/Tag/归档文档和测试证据保存。
- 任务：在任务表新增 V6-00～V6-15，覆盖 v5 实验快照、Identity、Wire、Message、
  Opaque API/Owner、Capability/MTU、Security、RouteSet、Metric/QoS、Transfer、Realtime、
  Cluster、参考产品、全量验证与 UCN 1.0 RC；冻结每项依赖、负向门禁和实机证据边界。
- 自审：新增 V6-00 独立自审报告；复核当前 Core 仍为 v5、W0～W3 descriptor、1 B
  HELLO、九档 Transfer 和 Cluster v3/v4 隔离事实，确认 RFC 不把目标能力误写为当前
  实现。候选无扩展基础长度按 `26 + 2 × AddressBytes` 复算为 28/30/32/34 B。
- 机械门禁：RFC 834 行、23 个一级编号章节连续、未决占位标记 0；V6-00～15
  共 16 项连续；本轮相关文档本地链接 0 坏链；Cluster 文档合同脚本 PASS
  (`phases=22 wire_types=33 tasks=12`)；`git diff --check` 无空白错误，仅既有换行提示。
- 结论/限制：`V6-00 = SELF-REVIEW PASS / EXTERNAL REVIEW PENDING`。本项没有修改
  协议版本、源码、CMake、Wire、API、ABI 或测试；没有运行 CTest，也未提交/推送。
  V6-00 外审前不得开始 v6 Encoder/Decoder；当前工作区既有 V5-64 修改保持原样。

### OP-460｜整改 V5-64 外审 A06：冻结 Candidate 路径证明

- 审计核实：确认 `learn_candidate_route()` 会在同 ID 更优 RREP 到达时改写 Candidate
  的 Link/Cost/Hop，却保留旧 Probe、RTT、Epoch、Activate 和 ACK Deadline；外审反例
  可让路径 A 的 ACK 提交未经 Probe/Activate 的路径 B。
- 实现：新增统一的 Candidate transaction-frozen 判定。任一 Probe/RTT/Activate 证明
  建立后，路径字段不同的同 ID RREP 返回 `UCN_ERR_STATE` 且不写 Candidate/Route；精确
  重复只幂等返回且不续期。ACK 继续直接消费冻结 Candidate 快照。Neighbor 主 Bearer 在
  事务中改变时清除 Candidate，必须用新 Candidate ID 重新发现和证明。
- 回归：增加等待 ACK、Probe 完成但尚未 Activate、本地 Activate 提交失败三组路径迁移
  反例；均注入同 ID 更优 RREP并逐字节检查 Candidate/Route 不变，随后只允许旧路径 ACK
  或重试。外审 A06 探针变为
  `rrep=-16 candidate_moved=0 ack=0 route_moved=0 route_epoch=10`。另用真实 Neighbor
  双 Bearer 回归证明已有 Probe 证据时主 Bearer 切换会清除 Candidate，不迁移旧证明。
- 软件验证：Windows GCC Full/Lite/Release/Service-OFF 与 MSVC Full 均 `58/58`，Nano
  `48/48`；Product128 为 `59 registered / 43 passed / 16 capacity-skipped / 0 failed`；
  WSL ASan/UBSan 与 `-fanalyzer -Werror` 均 `60/60`；Ninja 全新中文源码＋中文构建目录
  Full `58/58`。A04/A05/A06 三个旧漏洞探针均不再满足原漏洞断言。
- 最终一致性：`src/node/ucn_node.c` SHA256 为
  `88BE9273FBCB551B51563ACBF10915D98B4F9C2C3369E0362001EF056848A5BC`；507 个
  Markdown、1550 条本地链接、`BadLinks=0`；核心函数索引最新，双语函数注释
  `920/920`，`git diff --check` 无空白错误（仅既有换行提示）。
- 状态：V564-A04/A05 保持外审 PASS；A06 已完成代码与定向自审，V5-64 继续
  `AUDIT HOLD` 等待复审。本项不构成 ESP32、多 Bearer、故障注入或长稳实机结论，尚未
  提交、未推送。

### OP-459｜整改 V5-64 外审 A04/A05：ACK 精确绑定与有界重试

- 审计核实：确认旧 `PATH_ACTIVATE_ACK` 只按 Candidate ID 查找，未验证本地发起、Probe
  完成、已发送状态及 Candidate 已绑定 Epoch；确认 `activation_sent=true` 后调度永久
  跳过该 Candidate，ACK 丢失时只能等待 Candidate 自然到期，切换无法收敛。
- V564-A04：ACK 在任何 Route/Candidate/Stats 写入前精确核对
  `(local_origin,source,candidate_id,route_epoch,wire_profile)`、`originated_here`、Probe 发送与 ACK 均达到门限、
  发送尝试和 ACK Deadline。中继/目标 Candidate 首次 Activate Commit 时绑定 Epoch，
  同 ID 换 Epoch 零写拒绝；精确重复 ACK 仅在同一 Route 仍活动时幂等成功。
- V564-A05：Candidate 增加有界 Activate 状态。默认总 ACK Deadline 1000 ms、重试间隔
  250 ms、最多 3 次重试；每次保持 Candidate ID/Epoch 并分配新外层 Sequence。ACKED
  后停止；耗尽返回 `UCN_ERR_EXHAUSTED`、清 Candidate、保留旧 Active Route。
- 原子性延续：目标 ACK 失败与中继下游发送失败会同时恢复 Route 和 Candidate 快照；
  不会遗留首次 Epoch 绑定或双向半提交。外审旧 A04 探针现为
  `result=-16/installed=0/switches=0`；旧 A05 探针在 1001 ms 得到 Exhausted，不再维持
  “生命周期内仅发送一次”的漏洞断言。
- API/ABI：新增三个可由全局配置覆盖的 Activate 定时/次数宏和四项诊断统计；Node
  Storage Layout 从 9 升到 10。Host x64 Debug Nano/Lite/Full 为
  `3496/6952/11152 B`，9→10 增量为 `0/0/+144 B`，Wire v5 未改变。
- 软件验证：Windows GCC Full/Lite/Release/Service-OFF 与 MSVC Full 均 `58/58`，Nano
  `48/48`；Product128 为 `59 registered / 43 passed / 16 capacity-skipped / 0 failed`；
  WSL ASan/UBSan 与 `-fanalyzer -Werror` 均 `60/60`。中文源码与中文构建路径门禁使用
  Ninja，MinGW Makefiles 的中文源码根限制明确记为生成器边界。
- 最终一致性：`src/node/ucn_node.c` SHA256 为
  `0BDF4FF6C61DFE5C26594338BA6359F0D9FF70D8A7E5B9EF526577C1E18CA8BC`；507 个
  Markdown、1549 条本地链接、`BadLinks=0`；核心函数索引最新，双语函数注释
  `919/919`，`git diff --check` 无空白错误（仅既有换行提示）。
- 结论/限制：A04/A05 已完成代码与 Host 自审整改，V5-64 继续 `AUDIT HOLD` 等待外部
  复审；尚未据此声明 ESP32、多 Bearer、物理丢包、故障注入或长稳通过。本轮尚未提交、
  未推送。

### OP-458｜整改 V5-64 外审 A01/A02 与中文路径门禁 A03

- 审计核实：确认静态 Destination Route 会错误参与动态 Route Epoch 比较；确认中继
  `PATH_ACTIVATE` 原顺序在容量不足或发送失败时可能留下单向 Route/下游副作用；确认
  MinGW 命令行宏中的非 ASCII 绝对 golden 路径会损坏。
- V564-A01：动态 Route 继续严格匹配 `(origin,destination,epoch)`；静态 Route 改为只在
  没有动态匹配时使用的无 Epoch 通配 fallback。新增动态源→静态中继→静态目标反向
  Route 三节点回归，确认 Epoch 7 原样转发、业务到达且无 RERR/rejection。
- V564-A02：将 Candidate 激活拆成零写入 `prepare` 与无失败 `commit`；中继先同时预检
  两个 Candidate 和两个不同 Route 槽，再同时提交双向状态；下游发送/目标 ACK 失败均
  恢复 Route 快照，Candidate 保留到有界过期供重试，精确重放保持幂等。
- QA-A03：CMake configure 时复制 golden 到构建目录的 ASCII 相对文件名，CTest 固定
  工作目录；删除命令行绝对路径宏，维护模式只生成候选 actual，不自动覆盖提交基线。
- 定向与矩阵：新增单槽不足零发送/零写入、下游 `LINK_DOWN`、ACK `NO_SPACE`、成功
  重试和重复 Activate 回归；Windows GCC Full/Lite/Release/Service-OFF、MSVC 均
  `58/58`，Nano `48/48`。全新中文源码+中文构建副本 Full `58/58`。
- 外审探针复跑：静态 Epoch 探针由 `result=-12/static_egress_business=0` 变为
  `result=0/static_egress_business=1`；Candidate 单槽探针保持 `NO_SPACE`，但由
  `sends=1/reverse=1/forward=0` 变为 `sends=0/reverse=0/forward=0`，双 Candidate 保留。
- 最终自审：WSL ASan/UBSan 与 `-fanalyzer -Werror` 均 `60/60`；Product128 为
  `59 registered / 43 passed / 16 capacity-skipped / 0 failed`；507 个 Markdown、
  1548 条本地链接、`BadLinks=0`；生成函数索引最新，双语函数注释 `918/918`；
  `git diff --check` 无空白错误，仅既有 LF→CRLF 提示。
- 结论/限制：三项 P1 已完成代码与 Host 自审整改，V5-64 仍等待外部复审；没有据此
  宣称 ESP32、多 Bearer、无线丢包、实机故障注入或长稳通过。本轮尚未提交、未推送。

### OP-457｜完成 V5-64 多 Origin Route Epoch 所有权与全量自审

- 问题核实：确认旧动态 Route/Candidate 主要按 Destination 建键，多个 Source 经共享
  中继访问同一目标时，Epoch、RREP、Candidate 和 RERR 存在跨 Origin 覆盖或错绑风险。
- 所有权整改：动态 Route 固定键改为 `(traffic_origin,destination)`，Candidate 固定键
  改为 `(traffic_origin,destination,candidate_id)`；RREQ 学习 `(target,origin)` Reverse，
  RREP 学习 `(origin,target)` Forward。本机发送使用本地 Origin，中继业务使用原帧
  Source，目标按反向实例验证 Route Epoch。
- RERR 自审整改：发现 RERR 外层 Source 是故障检测中继，不能直接作为反向 Route
  Origin；改为从已验证 Payload 的 unreachable target 选择 `target→origin` 回程实例，
  到达源后只撤销对应 `(origin,target)`，不影响另一 Origin 的同目标业务。
- Candidate/防护：Probe/Activate 全程按 Origin 分域；A/B 可复用相同 candidate ID；
  只有 `originated_here` 且 `route_origin == local node` 的 Candidate 才能发起本机事务。
- Candidate 过期槽自审：发现激活复用仍标记 valid 的外域过期 Route 槽时可能继承外域
  Previous Epoch；改为只有同域未过期活动 Route 才建立 Previous，并加入外域 Epoch=9、
  新域 Epoch=10 的定向回归，确认新实例 Previous 完全清空。
- 固定资源/API：Route 表满返回 `UCN_ERR_NO_SPACE`、既有表逐字节不变，并增加
  `route_instance_table_full`；新增 `ucn_node_copy_route_summaries()` 导出 Origin、目标、
  Link、Hop、Cost 与 Current/Previous Epoch。静态 Route 以 Origin 0 表示通配；Nano
  继续只使用静态 Route。
- ABI/资源：Node Storage Layout 从 8 升至 9；Host x64 Debug Nano/Lite/Full 为
  `3496/6952/11008 B`，相对旧布局为 `0/+64/+72 B`。Wire v5 与 Cluster Record 未改，
  但透明 Node Storage 用户必须全量重编。
- 确定性测试：五节点 A/B/R/X/T 覆盖同时发现、不同 Ring/路径、B→A 交叉 RREP、
  不同 Epoch、伪造跨 Origin Epoch 拒绝、相同 Candidate ID 分域、A 域 RERR 与 B 域
  后续业务；另覆盖 Route 表满零覆盖。
- 最终矩阵：Windows GCC Full/Lite/Release/Service-OFF 与 MSVC Debug 均 `58/58`，
  Nano `48/48`；WSL ASan/UBSan 与 `-fanalyzer -Werror` 均 `60/60`。所有目标均在最后
  一次源码/测试修改后重建执行。
- 低资源门禁自审：额外用 `128 B Frame / 3 Link / 4 Peer / 6 Member` 产品配置复测时，
  发现通用 CTest 仍把需要 7 个 Peer 的 8 节点簇拓扑注册为必跑，导致模拟器正确拒绝
  容量后被误报为协议失败。现由模拟器以退出码 77 明确报告容量跳过，CTest 对 Cluster
  Scale 用例统一识别该退出码；最终 `59` 项为 `43 passed / 16 capacity-skipped / 0 failed`，
  受支持的 group=2/4 和所有 Scale 容量合同仍实际运行通过。
- 文档：新增 V5-64 实现文档和全量自审报告，并同步任务表、路线图、Route/AODV/API/
  诊断/资源和理论能力文档。状态保持“Host 软件自审完成，等待外部复审”；未做多板、
  ESP-NOW、多 Bearer、CPU/功耗或长稳实机验证，本轮未提交、未推送。

### OP-456｜RT-01～RT-07 外部复审通过并准备形成独立提交

- 外部复审确认 RT-A08-B 与 RT-A11 闭环，RT-A01～A11 当前没有保留已知
  P0/P1 软件阻断项；RT-01～RT-07 获得受限实验软件范围 GO。
- 同步任务表、Realtime RFC、详细设计、七份分项报告、全体报告与目录导航，统一
  外审状态；不把该签字扩大解释为生产 Node/Service RX/TX、默认固件、真实存储、
  硬件时间戳或时间精度放行。
- 提交前重新执行 staged `diff --check`、文档链接、函数索引、双语注释、默认产品
  实时符号隔离和仓库状态检查，保证代码、测试、文档与审计结论同批入库。

### OP-455｜核实并整改实时模块外审 RT-A08-B 与 RT-A11

- 复核：逐行检查 `ucn_time_domain_ingest_sample()`、
  `ucn_time_domain_get_clock_view()` 与 `ucn_time_authority` 的 Provider 回调门，确认
  “重锁后先暴露 LOCKED”以及未同步静态 Authority Owner 两项问题均真实存在。
- RT-A08-B：在 `ACQUIRING/UNSYNCED/HOLDOVER → LOCKED` 前先按候选
  offset/rate 计算当前 Domain Time，并与同 generation 已发布时间高水位比较；
  算术失败或倒退由 `ingest_sample()` 当场进入 FAULT、返回 `UCN_ERR_STATE`，不增加
  `lock_transitions`，不再等待 `get_clock_view()` 才发现。
- RT-A11：删除 `ucn_time_authority.c` 的静态 `provider_callback_owner`。新增调用方
  持有的 `ucn_time_authority_callback_gate_t`；同一执行域所有 Authority 必须共享
  一个 Gate，由 `ucn_port_ops_t` 的任务临界区保护 active owner。Gate 在 Provider
  回调前建立、返回后精确释放，递归或同时进入的另一 Authority 在零 Provider I/O、
  零对象写回下失败关闭。
- 回归：更新同 generation `11000 → 2000` 反例，断言第二次 ingest 本身就 FAULT；
  新增 POSIX 双线程、双 Authority、独立 Provider、共享 mutex Gate 测试，并在真正
  启用 `-fsanitize=thread` 的非 PIE 可执行文件上通过。
- 第四轮全体自审：Windows GCC Full `58/58`，Lite/Nano/Service OFF/Release 各
  `30/30`；WSL ASan/UBSan 与 `-fanalyzer -Werror` 各 `32/32`；Time Authority
  并发 TSan 定向测试通过；另使用现场 Visual Studio 2019 Build Tools 新建
  Full Release 构建并通过 `58/58`，不沿用旧 API 或旧构建目录作为当前证据。
- 资源：MinGW Release `ucn_time_domain` 为 `5056 B text/0 B bss`，
  `ucn_time_authority` 为 `5040 B text/0 B bss`；Host Authority Owner/Gate 为
  `112/32 B`，RT-07 `sim_domain/sim_member=10728/2152 B`。均不是 MCU 实测。
- 结论：RT-A08-B 与 RT-A11 分项及 RT-01～RT-07 第四轮全体自审通过，继续保持
  `AUDIT HOLD / EXTERNAL RE-REVIEW PENDING`；未接生产 RX/TX、默认固件或真实 BSP。

### OP-454｜核实并整改实时模块外审 RT-A08～A10

- 复核：使用当前工作区重新检查 Time Domain、Timed Link、Time Sync 与 RT-07
  Owner，确认外审两项 P0 和一项 P1 均真实存在，未把常规 CTest 全绿视为反例。
- RT-A08：把 acquisition/filter 清理与同 `domain_generation` 的单调/重放高水位
  分离。UNSYNCED 仍清旧窗口和解，但保留最后接受 sample 与最后发布 Domain Time；
  重锁后候选值低于已发布值时进入 FAULT 且 output 不写回。新增
  `11000 → 2000` 同 generation 确定性反例。
- RT-A09：删除无同步的进程静态 callback-owner 指针。新增调用者拥有的共享
  `ucn_timed_link_callback_gate_t`，同一执行域全部 Link 通过同一个 task/ISR/SMP
  安全锁域串行建立回调 Fence；明确 gate 初始化期和固定锁顺序。新增 POSIX 双线程、
  双 Link、不同 per-Link 锁测试，WSL GCC TSan non-PIE 在禁用 ASLR 后运行通过且
  无 data-race 报告。
- RT-A10：删除 destructive take，改成 `peek → retire → exact ack/pop`。Driver
  cancel 首次返回 `UCN_ERR_LINK_DOWN` 时，release count 与队首 key 保留；下一轮
  重试成功后才出队。错误 ACK 在 Master/Member 回归中均逐字节保持对象不变。
- 分项与全体自审：Windows GCC Full `58/58`；Lite、Nano、Service OFF、GCC
  Release、MSVC Release 各 `30/30`；WSL ASan/UBSan 和
  `-fanalyzer -Wall -Wextra -Wpedantic -Werror` 各 `31/31`。默认产品构建的
  `libucn_core.a` 实时符号为 0；RT-07 模拟器包含 10 个 Timed Link API 符号。
- 资源复测：Host Time Domain/Timed Link/Shared Gate 为 `200/184/32 B`；MinGW
  Release 七个实时对象 text 合计 `47704 B`，Timed Link 不再产生静态 BSS；这些
  仍不是 MCU Flash/RAM/栈或硬件时间戳实测。
- 文档/质量：函数索引重新生成并通过 `--check`；双语函数注释
  `definitions=905 missing=0`；505 个 Markdown、1537 条相对链接、`BadLinks=0`；
  实时源码动态分配与 `TODO/FIXME` 扫描均为 0，`git diff --check` 通过。
- 结论：RT-A08～A10 整改与第三轮 RT-01～RT-07 全体自审通过，状态继续保持
  `AUDIT HOLD / EXTERNAL RE-REVIEW PENDING`；未接生产 RX/TX、真实 BSP 或默认产品。

### OP-453｜核实并整改实时模块外审 RT-A01～A07

- 复核：逐项对照 `ucn_realtime_policy.c`、`ucn_time_domain.c`、
  `ucn_timed_link.c`、`ucn_time_sync.c` 和 RT-07 模拟器，确认外审 3 项 P0 与
  4 项 P1 全部真实存在；状态统一降为 `AUDIT HOLD`，未接入生产路径。
- RT-A01/A02：REQUIRED 接收强制同时通过 E2E 与 Source ACL。新增 uncertainty
  components + known mask，固定聚合 timer resolution、Link capture、filter
  residual、integer rounding 与 Path asymmetry；Domain/样本显式携带 known 状态，
  Policy 发送阶段另加已证明的 sample-capture bound。unknown、零界和溢出均
  fail-closed。
- RT-A03：最大 HOLDOVER 到期进入 UNSYNCED 时清空窗口、cursor/count、offset、
  rate、参考点、输出和有效样本标志；offset 100→900 的新源必须重新积累两次样本。
- RT-A04/A05：Timed Link 新增固定 reservation 生命周期；重复 submit、submit 后
  cancel、重复 completion/retire 均拒绝。Driver callback Fence 覆盖跨 Link、任务
  TX/RX 与 ISR RX 分配，失败不消耗 token。
- RT-A06：Time Sync 的超时、higher-sequence 替换和显式 Abort 先生成固定 release
  obligation，再清 pending；Owner 以 `ucn_timed_link_retire_event()` 回收 Driver
  reservation。release 队列满时保留原事务并失败关闭。
- RT-A07：重写 RT-07 时间戳数据通道，删除测试伪造 key。TX 真实执行
  allocate/reserve/submit/completion，RX 真实执行 ISR 分配与原子 Timed RX Item，
  丢包/切路执行 release/retire/reopen；可执行文件检出 9 个 Timed Link 符号。
- 第二轮自审：GCC `-O3 -Werror` 发现 ISR RX 发布槽位的优化器数组边界告警，已
  改为一次取得并判空 reservation 后再发布；同时发现 Member replacement 的新
  deadline 溢出检查晚于旧 T3 release，已前移检查并补 `UINT64_MAX` 附近的完整
  对象不写回回归。Windows GCC Full Debug `58/58`，
  Lite/Nano/Service OFF/Release 各 `30/30`；MSVC VS2019 Release `30/30`；WSL
  ASan/UBSan 与 `-fanalyzer -Werror` 各 `30/30`。产品 archive 实时符号为 0，
  RT-07 executable 的 Timed Link 符号为 9。
- 资源：Host `Time Domain/Timed Link/Sync Master/Sync Member=200/176/584/240 B`；
  RT-07 `sim_domain/sim_member=10640/2136 B`；MinGW Release 七对象 text 合计
  45760 B、data 0、bss 32 B。固定内存增长来自 reservation/release 表，目标 MCU
  仍须 RT-08/09 重新测量。
- 文档：同步任务表、RFC、详细设计、RT-01～RT-07 分项报告和全体自审报告。
  双语函数注释 `897/897`、函数索引 `--check`、505 个 Markdown/1537 条相对链接
  `BadLinks=0`、`git diff --check` 均通过。当前准确结论为“RT-A01～A07 整改及
  第二轮全体自审通过，等待外部复审”；未提交、未推送，不形成生产接线、真实
  BSP、掉电或实机精度结论。

### OP-452｜完成 RT-01～RT-07 实时模块全体自审与最终整改

- 范围：在七个分项阶段自审之后，对 Metadata、Policy、Domain、Timed Link、四报文 Sync、Capability、Authority 和 RT-07 双域模拟执行一次完整交叉自审；仍保持 `EXCLUDE_FROM_ALL`，不接生产 Node/Service/Adapter/Firmware。
- 新发现与整改：RT-02 原先直接链接 Service Command Guard Codec，导致 `UCN_FEATURE_SERVICE=OFF` 不能独立链接。现改为冻结 12 B 格式的私有无状态 Codec；Service ON 逐字节比较两份实现，Service OFF 保持零 Service 符号依赖。Timed Link 进一步把跨 Link callback Fence 检查提前到 token 分配之前，失败不消耗序号。
- 交叉门禁：确认 R15 的 diagnostic sample 只能更新诊断统计，不能增加有效样本、推动 LOCKED 或生成同步 Envelope；动态 Route 保持零 pending/零四报文。Time Domain 损坏对象、Timed Ring 损坏、Capability 过期槽复用及 Authority 回调重入均有确定性反例。
- 最终矩阵：Windows GCC Full Debug `58/58`、Lite/Nano/Service OFF/Release 各 `30/30`，MSVC VS2019 Release `30/30`；WSL Ubuntu-24.04-ROS ASan/UBSan 和 `-fanalyzer -Werror` 均 `30/30`。首次使用已不存在的 `Ubuntu-24.04` 名称只产生环境错误，改用现场注册的 `Ubuntu-24.04-ROS` 后实际门禁通过。
- 资源/隔离：Release 七对象 text 合计 40016 B、data 0、bss 32 B；这是 Host/MinGW 未链接对象值，不是 MCU 最终 Flash。产品构建实时 artifact=0，`libucn_core.a` 无实时符号，实时源码无动态分配/TODO。双语函数注释 `882/882`，函数索引检查通过。
- 文档：新增 [实时模块全体自审报告](../10-理论与规划/建议方案/UCN_Realtime_Module_RT01_RT07_全体自审报告.md)，同步 RFC、详细方案、RT-01/02 报告、任务状态与建议索引。全仓 505 个 Markdown、1537 条相对链接、`BadLinks=0`。
- 结论：RT-01～RT-07 为“默认关闭的软件候选全体自审通过，等待外部审计”；RT-08/09 实机、真实存储/掉电、生产接线与 RT-10 Hop-aware Wire 均未放行。

### OP-451｜完成 RT-07 双域集成模拟并交叉整改 RT-03/04/06

- 集成：新增默认测试专用的 `ucn_realtime_simulation_tests`，显式链接 RT-01～RT-06。建立 2 个 Time Domain、2 个 Master、8 个 Member，所有同步控制消息均经过实际 payload encode/decode，不直接填充 T1～T4 绕过 Wire 语义。
- 故障矩阵：注入漂移、FOLLOW_UP 丢失、重复/乱序 DELAY_RESP、合法跨 Domain SYNC、diagnostic-only 固定 Path、ordinary dynamic Route、Session/generation/Path 重绑、旧租约/旧帧、租约到期、HOLDOVER→UNSYNCED 和 Master 4 槽容量边界。结果为 39 个有效样本、8/8 Member 最终 LOCKED、模型最坏换算误差 40 us。
- 交叉自审：Time Domain 公共入口新增完整对象合法性门禁；Capability cache 支持不破坏活跃槽的过期槽惰性回收；Timed Link 把 Driver callback Fence 前移并覆盖递归 init/控制 API、交叉 Link、reopen/submit 竞态；TX/RX Ring 增加初始化与结构损坏检查。
- 回归：补合法租约的 Session mismatch、full-cache expired reclaim、跨 Domain 合法帧、两 Link 交叉回调失败不消耗 token、未初始化/损坏 Ring 输出不写回等确定性反例；RT-04 与 RT-07 定向测试 2/2 通过。
- 边界：上述结果只是 Host 确定性软件模型；没有生产 Node/Service/Adapter 接线，没有 UART/CAN/USB/Wi-Fi BSP、真实晶振/ISR 时间戳、Flash/eFuse witness 或掉电验证。RT-01～RT-07 仍待全体自审及外部复审。

### OP-450｜实现并分项自审 RT-06 能力租约与 STATIC_MASTER generation 防回退

- 实现：新增默认不链接的 `ucn_time_capability` 与 `ucn_time_authority`。前者用固定 4 槽完整绑定 Node/Session/Endpoint/Mode/Domain/generation/capability/双向 Path；后者按 witness `H+1` 保留与回读、Authority State 写入与回读的严格顺序发布 generation。
- 自审整改：禁止旧 Session/generation/Path 覆盖活跃租约；准入前复验缓存项；补双向 Path 归属；首次投产与 commissioned 严格互斥；持久状态允许合法的新启动 Session 与新 generation 一起更新；Owner 每个入口复验；Provider 回调前建立对象外和对象内重入门；明确 C 接口不能证明 witness 的物理独立性。
- 回归：新增 capability 与 authority 两组测试，覆盖满槽不淘汰、exact deadline、过期/重启/切路、同步/多次异步 PENDING、回调重入、全零/错 operation completion、回读不一致、已保留 generation 跳号、state 损坏恢复及高水位耗尽。
- 验证：Windows GCC Full Debug 的 RT-01～RT-06 为 7/7；Windows MSVC Full Release 为 7/7；`git diff --check` 无空白错误。Host 对象为 capability cache 304 B、authority owner 104 B；GCC Debug 对象 text 为 2716/5156 B。
- 限制：这仍是实验 API/Fake Provider 软件证据，未接生产 RX/TX，不代表真实 Flash、OTP/eFuse、安全元件、掉电或硬件时间戳通过。下一项为 RT-07 Host 多节点集成与故障模拟。

## 2026-09-03

### OP-445｜实现并自审 RT-01 Realtime Metadata v1 独立 Codec

- 边界：依据用户“开始写实时模块”的授权，只实施默认不链接的 RT-01 Codec；RT-00A R15 仍待外审，未接入 Endpoint Policy、Node RX/TX、Time Domain、四报文同步、Timed Link、Storage、Cluster 或固件。
- 实现：新增 `include/ucn/ucn_realtime.h` 和 `src/extended/time/ucn_realtime.c`，冻结候选 16 B 大端布局、LOCAL/SYNCED/DEADLINE 三种合法模式、严格版本/保留位/Domain/generation 组合、不写回语义和发送端 uncertainty ceil-log2 量化。CMake 新增 `EXCLUDE_FROM_ALL` 的 `ucn_realtime` archive，`ucn/ucn.h` 不隐式包含该模块。
- 测试：新增独立 `tests/test_realtime_codec.c` 与 CTest，逐字节验证三条 Golden Vector；覆盖 15/16/17 B、两个保留位、非法 Mode、LOCAL/共享 Domain 交叉组合、Domain ID/generation、uncertainty 0/1/2/1024/1025/`2^30`/超界/unknown，以及全部失败路径完整 output 不写回；公共头编译门禁同步加入 `test_public_headers.c`。
- 回归：Windows GCC Full/Lite/Nano Debug、Full Release，WSL GCC ASan/UBSan 和 `-fanalyzer -Werror` 均为 23/23；定向重建 `ucn_tests + ucn_realtime_codec_tests` 2/2。默认 `UCN_BUILD_TESTS=OFF` 产品构建只生成 `libucn_core.a`，没有 `libucn_realtime.a`。
- 隔离/资源：Release `ucn_realtime.c.obj` 为 `text=1280 B/data=0/bss=0`，Release archive 2640 B；`libucn_core.a` 不含 realtime 符号，生产 Core/Node/Routing/Transport/Service/Adapter/Port/Cluster 无实时 API 引用，未发现动态分配或可变静态状态，Node Storage 未改变。
- 可维护性：核心函数双语注释门禁为 `definitions=737 missing=0`；生成式源码阅读函数索引已刷新到 2026-09-03 并通过 `--check`。
- 文档：新增 Metadata v1 RFC 和 RT-01 自审报告，同步任务表、建议索引与测试地图。全仓 501 个 Markdown、1542 个本地链接，`BadLinks=0`；`git diff --check` 无空白错误，仅既有 LF/CRLF 提示。
- 限制：本机没有可用 Visual Studio 实例，MSVC 配置阶段无法开始，故 MSVC 明确未验证；未运行任何实机/COM/时间精度测试。RT-01 状态为“可用工具链自审完成，等待 MSVC 补验及外部复审”，不能解释为整套实时模块完成或生产放行。

### OP-444｜核实并整改 RT-00A 第五轮外审的 R15

- 复核：R15 成立。`LOCKED`、滤波窗口和 Domain uncertainty 是全局 Time Domain 状态，不按 Endpoint Requirement 分裂；此前“无可信 asymmetry 的固定 Path 可用于 PREFERRED/诊断”只限制 REQUIRED，仍可能被实现成 PREFERRED 样本推动全局 `LOCKED`，并与 unknown uncertainty 规则冲突。
- 整改：把固定 Path 四报文输出显式分成 `DIAGNOSTIC_ONLY` 与 `VALID_SYNC_SAMPLE`。缺少可信 `max_asymmetry_us` 的 PREFERRED 事务可以完成诊断交换，但不得进入有效滤波窗口、增加连续有效样本、更新 offset/drift、推动 `ACQUIRING -> LOCKED`、设置 `DOMAIN_TIME_VALID`，或生成 `SYNCED_STAMP/DEADLINE` Envelope；调用方只能读取诊断结果或显式回退 LOCAL/NONE。
- 测试门禁：新增有可信上界、无可信上界和动态 Route 三组对照。无上界固定 Path 必须发生四报文诊断但保持 Domain/滤波/Envelope 零副作用；动态 Route 继续保持零 pending、零同步帧。
- 状态：R13、R14 记录为外部复审 GO；`DOC-09/RT-00A` 更新为“第五轮整改完成并自审，等待外部复审”。RT-00/RT-01 继续 HOLD，16 B Envelope、候选 Endpoint、Wire/API/ABI/Storage/Feature 均未冻结或实现。
- 验证：全仓 496 个 Markdown 文件本地链接 `BadLinks=0`，方案 31 个一级章节连续、1309 行、尾随空白 0，SHA256=`67C663C9A50D94F28A72054FAB765878182763C2594A74C1AFEB3D0625E34E9D`；残留扫描确认现行合同不存在“PREFERRED 样本可推动 LOCKED”或“仅对 REQUIRED 禁止”的表述；`git diff --check` 无空白错误，仅既有 LF/CRLF 提示；HEAD 与 `origin/main` 均为 `69901bfb...`，所有工作树变化仍位于 `docs`。本轮只修改文档，不运行 CTest/硬件测试，也不形成时间精度或实时性结论。

### OP-443｜核实并整改 RT-00A 第四轮外审的 R13～R14

- 复核：R13、R14 均成立。方案第 8.3 节曾把“没有可信非对称上界的 Path”笼统写成可用于 PREFERRED，容易被解释为普通动态 Route 也能创建 v1 同步事务；第 12.4 节却明确禁止动态 Route，形成规范冲突。uncertainty 示例也把发送端编码上界误写成目标端组合 `U`。
- R13：依据当前 Path ID/逐跳转发表与 E2E AAD 合同，把“已安装且事务期间不可变的定向 Path”和“普通动态 Route”彻底分开。当时仍把无可信 asymmetry 的固定 Path 写成“只允许 PREFERRED/诊断、不能让 REQUIRED 进入 LOCKED”；第五轮 R15 已进一步收紧为诊断结果对全局滤波、Domain FSM 与 Timed Envelope 均零副作用。普通动态 Route 在任何 Requirement 下都不创建 v1 同步事务，PREFERRED 只允许显式回退 LOCAL/NONE，且必须零 pending、零四类同步帧。
- R14：量化示例改为发送端 `S=1025 us -> class 11`；Envelope 只编码发送端 `S`，目标端解码后才与当前接收端 uncertainty 做 checked-add 得到组合 `U`，避免重复叠加。
- 状态：`DOC-09/RT-00A` 更新为“第四轮整改完成并自审，等待外部复审”；RT-00/RT-01 继续 HOLD，16 B Envelope 和 `0xBC..0xBF` 仍未冻结或实现。
- 验证：逐字段核对当前 `ucn_frame_write_e2e_aad()`，确认 Source、Destination、Session 与可选 Path ID 进入 30 B AAD，而动态 Route Epoch 不进入；全仓 496 个 Markdown 文件本地链接 `BadLinks=0`，方案 31 个一级章节连续、1304 行、尾随空白 0，SHA256=`F1E9515E869B522F870A012212A70DBE55336CA7A35A7592C72E39F9FE5B1B61`；`git diff --check` 无空白错误，仅既有 LF/CRLF 提示；HEAD 与 `origin/main` 均为 `69901bfb...`，所有工作树变化仍位于 `docs`。本轮只修改文档，不运行 CTest/硬件测试，也不形成时间精度或实时性结论。

### OP-442｜核实并整改 RT-00A 第三轮外审的 R10～R12

- 复核：R10～R12 均成立。原方案没有唯一说明 `max_uncertainty_us` 是 Source、Local 还是组合门限；R09 虽已拆出四类本地 event key，却仍把双方 Link Instance 放入同一 Path Contract，并错误声称一个节点可原子取消四个 key；舵机示例的 `<=` 与已冻结的半开 Deadline 不一致。
- R10：把 `max_uncertainty_us` 唯一冻结为目标 Endpoint 允许的端到端组合上界：`U=checked_add(decoded_sender_uncertainty,current_receiver_uncertainty)`，只有两项 known 且 `U <= max` 才通过。等于门限接受、门限加一拒绝；发送端单项检查只可作为不改变最终结论的提前拒绝，不能替代目标端组合门禁。
- R11：将跨节点状态拆成受认证的 `WireTimeTxnKey` 与 caller-owned 本地事件绑定。Wire 键只包含 Domain/generation/sequence、Master/Member Node+Session 和正反 Path Identity；Master 只拥有 T1/T4，Member 只拥有 T2/T3，Link Instance generation 与 token 永不上 Wire。节点只原子撤销自己的 pending/key；对端通过认证的新 Session/generation/Path/sequence、可选 Abort 或强制 timeout 收敛。
- R11 边界：`TIME_DELAY_RESP_V1` 改为匹配 Wire 事务键并要求 Master 本地 T4 已完成，不再匹配 Member token。首版 REQUIRED 同步只允许两条已安装、不可变且有可信非对称上界的定向 Path。当时将普通动态 Route 笼统写成“可用于 PREFERRED/诊断”，该表述经第四轮 R13 复审确认过宽，现行合同由 OP-443 取代：动态 Route 不创建 v1 同步事务。补充单边 reopen/切路、迟到本地事件、迟到 Wire 报文和双方对象不保存对端 key 的回归合同。
- R12：舵机示例改为 `4,700 < 5,000`。文档同时明确 uncertainty 门限使用包含上界 `<=`，Deadline 使用半开区间 `<`，二者不能互换。
- 状态：`DOC-09/RT-00A` 更新为“第三轮整改完成并自审，等待外部复审”；`RT-00/RT-01` 继续 HOLD。16 B Envelope、候选 Endpoint、Wire/API/ABI/Storage/Feature 均未冻结或实现。
- 验证：同步测试矩阵、发布门禁、决策表、建议索引和任务表；全仓 496 个 Markdown 文件本地链接 `BadLinks=0`，方案 31 个一级章节连续、1293 行、尾随空白 0，SHA256=`BA913478AD7D230DD024C5C0DB14E5C808A1D2D0C4B9F68DF0239928D7EC7ED5`；`git diff --check` 无空白错误，仅既有 LF/CRLF 提示；跟踪与未跟踪变更均仍位于 `docs`。本轮不运行 CTest/硬件测试，也不形成时间精度或实时性结论。

### OP-441｜核实并整改 RT-00A 第二轮外审的 R06～R09 合同缺口

- 复核：四项 P1 均成立。普通状态双槽只能抵抗撕裂写，不能证明已发布后损坏的较新 generation 从未使用；现有 Command Guard 是 `uint32 issued_at_ms` 并由本机 `now_ms` 作半开过期判断，不能直接替代 `uint64` Domain Time；16 B Envelope 的 `SOURCE_HOLDOVER` 位不携带可验证的远端 holdover age；当前普通 RX Item 没有 token，T1～T4 也不能依赖一个 token 或两个 Ring 的顺序配对。
- R06：新增独立 `issued_generation_high_water` witness 合同。REQUIRED STATIC_MASTER 必须先原子保留并 reload 证明 H+1，再写 Domain 状态槽，最后才允许发布；保留后失败只允许跳号。若 witness 缺失/损坏/回退或槽位存在发布歧义则 fail-closed/换新 Domain。精确规定 `A=gen10`、已发布 `B=gen11` 后 B 损坏只能保留 12 或失败关闭，禁止再次发布 11。
- R07：冻结 Timed Command 的唯一时钟域。Envelope `capture_time_us` 是签发时刻；Guard `issued_at_ms` 只等于该值除以 1000 后的低 32 位，用于 ABI 绑定，绝不再用本机 uptime 或 Domain 低 32 位计算年龄。有效期取 Endpoint 与 Guard 两者最小值，并统一为 `age_upper_us < effective_max_age_us` 的半开区间；补充 checked future-skew 公式和 `C-R==U/U+1` 边界。
- R08：承认 v1 接收端无法验证发送端 holdover age。REQUIRED 固定拒绝 `SOURCE_HOLDOVER=1`；PREFERRED 默认拒绝，仅在产品显式信任发送端自检、使用 E2E 认证和专用 ACL 时例外。本地 HOLDOVER 仍独立按本地 age/uncertainty Policy 判断。
- R09：将 T1/T2/T3/T4 定义为四个不同本地 event key，Wire 只用消息角色、Domain/generation、Session 和 `sync_seq` 关联；pending 槽保存 `expected_event_role+sync_seq+key`。RX 禁止 frame Ring 与 timestamp sidecar Ring 顺序配对，改为原子入队 `{完整帧,event key,timestamp}`；Link Instance generation 与 event token 都使用 checked-next/no-wrap，耗尽进入 FAULT，只有停止硬件/DMA/回调、清空队列、重建 Owner 与 Node Session 后才能建立新命名空间。
- 状态：`DOC-09/RT-00A` 更新为“第二轮整改完成并自审，等待外部复审”；`RT-00/RT-01` 继续 HOLD。16 B Envelope、`0xBC..0xBF`、Wire/API/ABI/Storage/Feature 均未冻结或实现。
- 验证：逐项交叉核对 `ucn_service_command_guard_t`、`ucn_service_command_guard_validate()`、`ucn_deadline_expired()` 和 `ucn_adapter_rx_item_t` 的当前合同；同步任务表、建议索引、测试矩阵、资源预算与发布门禁。全仓 496 个 Markdown 文件本地链接 `BadLinks=0`；方案 31 个一级章节连续、1243 行、尾随空白 0；`git diff --check` 无空白错误，仅既有 LF/CRLF 提示；变更仍全部位于 `docs`。本轮是纯文档整改，不运行 CTest/硬件测试，也不形成精度或实时性结论。

### OP-440｜核实并整改实时方案 RT-00A 的四项外审缺口

- 复核：逐项对照当前源码确认审计成立。`ucn_frame.c` 严格拒绝未知 Flag；`ucn_link_ops_t.send()` 不返回 token；`ucn_adapter_rx_item_t` 与 `ucn_protocol_owner_rx_enqueue()` 不携带 timestamp/token；Command Guard 从 Payload offset 0 解码；Service 默认 Payload 为 32 B。因此四项均会影响首版 ABI/安全判断，不能留到编码时临时决定。
- uncertainty：首轮方案补充 timer 分辨率、Link 打点、路径非对称、滤波残差、晶振 holdover 漂移、采样锁存和整数舍入的保守求和；任一 unknown/溢出/超过 `2^30 us` 均编码 class 31，其他值使用唯一向上 ceil-log2。当时仅写“没有可信 Path asymmetry bound 时 REQUIRED 不得 LOCKED”，且把双方本地状态混入跨节点键；该合同已先后由 OP-441～OP-444 取代。现行规则是无可信 asymmetry 的结果只进诊断，不能改变任何全局 Domain 状态或生成 Timed Envelope。
- Deadline/ABI：冻结接收入队前与业务副作用前的两级 Deadline gate；Envelope 一直保留在固定 Payload 中，执行时重新解析，不给普通 `ucn_service_message_t` 增加时间字段。复合布局固定为 `Realtime Envelope | Command Guard | Business Payload`，必须使用新的 Endpoint/ABI；默认 32 B 下三者组合只剩 4 B 业务数据。
- Port/硬件：用可选 Timed Link/Source sidecar 替代无法落地的“按 token 读最近时间戳”草案；定义两阶段 TX token、RX sidecar、`{Link, Instance, direction, token}` 命名空间、pending 不复用、no-wrap、Ring 满失败关闭。`SAMPLE_CAPTURE_HW` 与 Link RX/TX 硬件时间戳拆成不同 Flag/Capability。
- Domain/控制面：32-bit generation 仅在单一认证 `STATIC_MASTER v1`、双槽持久递增、persist-before-sync、reload 证明、独立可信配网标记和成员新鲜启动握手合同下保留；主备/多 Master 后置并须重开 Era/字段宽度评审。候选四个 Q1 单播 Time Endpoint 为 `0xBC..0xBF`，每 Peer 一个 pending、按 Node ID 错峰并受 Owner/Link 控制预算；编号仍未发布保留。
- 状态：新增 RT-00A 冻结前章节与五项门禁，统一 HOLDOVER 为“UNSYNCED/FAULT 必拒；默认拒绝 HOLDOVER，Endpoint 明示且时长/uncertainty 合格才允许”。`DOC-09` 改为“整改完成并自审、等待外审”；RT-00/RT-01 继续 HOLD，不修改源码、Wire、API、ABI、Storage、CMake、测试或固件。
- 自审补强：明确专用 timed-control send 仍必须经过 Core/Path/Security，只在源端第一跳和目标端最后一跳打点；生产 REQUIRED Endpoint 必须 E2E Protected+ACL；提高 Service Payload 上限会放大每个固定队列项，不能以“Envelope 不新增字段”误称零 RAM。
- 验证：496 个 Markdown 文件本地链接检查 `BadLinks=0`；新方案 31 个一级编号章节连续、1151 行无尾随空白；`git diff --check` 无空白错误，仅既有 LF/CRLF 提示。确认变更仍全部位于 `docs`，本项纯文档整改，不运行 CTest/硬件测试，也不形成时间精度或实时性结论。

### OP-439｜形成可选实时元数据与分布式时间同步详细设计

- 核对：以 `main@69901bf` 为事实基线，复核 `ucn_frame_t`、Wire v5 W0～W3、固定 E2E AAD、Q0～Q3、Service Command Guard、本机 Deadline 和 Event-driven Owner；确认当前基础 Frame 没有通用跨节点时间字段，独立 MCU uptime 不能直接计算单向延迟。
- 设计：新增 `docs/10-理论与规划/建议方案/UCN_可选实时元数据与分布式时间同步详细设计方案.md`，把时间模式、降级要求和中继行为拆成三个维度；同一 Node 的不同 Endpoint/流可以分别使用 `NONE/LOCAL_STAMP/SYNCED_STAMP/DEADLINE`。
- Wire 边界：推荐第一阶段使用固定 16 B Payload Envelope，普通帧零额外 Wire 字节、旧中继透明转发、E2E Protected 自动保护时间字段；Hop-aware Deadline 明确后置到生产安全 S02 和新 Wire/AAD 评审，不在当前 v5 Flags 中临时插字段。
- 架构/任务：详细定义 Time Domain 状态、Master 来源、四时间戳流程、独立 Time Port Ops、ISR/Owner 边界、Capability、Required/Preferred 失败策略、Profile/资源预算、RT-00～RT-10 实施与单元/模拟/实机验收。旧 2026-08-15 建议标记为 SUPERSEDED 并链接新方案。
- 验证：新方案及相关索引的本地 Markdown 链接检查为 0 坏链，尾随空白为 0；当时 30 个一级编号章节连续，`git diff --check` 无空白错误（仅工作树既有 LF→CRLF 提示）。本次纯文档任务未运行协议 CTest 或硬件测试；后续 RT-00A 整改另见 OP-440。
- 限制：本项只写建议设计并同步索引、任务和记录；没有修改 Core/Cluster/Adapter 源码，没有启用时间 Feature，也没有形成 Host 或 MCU 精度、硬实时或 EtherCAT 等价结论。

### OP-438｜形成 UCN 后续主要工作与分阶段实施路线图

- 核对：以 `main@69901bf` 为事实基线，复核总任务表、当前成熟度、发布签字清单和 Cluster 发布阻断；没有把 Host 软件模型、UART/ESP-NOW 部分实测或受限 Cluster 组件扩大为生产完成。
- 路线：新增 `docs/00-项目管理/04-UCN后续主要工作与分阶段实施路线图.md`，将后续工作拆为 Core 协议收口、S02 生产安全、第一参考平台、真实 Cost、多 Bearer/资源长稳、Cluster Target 和发布工程七条主线。
- 重点：明确 Q0～Q3 先完成外审，V5-64 多 Origin Route Epoch 是下一项最值得优先处理的 Core 协议缺口；随后完成 HELLO Capability/Path MTU，并以 ESP32-S3+FreeRTOS+UART/ESP-NOW 作为推荐参考产品起点。
- Cluster：继续保持 M05 `AUDIT HOLD` 和 M14 `RELEASE NO-GO`；把 v4 生产接线、真实 Flash/掉电、C06 跨簇 Transfer、C07.7 主备实机和 C08 两级/万级规模列为独立发布线，不阻塞 Core v5 先形成候选版本。
- 治理：同步项目管理索引与 DOC-08；文档规定每项必须形成设计、单元/负向、模拟、构建矩阵、适用实机、自审和外审证据，并列出当前不应继续扩张的功能范围。本项不修改协议源码、Wire、API、ABI、Storage 或默认 Feature。

### OP-437｜完成 QOS4-08J 的 Q3 队列竞争与 Q0/Q3 五跳 RTT 实测

- 范围：ESP32 测试工程六板 A～F=`COM40/COM53/COM58/COM56/COM5/COM34`，UART-only 691200，A→F=`5 Hop/Cost 170`。本项未修改 UCN 主库 Core/Wire。
- 夹具：每个探针前把 A 的 Q3 队列填至明确 `UCN_ERR_NO_SPACE`；Q0 进入独立队列，Q3 探针保留首次尝试时间并等待 Owner drain 后重试。F 独立回报背景帧 received/invalid，避免以本地 accepted 冒充交付。
- 预检：R01 的探针期 admission wait=`0/0`，未形成目标竞争，排除。逐探针重新补满 Q3 后执行 R02/R03 两个独立同步复位正式轮。
- 正式结果：两轮 Q0/Q3 均 100/100，admission wait=`0/100` 每轮；背景合计 accepted/remote=`602/602`、非法=0，Q0/Q3 scheduled=sent=`202/802`。合并平均 RTT=`15.984/16.343 ms`，P50=`16.458/15.901 ms`，P95=`17.044/22.216 ms`，P99=`17.268/23.078 ms`。
- 链路/判定：十个 UART 接收方向 decode/length/overflow/noise/no-space/partial/drop 全为 0；C/uart0 与 E/uart0 各有一次 Adapter rejected warning，但 400 个探针和 602 个背景帧全部守恒。结论是 Q0 在该受控软件队列竞争中稳定收紧尾延迟；P50 并非单调更低，不能宣称严格优先、物理 UART 抢占或硬实时 SLA。
- 验证/证据：六个测试环境均构建成功；A 自动烧录并由 esptool 写后校验；分析器 `py_compile` 和 R02/R03 严格门禁均 PASS。日志 SHA256=`2BB5EAD7...BB2B95A`、`63C1BB41...A75127`；完整固件、JSON/Markdown Hash 和不可外推范围见测试工程 `docs/六节点实机测试/H6-16-Q-Q3持续队列竞争下Q0与Q3五跳RTT实测报告.md`。
- 下一步：QOS4-08 的软件队列竞争缺口已闭合。后续优先验证 CAN/CAN-FD/USB 的硬件优先级映射；当前只有 UART 台架时不得冒充完成，可先做六板 CPU/栈/RAM 精确采样。本轮文档与测试夹具尚未提交、未推送。

### OP-436｜完成 QOS4-08I 的 Q0/Q3 五跳 Transfer 压力 RTT 实测

- 范围：ESP32 测试工程 A～F=`COM40/COM53/COM58/COM56/COM5/COM34`，UART-only 691200，A→F=`5 Hop/Cost 170`。A 在 T32～T8K 等工作量 Transfer 压力中以 10 ms 周期交替发送 Q0/Q3，各 100 个/轮。
- 夹具：仅在测试工程扩展延迟类别和分析器；启动日志显式冻结 Q0/Q3，旧 Q0/Q1 日志回归仍 PASS。UCN 主库 Core/Wire 和默认产品行为未改。
- 正式结果：两轮 Q0/Q3 均 100/100、零拒绝/重复/非法；Transfer 每轮 9/9、127/127、351 Fragment、零重试/失败；十个 UART 方向零 Carrier/drop。合并平均 RTT=`21.583/21.708 ms`、P50=`19.611/18.923 ms`、P95=`37.411/35.842 ms`、P99=`43.086/42.521 ms`。
- 结论：当前负载证明 Q0/Q3 均能在五跳压力中有限到达，但统计差异小且排序不一致；它没有持续填满同一四级队列，所以不能据此声明 Q0 具有可测硬件抢占优势。下一项应使用持续 Q3 backlog + 低频 Q0 探针的专用竞争夹具。
- 证据：Run 1/2 日志 SHA256=`E97837FF...1FDCFD`、`51956367...C9CFB5`，两份严格 JSON 均 PASS。完整固件 Hash、分布与不可外推范围见测试工程 `docs/六节点实机测试/H6-16-O-Q0与Q3五跳压力RTT实测报告.md`。

### OP-435｜完成 QOS4-08H 六板 UART 五跳双向四级并发实测

- 范围：ESP32 测试工程 A～F=`COM40/COM53/COM58/COM56/COM5/COM34`，完整 UART-only 691200 线形拓扑；A/F 同时 Source+Sink，以 1 ms 业务节拍运行 Q0～Q3 `6:3:2:1`。
- 构建/烧录：新增六个五跳双向专用环境；六环境均构建成功并由 esptool 自动上传、写后校验，无需手动 BOOT/EN。A/F RAM=`69500 B`，B/C/D/E RAM=`65444 B`。
- 正式结果：Run 2/3 的四份方向分析全部 PASS。A→F/F→A 均为 5 Hop/Cost 170、1207/1207；每方向检查点 `600/300/200/100`，最终 `604/301/201/101`；Q0/Q2/Q3 gap、全类乱序/内容错误、Queue expired/error 和 10 个 UART 方向 decode/length/overflow/no-space/partial/drop 均为 0。双向检查点合计约 1592～1595 frame/s。
- 语义/边界：Q1 的 904/905 gap 为 Latest 合并；业务 Queue no-space 被夹具保留重试并最终完整发送，不是 UART Driver no-space。当前业务点未饱和，只证明五跳全双工流水线与四个中继有当前余量；不替代 H6-16-F 饱和矩阵、H6-16-G RTT 分布，也不放行硬实时、CAN/USB 或六板多 Bearer。
- 证据：Run 2/3 日志 SHA256=`E53271CC...13C8A9`、`515930D9...B8EAF6`，四份方向 JSON 均为 PASS。完整固件 Hash、统计和限制见测试工程 `docs/六节点实机测试/H6-16-N-Q0至Q3-UART五跳双向并发实测报告.md`。

### OP-434｜完成 QOS4-08G 的 UART 双向四级调度与接收余量实测

- 范围：ESP32 测试工程 A/B=`COM40/COM53`，UART-only 691200，一跳；两端同时 Source+Sink，以 1 ms 业务节拍运行 Q0～Q3 `6:3:2:1`。
- 反例与定位：首轮两端 Source 都提交 1207 帧，但 Sink 只有 1137/1147，B/uart0 有一次 COBS decode。测试环境仍为中继 B 创建未接线 UART1，且 1 s 大段诊断日志与业务窗口重叠，不能把该轮归因于四级调度器或 UART 固有能力。
- 测试夹具整改：新增默认关闭的 UART0-only 测试配置，删除悬空 Link 对象；恢复 30 s 诊断周期；分析器改用本轮 QOS BEGIN 路径证据并按最终累计 UART 快照判定。未修改主库 Core/Wire，也未降低 691200 或 1 ms 节拍。
- 正式结果：连续 Run 3/4 的四份方向分析均 PASS；每方向最终 Source/Sink=`604/301/201/101`、1207/1207，Q0/Q2/Q3 gap、全类乱序/内容错误、Queue expired/error 及 UART decode/length/overflow/no-space/partial/drop 全为 0。双向检查点合计约 1152～1626 frame/s。
- 证据/边界：日志 SHA256=`3CA40C3D...C2F0F4`、`5ABDB466...C5804B`。本项只签 A/B 单跳 UART 双向受限范围；五跳双向、生产统一 Adapter readiness、CAN/USB、硬实时 SLA 和功耗仍未通过。完整报告位于测试工程 `docs/六节点实机测试/H6-16-M-Q0至Q3-UART双向并发与接收余量实测报告.md`。

### OP-433｜完成 QOS4-08F 的 ESP-NOW 双向并发与单播确认边界实测

- 范围：ESP32 测试工程 A/B=`COM40/COM53`，两端同时 Source+Sink，ESP-NOW-only 一跳；每个方向运行 Q0～Q3 `6:3:2:1`，并继续使用 08E 的测试专属 TX-completion readiness gate。
- 反例：六节点稳定性基线的广播承载下，A→B/B→A 均由 UCN 成功提交 1207 帧但对端只收到 1201 帧，各缺 6 帧（约 0.497%）；两端本地 `txq.high/full=5/0`、callback failure=0，确认 readiness 只闭合本地背压，广播完成不是对端交付证明。
- 正式结果：保留 HELLO 广播、把已准入 Data 改为 ESP-NOW 单播后，连续两轮共四个方向严格分析均 PASS。每方向检查点精确 `600/300/200/100`，最终 Source/Sink 均 `604/301/201/101`、1207/1207；双向合计约 173 frame/s，Q0/Q2/Q3 gap、全类乱序/内容错误、物理 full 和 TX callback failure 均为 0。
- 结论/边界：08F 证明当前 A/B 单 Peer 环境可以双向并发推进，也冻结了广播/单播不同的交付语义；不等于六节点动态单播 Peer、端到端可靠 Data、主库通用 Adapter readiness、CAN/USB 或生产 SLA 已完成。详细证据见测试工程 `docs/六节点实机测试/H6-16-L-Q0至Q3-ESP-NOW双向并发与单播确认实测报告.md`。

### OP-432｜完成 QOS4-08E 的 ESP-NOW 四级调度与物理背压实测

- 范围：ESP32 测试工程 A/B=`COM40/COM53`，ESP-NOW-only 单跳；直接编译当前 UCN 工作树。C～F 保持 quiet，不把已知异常 COM58 纳入业务结论。
- 反例：固定 1 ms 时，1200 帧成功样本退化为 Q0/Q1/Q2/Q3=`1165/21/10/4`，物理队列 `high/full=16/1148`；固定 15/20/25 ms 虽得到精确成功比例，仍分别出现物理 full=7/13/19。证明固定周期只控制平均速率，不能代表异步 Driver 当前可接收。
- 测试夹具：ESP-NOW Adapter 暴露 Owner-only pending 观测；Owner 在 Core Step 前以物理 TX queue 高水位做 readiness preflight，满余量门关闭时等待 TX-completion 通知，10 ms polling 只保底。修改仅在 PlatformIO 测试工程、默认宏关闭；UCN 主库 Core/Wire 未改。
- 正式结果：A→B 两轮和 B→A 一轮均严格 PASS。检查点都精确为 `600/300/200/100`，帧率 `85.806～87.197 frame/s`；最终 Source/Sink 均为 `604/301/201/101`、1207/1207，错误/过期、Q0/Q2/Q3 gap、全类乱序/内容错误、物理 full 均为 0。Q1 gap 与 Latest-Value 合并语义一致。
- 结论/边界：08E 增加 ESP-NOW 单跳 AB 双向受限 PASS，但不等于主库生产 readiness API、双向同时空口竞争、六板无线、CAN/USB、CPU/栈/功耗或实时 SLA 已完成。详细日志 Hash、固定节拍反例与设计结论见测试工程 `docs/六节点实机测试/H6-16-K-Q0至Q3-ESP-NOW单跳与背压闭环实测报告.md`；QOS4-08 总体仍是部分实机通过。

### OP-431｜完成 QOS4-08D 的 AB 多 Bearer 功能门禁并保留六板硬件失败

- 范围：ESP32 测试工程 A/B=`COM40/COM53`，UART 691200 与 ESP-NOW 同时在线；Service 同时保持 Q0、两个 Q1 Key、Q2、Q3，Cost 夹具注入 UART 压力并恢复。C～F 作为六板正式轮控制节点，其中 C 为 COM58 实体板。
- AB 正式结果：严格分析 PASS。A/B 均完成 `UART→Wi-Fi→UART`，切换/恢复 A=`2850/3930 ms`、B=`2897/3983 ms`；四级背景的 Bridge/Sink 均为 `599/150/150/200/101`，Bridge failure、非法内容、乱序为 0，六个切换窗口业务持续推进。
- 六板阻塞：COM58/C 的完整组合镜像反复 WDT/Panic。纯 ESP-NOW Ring 120 s 稳定，停止 Wi-Fi 帧进入 UCN Core 后仍复位，COM56 同类完整镜像 120 s 稳定；将 COM58 应用 Flash 降至经镜像头核验的 40 MHz 后，180 s 内仍有 5 次非预期启动和 2 次 Panic。六板 75 s 轮中 C 又非预期启动 3 次并污染 D/uart0，严格总分析 FAIL。
- 结论/边界：QOS4-08D 为 `PARTIAL（AB 功能 PASS；六板硬件 FAIL）`。没有据此修改 UCN Core，也未把 COM58 故障写成 Q2/Q3 缺陷。换板后必须先做 120 s 单板完整组合，再重跑 75 s 六板严格门禁。完整原始日志、Hash、回溯和判定见 ESP32 工程 `docs/六节点实机测试/H6-16-J-QOS4多Bearer与COM58稳定性诊断报告.md`。

### OP-430｜完成 QOS4-08C 的六板五跳 Service Q1 多流公平性实机门禁

- 现场/范围：ESP32 测试工程使用 A～F=`COM40/COM53/COM58/COM56/COM5/COM34`、Node ID=`0x10000001..06` 和 A—B—C—D—E—F 五段 691200 UART；A→F 实际路由为 5 hop/Cost 170。专用环境直接编译当前 UCN 工作树的 Full+Service，默认产品不开启测试宏。
- 夹具：使用两个不同 Endpoint 的 Q1 Latest Key；热点 Key 持续更新，cold Key 每次只保留一个待验证样本。第二阶段再让 Q0/Q2/Q3 保持 backlog，实测 Q1 内部 cursor 与外层 `6:3:2:1` 的组合等待上界。新增完整 Payload/序号校验、出站观察和严格日志分析器。
- 失败与修正：不限速 Run 1 虽得到 Q1 等待 2/8，但 A/uart0 `no_space=1339`、Bridge failure 242/1097，属于物理过载混入公平性测量；测试夹具增加每 2 ms 最多一条的 Bridge 准入上限，Core、Service 和产品默认值未改。Run 2 数据全过但漏采 D BOOT，未放宽门禁。
- 正式结果：Run 3 六板各一次启动。纯 Q1 阶段 cold=200/200、最大 Q1/总等待=2/2；混合阶段 cold=200/200、最大 Q1/总等待=2/8，Bridge/Sink=`799/200/200/267/134`。Q0/cold/Q2/Q3 零缺口，热点 gap 与 Latest overwrite 一致；两阶段 Bridge failure/invalid/order=0，十个 UART 方向所有 Carrier/Queue 错误为 0。严格分析器 PASS。
- 证据/边界：正式日志 SHA256=`b63cb874...5c3e89`、JSON=`cc4d611c...e10315`，完整证据位于 ESP32 工程 `docs/六节点实机测试/H6-16-I-Service-Q1多流公平性实测报告.md`。本结果为 QOS-A02 的受限实机证据，不解除 QOS-A01～A08 的外审 HOLD，也不覆盖多 Bearer、CAN/USB/Wi-Fi、断链、CPU/栈/功耗或端到端可靠。

## 2026-09-02

### OP-429｜完成 QOS4-08B 的六板五跳四级调度实机门禁

- 现场：第六块板重新接好后，A～F 映射确认为 `COM40/COM53/COM58/COM56/COM5/COM34`、Node ID=`0x10000001..06`，物理链为 A—B—C—D—E—F；A→F 路由为 5 hop/Cost 170。
- 构建/烧录：ESP32 测试工程直接编译当前 UCN 工作树，新增六个默认产品不启用的 QOS4 五跳环境；六目标 Release 构建成功并完成 esptool 写后 Hash 校验。中继静态 RAM 约 62,876 B，端点约 68 KB。
- 正式结果：UART-only 691200、16 B payload、1 ms 测试节拍。1205 ms 检查点的 Queue sent 精确为 Q0/Q1/Q2/Q3=`600/300/200/100`，最终 A/F 均为 `604/301/201/101`、1207/1207；错误/过期、Q0/Q2/Q3 缺口、全类乱序和内容错误均为 0，Q1 的 903 个 gap 符合 Latest-Value 覆盖语义。六板各启动一次，45 s 内保持在线，十个 UART 方向 Carrier 错误为 0，严格分析器 PASS。
- 对比：F 端一跳/五跳接收跨度为 1244/1245 ms，对应约 `970.257/969.478 frame/s`。约 0.08% 差异仅说明当前 1 ms 业务节拍低于逐链路饱和点、转发可流水化；不能推广为饱和吞吐逐跳损耗，饱和数据仍以 H6-16-F 为准。
- 边界：QOS4-08 仍只是部分实机通过。多 Q1 key、硬件独立优先级队列、多 Bearer、CAN/USB/Wi-Fi、断链、CPU/栈/功耗均未覆盖；Q2/Q3 软件闭环仍等待外部审计签字。完整证据位于 ESP32 测试工程 `docs/六节点实机测试/H6-16-H-Q0至Q3四级调度实测报告.md`。

### OP-428｜完成 QOS4-08A 的 ESP32-S3 一跳四级调度实机门禁

- 现场：六板接入后 Windows 只实际枚举 A/C/D/E/F=`COM40/58/56/5/34`，历史 B/COM53 缺失；因此只用仍直连的 E→F 段测试，未宣称六节点或多跳通过。
- 源码：ESP32 测试工程直接编译当前 UCN 工作树；HEAD 基线为 `7512bc3`，关键 Hash 为 `ucn_node.c=cae11317...d84524`、`ucn_node.h=d43c3cb1...fa131`，覆盖本工作区尚未提交的 QOS-A01～A08 整改。
- 正式结果：691200 UART-only、1 hop/Cost 34、16 B payload、1 ms 测试节拍；1200 个成功队列发送精确为 Q0/Q1/Q2/Q3=`600/300/200/100`，错误/过期 0；排空后 Source/Sink 均为 `604/301/201/101`、总计 1207，Q0/Q2/Q3 零缺口、全类零乱序和内容错误。Q1 的 907 个 gap 是 Latest-Value 覆盖语义。
- 压力边界：不限速轮约 `1477.833 frame/s` 时出现 32 次物理提交错误，Q2/Q3 分别出现 13/7 个缺口；严格分析器按预期 FAIL。受控正式轮为 `983.607 frame/s`、16 B 应用 payload `15.369 KiB/s`，严格分析器 PASS。
- 边界：本轮只给 QOS4-08 增加“一跳 UART 部分实机通过”证据；未覆盖硬件独立优先级 FIFO、完整六节点、多跳、多 Bearer、CAN/USB/Wi-Fi、断链、CPU/栈/功耗或产品 SLA。完整证据位于 ESP32 测试工程 `docs/六节点实机测试/H6-16-H-Q0至Q3四级调度实测报告.md`。

### OP-427｜修复 QOS-A07 负枚举 Policy 旁路并清理 A08 当前文档残留

- 审计核实：QOS-A07 属实。旧 `ucn_node_find_route_policy()` 在合法性判断前把运行期枚举强转为 `uint8_t`，使 `-255/-256` 分别别名成 Q1/Q0；`send_endpoint_internal()` 又先查 Policy，存在非法 Class 命中 wildcard/指定 Policy 的旁路。修复前新增对抗回归已实际失败，证明反例可达。
- 双重门禁：`send_endpoint_internal()` 现在在任何 Policy 查询前以 `(uint32_t)traffic_class >= UCN_TRAFFIC_CLASS_COUNT` 失败关闭；Policy 内部 validator 改为接收原始枚举并独立做相同范围校验，不依赖调用方顺序。
- 回归：公共 Node/Endpoint 和 Policy 路径统一覆盖 `-1/-255/-256/CLASS_COUNT`；失败必须返回 `UCN_ERR_UNSUPPORTED`/无匹配，且 Link 发送次数、`policy_match_hits` 和 Node 发送统计不变。Nano/Lite/Full 均执行公共入口测试，Full 额外执行指定/wildcard Policy 对抗测试。
- 文档：QOS-A08 属实。已同步 Transfer 当前映射 `T32/T64 Direct→Q2、T128～T8K Fragment→Q3、ACK→Q1`，并更新节点内任务通信、Call Tree、快速手册、容量总览、分层和整体架构中的 Q0～Q3 当前语义；历史版本/当时实测记录不反向改写。
- 软件矩阵：Windows GCC Full/Lite/Release/Service-Off 均 `53/53`，Nano `43/43`；Windows MSVC Full（非 ASCII 路径）`50/50`；WSL ASan/UBSan 与 `-fanalyzer -Wall -Wextra -Werror` 均 `53/53`。
- 状态：QOS-A07/A08 整改自审通过，QOS-A01～A08 整体继续 `AUDIT HOLD / EXTERNAL REVIEW PENDING`。未连接、烧录或操作 ESP32；QOS4-08 仍只允许探索性硬件测量。当前工作区未提交、未推送。

### OP-426｜核实并整改 Q2/Q3 审计 QOS-A01～A06

- 审计核实：六项均可在当前 `7512bc3` 基线上确认。负 Traffic Class 在 MSVC 有跨编译器差异；Service Q1 固定从 slot 0 扫描可让第二个 Latest key 永久饥饿；Service 深度 256 与 `uint8_t` 索引/计数不相容；Node 三组统计不是同一漏斗；Windows 窄字符 `fopen` 无法可靠访问 CMake 注入的非 ASCII 路径；部分 current official/FSM 文档仍停留在 Q0/Q1。
- QOS-A01：Frame、Full/Lite Node、Nano Node 和 Service 统一使用无符号 Class 范围检查。新增 `-1` 枚举回归，验证编码大小为 0、返回参数错误且完整输出/长度不写回；MSVC 与 GCC 均已运行通过。
- QOS-A02：Service Router 增加固定 1 B `remote_q1_cursor`；成功取出 Q1 后从下一槽继续扫描。回归持续补充首个热点 key，并证明第二个冷 key 在两个已占槽内得到服务。
- QOS-A03：Service Binding、Remote Q0～Q3 与 Inbox 深度增加 `1..UINT8_MAX` 编译期合同。CMake `try_compile` 在 GCC/MSVC 均确认 255 可编译、256 被拒绝。
- QOS-A04：Node stats 新增 `tx_queue_sent_by_class[4]`。`enqueued/scheduled/queue_sent` 只描述 Node Queue；原 `tx_sent_by_class` 明确为即时发送、排队发送、转发等全部 Link 成功提交总量。Storage Layout 从 7 升至 8，Host x64 Debug Nano/Lite/Full 为 `3496/6888/10936 B`，每档固定增加 16 B。
- QOS-A05：Cluster golden trace Host 测试在 Windows 把 UTF-8 路径转换为宽字符，使用 `_wfopen` 与 `MoveFileExW`；MSVC 全项目使用 `/utf-8`。GCC 与 MSVC 在 `E:\AI\审计\UCN_QOS_非ASCII_*` 目录均 `ucn_tests 1/1` 通过，MSVC 复跑 `C4819=0`。
- QOS-A06：同步任务表、Q2/Q3 闭环报告、官方架构/数据路径/QoS/Service/资源/API/诊断文档、状态机总览、用户手册和源码阅读指南；历史阶段报告保留当时事实，不把旧数据改写成当前结果。
- 软件矩阵：Windows GCC Full/Lite/Release/Service-OFF 均 `53/53`，Nano `43/43`；WSL ASan/UBSan 与 `-fanalyzer -Wall -Wextra -Werror` 均 `53/53`；MSVC/GCC 非 ASCII 定向测试均 `1/1`。所有矩阵启用的 Service 255/256 配置合同均符合预期。
- 最终自审：Windows GCC Full/Lite/Nano、Release、Service-OFF，WSL ASan/UBSan 与 `-fanalyzer -Wall -Wextra -Werror` 全部通过；GCC/MSVC 中文构建目录的 `ucn_tests` 均 1/1 通过；生成式函数索引、双语注释覆盖、文档链接与 `git diff --check` 均通过。
- 状态：QOS-A01～A06 整改自审通过，等待外部复审；在外部签字前，Q2/Q3 仍保持 `AUDIT HOLD`，不得称为最终软件接口冻结。未连接、烧录或操作 ESP32；QOS4-08 仍为后续硬件测量。

### OP-425｜完整实现 Q2/Q3 四级业务队列并完成软件验证

- 任务：按 QOS4-01～08 将已有 Wire Q0～Q3 枚举补成 Node、Service、Transfer 和诊断一致的软件能力；本轮无 ESP32 接入，只允许形成 Host 软件结论。
- Node：Nano/Lite/Full 均增加独立 Q2 Normal FIFO 与 Q3 Bulk FIFO；立即发送、Endpoint、Path 和排队 API 接受全部四级。Q1 独占 Latest 语义，Q0 独占带非零 Deadline 的本地背压/route-wait 重试，Q2/Q3 保持 Best Effort FIFO。
- 调度：Node 与 Service Remote TX 冻结相同的 12 槽 `Q0,Q1,Q0,Q2,Q0,Q1,Q0,Q3,Q0,Q1,Q0,Q2`，满载权重为 `6:3:2:1`；空队列可跳过，retained Q0 继续保留 FIFO 所有权。
- Service：增加 Q2/Q3 Binding 模式、独立本机 Inbox、独立 Remote TX 队列、purge、满载和按 Class 读取统计；三节点 Bridge 可透明交付 Q2/Q3。
- Transfer：T32/T64 Direct 固定使用 Q2，T128～T8K Fragment 使用 Q3，ACK 保持 Q1；接收端拒绝错 Class。Transfer Step 仍走立即发送，产品 Owner 必须另行提供有界推进预算，Q3 标记不冒充本机自动公平。
- 配置/ABI：新增 Node 与 Service 的 Q2/Q3 深度、Binding 上限及编译期合法性检查；Node Storage Layout 从 6 升为 7。新增 `tx_enqueued_by_class[]`、`tx_scheduled_by_class[]`、`tx_sent_by_class[]` 及 Service Q2/Q3 背压/读取统计。
- 测试：Node 与 Service 分别持续 12,000 次四队列满载，精确得到 Q0/Q1/Q2/Q3 `6000/3000/2000/1000`；另覆盖 FIFO 顺序、队列满、purge、三节点桥接、Path、Transfer 映射、错 Class、配置和 Profile 行为。
- 软件矩阵：Windows GCC Full Debug `50/50`、Lite `50/50`、Nano `40/40`、Full Release `50/50`、Service-OFF `50/50`；WSL GCC ASan/UBSan `50/50`、`-fanalyzer -Wall -Wextra -Werror` `50/50`。Host x64 Debug Node 为 Nano/Lite/Full `3480/6872/10920 B`，只作趋势证据。
- 文档：新增 Q2/Q3 软件闭环报告，同步官方能力、调度、配置、资源、诊断、用户手册、总体架构与任务表；历史版本报告保留当时事实，不反向改写。
- 边界：未烧录、监听或操作任何开发板；UART/CAN/CAN-FD/USB/Wi-Fi 的硬件优先级、真实延迟/吞吐、CPU/栈/RAM、功耗、多跳和断链压力继续归 QOS4-08，开发板接入后另建实机报告。

### OP-424｜形成 UCN FPGA 硬件转发节点详细实施方案

- 背景：评估使用 FPGA 构建 UCN 硬件转发中继，以降低多端口中间节点的软件调度、复制、编解码和查表抖动；方案不能改变 MCU 节点可独立自组网的核心边界。
- 架构：冻结为“MCU/CPU 控制平面 + FPGA 数据平面”。Neighbor、Route/Path 决策、Cost/Policy、Cluster、安全与诊断仍由软件负责；FPGA 只加速已安装 Route/Path 的普通业务帧。
- 数据面：详细定义 Store-and-Forward、Carrier 解码、完整 CRC/Admission、Route/Path 查表、Hop Limit 修改、CRC 重算、Q0～Q3 仲裁和出口 Carrier；控制/本机/未命中/不支持帧进入慢路径。
- 协同：提出 Route/Path 双 Bank 或 Shadow Commit、generation fence、Command/Event Ring、寄存器、Buffer/Descriptor Owner、CPU/FPGA 双重转发防护和 OFF/MIRROR/SELECTIVE/FULL 灰度模式。
- 实施：拆分 F0 合同冻结、F1 C 参考模型、F2 UART 原型、F3 动态表、F4 多端口 QoS、F5 CAN/USB、F6 IP 骨干、F7 安全加速和 F8 产品化；每阶段定义交付物、测试和完成判据。
- 文档：新增 `docs/10-理论与规划/建议方案/UCN_FPGA硬件转发节点实施方案.md` 并同步建议索引和任务表 DOC-07。
- 边界：本项只形成未来实施规范；当前项目未增加 FPGA RTL、控制 BSP、硬件转发表或实机性能证据，不得写入官方“已实现能力”。

## 2026-08-31

### OP-423｜六节点 H6-12 反馈闭环：Q0 route-wait 与 Transfer ACK 恢复 Fence

- 来源：六块 ESP32-S3、A-B-C-D-E-F 五段 UART-only 混合负载。旧实现先暴露 Q0 已入队后
  在路由刷新空窗遇 `UCN_ERR_NOT_FOUND` 被后台终止；修复后又暴露重复累计 ACK 可连续触发
  Go-Back-N 恢复轮、过快消耗 Transfer 重试预算。
- Core：Full/Lite 动态 Mesh 的 Q0 `RETRY_ON_BACKPRESSURE` 在可恢复 route gap 中保留原
  FIFO 项、Deadline、Order 和 Payload，合并同目的地发现，并按 50 ms 静态间隔重试；恢复、
  过期和永久失败使用独立统计。Node Storage Layout 从 5 升至 6。Nano 不含动态发现，静态路由
  行为不变。该能力仍是本地所有权，不是远端 ACK 或端到端可靠重传。
- Transfer：TX slot 增加 `recovery_waiting_ack`。一次恢复 burst 完整提交后，重复旧 ACK 不会
  立即开启下一轮；只有累计 ACK 前进或 ACK timeout 才能解除 Fence。Recent Completion 满表
  改为淘汰最早到期记录，避免固定槽位偏置。新增重复 ACK 有界恢复和最早到期淘汰回归。
- 软件验证：Windows Full Debug/Lite/Nano/Full Release/Service-OFF=
  `50/50、50/50、40/40、50/50、50/50`；WSL Full ASan/UBSan `50/50`；WSL GCC
  `-fanalyzer -Wall -Wextra -Werror` `50/50`。Transfer 对象 8,840 B，固定增加 8 B。
- 实机验证：最终相同 R03 固件 Round 10/11 均为九档 9/9、`delivered=127/127`、
  `tx_failed=0`、365 Fragment/14 次成功恢复重发；Q0 route-wait terminal/expiry=0，十 UART
  物理/Carrier/Queue 错误=0。证据与固件 Hash 记录在 ESP32 工程 H6-12 报告。
- 文档：同步官方 Q0/诊断/调用树、当前 Storage ABI 和容量参数。历史 V5 阶段报告继续保留
  当时的 Layout/行为，不反向改写历史证据。
- 边界：普通 Q0/Q1 仍无端到端 ACK；需要可靠送达使用 Transfer，需要远端执行确认使用
  Result Endpoint。本项不证明 Cluster、真实 Flash 掉电、长稳或其他 MCU/Bearer 已完成。

## 2026-08-28

### OP-422｜将 v5 开发分支改名为 `v5-development`

- 原因：`codex/v5-adaptive-wire` 带有工具名称，不适合作为项目长期公开分支名；统一改为只表达版本和用途的 `v5-development`。
- 操作：从旧分支的同一提交 `5f98f4c6c979dc066abb47c185911e02dd35fe64` 创建并推送 `v5-development`，核对远程哈希一致后，删除远程与本地 `codex/v5-adaptive-wire`。
- 结果：远程仅保留 `main`、`v4`、`v5-development`；默认 `HEAD` 仍指向 v5 `main`，分支改名没有改变提交历史、标签、源码或工作区修改。

### OP-421｜整理 GitHub v4/v5 主分支

- 基线：核对远程默认 `main` 与标签 `v4.0.0-final-before-v5`，二者均指向提交 `25bcd223968a3a92f9ba72bc4fe0fd041b74e4f9`；当前已提交 v5 指向 `5f98f4c6c979dc066abb47c185911e02dd35fe64`，且 v4 是 v5 的直接历史祖先。
- 分支：从原 v4 `main` 创建并推送独立 `v4` 分支；随后把 `main` 以普通 fast-forward 从 v4 推进到当前 v5，共前进 157 个提交，没有 force-push、rebase 或历史重写。
- 默认入口：GitHub 远程 `HEAD` 继续指向 `refs/heads/main`，而 `main` 已指向 v5，因此打开仓库默认显示 v5；本地工作分支也切换为 `main`，跟踪 `origin/main`。
- 保留：操作完成时曾暂时保留原 `codex/v5-adaptive-wire` 作为安全别名；随后已按 OP-422 改名为 `v5-development`。标签 `v4.0.0-final-before-v5` 保持不变。
- 边界：本次只调整分支引用并推送已存在提交；未提交的 Core 双语注释继续保留在工作区，没有被纳入或丢失。

## 2026-08-27

### OP-420｜补齐非 Cluster 核心函数的中英文注释

- 范围：按 Nano、Lite、Full 与 Service 开关的真实编译条件枚举函数定义，为 `src/core`、`src/node`、`src/routing`、`src/transport`、`src/adapters`、`src/ports`、`src/service`、`src/extended/ucn_transfer.c` 以及非 Cluster 公共头中的 `static inline` 函数补齐双语用途注释；共覆盖 719 个实际函数定义。
- 形式：每个函数定义前使用独立的 `EN:` 与 `中文：` 行说明职责，重点区分编码/解码、校验、状态推进、队列、路由、Path/Policy、驱动适配、服务分发和 Transfer 分片重组，避免只重复函数名。
- 工具：新增 `tools/manage_core_bilingual_comments.py`。默认模式只检查覆盖率，`--apply` 只补缺失注释，`--refresh` 可按当前规则刷新生成式注释；工具使用 GCC `-aux-info` 而不是简单正则猜测函数定义。
- 导航：重新生成 `docs/源码阅读指南/08-函数签名与源码位置索引.md`，使 719 个函数加入注释后的源码行号保持准确；阅读指南补充注释范围与检查命令。
- 验证：覆盖检查为 `definitions=719 missing=0`；函数索引 `--check` 通过；Debug Full 57/57、Lite 50/50、Nano 40/40；`git diff --check` 无空白错误，仅既有 LF→CRLF 提示。
- 边界：本轮只增加注释、检查工具、索引和台账，不改函数签名、结构体布局、控制流、Wire、Feature 默认值或构建配置；所有 Cluster 源码与公共 Cluster 头保持未修改，Cluster 函数注释留待用户确认本轮效果后单独实施。

## 2026-08-26

### OP-419｜建立面向产品调用的 UCN 用户手册

- 目标：把“怎样使用 UCN”与“怎样阅读 UCN 内核”彻底分开；普通用户无需先理解 Frame codec、路由状态机或内部 helper，即可按固定路线完成选型、接入、收发、组网和产品验收。
- 文档：新增 `docs/用户手册/`，包含总入口与 16 篇编号手册。快速路线覆盖 Profile、Node/Link/Owner、Endpoint 和 UART/CAN/Wi-Fi/USB；进阶路线覆盖自动入网、指定路径、负载均衡、Service、T32～T8K Transfer、安全 Provider 和裸机/五类 RTOS；产品路线覆盖诊断、资源性能、六个组合场景、API/错误码速查与 Cluster 当前边界。
- 边界：示例只说明公共 API、产品调用顺序和验收方法；驱动、引脚、密钥、AEAD、RNG、持久化及业务 ACK 仍由产品实现。Cluster 明确保持发布阻断，不把 Host 软件证据、实验 Archive 或 Wire v4 codec 扩大为默认产品可用声明。
- 导航：`docs/README.md` 增加用户手册入口；任务表新增 DOC-06；源码审计继续使用独立的 `docs/源码阅读指南/`。
- 验证：用户手册本地 Markdown 链接、UCN 公共 API 符号、占位词和空白格式扫描通过；复用当前构建执行 Full 57/57、Lite 50/50、Nano 40/40；本任务只新增/同步文档，不修改 Core、Cluster、构建默认值或 Wire 行为。

### OP-418｜建立 Nano/Lite/Full Core 源码阅读与人工审计指南

- 目标：解决直接阅读 `ucn_node.c` 容易失去架构和调用顺序的问题，建立“公共合同→Nano 骨架→Lite 动态 Mesh→Full Path/Policy→TX/RX/Step→审计”的固定路线。
- 文档：新增 `docs/源码阅读指南/`，包含总入口、阅读方法、公共基础层、Nano/Lite/Full 三档详解、构建差异、完整调用链、审计清单和函数签名索引；各 Profile 均解释实际编译源文件、原生 API、Stub、参数、调用顺序和对应测试。
- 函数清单：新增 `tools/generate_core_reading_function_index.py`，调用 GCC `-aux-info` 按 Nano/Lite/Full 与 Service=ON 的真实条件编译结果提取全部公共/静态函数定义、参数和源码行号；三档函数全集由“公共层+对应差异层”组成。
- 导航：`docs/README.md` 增加 Core 源码阅读指南入口；任务表新增 DOC-05。
- 验证：函数索引生成器 `--check` 通过；10 篇阅读文档本地链接坏链为 0；复用当前构建执行 Full 57/57、Lite 50/50、Nano 40/40；`git diff --check` 无空白错误，仅既有 LF→CRLF 提示。本任务只更新阅读资料和生成工具，不改变 Core/Cluster 行为、Feature 默认值或发布状态。

### OP-417｜165 篇官方文档完成全量深度与一致性验收

- 深度：`docs/official` 共 165 篇、14111 行，中位数 80 行，最短 45 行；每篇均具有状态元数据，未发现 `TODO/TBD/待完善/后续补充` 等占位内容。两份导航 README 进一步补充能力成熟度判定、事实冲突裁决、读者入口、问题到模块映射和使用边界。
- 一致性：逐项复核 Core Wire v5、Cluster v3/32 B 与 v4/40 B、v4 encoder 默认关闭、Persistence Record v4/388 B、`ucn_cluster_t=1608 B` 和 Port Ops API v2；修正 Port 迁移示例中 `UCN_PORT_API_VERSION` 为源码实际的 `UCN_PORT_OPS_API_VERSION`。
- 链接与合同：165 篇本地 Markdown 链接扫描为 0 坏链；Cluster 文档合同通过（22 Phase、33 Wire Type、12 M14 Task），Cluster 资源合同通过（32 文件、动态分配 0）。
- 回归：复用 `build_m14_full` 执行完整 CTest 57/57 通过，覆盖源码/存储/Wire/资源/文档门禁、Core、Cluster 模型、Release codec、64/256/1000 节点模拟和 W0～W3/Mixed Scale；`git diff --check` 无空白错误，仅既有 LF→CRLF 提示。
- 结论：DOC-04 与 DOC-04F 标记完成。此次只重写和整理文档、索引与文档合同，不修改协议实现，不解除 M05 `AUDIT HOLD`、M14 `RELEASE NO-GO`，也不把 Host 软件证据扩大为真实 Flash、安全或硬件发布结论。

### OP-416｜官方文档 11～13 完成第二轮深度重写

- 诊断：完成9篇扩写，建立Driver/Source/Adapter/Neighbor/Route/Transfer/Service/Cluster分层日志与排障链，补齐入离网、Cost抖动、队列背压、吞吐损耗、Authority/Persistence和升级介质处理流程。
- 测试：完成13篇扩写，明确Safety/Liveness和证据等级、跨Profile/Feature/产品配置、Release/Sanitizer/Analyzer/Fuzz、Wire全字段negative、规模流量模型、硬件实测、性能资源长稳、安全真实掉电和Evidence包门禁。
- 兼容：完成10篇扩写，分离Wire/API/ABI/Storage四类版本，展开Core W0～W3、Cluster v3/v4隔离、Port API v2、Record v4/388 B迁移、灰度/回滚、签字角色、CHANGELOG和支持矩阵。
- 边界：明确开发中5.0.0、M05 `AUDIT HOLD`、M14 `RELEASE NO-GO`、真实Flash/安全/多平台未完成项；不因文档完善而升级成熟度。
- 验证：本批32篇正文约46～76行，本地链接0坏链，`git diff --check`通过；下一步进行165篇official全量一致性验收。

### OP-415｜官方文档 09～10 完成第二轮深度重写

- API：完成 16 篇参考扩写，覆盖基础类型/时间/错误、Wire Class 编解码、Node 生命周期、Link/Neighbor/Endpoint、Route/Path/Policy/Cost、Adapter/Port/Source、Service、Transfer、Security、Cluster/Federation 及统一所有权/失败写回合同。
- 示例：增加最小配置、Frame encode/decode、Node send/receive/step、Link ops、Endpoint handler、Path/Policy、Event Runtime、Service命令/结果、Transfer/release和安全Provider安装的伪代码或调用链。
- 集成：完成19篇指南扩写，给出Host/裸机/通用RTOS及FreeRTOS、Zephyr、NuttX、RT-Thread的Owner/ISR映射，以及UART/RS-485/USB、CAN/CAN-FD和Wi-Fi/ESP-NOW/BLE/LoRa的Driver→Source→Adapter接线与测试矩阵。
- 场景：补齐多Bearer多实例、任务间/跨MCU Service、T32～T8K Transfer、自动/固定/均衡路由、Cluster受限接入、安全Provider、自定义三层和旧项目分阶段迁移/回滚流程。
- 验证：本批35篇正文约45～104行，本地链接0坏链，`git diff --check`通过；硬件/厂商SDK和Cluster发布边界未被扩大。

### OP-414｜官方文档 07～08 完成第二轮深度重写

- 范围：完成 Cluster 与配置/资源共 28 篇深度扩写，正文由原 21～43 行概要提升为约 46～111 行的独立专题。
- Cluster：补齐 Core/Cluster 关系、Role/Phase/Epoch/Authority、不变量执行时机、Current v3 与实验 v4 分层、Membership/Persistence/Joint/Lease/Backup/Takeover/Handover/Recovery/Rekey/Federation 的状态、失败和重启路径。
- 配置：解释命令行/产品头/fallback 优先级、ABI 传播风险、多 Profile/Feature 构建、固定容量计算、时间回绕代数、Owner 实时预算和 MCU 资源测量流程。
- Cost：同步当前 Standard Adapter 典型 preset 与 LC-1 动态 penalty、过期排除、20% 滞回及目标板调优方法。
- 发布：补齐候选基线、验收矩阵、Wire/API/ABI/Storage 四类兼容判断和 Authority Fence 后的回滚流程；保持 M05 `AUDIT HOLD` 与 M14 `RELEASE NO-GO`。
- 验证：本批 28 篇本地链接 0 坏链，`git diff --check` 通过；没有改变代码、默认 Feature 或产品发布状态。

### OP-413｜官方文档 03～06 完成第二轮深度重写

- 范围：完成路由与链路、Transfer 与 Service、Adapter 与平台、安全共 43 篇深度扩写，正文由原 17～49 行概要提升为约 49～145 行的独立可读专题。
- 路由：展开 Link/Bearer/Neighbor/Route/Path 对象链、AODV 每跳处理和 RERR 依赖方向、缓存/失效、Path 安装/Trace、LC-1 当前分段公式、四种 Policy、Q1 Flow 与入离网/规模模型。
- 传输：补齐 T32～T8K Class 选择、14 B Fragment、CRC16+CRC32、累计 ACK/Go-Back-N、MTU 突降、多跳中继、Service Router/Bridge、Command Guard/Result 和分层性能定位。
- 平台：补齐 Link/Adapter 所有权、Standard Resolver、Event Runtime/Owner，UART/COBS、CAN-FD padding/Classic Carrier，以及裸机、FreeRTOS、Zephyr、NuttX、RT-Thread、Linux/ROS2、多无线和多实例的真实 glue 边界。
- 安全：建立资产/攻击者/信任边界，展开 Policy/Provider TX/RX 调用顺序、30 B AAD 精确布局、透明密文中继、Session/Sequence 持久化、Endpoint/诊断 ACL 和生产安全签字流程。
- 验证：本批 43 篇本地链接 0 坏链，`git diff --check` 通过；没有修改协议源码、Feature 或产品安全状态。

### OP-412｜官方文档 00～02 完成第二轮深度重写

- 范围：深度核对并扩写 `official/00-项目总览`、`01-总体架构`、`02-核心协议` 共 26 篇；专题正文普遍扩展到约 80～167 行，不以行数替代质量验收。
- 内容：补全 MCU-first 的设计缘由、模块依赖与 Owner/ISR 边界、完整 Frame 发送接收链、Node/Endpoint 业务寻址、Q0/Q1 调度、分层背压、Session/Sequence 安全边界、TTL/回绕时间和异构 Profile/MTU 协商。
- 示例：增加本机任务 Fast Path、远端命令/Result Endpoint、UART→CAN 异构中继、Q1 Latest、重启会话、防重放和 3 跳时延预算等可执行场景。
- 修复：同步修正总览功能索引及核心协议后续章节的 15 条过时路径；本批 26 篇本地链接扫描为 0 坏链。
- 验证：Cluster 文档合同 `phases=22 / wire_types=33 / tasks=12` 通过；本批 `git diff --check` 通过。
- 边界：本批仅更新当前源码说明，不改变 Core、Cluster Feature、默认编码器或发布状态；M05 `AUDIT HOLD` 与 M14 `RELEASE NO-GO` 保持。

### OP-411｜启动官方文档第二轮深度重写

- 反馈：用户复核发现首版 165 篇 official 文档多数只是概要，缺少来龙去脉、完整流程、API 调用、异常处理和可实施细节。
- 审计：165 篇中 134 篇少于 40 行；API、集成、诊断、测试和发布章节大量只有 13～20 行，确认不满足独立阅读要求。
- 标准：在官方写作规范中新增十项正文深度门禁，要求背景、边界、对象、流程、并发、配置/API、异常、示例、资源和证据全部闭环。
- 任务：新增 DOC-04 与 DOC-04A～F；按 `00～02 → 03～06 → 07～08 → 09～10 → 11～13` 分批源码核对、扩写和验证。
- 边界：Obsidian UCN 笔记用于继承 MCU-first 和“先解释问题再展开源码”的写法，但其快照较旧且保持只读；当前源码仍是唯一实现事实源。

## 2026-08-25

### OP-410｜官方正文 00～13 完成并建立参考/证据/实验/归档入口

- 正文：`docs/official` 已完成 00～13 共 165 篇，全部具有文档级别、实现状态、版本、事实源、核对 commit 和硬件边界状态块。
- 导航：新增 `reference/` 的公共 API/配置/Wire/CMake/模块映射与 5 张 Mermaid 图；新增 `evidence/`、`experimental/`、`archive/` 分层入口。
- 首页：重写仓库根 `README.md`，删除会随里程碑漂移的长篇阶段数值和旧文入口，只保留 MCU-first 定位、当前能力/阻断、构建命令和 official 导航。
- 旧文：`01-入门与使用`～`11-历史与复盘` 的 README 已标记迁移/证据/规划/归档属性；当前事实入口切换到 `official/`，旧文件暂保留兼容路径，待逐篇建立替代关系后再移动。
- 一致性：全仓库 465 篇 Markdown、526 条本地链接，坏链 0；official 状态块 165/165；Cluster 文档合同 `22/33/12` 通过；`git diff --check` 无空白错误。
- 软件门禁：重新 configure/build `build_m14_full`，CTest 57/57 通过；本次只改文档与文档路径，不改变协议代码、Feature 或发布状态。
- 边界：M05 `AUDIT HOLD`、M14 `RELEASE NO-GO`、真实 Flash/掉电、多 Bearer、安全和 MCU 资源门禁保持不变。

### OP-409｜官方文档 00～08 完成首轮源码化重写

- 产出：完成 `official/00-项目总览` 至 `08-配置与资源`，共 98 篇 Markdown；覆盖架构、Core Wire v5、路由、Transfer/Service、Adapter/Port、安全、Cluster Current/Target 边界和产品配置资源。
- Cluster：明确分离默认 v3/32 B、Wire v4/40 B Codec、M07～M09 受限组件和默认关闭的 M10/M11/M13 Archive；保留 M05 `AUDIT HOLD` 与 M14 `RELEASE NO-GO`。
- 资源：记录当前 M14 Host 证据 `cluster object=1608 B / archive text=133559 B / max static frame=1840 B / dynamic allocation=0`，明确不替代 MCU 实测。
- 核对：官方文档内部链接 0 断链；Cluster 文档合同 `phases=22 / wire_types=33 / tasks=12`；`git diff --check` 无空白错误，仅既有 CRLF 提示。
- 下一步：继续 DOC-03E，完成 API、集成、诊断、测试和发布文档；随后建立 reference/evidence/experimental/archive 索引及旧文替代关系。

### OP-408｜官方文档目录获确认并进入正文重写

- 确认：用户同意 `official/reference/evidence/experimental/archive` 分层、历史归档、实验隔离、`calltree/results` 稳定路径和逐目录重写方式。
- 治理：新增《官方文档写作与核对规范》，冻结事实优先级、页首状态块、用词、去重和逐批验收规则。
- 基线：当前源码基线为 `codex/v5-adaptive-wire@a093862`；项目版本 `5.0.0`、Core Wire v5、Port API v2、Cluster Current Wire v3/API v2，Wire v4 encoder 默认关闭。
- 任务：DOC-02 标记已确认，DOC-03 开始执行并拆分 DOC-03A～DOC-03F；本轮先写项目总览、总体架构和核心协议。
- 边界：Obsidian UCN 知识库只用于继承 MCU-first 与术语习惯，其源码快照较旧且保持只读；功能事实不从旧知识库反向覆盖当前代码。

### OP-407｜UCN 官方文档体系目录草案

- 需求：现有文档包含大量建议、阶段方案和审计材料，无法直接代表当前实现；本轮先设计完整目录，不提前重写正文。
- 核对：依据当前 `include/ucn/` 公共 API、`src/` 模块、CMake 静态库/Feature、测试与 Cluster 默认/实验隔离边界盘点项目能力。
- 产出：新增《UCN 官方文档体系目录草案》，规划 `official/reference/evidence/experimental/archive` 五类内容，覆盖 Core、路由、Transfer、Service、Adapter/Port、安全、Cluster、配置资源、API、集成、诊断、测试和发布。
- 边界：Wi-Fi/ESP-NOW/BLE/LoRa 仅规划产品 Adapter 对接规范，不冒充内置驱动；Cluster Wire v4、M10/M11/M13 等默认关闭或未生产接线能力进入实验口径；旧建议与审计保留归档，不直接作为现状。
- 状态：仅目录草案完成，等待用户确认；官方正文、旧文迁移和替代关系尚未开始。

### OP-406｜docs 文档分类整理与导航重建

- 盘点：`docs` 共 493 个文件，其中 226 个直接堆放在根目录；原有子目录仅有架构、快速手册、理论资料、调用树和结果目录。
- 整理：建立 `00-项目管理` 至 `11-历史与复盘` 的主题目录；Cluster 进一步按规范、早期设计、M03～M14 里程碑和复审分类；`calltree`、`results` 保持独立。
- 导航：新增 `docs/README.md` 和各一级目录 README；移动使用 `git mv`，不删除任何文档；同步修正 Markdown 相对链接、源码/CMake 中的 `docs/...` 路径。
- 状态同步：M14 候选已经提交为 `a093862`，仍未推送、未创建发布 tag；本次目录整理不改变 M05 `AUDIT HOLD` 或 M14 `RELEASE NO-GO`。
- 验证：原 493 个文件全部保留，新增导航后共 511 个文件；根目录只保留 `README.md`。Markdown 本地链接扫描 415 条、坏链 0；Cluster 文档契约 `phases=22 / wire_types=33 / tasks=12`；Windows GCC Full CTest 57/57；`git diff --check` 无空白错误，仅既有 CRLF 提示。

### OP-405｜M14 全体软件自审完成：矩阵全绿，发布仍 NO-GO

- 全体矩阵：Windows GCC Full 57/57、Lite 50/50、Nano 40/40、Service-OFF 50/50；WSL ASan/UBSan 54/54；GCC `-fanalyzer -Werror` 36/36；MSVC Release 50/50。
- 自审修正：Analyzer 首轮抓到 M10 property fixture 的 voter 数组未整体初始化且缺 quorum 上界断言；修正测试证明代码后 Analyzer/ASan 重跑通过，未改协议业务语义。
- 资源：资源门禁再次 PASS，object 1608 B、text 133559 B、最大静态栈帧 1840 B、动态分配 0。
- 结论：所有当前可实施软件项与逐项/全体自审已完成；14-03/07 仍 partial，14-08 blocked，14-11 release NO-GO，14-12 no tag。该候选随后提交为 `a093862`，尚未推送。

### OP-404｜CLV2-14-11/12：最终清单完成，发布保持 NO-GO

- Safety：逐项复核 Target 10 条 Safety。各组件均有可执行证据，但 production v4 端到端接线、M10 外审和真实 Flash/掉电证据不足，统一按 PARTIAL/BLOCKED 记录，没有以测试总数替代安全证明。
- Liveness：Current clean 规模收敛、Recovery 软件路径有受限证据；Takeover、Merge 和 Target churn 因 v4 production 未接入而 BLOCKED。
- 发布：新增兼容矩阵与回滚草案；14-03/07 partial、14-08 blocked、14-11 NO-GO，故 14-12 明确为 `BLOCKED / NO TAG / NO RELEASE`。
- 下一步：完成当前工作树的跨 Profile、Sanitizer、Analyzer、Release 和文档/资源全体自审，再将完整 M14 工件交外部审计；不会创建发布 tag。

### OP-402｜CLV2-14-10 完成：权威文档重写与代码—文档自动契约

- 核对：发现旧 Current FSM 仍把 Role+bool 当状态源，Cluster calltree 仍写 28 B/format v1，与 M14 当前源码冲突。
- 文档：重写 Current FSM 和 Cluster calltree；同步 Target/Wire 边界；新增 Persistence v4、Config v2、Timer Algebra、API/storage v2 迁移及冻结契约。
- 门禁：新增 `tools/check_cluster_docs_contract.py` 与 CTest，解析源码核对 22 Phase、33 Wire Type、8 个版本/尺寸和 12 个 M14 task。
- 验证：docs contract 独立执行 PASS；Phase/storage/Wire/resource/docs 五项定向 gate 5/5 PASS；差异无空白错误。
- 边界：文档继续明确 M05 `AUDIT HOLD`、14-03/07 partial、14-08 hardware blocked，未把隔离组件描述为生产接线。

### OP-401｜CLV2-14-08 实机门禁确认阻断

- 核对：14-08 要求真实四板、真实 Flash Provider、可控断电、Wire v4 与多 Bearer 原始日志；当前工作环境不具备这些可验证条件。
- 处理：新增实机门禁阻断记录，冻结必测矩阵和解除条件；不使用 Host fake、规模模拟或 ASan 结果冒充硬件证据。
- 结论：14-08 为 **BLOCKED / REAL HARDWARE REQUIRED**，并继续阻断 14-11 生产级签字与 14-12 release tag。

### OP-400｜CLV2-14-09 完成：资源、静态栈与 Feature-OFF 门禁

- 源码/工具：增加 2048 B Cluster storage 编译期预算、Cluster 动态分配扫描、干净 Release Profile/模块资源脚本及 CTest 门禁。
- 优化：门禁首次发现最大静态栈帧 2688 B；限制持久化状态比较与 Config 序列化 helper 的 Release 过度内联后降至 1840 B，Cluster text 138023→133559 B，协议行为不变。
- 结果：Host object 1608 B，text 133559 B，rodata 992 B，bss 8 B；Full/Lite/Nano 显式链接 Cluster 时成本相同，已记录为当前裁剪限制。Core-only 构建不生成 Cluster archive。
- 验证：Full 56/56、Lite 49/49、Nano 39/39、Service-OFF 49/49；动态分配扫描为 0；资源 CSV 写入 `docs/results/M14/`。
- 边界：Host archive/`.su` 不等于 MCU linker map、任务栈水位或真实控制流量，后者继续由 14-08 阻断。

### OP-399｜CLV2-14-07 阶段完成：64/256/1000 Current-FSM 规模矩阵与 Target 阻断确认

- 模拟：生产 archive 在 64/256/1000 Node 下执行 clean、impaired、mobility、score-shift、head-failover 共 15 个门禁；完整结果写入 `docs/results/M14/cluster_scale_matrix_2026-08-25.csv`。
- 结果：clean 均 8920 ms；mobility 最终 23000 ms；score-shift 恢复 4920 ms；impaired 均通过本地 Cluster 隔离且峰值控制发送为 13/14 条每 Node 每秒。
- 安全负例：Primary 故障后生产 v3 无权建立 Backup/Takeover，三档规模均为 0 Head；新增 `--expect-v3-authority-fence` 把该 fail-closed 结果固定为负向合同，不伪装成恢复成功。
- 自审边界：M07/M09/M10/M12/M13 只有受限组件证据，production v4 Owner/Wire/FSM 仍受 M05 `AUDIT HOLD` 冻结；故 Config/Backup/Recovery/Rekey churn 不可做成真实端到端规模结论。
- 结论：14-07 为 **PARTIAL / CURRENT-FSM MATRIX PASS / BLOCKED BY M05**。它阻断最终安全签字与发布，但不阻止继续 14-09 资源核算和 14-10 文档同步。

### OP-398｜CLV2-14-06 完成：v3/v4 Codec 与 Certificate Pending 长序列 Fuzz

- 测试：保留 v3 的 19 Type/20000 mutation + live RX，v4 的独立字段矩阵/4096 raw mutation；新增固定 seed `0x14F00D06` 的 65536 步 pending 状态序列。
- Stateful oracle：逐步组合 begin、duplicate、source/admission/key/config mismatch、fragment、deadline、expiry 与 Active Epoch change；所有非破坏性拒绝逐字节验证 slot 不写回，每步校验 count/mask/Config/deadline canonical 一致性。
- 自审：没有为 fuzz 修改 codec 实现；成功 raw 必须继续通过 semantic parser，失败 output 不写。pending 仍是 codec-only 对象，不建立 Authority、不进入生产 FSM。
- 验证：Windows Full 48/48；v4 Debug/O1/O2/O3 4/4；WSL ASan/UBSan Full 44/44，Leak gate 开启；差异检查无空白错误。
- 结论：14-06 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT M14 EXTERNAL REVIEW**；固定 seed/有限序列不等于形式化证明，M05 顶层 `AUDIT HOLD` 不解除。进入 14-07。

## 2026-08-24

### OP-397｜CLV2-14-05 完成：M08/M10 组合 Target Property Model

- 测试：新增 default-OFF `ucn_cluster_target_property_tests`，直接调用真实 Authority、Config quorum、Takeover、Certificate/Fence 和 14-04 invariant API。
- 状态空间：穷举 27 个 voter-exclusive 双 Head 分区、Stable/Joint 的 16 个 vote subset；固定 seed `0x14A05EED` 跑 4096×32=131072 个 M10 乱序/重复/replay/deadline/durable event。
- Oracle：Joint old/new 使用独立 popcount majority 对照实现；每个 Authority tick 检查 Network Single Authority；Epoch durable 后 Certificate 必须可验证并先 Fence 旧 Primary。
- 自审修正：初版测试把 time-driven Abort 的非 OK 误判为“必须 no-write”；按协议改为精确要求 `ABORTED + recovery_required + inactive`，普通拒绝仍逐字节 no-write。协议源码没有为测试放宽。
- 结论：14-05 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT M14 EXTERNAL REVIEW**；bounded model 不等于无限状态证明，不改变 M05/M10 外审状态。进入 14-06。

### OP-396｜CLV2-14-04 完成：Target Safety-1..10 可执行不变量引擎

- 源码：新增只读 `ucn_cluster_invariant_check()` 与 bounded network checker，返回 Safety-1..10 violation bit；不修复状态、无堆、失败不写 output。
- Debug 接线：非 test-hook Debug 构建在每次 Cluster Step/RX 完成后 fail-fast；Network checker 单独验证同一 Stable Cluster 不得出现两个 writable Authority。Release Hook 被编译移除。
- 覆盖：local checker 核对 Authority/quorum、legacy takeover write、Recovery isolation、Vote tuple、Config voter set、Replay identity、Fence ordering、no-wrap serial 与 persistence pending/fault/retry descriptor。
- 自审：十类故障逐项命中；真实 M08 Stable quorum 获权及 lease-loss Grace 均零违例；64/256/1000 clean/impaired 等 Debug 仿真无断言。Full 实验构建 44/44。
- 边界：本项是可观测状态诊断，不是协议形式化证明。M10/M11/M13 跨事务组合、随机序列与 replay/persistence ordering 继续在 14-05；M05 顶层仍 `AUDIT HOLD`。

### OP-395｜CLV2-14-03 模块范围完成：Strict-v4、v3 compat 与 dual-stack 物理解耦

- 模块：新增 `UCN_CLUSTER_ENABLE_V3_COMPAT`；v3 compatibility、strict-v4 codec 与 dual-format dispatcher 分别构建为三个 archive。推荐 Wire format 为 v4，但不改变 production 默认行为。
- 自审整改：发现最初 dispatcher 与 v4 codec 共用对象，strict-v4 archive 仍暗含 v3 decoder 符号；已把 dispatcher 移入独立源/目标。v3-OFF 的 v4 archive 未解析符号表为空，且不生成 v3 archive。
- 门禁：新增 Wire module source gate，禁止 codec 回流 Current Cluster archive、禁止 strict-v4 codec 引用 legacy decode，并固定 dual dispatcher 的独立边界。
- 验证：GCC Full（实验模块+O1/O2/O3）47/47、Lite 41/41、Nano 31/31、Service-OFF 41/41；v3 OFF/ON Release archive 构建和符号检查通过。
- 结论：14-03 的**模块拆分范围**为 SELF-AUDIT PASS；由于 M05 仍 `AUDIT HOLD`，production v4 RX/TX/FSM/Authority/default encoder 未接入，任务保持 `PARTIAL / BLOCKED BY M05`。继续不依赖生产 Wire 的 14-04。

### OP-394｜CLV2-14-02 完成：Cluster Public handle/storage 分离与 API v2

- 边界：`ucn_cluster.h` 改为 opaque handle；新增 `ucn_cluster_storage.h` 供唯一 Protocol Owner 静态分配。应用、Adapter 与 pointer-only 用户不能再通过公共头读写 member/backup/epoch 等内部字段。
- 版本：新增 `UCN_CLUSTER_API_VERSION=2`、`UCN_CLUSTER_STORAGE_LAYOUT_VERSION=2`。这是预发布阶段允许的源码/ABI 破坏性升级，要求全量重编译，但不改变 Wire。
- 门禁：新增 public/storage source gate、Owner allocation test；public-header test 明确不包含 storage。自审补抓并修复了 `EXCLUDE_FROM_ALL` Cluster simulator 的旧 `.role` 读取，Phase gate 扩展扫描 tools。
- 验证：GCC Full 22/22、Lite 20/20、Nano 20/20、Service-OFF 20/20；MSVC Release 显式构建 simulator 后 40/40；差异检查无空白错误。
- 结论：14-02 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT M14 EXTERNAL REVIEW**；进入 14-03。M05 顶层 `AUDIT HOLD` 不解除。

### OP-393｜CLV2-14-01 完成：Phase 唯一状态源与 Legacy Shadow 清理

- 源码：删除 Cluster 运行时 `role`、Shadow mapper/validator/sync 和可由 Phase 推导的 Backup/Recovery 布尔镜像；Role 只由 `ucn_cluster_phase_to_role()` 生成，初始化以外的 Phase 写只允许由 `cluster_transition()` 提交。
- 语义：`BACKUP_TAKEOVER` 不再同时暴露 Ready；Backup assignment 待发送工作从有界计数器推导；Recovery serial 耗尽显式进入 `DETACHED_OBSERVE`，不回绕、不继续伪装可恢复。
- 自审发现：修复了 stable assignment sweep 被错误 Phase 推导阻断的问题；同时纠正一个旧 fault-drop fixture 实际全分区却声称只丢包的测试缺陷。
- 门禁：新增 `tools/check_cluster_phase_source.py` 与 CTest source gate。GCC Full 21/21、Lite 19/19、Nano 19/19、Service-OFF 19/19；no-wrap/phase gate 均 PASS；差异检查无空白错误。
- 资源：Host Full `cluster_bytes=1608`，较 M13 的 1616 B 减少 8 B；不是 MCU 实测。
- 结论：14-01 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT M14 EXTERNAL REVIEW**；进入 14-02。M05 `AUDIT HOLD` 与 M13 外审状态不变。

### OP-392｜M14 连续实施启动：最终收敛、证据分层与分项自审

- 授权：用户明确要求继续进入 M14。建立 `CLV2-14-01..12` 连续实施计划，每个小节完成后独立自审，全部可实施项完成后再进行一次跨模块全体自审。
- 前置状态：M13 候选 `5c23078` 已推送但仍 `WAIT EXTERNAL REVIEW`；M05 顶层仍 `AUDIT HOLD`。M14 开工不追认任何未获外审的里程碑，也不自动授权 production v4 RX/TX/FSM、Authority、Adapter 或默认 encoder。
- 知识边界：依照 UCN 知识库的 MCU-first、固定资源、Host 证据不替代 MCU/实机原则，并以当前源码/任务表为最终事实。
- 硬件边界：14-08 必须有四板、真实 Flash/掉电、固件 Hash、介质与重复轮次日志；当前若无设备环境则标记硬件阻断，不用仿真伪装完成。14-12 必须等待 14-01..11 全部签字。
- 文档：新增 `UCN_V5_Cluster_M14_连续实施与自审计划_2026-08-24.md`，任务表将 M14/14-01 更新为进行中。
- 下一步：实施 14-01 Phase 唯一状态源和 Legacy Shadow 清理。

### OP-391｜M13 候选提交形成并发布远程审计基线

- Git：将 `CLV2-13-01..13` 的 Rekey/no-wrap 实验实现、Record schema v4/388 B、allocation history、Recovery serial、完整测试与逐项/全体自审材料形成候选提交 `5c23078`（`feat(cluster): implement M13 rekey and no-wrap safety`）。
- 审计基线：外部复审以 `d6eab1a..5c23078` 为主要功能区间；本 OP 与任务总览的提交哈希同步属于后续纯文档发布记录，不改变候选代码语义。
- 发布：推送目标为 `origin/codex/v5-adaptive-wire`。发布完成后本地与远程必须同 Head、工作树必须干净。
- 边界：提交与推送只提供可审阅工件；M13 仍为 **WAIT EXTERNAL REVIEW**，M05 顶层 `AUDIT HOLD` 不解除，不进入 M14，不宣称 Flash 掉电或 MCU 实机完成。

### OP-376｜M13 连续实施启动：13 项分节自审与最终全体自审门禁

- 授权：按用户要求启动 `CLV2-13-01..13`，每完成一个小节即进行合同核对、负向测试、受影响构建和独立记录；全部完成后再做一次跨事务/持久化/no-wrap/Profile/默认产品隔离的全体自审。
- 前置核对：07-00 的 Record provenance 已闭环；RFC4 Type 30/31/32 已冻结；M04 已有 Rekey/Tombstone 逻辑合同但运行时 Hook 仍 fail-before-I/O；M12.3 的 Epoch scope、ID history 与 Recovery serial exhaustion 明确转交 13-11..13。
- 架构边界：新增 Rekey 能力只进入 default-OFF 实验 archive 与受控测试。M05 顶层 `AUDIT HOLD` 不解除，production v4 RX/TX/FSM、Authority、Adapter、默认 encoder 与实机/掉电结论继续冻结。
- 文档：新增 `UCN_V5_Cluster_M13_RekeyNoWrap_连续实施与自审计划_2026-08-24.md`，任务表将 M13 标为连续自审进行中、13-01 标为 `IN PROGRESS`。
- 结论：下一步实现 13-01 Phase/state，并在通过该项自审后才进入 13-02。

### OP-377｜CLV2-M13 13-01 Phase/state 完成并通过分项自审

- 源码：Phase 枚举追加仅由 M13 transaction 使用的 `HEAD_REKEYING`；新增 default-OFF `ucn_cluster_rekey_experimental`、公共 guard header、bounded transaction 与发起入口。未给 Current FSM 增加任何可达边，未把 transaction 嵌入 `ucn_cluster_t`。
- 安全门：发起前执行 Authority preflight；只接受 `HEAD_STABLE + authority-active + Stable Config + live quorum`，并精确核对 current-schema durable active/max Epoch、committed Config、无 PREPARED/committed Rekey/Tombstone。transaction id/nonce 走 no-wrap 合法域。
- 回归：覆盖合法 begin、单对象二次 begin、过期 quorum、Joint Phase、legacy Record、Config PREPARED、0/越阈值 serial；全部失败路径保持 transaction 不写。
- 验证：MSVC Debug 定向 `2/2`；M13 OFF 默认 `ucn_cluster` 构建通过；production Cluster/codec/Adapter 引用扫描无 M13 接线；差异检查仅既有 CRLF 提示。
- 结论：13-01 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-02。M05 继续 `AUDIT HOLD`。

### OP-378｜CLV2-M13 13-02 serial threshold / rotation 路由完成并通过分项自审

- 源码：新增纯 `ucn_cluster_rekey_threshold_evaluate()`，把 Term、Config ID、Backup generation 的 reserved threshold 统一映射为 Rekey trigger；Snapshot ID 单独耗尽先映射为 generation rotation。
- 优先级：Backup generation 与 Snapshot 同时耗尽时只允许 Rekey；不会一边宣称 generation 可旋转、一边又处于 generation exhaustion。
- canonical：不存在 Backup/Snapshot 时对应 serial 必须为零；所有 present serial 必须在 `1..threshold`，越阈值拒绝且输出不写。
- 验证：定向 threshold matrix 通过；Cluster 源码 `UINT32_MAX→1` wrap pattern 扫描无命中；差异检查仅既有 CRLF 提示。
- 结论：13-02 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-03 Provider 新 Cluster ID。

### OP-379｜CLV2-M13 13-03 Provider successor identity 完成并通过分项自审

- 源码：`begin()` 强制调用产品 Cluster-ID Provider；request 绑定 REKEY purpose、parent Epoch/Config、boot incarnation 与 checked round。合法候选生成 successor `Cluster ID / Term=1 / same Head / Stable Config=1`，并填充 predecessor/successor Config ref 与 M04 Rekey ref。
- 原子性：0、广播、parent reuse、无 Provider、round exhaustion 在 transaction/round 写入前拒绝；只有完整 candidate 通过自身合法性检查才消费 round。
- 回归：验证 Provider request 全字段、successor 初值和 durable reference；四类失败路径逐字节检查 transaction 与 round 不变。
- 边界：13-03 不把 Provider callback 当作全局唯一证明；13-12 继续实现 durable allocation history/collision gate。
- 结论：13-03 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-04 typed Wire admission。

### OP-380｜CLV2-M13 13-04 Type30/31/32 typed Wire bridge 完成并通过分项自审

- 源码：新增 PREPARE/COMMIT decoded-raw builder 与 ACK admission。所有字段按 RFC4 固定 P0..P5 绑定 predecessor/successor Epoch、old/new Config、txid、nonce；ACK 还绑定 outer source、role、persistence generation/member nonce。
- 降级围栏：v3 profile、缺 Persistence/Rekey capability、source/profile 不同、冒充 Head 均 `ACCESS`；跨 txid/Epoch/new ID/Config/generation 均 `REPLAY` 且 typed output 不写。
- Commit 门：必须回读 exact successor active/max、successor Config、committed Rekey、terminal transaction 与 Tombstone，不能靠调用方手工设置 transaction state 放行。
- 边界：只处理 public raw frame value；没有启用 byte encoder、production dispatcher、FSM 或 Adapter。
- 结论：13-04 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-05 old Stable Config quorum。

### OP-381｜CLV2-M13 13-05 old Stable Config quorum 完成并通过分项自审

- 源码：transaction 冻结 canonical old Stable Config voter profiles；v3 或缺 Persistence/Rekey capability 的任一 voter 会让整个 begin 在 Provider 分配前 fail-closed。
- 持久化边界：只有 exact durable PREPARED 才开始 collection 并加入 Head self-bit；远端 ACK 只按冻结 old Config 位图计票。
- Replay：非 voter、跨 txid、低 member nonce 均零写拒绝；exact duplicate ACK 幂等；达到 majority 后单向进入 QUORUM。
- 验证：profile 降级、Config 并发前置门、self-only、2/3 quorum、重复/旧 ACK 回归通过。
- 结论：13-05 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-06 persistence ordering/crash matrix。

### OP-382｜CLV2-M13 13-06 persist-before-promise 与 crash ordering 完成并通过分项自审

- 源码：新增独立 Rekey persistence owner，按 `history durable proof → REKEY_PREPARE submit/reload → old Stable quorum → Authority revoke/Fence → successor+Tombstone COMMIT submit/reload` 单向推进；同步、异步、多次 Pending 共用 exact reload/journal 校验。
- 重入/时序：Provider `init/load/submit/poll` 的动态范围均先建立外置 I/O gate；Prepare/Wire/ACK 入口先执行 deadline preflight，过期事务零发送、零位图写入、零 Provider I/O。
- 失败语义：submit/poll/reload mismatch、超时与回调重入全部 fail-closed；持久化成功后的输出背压不伪装为 Provider 故障。
- 结论：13-06 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-07 Tombstone/replay。

### OP-383｜CLV2-M13 13-07 retirement Tombstone 与跨 Epoch replay 完成并通过分项自审

- 源码：Rekey Commit 在同一 canonical Record 中原子保存 retired Epoch、replacement Cluster ID 与 transaction ID；旧 Cluster identity 的 Type1..33 统一按 retirement identity 拒绝，不跨 Cluster 比较 Term。
- 回归：覆盖重启 reload、旧簇携带更大数值 Term、错误 replacement/txid 与撕裂 Record；退休身份均不能因数值更大而复活。
- 结论：13-07 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-08 successor materialize。

### OP-384｜CLV2-M13 13-08 successor materialize 完成并通过分项自审

- 源码：只有 exact durable successor、terminal Rekey journal、Tombstone 和 Authority revoked 同时成立时，才构造不可分割的 successor state 值；新 Epoch/Config/成员/Backup/serial 初值来自冻结事务，而非 Prepare 后的可变 RAM。
- 回归：覆盖 durable mismatch、未撤权、错误 Config/Tombstone、Prepare 后 Backup 变化与重复 materialize；失败输出保持不写。
- 边界：本项只生成候选状态值，不写 production FSM、Authority 或 Adapter。
- 结论：13-08 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-09 no-wrap CI。

### OP-385｜CLV2-M13 13-09 Cluster no-wrap CI 门禁完成并通过分项自审

- 工具：新增 `tools/check_cluster_no_wrap.py`，对 Cluster safety serial 的直接自增/自减、`UINT*_MAX → 0/1` 与显式 wrap idiom 做确定性扫描；门禁注册到所有受影响 CTest Profile。
- 验证：当前扫描 `PASSED files=31 fields=10`；脚本自身带 positive/negative fixture，防止空扫描假绿。
- 结论：13-09 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-10 safety suite。

### OP-386｜CLV2-M13 13-10 Rekey safety suite 完成并通过分项自审

- 测试：集中覆盖 threshold、Provider identity、allocation-history durable gate、PREPARE/ACK/COMMIT 顺序、旧 quorum、ACK 错绑/丢失、同步/异步/Pending/reentry/failure、restart、Backup freeze、successor materialize 与 Tombstone replay。
- 真实性：失败用例逐项断言 transaction/state/output 或 Provider 调用计数不变；不以 round-trip 自洽代替固定字段合同。
- 结论：13-10 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 M12 转交的 13-11..13 结构阻断。

### OP-387｜CLV2-M13 13-11 persisted Epoch scope / Recovery Tombstone 完成并通过分项自审

- Schema：writer 升级为 Record schema v4、固定 388 B；显式保存 Stable/Recovery scope、Recovery lineage/round/nonce/ID-round、retired identity Tombstone 与 M13 successor allocation-history fingerprint。v1/v2 280 B、v3 292 B 仅保留只读迁移。
- 恢复：Recovery record 重启后只恢复 Recovery 投影，不污染 Stable active/max 历史；旧 Recovery identity、错误 scope、非法 serial、CRC/长度/零尾均 fail-closed。
- 结论：13-11 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-12 durable allocation history。

### OP-388｜CLV2-M13 13-12 Cluster-ID allocation history 完成并通过分项自审

- 源码：新增 8-entry/280 B 固定 history Record，实施 ID 与 allocation identity 的一一映射、canonical encoding、CRC 和满载不淘汰策略。
- 顺序门：Rekey begin 先停在 `ID_HISTORY_DURABLE_REQUIRED`；只有 caller 将候选 history 原子存储、reload 后以 exact generation/body 确认，才允许提交 Rekey PREPARE。主 Record v4 同时绑定 history generation 与完整 fingerprint。
- 回归：固定 collision、同 ID 异 identity、同 identity 异 ID、错 body/generation、重启、空/满、CRC 与零尾均覆盖；满载返回 `UCN_ERR_NO_SPACE` 且不回收安全历史。
- 结论：13-12 为 **CODE COMPLETE / SELF-AUDIT PASS**；进入 13-13 Recovery no-wrap。

### OP-389｜CLV2-M13 13-13 Recovery serial no-wrap 完成并通过分项自审

- 源码：Recovery round、nonce 与 Cluster-ID allocation round 统一走 checked-next/rotation threshold；Record v4 持久恢复其 high-water，达到阈值返回 `EXHAUSTED`，不回到 0/1。
- 回归：覆盖阈值前进、耗尽拒绝、失败不消费、重启不回退和旧 Recovery identity replay；Recovery 与 Stable scope 互不污染。
- 结论：13-13 为 **CODE COMPLETE / SELF-AUDIT PASS**；13 个分项全部完成，进入全体自审。

### OP-390｜CLV2-M13 全体自审完成：受限实验软件范围等待外审

- 全局复核：重新核对旧 Stable quorum、history durable-before-PREPARE、persist-before-promise、Authority Fence、successor/Tombstone 原子性、durable proof、retired identity replay、serial no-wrap、Recovery scope 与 default-OFF 隔离。
- 主动整改：关闭 Wire/ACK/Prepare deadline 入口旁路、durable helper 缺 canonical validation、history 未持久化即可 PREPARE、history 冲突状态可编码、Provider init 重入覆盖不足及 schema 注释漂移。
- 验证矩阵：Windows GCC Debug Full `20/20`、Lite `18/18`、Nano `18/18`、Service OFF `18/18`、Release `20/20`；MSVC Debug（含 Scale）`30/30`；WSL ASan/UBSan `20/20`；WSL `-fanalyzer -Wall -Wextra -Werror` `18/18`；no-wrap gate `31 files / 10 fields`；默认 archive 与 production source isolation、`git diff --check` 均通过。
- 资源/证据：Host Full `cluster_bytes=1616`；Release 实验 archive text 合计 26376 B、BSS 16 B；Record codec SHA256=`76C0B187E518AD41A62660557C0A0FDC27B296903A060CA856218F218F97A4DC`。均不是 MCU Flash/栈/掉电实测。
- 结论：M13 更新为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL REVIEW（受限实验软件范围）**。M05 顶层 `AUDIT HOLD` 不解除，不进入 M14，不宣称生产可用。

### OP-375｜M12.3 审计候选提交与远程发布

- Git：将 M12.2 lineage adoption、M12.3 Recovery 生命周期整改、生产/对抗回归、任务表及自审/外审材料形成候选提交 `02358e9`（`fix(cluster): harden M12 recovery lifecycle`）。
- 发布：本记录随独立发布台账提交，一并推送至 `origin/codex/v5-adaptive-wire`；外部复审应以 `1fb4521..02358e9` 为主要代码审阅区间，并将后续发布台账提交视为文档记录。
- 边界：发布只提供可审阅代码工件，不改变 M12 的 `AUDIT HOLD / WAIT EXTERNAL`，不授权 M13、production v4 RX/TX/FSM、Authority、Adapter 或实机/掉电结论。

### OP-374｜M12.3 全体独立复审：运行期缺口整改、自审与 M13 阻断交接

- 复审范围：以 `1fb4521` 加 M12.2 未提交工作树为对象，重新审查 Recovery 域退出、identity、rank、round/member lease、Stable precedence、Wire v3、Persistence 与默认产品隔离；不沿用此前 PASS 结论。
- 运行期整改：Stable Join 清除旧 Recovery identity/nonce/source/deadline/ACK；Recovery Join 取消旧 Stable lineage-reset timer；same Recovery ID 精确绑定 `{Head,Term,parent,nonce}`，exact redeclare 重发 ACK；Type16/17 严格角色、非广播 ID、parent reuse 与非零 nonce；idle lineage/current-winner 仲裁 fail-closed；新 Recovery identity/stepdown 清旧成员；生产 Recovery provisional member按租约过期；live-primary 与 takeover-active Stable Backup 不再被 Recovery Declare 剥离。
- 对抗回归：新增 Stable→Recovery 延迟包、identity collision、parentless/same-parent stale Term、exact redeclare ACK、wrong-role/zero/broadcast/parent-reuse、跨轮成员、生产 archive 租约过期及 Stable Backup 整对象无写回测试；同时更新旧 fuzz seed，使其满足冻结后的严格角色合同。
- 独立验证：Windows MSVC Full Debug `41/41`、Release `41/41`、Lite `41/41`、Nano `31/31`、Service-Off `41/41`；WSL ASan/UBSan `41/41`，`-fanalyzer -Werror` `38/38`。定向生产 archive 与 Cluster 测试通过；Golden/OBSERVED、资源和差异检查在收口报告留证。
- 新确认边界：Record v1 无 Stable/Recovery Epoch scope；默认 32-bit mix 已观察到固定碰撞（两个 round 均得到 `3258608038`）；Recovery round/nonce 尚无 no-wrap/rotation。新增 `CLV2-13-11..13`，不在 M12 伪装闭环。
- 结论：`CLV2-12.3-01..04` 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL**；M12 整体继续 **AUDIT HOLD**，12-09 仍 PARTIAL，M13 未开始。当前工作树未提交、未推送。

### OP-372｜M12.1 外审复核：lineage adoption MAJOR 与 Term exact-binding MINOR，启动 M12.2

- 核实：外审指出的两条路径在当前源码成立。`handle_recovery_declare()` 在 join 时写 `recovery_cluster_id/cluster_id/term/head`，却未采用 frame 的 `recovery_parent_cluster_id` 与 Term 到本地 lineage；parentless 或 parent Term 落后 Member 因而可能绕开 current-winner fencing，或在下一轮 Recovery 丢失已接受的更高 lineage Term。已确认 M12.1 的 durable Term、有效 lineage 的 winner fence、Config forward refresh、12-09 PARTIAL 降级和 12-08 wording 原问题均已关闭。
- 同时确认 12-06 exact-round 缺 Term：同 source/nonce 的 DECLARE lease refresh 与 Recovery Head 接收 ACK 都未比较 `message.term`，可将 same cluster/nonce/parent 的错误 Term 当当前 round。
- 任务：新增 `CLV2-12.2-01..03`，范围严格限定为 lineage-aware DECLARE 的 ID/Term forward adoption、DECLARE/ACK Term exact binding 及定向/全量回归；不伪造 parent Config、不扩大 12-09 PARTIAL，不接入 v4/Authority/Adapter 或任何生产新能力。
- 状态：M12 回到 **AUDIT HOLD / M12.2 IN PROGRESS**；M13 继续未获授权。

### OP-373｜M12.2 lineage adoption 与 exact-Term binding：代码、自审及外审待签字

- 源码：`handle_recovery_declare()` 新增只在所有格式、parent-domain、winner rank 和 phase transition gate 成功后执行的 lineage evidence adoption。parent 为空时采用 frame 的非零 `parent_cluster_id/term`；相同 parent 仅前向提高 Term；`parent_config_id` 与 `recovery_round` 均不写。故 parentless survivor 加入 H1 后能立即以 H1 的 parent domain 执行 current-winner fence，落后 Term 也不会在下一轮 Recovery 丢失。
- 精确轮次：同 source/nonce 的 `RECOVERY_DECLARE` lease refresh 额外绑定 recovery ID、Term、parent；`RECOVERY_ACK` 在 member allocate、ACK count 和 lease 任何副作用之前绑定 `message.term == cluster->term`。wrong-Term 包返回 `UCN_ERR_REPLAY`，不延长 lease、不建立 member/ACK。
- 回归：新增 A–D 对抗集：parentless late survivor H1→delayed H2、A/T8→A/T9 后 lineage capture、wrong-Term DECLARE 无 lease 写入、wrong-Term ACK 无 member/ACK 写入并确认正确 ACK 可继续通过。夹具使用“非零且已过期 lease”模拟丢失旧 Head；零 deadline 是未武装 sentinel，接收门按 fail-closed 语义拒绝，不作为失联状态。
- 验证：MSVC Full Debug `41/41`、Full Release `41/41`、Lite `41/41`、Nano `31/31`、Service-Off `41/41`；WSL ASan/UBSan `41/41`、`-fanalyzer` `38/38`。Golden trace 比对通过，`OBSERVED-PAIRS=30` 且零违规；`git diff --check` 通过（仅既有 CRLF 提示）；变更生产模块无 v4/Authority/Adapter 新引用。
- 结论：`CLV2-12.2-01..03` 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**。M12 与 M05 继续 `AUDIT HOLD`，12-09 仍只为 PARTIAL，未获外审签字前不得进入 M13、不得扩展生产路径或实机结论。

### OP-357｜M11 R08-A 外审：candidate expiry replay-history 生命周期缺口与 R08-B 启动

- 外审核实：R08-A 已关闭同一 live candidate 内的 D1→D2→D1 ABA；但 `candidate_expire()` 整体清槽，同时删除 Epoch/Config nonce high-water 与 `hold_down_until_ms`。同 namespace 的旧 `nonce50/51` 因而可在 expiry 后重新成为 fresh samples，且 expiry 小于 hold-down 时会提前解除 anti-ping-pong fence。该 MAJOR 属实。
- 任务：新增 `CLV2-11-R08-B`。固定容量中分离 active candidate 与 replay tombstone；expiry 只结束资格/连续样本，history tombstone 不做时间淘汰。tombstone 满时必须 fail-closed，不能用清旧 history 换取新身份容量。
- 范围：M11 继续 `AUDIT HOLD`；R07 与 R08-A 同槽 ABA 的外审 PASS 保留。本轮不改变 default-OFF、production v4 RX/TX/FSM、Authority、Adapter、默认 encoder 或 M12 边界。

### OP-358｜M11 R08-B：expiry history / hold-down 闭环、自审与外审待签字

- 源码：`ucn_cluster_handover_candidate_t` 分离 `occupied`（identity/history）与 `active`（live hysteresis）；新增固定 4 条 `ucn_cluster_handover_replay_tombstone_t`。expiry 有空 tombstone 时转存 exact Epoch、Config ID/hash、nonce high-water、hold-down；tombstone 已满时只将 candidate 失活并原位保留 history。没有 signed freshness/incarnation 证明，因此不存在时间淘汰 tombstone 的路径。
- 行为：同 namespace 的旧/相同 nonce 无论 live、tombstone 或 full-history candidate 均在写入前 `UCN_ERR_REPLAY`；更高 nonce 可重启 activity，但不继承 samples，且继承原 hold-down。所有 tombstone 与 slot 都被 history 占用时，新 identity `UCN_ERR_NO_SPACE` 且整表不写。
- 回归：新增 D1 `100/101 → expiry → 50/51`、`102/103` sample/hold-down、tombstone 满后 expiry、所有 slot history 满后的 new identity 拒绝；断言每条失败路径 table `memcmp` 不变。
- 验证：Windows MSVC Full Debug `41/41`、Full Release `41/41`；Lite/Nano/Service-OFF 定向各 `1/1`；WSL GCC ASan/UBSan `41/41`、`-fanalyzer -Wall -Wextra -Wpedantic -Werror` 定向 `1/1`；default M11 OFF 的 `ucn_cluster.lib` 成功构建，production Cluster/Adapter 无 handover API 引用；`git diff --check` 通过（仅既有 CRLF 提示）。
- 结论：`CLV2-11-R08-B` 进入 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**；仍未提交。M11、M05 均保持 `AUDIT HOLD`，不得进入 M12 或接入生产 v4 RX/TX/FSM、Authority、Adapter、默认 encoder 或实机结论。

### OP-359｜M11 R08-B 外部复审 PASS，M11 受限实验范围结案

- 外部复审对象：`9386dca`。结论为 `BLOCKER=0 / MAJOR=0 / MINOR=0`；R08-B 外审 PASS，R08-A 继续 CLOSED。复审核实 expiry 只结束 `active`，但保留/转存 exact Epoch、Config ID/hash、nonce high-water 与 hold-down；D1 `100/101 → expiry → 50/51` 被拒绝，`102` 从 sample=1 恢复且 hold-down 不提前解除。
- 满载策略：4 tombstone + 4 candidate history 容量耗尽时，新 identity `UCN_ERR_NO_SPACE` 且 table 不写；外审接受“无可信 freshness/incarnation 证明时宁可拒绝新 admission、不得驱逐 replay proof”的 fail-closed 取舍。
- NIT 留档：production integration 前补 tombstone strict-forward 直接回归；`candidate_table_reset()` 继续只允许 initialization/destruction，不能被 runtime recovery 用作容量回收。两项均不阻塞本轮签字。
- 状态：M11 更新为 **DONE / EXTERNAL RE-REVIEW PASS / LIMITED EXPERIMENTAL GO**。该签字只覆盖 caller-owned、default-OFF 实验 archive；M05 顶层 `AUDIT HOLD`、M08 `WAIT EXTERNAL`、M10 外审等待以及 M12 未授权状态均不改变。未修改业务代码；本条文档同步尚未提交。

## 2026-08-23

### OP-356｜M11 R08-A review commit 与远端发布

- Git：将 R08-A replay namespace / hysteresis context 分离、ABA 与逐字段 mutation 回归、任务表、自审和外审材料整理为 review commit `fc92d91`（`fix(cluster): preserve M11 replay history`），推送目标为 `origin/codex/v5-adaptive-wire`。
- 外审范围：下一轮只需逐提交审阅 `2131915..fc92d91`；M03 R10、M11 R07 的已签 PASS 不重新打开，重点检查 R08-A 的 D1→D2→D1 nonce high-water、真正 Epoch/Config 进位、Config hash conflict 与无生产接线边界。
- 状态：`fc92d91` 仅为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**；M11 和 M05 继续 `AUDIT HOLD`，不进入 M12。

### OP-355｜M11 R08-A ABA replay namespace 自审与验证完成

- 源码：将 candidate 的两个概念拆开。`Epoch + Config ID/hash` 是不可回退的 replay namespace；只有 Epoch 严格前进、或同一 Epoch 下 Config ID 严格前进时，才创建新 namespace 并允许低 nonce。cluster size、available capacity、capabilities、wire format、Backup-policy compatibility 仅是 hysteresis context：变化时更新 offer、重置 `score_samples/first_seen_ms`，但必须携带高于当前 high-water 的 nonce。
- 回归：新增 `test_r08_replay_namespace_and_hysteresis_context()`。逐项变更 size/capacity/capability/wire/Backup-policy，均证明只重置样本；固定 `D1/100 → D2(capacity=7)/101 → D1/50,51`，两个旧包均 `UCN_ERR_REPLAY` 且 candidate table 字节不写；新 Epoch、同 Epoch 的 Config ID 严格前进均可从 `nonce=1` 开始，而旧 Epoch/Config 或同 Config ID 冲突 hash 均不可复活。
- 自审/验证：Windows MSVC Full Debug `41/41`、Full Release `41/41`；Lite/Nano/Service-OFF M11 定向 target 各 `1/1`；WSL GCC ASan/UBSan `41/41`；WSL `-fanalyzer -Wall -Wextra -Wpedantic -Werror` M11 定向 target `1/1`；默认 `M11=OFF` 的 `ucn_cluster.lib` 构建与 production source scan 均通过。`git diff --check` 无空白错误，仍仅有既有 CRLF/CP936 警告。
- 结论/限制：R08-A 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**；M11 继续 `AUDIT HOLD`，不进入 M12，不接入 production v4 RX/TX/FSM、Authority、Adapter 或默认 encoder。R07 与 M03 R10 的外部 PASS 保持有效。本条形成 review 工作树，尚未提交或推送，须先由外部复审检查实际代码与对抗回归。

### OP-354｜M11 R08 外部复审：ABA replay-history 缺口确认与整改任务启动

- 外部逐提交复审结论：M03 R10、M11 R07 均获 PASS；M11 R08 不能签字。现有 single-candidate 将 Epoch/Config 与 cluster size、available capacity、capabilities、wire format、Backup policy 一起作为 nonce reset proposal 域，会在 D1→D2→D1 的可逆上下文震荡中丢失 D1 的 nonce high-water，从而重新接收旧 nonce 并可能重造连续 score qualification。
- 决策：新增 `CLV2-11-R08-A`（MAJOR）。将 replay namespace 收紧为不可回退的 full Epoch + Config ID/hash；size/capacity/capabilities/wire/Backup-policy 改为独立 hysteresis context。context 变化清 `score_samples/first_seen`，但 nonce 必须继续严格递增；只有严格前进的 Epoch/Config 才允许从低 nonce 开始。
- 状态：M03 R10 更新为 **DONE / EXTERNAL RE-REVIEW PASS**；M11 R07 更新为 **DONE / EXTERNAL RE-REVIEW PASS**；M11 保持 **AUDIT HOLD / R08-A IN PROGRESS**。本轮不接入 production v4 RX/TX/FSM、Authority、Adapter 或默认 encoder，也不进入 M12。

### OP-353｜M03 R10 / M11 R07-R08 review commit 已推送

- Git：将 R10、R07、R08 的源码、生产/实验回归及任务/自审/审计台账形成 review commit `f009df2`（`fix(cluster): close epoch and candidate audit gaps`），并已推送至 `origin/codex/v5-adaptive-wire`。
- 审阅基线：外部复审应直接比较 `57c052e..f009df2`；该范围不包含 `main` 修改，也不改变 M05 顶层 `AUDIT HOLD`、M08/M10 的外审等待或任何 v4 生产接线边界。
- 后续：本条仅记录发布动作；代码结论仍是 M03 R10 与 M11 R07/R08 **SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**，等待外部逐提交检查和对抗回归复核。

### OP-352｜M03 R10 / M11 R07-R08 复审核实整改、自审与重新外审材料

- 审计结论与修改：（1）默认生产 `JOIN_PENDING` 重定向不再使用裸 `Term` 比较，统一构造 pending/remote Epoch 后调用 `ucn_cluster_epoch_compare()`；foreign 一律按 foreign policy 重定向，同簇仅 `LOWER/CONFLICT` 可改写，`HIGHER/SAME` no-write。（2）M11 candidate 的 `score_samples` 只累积连续达标 score，并绑定 local score、improvement percent、required samples/capabilities；这些 inputs 变化或不达标均清零，eligible 也拒绝 stale sample。（3）candidate proposal/replay 域扩展到 full Epoch、Config、size/capacity、wire/capabilities 与 Backup policy；新域重置 nonce/sample/first-seen，同域才执行严格 nonce replay。
- 新回归：生产 `ucn_cluster_receive()` 覆盖 foreign `A/100→B/1`、`A/1→B/100` 同样重定向，以及同簇新/旧 Term 的改写或保持；实验模型覆盖连续达标、阈值相等、低分清零、Epoch/Config/capability 域切换后的低 nonce 新 proposal、同域 replay no-write、local score/qualification inputs 切换后 eligibility 失效。
- 验证：Windows MSVC Full Debug `41/41`、Full Release `41/41`；R07/R08 handover target 在 Lite、Nano、Service-OFF 各 `1/1`；WSL GCC 13.3 ASan/UBSan `41/41`；默认 `M11=OFF` 的 `ucn_cluster.lib` 构建成功且 production Cluster/Adapter source scan 无 handover API；R07/R08 修改文件与 M03 merge 文件经 WSL GCC `-fanalyzer -Wall -Wextra -Wpedantic -Werror` 独立编译通过。`git diff --check` 无空白错误，仅保留仓库既有 CRLF 转换提示与 Windows CP936/C4819 编码警告。
- 台账/决定：M03 更新为 **AUDIT HOLD / R10 SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**；M11 更新为 **AUDIT HOLD / R07-R08 SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**。M08 默认 archive 内的 Authority Owner 是否做物理拆分仍是 release-control 决策，本轮不改为 default-OFF，以免在未完成独立审计前撤除默认产品的即时 Authority 安全预检。

### OP-351｜M03/M11 最新复审核实：Epoch 残余与 Candidate 迟滞域整改启动

- 审计核实：三项主要发现均属实。（1）默认生产 `JOIN_PENDING` 重定向直接比较 `candidate->term > pending_term`，不同 `cluster_id` 仍受数值 Term 影响，违反 M03 foreign-first。（2）M11 实验 candidate 将所有 fresh nonce 包累计为 `score_samples`，没有只统计连续达到 improvement threshold 的样本。（3）candidate 只按 `{cluster_id, head_node_id}` 绑定 replay/history，会让新 Epoch/Config proposal 误判 replay 或继承旧迟滞样本。
- 任务：新增 `CLV2-03-R10`、`CLV2-11-R07`、`CLV2-11-R08`。先修生产 JOIN_PENDING 的 comparator 收敛，再修 default-OFF M11 的连续样本与 proposal/replay domain，并补源码/生产 RX/实验模型回归。
- 状态：M03 改为 **AUDIT HOLD / R10 IN_PROGRESS**；M11 的先前受限 GO 被新的 R07/R08 整改暂时覆盖，改为 **AUDIT HOLD / R07–R08 IN_PROGRESS**。M05/M08/M10/M12 边界保持不变。
- 决策：M08 的“default archive 内 inert Authority Owner”属于后续 release-control 取舍，不在本轮改为 default-OFF；它当前仍承载默认产品的即时安全预检，未完成独立审计前不宜为物理隔离而撤去该安全收敛。

### OP-371｜CLV2-M12.1 外审收口完成：4 MAJOR + 1 MINOR 全部整改（SELF-AUDIT PASS / 待外审复审）

- 时间：2026-08-24。
- 背景：外部终审 b2fcde8 判定 M12 AUDIT HOLD（BLOCKER 0 / MAJOR 4 / MINOR 1），要求集中做 M12.1 closure。
- **MAJOR-1（durable Recovery Term 一致）**：(a) 新持久化 operation 类 `UCN_CLUSTER_PERSIST_OPERATION_RECOVERY_CREATE_COMMIT=15`——恢复域新身份允许携带 parent Term 镜像（term 为非零有效 serial），普通选举仍走 CLUSTER_CREATE_COMMIT 的 Term-1 规则；(b) begin_epoch 仅对 RECOVERY_DECLARE 动作选新类；(c) `declare_recovery_after_persistence()` 校验 durable {id,term,head} 与期望 epoch **逐字段一致**，否则 fail-closed；(d) `declare_recovery_head()` 直接采纳 durable epoch（不再从可变 lineage 重算）。测试：无 lineage Term-1 保留；parent T9 → durable T9 → RAM/wire T9（同步+异步）；Term-1 旧记录 vs parent T9 → 持久化故障 fail-closed 零发布。
- **MAJOR-2（Recovery Member 当前赢家 fencing）**：已跟随赢家的成员对**异源**候选按当前已接受 Head 排名（parent_term DESC, node ASC），不再与自身 node_id 比较；落后候选 REPLAY 零写。测试 4 组：跟随 H1 收 H2 → IGNORE；跟随 H2 收 H1 → SWITCH；T9 收 T8 → IGNORE；T8 收 T9 → SWITCH。
- **MAJOR-3（同 parent config 前向刷新）**：`cluster_lineage_capture()` 同 parent 分支允许 parent_config_id 前向升级（binding > 当前值才采纳），round 保留；stale 绑定不回退。测试：C12 → 绑 C13 同 parent 再捕获 → C13/round 3 保留；绑 stale C9 → 仍 C13。
- **MAJOR-4（12-09 scope 正式降级）**：采纳外审选项 B——12-09 = PARTIAL / boot-ID non-reuse only；重启入站 replay 保护（恢复域 tombstone）正式归 M13（schema v4）。12-09 报告重写（第 0 节裁决+降级），OP-368 补正。M12 不再宣称 12-01..12-10 字面全部完成。
- **MINOR（12-08 措辞）**：`recovery_quorum_met` 注释明确 Backup 门槛是"可见远端多数（保守隔离阈值）"，**不是** Config-majority/quorum 声明。
- 门禁：FULL Debug `All UCN tests passed`（含全部新增对抗测试）；ASan/LITE/NANO 待全量自审复核；Golden `8b80b08`、OBSERVED 零违例。
- 结论：M12.1 四项 MAJOR + MINOR 整改完成，SELF-AUDIT PASS，提交推送后待外审复审。工作区 `ucn-wt-m36`（分支 wt/m36）。
### OP-370｜CLV2-M12 全量自审完成：SELF-AUDIT PASS / WAIT EXTERNAL（停在外部审计边界）

- 时间：2026-08-24。
- 范围：12-01..12-10 全部完成并逐节点自审 PASS（OP-360..369），本 OP 为全量自审记录。
- 里程碑门禁 3/3：Recovery 使用新 cluster_id（12-02 provider 派生+validity 门+套件断言）；同 lineage 先比较 parent term/config（12-04 纯比较器+仲裁同父才比 term）；连续失败退避升级（12-03 指数+抖动+round++）。
- 禁止事项 3/3：无 cluster_id=parent 赋值（grep 全库）；parent_term/config 全部进入 ID/rank/退避/term 镜像；M01 零退避自旋删除（仅注释残留）。
- 门禁：FULL/ASan+UBSan/LITE/NANO 四 profile `All UCN tests passed` + authority 定向 target；OBSERVED-PAIRS 30 零违例；Golden `8b80b08` 逐字节不变、mismatch 0；cluster_bytes 1616（相对 M03 基线 +480，Host ABI，MCU 待测）；-Werror 零告警；diff --check 干净。
- 产物：M12 全量自审报告 + 9 份分项自审报告 + 实施计划 + 外部审计材料（UCN_V5_Cluster_M12_外部审计材料_2026-08-24.md）。
- 边界：round/lineage 持久化与恢复域专用 tombstone 归 M13（12-09 取"或"分支 boot incarnation）；线上 rank 为 v3 可用子集；M07/M10 owner 以谓词为契约。
- 结论：**M12 全量自审 PASS，按用户指令停在外部审计边界——不提交、不推送。** 工作区 `ucn-wt-m35`（分支 wt/m35）。
### OP-369｜CLV2-M12 12-10 完成：Recovery suite 全场景（分项自审 PASS）

- 时间：2026-08-24。
- 实现：`cluster_test_recovery_suite_m12`（test_cluster.c，注册于 12-08 之后）——12-01..09 机制的端到端组合，4 个 tick 驱动场景：(1) Primary+Backup 同死 + 双同 lineage 幸存者 → lineage 捕获（A/1）+ rank 仲裁收敛为单恢复域（node3 胜、node4 成员、term=parent_term、恢复 ID ≠ parent）；(2) TTL 循环（夹具 30ms）→ round 递增 ≥1、lineage 留存、再声明轮带新 ID；(3) Stable reclaim：parent 以 A/2 归来 → RECOVERY_HEAD 有序让位 + 成员 reclaim → 两者最终 MEMBER of A/2；(4) 异 lineage 岛（parent 1 vs 2）→ 双 RECOVERY_HEAD 并存、**互不让位**（Safety-4）、ID 互异。
- Safety/Liveness 映射：Safety-4（异 lineage 岛不合并）由场景 (4) 钉住；Liveness-4（无头域收敛）由场景 (1)(2) 钉住；Liveness-5（稳定头 reclaim）由场景 (3) 钉住。12-04/12-06 的单元级仲裁/replay 契约另由对应分项测试覆盖。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1616；-Werror 干净；diff --check 干净。
- **OBSERVED-PAIRS 29→30**：套件的 tick 场景把恢复域 rank 收敛/让位的相位复合纳入观察集，0 违例——证据正向增长。
- 结论：**12-01..12-10 全部完成，逐节点自审全部 PASS**。进入 M12 全量自审。工作区 `ucn-wt-m35`，不提交不推送。
### OP-368｜CLV2-M12 12-09 完成：恢复身份跨重启不复用（分项自审 PASS）

- 时间：2026-08-24。
- 实现与范围：任务允许"persist round/lineage **或至少** persist boot incarnation + tombstone"。M04 已有：受控启动 incarnation（04-09，REQUIRED 下 init 前 REPLAY_INCARNATION 严格递增并持久化）→ `config.cluster_id_incarnation` 回流（ucn_cluster.c L387/L485）→ 12-02 的链式 mix 把 incarnation 编入 Recovery ID。因此**重启后旧 Recovery ID 不可能被重新派生**——本节点以"验证 + 重启 replay 钉桩 + 边界文档"完成。
- 测试 `cluster_persist_test_recovery_identity_restart`（test_cluster_persist.c）：Boot1 工厂空 → 持久化 incarnation 建立（≠0、快照一致）→ 派生 id1；Boot2 装载 persisted 快照（incarnation=N）→ boot 边界派生 N+1 并落盘 → 派生 id2 ≠ id1；两 ID 均非 0/broadcast/parent。
- 边界（诚实声明）：(1) recovery_round/lineage 仍 RAM-only（v3 记录 292B 固定，扩展= schema v4，归 M13）；重启后 round 从 0 起、退避有界——**ID 不复用由 incarnation 保证**，这是任务"或至少"分支的完整交付；(2) 恢复域退役 tombstone 复用 M13 的 Rekey tombstone 槽语义，恢复域专用的 tombstone 需要 schema 扩展，明确归 M13；(3) 12-06 的 nonce 抗重放跨重启边界：nonce 无持久化，重启后旧轮帧靠"新 ID+incarnation"域隔离。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；OBSERVED-PAIRS 29 零违例；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1616；-Werror 干净；diff --check 干净。
- 结论：12-09 完成，分项自审 PASS。进入 12-10（Recovery suite）。工作区 `ucn-wt-m35`，不提交不推送。
- 补正（CLV2-M12.1 外审 MAJOR-4，2026-08-24）：本项自评 PASS 不成立——任务原文要求 boot incarnation + tombstone，本项只实现了前者。12-09 正式降级为 **PARTIAL / boot-ID non-reuse only**；重启入站 replay 保护（恢复域 tombstone）归 M13（schema v4）。详见 12-09 报告第 0 节。
### OP-367｜CLV2-M12 12-08 完成：min_recovery_peers 隔离策略（分项自审 PASS）

- 时间：2026-08-24。
- 实现（3 文件）：配置追加 `min_recovery_peers`（0 取默认 1——**默认禁止完全孤立自封**，产品可调高；校验 1..UCN_CLUSTER_MAX_PEERS）；`recovery_quorum_met()` 普通 Member 门槛改为 `visible_ADMITTED >= min_recovery_peers`；带 mirror Backup 保持**独立的镜像多数派门槛**（注释明确区分两类策略，不混称旧 quorum）。
- 测试 `cluster_test_recovery_isolation_policy`：(a) 0 可见 peer + 默认门槛 → 禁止自封；(b) 1 可见 peer 满足默认 1；(c) 门槛调 2：1 peer 拒绝、2 peer 通过；(d) Backup 镜像 3 voter：2/3 可见=多数派通过、1/3 拒绝。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；OBSERVED-PAIRS 29 零违例；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1616（配置追加 +4 B 被对齐吸收）；-Werror 干净；diff --check 干净。
- 结论：12-08 完成，分项自审 PASS。进入 12-09（Persistence）。工作区 `ucn-wt-m35`，不提交不推送。
### OP-366｜CLV2-M12 12-07 完成：Stable precedence 恢复域有序让位（分项自审 PASS）

- 时间：2026-08-24。
- 实现（4 文件）：
  1. 新 reason `UCN_CLUSTER_REASON_STABLE_RECLAIM = 32`（REASON_COUNT 33；静态断言 + phase_mapping_static 测试同步 +1）。
  2. 恢复域成员 reclaim 门（consider_head_offer，stale 防线之后、MEMBER 租约分支之前）：`role==MEMBER && recovery_scoped && candidate->cluster_id == parent_cluster_id && candidate->term >= parent_term` → MEMBER_ACTIVE/GRACE → JOIN_PENDING（STABLE_RECLAIM）+ begin_join_prepare_fields。**无 score 比较、无 capacity 门、绕过 11-08 成员冻结**；03-06 已拒更旧 Term；异父稳定头仍被排除（11-08 冻结继续生效）。
  3. RECOVERY_HEAD 的既有有序让位路径保持不变（任意合法稳定头优先——M01 01-04f 语义本就符合 12-07，新增 (d) 钉桩确认）。
- 测试 `cluster_test_recovery_stable_precedence`：(a) 成员收 parent lineage 更高 Term、score=100、capacity=0 → JOIN_PENDING + STABLE_RECLAIM + pending 三字段；(b) 异父稳定头 term=100 score=9000 → 保持恢复域零写；(c)+(d) 同 Term parent 稳定头 → RECOVERY_HEAD 有序 STEPPING_DOWN 保留。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；OBSERVED-PAIRS 29 零违例；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1616（无结构变化）；-Werror 干净；diff --check 干净。
- 结论：12-07 完成，分项自审 PASS。进入 12-08（Isolation policy）。工作区 `ucn-wt-m35`，不提交不推送。
### OP-365｜CLV2-M12 12-06 完成：Declare/ACK 绑定 lineage/round（分项自审 PASS）

- 时间：2026-08-24。
- 实现（3 文件）：
  1. ACK 回显：成员 ACK 携带 declare 的 `recovery_nonce`（当轮）+ `recovery_parent_cluster_id`（lineage 绑定）；codec 的 ACK 尾 12B 由全零改为 [nonce][parent][0]（旧帧解码为 0，保持 legacy 容忍）。
  2. 头侧 ACK 校验（handle_recovery_ack）：nonce 非零且 != 当前轮 → **REPLAY**（旧轮，零写，stale_messages++）；parent 非零且 != 本域 parent → ACCESS（异父岛）；重复同轮 ACK 保持幂等（仅租约刷新、ack_count 不增）。
  3. 成员侧 DECLARE 旧轮判别（handle_recovery_declare 最前）：`known_recovery_source == source` 且 `nonce < accepted_recovery_nonce`（next_nonce 单调，同源旧轮可判定）→ REPLAY 零写；同 nonce 异 cluster_id → REPLAY（不一致）；更大 nonce = 头的新轮 → 正常 rank/join 路径。
- 测试 `cluster_test_recovery_round_binding`：(a) 成员收同源旧轮（nonce 21 < 22）→ REPLAY、当前轮保留、零 transition、stale++；(b) 同源新轮（nonce 23 > 22、新 id 62）→ 采纳；(c) 头收旧轮 ACK → REPLAY、无成员槽、stale++；(d) 头收异父 ACK → ACCESS 零写；(e) 同轮重复 ACK → 幂等租约刷新、ack_count=1 不变。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；OBSERVED-PAIRS 29 零违例；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1616（无结构变化）；-Werror 干净；diff --check 干净。
- 结论：12-06 完成，分项自审 PASS。进入 12-07（Stable precedence）。工作区 `ucn-wt-m35`，不提交不推送。
### OP-364｜CLV2-M12 12-05 完成：Recovery 权威范围（分项自审 PASS）

- 时间：2026-08-24。
- 实现（4 文件）：
  1. 公共谓词 `ucn_cluster_recovery_scoped()`（RECOVERY_HEAD 角色，或 active cluster_id == recovery_cluster_id != 0 的恢复域成员）。
  2. M08 Authority Owner：`ucn_cluster_authority_runtime_init()` **最前**拒绝恢复域绑定（UCN_ERR_STATE，先于一切 config/timing 校验——恢复域永不发布为 parent Stable Authority）。
  3. Federation Directory：发布步对恢复域跳过新鲜注册/续租，但**保留撤回**（曾是稳定头后进入恢复域的节点必须能撤回旧记录）。
  4. v3 fail-closed 钉桩：恢复域成员收声称 parent 身份的 BACKUP_ASSIGN → UCN_ERR_ACCESS 零写（R01 栅栏）；RECOVERY_HEAD 收 parent lineage 的 HEAD_TAKEOVER 且 sender 伪造 → UCN_ERR_ACCESS 零写（R02/R03 历史证明门）。"禁止对旧 cluster_id 执行 takeover/config commit"在 v3 由既有 epoch/证明门按构造 fail-closed；M07/M10 实验 owner 以谓词为调用方契约（头注释声明）。
- 测试：test_cluster.c `cluster_test_recovery_scope`（谓词真值表 5 例 + BACKUP_ASSIGN/HEAD_TAKEOVER 两钉桩）；test_cluster_authority.c `test_recovery_scoped_cluster_never_gets_authority`（RECOVERY_HEAD 与恢复域 MEMBER 两种绑定均 UCN_ERR_STATE、runtime 未重绑、authority_active 恒 false；域解散后谓词翻转）。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed`；`ucn_cluster_authority_tests` 全过；OBSERVED-PAIRS 29 零违例；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1616（无结构变化）；-Werror 干净；diff --check 干净。
- 禁止事项核对：恢复域不发布 parent 权威（M08 最前拒绝+Federation 无注册）；跨父 Term 不比较（12-04 已锁）。
- 结论：12-05 完成，分项自审 PASS。进入 12-06（Declare/ACK 绑定 lineage/round）。工作区 `ucn-wt-m35`，不提交不推送。
### OP-363｜CLV2-M12 12-04 完成：Recovery lineage rank 与同父仲裁（分项自审 PASS）

- 时间：2026-08-24。
- 实现（4 文件）：
  1. 公共纯比较器 `ucn_cluster_recovery_rank_t/compare()`：同父 → parent_term DESC、parent_config_id DESC、score DESC、node_id ASC；异父 → UNRANKABLE；parent=0（未知 lineage）不可比。**Term 只在相等非零 parent 间比较**（M03 门禁）。
  2. 线协议：DECLARE 尾 12B 的既有零填充词承载 `recovery_parent_cluster_id`（旧帧读 0 → legacy 路径）；DECLARE.term 语义 = 父簇 term 镜像（declare_recovery_head 在有 lineage 时 term=parent_term，无 lineage 回退 1——既有 M01 全部回归不变）。
  3. 仲裁重写（handle_recovery_declare）：(a) lineage 帧且异父 → **绝不跨父让位**（各岛继续竞争，跨父收敛归 M11 Merge）；(b) lineage 帧同父 → 按 (parent_term DESC, node_id ASC) 排名让位（未起退避者仍总是接受）；(c) 无 lineage 帧 → 保留 M01 (nonce,node_id) 回退。v3 线无 score/config 通道：比较器全契约服务本地/v4 排序，DECLARE 仲裁用线上可得子集（文档声明）。
- 测试：(1) 比较器 6 组全契约——**T9 岛不被 T8 高 score（9000 vs 100）压制**、score DESC、node ASC、config DESC 压 score、异父 UNRANKABLE 双向、EQUAL/零父不可比；(2) 仲裁集成——T8 Head 收同父 T9 DECLARE → RECOVERY_YIELDED 让位并采纳 term 9；T9 收 T8 → 继续竞争零写；异父 term=100 也**不让位**（跨父 Term 不比较）。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1616（无结构变化）；-Werror 干净；diff --check 干净。
- **OBSERVED-PAIRS 30→29 归因**：消失对 14→4（RECOVERY_OBSERVE→MEMBER_ACTIVE 复合）——cluster_test_recovery_head 的 tick 级收敛路径由 (nonce,node) 仲裁切换为 lineage rank 后时序变化，被 15→4/16→14/5→3 等新复合替代；violations 恒 0，golden 不变，属 12-04 语义变化的可归因证据演进。
- 禁止事项核对：跨父 Term 永不比较（比较器+仲裁双重门）；未用 parent cluster_id 作 Recovery ID；未改退避。
- 结论：12-04 完成，分项自审 PASS。进入 12-05（Authority scope）。工作区 `ucn-wt-m35`，不提交不推送。
### OP-362｜CLV2-M12 12-03 完成：有界指数退避+确定性抖动+稳定加入重置（分项自审 PASS）

- 时间：2026-08-24。
- 实现（6 文件）：
  1. 配置追加 `recovery_backoff_base_ms`/`recovery_lineage_reset_ms`（0 取默认：base 200、reset 30000）；`base > max` 时**钳制**而非拒绝（兼容既有小 max 配置，退避钉在 max 仍非零有界）。
  2. `compute_recovery_backoff()` 重写：min(max, base<<attempt) + 确定性抖动（`cluster_id_mix(parent ^ round<<16 ^ node) % (base/4+1)`，attempt=min(round,4)），替代 M01 的 `node_id % max` 零值退化自旋；`cluster_id_mix` de-static 经 internal.h 暴露。
  3. TTL 到期/仲裁失败在 `stepdown_recovery_head()` 中 `recovery_round++`（退避升级 + 12-02 派生新 ID 的输入）。
  4. 稳定加入重置：`handle_join_accept` 站点武装 `lineage_reset_deadline_ms`（JOIN_ACCEPT 只来自稳定 Head，恢复域 Join 走 DECLARE/ACK 不武装）；MEMBER step 到期 `cluster_lineage_reset()` 清 parent_*/round/binding；任何 detach 取消计时器（lineage 留存）。
  5. 既有 M01 测试场景 (f)（钉零退避自旋）按 M12 授权改写为"钉正常提交"：backoff 恒非零 → RECOVERY_OBSERVE→RECOVERY_ELECTION 正常 commit（reason=RECOVERY_BACKOFF、count=1、deadline≠0）。
- 测试 `cluster_test_recovery_backoff_and_reset`：(a) 指数升级单调+封顶= max+恒非零；(b) 确定性（同状态同值）；(c) 真路径 TTL 到期 round 3→4 且 lineage 保留；(d) JOIN_ACCEPT 武装→到期清零 lineage/round+view；(e) 到期前 detach 取消计时器、lineage 保留；(f) 恢复域 Join（DECLARE）不武装。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；OBSERVED-PAIRS count=30 零违例；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1608→**1616**（+8 B：配置 2×u32+struct deadline，含对齐）；-Werror 干净；diff --check 干净。
- 禁止事项核对：TTL 后不再是固定间隔（指数+抖动）；退避有界（≤max）且非零。
- 结论：12-03 完成，分项自审 PASS。进入 12-04（Recovery rank）。工作区 `ucn-wt-m35`，不提交不推送。
### OP-361｜CLV2-M12 12-02 完成：Recovery ID 全 lineage 派生与唯一性（分项自审 PASS，含一处真实缺陷修复）

- 时间：2026-08-24。
- 实现（4 文件）：
  1. `ucn_cluster_id_request_t` 末尾追加 `parent_config_id/recovery_round`（位置兼容）；默认生成器从并列 XOR 改为**链式 mix**：每个输入（node/parent cluster/parent term/config/round/incarnation/对象 round/purpose）先经 `cluster_id_mix()` 雪崩再链入，位差不可能互相抵消。
  2. 新入口 `cluster_make_next_recovery_id()`（internal.h 暴露，测试钩子 `ucn_cluster_test_make_next_recovery_id`）；通用 `cluster_make_next_id()` 走同一 `_ex` 路径（config/round=0，行为不变）；恢复域声明站点改用新入口，携带 12-01 捕获的完整 lineage。
  3. 失败关闭保持：Provider 返回 0/broadcast/parent 复用 → `UCN_ERR_CONFIG`，**不消耗 cluster_id_round、不改 FSM**（03-08 契约延续）。
- **自审期间抓到的真实缺陷（测试先行价值）**：首版默认生成器用并列 XOR 混合字段，测试 (d) 立即失败——`recovery_round` 0→1 与对象轮 2→3 的位差在 XOR 中抵消，**连续恢复轮会派生出相同 ID**，直接违反 12-02"同节点不同 round ID 不复用"。修复为链式 mix，并新增 (e) 连续 8 轮序列钉桩（8×8 全对互异）。
- 测试 `cluster_test_recovery_id_uniqueness`：(a) 请求携带完整 lineage（purpose/parent 9/term 7/config 12/round 3/incarnation 5/对象轮 1）且首次分配消耗轮；(b) parent 复用拒绝且轮不消耗；(c) 0/broadcast 拒绝且轮不消耗；(d) 默认生成器按 recovery_round/incarnation/对象轮各维互异且永不等于 parent/0/broadcast；(e) 连续 8 轮全互异。
- 门禁：FULL Debug 与 ASan/UBSan `All UCN tests passed` 零报错；OBSERVED-PAIRS count=30 零违例；Golden `8b80b08` 不变、trace mismatch 0；cluster_bytes 1608（12-01 值不变，本节点无新字段）；-Werror 干净；`git diff --check` 干净。
- 禁止事项核对：生成的 Recovery ID 永不为 parent（validity 门）+ 永不为 0/broadcast；未忽略 parent term/config（已进请求与混合）。
- 结论：12-02 完成，分项自审 PASS。进入 12-03（有界指数退避+确定性抖动+稳定重置）。工作区 `ucn-wt-m35`，不提交不推送。
### OP-360｜CLV2-M12 12-01 完成：Recovery lineage 状态（parent 域字段+离簇前捕获）分项自审 PASS

- 时间：2026-08-24。
- 授权：用户指令接替完成 M12，逐节点自审通过后进入下一节点。12-01 为 P0 首节点：新增 recovery lineage 状态，在 Member/Backup Fence 离簇前捕获 parent 身份。
- 实现（5 文件 +220 行）：
  1. `ucn_cluster_t` 恢复域追加 `parent_cluster_id/parent_term/parent_config_id/recovery_round` + 内部绑定 `lineage_config_binding`（ucn_cluster.h 恢复区）；`ucn_cluster_view_t` 同步只读快照 4 字段；公共 API `ucn_cluster_lineage_bind_config()`（M07 实验 Config owner 的桥，0=无 config lineage，v3 默认产品恒 0）。
  2. `cluster_lineage_capture()`（ucn_cluster.c，经 internal.h 暴露）：优先取仍存活的稳定身份（`cluster_id != 0 && term != 0 && cluster_id != recovery_cluster_id`——恢复域自身绝不成为 parent），Detach 循环后再入时回退 03-06 历史（`last_cluster_id/max_seen_term`）；同 parent 只升级更高 Term 且**保留 round**（12-03 拥有 round 递增），异 parent 整体替换并消费 config 绑定、round 归零；无可信身份时零写返回。
  3. 两个真实 Fence 离簇点接线（站点所有，M01 顺序纪律）：MEMBER 宽限超时（GRACE_TIMEOUT）在 `set_detached()` 前捕获；Backup missed-heartbeat（PRIMARY_LOST）在 `backup_clear_sync()` 前捕获——两处都在 Active/Pending 清除**之前**。
  4. 测试（test_cluster.c 新增 `cluster_test_recovery_lineage_capture`，注册于 03-06 历史测试之后 + 白盒钩子 `ucn_cluster_test_lineage_capture`）：(a) Member 宽限超时真路径捕获 A/9/7 且 detach 后 parent 存活+view 快照；(b) Backup missed-heartbeat 真路径捕获；(c1) 同 parent 再入保留 round=3、升级 term 至 8；(c2) 异 parent 替换 lineage、round 归零、消费 bind_config=12；(c3) 恢复域（cluster_id==recovery_cluster_id）不被捕获为 parent。
- 门禁：FULL Debug `All UCN tests passed`；ASan/UBSan `All UCN tests passed` 零报错；Golden `8b80b08` 不变、trace mismatch 0；OBSERVED-PAIRS count=30 零违例；cluster_bytes 1584→**1608**（+24 B：5×u32 字段 + stats 成员前 4 B 对齐填充）；`git diff --check` 干净；-Werror 构建零告警。
- 禁止事项核对：未用 parent cluster_id 作为 Recovery ID（12-02 处理 ID 生成）；未忽略 parent term/config（字段+捕获+排序输入就位）；未改退避（12-03）。
- 结论：12-01 完成，分项自审 PASS。按用户指令进入 12-02（Recovery ID 唯一性）。本记录对应工作区 `ucn-wt-m35`（分支 wt/m35），不提交、不推送。
### OP-350｜M11 R06-B 外部复审 GO：受限实验范围签字

- 外部复审：R06-B 已确认闭环。撤权写入固定 Fence，任何非零 Fence 的 public `transaction_reset()` 均无副作用；`transaction_begin()` 仅接受完整零对象。`Revoke→Reset→Begin`、`Stepdown→Reset→Begin`、`Commit→Reset→Begin` 三条回归均保持对象逐字节不变，且不会恢复 `local_authority_active`。
- 外部验证结论：M11 配置 CTest `41/41` 通过；默认 `ucn_cluster` 构建通过，生产目录无 M11 引用。签字范围只覆盖 caller-owned、default-OFF 的 M11 实验模型与 Host 软件证据。
- 台账同步：任务表、M11 连续实施计划、自审报告与外审材料均改为 **DONE / 外部复审 GO（受限实验范围）**。
- 结论/限制：re-entry Fence 不是掉电或原始内存破坏防护；未来生产接线必须由 M04 持久化/reload 与真实 Authority Owner 保证。M05 继续 `AUDIT HOLD`，M08 继续 `WAIT EXTERNAL`，M10 仍待外审，M12 尚未开始。没有修改业务代码、没有提交或推送。

### OP-349｜M11 外审 R06-B：public Reset 不能绕过撤权 Authority Fence

- 审计核实：R06 的 Begin non-reset 围栏成立，但公开 `transaction_reset()` 仍可把已撤权、已发 Stepdown 或 Commit 的 caller-owned object 清零；随后 Begin 会重新设置 `local_authority_active=true`。该 P0 属实，范围仍限于 default-OFF 的 M11 实验 archive。
- 修改：transaction 增加 implementation-owned `authority_reentry_fence`。`revoke_authority()` 设置固定不可逆 Fence；public `transaction_reset()` 对任何非零 Fence 都严格 no-op。transaction validator 只接受零或正确 Fence，且要求撤权 nonce 阶段与 Fence 一致；错误的非零 Fence 同样 fail-closed。故 Reset 不能成为旧 Head Authority 的生命周期 release。
- 回归：扩展 R06 定向用例，依次覆盖 `Revoke→Reset→Begin`、`Stepdown→Reset→Begin`、`Commit→Reset→Begin`。每例均断言 Reset 后对象逐字节不变，合法 Begin 返回 `UCN_ERR_STATE`，`local_authority_active=false`；另由结构 validator 覆盖异常 Fence 的 no-write 拒绝。
- 验证：R06-B 后实际重建并执行 Windows MSVC Full Debug + config contract `41/41`、Full Release + config contract `41/41`、Lite + config contract `20/20`、Nano + config contract `20/20`、Service-OFF + config contract `20/20`；WSL GCC 13.3 ASan/UBSan `38/38`，以及 `-fanalyzer -Wall -Wextra -Wpedantic -Werror` 的 M11 定向 target `1/1`。此前台账把跨 Profile CTest 数量概括为 Full 的 41，现按本次实际测试集数更正；既有 CP936/C4819 编码警告未升级为错误。
- 台账：任务表、M11 连续计划、自审报告和外审材料均升级到 `R01–R06-B`。M11 继续 **AUDIT HOLD / WAIT EXTERNAL RE-REVIEW**；不得进入 M12、不得接入 production v4 RX/TX/FSM/Authority/Adapter/default encoder，也不提交或推送。

### OP-348｜M11 外审 R06：撤权 transaction 的二次 Begin Authority 复活整改

- 审计核实：R01–R05 均成立，但 `transaction_begin()` 在有效 transaction 上无条件 reset；因此 Begin→Ready→Revoke 后再次 Begin 会清除撤权 trace 并重新写入 `local_authority_active=true`。该 P0 属实，范围仍限于 default-OFF 的 caller-owned M11 archive。
- 修改：`transaction_begin()` 先完整校验输入，再要求 `transaction_is_reset()`；在合法 Begin 参数下，任何 active、terminal 或其他 non-reset 表示都在写入前返回 `UCN_ERR_STATE`，其它输入错误同样 no-write。public reset 的注释同步为显式生命周期前置条件，明确 Begin 不可覆盖已撤权/Stepdown/Commit transaction。
- 回归：新增 `test_r06_begin_cannot_reopen_revoked_transaction()`，覆盖 `AUTHORITY_REVOKED`、`STEPDOWN_SENT`、`COMMIT_SENT` 三个阶段；每次以不同 txid/now 重复 Begin，均断言返回 `UCN_ERR_STATE`、transaction 完整字节不变、`local_authority_active=false`。R06 后重新执行 Windows MSVC Full Debug/Release、Lite、Service-OFF 各 `41/41`，Nano `31/31`，WSL ASan/UBSan `38/38`，WSL `-fanalyzer -Wall -Wextra -Wpedantic -Werror` 定向 target `1/1`。
- 台账：任务表、M11 连续计划、自审报告、外审材料更新为 R01–R06；M11 保持 **AUDIT HOLD / WAIT EXTERNAL RE-REVIEW**，不进入 M12、不提交、不推送，M05/M08/M10 与默认产品隔离边界不变。

### OP-347｜M11 R01–R05 最终自审矩阵、生产隔离核对与外审材料更正

- 最终矩阵：Windows MSVC Full Debug + config contract、Full Release、Lite Debug + config contract、Service-OFF Debug + config contract 均为 `41/41`；Nano Debug + config contract 为 `31/31`；WSL GCC 13.3 ASan/UBSan 为 `38/38`；WSL `-fanalyzer -Wall -Wextra -Wpedantic -Werror` 的 M11 定向 target 为 `1/1`。此前台账中把 Service-OFF 写作“待完成”、把 analyzer 误概括为全量，现已在 M11 自审与外审材料中更正为实际范围。
- 最终源码核对：Type 26/27/28 只生成/接收零 nonce，Type 9/29 才持有撤权后的 fence nonce；所有 transaction 公开入口先经有界结构校验，`trace_count > 8`、非法 trace enum 或非法 state 都不读取 trace/不写 output；duration、Term、Config ID、txid 分别受安全时间/serial 域限制；`TARGET_COMMITTED` 超时进入 Abort/Observe，未接线 M04 `submit→reload` 证明前 target durable 恒 fail-closed、不会产生 Authority。
- 产品隔离核对：默认 `UCN_BUILD_CLUSTER_HANDOVER_EXPERIMENTAL=OFF` 构建成功；production Cluster、Adapter、公开 `ucn_cluster.h` source scan 未发现 `ucn_cluster_handover` 调用。`git diff --check` 通过，输出仅为既有 LF/CRLF 提示。
- 结论/限制：R01–R05 是 **SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**；M11 仍为 **AUDIT HOLD**，M05 `AUDIT HOLD`、M08 `WAIT EXTERNAL`、M10 外审等待以及默认 v4 RX/TX/FSM/Authority/Adapter/encoder 禁止均不改变。未提交、未推送，也未进入 M12。

### OP-346｜M11 外审 R01–R05 整改：RFC nonce、时间/serial、target durable 与公开 transaction 围栏

- 审计核实：M11 外审提出的 1 个 P0 与 4 个 P1 均属实，且仅存在于 default-OFF 的实验 handover archive：（R01）Type 26/27/28 的 typed model 错把 `stepdown_nonce` 纳入身份，RFC4 只有 Type 9/29 有该字段；（R02）policy 接受超过 `INT32_MAX` 的 duration、M11 自己未做 Term/Config/txid rotation guard；（R03）`TARGET_COMMITTED` 无 deadline 且可由 caller 给出的同 Epoch 伪造 durable；（R04）公开 `trace_count` 超过 8 可越界读取；（R05）原测试没有把这些对抗路径正式锁住。
- 修改：Prepare/Ready/Commit 的 typed nonce 固定为零并从 identity comparison 移除；nonce 改为 Losing Head 已验证 READY 后调用 revoke 时生成，仅由 Stepdown/Withdraw 使用。policy 的全部时间字段统一调用 `ucn_duration_is_valid()`，Epoch Term、Config ID、txid 统一通过 `UCN_CLUSTER_SERIAL_ROTATION_THRESHOLD` 域。target Commit/mark 接口带当前时间，`TARGET_COMMITTED` 到期必转 `ABORTED + Observe`；在未接线 M04 Provider `submit→reload` 证据前，`mark_target_epoch_durable()` 固定 `UCN_ERR_UNSUPPORTED`、`target_authority_ready()` 恒 false。
- 防御：新增私有 `transaction_is_valid()`，所有 public transaction 入口先检查 bounded state；`trace_count <= 8`、trace enum、状态、Epoch、Config、txid、deadline、nonce phase 都必须自洽。invalid caller-owned value 不读取 trace、不写 transaction/output。实验 API 随此调整：`begin()` 不再传 nonce，`revoke_authority()` 接收 Type 9/29 nonce，target Commit/durable API 显式携带 `now_ms`。
- 回归：`test_cluster_handover.c` 新增 Type 26/27/28 nonce zero/forgery、Type 9 nonce、`INT32_MAX+1`、Term/Config/txid threshold、target Commit 后 fake durable/no authority/timeout，以及 `trace_count=9` output/transaction no-write。Windows MSVC Full Debug+config-contract `41/41`、Release `41/41`，定向 handover `1/1` 均通过；既有 C4819 编码警告未新增为错误。
- 台账：M11 改为 **AUDIT HOLD / R01–R05 CODE COMPLETE / WAIT EXTERNAL RE-REVIEW**；M05 `AUDIT HOLD`、M08 `WAIT EXTERNAL`、M10 外审等待和默认产品 v4/RX/TX/FSM/Authority/Adapter/encoder 禁止全部保持。未提交、未推送，待完成 Lite/Nano、sanitizer/analyzer、默认 archive 与全量边界自审后再送外审。

### OP-345｜M11 Merge/Handover 全量受控实现、自审与外审材料

- 实现：新增 default-OFF 的 `ucn_cluster_handover_experimental` caller-owned archive 和硬 include 门，仅包含 fixed-capacity foreign Merge candidate、迟滞、可行性、Prepare/Ready/Commit、Stepdown target 与 same-cluster Planned Transfer value model；它不调用 v4 encoder、不发送/接收帧、不写 `ucn_cluster_t`、不接入 Authority、Adapter 或默认产品 FSM。M11 Stepdown 与 RFC4 Type 9 对齐，只绑定 old/target Epoch、txid/nonce，明确不携带或改变 Config。
- 默认产品安全收敛：删除 legacy `backup_challenge_after_persistence()`，把 `backup_challenge()` 固定为 no-write `UCN_ERR_UNSUPPORTED`；Backup 对 live Primary 的 score 仅作 lease evidence，Member 对 foreign/same score 仅清候选样本，均不再制造 `Term + 1`、`LEAVE` 或 `JOIN_PENDING`。历史测试代码一并删除，旧测试/注释改为只验证 retired path 的 fail-closed 合同。
- 分项自审：11-00..10 分别核对 header/archive 隔离、foreign Term 不比较、bounded candidate/replay、迟滞、capacity/wire/capability/config/Backup gate、typed retry/replay、`READY→revoke→Stepdown→Commit`、Member/Provisional/Backup target、默认 v3 自主切换移除、same-cluster confirmed Backup transfer 及赢家/撤权后旧 Head故障 Observe 收敛；定向 `ucn_cluster_handover_tests` 覆盖相应正反例。
- 全量验证：MSVC Full Debug/Release、Lite、Nano、Service-OFF 各 `17/17`；Full + config contract `41/41`；WSL GCC 13.3 ASan/UBSan `38/38`、`-fanalyzer -Wall -Wextra -Wpedantic -Werror` `38/38`。默认关闭构建成功，`nm` 未发现 `ucn_cluster_handover_(transaction|candidate|offer|member|feasibility)` 符号；production Cluster/Adapter source scan 无 M11 API 调用；`git diff --check` 通过（仅既有 CRLF 提示）。
- 自审发现并修正：（1）初版 Stepdown typed message 错把 target Config 当作成员当前 Config 比较；现已改为严格 Type 9 零 Config fields，并补跨簇 old Config 与 target Config 不同仍可安全接收、伪造 Config 字段 output 不写回的回归。（2）同簇 Backup 的 receiver context 现分离实际 old `local_epoch` 与仅供本次事务匹配的 `expected_target_epoch`；提前宣称 target Epoch 会被零写拒绝，避免 READY 被解释成未持久化的 Head。
- 结论/限制：M11 记为 **SELF-AUDIT PASS / WAIT EXTERNAL（受控实验软件范围）**。M05 `AUDIT HOLD`、M08 `WAIT EXTERNAL`、M10 外审等待不变；不构成 RFC4 生产互通、真实持久化/掉电、MCU 资源、无线/多跳、Authority 或实机放行。未提交、未推送，外部审计入口见 `UCN_V5_Cluster_M11_外部审计材料_2026-08-23.md`。

### OP-344｜M11 Merge/Handover 启动：受控实验范围、默认产品安全收敛和连续自审计划

- 授权：用户要求直接连续完成 M11；每个小项自审、完成后全量自审，再整理外部审计材料。
- 现场核对：M05 仍 `AUDIT HOLD`、M08 仍 `WAIT EXTERNAL`、M10 尚待外审复审，因此 M11 不得提前接入 production v4 RX/TX/FSM、Authority、Adapter 或默认 encoder。现有 v3 Merge 代码仍有 Member 按 score 直接 `LEAVE/Join` 与 Backup score→`Term+1` challenge 两条历史路径，均与 M11 目标冲突。
- 计划：新增 `UCN_V5_Cluster_M11_MergeHandover_连续实施计划_2026-08-23.md`；完整 Merge/Handover 采用 default-OFF、caller-owned 实验 archive，覆盖 foreign/same classifier、candidate/hysteresis、feasibility、Prepare/Ready/Commit、撤权顺序、Stepdown、Member switch 删除、Planned Backup transfer 和组合测试。默认产品只撤除历史自主切换，不获得 M11 v4 协议能力。
- 结论/限制：M11 状态为 **IN PROGRESS / 受控实验范围**；不提交、不推送。本阶段不将实验自审绿灯表述为 M05、M08、M10 或协议生产放行。

### OP-343｜M10 外审 R34 P1：终态回归区分度补强与复审重跑

- 审计核实：R31–R33 的修复已成立；外审进一步指出 R34 的两个终态反例区分度不足——只测到期 `step(1000)`，且 unreachable 命中已投票节点 `3`，旧 deadline/bitmap-overlap 逻辑也可能拒绝，不能单独证明 terminal gate。
- 修改：`test_impossible_timeout_and_durable_epoch()` 在 durable snapshot 后先执行未到期 `step(150)` 并逐字节断言无写；unreachable 改为未投票节点 `1`，继续逐字节断言 `UCN_ERR_REPLAY` 与 transaction/head-ready 不变。该用例现在分别锁住正常推进时刻、到期时刻和不依赖 bitmap-overlap 的 terminal 输入门。
- 验证：定向 `ucn_cluster_takeover_tests` `1/1`；Windows GCC Full/Lite/Service-OFF `38/38`、Nano `28/28`、Release/O1-O3 与 config-contract `41/41`、128 B/3-Link 默认关闭产品配置 `17/17`；WSL GCC 11.4 ASan/UBSan `38/38`、`-fanalyzer -Wall -Wextra -Wpedantic -Werror` `38/38`。`git diff --check` 通过（仅既有 CRLF 提示）。
- 结论/限制：R34 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**；这只纠正实验 archive 的测试证明力度，不改变 M05 `AUDIT HOLD`、M08 `WAIT EXTERNAL`、默认产品隔离或 Flash/掉电/实机未验收边界。本条不提交、不推送。

### OP-342｜M10 外审 R31–R34 整改、矩阵自审与复审材料更新

- 审计核实：外审指出的 R31（通用 `EPOCH_COMMIT` 绕过完整 current-Epoch VoteId）、R32（`EPOCH_DURABLE` 非单向终态）、R33（历史 Vote 阻塞连续 M10）和 R34（10-11 覆盖表述不实）均属实；首轮 M10 “WAIT EXTERNAL”自审基线已撤回，M10 继续 **AUDIT HOLD**。
- 修改：generic Epoch transition 在 current Active Epoch 有 complete takeover VoteId 时 fail-closed，保留 `TAKEOVER_EPOCH_COMMIT` 为唯一 certificate-bound successor 写入；`EPOCH_DURABLE` 改为 terminal，`step` 无副作用、迟到 vote/unreachable 零写拒绝、exact durable replay 幂等；新 Vote 仅拒绝 current-Epoch promise，历史 full/v1/v2 partial Vote 可由下一轮 v3 full Vote 原子替换。
- 回归：`test_cluster_takeover.c` 新增 generic bypass durable-record 无变化、durable 后未到期/到期 step、迟到 vote/unreachable、exact replay、首次 M10→dedicated Epoch→新 Snapshot→第二轮 M10，以及 current/historical v2 partial Vote 的正式断言；10-11 的宣称收窄为软件 fault/reorder 子集。
- 验证：Windows GCC Full/Lite/Service-OFF `38/38`、Nano `28/28`、Release/O1-O3 与 config-contract `41/41`、128 B/3-Link 默认关闭产品配置 `17/17`；WSL GCC 11.4 ASan/UBSan `38/38`、`-fanalyzer -Wall -Wextra -Wpedantic -Werror` `38/38`。默认 archive 和 production call-site 扫描仍无 M10 接线；`git diff --check` 通过（仅既有 CRLF 提示）。
- 结论/限制：R31–R34 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL RE-REVIEW**，尚未取得外部复审 GO；M05 `AUDIT HOLD`、M08 `WAIT EXTERNAL`、默认 v4 encoder/production RX/TX/FSM/Authority/Adapter 禁止以及 Flash/掉电/实机未验收均不变。本条不提交、不推送。

### OP-341｜M10 最终 Majority Takeover 受控实现、分项/全量自审与外审材料

- 实现：新增 caller-owned `ucn_cluster_takeover` value model 与独立 `ucn_cluster_takeover_persist` Provider owner；冻结 M09 committed snapshot + M07 canonical Config，完整 VoteId 绑定 `{cluster,old/proposed term,config,backup,generation,snapshot}`。Stable/Joint quorum、canonical Certificate/fragment/CRC、old Primary higher-term fence、staging exclusion、impossible/timeout 与 modular-clock 边界均进入定向测试。
- 持久化：Record writer 采用 append-only schema v3（292 B）；v1/v2 保持 280 B read-only decode。普通 partial Vote 永不作为 M10 proof；完整 M10 Vote 与 proposed Epoch 通过独立 operation，经 `submit → completion → load + exact journal/record` 后才由 owner 推进 RAM transaction。Provider `load/submit/poll` 使用 callback reentry fence，PENDING/失败/错 reload 不产生 Head-ready、Authority 或发送。
- 物理隔离：新增 `UCN_BUILD_CLUSTER_TAKEOVER_EXPERIMENTAL=OFF`。开启时 M10 仅生成独立 `ucn_cluster_takeover_experimental` archive；默认 `libucn_cluster.a` archive 不含 takeover object。M10 header 额外要求由该 target `PUBLIC` 导出的 `UCN_CLUSTER_TAKEOVER_EXPERIMENTAL_ENABLED=1`，裸包含已做编译期拒绝探针。扫描确认 production `ucn_cluster.c`、Adapter、public `ucn_cluster.h` 无 M10 API 调用，默认 v4 encoder 仍为 0。
- 分项自审：10-00..11 已逐项核对 no-write、full VoteId/partial legacy reject、Stable/Joint quorum、certificate、sync/async/reentrant persistence、fence、staging、legacy、timeout 与 property/reorder 软件故障模型；结果均 PASS。新增 `UCN_V5_Cluster_M10_分项与全量自审报告_2026-08-23.md` 和 `UCN_V5_Cluster_M10_外部审计材料_2026-08-23.md`。
- 验证：Windows GCC Full/Lite/Service-OFF 各 `38/38`、Nano `28/28`、Release/O1-O3 与 config-contract 各 `41/41`、用户 128 B/3-Link 产品默认关闭构建 `17/17`；WSL GCC 11.4 ASan/UBSan `38/38`、`-fanalyzer -Wall -Wextra -Wpedantic -Werror` `38/38`。Release 实验 objects：takeover `.text=24820 B`，persist owner `.text=4864 B/.bss=16 B`；均非最终 MCU 链接结果。`git diff --check` 通过（仅既有 CRLF 提示）。当前无可用 MSVC 实例，未宣称 MSVC 已复验。
- 结论/限制：M10 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL（受控实验软件范围）**。M08 仍 `WAIT EXTERNAL`、M05 仍 `AUDIT HOLD`；不得接入 production v4 RX/TX/FSM、Authority、Adapter、默认 encoder 或实机/Flash/掉电结论。本条不提交、不推送，等待独立外部审计。

### OP-340｜M10 启动：最终 Majority Takeover 受控连续实施

- 授权：用户明确要求连续实施 M10，逐项自审、最后全量自审后送外部审。现场仍显示 M08 `WAIT EXTERNAL` 与 M05 `AUDIT HOLD`，因此将 M10 固定为受控实验模型范围，而不是擅自接入产品路径。
- 方案：新增 `UCN_V5_Cluster_M10_最终MajorityTakeover_连续实施计划_2026-08-23.md`，冻结 10-00..11 的顺序、完整 VoteId、Record v3 迁移、Stable/Joint 双 quorum、RFC4 Certificate、persist-before-Head、旧 Primary Fence、staging/legacy exclusion、timeout/impossible 与 crash/property 验收。
- 边界：M10 API 必须 caller-owned；默认 v4 encoder、production `ucn_cluster.c` RX/TX/FSM、Adapter、Authority 和实机结论均继续禁止。该启动不将 M08/M09/M05 状态误记为整体放行。

### OP-339｜M09-R01 外部复审 GO：状态台账同步

- 外部复审结论：R01（Coverage 缺失 protected voter 不得借用 grace）已签署 **GO（受限范围）**。外审确认 `missing` 在 grace 前被识别，`takeover_ineligible` 在任何后续 `ADMITTED` 输入前先行拒绝；Stable、Joint 的 `C_new` voter 缺失以及恢复 `ADMITTED` 不复活均有定向回归。
- 台账同步：R01 报告、M09 全量/04-11 自审报告与任务表均更新为 R01 外部复审 GO；`CLV2-09-08` 标记 DONE。M09 总体状态改为 **AUDIT HOLD / BLOCKED BY M08 WAIT EXTERNAL**，避免将单项复审 GO 误写为 M09 或 M05 的整体放行。
- 结论/限制：不进入 M10，不修改生产 v4 RX/TX/FSM、Authority、默认 encoder 或实机声明；待 M08 外审状态同步后，才可重新评估 M09 的后续门禁。本条仅同步外审结论，未新增业务代码、未提交、未推送。

### OP-338｜M09 外审 P1 R01：缺失 protected voter 立即 Coverage fence

- 审计核实：外审 P1 属实。`coverage_is_valid()` 只验证表的 canonical 格式，不要求覆盖 Active Config 的所有 protected voters；原 `update_coverage()` 在 initial coverage 不成立而不存在显式 `REMOVED` 时错误进入 grace。缺失条目不是已知 transient `SUSPECT`，不能保留 future takeover eligibility。
- 修改：Coverage 更新改为显式区分 `ADMITTED / SUSPECT / REMOVED / missing`。只有明确 `SUSPECT` 才能启动 wrap-safe grace；`REMOVED` 或 `missing` 都立即清 grace、永久设置 `takeover_ineligible`。owner 一旦失效，随后完整 `ADMITTED` 输入仍返回 `UCN_ERR_STATE`；只允许新的 Backup assignment init 重置资格。
- 回归：新增 Stable `{1,2,3}` 缺 `3`、Joint `C_old={1,2,3}/C_new={1,2,4}` 缺 `4`、以及 missing 后恢复 ADMITTED 不复活；保留 SUSPECT flap、REMOVED immediate 和 deadline 回绕。定向 sync test 通过。
- 验证：Windows GCC Full/Lite/Nano 各 `16/16`、config-contract `19/19`、产品配置 `17/17`、Release/Service OFF 各 `37/37`、Windows GCC 14.2 `-fanalyzer -Wall -Wextra -Werror` `37/37`、RflySim-20.04 WSL GCC 11.4 ASan/UBSan `37/37`。`git diff --check` 通过（仅既有 CRLF 提示）。
- 结论/限制：R01 为 **CODE COMPLETE / SELF-AUDIT PASS**，但 M09 恢复为 **AUDIT HOLD / WAIT EXTERNAL RE-REVIEW**，不提交、不推送、不进入 M10。M08 仍为 `WAIT EXTERNAL`，即使本轮外审复核通过也必须先同步其依赖状态；M05 `AUDIT HOLD`、production v4 RX/TX/FSM、Authority、Flash/掉电与实机结论继续禁止。

### OP-337｜M09 最终自审纠偏、跨 Profile/工具链复验与外审材料收敛

- 自审纠偏：跨项核对 Target FSM 和旧 Delta 语义后没有沿用首版结论，发现并修正四项 M09 合同偏差：（1）`SYNC_BEGIN` 必须为 sequence `0`、Member 必须为 `1..N`、END 必须为 `N+1`；（2）已提交 mirror 的受保护 voter `SUSPECT` 才使用 grace，而 Core 已确认 `REMOVED` 必须即时永久取消该 assignment 的 future takeover eligibility；（3）Strict Backup profile 补齐 Runtime/member eligibility、Head-capable、Core admission、cooldown/blacklist、score 条件，并固定排序为 `head_score DESC, node_id ASC`；（4）Delta 只能更新 committed Snapshot 已证明 member 的动态 freshness，禁止新增成员、篡改 static eligibility 或 nonce 回退。
- 构建合同：Windows GCC `-fanalyzer` 首次构建发现 score 上限仅在 Full 间接可见，Lite/Nano 缺少声明；Profile public header 现直接 include canonical Cluster score 定义。该修复不改变协议语义，但防止 Profile 裁剪构建隐藏宏依赖。
- 新增/更新回归：nonzero BEGIN、把最后 Member sequence 作为 END、空 Snapshot `END=0`、REMOVED immediate fence、unknown/static-mutation/nonce-rollback Delta，以及完整 candidate eligibility / score / tie-break 都进入 M09 定向测试；所有拒绝路径保持 committed/output 不写回。
- 最终验证：Windows GCC Debug Full/Lite/Nano 各 `16/16`，新建 Service OFF `37/37`，config-contract `19/19`，用户产品配置 `17/17`，Release `37/37`；Windows GCC 14.2 `-fanalyzer -Wall -Wextra -Werror` `37/37`；RflySim-20.04 WSL GCC 11.4 ASan/UBSan `37/37`。Release 64-node clean model 仍为 `converged_ms=8920`；impaired 输出 `m03_isolation=not_requested`，未被误记为隔离通过。`git diff --check` 通过（仅既有 CRLF 提示）。
- 隔离/资源：production `ucn_cluster.c`、旧 Backup handler 和 public Cluster header 无 M09 API 调用；v4 encoder 默认仍为 0，enable=1 只出现于既有测试 targets。Release object `.text`：mirror `5008 B`、sync `7728 B`、profile `704 B`，三个 object `.data/.bss=0`；这不是最终 MCU 链接 Flash/RAM/stack 数字。
- 结论/限制：M09 为 **SELF-AUDIT PASS / WAIT EXTERNAL（受限实验软件范围）**。已完成每项自审和全局自审，现交外部审；不提交、不推送。M05 `AUDIT HOLD`、默认 production v4 RX/TX/FSM、Authority、M10 Vote/Takeover/Certificate、Flash/掉电、MCU/实机结论均继续禁止。

### OP-336｜M09 09-01..11 跨项全量自审完成，等待外部审计

- 全局核对：按任务表逐项回看 09-01..11。双 mirror 只有 `SYNC_END` 的 proof 成功路径可 atomic swap；Snapshot/READY/Delta 全部绑定 exact `BackupEpoch + snapshot_id + config_id/phase/hash`；staging、coverage grace、serial exhaustion、Strict profile 与 failure matrix 都不提供 M10 Vote/Authority/Takeover 行为。
- 隔离核对：扫描 production `src/extended/ucn_cluster.c`、旧 `src/extended/cluster/ucn_cluster_backup.c` 和 public `include/ucn/ucn_cluster.h`，没有 `backup_mirror`/`backup_sync`/`backup_profile` API 调用；新 modules 作为 caller-owned library value model，不嵌入 `ucn_cluster_t`。v4 encoder 宏仍只出现于既有 codec/host/release-gate 测试 targets；没有改变默认 0、production RX/TX/FSM 或 Authority。
- 全量验证：Windows GCC Debug Full/Lite/Nano/Service OFF 各 `16/16`；Windows GCC Release（含 64/256/1000 simulation 与 scale）`37/37`；产品 user-config `17/17`；WSL ASan/UBSan `16/16`；WSL GCC `-fanalyzer` `16/16`。Release simulator 64 clean 收敛 `8920 ms`，64 impaired 的 M03 isolation=passed；均为 Host 软件证据。
- 资源取证：Release object 的 `.text` 为 mirror `5008 B`、sync owner `6576 B`、profile `416 B`，三个 object `.data/.bss=0`；静态 archive 输入并不等于最终 MCU linked Flash。由于 owner 未嵌入 `ucn_cluster_t`，本次 Release simulator 的 `object_bytes=1584`；仍需未来真实目标链接/MCU 实测确认。
- 结论/限制：M09 为 **SELF-AUDIT PASS / WAIT EXTERNAL（受限实验软件范围）**。请进行外部审；此状态不解除 M05 `AUDIT HOLD`，不授权进入 M10、production v4 RX/TX/FSM、Authority、M10 Takeover、Flash/掉电或 MCU/实机完成声明。本轮不提交、不推送。详见 `UCN_V5_Cluster_M09_全量自审报告_2026-08-23.md`。

### OP-335｜CLV2-09-04..11 严格同步、Coverage、无回绕、Profile 与故障矩阵分项自审

- 09-04：`SYNC_MEMBER` 强制 assigned Head source、exact active staging SnapshotEpoch/Config、连续 sequence、unique Node ID、有效 nonzero member nonce；采用逐字段（非 C padding）FNV-1a hash。`SYNC_END` 必须同时匹配 final sequence/count/hash，并通过 Stable/Joint voter coverage，才以 candidate mirror 原子 swap；任一失败保留 committed，可本地 abort staging。
- 09-05/06：新增纯 READY verifier，精确绑定 Backup source、完整 SnapshotEpoch、final sequence/hash/Config；Delta 同时绑定 exact committed Epoch/Config/prior hash，只有连续 sequence 可替换 committed table。stale 不写回、gap 仅置 `resync_required` 并保留 committed，refresh staging 期间拒绝旧 Delta。
- 09-07/08：Coverage 固定为 canonical Core peer view；首次 commit 要求 Active Stable/Joint 的 old/new protected voter sets 全为 `ADMITTED`，non-voter/provisional legacy 明确不计。后续 `SUSPECT/REMOVED` 使用 `ucn_deadline_from_now/expired` 启动 grace；恢复清 grace，过期后永久 `takeover_ineligible`，等待新 assignment。
- 09-09/10：snapshot 到 serial rotation boundary 前拒绝；`next_generation()` 只递增、重新从 snapshot 1 full sync；generation boundary 返回 `UCN_ERR_EXHAUSTED` 以等待 M13 Rekey。Strict Backup profile 需要 v4、Backup/Joint/Persistence bits 和 mirror capacity，候选按最小 Node ID 确定性选择，所有 reject reason 可诊断。
- 09-11：加入 BEGIN、Member、END、READY、Delta、Config refresh 的 Primary failure matrix。测试证实刷新期间失败不会擦除 last committed；Config refresh 使用新的 owner atomic reset，故不保留旧 Config mirror，也不给 Takeover 资格。
- 验证：新增 `ucn_cluster_backup_sync_tests`、`ucn_cluster_backup_profile_tests`；Full/Lite/Nano/Service OFF 均完成全量 CTest `16/16`。源码扫描确认 production `ucn_cluster.c`、旧 `ucn_cluster_backup.c` 和 public `ucn_cluster.h` 无 mirror/sync/profile API 调用；`git diff --check` 通过（仅既有 CRLF 提示）。
- 结论/限制：09-04..11 均为 **CODE COMPLETE / SELF-AUDIT PASS（受限模型）**。M09 尚须做跨项全局自审（ASan/UBSan、`-fanalyzer`、资源与隔离复核）后才送外部审；M05 `AUDIT HOLD`、M08 外审等待、production v4 RX/TX/FSM、Authority、M10 Takeover、Flash/掉电和实机结论不放行，不提交、不推送。

### OP-334｜CLV2-09-03 strict SYNC_BEGIN owner 与分项自审

- 修改：新增独立 `ucn_cluster_backup_sync_owner`。该 owner 固定保存 assigned BackupEpoch 与 canonical Active Config；只接受 assigned Head 作为 source，并要求 incoming SnapshotEpoch 与 assigned epoch、Active Config 的 `config_id + phase + hash` 完全一致，才调用 mirror begin。
- 安全边界：begin 只开/清 staging，测试以已有 committed snapshot 模拟刷新中 Primary 故障，确认 committed member table 和 Epoch 全程不变。wrong source 返回 `UCN_ERR_ACCESS`，wrong BackupEpoch/Config ref 返回 `UCN_ERR_REPLAY`，active staging 覆盖返回 `UCN_ERR_STATE`，均逐字节 no-write。`owner_init()` 不读取旧 storage（允许新栈对象初始化）；显式 reassignment 采用候选对象原子替换，旧 committed/staging local mirror 必被清空，不可跨 BackupEpoch 继承。
- 自审：实现初版曾尝试检查 owner 是否全零来拒绝重 init；这会读取调用方新栈对象的未初始化 storage，不满足安全初始化合同。已改为“init 不读取 prior owner，成功时明确 reset/replace”，并增加 reassignment 清空 mirror 回归。
- 验证：新增 `ucn_cluster_backup_sync_tests`；Full/Lite/Nano/Service OFF 全量 CTest 各 `15/15` 通过，定向 mirror/sync CTest 通过；`git diff --check` 通过（仅既有 CRLF 提示）。扫描确认 production `ucn_cluster.c` 与旧 `ucn_cluster_backup.c` 未调用 mirror/sync API。
- 结论/限制：09-03 为 **CODE COMPLETE / SELF-AUDIT PASS（受限 sync owner）**。下项 09-04 才实现 member/end 的 proof 和 atomic swap；M05 `AUDIT HOLD`、M08 外审等待、production v4 RX/TX/FSM、Authority、Takeover Commit 与实机结论未放行，不提交、不推送。

### OP-333｜CLV2-09-02 SnapshotEpoch/Config binding 与分项自审

- 修改：在 M09 mirror value-model 中新增 exact `BackupEpoch={cluster_id,term,head_node_id,backup_node_id,backup_generation}` 和 `SnapshotEpoch={BackupEpoch,snapshot_id,config_id,config_phase,config_hash}`。所有 serial 均拒绝 0 和 rotation threshold 以外的值；Cluster/Node ID 拒绝 0/broadcast，Head 与 Backup 不得相同。Snapshot 的 Config ref 必须由 canonical `ucn_cluster_config_state_t` 和其 deterministic hash 派生。
- 事务边界：`begin_staging()` 现在必须携带有效 SnapshotEpoch；已存在 committed snapshot 时，只允许 exact 相同 BackupEpoch 下更大的 `snapshot_id`，旧或相同 ID 返回 `UCN_ERR_REPLAY`，不同 BackupEpoch 返回 `UCN_ERR_STATE`，要求未来 assignment owner 显式 reset/reassign，避免无证明跨 epoch 覆盖。active staging 不可被新 BEGIN 覆盖；abort 同时清 table 与 epoch metadata。
- 自审修正：首轮测试暴露“设置 `committed_valid=true` 却未设置 committed Epoch”的旧 fixture 已不再是合法模型；已将其修为 canonical Config 派生 Epoch，并将此类残缺状态固定为 `is_valid=false`。未增加 commit/swap/Delta/READY 或 production handler 接线。
- 验证：M09 mirror 定向测试 Full/Lite/Nano/Service OFF 均通过；重新全量构建后 CTest Full/Lite/Nano/Service OFF 各 `14/14` 通过；`git diff --check` 通过（仅既有 CRLF 提示）。源码扫描确认旧 `ucn_cluster.c` 与 `ucn_cluster_backup.c` 尚无 `ucn_cluster_backup_mirror` 调用。
- 结论/限制：09-02 为 **CODE COMPLETE / SELF-AUDIT PASS（受限 value-model）**。下项 09-03 只实现严格 SYNC_BEGIN owner；M05 `AUDIT HOLD`、M08 外审等待、production v4 RX/TX/FSM、Authority、Takeover Commit 和实机结论均未放行，不提交、不推送。

### OP-332｜CLV2-09-01 Backup 双缓冲状态模型与分项自审

- 修改：新增 `ucn_cluster_backup_mirror` 独立模块，含固定容量 `committed_members`、`staging_members`、`committed_valid`、`staging_active`、canonical reset/validity、staging begin/abort 与受限访问器；模型不提供未经 SnapshotEpoch/Config/Coverage proof 的 commit/swap API。
- RAM 设计：新增 `ucn_cluster_member_role_storage_t` union，Head/Member 仅表达一个 Runtime table，Backup 表达 committed+staging pair；该类型的大小严格等于 Backup pair，避免设计为 Runtime table 再额外叠加双 mirror。当前未把 union 直接替换进 `ucn_cluster_t`，因为旧 v3 handler 在 M06 已被 authority fence 拒绝、而 09-02..04 的 strict snapshot owner 尚未完成；提前切换会产生无证明的控制语义变化。
- 测试：新增 `ucn_cluster_backup_mirror_tests`，覆盖 reset 全字节 canonical、begin/abort 只清 staging 且 committed 逐字节不变、invalid storage fail-closed/no-write、Role union 尺寸合同。Full/Lite 各 `38/38`、Nano `28/28`、Service OFF `14/14` 均通过。
- 结论/限制：09-01 为 **CODE COMPLETE / SELF-AUDIT PASS（受限 value-model）**。下一项为 09-02 SnapshotEpoch + Config binding；M05 **AUDIT HOLD**、M08 外审等待、production v4 RX/TX/FSM、Authority、Takeover Commit 与实机结论均未放行，不提交、不推送。

### OP-331｜M09 启动：Backup 双缓冲受限实验实施计划与 09-01 建模

- 前置：用户明确授权继续 M09。当前 M08 为 **SELF-AUDIT PASS / WAIT EXTERNAL**，M05 顶层仍为 **AUDIT HOLD**；因此 M09 先只实现可独立验证的 Backup mirror 数据模型和 production-archive 单元测试，不把 v4 frame、Authority、Takeover Commit 或默认 encoder 接入产品路径。
- 现场基线：旧 v3 Backup 复用 `primary_members` 作为唯一 mirror，`SYNC_BEGIN` 会清空它；`membership_sequence` 被跨 snapshot 重用，`BACKUP_READY` 只绑定 generation/sequence，无法表达 Target 设计要求的 `SnapshotEpoch = BackupEpoch + snapshot_id` 和 committed/staging 原子交换。
- 计划：09-01 先定义 role-state union 中的 committed/staging mirror 和 canonical reset/validity；09-02 再补 SnapshotEpoch/Config binding；09-03..06 才接入严格同步、READY 和 Delta；09-07..11 完成 coverage/grace、无回绕、能力和故障矩阵。每项执行定向测试和自审，M09 完成后再作全量自审并送外部审。
- 结论/限制：本条只启动计划与 09-01，不构成 M08 外审通过或 M05 放行；不提交、不推送。

### OP-330｜合并审计 MAJOR-A 整改：Release typed-offer 门禁与 M03/M06/M08 台账收敛

- 审计核实：针对提交 `5a237a0` 的合并审计中，M05 MAJOR-A（Release `-O1..-O3` 下 typed `wire_offer` / `selected_wire_offer` 的 ABI padding `memcmp` 不确定）属实，优先整改；M06 的 v3 10 s 清扫、无 Backup/failover 是当前明确的受限产品姿态；M08 的 Authority Fence 不取代 M03 `TERM_CONFLICT_WAIT`，两层必须并存。
- 代码与测试：`from_word()` 和 `negotiate()` 的成功路径改为“零化局部对象 → 填充 → 完整复制”，输入不合法时仍完全不写回。测试以 `0xA5` 覆盖 caller 结构体并对完整对象 `memcmp`，锁住 padding。新增默认关闭的 `UCN_ENABLE_WIRE_V4_RELEASE_GATES`；仅在独立 test-only target 产生 GCC O1/O2/O3、MSVC O1/O2 codec gate，生产库和默认 encoder 配置不改变。
- M03 补证与修正：新增 Candidate 持有同簇较高 Term 时静默拒绝旧 Head offer、远端更高 Term 时以 `HIGHER_AUTHORITY` 进入 `JOIN_PENDING` 的真实 RX 回归；新增 `STEPPING_DOWN → TERM_CONFLICT_WAIT` 的真实 RX 回归。实现同时保证 Candidate 不会在角色本地 Join 路径误接纳同簇旧 Term。
- 台账：任务表明确 M06 v3 keepalive 不得续期 provisional deadline，M08 Fence（数据面即时撤权）与 `TERM_CONFLICT_WAIT`（控制面安全等待）为双保险；`.h743cmp/` 已加入仓库忽略。OP-278 是 05-09 P1 的整改过程记录，OP-279 是其后的最终外审 GO，二者无状态冲突。OP-249 的 Record SHA256 是当时 M04 基线，当前 `ucn_cluster_persist.c` SHA256 为 `3AAB4412EC78848DDA028239CB004672845BBD03D4E1E6341656F7B4130742FF`，本条作为后续比较基线。M06-01 维持 2026-08-22 的受限软件签字，未扩大为产品/实机签字。
- 验证：GCC Release gate O1/O2/O3 `3/3`；MSVC Release gate O1/O2 `2/2`；Windows GCC Full/Lite 各 `37/37`、Nano `27/27`、Service OFF `13/13`、MSVC Debug `24/24`；WSL ASan/UBSan `27/27`、WSL `-fanalyzer -Werror` `16/16`。`git diff --check` 无空白错误，仅既有 CRLF 提示。
- 结论/限制：MAJOR-A 为 **CODE COMPLETE / SELF-AUDIT PASS / WAIT EXTERNAL**。M05 顶层继续 **AUDIT HOLD**：没有放开 production v4 RX/TX/FSM、Authority、默认 encoder、Flash/掉电或 MCU 实机结论；本条也不进入 M09、不提交、不推送。

### OP-329｜M08 外审 P0 R31/R32 整改：当前时刻预检与原子 Config 撤权

- 审计核实：R31（Authority 只在旧 `cluster_step()` 更新，RX/Federation/TX 可能读到过期 active）与 R32（切到无 quorum Stable/Joint Config 后残留 active）均属实；M08 恢复为 **AUDIT HOLD**，不进入 M09。
- 修改：新增 `ucn_cluster_authority_runtime_preflight(now_ms)`，并前置到 Cluster TX token 前、RX 的 Head member handler 前、Federation step/query/public handover。`install_config` 增加 `now_ms`，先在候选 Config 上重算 Stable/Joint quorum，再原子撤销旧 authority；无 quorum 即刻进入 Grace。RX Head gate 对非 active 的 managed Owner 拒绝 Join/Keepalive/Leave 与 Head-consumed Backup 控制帧，避免 member/lease/token 副作用。
- 测试：新增/强化“过期先 Cluster TX、过期先 JOIN_REQUEST、过期先 Federation handover/locator、active→无 quorum Joint、active→无 quorum Stable”回归。Windows GCC Full `37/37`、MSVC Debug Full `24/24`、Lite `37/37`、Nano `27/27`、Service OFF `13/13`、WSL ASan/UBSan `27/27`、WSL `-fanalyzer -Werror` `16/16` 均通过；`git diff --check` 无空白错误（仅既有 CRLF 提示）。
- 结论/限制：R31/R32 为 **CODE COMPLETE / SELF-AUDIT PASS**，M08 为 **SELF-AUDIT PASS / WAIT EXTERNAL**；仍保持 M05 顶层 **AUDIT HOLD**，不提交、不推送、不进入 M09，不宣布 v4 production、Authority、Flash/掉电或实机已完成。完整证据见 `UCN_V5_Cluster_M08_R31-R32_整改自审报告_2026-08-23.md`。

### OP-328｜M08 08-01..12 完成：Authority/Quorum/Fence 分项与全量自审

- 操作：在 M07 受限外部 GO 的边界内连续完成 M08。新增 caller-owned `ucn_cluster_authority` Owner（公共契约要求其存储期覆盖已绑定 Cluster 的 step/TX 生命周期），追加 Authority phase/state、canonical Config quorum、same-step revoke、Grace restore hold、permanent Fence、timer algebra 与 partition property；并将 Core Cluster TX 和 Federation Directory 的受控实验路径接入统一 Authority gate。
- 安全收紧：`authority_active` 的唯一正向来源为显式安装的 canonical Config + voter keepalive Owner；未安装 Owner、`role==HEAD`、Neighbor `SUSPECT`、v3 高 Term 都不能授予 Authority。v3 高 Term 仅触发保守 Fence；只有未来 M10/M11 验证的 Stable Authority 才能选择 `JOIN_PENDING`。Grace/Fenced 不消耗 Cluster control token、不续租/注册/撤销 Directory locator。
- 测试：新增独立 `ucn_cluster_authority_tests`，覆盖 Stable/Joint quorum、同 step revoke、TX matrix、restore hold、grace/conflict/higher/fault/owner-step Fence、timer/profile no-write、Member grace 公式及 3/4/5/6 全 mask partition；Federation production-archive fixture 覆盖旧 Head 失权后无 Directory 写。Windows MSVC Full Debug `24/24`、Windows GCC Lite `37/37`、Nano `27/27`、Service OFF `13/13`、WSL ASan/UBSan `27/27`、WSL `-fanalyzer -Werror` `16/16` 全部通过；`git diff --check` 无空白错误（仅既有 CRLF 提示）。
- 自审结论/限制：M08 为 **SELF-AUDIT PASS / WAIT EXTERNAL**，未提交、未推送。M05 顶层 **AUDIT HOLD** 不解除：默认产品不启用 v4 encoder、production v4 RX/TX/FSM 或 v4 Authority；当前受控 Owner 只接受 `VOLATILE_TEST`，没有真实 Flash、掉电、MCU、无线/串口实机或生产持久化签字。完整证据见 `UCN_V5_Cluster_M08_分项与全量自审报告_2026-08-23.md`。

### OP-327｜M08 启动：Authority / Quorum / Grace / Fence 受控实施计划

- 操作：在 M07 外部复审 GO 后，按用户授权启动 M08；先复核 Current FSM、M07 Config/Joint value model、现有 `active_voter_set`、Cluster TX 与 Federation Directory 入口，建立 M08 专用实施计划。
- 修改：任务表将 M08 置为 `IN_PROGRESS`，新增 `UCN_V5_Cluster_M08_AuthorityFence_实施计划_2026-08-23.md`。计划明确 `authority_active => canonical Config quorum`、同 Step 撤权、Grace 无写权限、Fence 同 Term 不可恢复，以及逐项自审后再全量自审的节奏。
- 结论/限制：M05 顶层 **AUDIT HOLD** 继续有效；默认产品不启用 v4 encoder、production v4 RX/TX/FSM 或 v4 Authority。M08 只允许明确安装 canonical Config 的受控实验实例验证集成；无 Config/lease 证明的 REQUIRED Cluster 不得获得 Authority。此条仅为计划与状态更新，无业务代码改动、无提交、无推送。

### OP-326｜M07 外部复审 GO：受限实验 Config / Joint / Persistence 软件范围结案

- 外部结论：复审确认 R25/R26 的最终旁路已关闭，R24/R27/R28 保持通过，未发现新的 P0/P1；M07 可标记为 **DONE / 外部复审 GO（受限实验软件范围）**。
- 外部证据：R29 在持久化 Commit 和 Runtime Commit 前精确验证 `txid + C_new + bound Backup + ACK source`；R30 的对象外 callback scope 使首次 init 不读取未初始化字段，递归 init 在任何 owner 清零/写入前返回 `UCN_ERR_STATE`。外部独立复跑 Windows Full `33/33`、Service-OFF `12/12`、WSL Lite `33/33`、Nano `23/23`；Config 定向 ASan/UBSan、`-fanalyzer` 各 `3/3` 通过；`git diff --check` 仅 CRLF 提示。
- 状态同步：任务表、R24–R30 第三次自审报告均已改为外部 GO；本条仅更新文档，无业务代码改动、无提交、无推送。
- 结论/限制：M05 顶层仍为 **AUDIT HOLD**。本签字不授权生产 v4 RX/TX/FSM、Authority、默认 encoder 或实机/Flash/掉电结论；M08 仍保持 TODO，须在后续任务授权与审计后才可开始。

### OP-325｜M07 外审 R25/R26 旁路整改：精确 Backup gate 与 init callback scope

- 操作：核实外审结论：R24/R27/R28 保持通过；R25 的 Backup gate 只检查布尔状态，不能证明当前 Commit 的 tx/C_new/Backup；R26 的 `owner_init()` 在 Provider `load()` 回调中可递归清零自身，不能只依赖对象内 `io_active`。
- 修改：为 gate 增加 ACK source identity，并新增 `commit_allowed_for_tx()`；`begin_commit()` 与 runtime apply 均使用 exact `txid + C_new ref + bound Backup + ACK source` 许可，旧无上下文 helper 降为诊断。Provider init/load/submit/poll 统一通过模块私有 callback 动态范围门；该门不读取可能未初始化的 owner 字段，任何嵌套 Provider I/O 都返回 `UCN_ERR_STATE`。
- 测试：新增旧 ACK transaction、相同 txid 不同 C_new、错误 ACK Backup 的零-submit/无 durable-state 改变回归；runtime apply 错绑 gate 无 runtime/member 写回；fake Provider 在 load/submit/poll 内递归 init/Prepare/Joint/Commit/Abort/Poll，所有结果均为 `UCN_ERR_STATE`，递归 init 的 owner 前后逐字节一致。
- 验证：Windows GCC Full `12/12`、Config contract `15/15`、Full + Cluster simulator `23/23`、Lite `12/12`、Nano `12/12`、Service OFF `12/12`、产品配置 `13/13` 全部通过；WSL ASan/UBSan `12/12`、WSL `-fanalyzer -Werror` `12/12` 通过；`git diff --check` 通过，仅既有 CRLF 提示。源码复扫确认 production Cluster/Adapter 没有 Config 或 v4 API 引用，encoder enable 仍只在两个测试 target。
- 结论/限制：R29/R30 为 **CODE COMPLETE / SELF-AUDIT PASS**；M07 仍为 **AUDIT HOLD**，不提交、不进入 M08，不放开 M05 production v4 RX/TX/FSM、Authority、Adapter、Flash/掉电或实机声明；新的全量自审见 `UCN_V5_Cluster_M07_R24-R30_三次全量自审报告_2026-08-23.md`。

### OP-324｜M07 外审 P0/P1 整改 R24–R28 与二次自审

- 操作：接受 M07 外审 `NO-GO`，将里程碑恢复为 **AUDIT HOLD**；修复 Joint 绕过、Backup ACK 后置、Config Owner Provider I/O 重入、deadline 回绕和 Abort 缺少 C_new 绑定。
- 修改：新增持久化 `CONFIG_JOINT` journal operation；`begin_joint()` 必须在 runtime enter 前完成 `submit → load + exact proof`，`begin_commit()` 同时要求 exact durable Joint runtime 和 Backup pre-submit gate。Config Owner 在 `load/submit/poll` 回调前置 `io_active` 并在异步 completion 的 reload proof 前保持 pending。Config Tx 改用公共 modular deadline helper；终态 Config journal 继续保留 immutable C_new ref，使 Abort 的首次及重启 replay 同时校验 C_old/C_new/txid。
- 验证：新增/更新定向反例：双 ACK 无 Joint、重启仅 PREPARED、Backup 缺失/未 stage/未 ACK、load/submit/poll 同步递归五入口、`UINT32_MAX` 回绕、同 txid 不同 C_new Abort。Windows GCC Full `12/12`、带 Cluster simulator Full `23/23`、Lite `12/12`、Nano `12/12`、Service OFF `12/12`；WSL ASan/UBSan `12/12`、WSL `-fanalyzer -Werror` `12/12` 通过。
- 结论/限制：R24–R28 为 **CODE COMPLETE / SELF-AUDIT PASS**；M07 仍是 **AUDIT HOLD**，须进行新的 M07 全量自审与外部复审。默认产品仍未接入 v4 Config RX/TX/FSM、Authority、Adapter 或默认 v4 encoder；未进行真实 Flash、掉电、MCU 或互通测试。

### OP-323｜M07 07-00..12 全量自审完成，等待外部审计

- 操作：在 07-12 分项自审后，执行 M07 横向复核：voter/Commit 边界、Record v1/v2 provenance、Config body 双槽、M05 production 隔离、legacy bridge 删除及全构建矩阵。
- 验证：Windows GCC Full/Lite/Nano 各 `12/12`，Service OFF `12/12`，config contract `15/15`，user product header `13/13`，开启 production-archive simulator 的 Full `23/23`，WSL ASan/UBSan `12/12`，WSL `-fanalyzer` `12/12`；`git diff --check` 无空白错误（仅既有 CRLF 提示）。source scan 确认 `UCN_CLUSTER_LEGACY_V3_TEST_BRIDGE` 无匹配，production `ucn_cluster.c` 无 v4 codec/Config owner 引用，encoder enable 仅两个测试 target。
- 结论/限制：M07 标记为 **SELF-AUDIT PASS / 等待外部审计**；完整证据见 `UCN_V5_Cluster_M07_全量自审报告_2026-08-23.md`。绝不据此宣布 production ready 或进入 M08：真实 Flash/掉电/MCU、v4 Config wire RX/TX/FSM、Authority、Backup mirror、Takeover certificate 均未完成，M05 顶层 **AUDIT HOLD** 不变。

### OP-322｜CLV2-07-12 删除 M06 legacy auto-commit bridge 与分项自审

- 操作：完成 M07 最后一项，删除 `UCN_CLUSTER_LEGACY_V3_TEST_BRIDGE` 和 simulator 对 Cluster/membership 的重编译 override；simulator 改为直接链接 production archive。
- 修改：新增 public production-RX `JOIN_REQUEST` 回归，断言 v3 入网只生成 `PROVISIONAL/non-voting/v3` member，有 deadline、非 protected voter、非 Backup、未进入 active voter set；移除依赖已围栏 v3 Backup/Takeover 的 `head-failover` CTest 成功断言，M10 将以 frozen-Config v4 certificate 场景替代。
- 验证：bridge 名称 grep 无匹配；`handle_join_accept()` 范围无 voting 写入。启用 simulator 的 Windows GCC Full 重建后 CTest `23/23`（64/256/1000 clean+impaired、mobility、score-shift 和 group=2/4/8 isolation）通过；旧 head-failover 单独运行不收敛，符合 v3 authority fail-closed 的预期。
- 结论/限制：`CLV2-07-12` 为 **CODE COMPLETE / SELF-AUDIT PASS**；07-00..12 全部完成，进入 M07 全量自审。保留的 M01 test hook 仅在历史 Current-FSM fixture binary 内，非 public/product 开关、非 M07 Config target、非 simulator；没有启用 v4 production RX/TX/FSM、Authority、Adapter 或默认 encoder，M05 顶层 **AUDIT HOLD** 不变。

### OP-321｜CLV2-07-11 Config Body 双槽恢复与分项自审

- 操作：实现 caller-owned 的 CRC32 双槽 Config body 格式，并让实验 Config persistence owner 在 Prepare 前写 C_new body、在每次 reload/restart 时按 M04 ref 重新验证。
- 修改：新增 canonical deserialize、`ucn_cluster_config_store` 和定向测试；M04 `committed_config` 唯一选 Active body，PREPARED 时 C_old Active/C_new 仅 staged，缺 body 或 torn record fail-closed。
- 验证：proposing、prepared、Commit 持久化且尚未任何 v4 broadcast、local apply 后 restart、staged torn、fallback 和独立 reader replacement 均覆盖；owner 在破坏 staged body 后 restart 返回 `UCN_ERR_STATE` 且 output 不写回。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `12/12`。
- 结论/限制：`CLV2-07-11` 为 **CODE COMPLETE / SELF-AUDIT PASS**，进入 07-12。双槽只完成 Host/软件 CRC 与撕裂模型，真实 Flash 原子性、掉电波形、MCU 存储寿命及 M09 Backup mirror 均未宣称完成；无 production v4 RX/TX/FSM、Authority、Adapter 或默认 encoder 接线，M05 顶层 **AUDIT HOLD** 不变。

### OP-320｜CLV2-07-10 Config ID 无回绕门禁与分项自审

- 操作：为 Stable Config 增加显式 `rekey_required` hand-off；M13 未实现时禁止在同一 Cluster identity 下越过 serial boundary 创建新的 Joint Config。
- 修改：Config ID 合法域保持 checked serial；boundary Stable 仍可读，但 Addition/Removal planner 都返回 `UCN_ERR_EXHAUSTED`，transaction 保持无写回。
- 验证：`threshold-1 -> threshold` 最后一跳合法，promote 后必须 `rekey_required`；threshold 上 Joint 创建、ADD、REMOVE 均确定性拒绝，无 `MAX -> 1`。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `11/11`。
- 结论/限制：`CLV2-07-10` 为 **CODE COMPLETE / SELF-AUDIT PASS**，进入 07-11。没有实际执行 M13 Rekey，也没有改变 persistent cluster identity、Authority、production v4 RX/TX/FSM、Adapter 或默认 encoder；M05 顶层 **AUDIT HOLD** 不变。

### OP-319｜CLV2-07-09 Durable Commit/Abort 与分项自审

- 操作：完成实验 Config/Joint runtime 的 Commit/Abort terminal transition，并新增 M04 `CONFIG_ABORT` durable operation；超时不再只清 RAM。
- 修改：Prepare/staging/Commit/Backup/Joint 的 `C_new` identity 统一为 `Stable(C_new)`；修正初版把 Joint envelope 摘要作为 committed Config 所导致的 Commit 后 ref 不一致。ADD Commit 才改为 `COMMITTED/voting`，REMOVE Commit 才清 slot；Abort 保留 C_old、ADD provisional、REMOVE committed/voting。精确 terminal journal 支持重复 Commit/Abort 无二次 Provider submit、无 Runtime/member 写回。
- 验证：ADD/REMOVE Commit/Abort、重复 terminal replay、超时 Async `PENDING -> poll -> load proof` 与 stable-reference 一致性均已测试。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `11/11`。
- 结论/限制：`CLV2-07-09` 为 **CODE COMPLETE / SELF-AUDIT PASS**，进入 07-10。仅实验 Config/Joint/Persistence module 改动；无 production v4 RX/TX/FSM、Authority、Takeover、Adapter 或默认 encoder 接线。完整 Config body 双槽/掉电恢复仍归 07-11，M05 顶层 **AUDIT HOLD** 不变。

### OP-318｜CLV2-07-08 Durable Joint Identity Gate 与分项自审

- 操作：新增实验 Joint runtime gate。只有 C_old committed ref、C_new PREPARED ref、同 txid 和 old/new 双 quorum 同时成立时，才把 caller-owned active Config 从 Stable 切到 Joint。
- 修改：新增 `include/ucn/ucn_cluster_config_joint.h`、`src/extended/cluster/ucn_cluster_config_joint.c` 与独立测试；进入成功后 tx 标记为 `JOINT_DURABLE`。
- 验证：无 quorum 或缺 Prepare durable proof 时 runtime 完整无写回；完成 Prepare、ACK old/new quorum 后才进入 exact proposed Joint。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `11/11`。
- 结论/限制：`CLV2-07-08` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-09。Record 当前持久的是 C_old/C_new identity pair，不是完整 Config body slot；完整 body 双槽、重启/撕裂写恢复仍明确归 07-11。没有 Authority、Takeover、production RX/TX/FSM 或 Backup mirror 接线，M05 顶层 **AUDIT HOLD** 不变。

### OP-317｜CLV2-07-07 Backup Config Staging Gate 与分项自审

- 操作：新增 Config 的 HA Backup staging gate。它只绑定既有 v4 committed voter 的 Backup identity，不分配生产 Backup、不同步 member table、更不开放 Takeover。
- 修改：新增 `include/ucn/ucn_cluster_config_backup.h`、`src/extended/cluster/ucn_cluster_config_backup.c` 和独立 archive 测试。
- 验证：无 Backup 且 policy 允许时只产生 `commit_allowed=true/ha_ready=false`；policy 要求 Backup 时 fail-closed。已绑定 Backup 必须对 exact C_new digest 与 txid ACK，错 source/digest 无写回；exact ACK 后才 `ha_ready=true`。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `10/10`。
- 结论/限制：`CLV2-07-07` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-08。该 gate 未写 Backup mirror/Provider、未发送 ACK/Commit、未改产品 Backup FSM 或 Authority；M05 顶层 **AUDIT HOLD** 不变。

### OP-316｜CLV2-07-06 实验 Config 持久化 Owner 与分项自审

- 操作：新增独立 Config persistence owner，用 M04 Provider 的 `submit → completion → load + exact journal` 证明封装 Config Prepare/Commit；它不接入 `ucn_cluster_t`、产品 RX/TX 或默认 FSM。
- 修改：新增 `include/ucn/ucn_cluster_config_persistence.h`、`src/extended/cluster/ucn_cluster_config_persistence.c` 与 fake-provider production archive 测试；Config State 的 canonical 序列生成固定 16 B identity digest（不是安全 MAC）。
- 验证：Prepare durable 后才返回 `durable=true`；Commit 必须同时匹配 staging transaction 和 old/new Joint quorum。同步成功、PENDING 两次 poll 后 reload proof、submit 失败无 durable state 改动、无 quorum 时不 submit 均被覆盖。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `9/9`。
- 结论/限制：`CLV2-07-06` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-07。该模块仅给明确调用者提供安全的实验集成门；不会自行 ACK/Commit/广播、不会把 `VOLATILE_TEST` 当成持久化、不会改变默认 v4 encoder/RX/TX/FSM/Authority 或 M05 顶层 **AUDIT HOLD**。

### OP-315｜CLV2-07-05 Joint Quorum 与单节点 Delta 收紧自审

- 操作：新增 pure Joint quorum helper，要求同一 transaction 的 C_old 与 C_new quorum 同时满足；同时自审发现通用 config_tx 未约束 proposal delta，遂收紧为 ADD/REMOVE 均只能差一个 proposal Node ID。
- 修改：新增 `include/ucn/ucn_cluster_config_quorum.h`、`src/extended/cluster/ucn_cluster_config_quorum.c` 和独立测试；更新 config_tx 的 active/begin 结构合法域与无写回负例。
- 验证：Head self ACK、old 满/new 不满、new 满/old 不满及越界 bitmap 均不会错误形成 Joint quorum；替换多个 voter 或 proposal kind 与 delta 不一致均在 begin 前拒绝。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `8/8`。
- 结论/限制：`CLV2-07-05` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-06。helper 不发 Commit、不启用 Authority、不解析/签发证书，仍无 production v4 RX/TX/FSM 或 Adapter 接线；M05 顶层 **AUDIT HOLD** 不变。

### OP-314｜CLV2-07-04 Removal Proposal 与旧分母保留自审

- 操作：补齐 Config 删除的值层契约：已提交 v4 voter 先显式标记为 `REMOVING`，之后才可生成 removal Joint Config；其 `voting` 和 C_old 身份在 Commit 前均不消失。
- 修改：扩展 `ucn_cluster_config_proposal` 的 `mark_removing()` 与 `begin_remove_marked()`；扩展同一 production archive 测试。
- 验证：C_old `{1,4,9}` 删除 node `9` 时生成 C_new `{1,4}`，node `9` 仍在 `old_set` 且 Runtime value 仍为 `REMOVING/voting=true`；未标记、删 Head 均 `UCN_ERR_ARGUMENT` 且 transaction 无写回。Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `7/7`。
- 结论/限制：`CLV2-07-04` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-05。没有在 production lease timeout/LEAVE RX 上接线，也没有提交 Config、修改 active voter set、发送 wire、改变 Authority 或调用 Provider；M05 顶层 **AUDIT HOLD** 不变。

### OP-313｜CLV2-07-03 Provisional Addition Planner 与分项自审

- 操作：新增无副作用 Addition planner，仅将已验证的 v4 `PROVISIONAL/non-voting` Runtime member 投影为 C_new 候选，并在唯一 `config_tx` 内记录已处于 C_old 的 Head self ACK。
- 修改：新增 `include/ucn/ucn_cluster_config_proposal.h`、`src/extended/cluster/ucn_cluster_config_proposal.c` 和 production archive 测试 `tests/test_cluster_config_proposal.c`；CMake/public-header 加入对应模块。
- 验证：Stable `{1,4,9}` 生成 Joint `{1,4,9,21}`，C_old/C_new config identity 和 Head bit 均正确；输入 member 完整 `memcmp` 不变，仍是 `PROVISIONAL/non-voting`。非法 Head、v3 candidate 与已满 voter capacity 均拒绝且 transaction 不写回。首次构建发现测试宏被补丁文本转义为双反斜杠，修正测试源后重新构建；Windows Full/Lite/Nano、WSL ASan/UBSan CTest 各 `7/7`。
- 结论/限制：`CLV2-07-03` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-04。未发送 `CONFIG_BEGIN/MEMBER`，未修改 Runtime member/voter set，未进入 production RX/TX/FSM、Authority、Adapter 或 Provider；M05 顶层 **AUDIT HOLD** 不变。

### OP-312｜CLV2-07-02 单实例 Config Transaction 与分项自审

- 操作：新增独立、固定容量的 `ucn_cluster_config_tx_t`，把一次 Config 事务所需的 ID、add/remove proposal、C_old/C_new ACK bitmap、deadline、retry 和 persistence stage 收敛为单一 canonical value object。
- 修改：新增 `include/ucn/ucn_cluster_config_tx.h`、`src/extended/cluster/ucn_cluster_config_tx.c` 与 production archive 测试 `tests/test_cluster_config_tx.c`；CMake 加入独立 CTest，public-header 覆盖新接口。
- 验证：Stable `{1,4,9}` 到 Joint `{1,4,9,21}` 的 begin/ACK 映射、同一时刻二次 begin 无写回拒绝、非 voter/重复 ACK 无写回、deadline 边界、retry 耗尽、persist-stage 及 bitmap 损坏均通过。Windows Full/Lite/Nano 与 WSL ASan/UBSan CTest 各 `6/6`；源码扫描确认该模块无 v4 RX/TX/FSM、Authority 或 Adapter 调用。
- 结论/限制：`CLV2-07-02` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-03。它没有发送 Config 帧、修改 Runtime member、安装 voter set、建立 quorum 或调用 persistence Provider；默认产品和 M05 顶层 **AUDIT HOLD** 不变。

### OP-311｜CLV2-07-01 Config State 值模型与分项自审

- 操作：实现独立的 `ucn_cluster_config_state_t`，固定表达 Stable 的同一 C_old/C_new 以及 Joint 的 C_old→checked-next C_new；提供纯 promote、hash 和固定 canonical serialization。
- 修改：新增 `include/ucn/ucn_cluster_config_state.h`、`src/extended/cluster/ucn_cluster_config_state.c`、production archive 独立测试 `tests/test_cluster_config_state.c`；CMake 加入模块与独立 CTest，public-header 也覆盖该接口。
- 验证：排序 canonical、Stable/Joint/promote、hash/serialization 确定性、阈值/no-write 与损坏 state 回归通过。Windows Full/Lite/Nano CTest 各 `5/5`，WSL ASan/UBSan `5/5`；新模块没有 v4 RX/TX/FSM/Authority/Adapter 调用。
- 结论/限制：`CLV2-07-01` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可连续进入 07-02。它只是 Config 值模型；没有 Config transaction、持久化、quorum 权威、member status 或 wire 行为，M05 顶层 **AUDIT HOLD** 不变。

### OP-310｜CLV2-07-00 Record v2 PREPARED 来源区分与分项自审

- 操作：完成 M07/M13 的先决阻断项。将 physical Record writer 从 schema v1 升级为 v2，decoder 继续只读兼容 v1/v2，并把读取 schema 保留为 Runtime provenance。
- 修改：`LEGACY_PREPARED_ABORT` 只接受 `v1 PREPARED → v2 no-transaction`；`cluster_persistence_begin_state()` 的每个正常 runtime write 强制 v2；controlled boot 仅迁移 v1 PREPARED，v2 PREPARED 在其 M07/M13 owner 尚未恢复时 fail-closed。
- 测试：新增 writer-v2 / writer 拒绝 v1、v1 fixture decode、v1 Config/Rekey 受控迁移、v2 Config PREPARED restart 无 abort 回归；Full/Lite/Nano CTest 各 `4/4`，WSL ASan/UBSan `4/4` 通过。
- 结论/限制：`CLV2-07-00` 为 **CODE COMPLETE / SELF-AUDIT PASS**，可进入 07-01；外审仍统一留待 M07 final。v2 Config PREPARED 的真正 Resume/Abort/Commit owner 尚未实现，v2 Rekey 仍由 M13 管理并 fail-closed；M05 顶层 **AUDIT HOLD** 不变。

## 2026-08-22

### OP-309｜M07 连续实施授权与 07-00 持久化来源门禁启动

- 操作：按用户确认，M07 改为 `07-00` 先决门禁通过后连续实施 `07-01..12`；每项完成即自审，全部完成后再统一全量自审并送外部审。
- 修改：新增 `docs/07-Cluster簇/02-里程碑/M07/UCN_V5_Cluster_M07_完整连续实施与自审计划_2026-08-22.md`；任务表将 M07 标为 `IN PROGRESS / 连续实施与分项自审`，并明确 M05 默认产品边界仅对 M07 开放 target-private 测试/实验集成。
- 07-00 决策：采用 Record schema v2 + v1/v2 provenance 区分。旧 v1 PREPARED 才可一次性走 R23 legacy abort；未来 v2 Config/Rekey PREPARED 必须进入所属事务的 Resume/Abort/Commit，绝不能被启动迁移清除。
- 结论/限制：尚未修改业务代码或开启任何 M07 wire/FSM。默认产品继续关闭 v4 encoder、production v4 RX/TX/FSM、Authority 与 Adapter 接线；`VOLATILE_TEST` 不构成掉电安全证明，M05 顶层继续 **AUDIT HOLD**。

### OP-308｜M06 外部复审 GO：R01 P0 闭环与受限软件范围签字

- 外部复审：确认 `CLV2-06-R01` 的 production RX fence 已在任何状态、计时、统计和 shadow 同步之前拒绝 v3 Type 8、10..15、18、19；每帧返回 `UCN_ERR_ACCESS` 且完整 `ucn_cluster_t` 保持不变。
- 隔离核验：生产 archive 回归直接链接 `ucn_cluster` 且未启用测试宏；`ucn_cluster_sim` 与 `ucn_tests` 的 legacy bridge 均为 target-private，未泄漏到产品库。
- 台账：将 M06 及 06-02..09、R01 同步为 **DONE / 外部复审 GO（受限软件范围）**；更新 M06 全量自审报告和 R01 整改报告的最终结论。
- 结论/限制：M06 不再受 R01 P0 阻塞，但 **M05 顶层继续 `AUDIT HOLD`**。本签字不授权 production v4 RX/TX/FSM、Authority、Adapter 接线、真实 Flash/掉电或实机互通/资源完成声明；未创建提交或推送。

### OP-298｜M06 完整连续实施与分项自审授权

- 操作：按用户指令，将 M06 从“06-02 单项外审等待”调整为“06-01..09 连续实施；每一小项自审；最终 M06 全量自审后统一外审”。
- 修改：新增 `docs/07-Cluster簇/02-里程碑/M06/UCN_V5_Cluster_M06_完整连续实施与自审计划_2026-08-22.md`；同步任务表的 M06 顶层与 06-02/06-03 状态。
- 验证：计划明确了 06-03..09 的逐项验收、最终矩阵，以及 M05 `AUDIT HOLD` 的禁止边界。
- 结论/限制：本授权不解除 M05 顶层冻结；任何 v4 production RX/TX/FSM、Authority、通用 Adapter 接线或产品 auto-commit 仍不属于 M06 允许范围。

### OP-299｜CLV2-06-03 有界 Voter Set 与分项自审

- 操作：新增独立于 Runtime member table 的 canonical `ucn_cluster_voter_set_t`；实现排序构建、hash、contains、多数派与 64-bit logical bitmap 查询。
- 修改：`include/ucn/ucn_cluster_membership.h`、`include/ucn/ucn_cluster.h`、`src/extended/cluster/ucn_cluster_membership.c`、membership/public-header 测试；新增 06-03 自审报告并更新任务表。
- 验证：Full 构建通过；`ucn_tests` 与 `ucn_cluster_membership_model_tests` 均通过。模型覆盖最大 `MAX_MEMBERS + 1` 的最高 bitmap 位、排序/hash、非法/重复输入无写回及 malformed set 拒绝；production v4/Adapter 扫描为空。
- 结论/限制：该集合尚未接入 legacy takeover、证书或 Authority，不能提前宣称 M10 quorum 已实现；06-03 外审将与 M06 全量结果统一进行。

### OP-300｜CLV2-06-04 v4 Provisional Admission 与分项自审

- 操作：新增 post-validation `cluster_admit_verified_v4_provisional_member()`，为未来已完成 wire 验证的 RX Owner 提供 only-provisional 的 Runtime member 写入语义。
- 修改：membership internal API/实现与模型测试；新增 06-04 自审报告并更新任务表。
- 验证：Full 构建通过；`ucn_tests` 与 membership model 通过。模型验证旧 voter set 不因新 v4 node 变化，且非 Head/非法 ID/满容量失败不写回；`ucn_cluster.c`、Adapter 均未出现 v4 生产接线。
- 结论/限制：当前并不接收或发送 v4 Join；没有 Config Commit、voter 变更、Authority 或持久化承诺。06-04 外审合并至 M06 final。

### OP-301｜CLV2-06-05 Provisional Deadline 与分项自审

- 操作：为 Runtime provisional member 新增独立的有界 deadline 与 Head-side cleanup；添加 default/fast profile 配置。
- 修改：config、member record、membership expiry、统计、模型测试；新增 06-05 自审报告并更新任务表。
- 验证：Full 构建及 `ucn_tests`/membership model 通过。验证 deadline 边界、容量回收复用、重复接纳不延期，以及 committed record 不会被 provisional cleanup 删除。
- 结论/限制：该清理不执行 Config Commit、不改 voter set，不替代 M07 transaction；M05 production v4 仍冻结。

### OP-302｜CLV2-06-06 legacy v3 收紧与分项自审

- 操作：将产品 v3 Join 收紧为 `PROVISIONAL/non-voting`；只允许 canonical `COMMITTED + voting + wire=v4` record 进入 Backup、takeover 与 Recovery protected-voter 过滤。
- 修改：membership、backup、recovery 内部模型与 production-archive model test；新增 06-06 自审报告并更正任务表中已被本项收紧的 06-01 过渡桥描述。
- 验证：Full 构建通过；`ucn_tests`（test-hook copy）与 membership model（production archive）通过。production model 证明 v3 有 candidate metadata 仍不能成为 Backup，canonical v4 committed voter 才可进入选择。
- 结论/限制：test hook 只是维持旧 Current-FSM 回归的临时桥，06-09 才完成其最终隔离门禁，M07-12 负责删除；当前没有 v4 wire/FSM/Authority 接线。

### OP-303｜CLV2-06-07 成员只读摘要与分项自审

- 操作：扩展 member summary 的 status/voting/config_id 诊断字段，并统一两个只读 summary API 的填充逻辑。
- 修改：public summary 值类型、`member_summary_fill()`、membership model test；新增 06-07 自审报告并更新任务表。
- 验证：Full 构建与全 4 个 CTest 通过。模型证明 provisional/active config 可见、caller 修改 summary 不写回 owner table，失败路径保持 output 不变。
- 结论/限制：`config_id` 仍是诊断投影，不表示 M07 config transaction 或可用 Authority；M05 边界未改变。

### OP-304｜CLV2-06-08 Runtime/Voter 容量语义与分项自审

- 操作：定义包含 Head 的独立 voter capacity、只读 capacity view、细化 admission reason，以及未来 M07 Config Commit 专用的无副作用 voter-capacity preflight。
- 修改：config 尾部字段、public capacity view、membership preflight/diagnostic、模型/public-header 测试；新增 06-08 自审报告并更新任务表。
- 验证：Full 构建与 `ucn_tests`/membership model 通过。模型覆盖 Runtime 满、Voter 满、重复 Join、voter capacity 放宽后仍不自动提交，以及 owner view 的 no-write 错误路径。
- 结论/限制：M06 不执行真正的 Config Commit 或 voter set 更新；M07/M10 权责未提前进入，M05 生产边界不变。

### OP-305｜CLV2-06-09 test-only auto-commit bridge 与分项自审

- 操作：把旧 v3 Current-FSM 回归固定到 `ucn_tests` 自编译的 test-hook membership 副本；新增桥分支的显式断言，同时保留 production archive 的 opposite-branch 模型测试。
- 修改：CMake target-private internal include、`test_cluster.c` bridge assertion、06-09 自审报告和任务表。
- 验证：重新配置/构建 Full 通过，CTest `4/4` 通过。`ucn_tests` 证明 test bridge committed/voting/v3；membership model 证明 production v3 provisional/non-voting；CMake 宏只出现在 `ucn_tests` private definition。
- 结论/限制：bridge 只能服务测试，不是产品开关；M07-12 必须删除。M06 代码项至此完成，转入全量自审，尚未进入外部审计。

### OP-306｜M06 全量自审、仿真 bridge 收紧与外审移交

- 操作：连续复审 06-01..09 的生产/测试边界、完整构建矩阵、规模仿真、资源变化与 M05 隔离。自审中发现 historical Host `ucn_cluster_sim` 仍依赖旧 v3 Current-FSM 语义，导致生产 v3 收紧后两个 failover 回归失败。
- 修改：新增仅给 `ucn_cluster_sim` 的 private `UCN_CLUSTER_LEGACY_V3_TEST_BRIDGE` membership copy；与 `ucn_tests` 的 `UCN_CLUSTER_ENABLE_TEST_HOOKS` 分离。production `ucn_cluster` archive 与 model target 均无 bridge，继续把 v3 记录为 bounded `PROVISIONAL/non-voting`。同步修正过期的 public header、任务表、06-09 自审说明和资源记录；新增 M06 全量自审报告。
- 验证：Windows Debug Full/Lite/Nano 为 `4/4`；配置契约 `7/7`；产品配置 `5/5`；Service OFF `27/27`；全新 Release Full `27/27`；WSL ASan/UBSan 与 `-fanalyzer` 均 `4/4`。64-node clean/default failover/fast failover 的 Host simulator 重新收敛，Host x64 `sizeof(ucn_cluster_t)=1552 B`。`git diff --check` 通过（仅既有 CRLF 提示）；production `ucn_cluster.c`、Adapter/transport 均无 v4 RX/TX/FSM 调用，encoder 仅在两个 codec 测试 target 定义。
- 结论/限制：M06 为 **CODE COMPLETE / FINAL SELF-AUDIT PASS**，现在交由外部审计。Host simulator 的旧 v3 bridge 不代表产品兼容性或 v3 voter 权限；M07-12 必须删除。M05 顶层继续 `AUDIT HOLD`，本轮没有真实设备、Flash/掉电或 production v4 Authority 验证。

### OP-307｜CLV2-06-R01 生产 v3 Backup/Takeover 接收侧 P0 整改

- 操作：根据 M06 外审确认 P0：产品虽已禁止 Head 从 v3 provisional 选择 Backup，但 `ucn_cluster_receive()` 仍可把合法 v3 Type 8、10..15、18、19 分派至 Backup/Takeover handler，因而能反向建立 Backup、mirror、vote 或 Head/Term 变化。
- 修改：在 production RX owner 的 decode/source 完成后、`now/stats/shadow` 和任一 handler 前增加统一 v3 Backup-authority fence，返回 `UCN_ERR_ACCESS`。为保持历史 Host 回归，仅让 `ucn_tests` 和 `ucn_cluster_sim` 的 target-private 自编译 copies 定义 bridge；simulator 同时自编译 `ucn_cluster.c`，production archive/model target 不含这两个宏。新增无宏编译断言和 production-archive public RX 回归。
- 验证：新回归逐个编码 Type 8、10..15、18、19；覆盖 Type10 self/non-self、完整 Type12 BEGIN/data/END、Type8 Head takeover 与故意 shadow desync。每例要求 `UCN_ERR_ACCESS` 且 `memcmp(ucn_cluster_t)` 完全不变。Windows Full/Lite/Nano `4/4`、配置契约 `7/7`、产品配置 `5/5`、Service OFF `27/27`、Release `27/27`、WSL ASan/UBSan 与 `-fanalyzer` 各 `4/4`；`git diff --check` 通过（仅 CRLF 提示）。
- 结论/限制：R01 已完成自审，但 M06 仍为 **AUDIT HOLD**，等待外部复审。该临时 fence 不实现 v4 Authority；M07 真实 committed-v4 Config owner 通过独立审计后才可取代它。Host bridge 不构成产品 v3 权限或互通承诺。

## 2026-08-04

### OP-001｜建立实施台账

- 操作：创建 `docs/00-项目管理/00-任务表.md` 与本操作记录文件。
- 修改：把 UCN Core 的实现拆为工程骨架、基础帧、Port/Link、QoS、路由、按需寻路、安全边界、Host 边界和完整验证九个阶段；每个阶段都指定单测、模拟测试和集成验收。
- 验证：文档将在创建后通过结构检查；尚未进行协议源码构建。
- 结论/限制：当前仓库仅有架构文档，尚无可执行协议代码；真实驱动和生产级密码实现不在“模拟通过”范围内。

### OP-002｜验证实施环境与台账

- 操作：检查任务表/操作记录的 Markdown 代码块结构，并检查本机构建工具。
- 修改：将任务 T00 标记为已完成。
- 验证：任务文档代码块闭合；检测到 CMake 4.1.1、Ninja 1.11.1、GCC 14.2.0，可用于 C99/CMake/Ninja 构建。
- 结论/限制：从 T01 开始使用 CMake + Ninja + GCC 进行可重复构建与测试；当前尚未添加协议源码。

### OP-003｜建立 C99 协议骨架与基础帧实现

- 操作：新增 CMake/Ninja 工程、公共头文件、基础配置校验、固定 UCN 帧编解码与 CRC-16 校验，以及最初的单元测试入口。
- 新增：`CMakeLists.txt`、`include/ucn/` 下的 `ucn.h`、`ucn_types.h`、`ucn_frame.h`、`ucn_link.h`、`ucn_port.h`；`src/ucn_core.c`、`src/ucn_frame.c`；`tests/` 下的 Core 与 Frame 测试。
- 修改：无既有协议源码可修改。
- 验证：待执行 CMake 配置、构建、CTest 和后续虚拟 Link 单跳测试。
- 结论/限制：本批只实现 T01/T02 所需的固定帧与接口骨架；`ucn_link.h`/`ucn_port.h` 仅定义边界，尚未被 Core 驱动；CRC 只用于传输错误检测，不提供安全认证。

### OP-004｜完成工程骨架与基础帧单元测试

- 操作：使用 CMake 4.1.1、Ninja 1.11.1、GCC 14.2.0 配置并构建 Debug 工程，执行 CTest。
- 修改：将 T01 标记为已完成，将 T02 标记为进行中，等待其虚拟 Link 模拟验收。
- 验证：`cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug`、`cmake --build build --parallel`、`ctest --test-dir build --output-on-failure` 均成功；`ucn_tests` 通过 1/1。
- 结论/限制：已验证配置校验和固定帧的正常/损坏 CRC/错误版本/短帧/空负载错误分支；尚未验证真实 Link 或节点收发。

### OP-005｜实现 Node/Link 单跳模拟并修复构建依赖

- 操作：新增 `ucn_node`、Link 注册/状态检查、基于目标 Node ID 的单跳发送和接收回调；新增内存虚拟 Link 两节点测试及 Node 错误路径测试。
- 新增：`include/ucn/ucn_node.h`、`src/ucn_node.c`、`tests/test_node.c`、`tests/test_virtual_link.c`；更新 CMake 将 Node 和测试纳入构建。
- 修改：`ucn_link_t` 增加直接对端 Node ID 字段；在 `src/ucn_node.c` 补充 `ucn.h` 包含，使 `ucn_validate_config()` 声明可见。
- 验证：首次构建因缺少该声明而失败；失败后的 CTest 命中了旧可执行文件，结果已明确作废，必须在修复后清理并重新构建。
- 结论/限制：当前 Node 只实现直连 Link 收发，不含队列、多跳转发、AODV-Lite 或生产安全；虚拟 Link 仅用于协议模拟，不能代表真实 WiFi/CAN 驱动。

### OP-006｜完成基础帧与 Link 单跳验证

- 操作：执行 `cmake --build build --clean-first --parallel`，并仅在构建成功后执行 CTest 与测试可执行文件。
- 修改：将 T02、T03 标记为已完成。
- 验证：清理重建成功；CTest `ucn_tests` 通过 1/1；测试可执行文件输出 `All UCN tests passed.`。覆盖帧编解码、CRC/版本/长度错误、配置校验、Link 注册重复拒绝、Link down、无直连路由拒绝，以及两节点虚拟 Link 的有效载荷交付。
- 结论/限制：构建和测试结果来自清理后的新可执行文件，不再受 OP-005 旧二进制问题影响；Port 的平台实现、QoS 队列和多节点路由仍未实现。

### OP-007｜实现 Core Q0/Q1 固定队列

- 操作：在 Node 中新增静态 Q0/Q1 队列、发送请求结构、`ucn_node_enqueue()`、`ucn_node_step()` 和统计信息；实现 Q0 抢占、Q1 最新值覆盖与 deadline 过期丢弃。
- 新增：`tests/test_qos.c`。
- 修改：`ucn_types.h` 增加投递语义；`ucn_node.h` 增加可由编译期覆盖的 Q0/Q1 深度、静态队列和统计结构；`ucn_node.c` 新增队列调度逻辑；CMake 将 QoS 测试加入测试二进制。
- 验证：待执行清理重建、CTest 与 QoS 虚拟 Link 测试。
- 结论/限制：Core 只接受 Q0/Q1 和 `best_effort`/`latest_value`；Q2/Q3、可靠重传、分片与服务目录仍严格留在 Extended，尚未实现。

### OP-008｜完成 QoS 单元与虚拟链路验证

- 操作：清理重建工程并执行完整 CTest/测试二进制。
- 修改：将 T04 标记为已完成。
- 验证：CTest `ucn_tests` 通过 1/1；QoS 测试验证先入队 Q1、后入队 Q0 时 Q0 优先发送，验证 Q1 `latest_value` 覆盖旧值，验证 deadline 到期后不发送且增加过期丢弃计数。
- 结论/限制：Q0/Q1 调度只在单跳虚拟 Link 上验证；后续多跳转发必须继续保持该队列语义。

### OP-009｜实现固定路由缓存与两跳转发

- 操作：为 Core 增加固定容量 Route Cache、静态路由登记、入站重复帧缓存、TTL 递减和基于 egress Link 的转发；新增 A→B→C 三节点虚拟拓扑测试。
- 新增：`tests/test_route.c`。
- 修改：`ucn_node.h` 增加可编译期配置的路由/去重缓存及路由 API；`ucn_node.c` 在直连 Link 未命中时查询路由缓存，并在接收侧进行去重和转发；CMake 纳入路由测试。
- 验证：待执行清理重建、CTest 与三节点模拟。
- 结论/限制：本批只实现 T05 的固定路径转发；它不自动发现未知路径，AODV-Lite 的 RREQ/RREP/RERR 和断链修复仍属于 T06。

### OP-010｜完成两跳路由模拟验证

- 操作：清理重建并执行完整 CTest/测试二进制。
- 修改：将 T05 标记为已完成。
- 验证：CTest `ucn_tests` 通过 1/1；新增三节点模拟确认 A 经 B 向 C 发送 Q1 数据，B 的 Route Cache 选择 B→C Link，C 收到原始源 Node ID 与有效载荷。测试还覆盖路由 API 对未注册 egress Link 的拒绝。
- 结论/限制：已验证无 Linux 的固定两跳路径；重复帧与 TTL 分支已在 Core 实现，但尚需随 T06 的路由请求/错误场景扩展专门测试。

### OP-011｜实现受限 AODV-Lite 按需路由

- 操作：在不引入动态内存和 Linux 依赖的前提下，为 MCU Core 增加固定容量的按需路由状态和控制报文处理。
- 新增：`ucn_node_discover_route()` 与 `ucn_node_route_pending()`；固定大小的寻路请求表；`ROUTE_REQ`、`ROUTE_REPLY`、`ROUTE_ERROR` 的 12/12/4 字节控制载荷编码与处理；`tests/test_aodv_lite.c`。
- 修改：`ucn_node.h` 增加可编译期覆盖的寻路表容量、请求限速、超时和动态路由寿命；`ucn_node.c` 增加反向路由学习、RREQ 广播转发、RREP 回程建表、动态路由老化、链路失败时的 RERR 上游通知与动态路由清除；CMake 和测试入口纳入新测试。
- 验证：待执行清理重建、CTest 和三节点虚拟拓扑测试。
- 结论/限制：路由表、重复帧缓存和寻路表均是固定数组。当前“按需”由调用方在未知目的地时调用 `ucn_node_discover_route()` 发起，尚未把业务数据缓存到等待寻路的队列中；真实无线链路质量评估仍不在模拟范围内。

### OP-012｜完成 AODV-Lite 单元与三节点修复模拟

- 操作：清理重建工程并执行完整 CTest/测试二进制。
- 修改：将 T06 标记为已完成。
- 验证：`cmake --build build --clean-first --parallel` 成功；CTest `ucn_tests` 通过 1/1；测试可执行文件输出 `All UCN tests passed.`。`test_aodv_lite` 验证 RREQ 重复包被抑制、同一目的地的请求限速、无响应请求超时、RREP 反向建表、A→B→C 的未知目的地两跳收发、B→C 断开时 B 发出 RERR 且 A 清除动态路由、恢复链路后二次发现、动态路由老化后失效。
- 结论/限制：已在三个逻辑 MCU 节点的内存虚拟 Link 上验证“无 Linux 的发现—通信—断链—修复”闭环。RREQ 使用受 Hop Limit 限制的控制面泛洪，当前未实现度量值选路、多路径、全局拓扑表或业务数据等待寻路缓存，符合 Core 的小而受限定位。

### OP-013｜实现安全 Provider 边界并修复构建接口

- 操作：新增可注入的安全 Provider，把会话标识、持久化发送序号、出站授权和入站授权接入所有本机起始帧及入站帧路径；新增安全 Provider 单元/模拟测试。
- 新增：`include/ucn/ucn_security.h`、`tests/test_security.c`。
- 修改：`ucn_node.h` 增加安全 Provider 状态和 `ucn_node_set_security()`；`ucn_node.c` 增加序号分配/持久化、会话 ID 写入、TX/RX 授权调用；CMake 与测试入口纳入安全测试。
- 验证：第一次清理构建失败，原因是 `ucn_security.h` 重复 typedef 了已由 `ucn_link.h` 声明的 `ucn_link_t`，且 `send_control_on_link()` 遗漏局部 `result`；已改为直接包含 `ucn_link.h` 并补齐变量。第二次构建通过，但安全测试中把“被 TX 授权拒绝仍消耗序号”的设计误判为错误，已将断言更正为该重放保护语义。
- 结论/限制：本阶段没有自研密码算法，也没有把 CRC 当认证；Provider 必须由产品实现身份验证、ACL 以及所需的 AEAD/底层保护。所有失败都未使用旧测试二进制作为结果依据。

### OP-014｜完成安全 Provider 单元与虚拟链路验证

- 操作：在修正后清理重建工程，执行 CTest 和测试二进制。
- 修改：将 T07 标记为已完成。
- 验证：`cmake --build build --clean-first --parallel` 成功；CTest `ucn_tests` 通过 1/1；测试可执行文件输出 `All UCN tests passed.`。`test_security` 验证不完整 Provider/加载失败拒绝、会话 ID 传递、发送序号保存、逻辑重启后序号不回退、入站 ACL 拒绝、出站 ACL 拒绝，以及 Counter 写入失败时拒绝发送。
- 结论/限制：为避免重放，Core 在一次发送尝试取得序号后即先持久化，因此即使之后被 TX ACL 拒绝，该序号仍会被消耗；这是有意的“宁可跳号、不复用”策略。测试 Provider 只模拟会话和 ACL，不提供密码学安全；生产 AEAD、密钥轮换和设备入网认证仍未接入。

### OP-015｜完成 Host 非中心化边界模拟

- 操作：新增 Host 边界测试，使用和 MCU 完全相同的 `ucn_node_init()`、`ucn_node_register_link()`、`ucn_node_add_route()` 与 `ucn_node_send()` API 接入虚拟 Host；不引入 ROS2、Linux socket 或专用 Host 协议。
- 新增：`tests/test_host_boundary.c`。
- 修改：CMake 与测试入口纳入 Host 边界测试；将 T08 标记为已完成。
- 验证：`cmake --build build --clean-first --parallel` 成功；CTest `ucn_tests` 通过 1/1；测试可执行文件输出 `All UCN tests passed.`。测试先验证 MCU A→B→C 两跳，再在 B 上接入 Node ID 100 的 Host 并由 Host 经 B→C 发送，最后把 Host Link 标为 down 后再次验证 A→B→C 成功。
- 结论/限制：Host 在 Core 视角是普通节点，不承担寻路中心或必经网关；Host 的 ACL/会话路径复用 T07 的公共安全 Provider 测试。此结果是内存虚拟 Link 模拟，不代表 Linux 驱动或 ROS2 Bridge 已实现。

### OP-016｜新增完整协议集成场景并完成 Debug 验证

- 操作：新增四节点端到端集成测试，将 3 个逻辑 MCU 节点、1 个 Host、统一安全 Provider、AODV-Lite、Q0/Q1 队列、断链修复和 Host 退出放进同一个虚拟 Link 拓扑。
- 新增：`tests/test_integration.c`。
- 修改：CMake 与测试入口纳入集成测试。
- 验证：执行 `cmake --build build --clean-first --parallel`、`ctest --test-dir build --output-on-failure` 和 `build\\ucn_tests.exe`，均成功；CTest 通过 1/1，测试程序输出 `All UCN tests passed.`。集成测试验证：A 到 C 的未知路由发现；Q0 在先入队 Q1 后仍优先送达；Host 经 B 向 C 发送；B→C 断开时 A 收到失败并清除动态路由；恢复后重新发现并恢复发送；Host Link 断开后 A→B→C 持续可用；全部节点均经过同一会话/Counter Provider 路径。
- 结论/限制：这是当前 Core 最终的代码级集成闭环，不依赖 Linux。它只使用内存虚拟 Link，不能证明真实 WiFi、BLE、CAN 的时序、丢包、吞吐、EMC 或驱动行为。

### OP-017｜完成 Release 验证并记录 Sanitizer 环境限制

- 操作：配置独立 Release 构建并执行完整测试；随后尝试 AddressSanitizer + UndefinedBehaviorSanitizer 构建。
- 修改：将 T09 标记为已完成；未修改协议源代码。
- 验证：`cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DUCN_BUILD_TESTS=ON`、`cmake --build build-release --clean-first --parallel`、`ctest --test-dir build-release --output-on-failure` 和 `build-release\\ucn_tests.exe` 均成功，CTest 通过 1/1，测试程序输出 `All UCN tests passed.`。Sanitizer 配置未能完成：本机 MinGW 14.2 链接器报告缺少 `-lasan` 与 `-lubsan`，因此 Sanitizer **未执行**，不是通过结果。
- 结论/限制：当前 Debug 和 Release 均以 `-Wall -Wextra -Wpedantic -Werror` 编译并通过全部单元、模拟与集成用例；生产前仍需在具备 Sanitizer 的宿主工具链或目标板上补充运行，并接入真实硬件 Link、生产安全 Provider 和压力/故障注入测试。

### OP-018｜启动通用 Link Cost 选路扩展

- 操作：根据“质量选路不能只服务 WiFi”的约束，新增 T10，定义将各物理介质状态归一为通用路由代价的实施范围。
- 修改：`docs/00-项目管理/00-任务表.md` 新增 T10，并将本轮范围扩展为可选链路质量选路；本操作记录追加本条。
- 验证：待完成 Link API、AODV-Lite 路径代价传播、单元测试、双路径模拟后执行清理重建。
- 结论/限制：Core 只接收可选的 `route_cost`，不直接读取 WiFi RSSI、BLE RSSI、LoRa SNR 或 CAN 专有字段；未提供代价的既有 Link 必须回退为固定默认值，以保持兼容。

### OP-019｜实现通用 Link Cost 与低代价路径替换

- 操作：为 Link 增加可选质量回调，并把受限 AODV-Lite 从“第一个 RREP 建路”改为“允许较低累计代价的重复 RREQ 通过，并优先保留较低代价路径”。
- 新增：`tests/test_link_metrics.c`。
- 修改：`ucn_link.h` 增加 `ucn_link_metrics_t` 和可选 `get_metrics()`；`ucn_node.h` 增加默认代价配置、Route Cache 的代价/hop 字段及 RREQ 去重代价；`ucn_node.c` 增加代价读取和饱和累加、同一直接 Peer 的低代价 Link 选择、低代价 RREQ 重处理、RREQ/RREP 的累计代价/hop 传播、低代价动态路由替换；所有测试 Link 初始化补齐新回调字段；`ucn_types.h` 协议版本从 1 升至 2；架构与配置文档补充跨介质 Link Cost 规则。
- 验证：执行 `cmake --build build --clean-first --parallel`、`ctest --test-dir build --output-on-failure` 和 `build\\ucn_tests.exe`，均成功；CTest 通过 1/1，测试程序输出 `All UCN tests passed.`。
- 结论/限制：版本 2 的 RREQ/RREP 控制载荷从 12 字节扩展为 16 字节，v1 与 v2 节点不能混用；普通业务帧头未改变。`route_cost` 是 Adapter 归一后的值，Core 不含 RSSI/SNR/CAN 专有字段；质量平滑、防抖和采样频率仍由具体 Adapter 决定。

### OP-020｜完成 Link Cost 单元、双路径模拟与 Release 验证

- 操作：执行新的质量选路测试、完整 Debug 测试和独立 Release 测试。
- 修改：将 T10 标记为已完成。
- 验证：`test_link_metrics` 验证未上报指标时回退到默认代价、同一 Peer 存在多 Link 时选择较低代价 Link；在 A→B→C（总代价 60）与 A→D→C（总代价 2）双路径模拟中，高代价 RREP 先到后，低代价重复 RREQ 仍被目标处理，最终 A→C 数据从 A→D Link 发出并在 C 接收。随后 `cmake --build build-release --clean-first --parallel`、`ctest --test-dir build-release --output-on-failure` 和 `build-release\\ucn_tests.exe` 均成功，CTest 通过 1/1，测试程序输出 `All UCN tests passed.`。
- 结论/限制：不提供 `get_metrics()` 的现有 Link 继续以默认代价工作，现有业务 API 不需要变化。当前实现只在发现期选择更低代价路径，不会因瞬时 RSSI 自动频繁切路，也没有多路径备份；真实 ESP-NOW/WiFi、BLE、LoRa、CAN、UART Adapter 仍需分别提供健康指标并在硬件上测量。

### OP-021｜建立动态入网后续任务与安全边界

- 操作：根据“物理发现 → 候选 → 验证/准入 → 路由”的最终流程，新增 T11～T15：邻居生命周期、HELLO 自动准入、多介质 Adapter 契约、ESP32 实际接入、生产入网与规模验证。
- 修改：`docs/00-项目管理/00-任务表.md` 增加 T11～T15；本操作记录追加本条。
- 验证：待完成 T11 的 Core 实现、单元测试和虚拟 Link 模拟后执行清理重建。
- 结论/限制：稳定 Node ID 应来自出厂配置/持久化身份而不是运行时随意分配；默认策略保持人工准入。开放自动入网只适合实验网络，生产网络必须由身份/ACL Provider 明确放行。T14 需要用户指定 ESP 芯片型号、ESP-IDF 工程位置和实机授权后才能实现真实驱动与刷机验证。

### OP-022｜完成 T11 邻居 Candidate/Admitted 生命周期

- 操作：实现固定容量邻居表、准入策略 API、候选观察、手工准入/拒绝、Provider 准入和候选超时回收；新增专用单元测试。
- 新增：`include/ucn/ucn_neighbor.h`、`tests/test_neighbor_lifecycle.c`。
- 修改：`ucn_node.h` 增加邻居表容量、候选超时、策略状态和公开 API；`ucn_node.c` 增加 Candidate/Admitted/Rejected/Expired 状态机，`ucn_node_step()` 增加候选老化；CMake 与测试入口纳入测试。
- 验证：执行 `cmake --build build --clean-first --parallel`、`ctest --test-dir build --output-on-failure` 和 `build\\ucn_tests.exe`，均成功；CTest 通过 1/1，测试程序输出 `All UCN tests passed.`。测试覆盖默认手工候选、重复观察、拒绝后的重新发现、手工准入、Provider 允许/拒绝、候选表满和候选超时。
- 结论/限制：T11 不扫描 WiFi/BLE/CAN，也不自动发送 HELLO；Adapter 负责发现物理邻居并调用 `ucn_node_observe_neighbor()`。默认手工策略保证旧工程不因升级而自动接纳新设备。

### OP-023｜完成最小 HELLO 与自动准入闭环

- 操作：新增链路本地 `HELLO` 处理与 `ucn_node_probe_neighbor()`；将未知 Link 的普通数据和路由控制报文限制为拒绝，只有通过帧校验、Network ID 校验以及安全 Provider（若已配置）的 `HELLO` 才能绑定 Node ID 并进入入网策略。
- 新增：`tests/test_hello_join.c`，覆盖开放策略下的双方 HELLO、未知物理 Link 的 Node ID 绑定、自动注册、自动入网后的业务收发、未准入 Link 数据拒绝、HELLO 身份不匹配和错误 Network ID。
- 修改：`src/ucn_node.c` 增加 4 字节 Node ID `HELLO` 校验/处理、入站 Link 准入门禁；`include/ucn/ucn_node.h` 增加邻居探测 API；CMake 和测试入口纳入新测试；任务 T12 标记完成。
- 验证：执行 `cmake --build build --clean-first --parallel`、`ctest --test-dir build --output-on-failure` 和 `build\\ucn_tests.exe` 均成功；CTest 通过 1/1，测试程序输出 `All UCN tests passed.`。
- 结论/限制：HELLO 是一跳控制消息，不转发、不交给应用。开放策略只适合实验网；生产网仍必须通过安全 Provider 与 Provider 准入策略验证身份。Core 已能接收未知物理 Link 上的 HELLO，但“广播扫描/回调入队/介质地址映射”属于 T13 Adapter 契约，真实 ESP-NOW 自动组网仍属于 T14。

### OP-024｜启动多介质 Adapter 契约

- 操作：启动 T13，确定所有介质统一采用“驱动回调/轮询只入固定收包队列，协议任务再泵入 Core”的边界；物理地址到 Candidate Link 的映射由 Adapter 静态表维护，Core 只见 `ucn_link_t` 与已校验帧。
- 修改：任务 T13 标记为进行中。
- 验证：待完成公共 Adapter 队列/地址绑定接口、内存虚拟驱动测试和完整清理重建后记录。
- 结论/限制：默认收包队列将设为 2 帧，即约 512 B 原始帧数据区，可由编译期缩为 1；真实 ISR 回调应使用平台安全队列或极短临界区，不能在中断上下文执行 `ucn_node_receive()`、路由或应用回调。

### OP-025｜完成 T13 Adapter 公共契约与广播发现模拟

- 操作：实现 Adapter 公共边界、固定收包队列、物理地址到静态 Link 的绑定表与协议任务 Pump；补齐未知 Node ID 场景的物理广播 HELLO，使双方均从 `peer_node_id=0` 的 Candidate Link 开始完成自动入网。
- 新增：`include/ucn/ucn_adapter.h`、`src/ucn_adapter.c`、`tests/test_adapter.c`、`docs/06-平台与适配/UCN_Adapter_契约.md`；任务表增加按需启用的 T16 小 MTU Carrier，明确经典 CAN 不能直接承载 32 B UCN 基础头。
- 修改：`ucn_node.h`/`ucn_node.c` 增加 `ucn_node_broadcast_hello()`；`ucn_types.h` 允许编译期覆盖 `UCN_MAX_FRAME_BYTES`；`test_hello_join` 改为双方未知 Node ID 时的广播 HELLO 模拟；CMake 与测试入口纳入 Adapter 测试；T13 标记完成。
- 验证：Debug：`cmake --build build --clean-first --parallel`、`ctest --test-dir build --output-on-failure` 和 `build\\ucn_tests.exe` 均成功；Release：`cmake --build build-release --clean-first --parallel`、`ctest --test-dir build-release --output-on-failure` 和 `build-release\\ucn_tests.exe` 均成功；两套 CTest 均通过 1/1，测试程序均输出 `All UCN tests passed.`。额外配置 `build-mtu64`（`-DUCN_MAX_FRAME_BYTES=64`）并执行构建、CTest 和测试程序，也全部通过。测试覆盖物理地址绑定冲突、固定队列 FIFO/满队列、临界区钩子、未知 Link 数据拒绝、广播 HELLO 自动入网和后续业务收发。
- 结论/限制：公共 Adapter 只提供受限队列和地址/Link 映射，不是任何厂商驱动。默认 256 B UCN Profile 不可直接通过 64 B CAN-FD 或 8 B 经典 CAN；必须将全网 Profile 编译为适合最小 MTU，或在 T16 实现有界 Carrier 分段/重组。T14 仍需要指定 ESP32 型号、ESP-IDF 工程路径及真实硬件授权。

### OP-026｜同步最新架构与配置档案

- 操作：将整体架构和配置档案同步到 T12/T13 的实际边界：Candidate Link、物理广播 HELLO、Manual/Open/Provider 准入、Adapter 收包队列、物理地址与 Node ID 分离，以及最小 MTU Profile 约束。
- 修改：`docs/02-总体架构/UCN_整体架构设计.md` 与 `docs/01-入门与使用/UCN_协议分层与配置档案.md` 增加已实现/待实现区分及对 `UCN_Adapter_契约.md` 的引用。
- 验证：文档中的接口名与 `include/ucn/ucn_adapter.h`、`include/ucn/ucn_node.h` 一致；源码已在 OP-025 的 Debug、Release 与 64 B Profile 中通过测试。
- 结论/限制：架构目标中已有的 Coordinator JOIN 挑战、生产 AEAD 与真实驱动不因本轮 HELLO 模拟而视为已实现；它们继续受 T14/T15 约束。

### OP-027｜按当前源码重新校正文档状态

- 操作：逐项核对 `include/ucn/`、`src/` 与测试后，同步整体架构、协议档案、Adapter 契约和 ROS2 协同文档。
- 修改：所有文档明确标注版本 2 当前已实现的最小 HELLO/准入、Provider 边界、AODV-Lite、Q0/Q1、Adapter Queue 与虚拟 Host；把 `JOIN_*`、`HEARTBEAT`、生产 AEAD、真实 Linux/ROS2/ESP 驱动、目标板专用 `ucn_config.h`、实机资源报告和小 MTU Carrier 改为待实现/待验证。文档 API、资源宏和当前平铺目录改为源码中的真实名称。
- 验证：对照 `ucn_types.h`、`ucn_node.h`、`ucn_adapter.h`、`ucn_security.h` 和 `src/ucn_node.c` 完成接口/枚举/宏核对；文档修改不改变协议源码，沿用 OP-025 已通过的 Debug、Release 和 64 B Profile 测试结果。
- 结论/限制：历史对话文档与 `.docx` 需求复盘保留为历史资料，不改写为当前实现说明；当前实现状态以任务表、操作记录和这四份架构/契约文档为准。

### OP-028｜建立路由缓存与自动选路演进建议

- 操作：根据路由缓存、路径质量变化和切换期数据行为的讨论，新增独立建议文档，并在任务表创建可验收的后续任务 T17。
- 新增：`docs/04-路由与链路/UCN_路由缓存与自动选路建议.md`，明确当前 30 秒缓存是最大寿命而非自动切换周期；定义活动路径/刷新/候选路径、验证期限与最近使用时间的区分、质量防抖滞回、切换期 Q0/Q1 规则、固定资源边界与 T17.1～T17.4 验收顺序。
- 修改：`docs/00-项目管理/00-任务表.md` 增加建议文档入口和 T17“路由缓存续租与自动选路”。
- 验证：本次仅新增规划文档和待开始任务，不修改协议源码、线格式或测试；已逐项对照当前 `ucn_node.c` 的动态路由 30 秒老化、显式 `ucn_node_discover_route()`、RERR 断链清表、Q0/Q1 队列与通用 `route_cost` 边界。
- 结论/限制：T17 尚未实现；当前 Core 仍不会因普通业务发送续租动态多跳路径，也没有后台刷新、自动切换、候选备份或可靠重传。实施必须按任务表先完成单测和虚拟拓扑模拟，再进入 T14 指定硬件验证。

### OP-029｜补充候选路径先测后切建议

- 操作：采纳“旧路径持续业务、候选路径先发少量测试帧、验证成功后才切换”的建议，将 T17 的自动选路策略细化为 Make-Before-Break。
- 修改：`docs/04-路由与链路/UCN_路由缓存与自动选路建议.md` 增加 `PATH_PROBE/PATH_PROBE_ACK`、`candidate_id/route_epoch`、活动/候选路径并存、旧路径 grace window、探测流量预算与中继节点候选转发表要求；T17.2～T17.4 的测试门禁改为覆盖候选路径不覆盖旧路径、探测 ACK/安全、候选失败回退和验证后切换。`docs/00-项目管理/00-任务表.md` 同步 T17 的实现与验收描述。
- 验证：本次只调整建议和任务验收，不修改协议源码。已明确当前 UCN v2 仅以 `destination → egress_link` 转发，尚无路径标识、候选转发表或探测控制消息，因此不能把本建议表述为当前行为。
- 结论/限制：探测帧不能复用有副作用的业务数据；候选路径只能用受认证、固定频率和固定次数的控制帧验证。即使完成切换，在旧 Link 驱动内的帧也无法迁移，Q1 仍需依赖序号/时间戳处理少量乱序，Q2 才能提供 ACK 驱动的可靠重传。

### OP-030｜补充探测成本与频率建议

- 操作：量化“定期检查最优路径”的控制流量来源，区分零空口开销的 Adapter 本地采样、与分支数相关的 RREQ/RREP 候选发现、以及与跳数相关的 `PATH_PROBE/ACK` 验证。
- 修改：`docs/04-路由与链路/UCN_路由缓存与自动选路建议.md` 增加当前 32 B 帧头与 16 B RREQ/RREP 的成本计算、计划中 12 B Probe 载荷的字节跳数估算，以及 WiFi/ESP-NOW、CAN/RS485、LoRa 的起始频率 Profile。
- 验证：对照 `include/ucn/ucn_types.h` 的 32 B 帧头和 `src/ucn_node.c` 的 16 B RREQ/RREP 载荷、逐 Link RREQ 转发逻辑完成计算；`PATH_PROBE/ACK` 尚未实现，文中所有 Probe 大小、频率和切换门限均明确标注为待 T14 实机标定的建议值。
- 结论/限制：网络探测的主要成本来自候选 RREQ 泛洪而非少量单路径 Probe；稳定链路不应盲目按固定短周期全网寻路。当前协议没有 `PATH_PROBE/ACK`，因此未进行相关源码、单元测试或实机吞吐验证。

### OP-031｜建立节点入离网与网络稳定性建议

- 操作：根据断开检测、新节点接入、入网对现有网络的影响、无用 Link 回收和定期最优检查六项问题，新增独立节点生命周期建议，并在任务表创建 T18。
- 新增：`docs/04-路由与链路/UCN_节点入离网与网络稳定性建议.md`，记录当前“发送失败才发现离网”、双方 HELLO 立即完成虚拟单跳准入、HELLO 一跳不转发、动态路由 30 秒老化/Candidate 5 秒超时、已准入邻居和 Adapter 绑定不自动回收等事实；提出 `ADMITTED → SUSPECT → REMOVED`、认证 HEARTBEAT、随机退避、控制流量配额及 T17/T18 联动方案。
- 修改：`docs/00-项目管理/00-任务表.md` 增加建议文档入口及 T18“节点入离网与网络稳定”。
- 验证：本次只新增建议与待开始任务；已对照 `src/ucn_node.c` 的 Candidate-only 超时、发送 Link Down/RERR 清路由、HELLO 准入，及 `ucn_adapter` 的静态物理地址绑定。未修改当前 Core、线格式、测试或硬件驱动。
- 结论/限制：当前没有已准入邻居保活状态机、HEARTBEAT 处理、Adapter 自动解绑、周期最优多跳检查或实机接入时间数据。T18 的时间参数均为后续 Profile 起点，必须在 T14 指定硬件上测量后冻结。

### OP-032｜补充 Reticulum 与 ESP-WIFI-MESH 的稳定性设计参考

- 操作：调研 Reticulum 的目的地 Announce/按需 Path Request/控制面限流机制，以及 ESP-WIFI-MESH 的 Beacon 邻居发现、父节点恢复与树形路由机制，并将可迁移的设计原则写入 T18 建议。
- 修改：`docs/04-路由与链路/UCN_节点入离网与网络稳定性建议.md` 新增“可借鉴的 Reticulum 与 ESP-WIFI-MESH 机制”。明确 UCN 只吸收按需发现、控制面预算、随机退避、一跳健康状态与可测量恢复流程；不引入 Reticulum 的主机级长寿命路径目录，也不引入 ESP-WIFI-MESH 的 Root/Parent/Child 树和全局子树路由表。`docs/00-项目管理/00-任务表.md` 同步 T18 的参考边界。
- 验证：复核 Reticulum 官方文档/Transport 源码中的 Announce 与 Path Request 限流、路径清理和按需发现逻辑；复核 Espressif 官方文档中的 Beacon、RSSI 门限、父节点选择、关联超时和父节点切换丢包限制。已检查 Markdown 链接和 T18 任务表引用；本次不修改 UCN 协议源码、线格式或测试。
- 结论/限制：单 WiFi STA 不能同时关联两个上游父节点，因此不能把 ESP-WIFI-MESH 的父节点切换视为 UCN 所需的 Make-Before-Break 实现。UCN 的候选先测后切仅能在底层允许并存候选路径时实现；最终时序、控制流量和 RAM 消耗仍须在 T14/T18 实机测试冻结。

### OP-033｜建立 UCN 更新后设计方案

- 操作：将现有 MCU-first 架构、T17 路由缓存/候选切换建议、T18 节点入离网建议，以及 Reticulum/ESP-WIFI-MESH 的可借鉴边界整合为一份可实施的设计基线。
- 新增：`docs/02-总体架构/UCN_更新后设计方案.md`。文档定义当前 v2 与目标能力的边界、模块职责、邻居与路由双状态机、固定资源模型、v3 路径扩展建议、`HEARTBEAT`/`PATH_PROBE`/`PATH_ACTIVATE` 控制面、Cost/定时器/Profile、QoS 规则、Adapter 要求、实施顺序与单元/模拟/实机验收门禁。
- 修改：`docs/00-项目管理/00-任务表.md` 的架构依据增加该设计方案入口。
- 验证：逐项核对当前 `ucn_types.h` 的 v2/32 B 帧头/现有消息枚举、`ucn_node.h` 的 Link/Route/Neighbor/队列默认上限，及 T17/T18 既有建议；新增字段与报文均显式标记为 v3 目标设计，未写成当前能力。文档修改不改变协议源码、线格式、构建或测试结果。
- 结论/限制：实施 T17 前必须先冻结 v2→v3 升级、路径扩展字节布局和目标板资源预算；实施 T18/T14 前必须明确 ESP 芯片、底层连接模式和硬件测试条件。单 WiFi STA 的上游切换仍不能被宣传为绝对无缝双路径切换。

### OP-034｜将同节点数据区分方案纳入更新后设计

- 操作：核对当前帧编码与 `ucn_node_send()`/Q1 队列行为后，将“静态 Endpoint 复用 1 B `message_type`”确定为 MCU 默认数据分发方案，并将动态目录保留为 Extended 按需能力。
- 修改：`docs/02-总体架构/UCN_更新后设计方案.md` 增加 T19 Endpoint 分层、`0x40～0xBF` 静态编号范围、`(source_node_id, message_type)` 分发、Q1 `latest_value` 覆盖键、Endpoint ACL、动态目录/订阅的 4 B 信封边界以及对应测试门禁；`docs/00-项目管理/00-任务表.md` 新增 T19。
- 验证：对照 `src/ucn_frame.c` 的 32 B 帧头和 Payload 起点、`src/ucn_node.c` 的任意 `uint8_t message_type` 发送路径、控制报文特殊处理与 Q1 队列按 `(destination, message_type)` 覆盖逻辑完成核对。已检查文档内链接和任务表入口；未修改协议源码、线格式或既有测试。
- 结论/限制：当前代码尚未声明 `0x40～0xBF` 为正式业务范围，也没有 `ucn_endpoints.h`、Endpoint ACL 或动态服务目录；它们都是 T19 待实现内容。静态 Endpoint 不增加每帧字节，而动态目录只能在确有运行时发现需求时启用，避免给普通 MCU 节点增加固定 RAM 与通信负担。

### OP-035｜补全最新目标协议的逐帧线格式

- 操作：根据“最新设计中每一帧包含什么”这一问题，将业务、切换期业务、控制和 Extended 动态 Endpoint 的线格式收敛到同一处说明。
- 修改：`docs/02-总体架构/UCN_更新后设计方案.md` 新增“最新目标设计：单帧到底包含什么”。明确正常静态 Endpoint 帧为 32 B 网络头加业务 Payload，`message_type` 承担 IMU/气压计/温度等 Endpoint 区分；仅路径切换窗口使用 36 B 头并增加 `route_epoch`。同时冻结 v3 变长头中 CRC 的目标位置与覆盖范围，列出各控制帧的 Payload。
- 验证：对照当前 `src/ucn_frame.c` 的 v2 字段偏移、32 B 头、Payload 起点、CRC 计算和 `include/ucn/ucn_types.h` 的消息编号完成字段继承核对；36 B 头、Endpoint 范围、Heartbeat/Path 控制帧仍明确标注为 T17/T18/T19 的目标，不是当前编码器行为。未修改源码、线格式、构建或测试。
- 结论/限制：v3 的 CRC 位置变化与 `header_size` 解码是线格式破坏性变更，必须在 T17 实施前完成协议测试向量和全网升级策略冻结。当前 v2 仍只接受 32 B 头，CRC 位于 30..31。

### OP-036｜建立按需加密与透明转发安全建议

- 操作：根据“Node 可分别配置加密/不加密、解密/不解密”的需求，定义按 Node 默认策略、Endpoint 覆盖策略和对端密钥/ACL 表选择明文或端到端受保护业务帧的安全模型。
- 新增：`docs/03-核心协议/安全/UCN_按需加密与透明转发建议.md`。文档区分端到端 Payload AEAD 与可选逐跳 Link AEAD；定义 `PLAIN/ENCRYPTED/AUTO`、`PLAIN_ONLY/ENCRYPTED_ONLY/BOTH` 与透明中继模式，明确中继可路由密文但不持有终端业务密钥；列出不可变 AAD、可变 TTL/路径元数据、16 B Tag、帧长影响、入网/密钥边界及 T15 的单元、模拟和实机验收。
- 修改：`docs/02-总体架构/UCN_更新后设计方案.md` 增加 T15 按需安全策略与透明转发边界、端到端受保护业务帧、v3 Tag 长度/最大 Payload 及实施前参数；`docs/00-项目管理/00-任务表.md` 扩展 T15 验收，使其覆盖明文/密文共存、加密必需 Endpoint、无 Key 中继透明转发和真实资源测量。
- 验证：对照当前 `src/ucn_frame.c` 的固定 `32 B + Payload` 长度校验、`flags` 透传、CRC 覆盖范围，以及 `include/ucn/ucn_security.h` 仅有 Session/Counter/TX-RX 授权回调的边界完成核对。未修改协议源码、线格式、构建或测试；所有 Flag、Tag、AEAD、密钥和透明转发内容均标记为 T15/v3 待实现。
- 结论/限制：现有 Provider 不能直接对帧 `seal/open`，故生产 AEAD 需要扩展 Provider 和帧编解码路径，不能仅替换测试回调。端到端 AAD 必须排除中继必改的 Hop Limit/路径元数据；控制帧仍需要独立逐跳认证，不能直接套用透明业务密文语义。

### OP-037｜建立最新 UCN Core 的 R1 实施基线

- 操作：按“先核对、再按任务表更新代码”的要求，重新检查当前 v2 公共头、`ucn_node.c`、安全 Provider、全部现有测试、最新设计方案及待办任务，并将本轮可在 C99 虚拟拓扑中闭环的工作拆分为 R1.1～R1.5。
- 修改：`docs/00-项目管理/00-任务表.md` 将 T17/T18/T19 改为进行中，新增 R1.0～R1.5、更新前功能核对和明确外部前置条件；本轮顺序为邻居保活/回收、Active/Candidate 路由探测、静态 Endpoint 命名空间、完整验证。
- 验证：修改前实际执行 `cmake --build build --clean-first --parallel` 与 `ctest --test-dir build --output-on-failure`，结果为 `100% tests passed, 0 tests failed out of 1`。核对确认当前尚无已准入邻居 Heartbeat、自动离网回收、Candidate 路由/Probe、Endpoint 公共命名空间、生产 AEAD、v3 变长帧或真实 ESP32 Adapter。
- 结论/限制：R1 只实现不依赖 Linux、具体无线驱动、密码算法或产品 ABI 的 Core 闭环。T14、T15、T16 以及 v3 路径 Epoch/安全帧仍需各自的硬件或协议冻结门禁，不能因 R1 虚拟测试通过而标为已完成。

### OP-038｜实现邻居保活、离网回收与固定槽复用（R1.1/R1.2）

- 操作：在 Core 中实现一跳 `HEARTBEAT` 请求/回显、`ADMITTED → SUSPECT → REMOVED` 状态转换，以及已自动准入邻居离网后的路由清理、Link 注销、Node ID 清除和固定槽复用。
- 修改：`include/ucn/ucn_types.h` 增加 `PATH_*` 预留控制编号；`ucn_neighbor.h` 增加 `SUSPECT/REMOVED` 与保活时间字段；`ucn_node.h` 增加保活时间宏和统计；`src/ucn_node.c` 增加 8 B Heartbeat 请求/ACK、认证成功业务帧刷新 `last_seen`、Driver Down 立即移除、超时巡检和注销逻辑；新增 `tests/test_neighbor_heartbeat.c` 并接入 CMake/测试入口。
- 验证：新测试覆盖双向保活、正常业务刷新、静默丢包后 3 s 进入 `SUSPECT`、4 s 移除、Close 回调、`peer_node_id` 清零及新节点复用旧槽。实际执行 `cmake --build build --clean-first --parallel` 和 `ctest --test-dir build --output-on-failure`，结果 `100% tests passed, 0 tests failed out of 1`。
- 结论/限制：Heartbeat 是 v2 内的 8 B 一跳控制帧，Core 在设置 Security Provider 时会先经过既有授权回调；它不是生产级身份/AEAD 实现。随机退避、令牌桶、Adapter 地址池和真实 ESP32 离线时间仍属 T18 后续/实机工作。

### OP-039｜实现静态 Endpoint 命名空间与固定分发（R1.4）

- 操作：将最新方案中的 `0x40～0xBF` 静态 Endpoint 落为公共 C API，使业务无需增加帧字段即可按 Endpoint 区分同一节点上的多类数据。
- 修改：新增 `include/ucn/ucn_endpoint.h`、`src/ucn_endpoint.c`，提供静态 Endpoint/控制消息判断；`ucn_node.h/.c` 增加 `ucn_node_send_endpoint()`、固定上限 `ucn_node_set_endpoint_handler()` 表和“Endpoint 优先、通用 RX 回调回退”的接收分发；新增 `tests/test_endpoint.c` 并接入 CMake/测试入口。
- 验证：新测试覆盖 Endpoint 范围、控制编号隔离、专用发送 API、通用 v2 发送兼容、IMU/气压计分别分发，以及同目标不同 Endpoint 的 Q1 `latest_value` 覆盖隔离。实际执行 Debug 构建与 CTest，结果 `100% tests passed, 0 tests failed out of 1`。
- 结论/限制：本轮只冻结协议层命名空间，不擅自定义 IMU/气压计等产品 ABI，也未实现 Endpoint ACL 或动态服务目录；这些仍需要产品编号表、访问策略和 T15 安全模型共同冻结。

### OP-040｜实现受限候选选路并完成 R1 复核（R1.3/R1.5）

- 操作：在不改变 v2 32 B 普通业务帧的前提下，实现固定 Candidate 路由表、后台刷新、Cost 滞回、`PATH_PROBE/ACK` 三次验证和 `PATH_ACTIVATE/ACK` 中继激活；随后执行三套构建测试，并逐项复核本轮任务。
- 修改：`ucn_node.h/.c` 新增 Candidate 固定表、刷新/探测/切换参数与统计、`ucn_node_refresh_route()`、候选 RREQ 标志、中继 Candidate 转发表、Probe/Activate 控制处理以及 Q0/Q1 优先的控制调度；业务 API 拒绝 Core 控制报文。新增 `tests/test_candidate_route.c`，并增强 Heartbeat 测试覆盖 SUSPECT 恢复。`docs/00-项目管理/00-任务表.md` 标记 R1.3/R1.5 完成、新增 R1 更新后复核和 T20 首包自动寻路待办。
- 验证：四节点虚拟拓扑覆盖 A-B-C Active 与 A-D-C Candidate、同 Cost 拒绝、刷新窗口自动发现、Candidate 不影响旧业务、Q0 先于 Probe、ACK 丢失超时回退、三次 ACK 后 Activate 和切换。实际执行 Debug、Release 与 `UCN_MAX_FRAME_BYTES=64` Profile 的 `cmake --build ... --clean-first --parallel` 和 `ctest --test-dir ... --output-on-failure`，每套均为 `100% tests passed, 0 tests failed out of 1`。
- 结论/限制：R1 是 v2 的受限控制面切换，不含 v3 `route_epoch`/grace 的在途业务区分，也没有生产 AEAD、未知 Cost 保守策略、控制令牌桶或真实介质测量。首包 Q1 自动寻路/等待被单列为 T20，Q0 永不等待寻路。

### OP-041｜建立 UCN v3 全面升级任务表

- 操作：按“全面更新为 v3、先写任务表再实施”的要求，重新冻结本轮为破坏性协议升级，而不是把既有 v2 控制面误称为 v3。
- 修改：`docs/00-项目管理/00-任务表.md` 新增 V3.0～V3.7，依次覆盖 v3 线格式、Route Epoch/grace、Activate 控制面、可选端到端受保护帧、Node/Endpoint 安全策略、Q1 首包自动寻路和全量验证；明确每项的单元/模拟验收与 v2 拒绝规则。
- 验证：已对照当前 `include/ucn/ucn_types.h` 的 `UCN_PROTOCOL_VERSION = 2`、`src/ucn_frame.c` 的固定 32 B 编解码、R1 Candidate 逻辑和安全建议中的 36 B/Tag 目标，确认任务项对应现有差距。
- 结论/限制：本轮可完整交付 v3 Core、固定资源策略和可插拔 AEAD Provider 契约；真实密钥供应、经审计的目标平台 AEAD 实现、ESP32 Adapter 和实机资源/空口数据仍必须在 T14/T15 硬件门禁中单独验证，不能虚构为已经完成。

### OP-042｜完成 UCN v3 Core 全面升级与三档验证

- 操作：按 V3.0～V3.7 的顺序，将现有 v2 Core 破坏性升级为 v3，并完成帧、路由、策略、安全 Provider、自动首包和文档复核。
- 修改：`UCN_PROTOCOL_VERSION` 升为 3，库版本升为 `3.0.0`；`ucn_frame` 支持 32 B 基础头、36 B Route Extension、`ROUTE_EXTENSION=0x01`、`E2E_PROTECTED=0x02`、16 B Tag、变长 CRC 和 26 B 不可变 AAD，v2/未知 Flag 显式拒绝。`ROUTE_REPLY` 非 Candidate 载荷从 16 B 升为 18 B 并携带初始 Epoch；`PATH_ACTIVATE/ACK` 从 4 B 升为 6 B 并携带 Candidate ID + Epoch。`ucn_node` 增加 Current/Previous Epoch、默认 1 s grace、Candidate 激活保留旧出口、按 `(destination, epoch)` 转发、保守未知 Cost（1000）、源端控制 Token、Endpoint Q1 固定 4 槽/1 s 自动寻路等待。
- 修改：`ucn_security_ops_t` 增加可选选择、`seal/open` 回调；新增 Node 默认与固定 Endpoint 覆盖策略（明文/受保护/自动、接收要求、透明转发模式）。目标节点解密，普通中继不持有 Key 也可按路由头透明转发密文；控制帧标记为端到端受保护会被拒绝。Core 不提供自制密码算法。
- 测试：新增 `tests/test_v3.c`，覆盖 v3 32/36 B、Tag/CRC、v2 拒绝、AAD 不包含 Hop/Epoch、A→B→C 无 Key 中继、坏 Tag、`ENCRYPTED_ONLY`、`OPAQUE_E2E_ONLY`、Endpoint Q1 自动寻路/Latest-Value 合并/表满/超时/Q0 不等待/Token 拒绝；`test_candidate_route.c` 增加实际旧/新 Epoch 断言；`test_link_metrics.c` 改为验证未知 Cost 不压过已测量低 Cost。
- 验证：实际执行 `cmake --build build --clean-first --parallel && ctest --test-dir build --output-on-failure`、同样的 `build-release`，以及 `build-mtu64`（`UCN_MAX_FRAME_BYTES=64`）三套构建；三者均为 `100% tests passed, 0 tests failed out of 1`。文档本地链接检查输出 `Document local links: PASS`。
- 结论/限制：已完成的是 MCU-first、固定内存的 v3 Core 和虚拟拓扑验证，不依赖 Linux。真实 ESP32/ESP-NOW、CAN/BLE/LoRa Adapter、生产身份与经审计 AEAD Provider、密钥轮换/撤销/产品 ACL、RAM/Flash/栈/CPU/空口/EMC 数据仍未完成，保留 T14/T15/T16 实机门禁；不得把测试 Provider 或虚拟 Link 表述成生产安全或真实无线性能。

## 2026-08-08

### OP-043｜补充 v3 心跳范围与多路径并行边界

- 操作：根据“心跳是否全网发送、转发节点是否收到心跳、两条路径是否彼此等待”的问题，复核当前 v3 Core 的心跳发送/接收、业务转发与本地发送调度。
- 修改：`docs/02-总体架构/UCN_整体架构设计.md` 新增控制帧送达范围表和路径并行性说明；明确 `HELLO` 为一跳发现广播、`HEARTBEAT` 为已准入直连邻居单播且 `hop_limit=1`、`ROUTE_REQ` 才可受限扩散、业务只沿当前下一跳转发。文档用 A→B→C 示例明确中继 B 不会转发 A→B 的心跳。`docs/04-路由与链路/UCN_节点入离网与网络稳定性建议.md` 同步一跳保活、多跳健康判断、Q0/Q1 与 Heartbeat 调度顺序、共享中继/共享空口造成的实际竞争。`docs/00-项目管理/00-任务表.md` 新增 DOC-001 作为本次文档同步台账。
- 验证：对照 `src/ucn_node.c`：`send_due_heartbeat()` 遍历已准入邻居、以该邻居 Link 单播 8 B 请求；`handle_heartbeat()` 要求目标为本机、`hop_limit=1` 且从对应直连 Link 入站，并在同 Link 回复 ACK；`ucn_node_step()` 每次先处理一个 Q0、再一个 Q1，队列为空后才处理到期 Heartbeat/Probe/刷新。未修改 Core、线格式、测试或硬件驱动。
- 结论/限制：UCN 没有全网心跳或全局发送锁。不同路径可逻辑并行，但共享中继会按帧处理，ESP-NOW/WiFi 同信道会共享空口；真实吞吐、时延、队列溢出和 Heartbeat 在压力下的表现仍需 T14 多板实机测试，不能由 Core 行为直接承诺。

### OP-044｜更正节点彻底离网后的局部恢复流程

- 操作：针对“节点彻底离开后网络与原路径节点会做什么”的问题，复核 v3 的存活检测、Link/路由回收和 `ROUTE_ERROR` 发送/接收路径，并检查既有稳定性文档是否仍保留 v2 时代的陈旧结论。
- 修改：`docs/04-路由与链路/UCN_节点入离网与网络稳定性建议.md` 将“没有业务经过 C 就不会主动知道”的旧表述替换为当前实际流程：C 的直连邻居由 Driver Down 或 Heartbeat/有效帧超时先发现，默认 3 s 进入 SUSPECT、从最后有效帧起约 4 s 移除；移除只清本地以该 Link 为出口的动态 Active/Previous/Candidate 路由并关闭/解绑 Link。若上游业务随后仍到达失效中继，才回送 `ROUTE_ERROR(C)` 并使路径相关节点逐跳清除到 C 的动态/候选表项；没有全网离网广播。`docs/00-项目管理/00-任务表.md` 新增 DOC-002。
- 验证：对照 `maintain_neighbor_liveness()` 的 Down/3 s/4 s 分支、`remove_neighbor_entry()` 的动态路由失效/Link close/Node ID 清零、`send_route_error()` 的上游单播，以及 `handle_route_error()` 的按不可达目标清理。现有虚拟测试分别覆盖 Heartbeat 状态转换/槽复用和 B→C Link Down 时 B 回送 RERR、A 清路由；本次未修改 Core、线格式、测试或硬件驱动。
- 结论/限制：离网恢复是局部、按需的，不会使无关节点全网波动。静态路由不是动态路由自动回收的对象；真实 ESP-NOW Driver Down 的即时性、Heartbeat 检测时延、RERR 在多跳实际空口的收敛和替代路径成功率仍需 T14 多板实机验证。

### OP-045｜建立同对端多介质主备 Link 建议与 T21

- 操作：针对“一个节点同时拥有 WiFi、UART、CAN 等多种通信方式时如何处理不同路径”的问题，区分跨介质多跳路由、同对端直连多 Link、自动邻居准入和业务主备切换四个层次；并按架构决策流程检索本地知识库，未发现 UCN 专项同对端多介质设计，故以当前 Core 源码为最终依据。
- 新增：`docs/04-路由与链路/UCN_多介质同对端主备_Link_建议.md`。文档冻结 T21 的建议边界：一个 Neighbor 持有固定 Bearer 数组（建议默认最多 2）、每 Bearer 独立认证/心跳/Cost、正常单主单备、硬故障下一帧切备链、质量切换使用滞回/Probe、全部 Bearer 失效才移除 Neighbor/RERR；明确不做帧复制、负载均衡、经典 CAN 分段或 Linux 依赖。
- 修改：`docs/02-总体架构/UCN_整体架构设计.md` 在 Adapter 边界中明确当前跨介质转发已具备、同 Node ID 自动多 Bearer 仍未实现，并链接新建议；`docs/00-项目管理/00-任务表.md` 新增 T21 与 DOC-003。
- 验证：对照 `find_direct_link()`，静态注册的同对端多 Link 选择最低 `route_cost`；对照 `forward_route_request()`，RREQ 向可用 Link 扩散并累计 Cost；对照 `ucn_node_observe_neighbor()`，已 ADMITTED Neighbor 从不同 Link 再出现会返回 `UCN_ERR_CONFIG`。确认当前 Active/Candidate/Previous 不是业务复制或负载均衡。未修改 Core、线格式、测试或硬件驱动。
- 结论/限制：现有 Core 已具备跨介质统一路由的基础，但尚不具备同对端自动冗余。T21 必须先完成 C99 固定表/回归测试，再以 ESP-NOW+UART 或 CAN-FD 多板实测验证；在此之前不得承诺无缝切换或实际多介质冗余效果。

### OP-046｜沉淀策略路由、固定主备路径与可选负载均衡建议

- 操作：根据“部分业务必须走指定路线、路线断开才允许自动找替代，以及是否可选择自动分担负载”的需求，复核 v3 当前 Route/静态路由/Endpoint 发送和 RERR 行为；按架构决策流程检索本地知识库，未发现 UCN 专项策略路由笔记，故以当前 Core 源码为最终依据。
- 新增：`docs/04-路由与链路/UCN_策略路由与可选负载均衡建议.md`。文档冻结四种后续策略模式：默认 `AUTO_BEST`、`PINNED_STRICT`、只在硬故障切换的 `PINNED_FAILOVER`、默认关闭且按流亲和的 `AUTO_BALANCE`；提出固定容量的 Policy/Path/Flow 表、短 `Path ID`、受认证安装/撤销控制面和 Path 范围 RERR。明确不采用逐帧轮询、隐式复制、裸物理 Link API 或 Linux 依赖。
- 修改：`docs/02-总体架构/UCN_整体架构设计.md` 增加 T22 的未实现边界，并明确 T21 只处理同一下一跳的 Bearer 主备，不能代替跨中继的业务策略/负载均衡；`docs/00-项目管理/00-任务表.md` 新增 T22 与 DOC-004，并将新建议加入架构依据。
- 验证：对照 `find_direct_link()` / `find_link()` 的直连优先和单出口查询、`ucn_node_add_route()` 的“目标→本节点 egress”静态路由、`ucn_node_send_endpoint()` 的无策略参数 API、Active/Candidate/Previous 的单目标切换，以及当前仅含不可达目标的 RERR 处理。确认 `route_epoch` 只用于 Current/Previous grace，不能复用为业务 Path ID；确认当前没有按 Endpoint 的路径策略或自动负载均衡。本次未修改 Core、线格式、测试或硬件驱动。
- 结论/限制：该能力是 T22 后续设计，必须在 T21 后冻结 Path ID 线格式、ACL/AAD 保护、固定 RAM 上限和 Q0/Q1 规则，再经单测、虚拟拓扑、三档构建和多板实测验收；在此之前不得称 v3 已支持“指定完整路径”或“自动负载均衡”。

### OP-047｜建立 UCN Obsidian 知识库与后续同步约束

- 操作：按“为 UCN 建一份知识库，后续更新同步”的要求，依照 Vault 的源码知识库规范对当前 `include/`、`src/`、`tests/`、`docs/` 和 CMake 进行只读梳理，并在 `C:\Users\y2389_4rq4ld9\Documents\Obsidian Vault\知识库\UCN` 建立独立知识库。
- 新增：知识库根入口 `README.md`、`00-任务表.md`、`01-操作记录.md`、源码阅读路线、MCU-first 总体架构、构建与配置、帧与安全、Node/邻居/路由、Adapter、测试、设计路线图、关键结构体索引、术语表和源码地图；将 T21 同对端 Bearer 主备和 T22 策略路由/可选均衡明确标注为后续设计。
- 修改：本项目 `docs/00-项目管理/00-任务表.md` 新增 DOC-005；约定以后任何协议、源码、测试、架构或建议文档的实际更新，在项目文档记录后必须同步受影响知识库笔记，并在知识库任务表/操作记录留下验证证据。
- 验证：本次实际执行 `ctest --test-dir build --output-on-failure`，结果 `100% tests passed, 0 tests failed out of 1`；该结果来自 C99 虚拟测试。知识库的 Obsidian 内链和绝对源码路径检查结果记录在知识库 `01-操作记录.md`，不得将其替代真实 Adapter/生产安全或多板性能验证。
- 结论/限制：知识库用于持续追踪当前源码和设计边界，源码仍是最终事实；当前项目不是 Git 仓库，知识库快照以源码路径、版本号、日期和验证记录标识。真实硬件/资源结论仍需按 T14/T15/T16/T21/T22 的实测门禁更新。

### OP-048｜实现 v3 按需路径追踪诊断（T23）

- 操作：按已冻结的 T23 建议，将“查询一次实际经过哪些 Node”的能力实现为 Core 的按需诊断，不改变正常业务帧、Route Cache 或业务 Path 选择。
- 修改：`ucn_types.h` 新增 `UCN_FRAME_FLAG_DIAGNOSTIC=0x04`、`PATH_TRACE_REQ=0x18`、`PATH_TRACE_REPLY=0x19`；`ucn_endpoint.c` 将其归为控制帧；`ucn_node.h/.c` 增加由帧长/跳数推导的 Trace Node 上限、默认 2 槽 Pending/4 槽 Reverse/1 s 超时、独立 1 次/秒诊断 Token、结果回调和统计。Request 使用当前直连/Route Cache（不触发 RREQ），中继逐跳追加 Node ID 并仅保存 `(origin, trace_id) → ingress_link` 短期项，Reply 反向返回；控制 Trace 带 E2E Flag、格式异常或未注册 Link 均拒绝。`record_limit=0` 使用编译期最大值，非零值可主动产生截断结果。
- 测试：新增 `tests/test_path_trace.c` 并纳入 CMake/测试入口；覆盖 A→B→C 的 `[A,B,C]` 回调、独立 Token 限速、调用方截断、中继无下一跳、TTL、Pending 槽满和本地超时、E2E Trace 拒绝、未注册 Link 拒绝。`test_frame.c` 新增 `DIAGNOSTIC` 编解码与未知 Flag 拒绝回归。
- 文档/知识库：新增 `docs/04-路由与链路/UCN_路径追踪诊断建议.md`，同步整体架构、协议档案、更新后设计方案、任务表和知识库 Trace 专题、测试地图、结构体索引、术语、源码地图与操作记录。
- 验证：实际执行 `cmake --build build --clean-first --parallel`、`cmake --build build-release --clean-first --parallel`、`cmake --build build-mtu64 --clean-first --parallel`；每套均执行 CTest 和 `ucn_tests.exe`，结果均为 `100% tests passed, 0 tests failed out of 1` 且输出 `All UCN tests passed.`。项目文档本地链接检查和 UCN 知识库 Obsidian 内链检查均通过。
- 结论/限制：T23 Core 与 C99 虚拟拓扑闭环已完成；它只报告一次请求的路径快照，不锁定或平衡后续业务路径，也不提供拓扑保密/产品 Trace ACL。ESP-NOW/WiFi/CAN/UART 的真实路径、时延、断链、重组网、RAM/Flash/栈和空口开销仍须按 T23.5 至少三板实测，不能由本次结果替代。

### OP-049｜冻结 v3 快照并实现 v4 按需节点快照诊断（T24）

- 操作：按“先备份 v3、再更新 v4”的要求，在非 Git 工程中先建立独立快照 `E:\File\MESH\UCN_Backups\ucn-v3.0.0-before-v4-20260808`，包含 `CMakeLists.txt`、`include/`、`src/`、`tests/`、`docs/` 共 52 个文件、746,258 B；其 `CMakeLists.txt` SHA-256 为 `D51016624F24B269C238E892DE366DD6D08956FBC0C24D4601D820F92B361A1E`。随后才修改主工程。
- 修改：CMake/库版本升至 `4.0.0`，`UCN_PROTOCOL_VERSION` 升至 4，v3 帧明确拒绝；新增 `NODE_SNAPSHOT_REQ=0x1A`、`NODE_SNAPSHOT_REPLY=0x1B` 并归入控制帧。`ucn_node.h/.c` 增加固定 8 结果、1 Pending、2 Reverse、2 Reply Queue、2 s 窗口、突发 1/10 s Token、0～200 ms 确定性 Reply 抖动、统计、回调 API 和默认拒绝的 `ucn_node_set_node_snapshot_authorizer()`。
- 行为：管理 Node 发送一次受限广播；每个被授权节点依靠 Seen Cache 只处理/转发一次，建立 `(origin, query_id)` 临时 Reverse，排队自己的 Reply；Reply 沿 Reverse 回源，源端按 Node ID 去重并在窗口结束一次性回调。结果只含 Node ID 和当前直接注册 Link 数，不保存永久拓扑、不触发业务 RREQ、不给普通 Q0/Q1 增加字段。
- 测试：新增 `tests/test_node_snapshot.c`，覆盖 A→B→{C,D} 收集、回复回程、结果截断和默认 ACL 拒绝；`test_v3.c` 新增 v3 注入拒绝。实际执行 Debug、Release、64 B Profile 的 `cmake --build ... --clean-first --parallel`、CTest 与 `ucn_tests.exe`，三套均为 `100% tests passed, 0 tests failed out of 1` 且输出 `All UCN tests passed.`。
- ESP 构建：ESP 测试工程最初因显式 v3 Static Assert 正确拒绝 v4；同步门禁、测试架构和 v4 全量测试计划后，ESP32-S3 与 ESP-WROOM-32 两个 PlatformIO 环境均成功构建。未烧录、未进行真实无线节点快照。
- 文档/知识库：新增 [v4 节点快照诊断](../08-实现与验证/版本演进/UCN_v4_节点快照诊断.md)，同步整体架构、协议档案、更新后设计方案、任务表和 ESP 测试计划；项目文档本地链接 46 条、ESP 测试文档链接 11 条、知识库 Obsidian 内链 171 条均为 0 缺失。
- 结论/限制：v4 Core 只证明固定容量 C99 虚拟闭环。五板 ESP32 的收集率、窗口时延、Reply 冲突、空口负载、断链/分区/重组网、RAM/Flash/栈，以及生产 Snapshot ACL 均保留 T24.5 实机门禁；不因本次 CTest 或编译成功而宣称已验证真实全网查询。

### OP-050｜首次发布 UCN v4 Core 到 GitHub

- 操作：按“先发布当前 UCN，`docs/` 暂不上传”的要求，将非 Git 的主工程初始化为 Git 仓库，接入 `https://github.com/ice-infinite/UCN.git` 的已有 `main`（远端初始提交仅含 MIT `LICENSE`），并保留该许可证历史。
- 修改：根目录新增公开项目入口 `README.md`，说明 MCU-first、v4 Core、构建方式和真实硬件/生产安全边界；新增 `.gitignore`，明确忽略 `/docs/`、`/build*/` 和 `.codex_docx_review/` 等本地内容。
- 发布：提交 `6fef455 feat: publish UCN v4 core` 已推送至 `origin/main`。提交内容为 `.gitignore`、README、CMake、`include/`、`src/` 与 `tests/`；`docs/` 未暂存、未跟踪、未推送。
- 验证：推送前执行 `git diff --cached --check`、已暂存文件/忽略规则核对和凭据样式扫描，均无异常；执行 `cmake --build build --parallel` 与 `ctest --test-dir build --output-on-failure`，结果为 `100% tests passed, 0 tests failed out of 1`。
- 结论/限制：GitHub 当前是可继续开发的 v4 Core 源码基线；本地 `docs/` 仍保留，后续只有用户确认的文档才使用 `git add -f docs/<文件>` 单独纳入提交。真实多板 T24.5 与生产安全边界不因本次发布改变。

### OP-051｜新增 V4 协议核心说明并同步知识库

- 操作：按“单独写一份文档，重点介绍协议主要做什么、怎么做”的要求，先对照当前 v4 帧定义、Node 控制/转发路径、Endpoint、Adapter 以及既有整体架构和节点快照资料，再写入面向使用者的独立说明。
- 新增：`docs/08-实现与验证/版本演进/UCN_v4_协议核心说明.md`。文档以“MCU 自组网统一通信 Core”为主线，分开说明 Node/Endpoint、HELLO/Heartbeat、AODV-Lite/Route Cache、Active/Candidate/Route Epoch、通用 `route_cost`、Q0/Q1、端到端受保护透明转发、Trace/Snapshot、Adapter/Host 边界和固定资源原则。
- 修改：`docs/00-项目管理/00-任务表.md` 增加 DOC-008，并将新说明纳入架构依据；同步 Obsidian 知识库的总体架构入口、README 导航、任务表和操作记录。
- 验证：对照 `include/ucn/ucn_types.h` 的 v4/控制帧/Flag，`src/ucn_frame.c` 的 32/36 B 编解码和受保护帧，`src/ucn_node.c` 的 HELLO、Heartbeat、Route、Endpoint、Trace、Snapshot 分发，以及 `src/ucn_adapter.c` 的介质边界。随后执行项目 Markdown 链接与知识库 Obsidian 内链/绝对源码路径检查。本次只改文档，不改 Core、测试或 ESP 固件；既有虚拟 CTest 结果不被写成实机结果。
- 结论/限制：V4 已有固定资源的 C99 MCU 自组网 Core 闭环和虚拟拓扑证据；真实 ESP-NOW 多跳/无线性能、生产身份与 AEAD、经典 CAN Carrier、T21 多 Bearer 主备、T22 策略路由/负载均衡仍需独立实现和实机验收。

### OP-052｜公开完整 docs 文档集到 GitHub

- 操作：按“将 docs 文档上传到 GitHub”的要求，清点 `docs/` 内容，检查 Git 忽略规则、公开入口和敏感信息，再将整个目录与 README 导航提交到现有 `origin/main`。
- 修改：移除 `.gitignore` 中的 `/docs/` 忽略规则；根 `README.md` 改为说明 `docs/` 已公开，并链接 [UCN v4 协议核心说明](../08-实现与验证/版本演进/UCN_v4_协议核心说明.md)。完整发布 16 份 Markdown 与 1 份 DOCX，总计 478,995 B；不含构建目录、固件产物或第三方依赖。
- 安全筛查：对 Markdown 与 DOCX 正文/核心属性扫描私钥、GitHub Token、AWS Key、API Key、密码和 Secret 模式，结果均为 0 命中。`git diff --cached --check` 提示既有 Markdown 的双空格换行和少量历史尾空白；双空格为 Markdown 换行语义，未批量改写历史文档。
- 发布：提交 `39e8147 docs: publish UCN v4 architecture and design records`，已执行 `git push origin main`，远端从 `6fef455` 前进到 `39e8147`。
- 验证：推送成功后工作树无未提交的源码/公开文档改动；本次仅发布文档与 README/.gitignore，不改 C99 Core、测试、ESP 固件或协议线格式。知识库同步记录与本次 OP-052 随后另行提交。

### OP-053｜冻结节点内任务通信建议与 T25

- 操作：根据“任务是否可作为 Node、任务间通信是否可直接使用协议”的讨论，核对当前 Endpoint 注册、收包分发和协议任务执行边界，先记录后续实现方案，不改现有 Core。
- 新增：`docs/05-传输与服务/UCN_节点内任务通信建议.md`。冻结“一个 MCU 为一个 UCN Node，任务为静态 Endpoint/Service”的模型；定义本机直投与远端发送统一语义、协议任务独占 `ucn_node_t`、固定 Service 表/Inbox/TX Request Queue、Q0/Q1 投递规则、Payload 所有权、IMU/控制/舵机边界，以及 T25.1～T25.4 的单元、虚拟和 ESP32 实机门禁。
- 修改：更新整体架构、协议分层与配置档案、更新后设计方案、V4 核心说明和任务表（T25、DOC-010），并同步 UCN Obsidian 知识库。明确当前 `ucn_node_set_endpoint_handler()` 只是 Endpoint 到协议任务回调，不是 FreeRTOS 任务消息总线。
- 验证：对照 `include/ucn/ucn_node.h` 的固定 `endpoint_handlers` 与 `ucn_node_set_endpoint_handler()`，以及 `src/ucn_node.c` 的 `dispatch_endpoint()` / `ucn_node_receive()`：目标业务帧确实在 Endpoint 命中后直接调用回调；当前没有 Queue、Task Handle 或本机直投 API。本次未修改 C99 Core、线协议、CTest 或 ESP 固件，因此不把既有构建/虚拟测试写作 T25 验证。
- 结论/限制：T25 保持 MCU-first、固定资源和现有帧格式；它不让每个任务入网、不新增心跳/路由/Node ID。必须先完成纯 C Router 与 FreeRTOS Port 的单测和虚拟拓扑，再用 ESP32 测量队列、栈、RAM、时延和本地失联安全行为。

### OP-054｜建立路由策略与负载均衡执行建议及 T22 子任务

- 操作：根据“下一步做路由策略和负载均衡”的需求，核对 v4 Core 的通用 `route_cost`、单目标 Active/Candidate/Previous、20% Cost 滞回、Probe/Activate、`route_epoch` 和 RERR，以及 ESP32 双介质测试工程的 UART 优先/ESP-NOW 回退行为；在既有策略设计基础上增加可直接执行的分阶段建议。
- 新增：`docs/04-路由与链路/UCN_路由策略与负载均衡执行建议.md`。文档明确 Bearer、端到端 Path 与业务 Flow 三层边界；冻结 MCU-first、固定容量、按需控制面、Q0 不自动均衡、Q1 流亲和且不逐帧轮询等约束；定义 Quality/Cost 输入、`AUTO_BEST`/`PINNED_STRICT`/`PINNED_FAILOVER`/`AUTO_BALANCE`、独立受认证 Path ID、Path 范围 RERR、受限诊断和五板验证拓扑。
- 修改：`docs/00-项目管理/00-任务表.md` 将 T22 改为父任务，新增 T22.0～T22.7：资源/边界冻结、固定 Policy/Path/Flow 与质量输入、Path ID 控制面、固定主备、Q1 流亲和均衡、Bearer 联动、诊断、三档/虚拟/多板验收；原策略建议增加执行文档入口，并登记 DOC-011。
- 验证：对照 `include/ucn/ucn_link.h` 的 `get_metrics()`、`include/ucn/ucn_node.h` 的固定 Route/Candidate 容量与时间宏、`src/ucn_node.c` 的 Candidate Cost 门限/Active-Previous 切换/按目标 RERR，以及 ESP32 `DualMediaLink::send_unicast()` 的实际 UART 优先与 Wi-Fi 回退。此次只更新建议、任务和知识库同步资料，未修改 C99 Core、线格式、CTest、PlatformIO 固件或实机状态。
- 结论/限制：v4 当前仍不具备受认证 Path ID、按 Endpoint 的完整路径策略、路径范围 RERR、流绑定或自动均衡；T21 是 T22.2/T22.5 的硬前置。未完成 T22.7 的虚拟与真实多板验收前，不宣称已支持无缝切换、最优策略或真实负载均衡性能。

### OP-055｜实现 T21 固定 Bearer 邻居与第一阶段主备

- 操作：按任务表的依赖顺序启动 T21，先把同一 Node ID 的多个物理 Link 从“第二条配置冲突”改为固定 Bearer 集；没有修改 UCN 线格式、业务发送 API、Adapter 契约或引入动态内存。
- 修改：`include/ucn/ucn_neighbor.h` 新增默认 `UCN_MAX_BEARERS_PER_NEIGHBOR=2`、Bearer 状态/存活字段和 Primary 索引；`src/ucn_node.c` 改为逐 Bearer 自动准入、Provider 授权、Heartbeat、`last_seen`、Down 与回收。正常业务和缓存 Route egress 会解析到同一 Neighbor 当前 Primary；Primary 明确 Down 后下一帧使用健康 Backup，全部 Bearer Down 才注销全部 Link、清理动态 Route 并移除 Neighbor。泛洪候选仅允许当前 Primary Bearer。
- 测试：新增 `tests/test_neighbor_bearer.c` 并接入 CMake/测试入口，虚拟验证双 Bearer 自动入网只占一个 Neighbor 槽、第三条容量拒绝、第二 Bearer Provider 拒绝、Primary Down 后 Backup 收发、全 Down 后两条 Link 回收；原有单 Link 邻居/Heartbeat/路由/安全/Endpoint 回归同时保留。
- 验证：改动前后均执行 Debug `ctest`；改动后执行 `cmake --build build --parallel && ctest --test-dir build --output-on-failure`、同等 Release 与 64 B Profile，以及 `-DUCN_MAX_BEARERS_PER_NEIGHBOR=1` 的单 Bearer Profile，四组均为 `1/1` 通过。`git diff --check` 通过；本次未烧录 ESP32、未测实际 WiFi/UART/CAN 切换时延、RAM/Flash/栈或功耗。
- 结论/限制：T21.1/T21.2 已完成，T21.3 仅完成初选和硬 Down 主备切换；不含质量 EWMA、20%～30%滞回、Probe/ACK、切换统计、A→B→C Route/RERR 专项或实机多板验收。上述未完成项保留在 T21.3～T21.6，T22 仍不得开始编码。

### OP-056｜完成 T21.3 Bearer 质量滞回与 Probe/ACK 虚拟闭环

- 操作：在 T21.1/T21.2 的固定双 Bearer 与硬 Down 切备之上，完成同一 Neighbor 内的质量切换；明确不复用端到端 `PATH_PROBE`，也不新增控制帧或业务帧字段。
- 修改：`ucn_neighbor_bearer_t` 增加固定的质量采样/Probe 状态，`ucn_node_stats_t` 增加 Probe 发送、匹配 ACK、质量切换计数；Core 将现有一跳、8 B `HEARTBEAT` request/ACK 定向发送到候选 Bearer。默认候选 Cost 必须低至少 20%、连续 3 个 500 ms 窗口成立，并取得 2 次匹配 ACK（最多 3 次、100 ms 间隔）后才改 Primary；Primary Driver Down 仍立即改选健康 Backup。所有值都可通过编译期宏覆盖，Core 不读取 RSSI/UART/CAN 私有指标。
- 调度与边界：`ucn_node_step()` 先处理 Q0/Q1 待发业务，队列为空时才处理 Heartbeat 和质量 Probe，故 Probe 不抢占 Q0；Probe 只在当前候选直连 Link 上收发，不经中继，不改变 Route Epoch 或端到端 Candidate 状态。Adapter 继续负责把平滑后的真实质量提供为统一 `route_cost`。
- 测试：扩展 `tests/test_neighbor_bearer.c`，覆盖 5% Cost 抖动不探测/不切换、显著更优但连续 3 个 ACK 丢失仍保持 Primary、恢复 ACK 后重新满足采样并在第 2 次 ACK 后切换；既有双 Bearer 准入、容量、Provider、硬 Down 与全 Down 回收测试均保留。
- 验证：执行 Debug、Release、64 B MTU 和 `-DUCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CMake Profile 的构建与 CTest，均为 `1/1` 通过。未烧录 ESP32，未测真实 ESP-NOW+UART/CAN 切换时延、丢失/乱序、RAM/Flash/栈增量或功耗。
- 结论/限制：T21.3 已完成 C99/虚拟测试闭环，但这不是链路聚合、业务复制或真实“无缝”保证。T21 后续先做 A→B→C Route/Candidate/RERR 专项（T21.4），再做目标板资源与多介质多板验收；T22 仍保持未编码。

### OP-058｜完成 T21.5 目标板静态资源比较与运行时观测接入

- 操作：保持主库 Core 为唯一来源，在 `E:\File\PlatformIO\ESP32_UCN\ESP32S3_N16R8_UCN_Test1` 新增仅供资源取证的 S3 `resource_bearer1`、`resource_bearer2` Profile，并将周期性堆/栈观测加入正常测试固件；不改变 Core 帧格式、业务 API、邻居状态机或双介质选路语义。
- 构建与静态证据：两个 S3 Profile 均从本仓库的 `src/*.c` 直接构建成功。`Bearer=1` 为 RAM `42,852 / 327,680 B`、Flash `582,183 / 6,553,600 B`；默认 `Bearer=2` 为 RAM `43,108 B`、Flash `582,679 B`。ELF 符号 `g_node` 从 `0x13b0`（5,040 B）增至 `0x14b0`（5,296 B），`.dram0.bss` 同样从 26,880 B 增至 27,136 B，因此第二 Bearer 的静态增量为 256 B RAM；整机 Flash 增量为 496 B。正常可烧录 `node_a` Profile 复建结果与 `Bearer=2` 完全一致。`esp32_wroom_32` 也构建成功，为 RAM `45,216 / 327,680 B`、Flash `609,103 / 1,310,720 B`。
- 运行时观测：标准 ESP 测试固件每个统计周期额外输出 `RESOURCE` 行，包含内部/8-bit heap 的当前与启动以来最小剩余值，以及当前 Arduino `loopTask` 的剩余栈高水位（words/bytes）。这为后续空闲、ESP-NOW、UART-only、双介质回退与断链场景提供统一采样口；Wi-Fi/ESP-NOW 驱动任务拥有独立栈，仍需在产品任务配置下另行测量。
- 边界：上述 RAM/Flash 是包含 Arduino、Wi-Fi/ESP-NOW、UART Adapter、吞吐 Endpoint 与日志的整机测试固件，不是纯 Core 数字。当前 ESP `DualMediaLink` 对 Core 仍只暴露一个逻辑 `ucn_link_t`，所以本次证明的是固定双 Bearer Core Profile 的静态代价，**不是**实际双 Adapter/Bearer 切换已验收。完整口径写入 [T21 目标板资源报告](../08-实现与验证/版本演进/UCN_T21_目标板资源报告.md)。
- 验证：执行 `pio.exe run -e esp32s3_120_16_8-qio_opi_resource_bearer1 -e esp32s3_120_16_8-qio_opi_resource_bearer2 -e esp32s3_120_16_8-qio_opi_node_a -e esp32_wroom_32` 均成功；使用目标 toolchain 的 `nm -C -S --size-sort` 和 `size -A` 复核 `g_node` 与段增量。未烧录本次镜像，未采集 `RESOURCE` 实机行。
- 结论/下一步：T21.5 的可复现静态报告与观测代码已完成，但任务保留进行中，直到取得真实峰值堆/栈日志。T21.6 应先将 ESP-NOW 与 UART/CAN 以同一 Node ID 的两个独立 Adapter Link 接入，再测实际切换时延、丢失/乱序、资源和功耗；T22 仍未编码。

### OP-057｜完成 T21.4 Route/Candidate/RERR 与 Bearer 联动虚拟闭环

- 操作：按 T21.4 验收顺序，将当前 Route/Previous 的 egress 解析模型扩展到 Candidate 控制面；目标是一个物理 Bearer Down 时不把“经同一 Neighbor 的逻辑路径”误判为失效，且不混入 T22 的 Path ID 或策略路由。
- 修改：`find_candidate_link()` 先把 Candidate 学习时保存的物理 Link 解析为该 Neighbor 当前 Primary；源端 `PATH_PROBE` 与 `PATH_ACTIVATE` 也通过该解析后的 egress 发送。未修改帧格式、业务 API、RERR Payload、Adapter 契约或资源模型。
- 测试：扩展 `tests/test_neighbor_bearer.c` 为 A→B→C 虚拟拓扑：动态寻路与较优 Candidate 均先经 Primary 学习；Primary Down 后，业务、Candidate Probe×3、Activate/ACK 全程改走 Backup；B→C 断链时 RERR 经 Backup 返回 A 并清除 A 的动态 Route/Candidate；恢复后重新学习的动态 Route 在全部 A↔B Bearer Down 时被回收。另验证静态 Route 有意保留，后续发送返回 `UCN_ERR_LINK_DOWN`，由产品层选择恢复策略。
- 验证：执行 Debug、Release、64 B MTU 和 `-DUCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CMake Profile 的构建与 CTest，均为 `1/1` 通过。未烧录 ESP32，未测实际 ESP-NOW+UART/CAN 的 RERR 收敛、切换时延、丢失/乱序、RAM/Flash/栈或功耗。
- 结论/限制：T21.4 已完成 C99/虚拟闭环；静态 Route 的保留不是自动恢复功能。下一步为 T21.5 目标板资源报告，再做 T21.6 真实双介质/多板验证；T22 仍未编码。

### OP-059｜完成 T21.6.1 真实 ESP-NOW + UART 独立 Bearer 接入与两板控制验证

- 操作：在 ESP32 测试工程中将原先由 `DualMediaLink` 聚合的 ESP-NOW/UART 改为两个独立 `ucn_link_t`；保持主库 C99 Core、业务帧格式和既有虚拟测试不变。正常 A/B 及 `bearer_diag` Profile 直接让 ESP-NOW Peer Slot Link 和 UART Link（`0x70`）各自进入 Core；旧聚合逻辑保留为 `_legacy_dual` 对照 Profile。
- 修改：UART Adapter 现在拥有自己的 Link ops、状态、固定 Cost、RX Queue 和 `pump(node, now, max)`；ESP-NOW 只在 legacy 聚合模式下才覆盖 ingress Link。测试固件新增 `ADAPTER`、`BEARER`、Heartbeat/Probe/切换/回收统计以及两组 `bearer_diag`、一组 `node_b_wifi_diag` Profile。诊断 Profile 关闭旧吞吐发生器，避免速度压力污染 Bearer 健康和切换结论。
- 构建与回归：S3 正常 A/B、legacy A/B、Wi-Fi baseline、ESP-WROOM-32、A/B Bearer 诊断和 B Wi-Fi-only 诊断均成功构建；Core `build`、`build-release`、`build-mtu64`、`build-bearer1` 四个 CTest Profile 均为 `1/1` 通过。最终 Bearer 诊断镜像为 RAM `42,892 / 327,680 B`、Flash `579,423 / 6,553,600 B`。
- 实机证据：COM5(Node A=`0x10000001`) 与 COM34(Node B=`0x10000002`) 均成功烧录并同步 RTS 复位。两端启动后约 1 s 记录 `links=2`、`admitted=1`、`count=2`；UART `0x70/Cost=5` 为 Primary、ESP-NOW `0x01/Cost=10` 为 Backup，空闲 Queue 丢弃为 0，Ping 大多为 3–4 ms。实测内部最小 Heap 为 A=`315,432 B`、B=`315,428 B`，`loopTask` 剩余栈为 A=`5,400 B`、B=`5,396 B`。将 B 临时刷为相同 Node ID 的 Wi-Fi-only 诊断镜像后，A 保持 `admitted=1` 并仅保留 ESP-NOW Bearer；恢复 B 独立镜像后 A 自动重新显示 `count=2` 与 UART Primary。
- 结论/限制：这完成真实双 Adapter 接入、双 Bearer 合并、空闲基线和“远端 UART 停止/恢复”的控制实验；它不是物理拔线，也没有测 P50/P95 切换时延、持续业务丢失/乱序、反向 Wi-Fi 故障、驱动任务栈、功耗或三板 A→B→C/RERR。T21.6.2～T21.6.4 继续保持进行中；T22 不提前编码。

### OP-060｜完成 T22.1 固定策略模型与质量快照

- 操作：在不修改 UCN v4 帧格式、Route Epoch、安全 AAD 或既有自动路由行为的前提下，新建 `include/ucn/ucn_policy.h` 与 `src/ucn_policy.c`，并将固定 `policy_state` 嵌入 `ucn_node_t`。默认 Profile 为 Policy 8、当地 Path 8、Q1 Flow 8、Link 质量快照 4，均可用编译期宏缩小；不使用动态内存。
- 修改：新增 `(destination, Endpoint[, traffic class])` 策略精确/通配匹配、`AUTO_BEST`/`PINNED_STRICT`/`PINNED_FAILOVER`/`AUTO_BALANCE` 配置、当地 `local_path_id`、Q1 Flow 租约和清理 API。`local_path_id` 不是线上 Path ID；当前配置只可查询/统计，`ucn_node_send_endpoint()` 仍走原有 AUTO_BEST。`ucn_link_metrics_t` 增加可选 RTT、发送失败率和队列压力；`ucn_node_step()` 每 500 ms 以 25% EWMA 保存固定质量快照。ESP-NOW 和 UART Adapter 分别提供可得失败/队列指标，ESP-NOW 保留 RSSI EWMA；两者均不伪造 RTT。
- 测试：新增 `tests/test_policy.c`，覆盖默认兼容、精确优先/通配回退、重复更新、固定表满、非法 Q0/通配均衡、质量采样窗口与 EWMA、Path/Flow 容量、Q1 Flow 租约过期、Link Down 状态和旧 Endpoint 发送 API。Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CTest Profile 均为 `1/1` 通过。
- 构建：ESP32-S3 Node A 为 RAM `43,788 / 327,680 B`、Flash `584,015 / 6,553,600 B`；ESP-WROOM-32 为 RAM `45,928 / 327,680 B`、Flash `610,419 / 1,310,720 B`，均成功。没有烧录、串口或实机性能结论。
- 结论/下一步：T22.1 的固定资源和质量输入底座已完成；T22.2 必须先冻结受认证 Path ID、安装/撤销控制面和 AAD/RERR 范围，之后才能让 `PINNED_*` 真正影响转发；Q1 流亲和均衡留在 T22.4，不得提前把本地 Flow 表称为自动均衡。

### OP-061｜完成 T22.2 Path ID、安全控制面与双路径虚拟闭环

- 操作：在 T22.1 仅保存本地策略句柄的基础上，新增真正可上线的 Path ID 转发底座；保持 Linux 非前提、固定容量、无动态内存和普通 Endpoint 自动路由兼容。本轮没有把 `PINNED_*` 或 `AUTO_BALANCE` 接入 `ucn_node_send_endpoint()`，避免未完成的策略被误当作已生效。
- 修改：新增 `include/ucn/ucn_path.h`、`src/ucn_path.c`，默认 `UCN_MAX_PATH_FORWARD_ENTRIES=8`，表项以 `(owner_node_id, owner_session_id, path_id, destination)` 唯一识别并保存本跳下一跳/egress、终端标志与到期时间；`ucn_frame_t`/编解码增加 Path Flag、40 B 头和非零 `path_id`。`PATH_INSTALL` 使用 `{Path ID, Destination, Next Hop, Lease}`，`PATH_REVOKE` 使用 `{Path ID, Destination}`；远程控制同时要求既有 Security Provider 授权和 `ucn_node_set_path_control_authorizer()` 显式回调，未设置时默认拒绝。受保护业务的不可变 AAD 从 26 B 扩展为 30 B 并覆盖 Path ID；Path 范围 RERR 使用 `{unreachable, destination, owner_session, path_id}`，只撤销匹配 Path。
- 转发边界：源、每个中继和目标分别保存逐跳固定表项。中继通过 Path 表选本跳 egress，透明转发 E2E 密文且不调用 `open()`；只有目标节点解密和分发。不存在、过期或目标不匹配的 Path 会拒绝并上报 Path 范围 RERR。控制帧不得携带 Path ID/E2E；普通 Route 的旧 4 B RERR 语义仍保留。
- 测试：新增 `tests/test_path_control.c`，构造 A→B→D(P1) 与 A→C→D(P2) 双路径。覆盖 40 B 编解码与 Path AAD、默认控制面拒绝/显式授权、重复安装更新、租约过期、远程撤销、透明 E2E 中继、P1 的 B-D Down/RERR 只清 P1 且 P2 继续业务。Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CMake Profile 均为 CTest `1/1` 通过。
- 目标构建：未烧录或操作任何开发板。PlatformIO 仅执行构建：ESP32-S3 Node A 为 RAM `44,124 / 327,680 B`、Flash `585,623 / 6,553,600 B`；ESP-WROOM-32 为 RAM `46,264 / 327,680 B`、Flash `612,123 / 1,310,720 B`。Windows 长路径/LTO 串行提示未导致构建失败。
- 结论/下一步：T22.2 的 C99/虚拟/双目标编译验收完成，但真实多板 Path 安装、受权节点、链路故障收敛与资源/时延仍需 T22.7。下一项应是 T22.3：将 `PINNED_STRICT`/`PINNED_FAILOVER` 受控接入普通 Endpoint 发送，保持硬故障才切换的边界。

### OP-062｜完成 T22.3 Endpoint 固定严格路径与主备切换

- 操作：在已安装、受认证的 T22.2 Path 表之上，将策略真正接入 `ucn_node_send_endpoint()`；保持未配置业务、`AUTO_BEST` 和 `AUTO_BALANCE` 的既有行为，且不改 32/36/40 B 线格式。
- 修改：`ucn_route_policy_config_t` 增加 Primary/Backup 本地 Path 句柄，`ucn_policy_path_config_t` 增加线上 `wire_path_id`。已验证的 Policy Path 必须与本节点已安装的 `(Node, Session, Path ID, Destination)` 源端项以及 egress 对应；零 Path ID 仍可保存 T22.1 元数据，但不能被 PINNED 业务发送使用。`PINNED_STRICT` 只调用 Primary，失败直接返回；`PINNED_FAILOVER` 只在 `UCN_ERR_LINK_DOWN` 或 `UCN_ERR_NOT_FOUND` 时将该本地 Path 标为 Down 并尝试 Backup。队列背压、Security/ACL、参数或普通发送错误绝不触发切换。
- Q0/Q1 边界：Q0 可以通过预安装的 Backup 切换，但没有 Backup 时立即向本地返回失败，绝不 RREQ/等待。Q1 在 Primary 和 Backup 均硬失败且 `allow_discovery_on_hard_failure=true` 时，才复用现有受限 RREQ 与固定 pending 槽；此时显式允许退回 AUTO 路由，不宣称仍为固定 Path。`PINNED_STRICT` 配置发现回退会在设置时拒绝。
- 测试：扩展 `tests/test_path_control.c`。虚拟 A→B→D(P1)/A→C→D(P2) 覆盖 Strict 正常 P1、Strict P1 Down 不改路、Failover 正常 P1、P1 RERR 后 Q1/Q0 都切 P2、无 Backup 的 Q1 清旧静态路由后经 RREQ 选择 A→C→D、无 Backup 的 Q0 仍返回 `LINK_DOWN`；同时断言中继不解密 E2E、Path Flag、策略统计和 AUTO_BEST 旧回归。Debug、Release、64 B、Bearer=1 四个 CTest Profile 均为 `1/1` 通过。
- 目标构建：未烧录或操作开发板。PlatformIO 仅构建：ESP32-S3 Node A RAM `44,220 / 327,680 B`、Flash `586,223 / 6,553,600 B`；ESP-WROOM-32 RAM `46,360 / 327,680 B`、Flash `612,755 / 1,310,720 B`。Windows 长路径和 LTO 串行提示未导致失败。
- 结论/下一步：T22.3 的 C99/虚拟/双目标构建已完成，不等于真实无线无缝切换或自动负载均衡。下一项是 T22.4：仅 Q1 的可关闭流亲和均衡；应复用已验证 Path，禁止逐帧轮询、Q0 分流和帧复制。

### OP-063｜完成 T22.4 Q1 流亲和 AUTO_BALANCE

- 操作：在 T22.3 已验证 Primary/Backup Path 之上实现默认关闭的 `AUTO_BALANCE`；不改 32/36/40 B 线格式、不引入动态内存、不改变未配置或 `AUTO_BEST` Endpoint，也不让应用直接指定 Link/中继。
- 修改：`ucn_route_policy_config_t` 增加可选 `balance_flow_lease_ms`，零值采用 `UCN_POLICY_BALANCE_FLOW_LEASE_MS=2000`；新增队列压力阈值 `800‰` 和连续样本数 `3` 的编译期 Profile 宏。AUTO_BALANCE 只接受精确 Q1 Policy，Primary 必填、Backup 可选，禁止 Discovery 和 Q0；Primary/Backup 是最多两条已验证线上 Path 成员。固定 Flow 表以 `(destination, Endpoint, Q1)` 绑定成员 Path；同 Flow 在租约内保持原 Path，首次/租约到期使用平滑 Cost、RTT、失败率、队列压力与活跃 Flow 数的有界分数选择成员。连续压力样本或 `LINK_DOWN`/Path 不存在时才重绑；单次背压、ACL/Security、参数错误、逐帧轮询、复制、带宽聚合和隐式 RREQ 均被排除。
- 测试：`test_policy.c` 覆盖 Q0、无 Primary、Discovery 与非法租约配置拒绝；`test_path_control.c` 在 A→B→D(P1)/A→C→D(P2) 上覆盖同 Flow 固定 P1、第二 Flow 选 P2、连续三个 500 ms 高队列压力样本后 P1→P2、P1 Down 后仅受影响 Flow 重绑一次到 P2、租约过期，以及不复制 E2E 帧。Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CTest Profile 均为 `1/1` 通过。
- 目标构建：未烧录、监听、重启或改变任何开发板。PlatformIO 仅构建：ESP32-S3 Node A RAM `44,308 / 327,680 B`、Flash `587,671 / 6,553,600 B`；ESP-WROOM-32 RAM `46,448 / 327,680 B`、Flash `614,235 / 1,310,720 B`。Windows 长路径与 WROOM LTO 串行提示未导致失败。
- 结论/下一步：T22.4 已是受限的 Q1 流亲和均衡，不是实机吞吐优化、无缝切换或逐帧带宽聚合。下一项为 T22.5：让端到端 Path 与同 Neighbor 的 Bearer 主备联动，确保单 Bearer 切换不改 Path ID 或误失效其他 Path；真实多板压力、比例、时延、乱序和功耗留 T22.7。

### OP-064｜完成 T22.5 Path 与 Bearer 联动

- 操作：在 T21 已有“同一 Neighbor 的多 Bearer 发送时解析当前 Primary”基础上，补齐 Path 的故障范围与策略状态同步；保持 MCU-first、固定表、无动态内存，未修改 v4 的 32/36/40 B 帧、Path ID、E2E AAD、Adapter 契约或应用发送 API。
- 修改：`src/ucn_node.c` 为 Path 源端发送和中继转发统一在实际发送前解析 Path egress 的健康 Bearer。单物理 Bearer Down 时改用同 Neighbor Backup，保留 Path 表项、Path ID 与 Policy 状态；全部 Bearer Down 时才撤销使用该逻辑下一跳的非终端 Path。若被撤销 Path 属于本节点，同时将相同 `wire_path_id + destination` 的 Policy Path 标为 `DOWN`。中继收到已失效 Path 帧时仍经健康上游 Bearer 回 Path 范围 RERR；源只回收该 Path，不误清独立 Path。下游转发失败不会被伪装为当前一跳 Bearer 状态。
- 测试：扩展 `tests/test_path_control.c` 为 A⇄B 与 B⇄D 各两个 Bearer、另含独立 A→D(P2) 的受保护虚拟拓扑。覆盖 A-B 单 Bearer 切换与 P1 不变、B-D 单 Bearer 切换与 P1 不变、B-D 全 Down 后 B 仅清 P1 并通过健康 A-B Backup 回 Path RERR、A 收到后 P1 Policy Down、独立 P2 保留，以及 A-B 全 Down 只清本地依赖该 Neighbor 的 P3。Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CTest Profile 均为 `1/1` 通过。
- 目标构建：未烧录、监听、重启或改变任何开发板。PlatformIO 仅构建：ESP32-S3 Node A RAM `44,308 / 327,680 B`、Flash `588,047 / 6,553,600 B`；ESP-WROOM-32 RAM `46,448 / 327,680 B`、Flash `614,615 / 1,310,720 B`。Windows 长路径和 WROOM LTO 串行提示未导致失败。
- 结论/下一步：T22.5 已完成 C99/虚拟/双目标构建闭环；它证明“同 Neighbor Bearer 切换不会改变 Path ID，完整下一跳失效才缩小范围撤销 Path”，不证明真实 WiFi/UART/CAN 的切换时延、丢失、乱序、吞吐、功耗或多板收敛。下一项为 T22.6：提供限频、按需的策略/Path/Flow 诊断与统计；真实多板验收仍留 T22.7。

### OP-065｜完成 T22.6 受授权的按需策略/Path/Flow 诊断

- 操作：在 T22.1～T22.5 已有固定 Policy、Path、Flow、质量快照和 Bearer 联动之上，新增单 Node、按需、限频的管理查询。坚持 MCU-first、固定容量、无动态内存和业务帧零额外字段；没有把诊断做成周期性上报、全网泛洪或永久拓扑表。
- 修改：新增控制类型 `POLICY_DIAGNOSTIC_REQ (0x1E)` / `REPLY (0x1F)`，并更新控制帧范围。请求固定 8 B `{request_id, section, index, reserved}`，回复固定 32 B `{request_id, section, index, status, reserved, 24 B record}`，因此基础 32 B 头下正好满足 64 B Profile；二者为 `DIAGNOSTIC` Q1 控制帧，不允许 Path ID/E2E。`ucn_node_request_policy_diagnostic()` 让管理侧选择三页 Summary 或一个 Policy/Policy Path/Q1 Flow/Link-quality 槽位；回调获得结构化结果。Policy 规则新增本条 `match_hits`，Summary 分页覆盖 Strict/Failover/AUTO_BALANCE 的发送、硬故障、发现、拥塞/Down 重绑和质量统计。
- 权限与调度：目标默认拒绝；只有设置 `ucn_node_set_policy_diagnostic_authorizer()` 的产品 Node 才响应指定管理 Node，既有 Security Provider 的 RX 授权仍在前。请求方与响应方分别采用独立“突发 1、1 s 补 1”令牌；本地 Pending 与远端 Reply Queue 各默认 2 项、超时自动回收。诊断请求/回复仅在 `ucn_node_step()` 已无正常 Q0/Q1、Pending Q1、Heartbeat、Bearer/Path Probe 和 Snapshot Reply 时发送，所以不进入或挤占 Q0 业务队列。
- 测试：新增 `tests/test_policy_diagnostic.c`，虚拟 A(管理)→B(响应) 链路验证：Q0 业务先于诊断请求、8 B/32 B 线长度、默认每秒一令牌的限频、显式授权/默认拒绝、源端 Pending 满、空 Policy 槽、Policy 命中、Path 当前 Bearer + Cost/RTT/失败率/队列、Flow→Path/剩余租约、Link-quality、Summary 分页与无回复超时。并更新 `CMakeLists.txt`、`tests/test_main.c`、`tests/test_support.h`。Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CTest Profile 均为 `1/1` 通过。
- 目标构建：未烧录、监听、重启或改变任何开发板。PlatformIO 仅构建当前 Core：ESP32-S3 Node A RAM `44,540 / 327,680 B`、Flash `591,371 / 6,553,600 B`；ESP-WROOM-32 RAM `46,680 / 327,680 B`、Flash `617,807 / 1,310,720 B`。Windows 长路径提示和 WROOM LTO 串行提示未导致失败。
- 结论/下一步：T22.6 已完成 C99/虚拟/双目标构建闭环，可用于受授权、低频查看单 Node 的策略实际命中、Flow/Path 绑定、当前 Bearer 与质量快照；不代表生产 ACL、加密管理链路、真实诊断时延/开销、WiFi/UART/CAN 故障行为或多板收敛已验收。下一项为 T22.7：集中做全量回归、故障注入与多板实机验收。

### OP-066｜完成 T22.7.1 自动全量回归门禁

- 操作：将 T22.7 拆为“自动回归”与“分阶段多板实机验收”；新增 `tools/run_t22_7_core_regression.ps1`，每次均重新配置四个 CMake Profile，避免 MTU 或 Bearer 宏从旧构建目录串入其他 Profile。脚本只构建/测试，明确不包含 `upload`、串口监视、复位或 COM 口访问。
- 验证：脚本依次执行 Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 的 configure/build/CTest，四次均为 `1/1` 通过；`git diff --check` 通过。随后只运行 PlatformIO 构建：S3 Node A/B 均为 RAM `44,540 / 327,680 B`、Flash `591,371 / 6,553,600 B`，WROOM 为 RAM `46,680 / 327,680 B`、Flash `617,807 / 1,310,720 B`。Windows 长路径和 WROOM LTO 串行提示未导致失败。
- 故障覆盖边界：全量测试程序集已回归 T21 Bearer 主备/全 Down、T22 Path P1/P2 范围 RERR、Strict/Failover、Q1 Flow 拥塞或 Down 重绑、策略诊断的授权/限频/超时；这些是 C99 虚拟 Fault 注入证据，并不是无线实测结果。
- 结论/下一步：T22.7.1 已完成且不改变任何已连接的开发板。T22.7.2 仍需实际板卡按两板双 Bearer、三板 A→B→C/RERR、四/五板 P1/P2 Policy/Flow 的顺序测量 P50/P95、丢失、乱序、收敛、控制面、Heap/Stack 与功耗；此前不得宣传真实策略性能或无缝切换。

### OP-067｜新增 T25 节点内任务通信详细执行方案

- 操作：在既有“一个 MCU 一个 Node、Task 为静态 Endpoint/Service”的建议之上，新增 T25 的可评审实施设计；本次只更新文档，未修改 C99 Core、ESP32 测试工程、协议帧、路由、安全、构建配置或开发板状态。
- 新增：`docs/08-实现与验证/版本演进/UCN_T25_节点内任务通信详细执行方案.md` 定义了 Core 外纯 C `ucn_service` Router、固定 Service Binding、`ucn_service_send()`、Local Fast Path、Remote TX Request、Q0 FIFO/Q1 Latest、固定副本所有权、后续 Buffer Pool 边界、Protocol Task 独占、FreeRTOS Port 以及 T25.0～T25.5 的单元/模拟/实机门禁。
- 同步：`docs/05-传输与服务/UCN_节点内任务通信建议.md`、`docs/00-项目管理/00-任务表.md` 和 `docs/08-实现与验证/版本演进/UCN_v4_协议核心说明.md` 已链接该详细方案；T25 仍保持待开始，不能把评审设计描述为已实现能力。
- 验证：仅执行 Markdown 链接与任务状态核对；未运行 CTest/PlatformIO，因为没有代码或构建文件变化；未烧录、监听、复位或访问任何 COM 口。
- 结论/下一步：等待确认 Endpoint ABI、首版单消费者/固定副本边界及目标板容量后，从 T25.1 纯 C Router 和单元测试开始；完成后再接 Core 桥接和 FreeRTOS/ESP32。

### OP-068｜完成 T25.0 首版 Endpoint / Service 契约冻结

- 操作：按 T25 详细执行方案先完成其前置契约，新增 R1 静态 Endpoint、Payload 编码、Service 绑定、Q0/Q1、安全边界和固定资源基线；本次仍只修改文档，不修改 C99 Core、ESP32 测试工程、协议帧、路由、安全或构建配置。
- 新增：`docs/08-实现与验证/版本演进/UCN_T25_首版Endpoint与Service契约.md` 固定 `0x40/0x42/0x43` 传感器、`0x50` 电源状态、`0x60` 电机命令、`0x61` 舵机目标，全部采用显式 big-endian 固定 Payload；R1 首版为单 Endpoint 单消费者、固定副本，Router 默认 6 Binding、32 B Payload、Remote TX Q0/Q1 各 4、每个 Q0 Inbox 4 和每个 Q1 Inbox 1。
- 冲突处理：发现 ESP32 独立吞吐测试镜像当前临时占用 `0x40/0x41`，而更早总体设计已将它们定义为传感器范围。契约明确该测试镜像不与产品 ABI 同编译；在 T25.3 接入真实 Service 前，测试 Endpoint 必须迁至 `0xC0/0xC1` 或独立测试 Network。本轮不改测试固件，以免破坏既有实机吞吐基线。
- 同步：`UCN_T25_节点内任务通信详细执行方案.md` 和 `UCN_节点内任务通信建议.md` 的旧示例已统一为 R1 编号；任务表新增 T25.0（完成）与 T25.1（待开始），T25 父项改为进行中。
- 验证：文档相对链接、编号分区、Payload 长度与 QoS/消费者映射待本轮 Markdown 检查；未运行 CTest/PlatformIO、未烧录或访问开发板，因为本轮没有代码/构建输入变化。
- 结论/下一步：T25.0 的 Router 输入已固定，下一项直接实现 T25.1 纯 C `ucn_service` Router 和单元/虚拟拓扑测试；FreeRTOS Port 留待 T25.3。

### OP-069｜完成 T25.1 纯 C Service Router 与多 Profile 回归

- 操作：在 T25.0 R1 契约之上实现平台无关的固定 `ucn_service` Router；保持 Core/RTOS 边界，未修改 v4 帧格式、路由、Path、Security、Node API 或任一 Adapter。
- 新增：`include/ucn/ucn_service.h`、`src/ucn_service.c`、`tests/test_service.c`。Router 使用编译期 Profile（默认 6 Binding、32 B Payload、Remote TX Q0/Q1 各 4、Q0 Inbox 深度 4）；提供固定 Binding/本机 ACL/ready、Local Fast Path、Q0 FIFO、Q1 Latest、远端 TX 出队、目标端入站投递与统计。每个已入队的 Payload 都是 Router 自有固定副本，调用者返回后可立即复用原 Buffer。
- 修改：`CMakeLists.txt` 将 `ucn_service.c` 纳入核心库，`tests/test_main.c` 与 `tests/test_support.h` 注册 `test_service()`。测试覆盖配置拒绝、长度/QoS/ACL/ready、Q0 满/FIFO、Q1 覆盖、远端 Q0/Q1 队列满、Q0 优先、Remote Q1 按 `(destination, Endpoint)` 最新值合并，以及三个 Router 的 A→B→C 目标隔离。
- 验证：重新配置并构建 Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CMake Profile，CTest 均为 `1/1` 通过；`git diff --check` 通过。随后只执行 PlatformIO 构建：S3 Node A/B 均为 RAM `44,540 / 327,680 B`、Flash `591,371 / 6,553,600 B`，WROOM 为 RAM `46,680 / 327,680 B`、Flash `617,807 / 1,310,720 B`，三者均实际编译 `ucn_service.c`。没有上传、监听、复位或访问 COM 口。
- 结论/下一步：T25.1 证明了纯 C、固定资源的本机/远端任务消息边界；当前 ESP 镜像未创建 Router 实例，故上述整机资源没有 Router 运行时增量含义。下一项为 T25.2：仅在唯一 Protocol Task 中把 `ucn_service_remote_tx_take()` 有界桥接到 `ucn_node_send_endpoint()`，并将 Endpoint callback 接入 `ucn_service_deliver_remote()`；FreeRTOS 静态 Queue/真实任务和板级时延测试仍留 T25.3/T25.4。

### OP-070｜完成 T25.2 Protocol Task Bridge 与 A→B→C 集成模拟

- 操作：在已验证的纯 C Router 外新增单独的 `ucn_service_bridge`；它是产品/Protocol Task 适配层，不把 `ucn_node_t`、Link 或 FreeRTOS 依赖写回 `ucn_service` Router，也不改变 v4 帧、路由、Path、Security 或 Node API。
- 新增：`include/ucn/ucn_service_bridge.h`、`src/ucn_service_bridge.c`、`tests/test_service_bridge.c`。Bridge 初始化时核对 Router 与 Node 的本 Node ID；安装 Endpoint handler 前检查固定 handler 槽和所有权，拒绝覆盖已有外部 handler。`ucn_service_protocol_bridge_step()` 每次按产品传入上限取 Remote TX（Router 保持 Q0 优先）并调用 `ucn_node_send_endpoint()`；目标 Endpoint callback 只调用 `ucn_service_deliver_remote()`、累加投递/拒绝统计。
- 修改：`CMakeLists.txt`、`tests/test_main.c`、`tests/test_support.h` 纳入 Bridge 与专用测试。Bridge 不重试已被 Core 拒绝的已出队请求：Q0 无路由/Link Down 是显式 Bridge 失败，Q1 的受限寻路/Pending 仍属于 Core。由于既有 Endpoint handler 是 `void`，目标 Router 的未就绪/满队列只能记录目标本机 Bridge/Router 统计，不能伪造成对源端的端到端业务 ACK；可靠确认未在本轮引入。
- 验证：`test_service_bridge.c` 覆盖 ID 不一致、未安装 handler、外部 handler 冲突、无路由 Q0、Q0 先于 Q1 的每轮上限，以及真实虚拟 A→B→C。A 的 Router 经 Bridge 调用 Core；B 的通用 RX 回调保持 0；C 的 Bridge 只将 Q0/Q1 投递 C Inbox。另验证 C 的 Endpoint 未就绪时源端 Core 发送成功但 C 只增加本机拒绝统计。Debug、Release、`UCN_MAX_FRAME_BYTES=64` 与 `UCN_MAX_BEARERS_PER_NEIGHBOR=1` 的 CTest 均为 `1/1` 通过。未烧录、监听、复位或访问任何 COM 口。
- 结论/下一步：T25.2 已闭合 Router→Protocol Task→Core→目标 Router 的 C99/虚拟路径，且保持中继无业务投递。下一项是 T25.3：在 ESP32 Port 用 `xQueueCreateStatic()`/静态通知把 Inbox 接给实际 FreeRTOS Task，并测每个队列、Heap、栈、本机/远端并发和失联安全；接入产品 Endpoint 前先迁移当前 ESP 吞吐镜像的临时 `0x40/0x41`。

### OP-071｜完成 T25.3 ESP32 静态 FreeRTOS Port 构建接入

- 操作：主库 Bridge 新增平台无关的可选 inbound hooks：成对 lock/unlock 包住 Router 的短入站投递和 Remote TX 取出，observer 在 unlock 后执行。该 API 只保存 C 函数指针/上下文；没有引入 FreeRTOS 类型、动态内存、帧字段、Task ID、路由或安全语义。`test_service_bridge.c` 覆盖 hook 配置拒绝和真实 A→B→C 里的 6 次 lock/unlock、3 次目标观察回调。
- ESP32 Port：在 `E:\File\PlatformIO\ESP32_UCN\ESP32S3_N16R8_UCN_Test1` 新增 `include\ucn_service_freertos_port.h`、`src\ucn_service_freertos_port.cpp`。`main.cpp` 静态建立 R1 的 6 个 Binding、Router、Bridge、两个 `xQueueCreateStatic()` 的 Endpoint 事件 Queue 和一个 `xTaskCreateStatic()` 业务分发 Task；Arduino `loopTask` 继续是唯一 Node/Core owner，按 Adapter Pump → Bridge budget=2 → `ucn_node_step()` 驱动。Router Inbox 仍是唯一 Payload 队列，事件 Queue 仅保存 1 B Endpoint Token；Port 限制只有已绑定 Task 可读取对应 Service Inbox。
- Endpoint 修正：原先文档曾建议迁到 `0xC0/0xC1`，但源码 `ucn_endpoint_is_static()` 只允许 `0x40..0xBF`，该建议不可执行。现已把 Ping/吞吐从 R1 占用的 `0x40/0x41` 迁到保留测试窗口 `0xB0/0xB1`；历史实测仍按当时的 `0x40/0x41` 记录，不将旧结果伪称为新镜像实测。
- 验证：Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 的 CTest 均为 `1/1`，随后 PlatformIO 只构建 S3 Node A/B 和 WROOM：S3 A/B 均为 RAM `47,180 / 327,680 B`、Flash `594,783 / 6,553,600 B`；WROOM 为 RAM `49,320 / 327,680 B`、Flash `621,271 / 1,310,720 B`。相对 T25.2 的未实例化镜像，静态 RAM 均增加 `2,640 B`；S3 Flash 增加 `3,412 B`、WROOM 增加 `3,464 B`。没有 upload、monitor、reset、串口读取或修改 COM5/COM34/COM35 固件。
- 边界/下一步：构建证明 FreeRTOS API 和两类 ESP32 目标可编译，不证明新静态 Task 已运行、没有死锁、Q0/Q1 实际正确、Heap/Stack 安全或端到端性能。T25.4 应先烧录两块 S3，核对 `service_port=enabled`、`SERVICE`、`RESOURCE service_stack_hw`，再执行本机 Q1、远端 Q1/Q0、事件 Queue 满、断链本地安全和时延测试。

### OP-072｜新增 UCN 使用与调用手册

- 操作：新增 `docs/01-入门与使用/UCN_使用与调用手册.md`，将当前 C99 Core、Service Router/Bridge、ESP32 FreeRTOS Port 和策略路由的公开调用方式整理为产品接入手册；README 增加该手册入口。
- 内容：明确“一个 MCU 一个 Node、一个 Protocol Task 独占 Node”的并发边界；说明 Node/Link 初始化、Adapter RX 入队/Pump、Endpoint 收发、Service 本机 Fast Path 与跨 MCU Bridge、自动入网、多 Bearer、Path 安装、Strict/Failover、Q1 流亲和均衡、Security Provider 和按需诊断的使用顺序。手册同时给出 R1 Endpoint/QoS 表、常用错误处理和固定资源配置。
- 核对：逐项对照 `include/ucn/ucn_node.h`、`ucn_link.h`、`ucn_adapter.h`、`ucn_service.h`、`ucn_service_bridge.h`、`ucn_policy.h`、`ucn_path.h`、`ucn_security.h` 及现有策略/Path 虚拟测试；检查 README 和项目文档相对链接。未修改协议、Core、ESP 工程、构建配置或开发板状态，因此未重复执行 CTest/PlatformIO。
- 结论/限制：本文是当前 API 的使用入口，不把 Core/虚拟测试结果写成真实多板性能，也不将 Security Provider 误写为内置生产 AEAD。实际产品仍需按手册第 14 节完成 Adapter、板级资源、生产密钥和逐级实机验证。

### OP-073｜GitHub 发布前的构建产物筛查

- 操作：在提交最新 UCN Core、Path/Policy、Service Router/Bridge、测试、工具和文档前，检查当前分支、远程地址、工作区状态、未跟踪项与常见凭据模式。
- 修改：`.gitignore` 新增 `/build_*/`，覆盖本地 `build_t25_debug`、`build_t25_release`、`build_t25_mtu64` 和 `build_t25_bearer1` 等命名 CMake Profile 目录；这些目录保留在本机，不删除也不提交。
- 核对：远程为 `https://github.com/ice-infinite/UCN.git`，分支为 `main`；待提交项为协议源码、公开头文件、单元/虚拟测试、PowerShell 回归工具及项目文档。敏感信息模式扫描未发现私钥、Token、API Key 或密码赋值命中；`git diff --check` 通过。全量 `tools/run_t22_7_core_regression.ps1` 在本次 60 s 工具窗口内未输出并被超时中止，不把它记为通过；随后对现有 Debug 配置执行 `ctest --test-dir build_t25_debug -C Debug --output-on-failure`，结果为 `1/1` 通过。
- 结论/限制：发布内容只包含可复现的源文件与文档，构建输出不进入仓库；实际上传结果以提交哈希和 `git push origin main` 返回为准。

### OP-074｜建立 UCN YAML 调用关系树

- 操作：参考 `E:\File\PlatformIO\F405_Zephyr_Parachute\docs\calltree` 的“模块索引 + YAML 节点 + `called_by/calls`”模式，在 `docs/calltree/` 建立 UCN 调用关系树。
- 新增：`README.md`、`index.yaml`、模板和 Core、Frame、Adapter、Node Runtime、Neighbor、Route、Policy/Path、Security、Service、Diagnostic 十个模块树。节点记录真实函数、回调、固定队列与外部 Provider 边界；静态字节序/数组小工具归并至主函数节点，不制造不可读的大树。
- 覆盖：接收主链 `RX enqueue → RX pump → ucn_node_receive → 控制/转发/Endpoint → Bridge → Service Inbox`，远端发送主链 `Service send → Bridge step → send_endpoint → Route/Policy/Path → Security → Frame → Link`，以及 `ucn_node_step()` 的 Q0/Q1/保活/Probe/诊断/刷新优先级。
- 验证：10 个模块 YAML 均可由 PyYAML 解析；共 98 个节点、142 个内部/外部调用目标，所有非 `external.*` 的 `calls.target` 均能解析到树内唯一节点。README 与使用手册已加入入口。未改 C99/ESP 源码、构建配置或开发板状态，因此不重复执行 CTest/PlatformIO。
- 结论/限制：调用树描述当前源码调用关系，不代表消息可靠送达、生产 AEAD 内部实现或真实无线时延；协议行为和验证状态仍以源码、测试和实机记录为准。

### OP-075｜发布 UCN 调用关系树到 GitHub

- 操作：将 `docs/calltree/` 及其 README、使用手册、任务表和操作记录入口提交并推送到 `origin/main`。
- 发布范围：YAML 调用树包含索引、模板和十个模块树；不包含构建目录、固件产物、串口日志或任何 ESP32 源码修改。
- 核对：提交前已通过 YAML 解析、调用目标闭环检查（10 个模块、98 个节点、142 个调用目标）和 `git diff --check`；本次仅为文档发布，不重复运行 CTest 或 PlatformIO。
- 结论/限制：最终提交哈希和远程 `git push origin main` 响应是发布完成证据；调用树继续按源码演进同步维护。

### OP-076｜新增多运行环境快速使用手册

- 操作：在 `docs/快速使用手册/` 新增总览及六份独立文档：裸机、通用 RTOS、FreeRTOS、Zephyr、NuttX、RT-Thread；主 README 和使用与调用手册补入入口。
- 内容：统一冻结“一个 MCU 一个 Node、唯一 Protocol Task、ISR/驱动回调仅进有界 RX 队列、Service Router Inbox 为唯一 Payload 副本”的边界；每份文档给出静态资源、初始化、接收/调度、业务收发与验收顺序。FreeRTOS 明确引用 ESP32 测试工程的 `UcnServiceFreeRtosPort` 作为参考；Zephyr、NuttX、RT-Thread 和通用 RTOS 是基于现有 C99 API 的接入模板，并非仓库已实现的 Port。
- 核对：逐项对照 `ucn_port.h`、`ucn_link.h`、`ucn_adapter.h`、`ucn_node.h`、`ucn_service.h`、`ucn_service_bridge.h`、`src/ucn_service_bridge.c` 及 ESP32 Port 的静态 Queue/Task/Hook 实现；检查七个快速文档、README 和使用手册的相对链接。未修改 C99 Core、ESP 工程、构建配置、测试或开发板状态，因此不将本次文档更新写作 RTOS 适配或实机验证。
- 结论/限制：本文档可作为各系统的首个接入步骤，具体 BSP、Link 驱动、CAN Carrier、生产安全 Provider、资源阈值与实机时延仍必须逐板实现、构建和验证。

### OP-077｜登记 UCN 架构审查稳定化待办

- 操作：阅读 2026-08-10 对当前公开 `main` 的架构审查，将其中可执行问题登记为任务表的 `S01`～`S09` 稳定化阶段；在 P0/P1 未闭合前明确暂停新增 Extended 功能。
- 源码核对：`src/ucn_node.c` 的 `ucn_node_step()` 在 Q0/Q1 任一业务项存在时发送后立即返回，`send_due_heartbeat()`、Bearer/Path Probe、诊断和 Route Refresh 只在两个业务队列皆空时才会进入，故 S01 控制面饥饿风险为已确认问题。`ucn_node_receive()` 明确拒绝控制帧的 E2E 标志，但仍可通过 Security Provider 的 `authorize_rx()` 进行产品逐跳认证；这证明 S02 是生产 Provider/身份/控制面认证闭环任务，而非声称当前 Core 没有授权钩子。`src/ucn_policy.c` 当前未把 Cost、RTT、失败率和队列压力直接相加评分，`AUTO_BALANCE` 主要由 Q1 Flow 亲和和队列拥塞样本触发重绑，因此 S03 以“冻结指标契约、防重复计分”为目标。
- 登记：S01 调度公平、S02 生产安全、S03 跨介质指标、S04 Feature Profile、S05 随机压力、S06 三板多跳、S07 Task/Service 实机、S08 API/资源可理解性、S09 示例与 CI。既有 T21.6/T22.7.2/T32 不重复创建，分别被 S06/S07 收束和补充验收条件。
- 验证边界：本次仅核对源码和任务台账，没有修改协议、测试、构建、固件或开发板；所有新任务均为待解决事项，不能据此写成已实现或已实测。

### OP-078｜完成 S01 维护控制面有界调度的软件修复

- 操作：修改 `include/ucn/ucn_node.h`、`src/ucn_node.c` 和两组既有虚拟测试，消除 Q0/Q1 持续非空时维护控制帧只能等待业务队列清空的饥饿条件。
- 实现：新增可编译期配置的 `UCN_BUSINESS_TX_BURST_BEFORE_MAINTENANCE`（默认 4）和 `maintenance_preemptions` 统计。业务发送计数达到上限后，`ucn_node_step()` 先检查 Heartbeat、Bearer Quality Probe、Path Probe、Route Refresh；仅当其中存在实际到期动作才占用当前槽位，并清零计数。Snapshot 与 Policy Diagnostic 仍只在业务和必要维护均无工作时执行，不能抢占 Q0/Q1。Q0 保持 FIFO；当固定 Q0 队列以每轮一帧的速率超过“业务槽位减维护槽位”的承载能力时，`UCN_ERR_NO_SPACE` 仍按既有背压语义返回。
- 测试：`test_neighbor_heartbeat.c` 新增持续 Q1、持续 Q0、Q0+Q1 三种 30,000 ms 虚拟时间压力；所有场景均断言至少 30 个 Heartbeat、维护抢占统计增长、Neighbor 始终 `ADMITTED` 且无误 `SUSPECT/REMOVE`。`test_candidate_route.c` 新增连续四个 Q1 后 Route Refresh 进入维护槽的回归，确认候选发现启动而不是等空闲。
- 验证：重新构建并运行 Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 CMake Profile，CTest 均为 `1/1` 通过。尝试重新配置 ASan/UBSan 时，当前 Windows MinGW 14.2 链接器缺少 `-lasan` 与 `-lubsan`，编译器自检失败；这是本机工具链限制，未将 Sanitizer 写作通过。
- 边界：本次没有修改 ESP32 固件、没有上传/复位/监听 COM 口，也没有实测调度周期、无线时延或丢包。两块 S3 的压力镜像和三板多跳验证继续留在 S06/T22.7.2；生产身份、AEAD 和控制面逐跳认证仍由 S02 处理。

### OP-079｜完成 S03 Link Metrics 与 Cost 契约的软件收束

- 操作：在 `include/ucn/ucn_link.h` 公开冻结 Link Metrics 的归属、单位、范围和未知值语义；新增 `UCN_LINK_METRIC_PER_MILLE_MAX=1000`，并让 Policy 配置/采样复用该唯一边界。新增 `docs/04-路由与链路/UCN_Link_Metrics与Cost契约.md`，同步 README、总体架构、v4 核心说明与调用关系树。
- 契约：`route_cost` 定义为正值、逐跳可加的基础 Cost，不能重复揉入同次 RTT/失败率/队列压力；RTT 是一跳往返毫秒，失败率和队列压力分别来自 Adapter 的发送失败窗口与 Adapter 自身 TX Queue，均是 `0..1000‰`。每项独立 `valid`；未知用 `valid=false`，不用零值伪装。当前 Core 仅用 `route_cost` 做路由基础代价，并以持续队列压力触发 Q1 Flow 重绑，不存在四指标直接相加的自动评分。
- 测试：扩展 `tests/test_policy.c`，覆盖 `>1000‰` 失败率/队列压力被清为未知、原有 EWMA/采样窗口、Link Down/Path Down 与既有策略回归。Debug、Release、64 B MTU、Bearer=1 CTest 均 `1/1`；调用树 99 节点闭环、相关 Markdown 链接与 `git diff --check` 均通过。
- 边界：未新增综合评分、业务权重或任何介质的默认数值；真实 ESP-NOW/UART/CAN/LoRa 窗口、`route_cost` 等级和重绑阈值必须在 S06 的可复现硬件日志中标定。没有修改 ESP32 工程、没有上传或访问串口。

### OP-080｜重新审查并修复稳定化 P0/P1 边界

- 任务重开：按第二轮源码审查重新更新 `docs/00-项目管理/00-任务表.md`，新增/细化 S10～S14，并纠正此前只检查 `ucn_policy.c`、漏看 `ucn_node.c` 实际 `AUTO_BALANCE` 评分的结论。本轮没有把生产安全、Feature Profile 或实机指标误标为完成。
- Service 生命周期与语义：`ucn_service_set_ready(..., false)` 现在清空对应 Q0 FIFO/Q1 Latest，not-ready 时 `inbox_take()` 拒绝旧消息并记录 `binding_purges`。新增兼容的 `ucn_service_send_ex()`，明确区分 `LOCAL_DELIVERED` 与 `REMOTE_ENQUEUED`；后者不是 Bridge/Core/Link 或远端执行确认。高风险 Q0 可选 12 B Command Guard，包含 `command_id/issued_at_ms/valid_for_ms/result_endpoint`，只占选择使用它的业务 Payload，不扩大每条 Router Message；跨节点时间戳仍要求产品共享时间域。
- Cost/Policy：保留 `UINT16_MAX` 为 Unknown Cost，正常 Cost 上限为 `UINT16_MAX-1`；Known 永远优于 Unknown，累加饱和不会变成 Unknown。`AUTO_BALANCE` 删除 Cost、RTT、失败率、队列压力的跨量纲裸相加，只按 Known 基础 Cost 与活动 Flow 数评分，持续队列压力仍独立触发拥塞重绑。
- 控制面：RREQ Request、Heartbeat Request 和 Path Trace Request 改为“直连对端 + 控制类型”的固定令牌桶；一个对端耗尽预算不影响另一对端，邻居彻底移除时回收其预算。Path Trace 每个中继/目标新增显式 Authorizer，默认 `NULL` 拒绝，并在分配 Reverse/Reply 资源前执行授权和限流。
- 长期运行：新增 `ucn_time.h`，统一合法 Duration `1..INT32_MAX`、永不生成 0 的 Deadline、跨 `uint32_t` 回绕过期/即将到期/elapsed 判断；Core 内部去除裸 `now + timeout`。Sequence 接近阈值时调用可选 Security Provider `rotate_session()`，无回调/失败/重复 Session/非法起始序号均失败关闭；Seen Cache 身份扩展为 `(source, session, sequence)`，轮换后的新 Session 可安全从 Sequence 1 开始。另修复轮换发生后待发帧仍携带旧 Session ID 的时序错误。
- Link/API：公开 `Link.send()` 的生产契约为有界复制/固定 TX Queue 后返回，队列满为 `UCN_ERR_NO_SPACE`、Driver Down 为 `UCN_ERR_LINK_DOWN`，Core 不在单次调用栈内重试。新增 `ucn_frame_max_payload(flags)`，按 32/36/40 B Header 和 16 B Tag 返回真实 Payload 上限，并拒绝 Path ID 未同时带 Route Extension 的非法 Flag；Path Trace 小 MTU 宏不再无符号下溢。
- 测试：新增 Task Restart、Unknown/Known Cost、跨量纲评分隔离、按对端控制洪泛/恢复/类型隔离、Path Trace 默认授权、Sequence Rotation、Deadline/Neighbor/Route/Path/Flow/Trace 回绕、Link 背压单次调用和 Service Acceptance/Command Guard 测试。新增固定 Seed 的 32 Node 环网压力：5000 轮随机 Link Down/恢复、跨回绕 Q1 发送，逐次断言成功恰好交付一次且失败不误投递。
- 验证：全新重配 Debug、Release、`UCN_MAX_FRAME_BYTES=64`、`UCN_MAX_BEARERS_PER_NEIGHBOR=1` 四个 Profile，均编译 42 个目标并由 CTest `1/1` 通过；`UCN_MAX_FRAME_BYTES=47` 负向构建按预期由 Path Trace/Policy Diagnostic 静态断言拒绝。Calltree 11 个 YAML/102 节点/148 调用目标闭环，项目 Markdown 相对链接通过，`git diff --check` 通过。
- CI/边界：新增 `.github/workflows/core-ci.yml`，准备在 Ubuntu 执行四 Profile 与 ASan/UBSan；本轮尚未推送，故不能把远端 Actions 写成已通过。没有修改或烧录 ESP32 固件，没有访问 COM 口。S04 真正 Nano/Lite/Full 对象裁剪、生产身份/AEAD/逐跳认证、真实 Adapter WCET/Cost 标定和多板结果往返仍保留为后续门禁。

### OP-081｜归档稳定化第二阶段问题并建立执行任务

- 源码复核：以 `main@731e37e` 为基线，确认 Core Q0 在 `ucn_node_step()` 发送后无条件清槽、`send_frame_on_link()` 与 Step 可能重复累计失败；Service Bridge 会先取走 Router Remote TX 再直接调用 Core，没有 Q0 Pending 恢复点。确认 S01 目前只按业务发送数量保证维护机会，产品尚无最大 Step 间隔契约；ESP 参考工程只有首次 100～400 ms HELLO 抖动；Command Guard 是可选工具而非高风险 Binding 强制门禁；Path Install/Revoke 有认证/授权但无独立管理源预算；现有压力测试为 32 节点静态路由同步环网。
- 文档：新增 `docs/10-理论与规划/建议方案/UCN_稳定化第二阶段问题与执行建议.md`，将上述问题连同生产安全、Nano/Lite/Full 真裁剪、动态网络压力和文档漂移编号为 ST2-01～ST2-10，分别写明是否处理、实现边界、明确非目标和单元/模拟/实机门禁。Q0 只建议对瞬时 `NO_SPACE` 做有限本机重试，不升级成可靠传输；本阶段只增加本机 Outbound Observer，不增加通用远端 ACK/Q2。
- 任务：在 `docs/00-项目管理/00-任务表.md` 新增 S15～S20，顺序为 Q0 背压、Step 时限、高风险 Validator、HELLO 风暴控制、Path 管理预算和异步阶段；ST2-07～ST2-09 复用 S02/S04/S05/S08/S09，不重复建立并行实现。增加 10/10 建议—任务覆盖表，并明确 S15 为下一项代码任务。
- 漂移修正：项目 T22.4、总体架构、协议档案、两份路由建议、更新方案、调用树和知识库当前专题统一为 Known 基础 Cost ×（活动 Flow 数 + 1），RTT/失败率不进入裸评分，持续 Adapter Queue Pressure 独立触发拥塞重绑；Unknown Cost 统一为 `UINT16_MAX` 保留哨兵，Known 累加饱和到 `UINT16_MAX-1`。历史操作记录保留并由本条声明当前口径。
- 验证边界：本轮只修改项目/知识库文档，没有修改 `include/`、`src/`、`tests/`、CMake、ESP 固件或开发板状态；因此不把本轮记录为新的 CTest、PlatformIO 或实机结果。ST2-01～ST2-10 在建议文档和任务表均可检出，S15～S20 各有唯一任务行；项目 Markdown 与本轮知识库内链缺失均为 0；调用树 11 个实际模块、102 节点、148 调用目标可解析且内部目标缺失为 0；`git diff --check` 通过。

### OP-082｜完成 S15 Q0 有界背压与 Bridge 所有权闭环

- Core：在 `ucn_types.h` 增加显式 `UCN_DELIVERY_RETRY_ON_BACKPRESSURE`；只允许 Q0 且必须提供非零绝对 Deadline。`ucn_tx_item_t` 增加固定重试状态，`ucn_node_step()` 仅在 Link 返回 `UCN_ERR_NO_SPACE` 时保留原 FIFO 槽，默认最多重试 3 次、间隔 5 ms；Deadline、次数耗尽或其他错误终结。等待间隔内 Q1/诊断不能越序，但到期 Heartbeat、Bearer/Path Probe 和 Route Refresh 仍可取得必要维护槽。
- Service Bridge：在 `ucn_service_bridge.h/.c` 增加默认关闭的 Q0 背压 Policy、一个固定 Pending Q0、显式时钟 `step_at()` 和 final-only Outbound Observer。Pending 存在时不再从 Router 取下一条 Q0/Q1；只有 `NO_SPACE` 可重试，`LINK_DOWN/NOT_FOUND/SECURITY/...` 立即完成。Observer 中间重试不触发；`UCN_OK` 只表示本机 `LINK_QUEUE_ACCEPTED`，不是远端 Inbox/执行 ACK。
- 统计：Core/Bridge 分别增加 retry、exhausted、expired、terminal_failed。只要消息仍由 Core 槽或 Bridge Pending 持有，就不把中间 `NO_SPACE` 计为最终 Drop；耗尽/终止只归入一个最终分类，修复 Step 与 `send_frame_on_link()` 对同一失败重复累计的问题。
- 单元/模拟：扩展 `test_link_contract.c`，覆盖前 N 次背压后恢复、等待时不重复提交、默认单次失败、非法 Q1/无 Deadline、次数耗尽、终止错误零重试和过期；扩展 `test_service_bridge.c`，覆盖两个 Q0 不越序、恢复/耗尽/终止/过期、Observer 最终只回调一次和精确统计；扩展 `test_neighbor_heartbeat.c`，在 5000 ms 持续 Q0 背压下仍至少发送 5 次 Heartbeat，Neighbor 无误 `SUSPECT/REMOVE`。
- 构建：全新 `build_s15_debug`、`build_s15_release`、`build_s15_mtu64`、`build_s15_bearer1` 均编译 42 个目标，CTest 均 `1/1`。PlatformIO 只构建 S3 Node A/B 与 WROOM：S3 各 RAM `47,692 / 327,680 B`、Flash `596,463 / 6,553,600 B`；WROOM RAM `49,832 / 327,680 B`、Flash `622,943 / 1,310,720 B`。这些是当前完整测试固件绝对值，不作为纯 S15 增量。
- 文档/核对：同步任务表、稳定化第二阶段反向核对、使用手册、T25 详细方案、FreeRTOS 快速手册、调用树和 Obsidian UCN 知识库。调用树 11 个实际 YAML、106 节点、151 调用目标均可解析且内部缺失为 0；项目 33 份 Markdown 相对链接缺失为 0，UCN 知识库 42 份笔记内链缺失为 0，`git diff --check` 通过。实现没有修改线格式、路由发现、Q1 Latest、安全或远端 ACK 语义；S20 的本机 Observer 部分随 S15 落地，通用远端 ACK 仍不做。
- 硬件边界：本轮没有上传、复位、监听或访问 COM 口。真实 Wi-Fi/UART Queue 满/恢复、Link `send()` WCET、重试间隔精度、P50/P95/Max、栈/Heap 与执行器失联安全继续留 S06/S07；下一项按任务表执行 S16。

### OP-083｜完成 S16 Protocol Task 时限与维护上界闭环

- Core：在 `ucn_node.h` 新增默认 `UCN_MAX_STEP_INTERVAL_MS=10`，按 `(BUSINESS_BURST + 1) × MAX_STEP × MAX_NEIGHBORS × MAX_BEARERS` 计算 `UCN_MAINTENANCE_SERVICE_BOUND_MS`；默认上界为 800 ms。编译期检查 Step 非零/时间安全、上界不溢出，且 `Heartbeat + Maintenance Bound` 必须严格早于 Neighbor Suspect，否则失败关闭。
- 观测：`ucn_node_t` 新增固定 `step_observation_started`，Node Stats 新增 `last_step_ms`、`max_step_gap_ms`、`step_interval_violations`、`max_heartbeat_service_delay_ms`、`max_probe_service_delay_ms`。第一次 Step 只建立基线；后续用无符号差值支持 `uint32_t` 回绕。Heartbeat 与 Bearer/Path Probe 成功发送时记录相对到期时刻的最大调度延迟。超限只计诊断并继续调度，不在已经迟到时停止协议任务。
- 测试：`test_time.c` 覆盖首次、等于 10 ms、11 ms 超限及跨回绕；`test_neighbor_heartbeat.c` 在持续 Q0 业务下按 Profile 构造多 Neighbor/Bearer，断言全部到期 Heartbeat 在保守上界内发送，且无误 Suspect/Remove。全新 Release、64 B MTU、Bearer=1 加既有 Debug 四档 CTest 均 `1/1`；`UCN_MAX_STEP_INTERVAL_MS=50` 负向构建按预期由静态断言拒绝。
- ESP32 Port：S3/WROOM 公共 Profile 显式设为 10 ms；参考 `loopTask` 冻结最低优先级 1、最大 Block 1 ms、Wi-Fi/UART Pump 各 4 帧、Bridge 2 条，并统一给 Bridge/Node 传同一个 `now`。启动日志输出优先级、预算、800 ms 上界和 `link_send_wcet=unmeasured`；`STAT` 输出 Step 最大 Gap/违规。优先级不满足时拒绝进入 Ready。
- 构建资源：S3 Node A/B 均 `SUCCESS`，RAM `47,716 / 327,680 B`、Flash `597,083 / 6,553,600 B`；WROOM `SUCCESS`，RAM `49,856 / 327,680 B`、Flash `623,555 / 1,310,720 B`。相对 OP-082 的完整参考固件，三者 RAM 均增加 24 B，Flash 分别增加 620 B、620 B、612 B。首次三环境并行构建出现共享 `.pio/build` 清理告警，但各环境构建均成功；最终验证另以顺序构建排除并行清理干扰。
- 文档/边界：同步任务表、稳定化建议反向核对、架构/配置/使用/T25/六份 RTOS 快速手册、调用树和 Obsidian 知识库。没有修改 v4 线格式、路由选择、安全语义或网络负载，没有上传/复位/监听 COM。真实 Step Gap、Heartbeat/Probe P50/P95/Max、Wi-Fi/UART Link `send()` WCET、Task 抢占与栈/Heap 仍待 S06/S07；下一项按任务顺序执行 S18。

### OP-084｜完成 S18 高风险远端 Q0 强制 Validator

- Binding/Bridge：`ucn_service_binding_t` 新增 `require_remote_q0_validator`，只允许配置在 `accept_remote=true` 的 Q0 Binding。Bridge 新增编译期固定 `UCN_SERVICE_BRIDGE_MAX_VALIDATORS` 表和安装前 `set_validator()`；强制 Endpoint 缺任一 Validator 时，在注册任何 handler 之前整体返回 `UCN_ERR_CONFIG`。回调获得完整 Frame 及 Source/Session/Endpoint/Payload/Now，在 Router lock/`deliver_remote()` 前运行，拒绝项只进入 Bridge 统计和既有 Observer，不进入 Inbox。
- Replay：新增产品可选 `UCN_SERVICE_BRIDGE_REPLAY_DEPTH` 固定表，保存当前 `(Source Node, Source Session, Endpoint)` 和最后命令 ID；重复/旧 ID 返回 `UCN_ERR_REPLAY`，同源同 Endpoint 的其他 Session 返回 `UCN_ERR_SECURITY`，表满返回 `UCN_ERR_NO_SPACE`。Session 只有在产品 Security 已认证后显式 `rotate_session()` 才清空命令基线，旧 Session 不能自动切回；新 Session 0 被拒绝。无 malloc、无无限历史、无线格式变化。
- 产品边界：普通 Q1、本机 Fast Path 和未标记 Q0 不强制 Validator，也不增加 12 B Guard。ESP32 R1 `0x60/0x61` 保持原 16 B ABI，Validator 检查长度、序号、有效期、模式/掩码、标志及目标范围；消费 Task 再执行同一产品字段检查。当前明文 Bench 的 Session=0 只做格式门禁，只有未来非零认证 Session 才启用固定 Replay，不能写成生产防重放完成。
- 单元/模拟：`test_service.c` 增加非法 Binding 组合；`test_service_bridge.c` 覆盖缺 Validator、注册/移除/容量冻结、重复/旧命令、过期、坏长度、表满、显式 Session 轮换、旧 Session 拒绝、命令 ID 回绕、本机 Fast Path、普通 Q1、自定义 1 B 高风险格式，以及两节点命令→结果 Endpoint 往返。所有 Validator 拒绝均断言 Inbox 为空，并核对 accepted/rejected/missing 统计。
- 构建：既有 Ninja Debug、Release、64 B MTU、Bearer=1 四档均重新编译并 CTest `1/1`。PlatformIO 顺序构建 5 个镜像均成功：普通 S3 A/B 各 RAM `47,836 / 327,680 B`、Flash `598,211 / 6,553,600 B`；Service S3 A/B 各 RAM `49,004 / 327,680 B`、Flash `596,887 / 6,553,600 B`；WROOM RAM `49,976 / 327,680 B`、Flash `624,699 / 1,310,720 B`。这些是完整固件绝对值，不作为纯 S18 对象增量。
- 文档/边界：同步 S18 任务状态、稳定化建议、架构/配置、T25 ABI/执行方案、使用手册、调用树和 Obsidian 知识库。本轮没有上传、复位、监听或访问 COM 口；真实电机/舵机命令、结果 Endpoint、Task 二次检查、认证 Session 轮换、栈/Heap/时延继续留 S02/S07。下一项按任务表执行 S17 Adapter HELLO 启动风暴控制。

### OP-085｜完成 S17 Adapter HELLO 启动风暴控制

- Adapter Helper：`ucn_adapter.h/.c` 新增固定 `ucn_adapter_hello_scheduler_t`，状态为 Initial Jitter、有限 Fast Retry、指数 Backoff、Admitted Slow/Stop；每个实例使用非零 Adapter Token 对 Port Seed 加盐，配置校验所有 Duration、最大 500‰ 重试抖动及抖动后的最坏上界。到期先安装下一 Deadline 再返回一次 `hello_due`，不自旋、睡眠、入队或修改 Core/线格式。
- 状态边界：准入后 Slow 先按 Fast 间隔发一次互认 HELLO，再进入低频恢复；Stop 只供静态预准入/外部保证互认的流程。没有已准入 Bearer 后重新进入 Initial Jitter；显式 restart 使用新 Seed。`enabled=false` 允许静态 CAN/UART 完全关闭主动 HELLO，但产品仍须静态注册、配置 Peer Node ID 并准入 Link。
- 单元/模拟：新增 `test_adapter_hello.c`，覆盖精确状态/Deadline、Token 确定性、快重试次数、Backoff 上限、Slow/Stop、掉线、restart、静态禁用、非法配置与 `uint32_t` 回绕；当前 C99 ABI 下每 Scheduler 为 76 B。100 节点固定 Seed `0x5EED1234` 模拟每节点丢弃前 0..3 次 HELLO；无抖动/有抖动的 10 ms 峰值为 `100/9`、100 ms 峰值为 `175/52`，两者总量均 250，有抖动准入 P50/P95 为 `608/1177 ms`，同 Seed 重跑指标一致。
- ESP 参考：ESP-NOW 与独立 UART 各持有一个 Scheduler，不再共享固定 1 s Deadline；动态 Profile 为 100～400 ms 初始、250 ms 快重试最多 3 次、1～8 s Backoff、150‰ 抖动，准入后 WiFi 5 s/UART 30 s Slow。启动日志输出 Scheduler 字节数/参数，`STAT` 输出状态与计数；`EspNowAdapter::owns_link()` 只用于判断本 Adapter 是否已有准入 Bearer。S3 Node A/B、legacy dual、WiFi-only、Service A、WROOM 六个环境顺序构建均 `PASS`。
- 验证/边界：Debug 已先通过并输出模拟指标；随后 Debug、Release、64 B MTU、Bearer=1 四个独立 Profile 均构建并 CTest `1/1`。Adapter 调用树、项目/知识库文档与任务状态同步。没有烧录、复位、监听或访问 COM；100 节点结果只证明调度模型，不是 ESP-NOW 空口性能。真实多板峰值、准入 P50/P95、碰撞/丢包、资源/功耗与参数标定继续留 S06；下一项为 S19。

### OP-086｜完成 S19 已认证 Path 管理预算

- Core：`ucn_node.h/.c` 新增固定 `UCN_PATH_CONTROL_RX_SOURCE_DEPTH=4` 管理源表，键为认证后的 `(source Node, source Session)`，不含 ingress Link。每个来源的 INSTALL/REVOKE 各自突发 4 Token、每 1000 ms 补 1 Token；60 s 不活跃项只在下次请求时惰性回收。同源新 Session 只有在 Security 与产品 Authorizer 均接受后才复用并重置原槽，避免长期轮换泄漏槽；旧 Session 是否有效仍由产品 Security 决定。
- 顺序/统计：接收路径严格为 Security → 产品 `path_control_authorize` → 管理预算 → Path 写入。新增 INSTALL/REVOKE 授权拒绝、各自预算拒绝、管理源表满、Session 更新、惰性回收和 Path 表满计数。默认预算状态 `4×24=96 B`，8 个计数器 32 B，总计每个 Full Node 增加 128 B 固定状态；无 malloc、阻塞、正常业务帧字段或线格式变化。
- 单元/模拟：新增 `test_path_management_budget.c`，覆盖 Security/产品拒绝不分配槽、同源在两个 Bearer 间交替仍共享额度、INSTALL/REVOKE 隔离、洪泛/1 Token 恢复、不同来源、Session 更新与旧 Session 拒绝、源表满/60 s 回收、Path 表满。每个拒绝路径都对完整 `ucn_path_state_t` 做前后比较。既有 `test_path_control.c` 的 A→B→D / A→C→D 四节点逐跳管理、透明中继与 Path 回归同时通过；额外 `UCN_PATH_CONTROL_RX_SOURCE_DEPTH=1` 最小来源档案也通过 CTest。
- 构建：Debug、Release、64 B MTU、Bearer=1 四个 CMake Profile 均重新编译当前 33 个 C 测试源并由 CTest `1/1` 通过。ESP 参考 `STAT` 新增 `PATHCTL` 分类行；S3 Node A/B 均 RAM `48,116 B`、Flash `600,419 B`，legacy dual `48,092/600,567 B`，WiFi-only `46,156/597,935 B`，Service A `49,276/599,043 B`，WROOM `50,176/626,579 B`，六环境均构建成功。
- 文档/边界：同步任务表、稳定化建议反向核对、架构/配置/使用手册、调用树、ESP 操作记录与 Obsidian 知识库。本轮没有 upload、reset、monitor、复位或访问 COM；构建不证明真实管理洪泛下的 Heartbeat/RREQ/业务时延、功耗或生产 Session 吊销。上述实机门禁继续留 S02/S06；下一代码项为 S20。

### OP-087｜完成 S20 Service 异步阶段与业务结果关联

- Service API：`ucn_service.h/.c` 新增五阶段 `ucn_service_async_stage_t`，并以 `ucn_service_acceptance_stage()` 把兼容 `send_ex()` 的 `LOCAL_DELIVERED/REMOTE_ENQUEUED` 映射到 `LOCAL_INBOXED/REMOTE_ROUTER_QUEUED`。新增可选 8 B 大端 `ucn_service_result_header_t`，只允许 `REMOTE_INBOXED+ACCEPTED` 或 `REMOTE_EXECUTED+SUCCEEDED/REJECTED/FAILED/EXPIRED`；无状态 helper 同时核对 Command ID、Guard 指定 Result Endpoint 和结果头合法性。
- Bridge：保留既有 final-only Observer，新增结构化 `set_outbound_event_observer()`；同一最终点区分 Link Queue 接受、未启用重试的立即背压拒绝、S15 重试耗尽、Pending 过期和终止失败。两种 Observer 可同时安装并各自只调用一次；中间 `NO_SPACE` 不回调。只有成功事件带 `LINK_QUEUE_ACCEPTED` Stage，失败 Stage 固定为 `NONE`。
- 单元/模拟：`test_service.c` 覆盖 Acceptance 映射、结果头大端编码/附加正文、错 ID/Endpoint 和非法 Stage/Status。`test_service_bridge.c` 覆盖五类本机最终 Outcome、兼容/结构化 Observer 次数一致、两节点 Command Guard→`REMOTE_INBOXED`→`REMOTE_EXECUTED` 结果往返、Link Down，以及目标 Validator 拒绝后源端仅见本机 Link 接受、无伪远端 ACK并达到产品等待 Deadline。
- 构建：全新 `build_s20_debug/release/mtu64/bearer1` 四档均编译 44 个目标并由 CTest `1/1` 通过；64 B/Bearer=1 使用 `CMAKE_C_FLAGS` 实际覆盖宏。PlatformIO 顺序构建 S3 Service A/B 和 WROOM 均成功：S3 各 RAM `49,284 / 327,680 B`、Flash `599,095 / 6,553,600 B`；WROOM RAM `50,184 / 327,680 B`、Flash `626,623 / 1,310,720 B`。这些是完整测试固件绝对值，不是 S20 独立增量。
- 文档/边界：新增 `UCN_S20_异步阶段与业务结果关联.md`，同步任务表、稳定化反向核对、使用/T25 契约、调用树、ESP 操作记录与 Obsidian 知识库。调用树 11 个 YAML、114 节点、158 调用目标可解析且内部缺失为 0；项目 34 份 Markdown 的 122 个相对链接、UCN 知识库 47 份笔记的 382 个内链均无缺失，`git diff --check` 通过。普通消息、Router 队列、v4 帧头、路由、安全和 Q0/Q1 线语义未变；没有通用 ACK/Q2、自动重传、产品 Pending 表或动态内存。
- 硬件边界：本轮没有 upload、reset、monitor、复位或访问 COM。真实 Wi-Fi/UART Result Endpoint 时延、Validator 拒绝后的业务超时、Source/Session 等待表、断链和执行器本地安全继续留 S07；下一代码项进入 S05 动态综合压力。

### OP-088｜完成 S05 动态综合压力与 RREP 目标侧度量修复

- 动态事件网：保留 `test_stress.c` 的 32 Node 静态环网，新建 `test_dynamic_stress.c`。32 Node 各固定 4 Link，先从未知 Peer 经 HELLO/Open Join 准入，再运行首包 AODV、强制出口断开/RERR/有界恢复、随机双向 Down、Cost 波动、25‰ 丢失、80‰ 重复、0～7 ms 延迟/乱序、持续 Q1、间歇 Q0 与周期 HELLO。事件队列固定 1024 槽，失败报告 Seed、轮次和最近 16 个事件；`UCN_STRESS_EVENTS` 可选 1～1,000,000，默认 2000。
- 路由修复：动态测试首次把不同 Epoch 的缓存项拼接成链，复核真实转发后确认必须按 Current/Previous Epoch 检查，跨 Epoch 互指不是可执行环。同时确认 RREP 原实现把源到目标的同一总 Cost/Hop 复制给全部返回节点；改为目标从 0/0 回包、每个返回节点按 ingress 对应目标方向 Link 累加，使缓存字段成为本节点到目标的剩余度量。`test_link_metrics.c` 固定 A→D→C=`4000/2 Hop`、D→C=`2000/1 Hop` 回归；不增加 RREP Payload 或 v4 帧字节。
- 资源耗尽：同一新测试覆盖 Neighbor Candidate、静态 Route、Policy Path、Q1 Flow、Path Forward、本机/远端 Service Q0/Q1 表满；断言 `UCN_ERR_NO_SPACE`、到期/清除/Take 后复用。Seen、Candidate 和 Bridge 继续由重复事件及既有专项测试共同覆盖。
- 验证：默认 2000 轮通过；5 Seed×1000 轮通过；固定 Seed `0x51A05EED`×100,000 轮通过，统计为事件入队 614,713、投递 614,573、故障丢弃 14,766、重复注入 45,578、事件高水位 147/1024、业务投递 6844。Debug、Release、64 B、Bearer=1 四个独立 CTest 均 `1/1`；WSL Ubuntu 24.04/GCC 13.3 的 ASan+UBSan 同样 `1/1`。文档收尾后再次以 `-C Debug` 回归 `1/1`；项目 35 份 Markdown 相对链接、知识库 48 份 Wiki 内链均无缺失，调用树 11 个 YAML/114 节点/158 调用目标闭环，`git diff --check` 通过。百万轮入口已提供但本轮未执行。
- 文档/边界：新增 `UCN_S05_动态综合压力测试.md`，同步任务表、稳定化建议、v4 核心/路由方案、README 与 Obsidian 知识库。Host 测试的 1024 槽事件队列不进入 MCU，也没有修改 Node 固定资源、业务帧或线格式。本轮未构建/上传 ESP 固件、未复位或访问 COM；虚拟结果不代表真实介质性能，三板和 Task/Service 实机继续留 S06/S07。

### OP-089｜完成 S04 Nano/Lite/Full 真裁剪的软件闭环

- Profile/构建：新增 `ucn_profile.h`，冻结 Nano、Lite、Full 三档网络能力和正交 `UCN_FEATURE_SERVICE`。CMake 公开发布统一编译定义：Nano 改用独立 `ucn_node_nano.c`；Lite/Full 使用动态 Node；非 Full 用显式 Stub 返回 `UCN_ERR_CONFIG`；只有 Full 编译 Path/Policy；Service OFF 不编译 Router/Bridge。CI 工作流增加 Nano/OFF、Lite/ON、Full/ON 矩阵，远端 Actions 尚未运行，不能宣称远端通过。
- 对象裁剪：`ucn_node_t` 按 Feature 移除 Pending Q1 以外的高级状态边界，其中 Nano 无 Neighbor/Discovery/Security，Lite 无 Candidate/Path/Policy/Diagnostic，Full 保持默认布局和行为。Nano 同时从 Adapter 头和源码移除 HELLO Scheduler。Lite 源码中的 Candidate、Path、Policy、Trace、Snapshot 和策略诊断函数由编译期分支移除，不依赖链接器死代码消除。
- 功能测试：`test_profile.c` 由宏打印升级为三节点功能/负向测试。Nano 验证 A→B→C 静态多跳 Endpoint；Lite/Full 验证未知目的地 AODV-Lite 发现和交付；Nano/Lite 验证关闭 API 返回配置错误；Service ON 验证 Router 初始化。最终 API 核对又补齐非 Full 的 `ucn_path_*` 和 Policy 维护 Stub，56 个公开 Node/Path/Policy 符号在三档静态库中均无缺失。修复 Lite 初始化时 `control_tokens` 被误置于 Diagnostic 条件块的问题，并用 GCC 严格警告构建捕获/移除 Lite 未使用的 Path helper。
- 构建/资源：MSVC Debug 的 Nano/OFF、Lite/ON、Full/ON 均通过；GCC 14.2 Release `-Wall -Wextra -Wpedantic -Werror` 的 Nano/OFF、Lite/ON、Full/ON、Lite/OFF、Full/OFF 均通过，Full 完整 CTest 仍为 `1/1`。Service OFF 的 Host x64 结果为 Node `2640/5456/8136 B`，静态库 `.text` `13904/50308/106704 B`。最小帧 33/50/64 B 可编译，32/49/63 B 按预期拒绝。
- 文档/边界：新增 `UCN_S04_Feature_Profile与资源报告.md`，同步任务表、稳定化建议与 README。上述 `.a`/`sizeof` 只证明 Host 对象和源码裁剪，不是 ESP32/STM32 的最终 Flash/RAM；本轮没有构建或烧录开发板、没有访问 COM，目标 ELF、Task 栈、Heap、CPU、功耗和真实介质行为继续留 S06/S08。

### OP-090｜完成 S08 公共 API 与静态存储边界

- 头文件边界：新增 `include/ucn/ucn_node_storage.h`，将 `struct ucn_node`、Queue/Route/Candidate/Seen/诊断 Pending 等实现存储从 `ucn_node.h` 移出。`ucn_node.h` 现在只提供不完整 `ucn_node_t`、配置/结果/统计和全部公开函数；Core owner、Service Bridge 实现和白盒测试显式包含 Storage，业务/Adapter/Port 的指针声明继续只依赖 API 头。保留 C99 静态分配和 Profile 固定对象，不引入 malloc、Linux 或 RTOS 依赖。
- 下游接入：使用/快速手册的裸机、通用 RTOS、FreeRTOS、Zephyr、NuttX、RT-Thread 静态 Node 示例改用 Storage 头；ESP32 测试工程的 `main.cpp` 与 UART 协议 Bench 作为 Node owner 同步，三个 Adapter 公开头仍只依赖 `ucn_node.h`。
- 测试/版本：新增 API-only `test_public_headers.c` 与 owner-only `test_node_storage_header.c`，纳入三 Profile；历史 `test_v3()` 入口改为 `test_protocol_version()`。`tests/test_v3.c` 文件名/局部 Fixture 为旧日志可检索而保留，并明确当前覆盖 v4 行为与旧版本显式拒绝；历史 V3 完成表和旧操作记录作为迁移证据保留，不伪装成当前版本。
- 安全语义：Storage 中 Seen 表明确只做易失网络去重；它不认证来源、不持久化，也不替代生产 Security Provider 的 Replay Window、单调 Counter、Session/Key 撤销。Service Validator 的 Command Replay 只保护指定业务格式，同样不是全协议 Replay Window。
- 验证：GCC 14.2 Release 的 Nano/OFF、Lite/ON、Full/ON、Lite/OFF、Full/OFF 与 MSVC Debug 的 Nano/OFF、Lite/ON、Full/ON 均构建并 CTest 通过。33/50/64 B 正向可编译，32/49/63 B 按预期拒绝；三档静态库各有 56 个公开 Node/Path/Policy 符号，Host Node 大小仍为 `2640/5456/8136 B`。ESP 首次构建暴露旧脚本全量加入 `src/*.c`，被 Nano/Stub 编译门禁正确拒绝；修复为按 Profile/Service 选源后，S3 正常 Node（Full/ON，RAM `48124 B`、Flash `600819 B`）、S3 UART Bench（Full/OFF，`22152/185947 B`）与 WROOM（Full/ON，`50184/626803 B`）均成功。新增 `UCN_S08_公共API与静态存储边界.md` 并同步架构、使用手册、调用树、任务和知识库。
- 边界：头拆分不改变 v4 帧、运行时对象尺寸、路由或 API 符号。本轮 Host 证据不能替代 ESP32/STM32 的 ELF、栈/Heap、CPU、功耗或多板介质性能；生产安全留 S02，实机留 S06/S07，远端 CI 与公开示例留 S09。

### OP-091｜完成 S21 极限规模与全节点高负载模拟

- 工具：新增 Host-only `tools/ucn_scale_sim.c` 和 `run_ucn_scale_ladder.ps1`。模拟器真实实例化每个 `ucn_node_t`，提供 Tree/Ring4、local/two-hop/pairs/incast/all-to-all/mixed、固定 Seed、丢包/重复/延迟/断链、同时/分批预热、二叉事件堆和逐节点/全网 CSV；Harness 动态分配不进入 MCU Core。Full/Lite 构建 8 Node `ucn_scale_smoke`，Nano 保持静态功能测试且不编译动态规模目标。
- 规模结果：Full Tree Local 的 8→4096 阶梯均 100% 交付；1024 Node 的 Full M4/M8 与 Lite M4、32 B Payload 均 100%。远端 Two-hop 在 8/16/64/256/1024 Node 的受理后交付率为 100/68.750/28.125/15.234/12.207%，1024 Node Route HWM 最大 8、平均 5.999，说明默认固定 Route/并发建路工作集不能由直连 4096 结果外推。Ring4 分批预热可让 32/64 Node 远端 Pair 从 0 提升到 36.232/19.380%，128/256 仍为 0。
- 故障/正确性：256 Node、M4、25‰ 丢包、0～7 ms 延迟和周期断链通过，无环、无重复、无 Harness 背压，交付 99,373/102,100（97.329%）。80‰ 重复注入的 M1/M2 通过；M4 暴露 1070 次重复业务投递、252 个节点受影响，事件堆仅 1511/16384，确认不是 Harness 溢出。根因证据指向 `UCN_SEEN_CACHE_SIZE=8` 逐帧环在高并发乱序下提前淘汰；新增 P0 S22，要求按 Source/Session 位图窗口与 RREQ Best Cost 状态拆分，禁止仅扩大 Seen 表掩盖。
- 验证：2026-08-11 Windows Full Debug/Release、Lite Release 分别 CTest `2/2`，Nano Release `1/1`；WSL Ubuntu 24.04/GCC 13.3 的 Full/Service OFF、ASan+UBSan 为 `2/2`，额外 256 Node Local M4 和 64 Node Two-hop 无 Sanitizer 错误。新增 S21 方案、结果、112 份逐节点/汇总 CSV并同步 README、任务表和 Obsidian UCN 知识库。
- 边界：4096 是 Harness 主动上限下的稀疏独立直连虚拟链路，不是共享 Wi-Fi/CAN/UART 的节点上限；Host ABI、总分配、墙钟和虚拟时延不是 MCU RAM/栈/CPU/功耗/介质性能。本轮未构建、上传、复位、监听或访问任何开发板/COM，硬件规模继续留 S06/S07，下一代码任务为 S22。

### OP-092｜新增网络容量与关键参数总览

- 文档：新增`UCN_网络容量与关键参数总览.md`，将32位地址空间、默认4 Link/8 Neighbor/2 Bearer/8 Route/4 Discovery/16 Hop、寻址边界、缓存命中与冷寻路成本、Cost/均衡、帧效率、Profile资源、队列、保活/故障参数集中在一个入口，并由README直接链接。
- 证据：分别列出S21稀疏直连4096 Node、Two-hop下降、8/16/32 Node全对全结果，以及S22的256 Node、8%重复、M4下1070次重复业务投递；4096不写成共享介质或全对全上限。
- 资源：保留Host Node 2640/5456/8136 B与当前完整ESP测试固件构建数据，但明确Adapter、Service、RTOS栈、驱动和Security Provider在Node对象之外，Host ABI不冒充MCU资源。
- 同步：登记DOC-021，同步Obsidian知识库容量专题、测试入口、任务表与操作记录。本次仅更新文档，没有修改Core、线格式、CMake、测试或开发板/COM状态。

### OP-093｜完成 S22～S26 稳定化修复与回归

- 任务与重复抑制：先将审查问题登记为 S22～S26，再以固定 `(Source Node, Session)` 槽、最高 Sequence 和 32/64 bit 位图窗口替换逐帧 Seen 环；窗口支持乱序一次交付、重复/过旧拒绝、Session 切换、Sequence 回绕、超时回收和表满显式失败，重复/过旧重放不刷新槽时间。明文模式新增非零启动 Session 配置口；该状态仍是易失网络去重，不是身份认证或持久 Security Replay。
- RREQ 与接口边界：RREQ 使用独立固定 Best-Cost 表和 `Peek → Token → Commit` 顺序；重复请求不耗控制 Token，预算不足不污染更优 Cost。Service Router 拒绝 Node `0`/广播 Binding，并将 Binding 生命周期冻结为 Router 持有期内只读借用；Storage Layout 升为 2。
- Profile、Hop 与安全：CMake 改为按 Feature 选择真实行为测试，Nano/Lite/Full 不再只跑低档 Smoke；`UCN_MAX_HOPS` 允许产品编译期配置 1～254。Lite/Full 增加可选 `security_required` Fail-Closed 门禁，只有完整 Security Provider、有效 Session、认证收发能力以及默认/Endpoint 全部无明文策略时才 Ready；Nano 对“生产必须安全”配置编译拒绝。该门禁不等于已经提供或审计生产 AEAD、密钥、身份、撤销和安全存储。
- 软件验证：Windows Debug Service ON 的 Full/Lite 为 CTest `2/2`、Nano `1/1`；Release Service OFF 同样为 `2/2/1`。`UCN_MAX_HOPS=32` 为 `2/2`，0/255 按预期编译拒绝；Full 的 `UCN_SECURITY_REQUIRED_BY_DEFAULT=1` 构建通过，Nano 同配置按预期拒绝。WSL Ubuntu 24.04/GCC 13.3 的 ASan+UBSan Full/Service OFF 为 `2/2`，并通过 256 Node、M4、80‰ 重复场景且 `duplicate_business=0`。
- 缺陷重放与资源：Windows 固定场景 M1/M2/M4 分别交付 `25,600/51,200/102,400`，均 100%、重复业务 0、Route Loop 0、Harness 背压 0；逐节点与汇总 CSV 写入 `docs/results/S22/`。Windows x64 GCC Release、Service OFF 的 Node 为 Nano/Lite/Full `2648/5888/9400 B`，Core 静态库 `.text` 为 `13132/52120/109900 B`；这是 Host ABI/裁剪证据，不是 MCU RAM/Flash。
- 文档与硬件边界：新增 `UCN_S22_重复抑制与稳定化修复报告.md`，同步任务、网络容量、架构、配置、使用手册、快速手册、调用树和 Obsidian UCN 知识库。本轮没有上传、复位、监听或访问开发板/COM；真实多板、共享介质、目标 ELF/栈/CPU/功耗继续留 S06/S07，生产密码实现继续留 S02。

### OP-094｜冻结 v5 Adaptive Wire Profile 设计与执行顺序

- 方案：新增 `UCN_v5_Adaptive_Wire_Profile设计方案.md`，将 Build Profile、Wire Profile、Traffic Class 和 Security Policy 拆为四个正交维度；官方 Wire Profile 为 W0/W1/W2/W3，字段宽度分别为 1/2/3/4 B，最大 Hop 分别为 4/16/64/254，目标基础头为 17/21/26/30 B。
- 边界修正：W0 的一个数值地址域最多 254 个可用 Node ID；第一阶段短 Network/Node/Session 都是真实可表示数值，不做静默 Alias/NAT；Route Cost 暂保留 16 bit、Request ID 暂保留 32 bit。Session Slot、跨域 Directory/Alias、Gateway 和“外层 Wire + 内层规范 E2E”信封进入 Extended 任务，未在设计阶段冒充已实现。
- 任务：在任务表登记 V5-00～V5-07 和 DOC-023。执行顺序冻结为“v4 回归/提交/推送 → v4 标签与独立备份 → 从 v4 底座建立并推送 v5 分支 → V5-01 起逐项改代码和测试”。
- 当前状态：本项只新增方案、任务和入口；尚未修改 CMake 版本、协议版本、帧编码或运行时代码，也未访问开发板/COM。下一步先完成 v4 最终回归和可追溯备份。

### OP-095｜完成 V5-00 基线固化与 V5-01 官方 Wire Profile Codec

- v4 固化：Windows Debug/Release 的 Full/Lite/Nano 全部通过；WSL `Ubuntu-24.04-ROS` 的 Full/Service OFF ASan+UBSan 为 `2/2`，256 Node、M4、80‰ 重复重放为 `102400/102400`、重复业务 0。提交 `25bcd22` 已推送 `origin/main`，远端标签 `v4.0.0-final-before-v5` 指向同一提交。
- 备份与分支：由标签生成 `E:\File\MESH\UCN_Backups\ucn-v4.0.0-final-before-v5-20260811` 和同名 ZIP；解压为 249 文件、4,872,248 B，ZIP SHA-256 为 `B37D95AA0EA70C5C14DCEE5128CD135C4E0D8A707B8F593E99F69F5743B8C5CD`，其中 CMake `4.0.0`、协议版本 4。`codex/v5-adaptive-wire` 从该标签建立并已推送 GitHub。
- Codec：CMake/库/协议升为 `5.0.0`/5；新增 W0～W3 固定描述符、Version+Profile 字节、Traffic+Flags 字节、1/2/3/4 B 大端字段、17/21/26/30 B 基础头、Profile 派生 Route/Path Header、广播保留值、Profile Hop/字段上限、CRC 和 30 B AAD Profile 绑定。零初始化旧调用在 V5-01 明确解析为 W3，不静默缩短地址。
- 测试：新增四档完整 Golden Vector、Descriptor/Header Size、Route/Path、广播、字段/Hop 溢出、未知 Flag、CRC、AAD Profile 篡改和 v4 拒绝。Windows Full Debug/Service ON `2/2`，Release Service OFF 的 Full/Lite `2/2`、Nano `1/1`；WSL ASan+UBSan `2/2`。Nano/Lite/Full 的 33/50/64 B 最小配置构建通过，32/49/63 B 按预期编译拒绝。256 Node M4 重放 `102400/102400`、重复 0、Loop 0、背压 0，32 B Payload 的 Wire Efficiency 为 `50.383%`。
- 资源与边界：Host Node 仍为 Nano/Lite/Full `2648/5888/9400 B`；静态库 `.text` 为 `17228/56216/113996 B`，比 v4 各增加 4096 B。这不是 MCU Flash/RAM。V5-01 尚未把 Wire Profile 加入 `ucn_config_t`，Node 新发帧仍默认 W3；HELLO/RREQ 压缩、自动选档、Gateway、生产 AEAD 和实机分别留 V5-02～V5-07、S02、S06/S07。
- GitHub：实现提交 `262aad9 feat: add v5 adaptive wire profile codec` 已推送到远端 `codex/v5-adaptive-wire`；`main` 和 v4 标签保持不变，没有将 v5 线格式混入 v4 分支。

### OP-096｜完成 V5-02 Node 固定域档位

- API：新增 `ucn_node_set_wire_profiles()` 及两个只读查询接口。Node 初始化默认固定 W3；必须在注册 Link/安装 Security 前设置较小档位，避免运行中改变地址与 Session 语义，也避免扩展旧 `ucn_config_t` 导致未初始化字段。
- 门禁：所有本机新发控制/业务帧使用固定 TX 档；中继保留入站档位；接收在状态变更前拒绝超过最大 RX 档的帧。Network/Node/Hop/Plain Session/Provider Session/轮换 Session、Link 最小 MTU 和 W0 Route Epoch 均按官方描述符失败关闭，不截断数值。
- 测试：新增 `test_node_wire_profile.c`，覆盖四档双节点长度、默认/冻结、TX≤RX、字段/Session/Hop/MTU 越界及窄收宽拒绝；将 AODV-Lite 三节点测试改为 W0，验证 RREQ/RREP、业务中继、RERR 和修复。Windows Debug Full/Service ON 与 Lite/Service ON 均 CTest `2/2`，Nano/Service OFF `1/1`。
- 文档与边界：新增 `UCN_V5_02_Node固定域档位实现报告.md` 并把 V5-02 标为代码/软件测试完成。Host 软件通过不代表目标 MCU RAM/Flash/栈/吞吐；V5-03 继续压缩 HELLO/RREQ，自动选档留 V5-05，实机留 V5-07/S06。

### OP-097｜完成 V5-03 HELLO/RREQ 控制帧压缩

- HELLO：删除重复 Source 的 4 B Payload，发送固定为零载荷，接收身份只使用 Frame Source，并继续校验 Network、Profile、Ingress Link 和准入策略；旧 4 B 格式显式返回坏长度。
- RREQ：删除重复 Origin，载荷冻结为 `Target(Profile地址宽度)+Request ID(4)+Cost(2)+Hop(1)+Flags(1)`，四档为 9/10/11/12 B。Cache、转发 Cost/Hop、Candidate Flag 和预算逻辑全部改为 Profile 派生偏移，不缩短 Request ID/Cost。
- 测试：四档 HELLO/RREQ 正向向量通过；W0 三节点 AODV、W3 控制预算和既有回归通过；旧 4 B HELLO 与旧 16 B W0 RREQ 返回 `UCN_ERR_MALFORMED`。Windows Debug Full/Lite 为 CTest `2/2`，Nano 为 `1/1`。
- 边界：v5 协议版本与 Wire Profile 唯一决定控制载荷布局，不启发式兼容旧格式；CRC 仍不是来源认证，生产控制面逐跳认证继续归 S02。下一项进入 V5-04 安全绑定专项验证。

### OP-098｜完成 V5-04 安全绑定专项验证

- 透明中继：把现有三节点 E2E 测试切换到 W0 固定域，A/C 使用测试 Seal/Open Provider，B 只授权并透明转发；明确断言 B 不调用 Open，连续密文业务仍由 C 解密交付。
- 篡改门禁：测试旧 W0 Tag 在重编码成 W1、修改 Destination、修改 Payload Length 后均验证失败；线上 Ciphertext 篡改继续由 CRC/目标 Open 拒绝。中继测试的篡改偏移改为按实际 Profile/Flags 计算 Header，不再使用 W3 常量。
- 文档：新增 `UCN_V5_04_安全绑定验证报告.md`，同步 v5 方案和任务状态，冻结安全终止 Gateway 与未来 Outer/Inner 双层信封的边界。
- 限制：Fixture Provider 不是受审计 AEAD；生产身份、密钥、持久 Replay、撤销和逐跳控制面认证仍归 S02。本项只完成代码契约与软件负向测试，下一项进入 V5-05 自动最小选档。

### OP-099｜完成 V5-05 路由感知自动最小选档

- API：新增 `ucn_frame_select_min_wire_profile()`、Node 自动模式开关和每 Link Peer Ceiling 设置/查询；默认仍为固定档，未启用时行为不变。Link 注册清空未知 Ceiling，HELLO 用固定 TX 档发送并在准入后记录对端档位。
- 选择：业务新发帧按所有数值字段、Hop、Route/Path 扩展、配置上限、Link/状态 MTU 和 E2E Tag 选择最小官方档位；保护 Flag 在选档前确定，随后才 Seal，防止漏算 16 B Tag。AODV Route 用已学习 Hop，直连用 1 Hop；固定 Path 无端到端 Hop 元数据时保持保守默认。
- 路由/切换：未知路径仍以固定档 RREQ 证明整路接收能力；已解码中继帧永不改变档位。下一 Bearer 已知 Ceiling 不兼容时返回错误，不截断或复用旧 Tag；静态 Link 未学习能力时按产品固定域契约处理。
- 测试：Selector 边界、固定/自动、W0/W1 选择、Peer W0 无共同档、HELLO 学习和 W3 建路后两跳 W0 业务均通过。Windows Debug Full/Lite 为 `2/2`，Nano 为 `1/1`。实机吞吐和多 Bearer 干扰仍留 V5-07/S06；下一项只做 V5-06 Extended Gateway 设计评审。

### OP-100｜完成 V5-06 Extended Gateway 设计评审

- RFC：新增 `UCN_V5_06_Extended_Gateway设计评审.md`，区分 Global Identity、Domain ID、Local Alias 和 Generation；给出安全终止/透明 E2E 两种 Gateway、固定 Alias/Directory/Session/Pending/Route 表、租约状态机、Session Replay、双 Gateway、MTU/分片、威胁模型与迁移顺序。
- 资源：使用显式 RAM 公式和初步表项区间，不虚构单一节点上限；Nano 默认不编译，未来 Lite/Full 也必须由独立 Feature 选择。普通 `ucn_node_t` 本轮不增加 Gateway 表或动态内存。
- 决议：方向批准为未来独立 `ucn_gateway_ext`，第一实现候选是静态 Directory + 安全终止 Gateway。动态 Alias/Session Slot 和透明 Inner Envelope 在 S02 生产身份/AEAD/持久 Replay 完成前不实施。
- 边界：当前 v5 仍是单 Wire Domain 内 MCU-first 自组网；Linux 不是运行前提。V5-07 进入全量回归、资源、架构/手册/调用树与知识库同步，硬件条件之外的软件工作继续完成。

### OP-101｜完成 V5-07 软件发布门禁

- 修复：规模模拟器不再按旧 W3 固定偏移读取线上 Source/Message Type，统一通过 `ucn_frame_decode()` 统计 W0～W3；新增 `--wire-mode fixed|auto`，解码异常直接标记模拟失败。三节点 Security Fixture 改为 W3 控制建路、源端自动选择、实际 W0 受保护业务，继续断言中继不 Open、目标端解密。
- 回归：为动态 Profile 新增 `ucn_scale_auto_smoke`，固定/自动模式都进入 CTest。Windows Debug Full/Lite 为 CTest `3/3`、Nano `1/1`；GCC 14.2 Release/Service OFF Full/Lite 为 `3/3`、Nano `1/1`。WSL Ubuntu 24.04 的 Full/Service OFF ASan+UBSan 为 `3/3`，额外 256 Node M4/80‰ 重复自动档通过且无 Sanitizer 报告。
- 规模：同 Seed 的 256 Node、M4、32 B Payload、80‰ 重复对照均交付 `102400/102400`、重复/Loop/背压为 0。固定 W3 为 `6,503,840 B/50.383%`，自动最小档为 `5,175,840 B/63.310%`；CSV 写入 `docs/results/V5/`。
- 资源/MTU：GCC Release/Service OFF 的 Node 为 `2648/5888/9400 B`，Link 均 40 B，Archive `.text` 为 `19116/57688/117164 B`。Nano 33 B、Lite 50 B、Full 64 B Core 编译通过，32/49/63 B 按预期拒绝；明确最小 MTU 是 Core 编译门禁，不冒充大帧测试向量也能在 33 B 下全跑。
- 文档：新增 `UCN_V5_07_发布门禁与软件验证报告.md`，同步 README、整体架构、协议分层、Adapter、使用手册、六平台快速手册、容量/资源、调用树、任务表和 UCN Obsidian 知识库，登记 DOC-025。
- 边界：V5-00～V5-07 的软件范围完成。生产身份/AEAD/密钥/持久 Replay 仍归 S02；真实多板、多介质、目标 ELF/栈/CPU/功耗仍归 S06/S07；Extended Gateway 只完成 RFC，不进入当前小 Core。

### OP-102｜完成 V5-08 全档接收互操作

- 规则：冻结 Build Profile 与 Wire Profile 正交关系。Nano/Lite/Full 都使用 W0～W3 Decoder；产品推荐最低够用 TX/W3 RX，低档节点可接收高档普通 Endpoint 指令，但不会获得未编译的 AODV/Path/Policy/诊断 Feature。
- 测试：`test_node_wire_profile.c` 新增 W0 TX/W3 RX Node，对同一静态命令 Endpoint 依次注入 W0、W1、W2、W3，核对 Header 长度、实际 Profile、Payload 和分发次数；W0/W0 拒绝 W1 的负向门禁保留。
- 回归：Windows Debug/Release 的 Nano `1/1`、Lite/Full `3/3`；WSL Full 在 V5-09 配置契约同时启用时 ASan+UBSan `6/6`。本项没有增加 Node 字段、动态内存或线字节。
- 文档：新增 `UCN_V5_08_全档接收互操作报告.md`，同步 API 注释、v5 方案、固定域报告、整体架构、协议档案、使用/快速手册、任务表与 UCN 知识库。

### OP-103｜完成 V5-09 全局公共配置与回退双保险

- 公共入口：新增 `include/ucn/ucn_config.h`；排除 include guard 与配置控制宏后，集中列出公共头现有 103 个分散 `#ifndef UCN_*` 默认。`ucn_profile.h`/`ucn_types.h` 优先加载它，原头文件默认定义全部保留。优先级为编译器/CMake、产品 `UCN_USER_CONFIG_HEADER`、统一默认、原文件回退。
- 构建入口：CMake 增加 `UCN_USER_CONFIG_HEADER` 与 `UCN_USER_CONFIG_INCLUDE_DIR`，以 `PUBLIC` 配置传给 Core 消费者；独立 Full/Service ON、128 B Frame/64 B Payload 产品头构建通过。运行期 Node/Network ID、Session、密钥、ACL、引脚和驱动句柄没有进入公共编译头。
- 测试：新增默认、`UCN_CONFIG_NO_DEFAULTS` 回退和产品部分覆盖三项契约测试，独立执行 `3/3`；排除 include guard 与配置控制宏后，自动盘点现有分散 `#ifndef` 默认项为 `103/103`。33/50/64 B 最小 MTU 编译通过，`UCN_MAX_HOPS=255` 按静态门禁预期拒绝，WSL ASan+UBSan 含配置专项 `6/6`。
- 过程修正：最初把“256 B 默认值”契约无条件加入所有构建，导致 33 B 专用 Build 被测试自身污染；已改为 `UCN_BUILD_CONFIG_CONTRACT_TESTS=ON` 才启用的独立测试组。普通产品和最小 MTU 构建不再携带默认值专项，边界与既有门禁一致。
- 文档/知识库：新增 `UCN_全局公共配置说明.md`，同步 README、架构、协议档案、快速手册、任务表和 UCN Obsidian 构建/帧/测试/路线图/源码地图/总账。未上传、未烧录、未访问串口。

### OP-104｜完成 V5-10 单档与混档极限模拟

- 模拟器：`ucn_scale_sim` 新增 `--wire-profile w0|w1|w2|w3|mixed`；每个 Node 真实配置固定 TX/W3 RX，逐节点 CSV 增加档位，新增逐档 `_profiles.csv` 与 W0～W3 节点数汇总。阶梯脚本可选择 Lite/Full、固定/自动档和多种 Wire布局，并跳过 W0/mixed 超过 254 的无效单域组合。
- 协议修复：首轮 mixed Smoke 发现 HELLO 把 Peer TX 档误当成 RX Ceiling，导致 W0 TX/W3 RX 节点无法由 W3 Peer 正常发送。HELLO 现用 1 B 发布 `max_receive_wire_profile`，并拒绝非法/矛盾声明；W0 A 与 W3 B 双向业务专项、Lite/Full mixed Smoke 均通过。
- 地址边界：W0 和包含 W0 的四档 mixed 在扁平单域都以 254 Node 为硬上限，255 负向拒绝；4096 四档混合不是合法单域。W1/W2/W3 在当前 Harness 4096 上限分别完成测试，不能外推成真实介质容量。
- 结果：19/19 组 Release 正式场景为 PASS。本地高负载 W0/254、mixed/254、W1/W2/W3/4096 均 100% 交付；全网同时 two-hop 时交付约 15.35%/11.38%，各档交付完全一致，瓶颈是默认 Route/Discovery/Pending 工作集。25‰ 丢包、80‰ 重复、7 ms 延迟和周期断链下五种布局均无重复业务、Route Loop 或 Harness 背压。
- 回归：Windows Debug 的 Nano `1/1`、Lite/Full `9/9`，GCC `-Wall -Wextra -Wpedantic -Werror` Release 的 Lite/Full `9/9`；其中五布局 Smoke、W0/mixed 255 拒绝和 auto 全覆盖。WSL Full ASan+UBSan 含配置契约为 `12/12`，另跑 mixed/254 故障压力通过。新增 `UCN_V5_10_单档与混档极限模拟报告.md` 与 `docs/results/V5_10/` 总表/逐节点证据；未上传、未烧录、未访问串口。

### OP-105｜确认 v5 分支审计问题并拆分 V5-11～V5-16

- 基线：对照本地/远端一致的 `codex/v5-adaptive-wire@fe3cb0d`，逐项读取 Frame/Node/Link/Storage、Path/AODV/Wire 测试和现有知识库，不把外部审计文本直接当源码事实。
- 确认：控制面固定 TX 档、Path 可写入不可表达状态、16 bit 累计 Cost、全局而非 per-Link RX、RREP 冗余/一致性缺口、部分控制 Payload未压缩和版本 6 bit 缺断言均真实存在；其中 Link MTU 当前明确是 UCN 逻辑 MTU，因此 per-Link RX 属于增强而非现有契约错误。
- 决策：建立 V5-11 硬门禁、V5-12 自动控制面、V5-13 per-Link RX、V5-14 32 bit Cost/RREQ/RREP、V5-15 其余控制 Payload和V5-16 Authorized Class。Gateway继续归 V5-06；生产安全继续归 S02；不采用默认只编 W0 Decoder，因为 V5-08 已冻结全节点四档解析目标。
- 文档：新增 `UCN_V5_11_审计问题确认与执行方案.md`，登记 DOC-029；本项只完成确认/拆分，代码从 V5-11 开始，未访问硬件/COM。

### OP-106｜完成 V5-11 审计硬门禁

- 修改：将 `UCN_PROTOCOL_VERSION` 纳入全局公共配置并增加 `<=0x3F` 的 C99 编译期断言，避免 6 bit Wire Version 被静默截断。
- 修改：Path 本地安装/撤销、远端发送和接收安装均按实际 Wire Profile 校验 Path ID、Destination 与 Next Hop 的可表达范围；远端越界帧在授权回调和写表前失败关闭。
- 修改：旧 18 B RREP 在迁移到 V5-14 新格式前，强制 Payload Origin/Target 与 Header Destination/Source 一致。
- 测试：`build_v5_full` Debug 编译通过，CTest `9/9`；新增 W0 Path ID 256、本地越界目标/下一跳、远端伪造 Path 安装不写表、RREP Origin 不一致拒绝测试。
- 负向测试：独立 `build_v5_invalid_protocol64` 以 `-DUCN_PROTOCOL_VERSION=64` 构建，按预期在 `ucn_types.h` 编译期断言处失败。
- 范围：未访问硬件/COM；下一项进入 V5-12 控制面自动选档。

### OP-107｜完成 V5-12 控制面自动选档闭环

- 修改：新增显式 Profile 与自适应 Profile 两种内部控制发送入口；固定模式仍使用 Node TX Profile，Auto 下 HELLO/Heartbeat 才交给最小可表达档选择器。
- 修改：RREQ 在发送前按 Network/Source/Target/Session/配置 Hop 选择能表达完整搜索域的最小档，转发过程中不升降档；RREP 显式继承收到的 RREQ Profile，避免返回链意外扩宽。
- 边界：PATH/TRACE/SNAPSHOT 等带额外 32 bit 语义字段的控制 Payload 不做隐式缩窄，继续由 V5-15 的显式 Profile-aware Codec 处理。
- 测试：新增 W3 TX Maximum + Auto 源节点、两个 W0 RX/TX 节点的 HELLO→RREQ→RREP→Data 两跳完整链，逐段断言线上均为 W0；保留原固定 W0/W1/W2/W3 控制帧测试；新增 W1 搜索域遇 W0 Peer RX Ceiling 返回 `UCN_ERR_UNSUPPORTED` 且不残留 Pending。
- 验证：`build_v5_full` Debug 编译通过，CTest `9/9`；未访问硬件/COM。

### OP-108｜完成 V5-13 per-Link 本地 RX Ceiling

- 修改：`ucn_link_t` 增加本地 RX Wire Ceiling，零值/`UNSPECIFIED` 继承 Node 上限；字段放入现有对齐空隙，当前 Host Full/Nano 的 `ucn_link_t` 都保持 40 B。
- 修改：新增 `ucn_node_set/get_link_local_wire_profile_limit()`；允许在注册前收窄 Link，使小 MTU 介质无需为了 Node 的全局 W3 RX 上限而被拒绝。
- 修改：Link 注册按有效本地 Ceiling 校验最小 Header；Ingress 使用 Node/Link 有效上限失败关闭；HELLO 在每条 Link 上广告该 Link 的实际本地接收上限，不再一律广告 Node 全局值。
- 修改：Full/Lite 与 Nano 实现保持同一 API/接收语义；Link MTU 仍表示 Adapter 完成分段重组后的 UCN 逻辑 MTU。
- 测试：同一 W3 Node 的 W0 Link 拒绝 W1/W3、W3 Link 接受 W1/W3；W0 Link 以 `W0 Header+1` MTU 注册并发送 HELLO，帧与广告值均为 W0；非法档位和 MTU 不足的扩宽请求被拒绝。
- 验证：Full Debug CTest `9/9`，Nano CTest `1/1`；资源输出 Full `node=9400 B/link=40 B`、Nano `node=2648 B/link=40 B`；未访问硬件/COM。

### OP-109｜完成 V5-14 长距离 Cost 与 RREQ/RREP

- 修改：新增 `ucn_route_cost_t=uint32_t`，Route/Candidate/RREQ 状态保存 32 bit 累计 Cost；Link 指标输入仍为 16 bit。累计 Unknown 为 `0xFFFFFFFF`、最高 Known 为 `0xFFFFFFFE`，真实加法溢出失败关闭。
- 线格式：W0/W1/W2/W3 Cost 使用 1/2/3/4 B，全 1 为各档 Unknown；RREQ Payload 为 `8/10/12/14 B`，RREP 为 `8/10/11/12 B`。Request ID 固定 4 B，RREP Origin/Target 改由 Header Destination/Source 唯一表达，旧 18 B 格式拒绝。
- 测试：四档最高 Known/Unknown/再加一溢出、80,000 对 100,000 双路径选择、AODV/Candidate 回归通过。规模模拟器新增 `line` 与 `end-to-end`，并修复端到端 0 交付也判 PASS 的漏判。
- MTU：移除 Lite 对旧 18 B RREP 的残留门禁，当前最大 W3 动态路由控制载荷为 14 B；46 B Core 构建通过、45 B 按预期拒绝，并修复 Lite 裁剪下新 Profile helper 的未使用警告。
- 长链：201 Node/200 Edge、每 Edge Cost=1,000；Hop 预算 200 为 `accepted=20/delivered=0/FAIL`，预算 201 为 `20/20/PASS`。这是 Host 状态机证据，不代替真实 MCU 长链性能。
- 文档：新增 `UCN_V5_14_长距离Cost与RREQ_RREP实现报告.md`，同步 Cost 契约、V5-03 历史报告、任务表与调用树。

### OP-110｜完成 V5-15 Profile-aware 控制载荷

- 修改：普通/Path RERR、PATH_INSTALL/REVOKE、Path Trace、Node Snapshot Reply 中的 Node/Path/Session 字段全部按帧 Wire Profile 宽度编解码；Snapshot 待发槽保存请求 Profile。
- 自动档：Path 控制和 Trace 新发请求按全字段、Hop、MTU、Peer RX Ceiling 选择最小安全档；RERR、Trace/Snapshot Reply 保持触发/请求 Profile，中继不静默升降档。
- 测试：四档长度/字段、坏长度、精确最小 MTU、W0 旧固定宽度拒绝、W3 Auto→W0、Trace 截断和 Path authorizer/token/写表顺序均覆盖；Full 单测通过。
- 评估：Candidate Probe/Activate 不携带通用 Node/Path 字段；Policy Diagnostic 是独立固定 32 B、分页 Schema，因此本轮保持版本不变，不为表面压缩制造新兼容面。
- 资源：当前 Host Full `ucn_node_t=9464 B`、`ucn_link_t=40 B`，无动态内存。新增 `UCN_V5_15_Profile感知控制载荷实现报告.md` 并同步调用树。

### OP-111｜冻结 V5-16 Authorized Class 与控制预算设计

- 文档：新增 `UCN_V5_16_Authorized_Class与控制预算设计.md`，严格区分 Wire Profile、RX Ceiling 和授权等级。
- 模型：定义 C0 Participant、C1 Edge Relay、C2 Mesh Operator、C3 Backbone/Gateway；等级绑定认证 Principal、Session Generation、ACL、Token Budget 与 Fanout，不允许 Frame 自报 Profile 获得权限。
- 状态：冻结接收入站顺序、升级/降级、Session 轮换、重启、多 Bearer、撤销和表满时的默认拒绝行为。
- 决策：当前 Fixture Provider 不是生产身份基础，S02 未完成前不写“看似有等级、实际可伪造”的实现；Extended Gateway 继续归 V5-06 独立模块。本项是设计完成，不宣称代码已启用。

### OP-112｜完成审计轮全量回归、反向核对与知识库同步

- 回归：Windows Debug/Release 的 Full/Lite 均为 CTest `10/10`，Nano 为 `1/1`；独立配置契约 `4/4`。新增 `ucn_scale_end_to_end_smoke`，使线形端到端必须有实际交付才能通过。
- Sanitizer：WSL Ubuntu 24.04 Full/Service OFF 的 ASan+UBSan 为 `13/13`，未报告越界、Use-after-free、泄漏或未定义行为。
- 负向：Protocol Version=64 按 6 bit 门禁拒绝；Nano/Lite/Full 最小帧为 33/46/64 B，32/45/63 B 拒绝；Hop 预算 200 的 201 Node/200 Edge 场景 0/20 并判 FAIL，预算 201 为 20/20/PASS。
- 资源：GCC 14.2 Release/Service OFF 的 Node 为 `2648/5888/9464 B`，Link 均 40 B，Archive `.text` 为 `19384/58580/116360 B`。
- 文档：同步 README、架构/协议/容量/Cost/最小 MTU、V5-14/V5-15 报告、V5-16 设计、四份调用树、DOC-030～032，以及 UCN Obsidian 帧/路线图/Adapter/构建/测试/任务/操作记录。
- 静态检查：项目 Markdown 相对链接、Obsidian 内链、11 份调用树 YAML 和 123 个 Node/180 个调用引用均闭环；`git diff --check` 无空白错误。
- 边界：未提交、未推送、未烧录、未访问 COM。生产身份/AEAD/撤销/Authorized Class 执行层仍归 S02，真实 MCU/介质性能归 S06/S07。

### OP-113｜建立审计遗留问题实际修复方案与 V5-17～V5-21 任务

- 复核：以本地/远端一致的 `codex/v5-adaptive-wire@84880ce` 为基线，确认旧审计把 V5-11～V5-16 判为未公开的前提已经过期；累计 32 bit Cost、RREP 新格式、Path 表达门禁、Profile-aware 控制载荷、per-Link RX 和 Version 断言不重复返工。
- 真实遗留：确认 Pinned Path 仍使用 Node 默认 Hop、RREQ 尚无有界逐级扩散、RX Ceiling 仍在完整 Decode/CRC 后检查；生产安全和 Authorized Class 执行层仍未完成。
- 设计：新增 `UCN_V5_审计遗留问题实际修复方案.md`，冻结“Wire 可表达不等于业务可用”的四层边界；W0～W3 的 4/16/64/254 只作编码上限，产品默认仍以 16 Hop 或更小值运行。Route 还必须通过业务 Hop、Cost 和可选已验证 RTT 门禁。
- 任务：新增 V5-17 路由可用性/延迟、V5-18 Pinned Path Remaining Hops、V5-19 2→4→8→16 有界 Expanding Ring、V5-20 Ingress Profile Peek、V5-21 Authorized Class 执行层；V5-21 明确阻塞于 S02，不允许从 Wire Profile 自报值提权。
- 验证：项目 Markdown 相对链接、知识库 WikiLink、代码块闭合、V5-17～V5-21/T55/L070 映射和 `git diff --check` 均通过。未修改源码/测试，未提交、未推送、未访问硬件/COM。

### OP-114｜执行 V5-17～V5-20 实际修复并完成软件闭环

- V5-17：新增 Node 默认与单 Policy 的 Hop/Cost/已验证 RTT 约束、路由质量查询和直接/动态/Pinned/Balance 路线门禁；Candidate Path Probe ACK 计算端到端 RTT EWMA，有限 Cost 未知或必需 RTT 未验证时失败关闭。动态 Route 实质更新时即使首跳不变也清除旧端到端 RTT，必须重新验证。Nano 的高级路由 API 保持统一签名并返回 `UCN_ERR_CONFIG`。
- V5-18：Path 配置、转发表和公开安装 API 增加 `remaining_hops`；PATH_INSTALL 四档载荷更新为 8/11/14/17 B，旧长度拒绝；源、中继、终端按 2→1→0 一类真实 Scope 校验并逐跳递减。Node Storage Layout 版本从 2 升到 3。
- V5-19：实现默认 2→4→8→16 Expanding Ring，单 Ring 250 ms、总发现预算 1000 ms；每轮分配新 Request ID 并重新选择最小可表达 Profile。自动 Q1 Pending 不再反复重启活动 Discovery，规模模拟中的发现饥饿一并修正。
- V5-20：新增公开 3 B Magic/Version/Profile Peek；Full/Lite/Nano 均在完整 Decode/CRC 前执行 per-Link RX Ceiling。新增坏 CRC 高档帧测试后发现 Nano 顺序不一致，已修正 Nano 接收入口并保留同一错误优先级。
- 测试：Windows Debug Full/Lite `10/10`、Nano `1/1`；Release Full/Lite `10/10`、Nano `1/1`；独立配置契约 `4/4`；完整 2→4→8→16 截止、坏 CRC 早拒绝、Path 四档/逐跳、约束与 RTT 回归均通过；`git diff --check` 无空白错误。
- 资源：GCC 14.2 Release/Service OFF 的 Nano/Lite/Full Node 为 `2648/5944/9728 B`，Link 均为 40 B，Archive `.text` 为 `19692/65720/123876 B`；这是 Host ABI/裁剪证据，不是目标 MCU RAM/Flash。
- Sanitizer：从 WSL Ubuntu 24.04 新建独立 `build_v5_v517_asan`。首次运行发现测试缓冲仍按 17 B 最大合法 Payload 分配，却故意声明 18 B 坏长度，导致测试自身越界读取；扩大坏长度测试缓冲后，Full/Service OFF 的 ASan+UBSan `13/13` 全通过。
- 边界：本轮未访问硬件/COM；已按用户后续授权纳入 `codex/v5-adaptive-wire` 本地提交，尚未推送远端。V5-21 继续严格阻塞于 S02，真实介质 RTT/CPU/栈/功耗归 S06/S07。

### OP-115｜建立最新审计交叉问题修复建议与 V5-22～V5-26

- 基线：复核本地与远端一致的 `codex/v5-adaptive-wire@a8567e0`，逐项对照 Pending Q1、Wire Cost、Candidate Probe/Activate/Epoch、Full/Lite/Nano Ingress 和公开 Cost 契约。
- 结论：四项 P1 均真实存在；其中 W0 Cost 改 2 B 只覆盖单跳，无法覆盖 W0 四跳或 W1 十六跳合法累计值，因此采用控制 Payload Cost Width `3/3/3/4 B`，普通业务 Header 不变。
- 文档：新增 `UCN_V5_最新审计交叉问题修复建议.md`，冻结 Q1 绝对 Deadline、Candidate Profile 连续性、运行期 Hop Scope 和明确不修改项。
- 任务：建立 V5-22～V5-26；因 DOC-030 已用于 V5-14，新的文档/知识库闭环任务顺延为 DOC-033，不覆盖历史编号。
- 保留：S02/V5-21 继续阻塞；不从 Wire Profile、Node ID 或 Bearer 推导权限，不修改 Extended Gateway，不把 Host 软件测试写成实机结果。
- 状态：本项只建立建议和执行台账；随后从 V5-22 开始修改源码和测试，尚未访问硬件/COM。

### OP-116｜执行 V5-22～V5-26 并完成审计交叉修复闭环

- V5-22：Q1 Pending 改为绝对 Deadline；内部重试不再重新入队或延长等待，新业务值覆盖旧值时才刷新 Deadline。恢复路由后只发送最新值一次，并增加覆盖、重试、成功和永久失败统计。
- V5-23：RREQ/RREP 的 Wire Cost Width 固定为 W0/W1/W2/W3=`3/3/3/4 B`；各档合法最大 Hop×单跳最大 Cost 均可表示，旧 W0/W1 控制载荷按坏长度失败关闭。普通业务基础头和独立 Link Cost 契约未改变。
- V5-24：Candidate Route 保存发现时的真实 Wire Profile；Probe、ACK、Activate 和 Route Epoch 均沿用该 Profile。混档拓扑、错误 Profile ACK 和 W0 Epoch 边界均由专项测试覆盖。
- V5-25：Ingress、Route/Candidate 学习、RREQ 原始 Ring、RREP 已学习 Hop、PATH_INSTALL Remaining Hops、Path Trace 返回以及本地安装均执行运行期 Hop Scope；Nano 同样在接收入口拒绝超预算帧。
- V5-26：补充 Q1 绝对截止、Cost 最大边界与逐跳转发、候选 Profile 连续性、错误 ACK、Epoch 保留值、Nano/Lite/Full 全档接收及运行期 Hop 拒绝交叉测试。
- 回归：Windows GCC 14.2 的 Debug/Release、Service ON/OFF 三 Build Profile 均通过；Full/Lite 为 `10/10`，Nano 为 `1/1`。WSL Ubuntu 24.04 GCC 13.3 Full/Service OFF 的 ASan+UBSan 与配置契约共 `13/13`，未报告内存或未定义行为错误。
- 资源：Release/Service OFF Host ABI 下 Nano/Lite/Full Node 为 `2648/5960/9744 B`，Link 均为 `40 B`，Archive `.text` 为 `19724/67316/125448 B`；仅作为主机裁剪证据，不等同目标 MCU RAM/Flash。
- 文档：同步 README、架构、协议/Profile、容量、Cost、V5-03/V5-14/V5-15/V5-17～20 关联报告、四份调用树、任务表与 UCN Obsidian 知识库；旧操作记录中的阶段数值作为历史证据保留。
- 静态核对：建议→任务→源码→专项测试→保留项共 12 个反向映射均存在；项目 Markdown 共检查 182 个相对链接、UCN 知识库 57 份笔记/491 个 WikiLink，缺失均为 0；调用树索引覆盖 10 个模块、128 个节点和 189 个调用引用，重复 ID、内部缺失目标与源文件坏路径均为 0；`git diff --check` 无空白错误。
- 保留边界：S02/V5-21 生产安全与 Authorized Class 继续阻塞；Extended Gateway、权限模型和独立 Link Cost 语义不改。未访问硬件/COM、未烧录、未提交、未推送。

### OP-117｜全面核对 v5 文档与当前代码

- 基线：以本地和远端一致的 `codex/v5-adaptive-wire@f941ae9` 为准，逐份核对原有 16 份 v5 专题，并交叉检查 README、整体架构、协议分层、容量、使用手册、全局配置、Cost 契约、任务表和调用树。
- 修正：V5-05 补齐控制面 Auto、2→4→8→16 Ring、Candidate Profile 和 Path `remaining_hops`；总设计修正 Authorized Class 与 Wire Profile 的正交关系、W0 Epoch 精确值/grace 语义；阶段性测试数、资源值和“下一项”均改成历史/当前分栏；旧 V5-09/V5-10 状态头、“v4 当前”、快速手册 V5-07、调用树 V5-20 和 S08 指向 V5-01 的旧入口均同步到 V5-26。
- 当前事实：Base/Route/Path Header 为 17/21/26/30、18/23/28/32、19/25/31/36 B；Cost 为 3/3/3/4 B；默认 Build/Node Hop 上限 16；Q1 Pending 深度 4、绝对 Deadline 1000 ms；Storage Layout Version=4。
- 资源：重新执行 Release/Service OFF 的测试二进制与 `size`，Nano/Lite/Full Node 为 `2648/5960/9744 B`，Link 均 `40 B`，Archive `.text` 为 `19724/67316/125448 B`；仅作为 Host 证据。
- 回归：Windows Debug/Release/Service OFF 的 Full/Lite 均 `10/10`，Nano 均 `1/1`；Windows 配置契约 `4/4`；WSL Full ASan+UBSan 与配置契约 `13/13`。
- 静态核对：17 份 v5 文档提取 50 个协议标识符，无意外代码缺失；`UCN_ERR_SCOPE`/`ucn_gateway_ext` 仅作为“当前不存在”边界。项目 59 份 Markdown 相对链接无缺失；调用树 10 模块/128 节点/189 调用无重复、缺目标或真实源码坏路径；知识库 58 份笔记 WikiLink 无缺失；`git diff --check` 通过。
- 知识库：新增“v5 文档与代码一致性审计”，同步当前基线、测试/资源、路线图、T57 与 L073；历史操作记录不回写，只由当前页面明确替代关系。
- 边界：本轮仅修改文档与知识库，未改协议源码、构建配置和测试代码，未访问硬件/COM。S02/V5-21、Extended Gateway、真实 MCU/多板多介质与小 MTU Carrier 仍保持未完成；未提交、未推送。

### OP-118｜登记 V5-27～V5-30 代码缺陷修复任务

- 基线：核对 `codex/v5-adaptive-wire@f941ae9` 与新增 `UCN_V5_代码缺陷审计报告.md`；确认本地与远端提交一致，并保留工作区已有文档修改。
- 结论：V5-AUD-01、V5-AUD-02、CUR-AUD-01 均有当前源码证据；前两项分别为 v5 Path/Wire 与动态 MTU 问题，Policy 误判根因已存在于 v4 基线。
- 补充边界：Path 黑洞不只发生于主备切换，首跳能力高于后续活动跳时同样可能出现；统一 Build MTU 契约与逐 Link 动态 MTU 必须在实现中明确分层。
- 任务：建立 V5-27 动态 MTU、V5-28 Path/Bearer 能力与确定性 RERR、V5-29 Policy 逻辑 Bearer 活性/质量、V5-30 专项回归与文档闭环。
- 设计约束：继续使用 C99、固定容量和 Feature 裁剪；Path/Policy 新状态只进入 Full，不向 Nano/Lite 增加无关常驻表；中继禁止改写已封装 E2E 帧。
- 当前状态：已完成审计确认和任务登记，接下来从 V5-27 开始修改源码与测试；尚未访问硬件/COM。

### OP-119｜执行 V5-27～V5-30 并完成异构 Bearer、动态 MTU 与 Policy 闭环

- V5-27：新增公共 `ucn_link_effective_mtu()`，统一 Full/Nano 的静态/运行时 MTU 语义；支持 `.mtu=0` 动态 Link，零有效 MTU失败关闭，恢复后无需重注册。
- V5-28：为 Path 表和 PATH_INSTALL 增加端到端最大 Wire Profile/最小 MTU；本地同邻居 Bearer 求共同能力，新增 capability-aware 安装 API。PATH_INSTALL 当前为 `11/14/17/20 B`，Storage Layout Version=5。中继遇到 `UNSUPPORTED/TOO_LARGE` 时撤销匹配 Path、累计诊断并回送 Path-RERR；低 TX/W3 RX 中继仍可透明转发高档 E2E 帧。
- V5-29：物理 Link 继续独立采样；Path 活性、持续拥塞和 AUTO_BALANCE 改为解析当前逻辑 Bearer。主 Bearer Down、备链健康时 Policy 不再误判整条 Path Down，并可从旧 Down 恢复 Verified。
- V5-30：补充动态 MTU、主 W3/备 W0、MTU 40、窄后续跳确定性 RERR、透明密文中继、周期质量刷新与 Storage/控制载荷测试；更新调用树、架构、分层、容量、使用手册、全部 v5 当前状态页和 UCN Obsidian 知识库。
- 回归：Windows Debug/Release 的 Full/Lite 各 `10/10`、Nano 各 `1/1`；Release/Service OFF 再执行 Full/Lite `10/10`、Nano `1/1`。WSL Full ASan+UBSan 与配置契约 `13/13`，GCC `-fanalyzer` 与配置契约 `13/13`。
- 资源：Windows x64 GCC 14.2 Release/Service OFF 的 Nano/Lite/Full Node 为 `2648/5960/9752 B`，Link 均 `40 B`，Archive `.text` 为 `19820/68180/129124 B`；仅是 Host ABI/裁剪证据。
- 静态核对：项目 60 份 Markdown 相对链接缺失 0；知识库 59 份笔记、511 个 WikiLink 缺失 0；代码块未闭合 0；调用树 10 模块、131 节点、193 个调用引用无重复/缺目标/坏源码路径；`git diff --check` 无空白错误。
- 边界：没有提交、推送、烧录或访问硬件/COM。S02/V5-21、Extended Gateway 和 S06/S07 真实多板/多介质时延、吞吐、CPU、栈、功耗仍保持未完成。

### OP-120｜登记 PATH_INSTALL v5 兼容与非 Full API 符号修复

- 复核：当前工作树把 PATH_INSTALL 从 `8/11/14/17 B` 扩为 `11/14/17/20 B`，接收端只接受新长度，但 `UCN_PROTOCOL_VERSION` 仍为 5 且 HELLO 没有 Path Schema 协商；新旧 v5 节点双向都会按坏长度拒绝。
- 符号：公共头无条件声明两个 capability API，但现有 Lite/Nano 静态库只导出旧 Path API；`test_public_headers.c` 和非 Full Profile 测试没有引用新符号，因此回归未发现链接缺口。
- 决策：不把整个协议升级到 v6。旧 API 固定发送 v5 基础格式，新 capability API 显式发送扩展格式，新接收端双格式解码；扩展能力只在调用方明确选择新 API 时使用。
- 任务：建立 V5-31 Schema 兼容、V5-32 非 Full Stub/链接门禁、V5-33 发布回归与文档知识库同步。
- 边界：本项只登记修复合同；尚未提交、推送、烧录或访问硬件/COM。

### OP-121｜完成 PATH_INSTALL 双格式兼容与非 Full API 发布修复

- V5-31：保留 `UCN_PROTOCOL_VERSION=5`；`ucn_node_send_path_install()` 恢复发送基础 `8/11/14/17 B` Schema，`ucn_node_send_path_install_capable()` 显式发送扩展 `11/14/17/20 B` Schema。接收端只接受这两组精确长度；基础格式不携带能力，由接收节点按本地 Bearer 交集推导，坏中间长度仍失败关闭。
- 兼容边界：旧 v5 接收端继续接收旧 API 生成的基础格式；旧接收端没有扩展 Schema 能力，调用方主动使用 capability API 前必须确认目标支持。没有伪造 HELLO 协商，也没有把局部修复包装成协议 v6。
- V5-32：Lite/Nano 的 `ucn_profile_stubs.c` 补齐 `ucn_node_install_local_path_capable()` 与 `ucn_node_send_path_install_capable()`；NULL Node 返回 `UCN_ERR_ARGUMENT`，有效 Node 返回 `UCN_ERR_CONFIG`。公共头测试和非 Full Profile 测试直接引用两个 API，三档静态库均用 `nm` 核实导出符号。
- 专项测试：W0～W3 同时覆盖基础/扩展发送与接收、自动 W3→W0 基础格式、四档坏中间长度拒绝和授权调用顺序；现有端到端瓶颈、异构 Bearer 与 Path-RERR 测试继续覆盖扩展格式。
- 回归：Windows Full/Lite/Nano Debug+Service ON 分别 `10/10`、`10/10`、`1/1`；Release+Service OFF 同样全部通过；配置契约 Full 为 `13/13`。WSL Full ASan+UBSan 与配置契约 `13/13`，GCC `-fanalyzer` 与配置契约 `13/13`；`git diff --check` 无空白错误。
- 资源：Windows x64 GCC 14.2 Release/Service OFF 的 Nano/Lite/Full Node 仍为 `2648/5960/9752 B`，Link 均为 `40 B`；静态库文件大小为 `35612/87032/152674 B`，只作 Host 构建证据，不等同 MCU Flash。
- 文档与知识库：新增 V5-31～V5-33 修复报告，同步任务表、载荷表、API 说明、调用树和 UCN Obsidian 知识库；项目 204 个 Markdown 相对链接缺失 0，知识库 60 份笔记/525 个 WikiLink 缺失 0，调用树 10 模块/132 节点/194 个调用引用无重复、缺目标或源码坏路径；旧阶段记录保留为历史证据。
- 边界：本轮没有访问硬件/COM、烧录、提交或推送。S02/V5-21、Extended Gateway 与 S06/S07 实机测试仍保持原边界。

### OP-122｜发布 V5-27～V5-33 修复到 GitHub v5 分支

- 发布范围：统一提交 V5-27～V5-33 的异构 Bearer、动态 MTU、逻辑 Bearer Policy、PATH_INSTALL 双格式兼容、Lite/Nano capability API Stub、正式测试、调用树及配套文档。
- 目标：`codex/v5-adaptive-wire` 推送至 `origin`（`https://github.com/ice-infinite/UCN.git`）；提交说明为 `feat(v5): harden adaptive path and bearer routing`。
- 发布前核对：本地与远端旧基线均为 `f941ae9`、分支无分叉；50 个发布文件的敏感凭据模式命中为 0；修正 Markdown 行尾空白后执行 `git diff --cached --check`。
- 验证沿用：Windows Full/Lite/Nano Debug 与 Release/Service OFF 回归、WSL ASan+UBSan、GCC `-fanalyzer`、跨 Profile 公共 API 符号核对、项目链接与知识库内链检查均已通过，详细结果见 OP-119、OP-121 和两份专项修复报告。
- 边界：仓库外的 Obsidian UCN 知识库不纳入 Git 提交，仅在远端推送成功后同步发布状态；本次仍未访问硬件/COM、烧录或新增实机验证。

### OP-123｜按当前 v5 实现更新多环境快速使用手册

- 范围：更新 `docs/快速使用手册/` 总览和裸机、通用 RTOS、FreeRTOS、Zephyr、NuttX、RT-Thread 六份手册，同时修正 `UCN_使用与调用手册.md` 中相同的示例问题。
- Profile：用 Nano/Lite/Full 与正交 Service Feature 的真实源文件矩阵替代固定源文件清单；明确 Lite/Nano 的 Path capability API 仅提供返回 `UCN_ERR_CONFIG` 的公共符号，Full 才有实现。
- 初始化：W1 示例 Network ID 改为可表达的 `0x4E01`；Link 仅调用 `ucn_node_register_link()`，不再提前重复 `open()`；注册或初始化失败进入产品安全态，不静默继续。
- 调度与安全：Bridge 改用显式 `ucn_service_protocol_bridge_step_at()`，与 `ucn_node_step()` 共用本轮单调时钟；补充非零 Boot Session、生产 Security Provider、远端 Q0 Validator 和失败不降级约束。
- Link：统一描述静态/动态 MTU 取最小值、双零暂时 Link Down、运行时恢复无需重注册，以及每 Link 本地接收 Profile 上限；Path 扩展格式仅在 Full 且调用方已确认目标能力时使用。
- 平台模板：修正 RT-Thread 线程控制块类型；Zephyr 示例补齐 Full+Service 源集和专用协议栈配置；FreeRTOS Service ID 改为产品常量，并明确仓库外 ESP32 工程含 `UCN_PROTOCOL_VERSION == 4` 门禁，只能作为 v4 Port 结构历史参考，不能声称已通过当前 v5 编译。
- 验证：Windows 现有构建 Full `13/13`、Lite `13/13`、Nano `4/4` 全通过；8 份更新文档的相对链接、代码块闭合、74 个 UCN 库调用标识符、6 个产品模板入口、旧错误模式和 `git diff --check` 均通过。
- 边界：本轮未修改 Core/测试源码、仓库外 ESP32 工程、硬件、COM 或固件；文档更新尚未提交或推送。

### OP-124｜冻结标准 Port/Adapter 封装与默认 Link Cost 初始基线

- 设计：新增 `UCN_标准Port_Adapter封装与默认Cost基线方案.md`，规定产品配置 → 标准 Port → 标准 Adapter → Core 的四层边界；后续 API 以静态 Preset/配置对象绑定真实 HAL，不向 `ucn_core` 混入 ESP-IDF、Zephyr、NuttX 或具体驱动。
- 介质：冻结 UART/RS-485、经典 CAN、CAN-FD、Wi-Fi/ESP-NOW、USB CDC 的初始 Cost 表；经典 CAN 在有界 Carrier 分段/重组完成前必须配置失败，USB 首版只支持固定 CDC 点对点 Link，Wi-Fi 只作一跳 Bearer、UCN 自身继续负责 Mesh。
- Cost：维持 `route_cost` 为稳定、可加基础 Cost；新增后续 `effective_select_cost = base + queue + failure + RTT` 的本节点选择分合同。动态项来自既有 500 ms 采样/25% EWMA，不能写回 RREQ/RREP、不能让 Q0 逐帧切换，也不能与 Adapter 重复加权。
- 依据：核对当前 `ucn_link.h`、`ucn_policy.h/.c`、Node 选路和 Cost 契约；ESP-NOW 默认 1 Mbit/s、USB FS/HS 12/480 Mbit/s、经典 CAN 1 Mbit/s/CAN-FD 数据阶段能力的外部标准资料仅用于设定保守初值，未当作实测吞吐。
- 任务：新增 V5-34～V5-41 和 DOC-037，依次覆盖 Preset Resolver、动态选择分、四类 Port、ESP32 v5 UART/ESP-NOW、CAN-FD/USB、经典 CAN Carrier 与实板标定。
- 边界：本轮仅新增设计、任务和 Cost 契约交叉链接；未修改 Core/测试/外部 ESP32 工程，未构建、烧录、访问硬件或 COM，未提交/推送。

### OP-125｜补充扩展 Bearer 路线与单一 Mesh 责任边界

- 设计：在标准 Port/Adapter 方案新增以太网、RS-485、BLE LE、IEEE 802.15.4、NRF24/私有 2.4G、LoRa P2P/FSK、UWB、SPI/I²C、受控 IP Tunnel 的 P0/P1/P2 路线。
- 边界：明确 Wi-Fi Mesh、BLE Mesh、Zigbee Mesh、Thread Mesh 与 LoRaWAN 不能与 UCN 路由层直接叠加；前四类应使用底层一跳 Bearer，LoRaWAN/既有 Mesh 只能在产品明确控制下作为 Tunnel/网关。
- 门槛：新增 Adapter 必须先定义逻辑 MTU/Carrier、有界 TX/RX Queue、Up/Down、指标归一、原生网络层责任、目标 MCU 资源和实机验收，才可获得标准 Preset。
- 任务：新增 V5-42、DOC-038；本轮继续只修改设计/任务/记录，未新增 Core/Adapter/Port 驱动、测试、构建、硬件访问或提交推送。

### OP-126｜冻结 LC-1 Link Cost 可执行计算规范

- 设计：新增 `UCN_Link_Cost计算规范.md`，将此前“动态 Cost 建议”收束为单一 LC-1 合同：固定 Adapter 统计窗口 `1000 ms`、Core 采样 `500 ms`、整数 EWMA `floor((3×旧值+样本)/4)`、Queue/TX 失败/RX 失败/RTT/介质占用/介质质量/新鲜度的逐区间惩罚，以及唯一的静态 `administrative_bias(-32..+64)` 减项；明确占用与质量不得由同一原始量重复扣分。
- Preset：补全以太网、BLE LE、原始 802.15.4、私有 2.4G、FSK/LoRa、UWB、SPI/I²C 与受控 LAN Tunnel 的条件初始 `base_cost`；公网/蜂窝/未知多层 Tunnel 固定为 Unknown。所有扩展值在 V5-42 Adapter 准入和 V5-41 实机标定前均不由当前代码自动采用。
- 判定：明确先状态门后评分；`is_up=false`、MTU/安全/Wire Class 失败和超过 `5000 ms` 的 metrics 直接排除，不能伪装为大 Cost。Known 基础 Cost 永远优于 Unknown；全 Unknown 时保留活动 Link 或按 Flow 数、`link_id` 确定性回退。
- 切换：冻结候选须满足 `candidate×100 ≤ active×80`、连续 3 个 500 ms 样本、2 次 Probe/ACK、切换后 3000 ms 保持期；硬失效立即处理；Q1 Flow 只在租约、持续拥塞或硬失效时重绑，Q0/Pinned 边界不变。
- 任务：新增已完成的 V5-43/DOC-039 和待开始的 V5-44；V5-44 负责新增可选 RX failure、介质占用、介质质量、时间戳/坏指标诊断与 Resolver，V5-36 改为依赖 V5-44 的策略接入任务。C01～C10 测试向量已写入规范。
- 边界：本轮仅更新项目规范、既有方案引用、任务表和知识库；未修改 `ucn_link.h`、Core、Adapter、Port、测试、外部工程、硬件或 COM，未构建、提交或推送。LC-1 数值在 V5-44 与 V5-41 实机标定前均未生效。

### OP-127｜完成 V5-35 标准 Preset Resolver 与产品配置链接门禁

- 实现：新增 `include/ucn/ucn_standard_adapter.h` 和 `src/ucn_standard_adapter.c`。公开 `ucn_standard_preset_resolve()`、`ucn_standard_link_config_resolve()`，用只读静态表解析 Bearer、速率、基础 Cost、RTT 参考、最大逻辑 MTU、Carrier/条件标记和每 Link 显式覆盖；不引入 SDK、GPIO、RTOS、驱动回调、动态分配、Node 字段或 Wire 变更。
- 语义：`required_logical_mtu=0` 请求当前 `UCN_MAX_FRAME_BYTES`，超过 Preset 最大 MTU 即失败，禁止静默截断；经典 CAN 必须显式声明已启用 Carrier；零 Link ID、广播 Peer、非法地址、未知 Preset、无效 Cost/RTT 覆盖或越界 `administrative_bias(-32..+64)` 均失败关闭。UART/RS-485、CAN/CAN-FD、ESP-NOW/Wi-Fi、USB CDC 直接给出静态表；扩展 Bearer 只作为 `CONDITIONAL` 元数据，未生成 Adapter。
- 测试：新增 `tests/test_standard_adapter.c`，并纳入主测试、公共头测试与 CMake。Windows Debug 下 Full+配置契约 CTest `4/4`、Lite `1/1`、Nano `1/1` 均通过；WSL GCC 13.3 Full+配置契约 CTest `4/4` 通过。使用 `tests/config/ucn_test_user_config.h`（`UCN_MAX_FRAME_BYTES=128`）构建 `ucn_standard_adapter_product_config_test` 并 CTest `1/1` 通过，证明产品公共配置可以传递到 Resolver 和库链接。
- 发现：同一 128 B 产品头执行完整历史 `ucn_tests` 时，少数 Frame/Wire/AODV/压力/Service 用例仍硬编码默认 256 B 预期；Core 与 Resolver 编译链接正常，这属于测试矩阵未从全局配置派生的缺口。已登记 V5-45，后续只修正测试预期，不为通过测试改变协议/MTU/Preset 行为。
- 文档：新增 DOC-040 实现报告，更新标准 Port/Adapter 基线、任务表和 UCN 知识库；仍未访问硬件/COM、烧录、提交或推送。MSVC 仅保留既有 `ucn_endpoint.h`/`ucn_security.h` C4819 代码页警告，未由本任务新增。

### OP-128｜完成 V5-45 代表性产品配置全量回归

- 问题复现：使用既有 `tests/config/ucn_test_user_config.h`（128 B Frame、64 B Payload、8 Hop、3 Link、4 Neighbor、单 Bearer、4 Service Binding、1 个 Q0 Binding/Validator）构建完整 `ucn_tests`。首次失败来自测试把默认 256 B Frame、16 Hop、4 Link、双 Q0 Binding/Validator 当作固定前提；Core 和 V5-35 Resolver 本身没有编译或链接故障。
- 修复：`test_frame.c` 从 W3 帧头、E2E Tag、`UCN_MAX_FRAME_BYTES` 与 `UCN_MAX_PAYLOAD_BYTES` 推导上限；Wire/Profile 与 AODV Ring 从当前 Hop 上限推导；Service/Bridge 按配置可容纳的 Binding/Validator 子集运行；32 Node×4 Link 压力先检查每 Node 至少 4 Link/Neighbor，条件不满足时明确跳过大拓扑但仍运行固定资源边界测试。未修改 Core、公共协议、Preset、MTU 或产品配置以迁就测试。
- 验证：Windows MSVC Debug 默认 Full CTest `4/4`、Lite `1/1`、Nano `1/1` 均通过；Full + 产品头完整 CTest `2/2`（`ucn_tests` 与 `ucn_standard_adapter_product_config_test`）通过；WSL GCC Full CTest `4/4` 通过。MSVC 仍只出现既有 `ucn_endpoint.h`/`ucn_security.h` C4819 代码页警告。
- 边界/下一步：128 B 产品头的 `UCN_MAX_LINKS=3` 不具备 32×4 Host 压力拓扑前提，不能据此声称该压力场景已运行；真实多板/多介质仍归 S06/S07。下一实现任务回到 V5-37：四类标准 Port 外壳与 Host Fake HAL。

### OP-129｜完成 V5-37 四类标准 Port 外壳与 Host Fake 回归

- 实现：新增 `include/ucn/ucn_standard_port.h`、`src/ucn_standard_port.c` 和 `tests/test_standard_port.c`。Owner 静态引用已有 Node、Adapter RX Queue、Port 时钟/临界区、可选 Runtime Hook 与可选 Service Bridge；没有创建 SDK Task、Queue、信号量、驱动或动态内存。
- 运行合同：驱动/ISR 只能调用 `ucn_standard_port_owner_rx_enqueue()` 复制完整帧，再按需通知；唯一 Owner 才按固定预算执行 `RX Pump -> 可选 Bridge step_at -> node_step`。一轮只采样一次 `now_ms`；等待最多 `UCN_MAX_STEP_INTERVAL_MS`。Core 空闲 `UCN_ERR_NOT_FOUND` 保留在 Owner 统计中，但向周期调度循环归一为 `UCN_OK`，避免正常空闲被误报成失败。
- 测试：Host Fake 覆盖五种模式、Hook 缺失失败关闭、动态 HELLO 准入、ISR 通知、每轮一帧 Pump、过大帧拒绝、等待裁剪、临界区配对及可选 Service Bridge 同时钟发送。公共头 API-only 测试直接引用 Owner API，防止裁剪 Profile 后只声明不链接。
- 验证：Windows MSVC Debug Full+配置契约 CTest `4/4`、Lite `1/1`、Nano `1/1`、Full Service OFF `1/1`、Full+128 B 产品配置 `2/2` 通过；WSL GCC 严格告警 Full+配置契约 CTest `4/4` 通过。MSVC 仍仅有既有 `ucn_endpoint.h`/`ucn_security.h` C4819 代码页警告。
- 文档：DOC-042 状态改为软件完成，新增 DOC-043 实现报告；详细使用手册、快速入口、任务表、调用树与 UCN 知识库均已同步到 Owner API。没有创建真实 FreeRTOS/Zephyr/NuttX 对象、没有修改外部 ESP32 工程、没有烧录/访问 COM；实际 Adapter 与实机时序/资源测试继续由 V5-38～V5-42 负责。

### OP-130｜完成 V5-46 平台 Port 分目录解耦

- 原因：复核 V5-37 后确认集中式 ucn_standard_port 用一个运行模型枚举承载多个 RTOS，新增平台会修改共同入口，不满足“一个平台一个文件、互不耦合”的长期扩展要求。
- 实现：删除未发布的 ucn_standard_port.h/.c 与 test_standard_port.c；新增 include/ucn/ports/ucn_protocol_owner.h、src/ucn_protocol_owner.c，只保留 Node、Adapter RX Queue、预算、时钟和 Bridge 顺序。新增裸机、FreeRTOS、Zephyr、NuttX、RT-Thread、Host Fake 各自独立的 include/ucn/ports/ucn_port_*.h 和 src/ports/ucn_port_*.c。
- 合同：公共 Owner 不再有平台枚举、等待/通知 Hook 或 SDK 类型；每个 RTOS Port 只接受自己的 notify/wait 回调。成功入队后才通知，任何 ISR 不会 Pump/路由；等待仍裁剪到 UCN_MAX_STEP_INTERVAL_MS，Owner 同轮仍共享一次 now_ms。
- 测试：以 tests/test_protocol_owner.c 取代旧测试，覆盖六 Port 的 API 链接、缺 Hook 拒绝、等待裁剪、ISR 通知、单轮 RX 预算、过大帧、Service Bridge 与统一时钟；公共头测试直接引用全部独立 Port 初始化符号。MSVC Debug Full+配置契约 4/4、Lite 1/1、Nano 1/1、Service OFF 1/1、Full+128 B 2/2，WSL GCC Full+配置契约 4/4 均通过；额外以 Lite、Tests/Scale OFF 验证默认构建只生成 ucn_core，未选择 Port 不参与 Core 构建。
- 文档：新增 DOC-044，更新任务表、使用手册、快速入口、调用树和知识库；DOC-042/043 标记为已被 V5-46 当前架构替代，但保留历史证据。没有引入 FreeRTOS/Zephyr/NuttX/RT-Thread SDK、BSP、介质驱动、烧录或 COM 操作。

### OP-131｜启动 V5-47 工程架构治理

- 原因：协议、Profile 与 Platform Port 的职责已经具备明确边界，但内部 `src/` 仍以平铺文件为主，仓库没有把 Foundation/Core、Node、Transport、Routing、Service 与 Ports 的依赖方向显式固化；后续加入实际 UART/CAN/Wi-Fi/USB Adapter 和新的 RTOS 容易重新耦合。
- 先行文档：新增 DOC-045《UCN V5-47 项目架构治理实施方案》，并在 `docs/架构/` 建立系统边界、模块依赖规则、目录迁移兼容策略和构建测试地图。明确 Linux 只是可选 Host/Adapter 环境，绝不是路由前提；真实 Adapter 未实现前不创建空的 UART/CAN/Wi-Fi 代码目录。
- 实施约束：仅移动内部实现文件和调整 CMake/调用树；公开 `include/ucn/...` 头路径、协议 Wire、Profile 行为和对外 API 不变。测试文件本轮保留物理平铺，通过测试地图分类，避免一次迁移同时引入路径与行为风险。

### OP-132｜完成 V5-47 工程架构治理与回归

- 实现：将原平铺 C99 实现归入 `src/core`（Core/Frame/Endpoint）、`src/node`（Node/Nano/Stub/去重内部头）、`src/transport`（Adapter/Preset Resolver/Protocol Owner）、`src/routing`（Path/Policy）、`src/service`（Router/Bridge）和既有 `src/ports`。新增 `src/README.md`、`tests/README.md`，公开头继续保持原 `include/ucn/...` 路径。
- 构建：CMake 新增 `UCN_FOUNDATION_SOURCES`、`UCN_TRANSPORT_SOURCES`、`UCN_NODE_SOURCES`、`UCN_ROUTING_SOURCES`、`UCN_SERVICE_SOURCES`，再组合为 `ucn_core`；按 Profile/Service 只追加对应目录。Platform Port 仍是独立可选目标，Core-only Lite（Tests/Scale OFF）只生成 `ucn_core`。
- 同步：新增 `docs/架构/` 四份架构导航文档与 DOC-045，更新根 README、快速手册的直接源文件矩阵、Zephyr 示例、S04/Service 说明、调用树、任务表和 UCN 知识库。历史操作记录保留旧阶段路径，当前导航和源码地图指向新目录。
- 验证：Windows MSVC Debug Full+配置契约 CTest `4/4`、Lite `1/1`、Nano `1/1`、Full Service OFF `1/1`、Full+128 B 产品配置 `2/2`；WSL GCC Full+配置契约 `4/4` 均通过。CMake 源路径、非模板调用树路径、项目 Markdown 相对链接均为 0 缺失，`src` 根目录 C/H 文件为 0；MSVC 仍仅出现既有 `ucn_endpoint.h`/`ucn_security.h` C4819 代码页警告。
- 边界：未改 Wire、算法、公共 ABI、外部 ESP32 工程或硬件；未引入真实 Adapter、RTOS SDK/BSP、Host/Extended、烧录或 COM 操作。

### OP-133｜启动 V5-48 审计复审修复

- 原因：复审确认三个真实问题：`from_isr` 只到通知而没有进入 Adapter Queue 临界区；128 B/3-Link 产品配置错误运行 8 节点三叉树 Scale Smoke；`ucn_path_forward_config_t` 在历史八字段中间插入 capability 字段，破坏位置初始化源码。
- 计划：新增 Task V5-48 与 DOC-046。Queue 采用显式 Task/ISR 双入口；ISR 临界区必须成对传递可恢复 token，缺失时 `from_isr` 失败关闭，不再回退使用任务锁。Scale 默认 Smoke 按已编译 `UCN_MAX_LINKS` 自动选合法拓扑；Path capability 字段后移至历史八字段之后，并补回归。
- 验收边界：Host Fake 必须可见 ISR 锁/token 路径；128 B 产品构建须运行通过合法 Scale；仍不把 C99 Host 测试宣称为真实 FreeRTOS/Zephyr/NuttX/RT-Thread ISR、BSP、驱动或实机验证。

### OP-134｜完成 V5-48 审计复审修复与严格源码兼容回归

- ISR Queue：`ucn_port_ops_t` 增加独立的 `enter_critical_from_isr()` / `exit_critical_from_isr(token)`；Adapter 与 Owner 增加 `*_from_isr()` 入口。五个独立 RTOS/Host Fake Port 依据 `from_isr` 分流，ISR 缺少 token 对即返回 `UCN_ERR_CONFIG`，绝不退回任务锁。`test_adapter` 与 `test_protocol_owner` 分别覆盖 token 回送、缺锁失败关闭和五类 Port 的可观察 ISR 路径。
- Scale：`ucn_scale_sim --topology auto` 在 `UCN_MAX_LINKS>=4` 选 tree、`2..3` 选 line；新增 `--topology-capacity-contract` 与 `ucn_scale_tree_capacity_contract`，明确打印 `SUPPORTED` 或 `CAPACITY_SKIPPED`。128 B/3-Link 产品配置常规 Smoke 实际使用 line，显式 tree 只报告容量跳过，不再把不可能拓扑当作 Core 回归失败。
- Path 源兼容：最初仅将 capability 字段后移；WSL 严格 GCC 发现旧八字段位置初始化仍触发 `-Wmissing-field-initializers -Werror`。最终恢复 `ucn_path_forward_config_t` 为精确历史八字段，新增 `ucn_path_install_capable(state, config, capability)` 承担可选能力写入；旧 `ucn_path_install()` 保持原 API 并传 `NULL`。Full 实现和 Lite/Nano Stub 均提供该符号，公共头测试直接引用它。
- 验证：Windows MSVC Debug Full+Scale+配置契约 `14/14`；Full+128 B/3-Link 产品头 `15/15`，直接模拟确认 auto=`line` 且 tree=`CAPACITY_SKIPPED`；Lite、Nano、Full Service OFF 各 `1/1`；WSL GCC 13.3 `-Wall -Wextra -Wpedantic -Werror` Full+Scale+配置契约 `14/14`，ASan+UBSan+Leak Detection `14/14`。MSVC 仍只有既有 `ucn_endpoint.h`、`ucn_security.h` 的 C4819 代码页警告；本轮未新增。
- 边界：以上是 C99/Host Fake/Host Scale 软件证据，不证明真实 FreeRTOS、Zephyr、NuttX、RT-Thread 的 SDK 临界区、嵌套 ISR、调度、BSP、驱动、资源或实板通信。未烧录、未访问 COM、未提交或推送。

### OP-135｜完成 V5-44 / V5-36 LC-1 动态 Cost 与选路闭环

- Core：新增公开 `ucn_link_cost.h` 与纯 C `src/core/ucn_link_cost.c`，实现 LC-1 固定惩罚表、`floor((3×prev+sample)/4)`、Unknown、Invalid、陈旧状态门、管理偏置、`1..65534` 饱和和 20% 比较；无浮点、Heap、RTOS/SDK 依赖，Wire v5 不变。
- Metrics/Policy：在 `ucn_link_metrics_t` 尾部追加 RX failure、medium busy/quality、同源标志、快照时间戳、RTT 参考、管理偏置与 Adapter bad-metric；Full 固定快照维护六项独立 EWMA、LC-1 结果和拒绝/陈旧统计，既有本地质量查询 API 直接暴露诊断。外部 Adapter 必须随新头重新编译并清零输出结构，旧字段赋值源码仍以新字段 Invalid 运行。
- 选路：同 Neighbor Bearer 按动态有效分执行 Known 优先、20%/3 样本/2 Probe ACK/3000 ms 保持期，硬失效立即切换；Candidate 只替换本节点出口贡献做本地比较，保存/转发的累计 Cost 不变；Q1 新建/到期 Flow 按 `effective_cost×(flow_count+1)` 选择，未到期 Flow 仍只因硬失效或既有三次队列拥塞门限重绑。Q0、Pinned、逐 Flow 租约和不复制/不条带化边界保持不变。
- 测试：新增 `test_link_cost.c` 的 C01～C10、溢出、非法偏置、同源去重和 EWMA；扩展 Bearer、Candidate、Path Control、Policy 专项。Windows Full/Lite/Nano=`11/11、11/11、1/1`，Full Service OFF=`11/11`，128 B/3-Link 产品头=`15/15`；WSL GCC 13.3 ASan+UBSan+Leak 与 `-fanalyzer` 均 `11/11`。产品配置首次验证因 PowerShell 将未加引号的 `.h` 参数拆分而配置失败，改用显式引号重跑后 15/15；协议源码没有因此修改。
- 资源：WSL GCC 13.3 Release/Service OFF 的 Nano/Lite/Full Node 为 `2648/6024/10080 B`，相对 V5-33 Host 记录为 `+0/+64/+328 B`，Link 三档仍 40 B；Archive `.text` 为 `27662/73735/139017 B`，纯 Resolver 对象为 1625 B。固定状态无动态内存；Archive 不是 MCU Flash。
- 文档：任务 V5-44/V5-36 标记为 Host 软件完成，LC-1 规范、Metrics/Cost 契约、标准 Port/Adapter 方案、S04 资源、根 README、源码/测试地图和调用树已同步；新增 DOC-047 实现报告。未烧录、未访问 COM、未提交或推送，真实 Adapter 与实机标定继续归 V5-38～V5-42。

### OP-136｜完成 V5-38/V5-49 ESP32 三节点多 Bearer 首轮实测

- 台架：COM5/COM34 为 ESP32-S3 节点 A/B，COM35 为 ESP-WROOM-32 节点 C；A—B 使用 GPIO15/16 交叉 UART，B—C 使用 B GPIO19/20 与 C 显式 GPIO16/17 交叉 UART，ESP-NOW 同时在线；准入逻辑强制 A—B—C。
- 修复：外部 Adapter 统一使用 `pump(now_ms)` 的 Metrics 采样时间戳，解决多 Link 采样出现未来 1 ms 而被判 `METRICS_STALE`；Core 在 RREQ Origin 首次 Flood 前写自身去重缓存，阻止同邻居另一 Bearer 回流造成自路由，新增 Origin Loop Guard。
- 软件/构建：主库 Full CTest `14/14`；三目标 RAM/Flash 为 A=`46,308/590,971 B`、B=`48,228/591,207 B`、C=`50,776/625,859 B`，COM5/COM34/COM35 均完成写入与 Hash 校验。
- 正常实测：A/C 各 2 Link、B 4 Link，全部无 Metrics Stale；UART=34、ESP-NOW=45，A↔C 两跳最终为 UART+UART/cost 68，无自路由。直连 ping 多为 7～22 ms、两跳多为 15～30 ms。
- 热断/恢复：A—B UART 断开约 5 s 切 Wi-Fi、约 10 s 撤销 UART 准入、约 25 s Route 更新为 cost 79；恢复约 23～28 s 重新发现、约 38 s 选回 UART、约 53 s 回到 cost 68。切换有少量 ping 丢失，不能宣称零丢帧。
- 节点离网：B 长离线约 19 s；A/C 约 6 s 进入失效、约 11 s 清为 `links=0`，发送失败 17 次；B 返回约 2～3 s 重新准入、约 12 s 选回 UART。
- 最终快照：无复位监听确认 A↔C 均为 `2 hops/cost 68/epoch 107`，B 到 A/C 均为 `1 hop/cost 34`，证明长离网后最终能够完全收敛。
- 文档/后续：新增 V5-49 实测报告和 V5-50；后者处理 Route 刷新落后 Bearer 约 15～20 s、热切换少量丢帧、悬空 UART RX 噪声和启动瞬态错误。本轮未宣称长期稳定、功耗、生产安全或其他 RTOS/Bearer 已验收，未提交或推送。
### OP-137｜完成 V5-50 软件修复并进入三板重复实测

- 操作：针对 V5-49 暴露的 Route 物理出口滞后、发送阶段硬失败和悬空 UART RX 噪声，先建立失败单测，再修复 Core 与外部 ESP32 UART Adapter。
- Core：`send_frame_on_link()` 在状态查询、MTU 或 Driver 发送返回 `UCN_ERR_LINK_DOWN` 时标记对应 Bearer；普通 Route/中继允许同逻辑 Neighbor 的不同 Backup 单次重试。Neighbor Primary 改变时同步 Active/Previous/Candidate/Static Route 的物理出口，按旧/新本地基础 Cost 校正累计 Cost并清除旧端到端 RTT；Route Epoch 保持不变。远端中继失败继续通过 RERR 表达，不伪装成本地物理发送失败。
- UART：外部三板工程为启用 RX 配置 `INPUT_PULLUP`；新增 UART0/UART1 逐口启用开关，禁用口不启动 Driver、不分配串口 Buffer、不 Pump/HELLO；新增每 Pump 默认 4 次噪声错误预算及可见计数。
- 测试：Windows Full `14/14`、Lite `11/11`、Nano `1/1`、Full Service OFF `11/11`、128 B/3-Link `12/12`；WSL ASan/UBSan/Leak Detection `14/14`。PlatformIO A/B/C 三目标均构建成功，当轮尺寸为 `46652/594631 B`、`48580/594967 B`、`50784/628899 B`（RAM/Flash）；后续 OP-138 的 Adapter 顺序修复后重新构建。
- 烧录状态：COM5/COM34 已写入并校验；COM35 首次上传因 `Wrong boot mode 0x13` 等待手动 BOOT+EN。三板最终同源后继续至少 10 轮热断/恢复，不把当前软件回归写成零丢帧实机结论。
### OP-138｜修复 Arduino RX 外设脱绑定并建立 V5-50 三板基线

- 时间：2026-08-13。
- 现象：三板首次同源监听中 ESP-NOW 正常，但所有 UART 均为 `tx_frames` 增长、`rx_bytes=0`，A↔C 仅能以 Wi-Fi 两跳 Cost 90 通信。
- 根因：Arduino-ESP32 3.3.7 的 `pinMode()` 会清除引脚已有的 Peripheral Manager 总线；Adapter 在 `HardwareSerial::begin()` 后设置 RX `INPUT_PULLUP`，导致 UART RX 矩阵被静默解除。
- 修复：将 RX 上拉移到 `HardwareSerial::begin()` 之前，再由 `begin()` 完成 UART 外设绑定；保留逐口关闭和每 Pump 噪声错误预算。
- 验证：A/B/C 三目标重新构建并全部 Hash 烧录成功，最终 RAM/Flash 分别为 `46652/594627 B`、`48580/594975 B`、`50784/628899 B`。三板基线为 A/C 各 2 Link、B 4 Link；两段 UART 均为 Primary、单跳 Cost 34，A↔C 两跳 Cost 68；UART 接收持续增长且解码、长度、溢出、噪声预算、发送空间和短写错误均为 0。
- 边界：本项只关闭正常基线和 Adapter 调用顺序缺陷；10 轮热断/恢复、切换时延、丢帧与乱序统计仍在 V5-50 门禁中，未宣称零丢帧无缝切换。
### OP-139｜完成 V5-50 十轮热断恢复矩阵

- 时间：2026-08-13。
- 方法：保持 B—C UART 与 ESP-NOW 在线，只反复断开/恢复 A—B UART；第 1 轮先观察 90 s 稳态断开，其余 9 轮由 Host 解析 A→C Route，自动计数 `68→79→68`，并跟踪 A 收到的 C Ping 序号。
- 结果：10/10 轮均自动回退和恢复；9 个自动轮次 Route/Primary 在同一 5 s 快照内同步，未复现 V5-49 的 15～20 s Route 滞后。发送拒绝 0、重复 0、乱序 0；A→C 共 26 个序号缺口，逐轮为 `4/2/3/3/3/4/3/2/2`。第 1 轮未启用逐序号记录，不并入精确总数。
- UART/资源：反复插拔后 A 端累计新增 2 次 COBS decode error，B/C 与 length/overflow/noise-budget/no-space/partial 均为 0；断开稳定时 RX 字节冻结。最终恢复 A/C 各 2 Link、B 4 Link、A↔C Cost 68，Heap/Stack 无趋势性恶化。
- 判断：V5-50 的 Route 快速同步和 UART 空闲硬化通过，但普通 UART 在 2500 ms 健康超时前仍会本地 TX 成功，无法触发同调用 Backup 重试，故零丢帧未通过。新增 V5-51 评审主动探测/ACK、硬件断线检测和关键流有界双发+去重；不得简单无限缩短超时或全流复制。

### OP-140｜完成 V5-51 Link 存活档与软件门禁

- 时间：2026-08-13。
- 方案：复用现有 UCN 一跳 Heartbeat，不新增 UART 私有 Probe Wire。Link 增加零初始化兼容的 DEFAULT/FAST 存活档；FAST 固定为 `250 ms Heartbeat / 1250 ms SUSPECT / 2000 ms DOWN`，默认档保持 `1000/3000/4000 ms`。硬件 DCD/CTS/GPIO 或 Bus-Off 仍可由 Adapter 更早报告 Down；Suspect 后双发不能覆盖判疑前窗口，因此不作为默认修复。
- Core：Heartbeat、判疑、回收按每 Link 档位取值；Primary 进入 SUSPECT 且存在同 Neighbor ADMITTED Backup 时立即接管。周期 Heartbeat 从通用 RREQ/Probe/Activate Token Bucket 分离，仍由固定 Link/Bearer 上限和档位节拍约束；入站 Heartbeat Request 保留每 Peer 固定预算。注册拒绝非法档位，时间回绕与维护服务上界有编译/专项门禁；v5 Wire 不变。
- 测试：混合 FAST/DEFAULT 节拍、静默主备切换、恢复、通用 Token=0、回绕、非法档位、持续 Q0/Q1 和多 Bearer 维护上界均通过。Windows Full+配置 `14/14`、Lite `11/11`、Nano `1/1`、Full Service OFF `11/11`、128 B/3-Link `12/12`；WSL GCC ASan+UBSan+Leak 和 `-fanalyzer` 各 `14/14`；`git diff --check` 无内容错误。
- ESP32：UART Adapter 显式选择 FAST；Bench 增加 `V5EVENT` 毫秒 Bearer/Primary/Route 事件、最近有效 UART RX 与 Heartbeat/去重统计。A/B/C 构建成功，RAM/Flash=`46828/595727 B`、`48756/596095 B`、`50960/629979 B`；三者 RAM 比 V5-50 Bench 增加 176 B，Core Host Link 仍为 40 B。
- 烧录/启动：A/COM5、B/COM34 已写入并完成 Hash 校验。15 s 并行日志确认 UART `live=1`、ESP-NOW `live=0`，A→C 两跳 Route 为 UART/cost 68；Heartbeat 持续增长，最大维护服务延迟 114 ms，A/B UART decode/length/overflow/no-space/partial 均为 0。C/COM35 构建正常但上传因 `Wrong boot mode 0x13` 失败，等待手动 BOOT+EN，当前不可写成三板同源。
- 边界：尚未执行 V5-51 的三板 10 轮真实热断/恢复。目标是证明 1.25 s 判疑上界并量化缺口，不承诺端到端可靠、硬实时或零丢帧。

### OP-141｜完成 V5-51 三板十轮 UART 静默故障门禁

- 时间：2026-08-13。
- 执行：三板 V5-51 同源后，用外部 ESP 工程 `scripts/v5_liveness_monitor.py` 一次性打开 COM5/COM34/COM35，持续约 467 s，不在轮次之间重开串口；完成 10 轮 A—B UART 断开/恢复。原始日志为 `test_results/v5_51_liveness_20260813_011133.log`，分析器输出已固化为逐轮 CSV。
- 状态机：A/B 各精确出现 10 次 UART SUSPECT 与 10 次 DOWN，C 为 0；每轮当前 Primary 均切换到 ESP-NOW Backup，并最终回到 UART Primary。9/10 轮 Route→Wi-Fi 事件与 SUSPECT 相差 0～16 ms，第 10 轮约 1 s；最终 A↔C=2 Hop/cost 68、B 两条 UART 直连 cost 34。
- 业务：全 10 轮 A/C 端分别缺 11/13 个离散序号；固定动作的第 2～10 轮为 9/11 个，两个方向最大连续缺口均为 1。467 s 观测重复、倒序、Ping Request/Reply 提交拒绝、板卡重启和异常均为 0。
- Carrier/资源：四个 UART 的 decode/length/overflow/noise/no-space/partial 与 RX Queue 满丢弃保持 0；Heartbeat 最大服务延迟 A/B/C=`58/142/1 ms`，Internal Heap 首末不变，栈高水位无持续下降。三板 Heartbeat Request 净增 8254，约 17.75 request/s。
- 诊断边界：`V5EVENT age` 来自 Adapter 最近完整 Carrier 帧，而 Core 判疑使用协议门禁后的 `bearer.last_seen_ms`。17/20 个端点代理值为 1248～1249 ms，另 3 个差值不能解释成 Core 门限错误；1250/2000 ms 精确门限由专项软件测试直接覆盖。当前 Bench 未启用 CPU Run-Time Stats，CPU% 和功耗绝对值继续归资源标定。
- 结论：V5-51 当前三板故障功能门禁通过，Wire 与固定资源边界未改变；仍不承诺零丢帧、端到端 ACK/重传或硬实时。新增 [十轮实测报告](../08-实现与验证/版本演进/UCN_V5_51_三节点十轮实测报告.md)，并同步任务表、方案、外部工程和知识库。

### OP-142｜发布 V5-35～V5-51 当前完整实现到 GitHub

- 时间：2026-08-13。
- 发布范围：标准 Preset/Port/Protocol Owner、LC-1 动态 Cost、工程源码分层、ISR Queue 与容量合同、三节点多 Bearer、V5-50/V5-51 快速故障收敛，以及对应测试、调用树、快速手册、架构和实测报告；外部 PlatformIO 工程、串口原始日志和本机 Build 目录不进入主仓库。
- 发布检查：远端 `origin/codex/v5-adaptive-wire` 与本地发布基线无分叉；暂存共 128 个文件、7201 行新增、602 行删除，旧 `src` 文件均识别为目录迁移；无超过 1 MiB 的新发布文件，GitHub/AWS/私钥/Bearer 凭据特征命中 0，`git diff --cached --check` 通过。
- 回归：Windows Full `11/11`、Lite `11/11`、Nano `1/1`、Service OFF `11/11`、128 B 产品配置 `12/12`、配置契约 `14/14`；WSL ASan+UBSan+Leak 与 `-fanalyzer` 各 `14/14`。
- Git：功能提交 `9c761d8`（`feat(v5): complete adaptive ports and resilient routing`）已推送到 `https://github.com/ice-infinite/UCN.git` 的 `codex/v5-adaptive-wire`；本条发布记录随后以独立文档提交同步，不改 `main` 或 v4 标签。

### OP-143｜冻结九档逻辑消息等级与有界分片建议（未实现）

- 原因：用户要求未来大消息不允许任意无上限，而是按固定等级管理；最终确认的唯一等级为 **32 B、64 B、128 B、256 B、512 B、1 KiB、2 KiB、4 KiB、8 KiB**。这不是前一版“仅 64/256/512/2K/4K/8K”，也不是替代现有 `UCN_MAX_FRAME_BYTES`。
- 设计：新增《UCN 消息大小等级与有界分片重组建议》，将其命名为独立 `Transfer Class`，明确与 Wire Class（W0～W3）和 QoS（Q0/Q1）正交。T32/T64 永不分片；T128～T8K 按端到端最小 MTU 在必要时分片，分片信封固定 14 B、ACK 固定 8 B、严格顺序 Stop-and-Wait、每片最多 3 次重试、绝对 Deadline、最大 512 片。
- 资源/安全边界：默认 `UCN_FEATURE_TRANSFER=0`，Core/Transfer OFF 不新增大 Buffer 或线上开销；只有目标 Endpoint 和目标节点显式开通的 Class 才可接收。中继不重组；接收端使用固定 Slot，完整消息以 Slot Index 交给任务并显式 release。每片/ACK 沿用现有 Provider/E2E，旧 v5 对端不主动接收未知 Transfer 帧。
- 台账：任务表增加 `EXT-01`～`EXT-07`，执行顺序为契约 → Codec → 固定 TX → 固定 RX → 安全/MTU → 软件规模 → 多 Bearer 实机。当前仅完成设计和任务登记；没有修改 C99 协议代码、线格式、构建配置、ESP 固件或硬件测试，也不得表述为已支持自动分片、8 KiB 数据或可靠文件传输。

### OP-144｜实现九档消息大小与有界 Transfer 首版

- 时间：2026-08-13。
- 公共接口：新增 `include/ucn/ucn_transfer.h`，固定 T32/T64/T128/T256/T512/T1K/T2K/T4K/T8K 九档，新增 `0x22 TRANSFER_FRAGMENT` 与 `0x23 TRANSFER_ACK`。T32/T64 复用现有单帧 Endpoint；T128～T8K 使用 14 B 信封、8 B ACK、完整消息 IEEE CRC32、严格顺序 Stop-and-Wait 和固定完成状态。
- 静态资源：新增独立 `ucn_transfer` CMake Target 和 `src/extended/ucn_transfer.c`；Target 为 `EXCLUDE_FROM_ALL`，Core-only 产品不构建它，`ucn_node_t` 不增加大缓冲。Transfer TX 借用调用者只读 Buffer；RX 使用可裁剪固定 Slot，完成后以带代际的 Handle 交付并显式 release。Host 64-bit 默认 `1×8192 B RX` 对象为 8792 B；产品测试 `2×512 B RX` 为 1664 B，目标 MCU 数值仍待目标编译器测量。
- 路由/可靠性：每次 `ucn_transfer_step()` 至多发送一片；每片默认 3 次重试，最终 ACK 丢失通过 Recent Completion 只重 ACK、不重复执行。实际 MTU 返回 TOO_LARGE 时逐步收窄片长，最小 16 B 仍不适配则立即失败；中间节点只由 Core 转发，不创建重组对象。发送前必须配置对端最大 Class，未配置或超上限失败关闭。
- 安全边界：Binding 可要求 E2E，明文 Fragment 会被拒绝；Fragment/ACK 当前复用 Node 全局安全策略。尚未实现按 Fragment 内目标 Endpoint 自动选择独立 TX 安全策略，也未实现动态 HELLO 能力协商或完整 Path 最小 MTU 主动传播，这些保留在 EXT-05，不能宣称生产安全已闭环。
- 专项测试：新增 `tests/test_transfer.c`，覆盖九档最大值、CRC32 标准向量、坏 Flag/Class/Reserved、静态能力拒绝、64/128/250/256 B MTU、32 B MTU 确定性失败、两跳透明中继、ACK 丢失、最终 ACK 持续丢失与重试耗尽、CRC 错、RX 超时、单/双 Slot 满、代际 Handle 和一次执行。公共头链接与全局配置默认/回退/覆盖测试同步更新。
- 回归：Windows Full/Lite/Nano 均通过；`512 B 最大消息 + 2 RX Slot + 64 B Payload` 产品裁剪为 5/5；Core-only Lite/Service OFF 只生成 `libucn_core.a`；WSL ASan/UBSan 与 GCC `-fanalyzer` 均通过。以上均为 Host 软件证据，ESP-NOW/UART/CAN-FD 的真实吞吐、CPU、栈、功耗和多板故障注入仍归 EXT-07。

### OP-145｜完成 V5-52 N8R8 三节点九档 Transfer 实测

- 时间：2026-08-13。
- 台架：A/COM5 与 B/COM34 为 ESP32-S3-N16R8，C 改为 ESP32-S3-N8R8/COM38；A—B 使用 GPIO15/16 交叉 UART，B—C 使用 B/C RX19/TX20 交叉接线，ESP-NOW 同时在线。Node ID 为 `0x10000001/2/3`。
- 外部工程：新增 N8R8 PlatformIO 环境、Transfer 可选源和 T32～T8K Bench；A 发送，B 不实例化 Transfer 对象、只透明转发，C 使用 1 个 8 KiB RX Slot。监测脚本新增打开端口后 EN 复位，以同一日志覆盖 BOOT、T32 和 COMPLETE。
- 构建/写入：A/B/C RAM/Flash=`63716/601435 B`、`48756/596095 B`、`55332/597615 B`；三目标均构建成功并完整写入、Hash 校验。只写应用镜像导致 COM5/COM34 无输出的诊断过程保留，最终使用 PlatformIO 完整 bootloader/partition/boot_app0/application 写入恢复。
- 路由/结果：A↔C 最终为 UART 两跳 Cost 68；T32/T64 单帧到达，T128～T8K 分别 `1/2/3/6/11/21/41` 片。9/9 `valid=1`，A 总计 85 Fragment、0 重试/失败，C 85 ACK、7 次重组、0 CRC/超时/拒绝；8 KiB 确认耗时 2528 ms。
- Carrier/资源：本轮 A/C UART 全部错误为 0，B 历史 decode=2 未增加，三端 Queue Drop=0；最小 Heap A/B/C=`294608/309432/305036 B`，栈余量=`5008/4972/5020 B`。
- 文档：新增 [V5-52 实测报告](../08-实现与验证/版本演进/UCN_V5_52_N8R8三节点Transfer实测报告.md)，EXT-07 改为部分完成。ESP-NOW-only、切换中 Transfer、并发/Q0、CAN-FD、CPU/功耗和长期稳定仍未验收；未提交、未推送、未同步知识库。

### OP-146｜完成 V5-53 三节点九档等载荷压力测试

- 时间：2026-08-13。
- 外部 Bench：新增三个压力环境、等 8 KiB 负载矩阵、C 端固定序号图和可重发统计请求/回报；新增 CSV 解析器。首轮发现统计时间戳回绕和一次性回报丢失，修复后才形成正式 Run 3/4，早期日志只作为工具调试和无节流过载证据。
- 构建/烧录：A/B/C RAM/Flash=`63,756/602,607 B`、`48,756/596,111 B`、`55,796/598,411 B`；所有最终上传显式绑定 COM5/COM34/COM38 并通过 Hash。一次自动端口识别曾把 C 镜像写到 COM5，随后 COM5 已用正确 A 镜像完整覆盖恢复。
- 实测：T128～T8K 两轮共 14 个阶段全部完整到达，无无效/重复/CRC 错；仅 Run 4 的 T2K 重传 1 片后成功。可靠有效吞吐 `2.48～3.17 KiB/s`，T4K 平均 `3.13 KiB/s`，较 T128 平均提升约 21.8%。
- 边界：T32/T64 为无端到端 ACK 的 Direct 快速帧；两轮完成率分别为 `98.44%/98.05%` 和 `100%/96.09%`，B 固定 UART RX Queue 出现对应 Drop。不能通过继续放慢固定间隔把它描述为可靠协议；必须可靠时需应用 ACK/重发或使用可靠 Fragment 等级。
- 文档：新增 [V5-53 压力报告](../08-实现与验证/版本演进/UCN_V5_53_三节点Transfer压力测试报告.md)，更新 V5/EXT-07/DOC 台账。未覆盖 ESP-NOW-only、921600 UART、Bearer 切换、多源并发/Q0、CAN-FD、CPU%/功耗；本轮未提交、未推送、未同步知识库。

### OP-147｜完成 V5-54 921600 UART 三节点压力对比

- 时间：2026-08-14。
- 外部 Bench：节点间 UART 波特率改为环境可覆盖，默认仍为 115200；新增三套 921600 环境，Console/上传和 Link Cost 不变。三目标 RAM/Flash=`63,756/602,631 B`、`48,756/596,127 B`、`55,796/598,431 B`，显式写入 COM5/COM34/COM38 并完成 Hash。
- 实测：T128～T8K 三轮 `21/21` 完整交付，无无效/重复/CRC/Wire 错、Transfer 失败或重传。可靠等级平均吞吐 `2.825→10.296 KiB/s`，平均 3.63×，耗时下降 72.3%；T8K 平均 `11.860 KiB/s / 676 ms`。
- 边界：T32/T64 原节拍下完成率 `98.57%/97.92%`，固定 Queue 仍 Drop；后台 Ping 一轮 70/70、一轮 A/C 55/70、第三轮约 69～70/70，说明高密度 Transfer 偶发挤压背景 Q1。下一实机优先项应为 Transfer 与 Q0/Q1 并发调度预算，而不是继续单独提高 UART 位速率。
- 文档：新增 [V5-54 报告](../08-实现与验证/版本演进/UCN_V5_54_921600波特率三节点Transfer压力测试报告.md)，更新 V5/EXT-07/DOC 台账。未提交、未推送、未同步知识库；短线结果不外推为长线/不同 UART/EMC 或所有产品默认配置。

### OP-148｜完成 V5-55 UART 1.5M～5M 极限扫描

- 时间：2026-08-14。
- 外部 Bench：将可配置 UART 编译门限对齐 ESP32-S3 SDK 的 `5,000,000 baud` 上限，新增 `1.5M/2M/3M/4M/5M × A/B/C` 共 15 个环境；15/15 构建成功。首次 COM38 上传因 Windows GBK 输出进度块异常而卡住，终止后使用 UTF-8 和固定 921600 上传速率重新完整写入并 Hash 校验，不属于板卡/协议故障。
- 实测：1.5M/2M 各 1 轮，3M/4M/5M 各 3 轮，共 11 轮、99 个压力阶段；T128～T8K 可靠阶段 `77/77` 完整交付。3M 三轮 0 重试/0 Wire 错，平均 `12.523 KiB/s`；4M 三轮有 1 次重试但 Wire 错为 0；5M 三轮有 1 次 COBS decode 错和 1 次重传，协议完成恢复。
- 判断：5M 为当前 SDK 功能可恢复上限，但 5M 与 3M 平均吞吐仅差约 0.07%；当前短线工程推荐档为 3M，三板最终恢复并保留 3M 固件。T32/T64 合并交付仍约 98%，提高位速率不改变其无 ACK 边界。
- 证据/文档：外部 Bench 保存 11 份原始日志、99/11/5 行三类 CSV和 15 目标构建日志；新增 [V5-55 报告](../08-实现与验证/版本演进/UCN_V5_55_UART极限波特率扫描报告.md)，同步 V5/EXT-07/DOC 和 Transfer 建议。未提交、未推送、未同步知识库；未覆盖长线、EMC/温漂、不同 MCU/收发器、CPU/功耗、Q0 严格延迟或多源并发。

### OP-149｜完成 V5-56 Transfer 固定窗口优化与实机对比

- 时间：2026-08-14。
- 代码：Transfer 默认运行窗口保持 1，新增编译最大窗口 4、本机窗口和 Peer 窗口显式 API；既有 `next_expected_offset` 作为累计 ACK。接收端仍严格顺序/零乱序缓存，发送端在 ACK 缺口或超时时执行受既有重试上限约束的 Go-Back-N。`ucn_transfer_config_t` 未插字段，Wire/协议版本未改变。
- 软件门禁：新增旧 Peer 窗口 1 退化、丢 START、丢中间片、活动 TX 改窗拒绝和旧位置初始化测试。Windows Full/Lite/Nano 各 1/1、512 B/双 RX/最大窗口 2 产品配置 5/5；WSL GCC 13.3 ASan+UBSan+Leak 与 `-fanalyzer` 各 1/1。三目标窗口 1/4 均重新构建成功。
- 实机：最终 API 版本重新完整烧录 COM38→COM34→COM5 并 Hash 校验，三板保留 3M/窗口 4；三轮 T128～T8K `21/21`、0 重试/0 Wire 错，窗口峰值 4、恢复轮 0。可靠等级平均 `12.523→19.121 KiB/s`（+52.7%），T8K `14.041→24.750 KiB/s`、耗时 `570.3→323.3 ms`（-43.3%）。
- 资源/边界：Transfer 对象 `8672→8700 B`（+28 B），最终 A/B/C RAM/Flash=`63,788/603,159 B`、`48,756/596,163 B`、`55,828/598,887 B`。Direct T32/T64 仍不可靠；窗口能力尚未自动协商，ESP-NOW-only、丢包无线、切换、多源/Q0、CPU/功耗与长稳仍待 EXT-07。
- 证据/文档：新增 [V5-56 报告](../08-实现与验证/版本演进/UCN_V5_56_固定窗口Transfer优化与三节点对比报告.md)，外部工程保存窗口 1/4 各三轮日志、27+27 行汇总、九档对比与逐轮健康 CSV。未提交、未推送、未同步知识库。

### OP-150｜完成 V5-57 外设事件驱动 Owner 与窗口 8 实测

- 时间：2026-08-14。
- 架构纠正：正常 RX 不再依赖高频轮询。UART ISR 先进入框架 Driver Ring，Arduino UART Event Task 的 `onReceive()` 和 ESP-NOW RX/TX completion 只通知唯一 UCN Owner；Owner 被立即唤醒后按 24 Round/2000 us 预算批量 Pump/Node/Transfer。ISR 不解析、寻路、解密、重组或打印；10 ms 等待只用于维护定时器、漏通知和无中断平台兜底。
- 背压修复：UART Adapter Queue 满时不继续排空 Driver Ring，避免密集小帧被主动丢弃。首次 Event Smoke 的 T32 `254/256`、B Drop 3 修复后，后续正式三轮 B Drop 保持 0。
- 定时修复：初版固定 10 ms 等待拖慢小消息节拍，最终等待上限改为 10 ms 与 HELLO/Ping/Stats/Transfer Deadline/下一压力消息时间的最小值，到期任务立即运行。
- Transfer：编译默认最大窗口由 4 放宽到 8，运行期默认仍为 1，Peer 未显式声明仍退化为 1；Wire、ACK 和协议版本不变。三目标最终 RAM/Flash=`63836/604515 B`、`48820/597431 B`、`55876/600171 B`，已写入 COM5/COM34/COM38。
- 实测：最终事件固件三轮 27/27 阶段通过、Transfer 重试 0、B UART Queue Drop 0。九档等权平均 `15.712→17.283 KiB/s`（较窗口 4 +10.0%），T8K `24.750→29.480 KiB/s`，本地完成三轮均为 217 ms。T512 三轮恰与三板 5 s `V5STAT` 同步打印重叠而显示 -19.9%，仍 16/16 完整交付；归类为 Bench 同步日志阻塞，不误判为协议损坏。
- 软件门禁：Windows Full/Lite/Nano 各 1/1、产品配置 5/5；WSL ASan+UBSan 与 `-fanalyzer` 各 1/1。新增 [V5-57 报告](../08-实现与验证/版本演进/UCN_V5_57_事件驱动Owner与窗口8三节点报告.md)，同步架构、使用手册、调用树和任务表。未提交、未推送、未同步知识库。

### OP-151｜完成 V5-58 公共多 Source Event Runtime 软件闭环

- 时间：2026-08-14。
- 公共实现：新增 `include/ucn/ports/ucn_event_runtime.h` 与 `src/transport/ucn_event_runtime.c`，所有 Profile 的 `ucn_core` 均包含该 SDK 无关 Runtime。默认静态 8 Source，Task/ISR 分离置位、事件 OR 合并、Source/Round 固定预算、Owner 饱和续跑、有界 Wait、超时 FALLBACK、可选 Yield 和完整帧 Submit 全部进入公共 API；现有六个单 Queue Platform Port 保持兼容。
- 并发边界：UART/CAN/USB/Wi-Fi 等每个实例持有独立 Driver Ring/Frame Queue 和 Source ID；ISR 只搬数据、置位并通知，`service()` 只在唯一 Owner 上下文 Drain/解 Carrier/提交完整帧。RTOS 只实现 `notify_owner/wait_owner/yield_owner`，裸机可省略 Scheduler Hook。Runtime 不包含 SDK 类型、GPIO、驱动 Handle 或具体 Carrier。
- 软件测试：新增 `test_event_runtime`，覆盖双 Source 同时与同源合并、Source 回调中并发新事件、Task/ISR token、完整帧、配置深度 Queue 满、Source/Round Budget、Yield、Wait 裁剪、Fallback、裸机、非法输入和 Source 畸形/错误；公共头、全局默认/回退/产品覆盖和 Profile 资源输出同步更新。
- 回归：Windows Full/Lite/Nano 各 1/1、128 B/3 Source 产品配置 5/5、Full Service OFF 1/1；WSL ASan+UBSan 1/1、GCC `-fanalyzer` 1/1，`git diff --check` 通过。Host x64 Runtime 对象默认 8 Source 时 Full/Lite 432 B、Nano 408 B，3 Source 产品配置 304 B；不含 Node、公共 RX Queue 和 Driver Ring。
- 文档：新增 [V5-58 方案/实现](../08-实现与验证/版本演进/UCN_V5_58_标准事件Runtime与多Bearer接入方案.md) 和 `event_runtime.calltree.yaml`，同步 README、Adapter 契约、架构、使用手册、全局配置及裸机/通用 RTOS/FreeRTOS/Zephyr/NuttX/RT-Thread 快速手册。真实 Stream Ring/Carrier、CAN Carrier、各 SDK glue 与多中断实机分别转 V5-59～61；本轮未烧录、未提交、未推送、未同步知识库。

### OP-152｜完成 V5-59 公共 Stream Carrier/Source 软件闭环

- 时间：2026-08-14。
- 设计/实现：核对三板 ESP32 Bench 的 COBS+0 载体后，在主库新增 `include/ucn/adapters/ucn_stream_source.h` 与 `src/adapters/stream/ucn_stream_source.c`。每个 UART/RS-485/USB CDC 实例使用独立调用者 Ring/Frame Storage 和 Source ID；Task/ISR 整块写入，Owner 原地 COBS Decode，再提交 V5-58 公共 Queue。模块不包含 SDK、GPIO、串口 Handle、DMA/USB 类型或动态内存。
- 正确性修复：首次实现只用布尔 Gap 会在 Ring 满时把缺口前已经完整排队的 Carrier 一起丢弃；改为记录 `bytes_before_gap`，先交付缺口前数据，到真实缺口后才丢弃至下一 `0x00`。Queue 满时保留一个已解码帧并停止消费后续 Carrier，下一 Drain Round 重试。
- 测试：新增 `test_stream_source`，覆盖 TX COBS 精确容量、拆包、粘包、Wrap、多 Source、Task/ISR token、无 ISR token、Reset/Fallback、坏 COBS、空/短/超长 Carrier、错误预算、Ring 满的缺口顺序和公共 Queue 背压。公共头链接、配置默认/回退/产品覆盖和资源输出同步更新。
- 回归：Windows Full `14/14`、Lite `11/11`、Nano `1/1`、Full Service OFF `11/11`、128 B/3 Source 产品配置 `5/5`；WSL ASan+UBSan `1/1`、GCC `-fanalyzer` `1/1`。默认/产品 Host x64 Stream Storage=`771/258 B`，Source 对象=`240 B`；这些不等于目标 MCU ABI。
- 文档/边界：新增 [V5-59 方案/实现](../08-实现与验证/版本演进/UCN_V5_59_标准Stream_Carrier与UART_USB_Source方案.md) 和 `stream_source.calltree.yaml`，同步 README、Adapter 契约、架构、使用手册、全局配置及任务表。真实 UART DMA、USB CDC、RS-485 半双工/流控、RTOS ISR、CPU/栈/功耗与长稳继续 V5-61；CAN/CAN-FD 保持独立 V5-60。本轮未烧录、未提交、未推送、未同步知识库。

### OP-153｜完成 V5-60 CAN/CAN-FD Frame Source 与经典 CAN Carrier 软件闭环

- 时间：2026-08-14。
- 设计边界：新增 [V5-60 设计/实现](../08-实现与验证/版本演进/UCN_V5_60_CAN_CANFD_Frame_Source与经典CAN_Carrier设计.md)。CAN-FD 不增加新 UCN Wire Header，而是把完整 v5 帧零填充到最小合法 DLC；接收端用新增 `ucn_frame_peek_encoded_size()` 恢复真实长度并拒绝非零 Padding。经典 CAN 固定为 `C1 START / C2 CONT + Transfer ID + 严格 Segment Index`，最多 256 段/1278 B；Carrier 与 Extended Transfer 不共用分片、Slot、序号或超时。
- 公共实现：新增 `include/ucn/adapters/ucn_can_source.h` 与 `src/adapters/can/ucn_can_source.c`。每个控制器实例使用调用者 Frame Ring、Source ID、静态 CAN ID→Link Resolver 和可选固定 Reassembly Slot/Storage；Task/ISR 整帧全入或全拒，Owner 有界解析/重组并提交公共 RX Queue。模块不含 CAN SDK、寄存器、引脚、收发器、动态内存或自动硬件恢复。
- 故障/指标：加入 Active/Error-Passive/Bus-Off/Recovering 显式状态；Bus-Off/恢复中清 Ring、隔离旧重组并拒绝新 RX，产品仍必须同步 Link `is_up` 并完成真实控制器恢复。Health 输出 Ring 千分比压力、活动 Slot 和累计失败输入，真实 TEC/REC、仲裁等待和总线利用率仍由产品 `get_metrics()` 提供。
- 测试：新增 `test_can_source`，覆盖全部 CAN-FD DLC 边界、双控制器/双 Source、Task/ISR token、缺 ISR 锁、过滤、非零 Padding、Ring/公共 Queue 背压保留、经典 CAN 正常/末段、乱序/后到段、同 ID 重启、丢段超时、槽满、坏 CRC、Error-Passive、Bus-Off/恢复。公共头链接、全局默认/本地回退/产品覆盖和 Profile 资源输出同步更新。
- 回归：Windows Full `11/11`、Lite `11/11`、Nano `1/1`、Full Service OFF `11/11`、128 B/4 Ring/1 Slot 产品配置 `5/5`；WSL ASan+UBSan `1/1`、GCC `-fanalyzer` `1/1`。默认/产品 Host x64 CAN Convenience Storage=`1184/464 B`，Source 对象=`256 B`；不是目标 MCU ABI。
- 文档/边界：同步 README、整体架构、分层/配置、Adapter 契约、使用手册、快速手册、全局配置、构建测试地图、任务表和 `can_source.calltree.yaml`。V5-40 软件部分并入 V5-60；真实 CAN/CAN-FD 控制器、125K～1M 仲裁/拥塞、Bus-Off 时序、硬件 Filter、Q0/Q1 延迟、CPU/栈/功耗和长稳继续 V5-61/V5-41。本轮未烧录、未提交、未推送、未同步知识库。

### OP-154｜完成 V5-62 预发布破坏性 API 升级与三项审计缺陷修复

- 时间：2026-08-14。
- 决策：项目尚未对外稳定发布，允许为正确性执行源码/ABI 破坏，不再为旧六/八字段位置初始化保留伪兼容。Wire Header、消息编号、经典 CAN C1/C2 Carrier 和 Transfer Fragment/ACK 均不变，`UCN_PROTOCOL_VERSION` 保持 5；工程 Port API 独立标为 V2。
- Port API V2：`ucn_port_ops_t` 前置 `struct_size/api_version`，新增 `UCN_PORT_OPS_API_VERSION=2` 与统一兼容检查。Protocol Owner、Event Runtime、Adapter、Stream Source、CAN Source 在读取回调前失败关闭；仓库全部初始化改为 C99 具名方式，测试覆盖短结构和错误版本。外部产品必须清理旧对象并全量重编译。
- Transfer：`ucn_transfer_config_t` 强制提供权威单调 `now_ms/context`，Send、RX 和 Step 统一采样；`ucn_transfer_step()` 删除外部时间参数。专项覆盖缺时钟拒绝、uptime 100000 ms 初始化后立即发送，以及长空闲到 500000 ms 后再次发送，均能正常产生 Fragment 并完成交付。
- CAN：经典 CAN 完成 Carrier 后立即结束本轮物理 Drain，下一轮先提交完成 Slot；只有未完成 Slot 可被同 ID 新 START 重启，Queue 背压继续保留 Slot/物理帧。专项把两条同 CAN ID Carrier 的 8 个物理帧先全部放入 Ring，再 Drain，验证两条均交付且无静默 restart。
- 回归：全新 Windows MinGW Debug 为 Full+配置契约/Scale `14/14`、Lite `11/11`、Nano `1/1`、Full Service OFF `11/11`、128 B/3-Link 产品头+配置契约 `5/5`；WSL GCC 13.3 ASan+UBSan `1/1`，`-fanalyzer` 构建与 `1/1`。`git diff --check` 通过；16 份 Call Tree YAML 全部可解析，项目 Markdown 本地链接缺失 0、代码围栏不平衡 0。
- 文档：新增 [V5-62 修复报告](../08-实现与验证/版本演进/UCN_V5_62_Port_API_V2与审计缺陷修复报告.md)，登记 V5-62/DOC-056/DOC-057；同步 README、整体架构、协议分层、Adapter 契约、使用手册、六平台快速手册、V5-56/58/60 历史说明、代码审计、架构地图与 Call Tree。
- 边界：本轮未访问/烧录开发板，未运行真实 FreeRTOS ISR 或 CAN 控制器；外部 ESP32 Bench 尚未迁移本次 API。实机仍归 V5-61/EXT-07。本轮未提交、未推送、未同步知识库。

### OP-155｜完成 V5-63 扩展环 Epoch 修复与四板 UART 三跳门禁

- 时间：2026-08-14。
- 实机复现：外部 PlatformIO Bench 使用 COM40/41/43/44 四块 ESP32-S3-N16R8，按 A-B-C-D 三段 3M UART 线形连接。首次 UART-only 运行已经形成 3 Hop/Cost 102，却因反向路线停留在小 Ring 的旧 Epoch 而在 B/C 拒绝合法业务。
- 代码：`learn_route()` 对同 Cost、同 Hop、同出口的新 Epoch 执行同路径刷新，并把旧 Epoch 放入 `UCN_ROUTE_EPOCH_GRACE_MS`；较低 Cost 路线仍清除旧 RTT，同路径 Epoch 刷新保留有效 RTT。
- 回归：新增四节点扩展环测试，2 Hop Ring miss 后 4 Hop Ring hit，核对 A↔D 及中继正反向路线 Epoch 一致并实际交付 Q1。Windows Full/Lite/Nano/128B 产品/Service OFF 为 `14/14、11/11、1/1、5/5、11/11`；独立 WSL ASan/UBSan 与 `-fanalyzer` 均 `1/1`。
- 实机结果：修复后 A→D 三跳 T32～T8K 9/9 CRC 正确，85 Fragment、0 重试、0 失败；8 KiB 260 ms，75 s 新启动窗口无 Panic/Watchdog。详见 [V5-63 报告](../08-实现与验证/版本演进/UCN_V5_63_扩展环Epoch一致性与四节点三跳实测.md)。
- 边界：四板最终固件为 UART-only 单源诊断配置。ESP-NOW-enabled 四节点此前仍有 Panic；多 Origin Epoch 所有权、逐段断链/恢复、长稳、CPU/功耗均未由本轮证明，分别登记 V5-64/V5-61/EXT-07。本轮未提交、未推送、未同步知识库。

### OP-156｜登记 V5-65 Transfer 冷启动寻路重入并完成固定路由跳损对比

- 时间：2026-08-14。
- 新实机证据：外部四板 Hop Benchmark 在无预热路线时直接提交 T8K。自动 H1/H2 各 10/10；自动 H3 接受 Transfer 后 45 s 保持 0 Fragment，A 为 96 RREQ/0 RREP，D 为 0 RREQ/0 RREP。三段 UART/Heartbeat 正常，无 Wire/CRC/Panic/Watchdog。
- 根因：`ucn_transfer` 的首片发送每次收到 `UCN_ERR_NOT_FOUND` 都调用可重启的 `ucn_node_discover_route()`；活动 Discovery 每 100 ms 重发并重置 250 ms Ring Deadline/Overall Start，初始 2-Hop Ring 永远无法扩到 4 Hop。这是单 Origin 的 V5-65，不是 V5-64 多 Origin 所有权。
- 数据面对照：四节点安装相同静态树路由后，1/2/3 Hop 各 10/10、总计 1,230 Fragment、0 重传/CRC/Queue/UART 错。中位吞吐为 `45.714/37.471/34.858 KiB/s`，相邻损失 `18.033%/6.972%`，3 Hop 相对 1 Hop 下降 `23.748%`。固定路由结果不替代自动寻路失败。
- 文档：新增 [V5-65 缺陷登记](../08-实现与验证/版本演进/UCN_V5_65_Transfer冷启动寻路重入缺陷.md)，登记 V5-65/DOC-059，并在 V5-63 报告说明先前九档测试未覆盖 T8K-first 冷启动边界。当前只更新任务/证据，尚未修改 Core 或执行修复回归；未提交、未推送、未同步知识库。

### OP-157｜修复 V5-65 并完成四板自动 1～3 跳复测

- 时间：2026-08-14。
- 代码：`ucn_transfer` 新增内部 `ensure_route_discovery()`；Fragment 发送和返回 ACK 遇到 `UCN_ERR_NOT_FOUND` 时，只有目的节点没有 Pending Discovery 才发起寻路。已活动 Ring 继续由 Core 按 Deadline 扩圈，公开管理面 Restart API 与 Wire v5 均未改变。
- 回归：`test_transfer` 新增四节点 T8K-first 冷启动测试；10/110/210 ms 高频 Step 始终只有 1 次 2-Hop RREQ，261 ms 正常扩为 4 Hop并完整交付。Windows Full/Lite/Service OFF=`11/11`、Nano=`1/1`、128 B/3-Link 产品=`12/12`；WSL ASan+UBSan、`-fanalyzer` 均 `11/11`，`git diff --check` 通过。
- 实机：外部 COM40/41/43/44 四块 N16R8 保持 A—B—C—D 三段 3M UART，关闭静态路由并全量烧录。自动 H1/H2/H3 各 `10/10`、总计 1,230 Fragment、0 重试/失败；H2 为 1 RREQ/2 Hop/Cost 68，H3 为 2 RREQ（2→4 扩圈）/3 Hop/Cost 102。中位吞吐 `46.109/37.209/32.587 KiB/s`，相邻损失 `19.303%/12.422%`，H3 相对 H1 下降 `29.327%`。
- 结论：原 96 RREQ/0 Fragment 故障已闭环；当前板上 A 为动态 H3，B/C/D 为动态中继/接收固件。V5-64 多 Origin、ESP-NOW 四节点、逐段断链、长稳、CPU/功耗仍未完成。本轮未提交、未推送、未同步知识库。

### OP-158｜建立 UCN 理论能力边界与最终目标

- 时间：2026-08-14。
- 文档：新增 [UCN 理论能力边界与最终目标](../10-理论与规划/建议方案/UCN_理论能力边界与最终目标.md)，把最终定位冻结为 MCU-first、无 Linux 依赖、统一多介质、固定资源 Core 与按需 Extended/Host；说明最终开发体验、能力全景、典型用途和明确不承担的职责。
- 极限口径：分别记录 Wire 地址/Hop 表示范围、当前 Build/固定表边界、建议产品分级和必须由实机冻结的吞吐/时延/资源规格；明确 W3 的 4,294,967,294 个单播地址不是单网并发容量，当前 16 Hop 不应扩张为无意义的超长链路，超过 8 KiB 应走有界流式分块而非超大单帧。
- 当前证据：引用现有 Host 规模模拟和四块 ESP32-S3 3 Mbaud UART T8K 自动 1/2/3 Hop 数据；保留单源、UART-only、短线台架边界，不外推为 Wi-Fi/CAN、多源或生产承诺。
- 台账：新增 DOC-060；本项不修改源码、公共 API、Wire、构建配置或实机固件，也不把规划中的 Join、生产 AEAD、Q2/Q3、服务发现和 Host Bridge 写成已实现。

### OP-159｜完成 V5-66 有界多消息并发与四板三跳对比

- 时间：2026-08-15。
- 代码：`ucn_transfer_peer_capability_t` 增加本地产品并发上限，Peer 登记时默认归一为 1；新增 `ucn_transfer_set_peer_concurrency_capability()`。分片 Send 在占用 TX Slot 前统计同目的活动消息并失败关闭。T32/T64、Fragment/ACK Wire、窗口、CRC、重试和路由均未改变。
- 单测：覆盖默认 1、非法 0、未知 Peer、超过本机 Slot，以及专用 4 TX/4 RX 配置中显式放宽到 2 后两条 T128 同时在途并完成。Full/Lite/Nano=`11/11、11/11、1/1`，四槽产品=`12/12`；公开头链接通过。
- ESP32：外部 Bench 使用一个 8 KiB 只读 Arena，四槽发送端不复制四份 Payload。Windows 重新枚举后按 Node ID 确认 A/B/C/D=`COM47/COM48/COM46/COM40`；六环境构建、四板烧录和 Hash 校验完成。
- 实测：3M UART 的 1/2/3 跳各 3 轮、九档共 `81/81`；重传、Invalid、Duplicate、发送拒绝和消息失败为 0。三跳 T128/T256/T512/T1K 分别提升 `159.0%/163.0%/122.7%/50.0%`；T4K/T8K 回退，四槽不设为默认。
- 资源/边界：A 静态 RAM `64,556 B`，比单槽 +192 B；四 RX Slot 的 B/C 为 `83,716 B`，比单槽 B 约 +24.1 KiB。Wire v5 保持兼容，但预发布公共 C 对象须随新头文件全量重编译。详见[V5-66 报告](../08-实现与验证/版本演进/UCN_V5_66_有界多消息并发Transfer优化.md)；本轮未提交、未推送、未同步知识库。

### OP-160｜完成单层自动分簇首阶段与大规模口径校正

- 时间：2026-08-15。
- 架构：新增可选 `ucn_cluster` Extended，不改变 Core Wire v5。Core 仅增加 Owner-only 只读 `ucn_node_copy_neighbor_summaries()`；Cluster 使用一跳已准入邻居，不把 Node Snapshot 当选举输入，也不依赖 Linux。固定 Endpoint=`0xA0`、Cluster Format=`1`、Payload=`28 B`。
- 代码：新增固定 Peer/Candidate/Member 的 `ucn_cluster_t`，实现 Codec、静态评分+Node ID 确定性选举、Join 接受/拒绝、容量上限、轮转广告、Member/Head 租约、Keepalive、Term/Nonce 陈旧拒绝和 Head 失效重选。默认容量 `8/8/16`，Host x64 对象 `880 B`；Core-only Nano 构建未生成 Cluster 产物。
- 测试：新增 `test_cluster` 和 `ucn_cluster_sim`。Full/Lite/Nano 各 `4/4`；全局默认/回退/产品覆盖（Cluster 4/3/6 槽、6000 ms Lease）与原 Scale 合并门禁 `17/17`；WSL ASan+UBSan、GCC `-fanalyzer` 各 `14/14`。64/256/1000 隔离分组模拟均约 `8910 ms` 收敛，对应 `8/32/125` 个 Head、`56/224/875` 个 Member，拒绝 0。Call Tree 17 份 YAML/176 Node ID 可解析且无重复，`git diff --check` 通过。
- 文档：建立 [V5 自动分簇详细设计](../07-Cluster簇/01-早期设计与实测/UCN_V5_67_自动分簇详细设计与首阶段实现.md) 和 C00～C08；重写万级与分簇可行性文档，明确 W3 地址、Host 压测、`PATH_INSTALL` 精确路径和单层收敛都不能证明万级数据面。同步 README、整体架构、分层档案、全局配置、使用手册和 Cluster Call Tree。
- 边界：当前只完成单层首阶段。动态评分、丢包/移动/非对称链路、真实 MCU、生产安全、簇间 Locator/Directory/Tunnel、备用 Head、拆分/合并、独立控制预算和多级簇仍为 C04～C08；本轮未烧录、未提交、未推送、未同步知识库。

### OP-161｜完成 C04 受损链路、重复 Head 消解与故障恢复模拟

- 时间：2026-08-15。
- API：新增 Owner-only `ucn_cluster_set_head_score()`，允许产品提交 `0..10000` 的已滤波能力分；Candidate/Head 后续广告立即使用新分数，但单次变化不强迫现任 Head 退位。
- 首次缺陷证据：加入 15% 丢包、0～40 ms 延迟/乱序、5% 重复和选举期 2 s 单向阻断后，原状态机在 64/256/1000 节点分别残留 `10/39/143` 个 Head，证明候选广告丢失会形成永久重复 Head。
- 修复：Head 收到确定性更优的 Head 广告时累计连续优胜样本；达到配置样本数且满足最小任期后，清理本机 Head 成员槽并加入更优 Head，旧成员通过既有租约恢复。该逻辑只负责局部重复 Head 收敛，不冒充 C07 的备用、协同退位或全局拆并。
- 鲁棒模拟：64/256/1000 受损场景最终均恢复为 `8/32/125` 个 Head 和 `56/224/875` 个 Member，收敛约 `39.66/40.70/41.84 s`；丢弃 `815/3157/12282`，重复 `210/871/3280`，Switch 事件 `10/25/64`，每节点任意 1 s 控制发送峰值 `12/13/14`。64 节点 Head 于 12 s 故障后恢复 16.16 s；成员离开超过租约并在 23 s 返回后 0.41 s 重入；4 s 评分切换后 4.91 s 选出新最优 Head。
- 回归：Windows Full/Lite/Nano 各 `10/10`；全局默认/回退/产品覆盖与原 Scale 合并 `23/23`；WSL ASan+UBSan、GCC `-fanalyzer` 各 `20/20`。Host x64 Cluster 对象仍为 `880 B`，控制门禁为 32 条/节点/1 s，当前所有场景最大 14。
- 文档：同步 C04、README、详细设计、使用手册、万级/分簇能力文档和 Cluster Call Tree。持续跨簇移动、共享无线竞争、隐藏节点、完整拆并、独立 Token Bucket、真实 MCU、簇间 Locator/Tunnel 和多级簇继续 C05～C08；本轮未烧录、未提交、未推送、未同步知识库。

### OP-162｜完成 C05 四节点自动分簇首轮 MCU 实测

- 时间：2026-08-15。
- 台架/固件：外部 PlatformIO Bench 使用 A/B/C/D=`COM47/COM48/COM46/COM40` 四块 ESP32-S3-N16R8，Node ID=`0x10000001..4`，3 Mbaud UART A—B—C—D，Score=`7000/9000/8000/6000`。新增 Cluster ON/OFF 源选择、四角色常规容量 2/专项容量 1 环境、Owner 接入、Endpoint `0xA0`、状态/CPU/成员诊断和同步四串口监控；非 Cluster 环境继续构建。
- 构建/烧录：常规和容量 1 各四环境成功；四板均完成 Bootloader/分区/应用整镜像写入并通过 Hash 校验，容量测试后恢复常规固件。常规 A/B/C/D RAM=`57012/50748/50748/56996 B`、Flash=`604203/599435/599439/603647 B`，ESP32 `sizeof(ucn_cluster_t)=860 B`。
- 选举/容量：容量 2 时 B 在约 8.31 s 成为 Head，A/C 在约 8.33/8.84 s 加入，D 在约 16.32 s 成为独立 Head；容量 1 时严格形成 `{B,A}` 和 `{C,D}`，无超额接纳。Cluster 只比较一跳邻居，D 独立成簇不影响 A→D 三跳数据面。
- 故障/稳定性：一次 20 s 和三次独立 12 s B-Head 复位均恢复；重复三轮租约失效约 7.2～7.8 s，释放后首个三跳冷路由包 264～266 ms，随后 2～3 ms，B 约 3.7 s 加入新 Head A。120 s 收敛后无角色抖动、误过期或 Cluster API/格式/安全/陈旧错误，A 的 156 个 Ping 全部收到回复。
- CPU/诊断：115 s 快照 Cluster 累计约占 0.037%～0.069% 单核时间，单次最大 165～225 us；最低 Heap A/B/C/D=`328140/333364/333068/328156 B`。UART dropped/no-space/decode/length/overflow 为 0；少量 `rejected_by_core` 与路由缓存刷新一致，属于 Core 对已 Pump 控制帧返回非 OK，不是 RX Queue 满，业务未丢。
- 决策/遗留：恢复后的高分 B 不立即抢占 A，保留首阶段稳定优先语义，把最低任期后的优选 Head 恢复明确转入 C07。独立 Bearer 插拔、小时级长稳、功耗仪表、受保护控制仍未完成，C05 标记部分完成。详见[C05 实测报告](../07-Cluster簇/01-早期设计与实测/UCN_V5_C05_四节点自动分簇实测报告.md)；本轮未提交、未推送、未同步知识库。

### OP-163｜实现 C05.1 显式快速 Cluster 故障恢复档

- 时间：2026-08-15。
- 代码：在 `ucn_cluster_config_t` 增加 `recovery_observation_ms`；将 Detached 观察改为绝对 Deadline。只有 Member 的 Head 租约到期才使用该恢复观察值，冷启动、Join 拒绝、明确 Stepdown 与候选落选仍使用完整 `observation_ms`。新增 `ucn_cluster_config_apply_timing_profile()`：`DEFAULT` 保持 `5 s/5 s/3 s/1 s/1 s/2 s/8 s/30 s`，`FAST_FIXED` 明确为 `1 s/0.5 s/1 s/250 ms/250 ms/0.5 s/2 s/10 s`。
- 安全：初始化新增租约关系校验，Advertisement/Keepalive 周期必须均不大于 Lease 的三分之一；非法组合返回 `UCN_ERR_CONFIG`。该变化不修改 Core Wire v5、Cluster Format 1/28 B、Node ID、数据面路由或默认档；`ucn_cluster_t` Host/ESP32 对象大小保持不变。
- 测试：Full、Lite、Nano Cluster 套件均 `12/12`；含全局默认/回退/产品覆盖和 Scale 的配置门禁 `25/25`；WSL ASan/UBSan 与 GCC `-fanalyzer` 各 `22/22`；`git diff --check` 通过。64 节点快速 Head Failover 为 `3.68 s`，快速受损（15% 丢包、0～40 ms 抖动、5% 重复和短时单向阻断）收敛 `3.61 s`，控制峰值 `35/节点/1 s`，在快速档独立门限 `40` 内。
- MCU 准备：外部 Bench 已新增四个 `v5_cluster_fast_*` 目标并全部构建；首次 COM47/48/46/40 批量上传卡在 COM47 esptool 连接阶段，停止超时进程后四口串口正常。监听确认四板仍为 C05 默认档，因此不把 Host 结果冒充实测。待逐板下载模式烧录、启动配置核验和至少四轮 B-Head 故障恢复后才关闭 C05.1/DOC-062。
- 文档：新增[快速簇恢复档设计与验证](../07-Cluster簇/01-早期设计与实测/UCN_V5_C05_1_快速簇恢复档设计与验证.md)，同步任务表、详细设计、README、使用手册和全局配置说明；未提交、未推送、未同步知识库。

### OP-164｜完成 C05.1 快速恢复档四节点短窗 MCU 验证

- 时间：2026-08-15。
- 台架/烧录：外部 Bench 的 A/B/C/D=`COM47/COM48/COM46/COM40` 四块 ESP32-S3-N16R8 在 3 Mbaud UART A—B—C—D 线上自动烧录 `v5_cluster_fast_*`；esptool 全部连接、Hash 校验、RTS 自动复位成功，无 BOOT/EN 手动操作。此前上传失败为 Windows GBK 下 PlatformIO 输出 Unicode 进度条的编码异常，不是自动下载电路或协议问题。
- 配置/故障：四板启动均确认 `FAST_FIXED=1 s/0.5 s/1 s/250 ms/250 ms/0.5 s/2 s/10 s`。四轮合格 B-Head（COM48）8 s 复位中，C 于 `2.062–2.063 s` 加入 D，A 于 `3.500–3.516 s` 接任 Head，B 释放后 `1.140–1.156 s` 加入 A；每轮 `MONITOR_DONE failures=0`，最终 `api_fail/malformed/security/stale=0`。恢复后 A→B Ping=`0–1 ms`、A→D 三跳通常=`2–3 ms`，UART 解码/长度/溢出/丢帧均为 0。
- 诊断/限制：外部 Bench 仅增加了失败时输出 `C05RXERROR` 的诊断，不改 Core 或状态机；首个预诊断轮曾见 B `api_fail.rx=1`，当时无返回码，加入诊断后的四轮未复现，不能称为修复。该测试因 B 是物理中继而在故障期间天然分割 A 与 C/D 数据面，未把该窗口业务不可达归因给 Cluster。CPU 快照约 `0.071%–0.100%` 单核、最低 Heap A/B/C/D=`328140/333356/333060/328148 B`；长稳、功耗、独立 Bearer 和无线档仍待。
- 构建/文档：四个快速 Bench 环境重新编译成功；同步 C05.1 设计文档、任务表与外部实测报告；未提交、未推送、未同步知识库。

### OP-165｜冻结 C06 簇间寻址、目录与隧道实施边界

- 时间：2026-08-15。
- 源码核对：确认 `ucn_cluster` 当前只维护一跳 Peer/Candidate/Member，控制 Endpoint=`0xA0`；Core 按最终 `destination` 转发，而静态 Endpoint 可独立接入 Extended。当前不存在 Locator、Directory、Gateway、Tunnel 或跨簇 Transfer 代码，不能把单层 C05 的选举/租约写成跨簇能力。
- 设计：新增 [C06 簇间寻址目录与隧道详细设计](../07-Cluster簇/01-早期设计与实测/UCN_V5_C06_簇间寻址目录与隧道详细设计.md)。第一阶段定义独立 `ucn_cluster_federation` Archive 与 Endpoint `0xA1`；普通节点只到本簇 Head，Head 以有限 Directory Locator Cache 获得远簇 Head，再以 Core 外层帧透明跨普通中继，目标 Head 向本簇成员交付。普通 Core 中继不保存远端成员 Route/Path。
- 失败关闭：冻结固定 Directory 副本、Locator 的 Node/Cluster/Head/Term/Lease/Nonce、有限 Pending/Seen 表、TTL/回环/Error、MTU 不截断和“不自动劫持既有 `ucn_node_send*()`”规则。默认要求内层 E2E Provider；仅外层保护的诊断模式必须由产品显式选择，不能标为端到端安全。
- 台账：C06 拆为 C06.0～C06.6；C06.0 为已完成设计，其余 Codec、目录、Tunnel、小消息/Transfer、Host 和 MCU 门禁均待实现。C07/C08 仍不得提前实现或宣称自动目录、多 Gateway、目录接管或万级数据面。
- 验证/限制：本次为源码与架构设计核对，未修改 Core Wire、Cluster 状态机、公开 API、构建目标或实机固件；未提交、未推送、未同步知识库。

### OP-166｜完成 C06.1 Federation Codec 与只读 Cluster View

- 时间：2026-08-15。
- 代码：新增可选 `ucn_cluster_federation` Archive、公共头 `ucn_cluster_federation.h`、Endpoint=`0xA1`、Format=`1` 与 Register/Withdraw/Query/Reply/Submit/Data/Deliver/Error 的精确 Codec。公共前缀固定 8 B；Locator 为 32 B、Query/Error 为 20 B、Data 固定头为 28 B。未知 Flags、非法 Kind、零 Transaction/Hop、非法 Node/Cluster/Term/Lease、Endpoint/QoS、长度不一致和“固定头+Inner 超过 `UCN_MAX_PAYLOAD_BYTES`”均失败关闭。
- 资源/边界：C06.1 只冻结格式，不创建 Directory、Cache、Pending 或 Tunnel Runtime 对象，也不暴露可发送的跨簇业务 API；因此不会静默丢弃“看起来可用”的隧道帧。新增 Federation 容量宏进入 `ucn_config.h` 与产品配置契约，但这些表将在 C06.2 之后才进入应用自有对象。Core Wire v5、Core `ucn_node_t`、既有 Cluster 选举状态机和普通 `ucn_node_send*()` 语义均未改变。
- Cluster 边界：`ucn_cluster_get_view()` 与 `ucn_cluster_copy_member_summaries()` 提供唯一 Owner 使用的只读 View/成员快照，避免后续 Federation 直接依赖/修改 `ucn_cluster_t` 私有状态。
- 测试：新增 `test_cluster_federation`，覆盖所有已定义 C06.1 格式 Golden Round-trip、短 Buffer、版本/Flags/Hop/长度损坏、Inner MTU 上界，以及 Cluster View/Member Copy。Windows Full/Lite/Nano 各 `1/1`；全局默认/原始回退/产品覆盖配置契约 `4/4`；128 B 产品全量 `2/2`。WSL GCC ASan+UBSan `1/1`、`-fanalyzer` `1/1`；`git diff --check` 待最终文档同步后统一执行。
- 文档/台账：C06.1 标记完成并更正为“格式库而非 Runtime”；C06.2 才实现 Directory/Lease/Cache/Next-Cluster。未烧录、未提交、未推送、未同步知识库。
- 调用树：新增 `cluster_federation.calltree.yaml` 并更新索引/阅读入口；明确 C06.1 只存在 Codec 与 Cluster View，不能从调用树误读出 Endpoint Handler、Directory 或业务 Tunnel 已可运行。

### OP-167｜完成 C06.2 Locator Directory 与簇级锚点软件闭环

- 时间：2026-08-15。
- 代码：在可选 `ucn_cluster_federation` 中实现静态 Directory Authority、Head 本机/成员 Locator 续租、成员离开/Head Term 变化的先撤销后注册、固定 Pending Query、有限期 Locator Cache，以及 `Cluster ID -> Head Node ID` Next-Cluster 锚点。每个目录副本由产品固定 Node ID 配置；Authority 必须在该列表中并提供 `authorize_head()`。Runtime 仍只接受 Locator/Query/Reply 与 Directory Not Found，`TUNNEL_SUBMIT/DATA/DELIVER` 明确返回 `UCN_ERR_UNSUPPORTED`。
- 正确性修复：新增 Local Locator 的副本进度与 Withdraw 状态；否则成员离开或本 Head 身份改变会仅等旧租约到期，且相同 Node ID 可能无法重新注册。新增“零时钟冷启动立即发布”处理，避免 `0 ms` 被 Deadline 的无期限哨兵吞掉。Directory 对超长远端租约按本地上限收敛，Cache/Next-Cluster 先完整分配后写入，避免 Next-Cluster 表满时形成半更新。
- 测试：扩展 `test_cluster_federation` 的两 Head/一远端 Head Host 队列模拟，覆盖受保护控制、Head 授权、Nonce 重放、旧 Term、两副本中一副本发送失败或已发送但 Query Timeout 后无回复的回退、成员撤销、Head Term 切换、Directory/Cache 过期、无 Authority 配置失败关闭和 Directory 满表拒绝。Windows Full/Lite/Nano Debug=`1/1、1/1、1/1`，默认/回退/产品覆盖配置契约=`4/4`，128 B 产品全量=`2/2`；WSL GCC ASan+UBSan=`1/1`、`-fanalyzer`=`1/1`。这些均为 Host 软件证据，未替代 MCU 两簇实测。
- 公共边界/文档：补充 `ucn_cluster_get_member_summary_at()`，并将所有 Federation Runtime 公开 API 加入公共头符号门禁；同步任务表、C06 设计、全局配置说明、调用手册和调用树为 C06.2 当前事实。Core Wire v5、Core Node 常驻对象、既有 Cluster ABI 与普通发送 API 未改；未烧录、未提交、未推送、未同步知识库。

### OP-168｜完成 C06.3 单帧跨簇 Tunnel Host 软件闭环

- 时间：2026-08-15。
- 代码：在可选 `ucn_cluster_federation` 内实现显式 `ucn_cluster_federation_send()` 与 `TUNNEL_SUBMIT → TUNNEL_DATA → TUNNEL_DELIVER` 数据面；不拦截普通 `ucn_node_send*()`。产品 Send 回调新增 Traffic Class 参数，Q0/Q1 从 A 经 H1、H2 到 C 不改写。`TUNNEL_DELIVER` 扩为 28 B 固定头，携带 Origin/Destination Cluster；Format 仍为独立 Endpoint `0xA1`，Core Wire v5 和 `ucn_node_t` 未改变。
- 安全/失败关闭：默认仍为 C06.2 Directory-only；只有 `enable_tunnel=true` 才开启数据面，此时默认要求 `seal_inner/open_inner/deliver`，缺任一 Provider 初始化失败。仅 `PROTECTED_OUTER_ONLY` 可显式作台架诊断，不能称作端到端安全。来源 Member/Head、目标 Cluster/Member、TTL、固定 Seen `{Transaction,Origin,Final}` 和远端 Head Error 关联都在 Owner 内检查；Cache 未命中不缓存业务数据，而是启动有界 Query、回送 `DIRECTORY_NOT_FOUND`，应用待 Locator 后使用新 Transaction 重试。
- 测试：扩展四节点 Host 队列模拟，验证 A(Member)→H1→H2→C(Member) 的 Q0 三段均保持 Q0，H1 没有直接给 C 发包；覆盖缺内层 Provider 配置拒绝、伪造 Submit、H2 Data 重放、H2 不再拥有 C 的 `DIRECTORY_STALE`、H1→H2 断链 `DOWNSTREAM` 和 TTL 耗尽。Windows Full/Lite/Nano=`1/1、1/1、1/1`，配置契约=`4/4`；当前 Full/Lite/Nano x64 `sizeof(ucn_cluster_federation_t)=2944 B`。这些均为 Host 软件证据，未替代 MCU 两簇实测。
- 边界/文档：C06.4 之前不支持跨簇 T32～T8K Transfer、Fragment/ACK Tunnel、自动 Gateway/目录接管或第三个 Head 转发；Directory 查询完成前不静态缓存 Payload。同步 C06 详细设计、任务表、全局配置、使用手册和调用树；未烧录、未提交、未推送、未同步知识库。

### OP-169｜冻结 C07 主备簇头、快速恢复与目录交接整体方案

- 时间：2026-08-15。
- 文档：新增 [C07 主备簇头、快速恢复与目录交接整体方案](../07-Cluster簇/01-早期设计与实测/UCN_V5_C07_主备簇头与快速代理恢复整体方案.md)，作为后续实现唯一设计基线；现有方案评估/详细草案仅可作为历史输入，不能据其直接实现。
- 关键决策：Cluster 控制 Endpoint `0xA0` 采用一次性 `Format v2` 升级而非在 v1 中偷加 Type/Flags；主备使用“覆盖资格 + 快照/增量同步 + Ready + 租约/多数确认”，不使用成员永久双发 Keepalive。主备双失联时采用短寿命 `RECOVERY_HEAD`，而非声称透明业务代理。
- C06 衔接：Directory 增加按 Cluster 的 Head Lease 与受认证原子 Handover；解决旧 Head 突断后新 Head 不能覆盖未到期 Locator 的现状。跨簇在途业务仍以显式错误和新 Transaction 重试为边界，不承诺零丢失。
- 台账：任务表将 C07 拆为 C07.0～C07.7；本轮只完成设计，没有修改 Cluster/Federation 源码、Wire、构建目标或实机固件，没有执行代码测试、提交、推送或知识库同步。
### OP-170｜完成 C07.1 Cluster Control Format v2 Codec

- 时间：2026-08-15。
- 代码：依据 C07.0 设计基线，把 ucn_cluster 控制格式从 v1 一次性升级为 v2。UCN_CLUSTER_FORMAT_VERSION 改为 2，解码明确拒绝 v1 帧（UCN_ERR_MALFORMED）。新增消息类型 BACKUP_ASSIGN(10)、BACKUP_READY(11)、BACKUP_MEMBER_SYNC(12)、PRIMARY_HEARTBEAT(13)、TAKEOVER_PREPARE(14)、TAKEOVER_ACK(15)、RECOVERY_DECLARE(16)、RECOVERY_ACK(17)；新增角色 UCN_CLUSTER_ROLE_RECOVERY_HEAD(8)。Wire 保持固定 28 B：前 16 B 为 Version/Type/Role/Flags/ClusterID/Term/HeadID，后 12 B 按 Type 重解释（Type 1-9 保留 v1 的 score/capacity/lease/nonce 语义，Type 10-17 使用新增的 backup_generation/membership_sequence/member_node_id/member_lease_ms/member_nonce/recovery_nonce/recovery_ttl_ms/sync_token 字段）。ucn_cluster_message_t 仅追加内存字段，Wire 长度与 Core Wire v5、Endpoint 0xA0、ucn_node_t、Cluster/Federation 对象布局均不变。
- 负向门禁：role_is_valid/message_type_is_valid 扩展到新上限；message_is_valid 按 Type 分派校验（BACKUP_ASSIGN/TAKEOVER 要求非零 generation，MEMBER_SYNC 要求合法 member_node_id/lease/nonce，RECOVERY_DECLARE 要求非零 nonce 与合法 TTL，保留段零化）；encode/decode 用 encode_trailing_12b/decode_trailing_12b 按 Type 分派后 12 B，未用字段保持零。
- 测试：test_cluster.c 新增 cluster_test_v2_codec()，覆盖 8 个新 Type 的 Golden round-trip、v1 帧拒绝、未知 Type(18)/非法 Role(9) 拒绝、错误长度拒绝。Full ucn_tests 全绿（All UCN tests passed.，cluster_bytes=888）；Lite/Nano 均重新编译通过；ucn_cluster_sim 64 节点 clean 收敛 8910 ms、8 Head/56 Member、rejected/dropped/duplicated=0，证明 v2 Codec 未破坏既有选举/加入逻辑。
- 边界：本轮只完成 Codec 与格式层；Backup 状态机、成员同步、多数接管、RECOVERY_HEAD 生命周期、Directory Handover 分别归 C07.2～C07.5，尚未实现。未提交、未推送、未同步知识库；未做 MCU 烧录。
### OP-171｜完成 C07.2 Backup 资格、快照同步与 Ready/失步降级

- 时间：2026-08-15。
- 代码：在 ucn_cluster 内实现单层 Backup 状态机（Format v2 之上）。ucn_cluster_t 新增 backup_node_id/backup_generation/membership_sequence/backup_ready/backup_syncing/backup_primary_node_id/backup_sync_cursor/next_backup_heartbeat_ms/backup_primary_deadline_ms/backup_missed_heartbeats；新增 flags UCN_CLUSTER_FLAG_SYNC_BEGIN/END（仅 BACKUP_MEMBER_SYNC 合法）。Head 在第一个 head-capable 成员加入后发 BACKUP_ASSIGN，按 BEGIN→N 条 Member→END 快照+单调 membership_sequence 同步；Backup 复用 members[] 作只读镜像，覆盖检查（一跳已准入 Peer 覆盖除自身外全部镜像成员）通过后回 BACKUP_READY。成员 Join/Leave/租约过期触发 backup_resync 重发快照；Backup 连续 UCN_CLUSTER_BACKUP_MISS_LIMIT=3 次错过 PRIMARY_HEARTBEAT（keepalive 周期）即失步降级回 DETACHED 走既有重选（接管留 C07.3）。
- 修复：assign_backup 仅从 candidates（发过广告=head_capable）中选成员，避免向无能力成员发快照产生 rejected；handle_backup_assign 接受 JOIN_PENDING（JOIN_ACCEPT 在途）并在 handle_join_accept 保留 BACKUP 角色；覆盖检查排除 Backup 自身；message_is_valid 对 BEGIN/END 不再要求 member 字段；expire_members/remove_member 在失效成员为 Backup 时清除 backup_node_id 避免向已降级节点续发快照。
- 测试：test_cluster.c 新增 cluster_test_backup_sync()，覆盖 BACKUP_ASSIGN 状态转移、BEGIN/成员/END 快照同步、覆盖通过回 READY、缺号重放失步降级；既有 election/failover 断言适配 BACKUP 角色。Full ucn_tests 全绿（cluster_bytes=928，较 v1 Codec 阶段 +40B）；Lite/Nano 编译通过；ucn_cluster_sim 64 clean rejected=0、56 Member、8910 ms 收敛；fast-fixed head-failover recovery 由 3680 ms 降至 2900 ms（Backup 用 keepalive 快速检测失联早于成员租约过期）。git diff --check 无空白错误。
- 边界：本轮完成 Backup 资格/同步/Ready/失步降级，不含 C07.3 多数确认接管（仍为降级重选，不是成员免重入接管）、C07.4 Directory Handover、C07.5 RECOVERY_HEAD/独立预算。max_tx_1s 峰值 54~56 超出 fast 档 40 门限，需 C07.5 Token Bucket 收紧。未提交、未推送、未同步知识库；未做 MCU 烧录。
### OP-172｜完成 C07.3 多数确认快速接管与成员免重入

- 时间：2026-08-15。
- 代码：在 ucn_cluster 内实现多数确认接管（Format v2 之上）。ucn_cluster_t 新增 backup_takeover_active/backup_takeover_deadline_ms/backup_takeover_ack_count/backup_takeover_acked（位图）/known_backup_node_id/member_voted_term；新增 UCN_CLUSTER_TAKEOVER_WINDOW_MS=1000。Backup 连续 BACKUP_MISS_LIMIT 次错过 PRIMARY_HEARTBEAT 后，若 backup_ready 则进入接管：向镜像成员广播 TAKEOVER_PREPARE，成员每 Term 至多回一次 TAKEOVER_ACK，Backup 收到严格多数 floor(active/2)+1 后原子变 Head（term+1、cluster_id 保持不变、续租镜像成员并移除自身条目），广播 HEAD_TAKEOVER；成员收到更高 Term 的 HEAD_TAKEOVER 直接切换 Head 指针免重入。确认窗口超时未达多数降级回 DETACHED 重选（2 成员簇天然无多数即降级）。旧/同 Term 的 HEAD_TAKEOVER 拒绝（防双主）。
- 修复：receive 拆分 HEAD_TAKEOVER 出 ADVERTISE/HEAD_DECLARE 分支；豁免 TAKEOVER_PREPARE/ACK 的 head_node_id==source 前置检查；complete_takeover 为继承的镜像成员续租并移除自身，避免接管后立即被 expire_members 清空；handle_head_takeover 对 term<=current 返回 REPLAY。
- 测试：test_cluster.c 新增 cluster_test_takeover_guard()（旧/同 Term 拒绝、高 Term 免重入切换）；既有 failover 断言改为接管语义（cluster_id 保持 1、term 递增为 2）。Full ucn_tests 全绿（cluster_bytes=944）；Lite/Nano 编译通过；ucn_cluster_sim fast head-failover recovery 由 2900 ms 降至 1140 ms（接管较降级重选快约 2.5 倍），head_switches=8、业务交付正常；clean 场景 rejected=0。git diff --check 无空白错误。
- 边界：本轮完成本簇多数接管；不含 C07.4 Directory 原子 Handover、C07.5 RECOVERY_HEAD/独立 Token Bucket。fast 档接管门控 1140 ms 为 Host 模拟，MCU 实测待 C07.7。max_tx_1s 峰值 54~56 仍待 C07.5 预算收紧。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-173｜完成 C07.4 Directory 原子 Head Handover

- 时间：2026-08-15。
- 代码：在可选 `ucn_cluster_federation` 内实现 C07.4 Directory 原子 Handover（独立 Endpoint `0xA1`，Format v1 不变）。新增 Kind `UCN_CLUSTER_FED_KIND_LOCATOR_HANDOVER=11` 与固定 40 B 帧（8 B 公共头 + 32 B body：`cluster_id/new_head_node_id/new_term/backup_generation` + 16 B 不透明 proof）。新增固定 `ClusterHeadLease` 表 `UCN_CLUSTER_FED_MAX_CLUSTER_HEAD_LEASES=8`（`cluster_id/head_node_id/term/backup_generation/lease_expires_at_ms/handover_proof`）与产品 `authorize_handover()` 回调（可选，NULL 仅依赖 `authorize_head` 作台架诊断）。Authority 处理 HANDOVER：`outer_source==new_head`、`authorize_head + authorize_handover`、`new_term>stored_term` 三者通过后在同一次 Owner 操作原子替换整条 Lease，同/旧 Term 返回 `UCN_ERR_REPLAY`。`handle_locator_register/withdraw` 在 Lease 存在且 Head 不匹配时先于逐成员比较返回 REPLAY（旧 Head 更新从此被拒绝）；`handle_locator_query` 的 Reply 由当前 Lease 解析出 `head_node_id/term`，实现一次 Lease 替换重接全部成员 Locator，不再逐成员撤销/注册。新增公开 `ucn_cluster_federation_publish_handover()` 与 `step()` 内本地 Head 转移/Term 变化自动发布一次握手；`init` 以当前 owner view 播种 `was_local_head/last_handover_term`，避免已为 Head 的节点误发握手。`expire_federation_state` 同步回收过期 Lease。
- 门禁：`LOCATOR_HANDOVER` 校验 `cluster_id!=0`、new Head 合法、`new_term!=0`；固定 40 B 长度错误仍 MALFORMED；未知 Kind/角色仍 UNSUPPORTED；`kind_is_valid/message_is_valid/encoded_size/encode/decode` 全覆盖新 Kind。
- 测试：`test_cluster_federation.c` 新增 `federation_test_handover_runtime()`，覆盖 Codec round-trip（非零 proof）、旧 Head 先注册成员→Backup 握手建立 Lease→同 Term 握手 REPLAY→旧 Head 再注册 REPLAY（先于记录比较）→Directory Reply 经 Lease 解析出新 Head/新 Term→新 Head 以更高 Term 重注册成功（nonce 重启仍被 Term 覆盖）→`authorize_handover` 拒绝（ACCESS）与放行后 Term 递增。Full ucn_tests 全绿（`All UCN tests passed.`，federation_bytes=2944→3296，+352 B 为 8 条 Lease 与跟踪/统计字段）；Lite/Nano 编译通过；`ucn_cluster_sim` 不改 `ucn_cluster`，复测 clean=56 Member/8910 ms/rejected=0、fast-fixed recovery=1140 ms/head_switches=8/max_tx_1s=56，与 C07.3 基线一致。`git diff --check` 无空白错误。
- 边界：本轮完成单 Authority 的 Lease 建立/原子替换/旧 Head 拒绝/Reply 解析与发布 API；不含多 Authority 副本间 Lease 复制、Authority 不可达时新 Head 的有界重试与显式错误、Cache 陈旧重试与 C06.4 Transfer 边界联测（归 C07.6）。"只接受 BACKUP_READY Backup" 由产品 `authorize_handover` 回调验证（协议侧无簇内成员态可见性）。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-174｜完成 C07.5 独立控制面 Token Bucket 与 RECOVERY_HEAD

- 时间：2026-08-15。
- 代码（Token Bucket）：在 `ucn_cluster` 内实现独立固定控制面 Token Bucket（§9.1"独立固定 Token Bucket"的单一聚合池实现），不与业务 Q1 队列竞争。`ucn_cluster_t` 新增单个 `ucn_cluster_token_bucket_t{uint16_t tokens, uint32_t last_refill_ms}`；配置新增 `token_bucket{burst,refill_ms}`（零值回落宏默认）。DEFAULT 档 burst=8/refill=41 ms（峰值上界≈32/s），FAST_FIXED 档 burst=8/refill=32 ms（峰值上界≈40/s）。`send_message`/`send_cluster_message` 统一收敛到 `cluster_transmit()`，先 `token_bucket_refill/take` 再 `config.send`；预算耗尽返回 `UCN_ERR_NO_SPACE`（延后而非无界广播）并计数 `stats.token_deferred`。`send_backup_snapshot_step` 改为"先构建、发送成功后才推进 `membership_sequence`/`backup_sync_cursor`"，Token Bucket 延后时保留同一帧，Backup 永不见序号缺口。
- 代码（RECOVERY_HEAD）：实现主备双失联的短寿命临时恢复簇头。`ucn_cluster_config_t` 新增 `recovery_head_ttl_ms`（默认 30000/FAST 10000）与 `recovery_backoff_max_ms`（默认 2000/FAST 500）；`ucn_cluster_t` 新增 `recovery_eligible/recovery_cluster_id/recovery_deadline_ms/recovery_cooldown_until_ms/recovery_backoff_deadline_ms/recovery_nonce/known_recovery_source/recovery_ack_count/recovery_acked`。Member/Backup 失主（租约到期、接管无多数降级）置 `recovery_eligible`；DETACHED+head_capable 节点观察期满后按 `(node_id % backoff_max)` 退避声明 RECOVERY_HEAD，`recovery_cluster_id=本节点 ID`（全新恢复域，不冒充旧簇）、term=1，广播 `RECOVERY_DECLARE(nonce,ttl)`；无主成员回 `RECOVERY_ACK`（每 nonce 一次）。寿命到期 `stepdown_recovery_head()` 有序退位回 DETACHED 并保留下次有界重退避；收到更高 Term 的 `HEAD_TAKEOVER` 时立即退为 MEMBER 免重入。`receive` 增加 RECOVERY_DECLARE/ACK 分派，`head_node_id!=source` 豁免 RECOVERY_ACK；`handle_head_takeover` 接受 RECOVERY_HEAD 角色；`begin_join` 清除在途恢复候选态。
- 修复：`recovery_cooldown_until_ms` 零值哨兵被 `!ucn_deadline_expired(now,0)` 误判为"仍在冷却"，导致首次声明永远被跳过——补 `cooldown!=0` 判断。
- 测试：`test_cluster.c` 新增 `cluster_test_recovery_head()`（主备双失→退避声明 RECOVERY_HEAD→成员 ACK→寿命退位→更高 Term HEAD_TAKEOVER 免重入回切为 MEMBER）；共享夹具注入宽松 Token Bucket（burst=UINT16_MAX/refill=1 ms）以免压缩时钟下误限速。Full ucn_tests 全绿（`All UCN tests passed.`，cluster_bytes=944→1008）；Lite/Nano 编译通过。`ucn_cluster_sim` 64 节点：clean default 56 Member/11010 ms/max_tx_1s=30（≤32）、clean fast 56 Member/3090 ms/max_tx_1s=39（≤40）、fast-fixed head-failover 48 Member/recovery=1140 ms/head_switches=8/max_tx_1s=39（由 56 收紧到 ≤40）。`git diff --check` 无空白错误。
- 发现：`ucn_cluster_sim --scenario impaired`（15% 丢包/0～40 ms 抖动/5% 重复/单向阻断）暴露既有主备双主收敛缺陷——某节点以 MEMBER 角色向自身 `head_node_id==local` 发 KEEPALIVE（`SELF-SEND type=5 role=3`）；在禁用 Token Bucket（burst=UINT16_MAX/refill=1 ms）下复现，证明与本轮预算无关，归 C07.6 分区/双主收敛专项（旧 OP 基线曾通过，C07.1~C07.3 引入的主备双主回切路径需复核）。
- 边界：本轮完成 Token Bucket + RECOVERY_HEAD（声明/ACK/寿命/退位/回切）；稳定回切 §8 的 `STEPPING_DOWN` 协调（旧 Head 先有序退位、新 Head READY + Directory Handover）与 HEAD 路径 `switch_improvement_percent` 门槛细化未完成，仍沿用 C05 的 `candidate_better` 评分回切，归下一轮（OP-175）与 C07.4 联测。RECOVERY_HEAD 的"简化 Join/ACK"本轮只做 ACK 观测，未接满成员加入/物理表上限门禁。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-175｜完成 C07.5 稳定回切（§8 有序 STEPPING_DOWN）

- 时间：2026-08-15。
- 代码：落实 §8 自动回切。`ucn_cluster_t` 新增 `stepdown_deadline_ms`；新增 `send_head_stepdown()`（向全体成员广播 `HEAD_STEPDOWN`）与 `begin_ordered_stepdown()`（先通知成员、置 `STEPPING_DOWN` + `stepdown_deadline_ms=now+keepalive_interval`、暂存 `pending_*` 目标、计数 `head_switches`）。`consider_head_offer` 的 HEAD 分支由 C05 的 `candidate_better`（等分靠 Node ID）改为 `score_improves_by(candidate, 现任, switch_improvement_percent)`，满足连续 `switch_required_samples` 且 `head_min_tenure_ms` 已到后进入 `begin_ordered_stepdown`（不再 `clear_members` 硬切）；RECOVERY_HEAD 观察到稳定 HEAD 广告时同样走 `begin_ordered_stepdown` 有序回切到原簇。`ucn_cluster_step` 新增 `STEPPING_DOWN` 到期转换：`clear_members → JOIN_PENDING`（复用 `pending_*` 与 `send_join_request`）。`stepdown_deadline_ms` 归零避免哨兵误判。
- 测试：`test_cluster.c` 新增 `cluster_test_stable_switchback()`（低分 Head 收到更优 Head 的连续 ADVERTISE，跨 `switch_required_samples` + 最小任期后进入 `STEPPING_DOWN`，`pending_head_node_id` 指向更优 Head，到期转 `JOIN_PENDING`）。Full ucn_tests 全绿（`All UCN tests passed.`，cluster_bytes=1008→1016，+8 B 为 `stepdown_deadline_ms`）；Lite/Nano 编译通过。`ucn_cluster_sim` 64 节点：clean default/fast、fast-fixed head-failover、mobility、score-shift default 均 exit 0，fast head-failover recovery=1140 ms/max_tx_1s=39 无回归。`git diff --check` 无空白错误。
- 发现/遗留：`ucn_cluster_sim --scenario score-shift --profile fast-fixed` 未闭环（exit 1，head_switches=0）——根因是"Backup 评分挑战"缺失：Backup 不广播广告，现任低分 Head 无从看到其更高分，故 §8 的候选（恢复高分旧 Head 重新入网后以 HEAD/CANDIDATE 广告被现任观测）路径不覆盖"在役 Backup 静默变高分"这一情形；DEFAULT 档因收敛慢于 4 s 评分切换点（选举期直接选出更高分节点）而通过。此情形与 C05 OP-1333"最低任期后的优选 Head 恢复转 C07"对应，属后续独立项（候选主动发起挑战 + Backup 覆盖/安全资格 + C07.4 Directory Handover 联测），归 C07.6 分区/优选 Head 恢复专项。
- 边界：本轮完成 §8 条件 1/2/4 的 Cluster 侧（最小任期 + 评分门槛 + 连续样本 + `HEAD_STEPDOWN`/`STEPPING_DOWN` 有序退位与 RECOVERY_HEAD 回切）；条件 3（候选 Backup 覆盖/安全资格）与条件 4 的"新 Head 完成 Directory Handover"仍待 C07.6 与 C07.4 联测。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-176｜完成 C07.6 规模阶梯、自投递缺陷修复与 -fanalyzer 门禁

- 时间：2026-08-15。
- 代码（缺陷修复）：`handle_backup_assign` 在 MEMBER→BACKUP / JOIN_PENDING→BACKUP 时立即提交 `cluster_id/term/head_node_id`（此前只在随后的 JOIN_ACCEPT 提交）。丢包链路上 JOIN_ACCEPT 若被丢弃，BACKUP 会带着 `head_node_id==local` 的陈旧身份向自身发 KEEPALIVE（`SELF-SEND type=5 role=3`），现改为指派即提交身份，杜绝自投递。`consider_head_offer` HEAD 分支补 Term 单调：`candidate->term > 现任` 立即有序退位（旧 Head 不得凭原始高分抢回）、`candidate->term < 现任` 永不跟随（陈旧 Head 不得被追随）、同 Term 才走评分门槛 + 样本 + 最小任期（对齐 §5.3）。
- 代码（规模阶梯）：`ucn_cluster_sim` 新增 `--group 2|4|8` 运行时组大小（替换固定 `CLUSTER_SIM_GROUP_SIZE=8`，网络结构体新增 `group_size`，`sim_init/run_simulation` 透传），支持 2/4 节点小簇与 16/256/1000 节点大簇；`--nodes` 改为「`--group` 的倍数」校验。
- 测试：Full ucn_tests 全绿（`All UCN tests passed.`，cluster_bytes=1016 不变）；Lite/Nano 编译通过；`git diff --check` 无空白错误。`ucn_cluster_sim` clean 阶梯全部 exit 0：2 节点 1 Head/1 Member/8010 ms/max_tx_1s=7、4 节点 1 Head/3 Member/8690 ms/15、16 节点 2/14/11010 ms/30、256 节点 32/224/11010 ms/30、1000 节点 125/875/11010 ms/30（控制峰值均 ≤ 门限）。64 节点 clean fast、fast-fixed head-failover（recovery=1140 ms/max_tx_1s=39）、mobility、score-shift default 均 exit 0。GCC 14 `-fanalyzer -std=c99 -Wall -Wextra` 对 `ucn_cluster.c` + `ucn_cluster_federation.c` 静态分析 exit 0 无发现。
- 发现/遗留：① `ucn_cluster_sim --scenario impaired --profile fast-fixed` 自投递已消失（不再崩溃），但收敛后 heads=9（组 0 内同 Term=1 双主 idx=0/score=9000 与 idx=1/score=7000 并存，head_switches=54）——根因是受损期双候选各自完成本地选举形成同 Term 双主，随后"退位→Join 失败/成员在双主间震荡"，双主收敛需专项（退位后的候选 Join 去重与成员吸附方向），归后续 OP。② `score-shift --profile fast-fixed` 仍 exit 1（Backup 评分挑战未实现，见 OP-175）。③ ASan/UBSan 为 WSL 门禁：MinGW GCC 链接 UBSan 失败（`collect2 ld 返回 1`），本机 Windows 环境不承载，维持「WSL GCC ASan+UBSan」既有口径。
- 边界：本轮完成 Host 规模阶梯 + 自投递缺陷修复 + Term 单调回切 + `-fanalyzer`；不含 impaired 双主收敛闭环、Backup 评分挑战、Directory Handover 与 C06.4 Transfer 联测（均归后续）。Host 模拟不代表 MCU 空口性能，真实多板实测仍归 C07.7。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-177｜修复快照丢帧重传、Token Bucket 精算与 Backup 失步不再脱管

- 时间：2026-08-15。
- 代码（快照丢帧重传）：`handle_backup_member_sync` 遇序号缺口不再 `backup_clear_sync`（脱管），改为"保持 SYNCING、`membership_sequence` 归零、续租 `backup_primary_deadline_ms`、返回 REPLAY"，等待 Head 的有界快照重传（新 BEGIN 重置序号）。Head 侧 `ucn_cluster_t` 新增 `backup_resync_deadline_ms`；`assign_backup` 与 `backup_resync` 均置该 deadline；`ucn_cluster_step` HEAD 分支在「已指派 Backup 且未 READY 且快照已走完（`backup_sync_cursor > member_count+1`）且 deadline 到期」时 `backup_resync` 重发快照（仅快照完成后才重传，避免加入期反复重传挤占 JOIN）。根因：此前一条快照帧被 15% 丢包丢掉即序号缺口→Backup 脱管→重选→与新 Head 形成同 Term 双主，impaired 出现 heads=9/head_switches=54。
- 代码（Token Bucket 精算）：`token_bucket_refill` 不再把 `added` 截断到 `burst`，改为按 `elapsed/refill_ms` 全量推进 `last_refill_ms`（仅令牌数封顶 `burst`）。原实现 `added` 截断导致 `last_refill_ms` 滞后，空闲"信用"以突发令牌形式泄漏（impaired 场景 max_tx_1s 47 > 40 门限）；修正后任何 1 s 窗口令牌上界 = `burst + 1000/refill_ms`（FAST=8+31=39、DEFAULT=8+24=32）。
- 测试：`cluster_test_backup_sync` 缺口断言由"脱管 DETACHED"改为"保持 BACKUP + backup_syncing=true + backup_ready=false"。Full ucn_tests 全绿（`All UCN tests passed.`，cluster_bytes=1016）；Lite/Nano 编译通过；`git diff --check` 无空白错误；GCC 14 `-fanalyzer` exit 0 无发现。`ucn_cluster_sim` 64 节点：clean default/fast（56 Member/max_tx_1s=30/39）、mobility、score-shift default 均 exit 0；fast-fixed head-failover recovery=2160 ms/max_tx_1s=39（Token Bucket 正确限速后接管广播由 1140 ms 增至 2160 ms，仍在 4000 ms 门限内，符合 §9.1「恢复门槛必须计入发送时间」）。
- 发现/遗留：`ucn_cluster_sim --scenario impaired --profile fast-fixed` 已不再崩溃且控制峰值正确（max_tx_1s=39），但未完全收敛（heads=11/members=48）：根因是 15% 丢包下 `UCN_CLUSTER_BACKUP_MISS_LIMIT=3` 会以约 0.34%/三次连丢 的概率误判主 Head 失联而错误接管（false takeover），每次错误接管抬升 Term、扰动成员，部分成员在重收敛中滞留；同 Term 双主震荡（OP-176 诊断）已被快照重传修复（head_switches 54→24、heads 9→…，但 false takeover 仍产生临时分裂）。`score-shift --profile fast-fixed` 仍 exit 1（Backup 评分挑战未实现，见 OP-175）。
- 边界：本轮完成快照丢帧重传 + 缺口容忍 + Token Bucket 精算；不含 15% 丢包下 false takeover 抑制（需提高 MISS_LIMIT 或引入"主 Head 仍存活"复核，会影响接管时延）、Backup 评分挑战、Directory Handover/C06.4 Transfer 联测（均归后续）。Host 模拟不代表 MCU 空口性能，真实多板实测仍归 C07.7。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-178｜抑制丢包误接管（§5.1 主租约到期 + 成员退位宽限）

- 时间：2026-08-15。
- 代码：落实 §5.1 条件 2「等待 Primary 租约到期」与成员退位竞态修复。`ucn_cluster_t` 新增 `backup_primary_lease_deadline_ms`（在 `handle_backup_assign`、`handle_backup_member_sync` 的 BEGIN/缺口/成员帧、`handle_primary_heartbeat` 五处以 `lease_ms` 续租）；`ucn_cluster_step` 的 BACKUP 触发改为「`backup_ready && !takeover_active && MISS_LIMIT 连丢 && Primary 租约已到期` 才 `start_takeover`」，否则保持 BACKUP 等待（不再在心跳连丢即脱管）。此前 15% 丢包下三条心跳连丢（约 0.34%）即误判主 Head 失联而错误接管，每次抬升 Term、扰动成员；现需 Primary 租约（FAST=2000 ms）也到期，短突发丢包不再抢占活 Head。
- 代码（退位宽限）：`ucn_cluster_t` 新增 `head_grace_deadline_ms`；成员 Head 租约到期后不立即 `set_detached`，而是保持 MEMBER 一个 `keepalive_interval_ms` 的宽限期，期间仍可回 `TAKEOVER_ACK` 或按 `HEAD_TAKEOVER` 切换，宽限到期才脱管；宽限在 `set_detached`、租约续租（`consider_head_offer` 同 Head 分支）、`handle_join_accept`、`handle_head_takeover` 处清零。修复了「成员租约与 Backup 主租约同刻到期 → 成员先脱管 → 无法 ACK → 接管落空」的竞态（单测微时序下 2 ms 级竞态）。
- 测试：`cluster_test_recovery_head` 循环按新退位时序扩展（声明~24 ms、退位即时断言、重退避后回切）。Full ucn_tests 全绿（`All UCN tests passed.`，cluster_bytes=1016→1024，+8 B 为两个 lease/grace deadline 字段）；Lite/Nano 编译通过；`git diff --check` 无空白错误；GCC 14 `-fanalyzer` exit 0 无发现。`ucn_cluster_sim` 64 节点：clean default/fast（56 Member/max_tx_1s=30/39）、mobility、score-shift default 均 exit 0；fast-fixed head-failover recovery=2660 ms/max_tx_1s=39（检测+租约等待合计落入 §5.2「约 2–3 s」正式口径，< 4000 ms 门限）。
- 发现/遗留：`ucn_cluster_sim --scenario impaired --profile fast-fixed` 误接管已消除（heads=8 正确、head_switches 24→14、members 37→47、max_tx_1s=39），但仍未完全收敛（members=47/56）：剩余是 15% 丢包下成员在受扰窗口内脱管后重入缓慢（JOIN_REQUEST/ACCEPT 多次丢包 + Token Bucket 限速 + 宽限叠加），非双主/误接管问题；`score-shift --profile fast-fixed` 仍 exit 1（Backup 评分挑战未实现，见 OP-175）。
- 边界：本轮完成主租约到期门禁 + 成员退位宽限，杜绝丢包误接管；不含 15% 丢包下成员重入提速、Backup 评分挑战、Directory Handover/C06.4 Transfer 联测（均归后续）。Host 模拟不代表 MCU 空口性能，真实多板实测仍归 C07.7。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-179｜完成 §8.3 Backup 评分挑战（score-shift fast 闭环）

- 时间：2026-08-15。
- 代码：落实 §8.3「候选通过 Backup 覆盖/安全资格」的评分挑战路径。`consider_head_offer` 新增 BACKUP 分支（置于容量检查之前，因为挑战无需现任有剩余容量）：当 Backup 观察到其 Primary 的 HEAD 广告、`cluster_id/term` 一致、`score_improves_by(本机 head_score, Primary 广播 head_score, switch_improvement_percent)` 且 `head_min_tenure_ms` 已到，调用新增 `backup_challenge()`：清 Backup 状态、计数 `head_switches`，并**在同一 Cluster 内**转 CANDIDATE（`cluster_id` 保留、`term+1`、`head_node_id=本机`、`current_head_score=本机`、重设 `election_deadline/next_advertise`、计数 `elections_started`）。Backup 转 CANDIDATE 后广播更高分，完成选举变 HEAD（同簇更高 Term），现任 Head 经 Term 单调有序退位，实现「更高分 Backup 主动挑战、低分现任有序让位」。此前 Backup 不广播，现任无从观测其更高分，`score-shift --profile fast-fixed` 永不切换。
- 修复：初版 `backup_challenge` 走 `set_detached` 会清空 `cluster_id` 另起新簇（cluster 2）导致无法与原簇（cluster 1）收敛；改为同簇转 CANDIDATE 保留 `cluster_id`。初版 BACKUP 分支放在 `available_capacity==0` 检查之后，而满簇现任广播容量为 0 会提前 return；移至容量检查之前。
- 测试：`test_cluster.c` 新增 `cluster_test_backup_challenge()`（低分 Head + 高分 Backup，Backup 观察到满簇现任的 HEAD 广告后转同簇 CANDIDATE、term+1、head_node_id=本机）。Full ucn_tests 全绿（`All UCN tests passed.`，cluster_bytes=1024 不变）；Lite/Nano 编译通过；`git diff --check` 无空白错误；GCC 14 `-fanalyzer` exit 0 无发现。`ucn_cluster_sim` 64 节点全部 exit 0：clean default/fast、fast-fixed head-failover（recovery=2660 ms/max_tx_1s=39）、mobility、score-shift default；**score-shift fast 由 exit 1 转 exit 0**（heads=8/members=56/converged=13940 ms/head_switches=16=8 挑战+8 有序让位）。
- 发现/遗留：`ucn_cluster_sim --scenario impaired --profile fast-fixed` 仍 exit 1（members=47/56）——误接管/双主已消除，剩余为 15% 丢包下成员重入缓慢（JOIN_REQUEST/ACCEPT 丢包 + Token Bucket 限速叠加），非控制面正确性问题。Directory Handover 与 C06.4 Transfer 联测、ASan/UBSan（WSL 门禁）仍待后续。
- 边界：本轮完成 Backup 评分挑战（同簇主动挑战 + 现任有序让位）；不含 15% 丢包下成员重入提速、Directory Handover/C06.4 Transfer 联测（均归后续）。Host 模拟不代表 MCU 空口性能，真实多板实测仍归 C07.7。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-180｜JOIN_PENDING 重定向 + C07.4 Directory Handover 联测

- 时间：2026-08-15。
- 代码：`consider_head_offer` 新增 JOIN_PENDING 分支：JOIN_PENDING 节点观察到与 `pending_head_node_id` 不同或更高 Term 的 Head 广告时 `begin_join` 重定向，避免接管/换 Head 后仍向陈旧 `pending_head` 反复发 JOIN_REQUEST。
- 测试：`test_cluster_federation.c` 新增 `federation_test_handover_autopublish()`（C07.4 联测）：Member 节点经"Member→Head + Term 提升"后 `ucn_cluster_federation_step()` 自动 `publish_handover`（`was_local_head` 由 init 播种 false→true 触发），Directory Authority 收到后 `ClusterHeadLease` 建立（`head_node_id`/`term` 正确），验证"簇接管 → Federation 自动发布 → Authority 原子 Lease"端到端闭环。Full ucn_tests 全绿（`All UCN tests passed.`，cluster_bytes=1024、federation_bytes=3296 不变）；Lite/Nano 编译通过；`git diff --check` 无空白错误；GCC 14 `-fanalyzer` exit 0 无发现。`ucn_cluster_sim` 64 节点 clean/head-failover/mobility/score-shift（default+fast-fixed）全部 exit 0。
- 发现/遗留：`ucn_cluster_sim --scenario impaired --profile fast-fixed` 仍 exit 1（members=47/56，6 DETACHED + 3 JOIN_PENDING）——误接管/双主/评分挑战均已闭环，剩余为 15% 丢包 + 2000 ms 单向阻断下的成员重入慢（JOIN_REQUEST/ACCEPT 丢包重试 + Token Bucket 限速），属压力鲁棒性而非控制面正确性；本轮 JOIN_PENDING 重定向未改变该数字（其 stuck 主因是 DETACHED 节点等待被丢的 Head 广告）。ASan/UBSan 为 WSL 门禁。
- 边界：本轮完成 JOIN_PENDING 重定向 + C07.4 端到端联测。C07.6 Host 软件门禁至此完成主体（规模阶梯/自投递/Term 单调/快照重传/Token Bucket/误接管/评分挑战/Directory 联测 + -fanalyzer）；唯一未闭环为 impaired 压力下的成员重入提速（鲁棒性）。Host 模拟不代表 MCU 空口性能，真实多板实测仍归 C07.7（需物理硬件，本 Windows Host 环境不承载）。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-181｜新增《跨节点时间同步与确定性时延建议》归档稿

- 时间：2026-08-15。
- 文档：新增 [跨节点时间同步与确定性时延建议](../05-传输与服务/UCN_跨节点时间同步与确定性时延建议.md)，**仅归档、未实现**。背景：参照 EtherCAT 实时性评估"帧内加时间戳做节点间同步"。核心结论：时间戳只能测量/补偿延迟，不能创造确定性（EtherCAT 硬实时来自单主调度+固定拓扑+硬件直穿+分布式时钟）；UCN 端到端抖动来自介质/排队/寻路/Token Bucket/调度步进，且能力边界文档明确"不能把非确定性 Wi-Fi 变成硬实时总线"。
- 方案分层：A 单向 T1 时间戳（+4 B/帧，零状态，延迟诊断/日志关联，推荐起步）；B 轻量主从同步（简化 PTP 四时间戳，硬前提是驱动/ISR 层打"线时刻"，有线 us~数十 us、无线 ms 级）；C 完整分布式时钟（频率补偿+透明时钟，慎做）；D 不靠时间戳的有界延迟（Q0 固定路径+预留 Token 预算+无重传+deadline 丢弃）。
- 代价量化：帧 +4~8 B（100 B 帧约 8% 带宽）、每节点 12~20 B 固定状态、SYNC/DELAY 每 100 ms~1 s 一次（复用 Cluster Token Bucket）、硬件时间戳为关键前置（协议层打点仅 ~10 ms 级）。
- 边界：本文为建议稿，未改源码/Wire/构建目标；未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-181｜修复 C07 主备授权、受损重入、Directory proof 与容量门禁

- 时间：2026-08-15。
- 代码（Cluster v3）：把 Cluster Control 破坏性升级为 v3。`HEAD_TAKEOVER` 的专用尾部固定携带 `backup_generation`；`BACKUP_ASSIGN` 向每个 Member 宣布 `{backup_node_id, backup_generation}`，成员仅接受这个已知 Backup 的 `TAKEOVER_PREPARE`/`HEAD_TAKEOVER`。Head 选择的 Backup 优先取得指派；首次入簇 Member 进入可重试的定向指派队列；一个租约后启动有界全员复告。令牌不足不推进指派/同步 cursor。`UCN_CLUSTER_MAX_MEMBERS` 明确限制为 32（32 bit 多数 ACK bitmap），产品 `member_capacity` 必须不超过 `UCN_CLUSTER_MAX_PEERS`。
- 代码（预算与恢复）：Backup 快照以 `BEGIN→N→END` 每帧不超过 `UCN_CLUSTER_BACKUP_SYNC_MAX_SPACING_MS=250 ms` 推进；普通 Head 广告优先于后台复制，避免受损时控制面饿死 JOIN/租约。Backup 观察到同一 Primary/Cluster/Term 的受保护 Head 广告时也续 Primary 租约，和直接 `PRIMARY_HEARTBEAT` 组成双重存活证据，避免活主在丢心跳时被错误接管。
- 代码（Federation）：受保护 Federation 初始化现在同时要求 `authorize_handover` 与 `build_handover_proof`；自动 Handover 调用 Builder 生成 proof。相同 Term、相同字段与 proof 的重发只刷新 Lease，冲突同 Term 拒绝。无 C06.3 ACK 时，自动发布先以 250 ms 间隔发最多 3 次，再按 `locator_refresh_ms` 周期续发同一幂等 Handover，不能将本地 send 成功误当 Directory 已确认。
- 测试：新增非法 `member_capacity > UCN_CLUSTER_MAX_PEERS`、伪造非 Backup `TAKEOVER_PREPARE`、受保护 Federation 缺 proof Builder、首轮 Handover 发送失败后重试、相同 Term 幂等 Handover 的回归。Windows Debug Full/Lite/Nano 均 `12/12` CTest；涵盖 64/256/1000 clean/impaired、默认/FAST Head 故障、移动、评分切换。64 节点 FAST 受损样本为 `8 Head + 56 Member`、`15400 ms` 收敛、控制峰值 `27 frame/s`；1000 节点受损也通过，修复了活 Primary 被 Backup 误接管的残余状态。当前 Host x64 Debug 对象为 `cluster_bytes=1048`、`federation_bytes=3328`。
- 边界：以上是确定性 Host 模型与软件测试证据，不等于 Wi-Fi、ESP-NOW、CAN 或 MCU ISR/BSP 已验收；C07.7 仍需满足 Backup 一跳覆盖的实板主断、主备双断、旧主恢复、跨簇目录与长稳/功耗测试。C06.3 未新增端到端 Handover ACK，当前用幂等周期续发维持 Directory Lease。

### OP-182｜C07.7 四板实机前置：线形拓扑覆盖失败关闭

- 时间：2026-08-15。
- 台架：`E:\File\PlatformIO\ESP32_UCN\ESP32S3_N16R8_UCN_Test1` 的四板 Profile 直接引用本仓当前 C07 v3 源。四块 ESP32-S3-N16R8 端口为 A/B/C/D=`COM47/COM48/COM46/COM40`，使用 UART-only、3 Mbps、FAST_FIXED、Endpoint `0xA0`。A/B/C/D 四环境均构建成功，资源分别为 RAM=`57172/50908/50908/57156 B`、Flash=`610091/605379/605379/609559 B`；四次 esptool 全量写入均完成分段 Hash 校验。首次 A 上传的 Windows GBK 进度字符异常留下本任务创建的进程树并占用 COM47，结束该精确进程树后以 UTF-8 子进程重传成功，非板卡或协议错误。
- 冷启动证据：四端口 RTS 同步复位后，A/B/C/D 全部启动、准入和数据面收发；B（score 9000）约启动后 `2.31 s` 成为 Head，A/C 可加入并观察到 `BACKUP` 路径。A→D 路由为 `3 hops/cost 102`，业务 Ping 稳态 `0–3 ms`。四段 UART 的 decode/length/overflow/no-space/queue-drop 均为 0，未见 Panic/Watchdog。
- 结论：此台架是 A—B—C—D 线形；B 的直接成员 A/C 之间不能绕开 B 一跳互达。当前 Backup 镜像中的 `backup_covers_all_members()` 因而失败，Backup 不会形成 `READY` 或参与多数接管；这是 C07 的明确失败关闭，不是 C07 接管失败。随后局部簇间的 v3 控制帧返回 `UCN_ERR_ACCESS(-15)`，表示状态机拒绝非当前簇控制帧，不能归类为 Wire 解析、队列或安全 Provider 故障。
- 边界/下一步：没有在不合格拓扑上注入 B 主断，避免伪造 C07.7 结论。正式 C07.7 需使 Primary 和 Backup 同时一跳覆盖每个 Member，且 Backup 外仍有两个可投票 Member；当前硬件需要共享 CAN/RS-485、第三独立 Bearer，或先修复并验证四节点 ESP-NOW 覆盖。C07 代码保持不改，待合格拓扑再测主断、主备双断、旧主回归、C06 Directory 与长稳/功耗。

### OP-185｜CLV2-M00 冻结基线：Golden Trace、故障注入与测试夹具（待审计）

- 时间：2026-08-15。
- 操作：按《UCN_V5_Cluster_Current_to_Target_v2_详细修改方案与任务表》CLV2-M00 冻结基线，建立不可回退门禁。**M00 只加观测/测试/基建，不改变任何生产协议行为**（src/extended/ucn_cluster.c 语义零改动，仅测试侧与 CMake 变更）。
- 代码（测试基建）：
  - `tests/test_cluster.c` 新增 `cluster_test_golden_trace()`（CLV2-M00-03）：确定性 Golden Trace 覆盖典型生命周期（选举→入簇→Backup 指派→主断接管），跨 3 节点 300 ms 按快照差异输出 CHG 行；首次运行生成 `tests/golden/cluster_golden_trace.txt`（34 行）并提交，后续 M01 重构必须保持该 Trace 逐字节不变。
  - 新增 `tests/cluster_test_fixture.[ch]`：`set_epoch/set_role/set_backup/set_vote` 等状态注入夹具，供测试直接构造边界状态；迁移代表性既有测试使用夹具。
  - 新增故障注入网络（fault network）：`cluster_test_network_t` 支持丢帧/失活注入（`drop_one_in`、`alive=false` 等），3 个故障测试（主断接管、Backup 双断、丢包下重入）通过。
  - `CMakeLists.txt`：ucn_tests 增加 `UCN_CLUSTER_GOLDEN_DIR` 宏与 `cluster_test_fixture.c` 源；`tests/golden/cluster_golden_trace_actual.txt` 为运行时产物，加入 `.gitignore`。
- 验证：FULL 22/22、LITE 22/22、NANO 12/12 全绿；ASan/UBSan 22/22 通过；GCC 14 `-fanalyzer` exit 0 无发现；golden trace 34 行已生成并与参考一致；`cluster_bytes=1080`（M00 基线，未变更生产代码）。
- 文档：新增 `docs/results/cluster_v2_baseline.md`（50 行）记录基线清单：commit a571853=语义基线、6bea852=M00 测试基建基线、测试矩阵结果、golden trace 哈希与大小、cluster_bytes、三档 Profile 状态；更新方案文档执行状态表 M00 → DONE（标注"待用户审计"）。
- 结论/限制：M00 全部产物可复现、可回退（任何 M01+ 改动若破坏 Golden Trace 即门禁失败）。真实实机（C07.7 合格一跳拓扑）与 impaired 压力重入提速不在 M00 范围。已提交 6bea852（基建）+ adc1b10（方案文档），已推送 origin/codex/v5-adaptive-wire。**等待用户审计 M00 后再进入 M01。**

### OP-186｜CLV2-M00.1 审计闭环比：封死门禁 fail-open 并补齐 6 项收尾

- 时间：2026-08-15。
- 背景：用户对 M00 审计结论为 AUDIT HOLD（基线/结果/生产语义未变 PASS；门禁基建 CONDITIONAL）。本操作按审计要求完成 6 项收尾，全部只改 test/tools/docs，生产 `src/extended/ucn_cluster.c` 零语义改动。
- ①门禁脚本 fail-closed：重写 `tools/m00_matrix.sh`——`set -euo pipefail`、ROOT 由脚本位置推导（去硬编码）、6 路（full/lite/nano/asan/analyzer/core_only）逐 PID `wait` 收集状态、任一失败整体非 0；core_only 路显式断言 `libucn_cluster.a` 不存在。负向验证 `tools/m00_negative_gate_check.sh`：人为注入 `-DUCN_PROFILE=BOGUS` → nano 路 FAIL → 脚本 exit 1、`MATRIX: FAILURE`；随后恢复正版矩阵 6/6 PASS。
- ②Golden Gate 封死两个漏洞：CMake 拆分 `UCN_CLUSTER_GOLDEN_REFERENCE_DIR`（源树，测试只读）与 `UCN_CLUSTER_GOLDEN_ACTUAL_DIR`（各自 build 目录，5 路并行无共享文件竞争）；reference 缺失默认 FAIL（`tools/m00_golden_missing_check.sh` 验证 rc=1），仅 `UCN_UPDATE_CLUSTER_GOLDEN=1` 允许显式重生成并提交；Trace 升级为 `old/event/new` 三态行（SYNC/STEP/RX:<type> 事件粒度），golden 由 34 行 CHG 快照升级为 37 行带事件与前后状态。
- ③资源基线：新增 `tools/cluster_size_report.sh`（`-fstack-usage` 专用构建 + `size -A` 汇总），产出 `docs/results/m00_matrix/size_report.md`：FULL/LITE/NANO `sizeof(ucn_cluster_t)=1080`、`.text=30780`、`.rodata=320`、`.data=0`、`.bss=0`、ucn_cluster.c 最大静态栈帧 208 B；CORE_ONLY ucn_core `.text=137360`、cluster 未链接。
- ④Codec fuzz smoke：新增 `tests/test_cluster_fuzz.c`——Type1-19 各一合法 seed（encode 校验），xorshift32 固定种子随机 1-4 字节翻转 20000 cases，1/8 附加长度损坏用例；断言 decode 只返回 OK/MALFORMED/ARGUMENT、decode-OK 满足线格式不变量（type/role/epoch/flags）、live receive 只返回文档化错误集合；在 ASan/UBSan 矩阵路下运行。
- ⑤Fault Harness 补齐：`cluster_test_fault_t` 新增 dup/delay/reorder（deterministic one_in + xorshift32 延迟量）、partition heal、Owner step skip mask、neighbor state override（SUSPECT/REMOVED flap）、storage-failure placeholder（M04 连接 Persistence Provider）；新增统一 `cluster_test_fault_setup()` 并迁移全部 fault 测试；新增 5 个故障测试（deterministic replay、dup/delay/reorder 收敛、skip owner step、neighbor flap、partition heal），fault 测试总数 3→8。
- ⑥审计文档：`cluster_v2_baseline.md` 重写 §8（6 路 + 专项证据）、新增 §9（00-01~08 逐项 PASS/证据表）、§10 日期更正（WSL 时钟漂移误写 08-17，实为 2026-08-15，Host/WSL/Git 三时钟一致）；方案文档状态表 M00 → AUDIT HOLD（待用户复签）；本记录为 OP-186。
- 验证（全部在 WSL Ubuntu-24.04-ROS gcc 13.3.0）：6 路矩阵 ALL PASS（full/lite 22/22、nano 12/12、asan 22/22、analyzer 0 告警、core_only cluster-absent）；golden identical；golden missing → FAIL（rc=1）；forced matrix failure → FAIL（rc=1）；fault deterministic replay 两次运行逐节点 (role,term) 一致；size report 生成。
- 结论/限制：门禁基建 fail-closed 达标，M00 等待用户复签后进入 M01。storage-failure 为 placeholder（M04 激活）；max static stack 为单帧上限（不含动态调用链求和，M01/M02 需控制帧增长）。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-187｜CLV2-M00.2 Clean Reproducibility Closure

- 时间：2026-08-15。
- 背景：用户对 M00.1（6206ce2）复签：Golden/Fault/Fuzz/Fail-closed 六项全部 PASS，仅剩 2 项证据收尾——Matrix 非真正 clean build（日志出现 `ninja: no work to do`）、资源基线 stack 为 FULL 值复制三份而非每 Profile 实测。本操作只改 tools/docs，生产 `src/extended/ucn_cluster.c` 零改动。
- ①Clean-by-default：`tools/m00_matrix.sh` 每次 canonical 运行先 `rm -rf` 六个 build 目录（full/lite/nano/asan/analyzer/core_only）再从零 configure/compile/ctest；新增 `--incremental` 开发模式（其日志不作为审计证据）；修正 FAIL 提示日志路径为 `m00-<name>.log`。
- ②Per-profile stack：`tools/cluster_size_report.sh` 改为 FULL/LITE/NANO 各建独立 clean `-fstack-usage` 树（`build-m00-stack-{full,lite,nano}`，每次 rm -rf 重建、并行构建），分别测量 ucn_cluster.c 最大静态栈帧：实测 208/208/208（独立测出、恰好一致），不再复制 FULL 值。
- ③trap 加固：`m00_golden_missing_check.sh` 加 `restore_golden` trap（EXIT/INT/TERM 恢复被移走的 golden）；`m00_negative_gate_check.sh` 加 `cleanup_bad` trap（清理临时坏脚本与日志）。
- ④文档：`cluster_v2_baseline.md` 修 `core_only.log`→`m00-core_only.log` 路径、§8 增补 §8.1 Clean Closure 证据（clean 编译行数 full=90/lite=80/nano=65/asan=90/analyzer=90/core_only=14，无 `no work to do`）、§9 更新 00-02/00-05 证据绑定；本记录为 OP-187。
- 验证（全部真实执行）：clean 矩阵从零重跑 6/6 ALL PASS；size report 三档 clean stack 实测并写入 `docs/results/m00_matrix/size_report.md`；golden missing → rc=1；matrix 注入 BOGUS → rc=1 且 `MATRIX: FAILURE`；恢复后 canonical 矩阵 6/6 PASS。
- 结论/限制：M00.2 完成，M00 等待用户正式复签 PASS 后进入 M01。`--incremental` 模式的日志不能作为审计证据（脚本头已注明）。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-188｜CLV2-M01 第一批（01-01~03）：Shadow Phase 落地

- 时间：2026-08-15。
- 背景：M00 经用户正式签字 PASS（M00.1 6206ce2 + M00.2 2b222b7 闭环比），授权进入 M01。按用户要求第一批只做 CLV2-01-01~03（Shadow Phase 映射），不做 01-04 统一迁移入口，Phase 绝不驱动协议。
- ①CLV2-01-01：`include/ucn/ucn_cluster.h` 新增 `ucn_cluster_phase_t`（17 值：DISABLED/DETACHED_OBSERVE/ELECTION/JOIN_PENDING/MEMBER_ACTIVE/MEMBER_TAKEOVER_GRACE/HEAD_NO_BACKUP/HEAD_BACKUP_ASSIGNING/HEAD_BACKUP_SYNCING/HEAD_STABLE/BACKUP_SYNCING/BACKUP_READY/BACKUP_TAKEOVER/STEPPING_DOWN/RECOVERY_OBSERVE/RECOVERY_ELECTION/RECOVERY_HEAD + COUNT；不含 Quorum/Fence/Config/Rekey Phase）与 `ucn_cluster_transition_reason_t`（29 值含 COUNT）。`ucn_cluster_t` 新增 shadow 字段：`shadow_phase`、`transition_reason`、`shadow_transition_count`（注释明确生产逻辑 M01 期间禁止读取做决策）。
- ②CLV2-01-02：`src/extended/ucn_cluster.c` 新增 `cluster_phase_from_legacy_state()`（role+bool+deadline 全组合唯一映射，含 recovery cooldown/backoff 细分与 head 侧 backup 三阶段）；`ucn_cluster_step`/`ucn_cluster_receive` 改为 inner+wrapper，wrapper 在每次 Step/RX 后调用 `cluster_shadow_sync()` 保持镜像对齐；`ucn_cluster_init` 播种 INIT。生产行为路径零改动（M01 MUST NOT 清单全部遵守：Wire/Term/选举/Join/Backup/Recovery/Timer 未动，未加 Quorum/Persistence/Fence/Joint Config）。
- ③CLV2-01-03：`cluster_reason_from_diff()` 覆盖当前 FSM 全部实际 phase 迁移对的 reason 推断表（首轮运行暴露 ELECTION→HEAD_BACKUP_ASSIGNING 缺项——选举获胜时 backup 指派在重启/恢复路径幸存，已补 ELECTION_WON 覆盖全部 HEAD_* 落点）；`cluster_rx_reason_from_type()` 提供 RX hint（diff 表优先，hint 仅兜底，绝不覆盖精确匹配）。
- ④测试：`tests/test_cluster.c` 新增 test-only 映射镜像 `test_derive_phase()`（spec 重复实现，防止生产映射错误自我通过）、`cluster_test_shadow_audit()` 挂入普通与 faulted tick（每 tick 断言 shadow_phase==spec 推导 + 每次观察到的 phase 变化 reason 非 UNKNOWN）、`cluster_test_phase_mapping_static()`（枚举计数 + 全部 role×bool×deadline 组合逐一断言期望 phase）、`cluster_test_shadow_lifecycle()`（300ms 生命周期后 HEAD_STABLE/BACKUP_READY/MEMBER_ACTIVE 齐备且每节点迁移数>0）。
- 验证：Golden Trace 37 行逐字节 IDENTICAL（M01 行为等价门禁直接通过，无需重新生成）；clean 6 路矩阵 ALL PASS（full/lite 22/22、nano 12/12、asan 22/22——shadow 审计+fuzz 在 sanitizer 下运行、analyzer 0 告警、core_only cluster-absent）；`cluster_bytes` 1080→1096（+16 B shadow 字段，M01 允许的资源增量）。
- 结论/限制：Shadow Phase 已对齐 legacy 状态，生产仍由 legacy 驱动。01-04（唯一迁移入口 cluster_transition）需先经用户审计本批（尤其 Shadow 映射的枚举与 reason 推断表）后再实施。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-189｜CLV2-M01.0.1 Shadow 语义闭环比（Head 阶梯/Grace 语义/Reason 语义）

- 时间：2026-08-15。
- 背景：用户审计 M01 Batch-1（d197a73）：01-01 PASS、01-02 HOLD（Head ASSIGNING/SYNCING 映射用了 Backup 侧 mirror 字段；Grace deadline 过期被误推导回 MEMBER_ACTIVE）、01-03 CONDITIONAL（reason 推断仅 best-effort，RECOVERY_WIN 不能兼表败方）。按 M01.0.1 closure 五项修复，全部只改 shadow 观测层与测试，生产协议行为仍零改动。
- ①Head 阶梯映射修正：`cluster_phase_from_legacy_state()` 的 HEAD 分支改为真实字段阶梯 `backup_node_id==0 → HEAD_NO_BACKUP；backup_ready → HEAD_STABLE；backup_assign_pending → HEAD_BACKUP_ASSIGNING；否则 → HEAD_BACKUP_SYNCING`（assignment 完成后、READY 前即 snapshot 在途）。`backup_syncing` 不再参与 Head 相位（其为 Backup 侧 mirror 字段）。测试镜像同步修正；移除人为构造的 `HEAD + backup_syncing=true` 假状态用例。
- ②Member Grace 语义修正：MEMBER 分支改为 `head_grace_deadline_ms != 0 → MEMBER_TAKEOVER_GRACE`，不再读取 timer expiration——到期是事件，由 owner 的 timeout action 消费（→DETACHED/RECOVERY），绝不静默推导回 ACTIVE。消除审计指出的 RX 竞态假迁移（时钟过 deadline 但无 lease 刷新却显示 HEAD_LEASE_RENEWED）。
- ③Legacy 不变量：新增生产 `cluster_legacy_state_is_valid()`（HEAD+ready 而 backup_node_id==0 非法；HEAD 携带 backup_syncing 非法；BACKUP ready&&syncing 非法；BACKUP takeover&&syncing 非法）；`cluster_shadow_sync()` 对非法组合 fail-closed（不铸迁移）。测试侧镜像 `test_legacy_state_valid()` 挂入每 tick audit，static 测试构造 4 组矛盾组合断言 invalid。
- ④Reason 语义：`cluster_reason_from_diff()` 注释改为 BEST-EFFORT ONLY / NOT AUTHORITATIVE / MUST NOT DRIVE FSM（同一 old/new 对可有多事件原因，01-04 将以显式 reason 取代推断）；reason 枚举新增 `UCN_CLUSTER_REASON_RECOVERY_YIELDED`（败方加入赢家簇，与 RECOVERY_WIN 分离，COUNT 29→30）；diff 表补 `RECOVERY_HEAD→MEMBER_ACTIVE = RECOVERY_YIELDED` 与 `HEAD_STABLE→HEAD_BACKUP_SYNCING = RESYNC_STARTED`（新映射下分区场景暴露）。recovery_conflict_resolved 测试断言败方 reason != RECOVERY_WIN 且 != UNKNOWN。
- ⑤测试增强：编译期计数围栏（C99 typedef 数组模式，phase==17、reason==30，置于公共头）；static 组合表改真实组合（Head 四阶梯 + periodic-refresh STABLE、grace 三态含 expired-but-unconsumed→GRACE）；`cluster_test_shadow_lifecycle()` 断言真实 Head 阶梯按序出现 `HEAD_NO_BACKUP→HEAD_BACKUP_ASSIGNING→HEAD_BACKUP_SYNCING→HEAD_STABLE`；新增 `cluster_test_shadow_grace_timeout()`（head+backup 双死，member 经 ACTIVE→GRACE→expired 仍 GRACE→超时 action→RECOVERY_OBSERVE）。
- 验证：Golden Trace 37 行逐字节 IDENTICAL（git diff tests/golden 为空）；clean 6 路矩阵 ALL PASS（full/lite 22/22、nano 12/12、asan 22/22、analyzer 0 告警、core_only cluster-absent）；首轮运行捕获 `HEAD_STABLE→HEAD_BACKUP_SYNCING` reason 缺项已补。
- 结论/限制：Shadow 语义已按审计校准；reason 推断明确降级为 best-effort 诊断，01-04 起由显式 reason 取代。待用户复签后进入 CLV2-01-04（唯一迁移入口 cluster_transition）。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-190｜CLV2-M01.0.2 Reachability Closure（takeover+syncing 可达性）

- 时间：2026-08-15。
- 背景：用户审计 M01.0.1（33c8bae）确认 Head 阶梯/Grace/Reason 全部修对，但发现 legacy validity 中 BACKUP takeover_active && backup_syncing 被误判为不可能组合——实际 Current FSM 可达：takeover 开始后，迟到的旧 Primary 同 generation Type12（SYNC_BEGIN）经 handle_backup_member_sync()（无 takeover guard）执行 clear_members()+backup_syncing=true，而 backup_takeover_active 保持 true。M01 原则：Current 可达状态必须被 Phase 如实表达，不得按理想上不该存在判非法。
- ①生产 cluster_legacy_state_is_valid()：删除 takeover_active && syncing 非法判断（注释说明可达性与 M09/M10 归属）；保留其余三条（HEAD ready&&node==0、HEAD backup_syncing、BACKUP ready&&syncing）。测试镜像同步删除。
- ②static 用例：BACKUP + takeover_active + syncing + ready=false → VALID 且 phase 为 BACKUP_TAKEOVER（takeover 优先级最高，映射顺序不变）。
- ③真实时序测试：新增 cluster_test_shadow_takeover_late_sync()——正常形成 READY Backup → skip Head step 使 primary lease 到期 → takeover 开始 → 在 step 后、deliver 前注入同 generation SYNC_BEGIN（窗口精确，避免同 tick 完成接管漏检）→ 断言 Current handler 照旧处理（takeover_active 保持、syncing=true、ready=false）、legacy valid、shadow 仍 BACKUP_TAKEOVER、后续 tick audit 全过。
- ④Known Deficiency 记录：方案文档 M10 节新增注记——迟到 Primary Type12 在 takeover 期间可修改/清空 mirror（含 clear_members），由 M09 committed/staging mirror + M10 frozen TakeoverConfig（CLV2-10-01/10-08）解决；M01 不改生产。
- ⑤非 blocker 项：recovery 败方 reason 断言收紧为 == RECOVERY_YIELDED（原为 != RECOVERY_WIN）。
- 验证：Golden Trace 37 行逐字节 IDENTICAL；clean 6 路矩阵 ALL PASS（full/lite 22/22、nano 12/12、asan 22/22、analyzer 0 告警、core_only cluster-absent）；late-sync 测试注入窗口验证成功（首版因 tick 内 step→deliver 完成接管而漏检，改手工 tick 后精确捕获）。
- 结论/限制：Shadow 语义全部按审计校准，M01.0.1/0.2 两轮 closure 完成。待用户复签后进入 CLV2-01-04（唯一迁移入口 cluster_transition，显式 reason 取代推断）。未提交、未推送、未同步知识库；未做 MCU 烧录。

### OP-191｜确立分级评审工作流（证据优先/测试门禁/按规模定冗余度）

- 时间：2026-08-15。
- 决定：用户批准新的评审方案——小修复（S 档）：1 复现 + 1 规格 + 单写手 + 双对抗评审（5 角色）；大实现（L 档）：1 复现 + 1 规格 + 双写手 + 三评审（7 角色）。原则：证据优先、测试门禁、按任务规模决定冗余度。
- 落地：新增 docs/00-项目管理/UCN_评审工作流.md 定义分级、角色职责、通用门禁（测试先行、Golden 37 行不变、clean 6 路矩阵、-Werror 零告警、评审全 PASS 才提交推送、用户终审才关闭）与档位判定（生产 diff ≤60 行且 ≤2 函数且无新文件 → S 档；其余 → L 档；边界取 L；§4 含零行为变更纯新增豁免条款，判定时须在 OP 记录声明豁免理由）。
- 当前任务归类：CLV2-01-04a（cluster_transition 框架 + 迁移矩阵，零生产行为变更）按 S 档执行：单写手（Code Agent c0965118，Flash Max）+ 双对抗评审（评审 A 协议语义 + 评审 C 任务一致性；评审 B 测试质量待命，A/C 有分歧时启用仲裁）。01-04b~f 每步按同样 S 档执行；M02+ 大实现按 L 档。
- S 档豁免声明（01-04a）：生产 diff 为纯新增静态代码，Golden Trace 逐字节不变、cluster_transition() 无任何生产调用点（未接线）、cluster_bytes 1096 不变——符合工作流 §4 豁免条款，按 S 档执行。
- 评审周期记录（01-04a）：Code Agent 交付 → 双评审 A（CONDITIONAL：F1 phantom-takeover 链、F2 矩阵过度许可约 12 对、F3/F4 MINOR）+ C（PASS）→ B 仲裁（CONFIRM-A，F2 扩为 27 死对，test-6 修正，T-A/T-B 强化）→ owner 裁决（保留 ELECTION→HEAD_BACKUP_*，实测裁决争议对）→ Code Agent 两轮返工（F1~F6 + T-A observed-pairs 捕获 35 对 0 违例 + T-B 链回归）→ 复审 C（PASS，3 NIT）与 A（完成后记录）→ 全 PASS 后提交推送。
- 未提交、未推送。

### OP-192｜CLV2-01-04a 完成：cluster_transition 唯一迁移入口框架 + 迁移矩阵（待用户终审）

- 时间：2026-08-15。
- 流程：S 档（零行为变更豁免，见 OP-191）单写手（Code Agent c0965118）+ 双对抗评审；三轮评审循环：A CONDITIONAL（F1 phantom-takeover/F2 矩阵过度许可/F3/F4）→ B 仲裁 CONFIRM-A（27 死对 + test-6 修正 + T-A/T-B 强化）→ owner 裁决（保留 ELECTION→HEAD_BACKUP_*，实测裁决争议对）→ 返工 F1~F6 + T-A/T-B → 复审 A（新 G1 世代清零 MAJOR）+ C（PASS）→ 第三轮 G1~G4 修复 → A 终复审 PASS + C 复审 PASS。
- 代码（全部纯新增/未接线）：cluster_transition()（static，RULE 注释原文置顶，old==shadow+矩阵校验，非法 Debug assert/Release UCN_ERR_STATE fail-closed 且零状态写入）；CLUSTER_TRANSITION_ALLOWED 17×u32 bitmask 矩阵（67 允许对）；cluster_transition_apply_legacy() 逐目的地镜像清理（非 BACKUP 目的地清 backup_takeover_active + 按真实 site 语义清 mirror，helper 精确匹配 backup_clear_sync 字段集；backup_generation 保持 caller-owned——修复 G1 世代 fencing 隐患；BACKUP_TAKEOVER 目的地最小化，M01.0.2 takeover+syncing 组合可表示且 entry 不清 syncing）；reason 经 cluster_reason_from_diff 兜底（接受迁移永不记录 UNKNOWN）；assert #ifndef NDEBUG 门控。UCN_ERR_STATE=-16 纯增量枚举。
- 测试：CLUSTER_TRANSITION_SPEC 67 对 + EXCLUDED 22 对（钉死拒绝测试）；17×17 全扫描（允许对全过 + 排除对全拒且 memcmp 零状态变化）；场景含 6b/6d 世代保留（generation=5U 全程不变）、6c T-B 链回归（TAKEOVER→MEMBER→重指派→BACKUP_SYNCING 无 phantom 无 TAKEOVER_PREPARE）；T-A observed-pairs 捕获（35 对 0 违例，gate 最后运行）；F4 UNKNOWN 回退用例。
- 证据：T-A 收集器实测裁决 3 争议对（DETACHED_OBSERVE→RECOVERY_OBSERVE 剔除、HEAD_STABLE→HEAD_BACKUP_ASSIGNING 保留、RECOVERY_HEAD→JOIN_PENDING 剔除）+ 补 3 个实测缺失真实对（BACKUP_READY→HEAD_NO_BACKUP、STEPPING_DOWN→MEMBER_ACTIVE/DETACHED_OBSERVE，tick 复合粒度，覆盖 B 清单第 10 项）；ELECTION→HEAD_BACKUP_* 保留（M01.0.1 t=269 实测 + 捕获确认 2→7）。
- 验证：clean 6 路矩阵 ALL PASS；Golden 37 行逐字节不变；cluster_bytes 1096 不变；-Werror 零告警；评审 A 终复审 PASS（G1~G4 全部验证关闭）+ 评审 C 复审 PASS（3 NIT：豁免声明/里程碑注释已落实，reason 标签可选优化不做）。
- 限制：未接线（01-04b~f 按序迁移 site，每步必须保留 site 自身 exit actions；复合对不得作为单次 transition 调用；backup_generation caller-owned 契约已由 6b/6d 钉死）。待用户终审后进入 01-04b。已提交将推送。

### OP-193｜CLV2-01-04a.1 Framework Closure 完成（观察边≠直接边、目标Phase≠唯一副作用）

- 时间：2026-08-15。
- 背景：用户终审 1532d30 为 AUDIT HOLD，2 个 MAJOR 框架模型问题（①单矩阵混入 tick 观察复合对与单 site 直接边；②apply_legacy 仅按目标 Phase 清理无法复刻 Current 各 site 副作用）+ 2 MINOR。按 01-04a.1 五项收尾执行，仅改未接线框架与测试，生产行为零变更。
- ①拆矩阵：CLUSTER_TRANSITION_DIRECT_ALLOWED（61 条单 site 直接边，逐条引用生产写点函数+行号；cluster_transition() 只用此表）与 CLUSTER_TRANSITION_OBSERVED_ALLOWED（DIRECT + 4 个 tick 复合对：HEAD_STABLE→HEAD_BACKUP_ASSIGNING、BACKUP_READY→HEAD_NO_BACKUP、STEPPING_DOWN→MEMBER_ACTIVE/DETACHED_OBSERVE；仅供 T-A 门禁）。复合捷径从 DIRECT 删除；HEAD_NO_BACKUP→SYNCING/STABLE 无单 site 整体删除；接线纪律注释（复合对不得单次调用）。
- ②收缩 apply_legacy：只写 role + 每 Phase 所有入边绝对一致的最小公共不变量；删除全部 destination 式 mirror/known_backup 清理（cluster_transition_clear_backup_mirror 整体删除）；特殊 exit 留在 site（01-04b~f 逐 site 迁移后决定收归）。保留态语义钉死：JOIN_PENDING 先收 BACKUP_ASSIGN（backup 非自己）再 JOIN_ACCEPT 后仍记得 known_backup（Test A 场景 10）；backup_challenge→CANDIDATE→JOIN_PENDING 保留 members/generation（Test B 场景 11）；6c 链回归改由 site 侧模拟清理 + 无 phantom/无 TAKEOVER_PREPARE。
- ③Pre-transition 纪律：cluster_transition() 内 #ifndef NDEBUG 断言 derive(legacy)==old_phase（site 不得先改 phase-relevant 字段再调用；调用后写字段由 step/RX 末尾 shadow sync 校准）；测试经 cluster_test_seed_legacy 播种。
- ④Recovery 诚实：RECOVERY_ELECTION entry 只写 role+eligible=true，不再自动铸 backoff timer（方案文档注记：01-04f 时 caller 必须提供 Current 计算的 deadline/nonce）。
- ⑤评审意见落实：61 条 site 行号全部刷新并 grep 验证（表头注明行号随重构漂移、函数名为准）；HEAD_NO_BACKUP 注释改为目的地不变量语义（node_id=0 定义该 Phase，非每入边写入；ELECTION 入边 complete_election 保留 backup 状态，01-04b 接线须按调用方状态分发 NO_BACKUP/ASSIGNING/SYNCING/STABLE——已写入方案文档 01-04b 接线规则）；post-commit derive 断言注记入文档（接线时实施）；EXCLUDED 22→24 钉死补齐。
- 验证：DIRECT 61 边逐条经评审 A 对 site 写点审计（含 STABLE→ASSIGNING 复合判定：阶梯中 ready 优先于 assign_pending，无单 site）；T-A 35 对 ⊆ DIRECT∪OBSERVED 零违例；17×17 扫描（DIRECT 成员断言，复合对→UCN_ERR_STATE+memcmp 不变）；clean 构建零告警、All UCN tests passed.、Golden 37 行逐字节不变、cluster_bytes 1096；双评审 A/C 全 PASS。
- 限制：仍未接线（01-04b 起逐 site 迁移，每步保留 site 自身 exit actions + Golden/fault/matrix 回归；complete_election 接线按备份状态分派目的地；复合对不得单次调用）。待用户终审后进入 01-04b。已提交将推送。

### OP-194｜CLV2-01-04b.1 完成：首个生产接线（start_election → cluster_transition）

- 时间：2026-08-15。
- 范围：01-04b 第一小批——迁移 start_election() 单个 site（用户终审批准的接线纪律：不碰共享 helper、只迁指定 site、Golden 变一行即视为行为变化禁止 update）。
- 代码（src/extended/ucn_cluster.c +39/-0）：start_election() 唯一的 role=CANDIDATE 写入替换为 cluster_transition(DETACHED_OBSERVE→ELECTION, ELECTION_STARTED, now)，fail-closed 早退（拒绝时不设任何选举字段，下一步重试）；其余副作用（cluster_id=local、term+1 含 0 回绕、head=local、score、role_since、election_deadline、next_advertise、stats++）保持原顺序；新增 post-commit derive 断言（#ifndef NDEBUG assert derive==ELECTION）。生产代码首次出现真实 transition 调用点（原仅 test hook）。
- NIT-1 修复：新增 test-only hook ucn_cluster_test_observed_pair_allowed()（读生产 CLUSTER_TRANSITION_OBSERVED_ALLOWED 表，边界检查）；删除测试侧 CLUSTER_TRANSITION_OBSERVED_EXTRA[]，T-A 门禁单源化（消除生产/测试双规格漂移，用户 NIT-1 方案 A）。
- 评审：A PASS（3 NIT：release 静默重试可加统计计数、post-assert 仅 role 派生、count 中步++ 时序无观察差异）+ C PASS（2 NIT 顺延 01-04b.2：election 路径 reason 断言、行号 +15 漂移）。fail-closed 早退路径论证：正确操作下 transition 必然成功（唯一调用点守卫保证 shadow==DETACHED_OBSERVE）；bug 漂移时拒绝 + 每 Step 有界重试，严格比旧无条件行为更安全。
- 验证：行为字段级等价（副作用顺序不变）；shadow/reason/count 与旧步末推导最终一致；Golden 37 行逐字节不变（git diff tests/golden 空）；observed 门禁 35/0；矩阵 17×17 全过；Debug 构建所有框架断言活跃且全程未触发；cluster_bytes 1096；-Werror 零告警；生产调用点恰 2 个（test hook + start_election），无越界接线。
- 限制/顺延：01-04b.2（complete_election：败选 ELECTION→DETACHED_OBSERVE + 胜选按保留 backup_* 状态动态分派 4 个 HEAD_* 目的地）将同时落实 C 的 2 个 NIT（election reason 断言 + 行号刷新）。已提交将推送。

### OP-195｜CLV2-01-04b.2 完成：complete_election 迁移 + pre-derive 双模式 fail-closed + election reason 断言

- 时间：2026-08-15。
- 范围：01-04b 第二小批——complete_election()（败选/胜选两路径）+ 用户 MINOR（release 下 pre-derive 真正 fail-closed）+ NIT（真实选举路径 reason 断言闭环）。
- 代码：败选路径 transition(ELECTION→DETACHED_OBSERVE, ELECTION_LOST) 严格先于 set_detached()（TRAP 1；set_detached 的 role 重写冗余无害、epoch/vote/lease/grace/known_backup 清理保持 site-owned）；胜选路径按 pre-call 保留 backup_* 状态动态分派 4 个 HEAD_* 目的地（node_id==0→NO_BACKUP；ready→STABLE；assign_pending→ASSIGNING；否则→SYNCING，TRAP 2）后 transition(ELECTION→target, ELECTION_WON)，其余副作用原顺序；两路径均 post-commit derive 断言 + fail-closed 早退（拒绝时保留 CANDIDATE + 过期 election_deadline → 每 Step 重试自愈）。cluster_transition() pre-derive 升级为双模式运行时校验（!valid || derive!=old → NDEBUG 门控 assert + UCN_ERR_STATE，任何写入之前；shadow/DIRECT 检查仍先行）。
- 测试：cluster_test_election_join_and_failover 加逐节点 phase watcher（进入 ELECTION→ELECTION_STARTED；ELECTION→HEAD_*→ELECTION_WON；ELECTION→DETACHED_OBSERVE→ELECTION_LOST；saw_* 终断言三者全观察到——真实路径验证，无假阳性）；92 条行号引用刷新审计零陈旧。
- 评审：A PASS（3 NIT：set_detached 冗余重写随 b.3 迁移消失、runtime 校验使 fail-closed 更严格（正确流不可达）、reason 断言覆盖单场景其余由全局 audit 兜底）+ C PASS（无实质发现）。TRAP 1/2/3 全部验证：loss transition 先于 set_detached；win 分派读 pre-call 状态（post-call 会被 apply_legacy 强制 node_id=0 污染）；两调用前无 phase-relevant 变更（candidate 扫描只读）。
- 验证：字段级等价（两路径 union 一致）；Golden 37 行逐字节不变；observed 35/0；矩阵 17×17；Debug 全断言（pre-transition/runtime pre-derive/双 post-commit）活跃未触发；生产调用点恰 3 个接线 site + test hook；cluster_bytes 1096；-Werror 零告警。
- 下一步：01-04b.3/.4/.5 按点并行模式执行（§5.1：3 个 worktree + 3 个 Agent 各自开发一点，统一合并+门禁+评审）。已提交将推送。

### OP-196｜CLV2-01-04b 完成：选举/Join 组全部接线（点并行模式首次实战）

- 时间：2026-08-15。
- 范围：01-04b 五小批（b.1 start_election、b.2 complete_election 此前单提交；b.3 join callers、b.4 join accept/reject、b.5 stepdown join 本次按点并行合并）。生产接线 site 共 6 个（7 处调用：complete_election 双路径）。
- 点并行（工作流 §5.1 首次实战）：b.3/b.4/b.5 各自独立 worktree + 分支（wt/b3..b5，基线 2210a9a），每点一个 Flash Max Agent（155b09e/924611a/91d4785），各自自测全绿 + golden EMPTY + 本地 commit；统一顺序合并（d809347/b0233c8/3c5b4b3）——冲突均为注释行号与测试插入位置（无真实代码冲突）；合并事故一次（重组脚本截断测试文件）已用 checkout --ours 恢复 + 补回 b5 函数，教训记录于工作流 §5.1（冲突重组须保留文件中间段）。
- b.3：consider_head_offer 的 DETACHED_OBSERVE/ELECTION→JOIN_PENDING（pre-call 按 role 分派 old、JOIN_INITIATED、fail-closed、post-assert）；begin_join_prepare_fields() 拆分（begin_join 保留 role 写供 MEMBER/BACKUP/RECOVERY/retarget 等 legacy 调用者，字段集与原 begin_join 逐项一致）；recovery_eligible 保持 legacy begin_join（01-04f）；retarget 不 transition（自环）。
- b.4：handle_join_accept（JOIN_PENDING→MEMBER_ACTIVE，BACKUP 乱序守卫跳过 transition 仅刷新 epoch；known_backup 保留）+ JOIN_REJECT（→DETACHED_OBSERVE，transition 先于 set_detached，epoch+txid fencing 保留）。
- b.5：HEAD_STEPDOWN JOIN_PENDING 子分支（→DETACHED_OBSERVE，显式 STEPDOWN_ORDERED——避免 fallback 铸 JOIN_REJECTED 语义错；transition 先于 set_detached；nonce fencing 保留；MEMBER/BACKUP 子分支不动）。
- 测试（每点 test-first）：b.3 head_offer_join_wiring 四场景（CANDIDATE/DETACHED/recovery 保持 legacy/fail-closed shadow mismatch）；b.4 join_reject_shadow_transition + join_accept_out_of_order_after_backup_assign（乱序 late-ACCEPT 保持 BACKUP_SYNCING、count=1、epoch 刷新仍跑）；b.5 join_pending_stepdown（fresh nonce→STEPDOWN_ORDERED + stale nonce→fenced no-op）。
- 评审：统一双评审 A（协议语义 PASS，3 NIT：RX fail-closed 返回 UCN_ERR_STATE 不在 deliver 允许集（正确流不可达）、STEPDOWN_ORDERED 与旧 diff 表 JOIN_REJECTED 的语义差异为有意且仅诊断、RECOVERY_* legacy 与 DIRECT 表超集关系）——点间交互全部验证（双入口 pre-derive、retarget 自环、乱序链、helper 拆分字段级一致）；C（一致性 PASS，2 NIT：统一 OP 记录待补、术语 6 site/7 call）。
- 验证：Golden 37 行逐字节不变（efc38ff..3c5b4b3 diff 空）；observed 35/0；矩阵 17×17；Debug 全断言活跃；生产调用点恰 6 site/7 call + test hook（grep 确认）；cluster_bytes 1096；-Werror 零告警。
- 限制/下一步：01-04c（MEMBER/GRACE：grace arm/timeout、MEMBER refresh、better-head LEAVE、STEPDOWN MEMBER 分支）待用户终审 01-04b 后启动；worktree wt/b3..b5 待清理。已提交将推送。

### OP-197｜CLV2-01-04b.6 完成：HEAD_STEPDOWN replay-fence fail-closed closure（用户 MAJOR 关闭）

- 时间：2026-08-15。
- 背景：用户终审 01-04b（292aac5）发现 1 个局部 MAJOR——HEAD_STEPDOWN 处理在 role 分支前写 last_stepdown_nonce，JOIN_PENDING 子分支 transition 失败时 replay fence 已推进（半提交：协议状态被消费而 Phase 未迁移），违反 fail-closed 零半提交契约。
- 修复（用户指定精确形态）：HEAD_STEPDOWN 改为先按 role 分支；JOIN_PENDING 子分支先 cluster_transition(JOIN_PENDING→DETACHED_OBSERVE, STEPDOWN_ORDERED)，成功后才消费 last_stepdown_nonce + set_detached() + post-commit derive 断言；失败返回 UCN_ERR_STATE（fence 未消费、不 detach、后续合法 STEPDOWN 仍可接受）；MEMBER/BACKUP 未迁子分支保持 Current 原顺序（先 nonce 后分支）。epoch/term/nonce fencing 原样。
- 测试：cluster_test_join_pending_stepdown 新增 case (c)——shadow 失同步（shadow=DETACHED_OBSERVE vs role=JOIN_PENDING）→ transition 在 shadow==old 检查（先于 derive 检查）失败 → 断言 UCN_ERR_STATE、fence 未消费、role/pending 不变、end-of-RX sync 将 shadow 回对齐 JOIN_PENDING、无 STEPDOWN_ORDERED 记录；(a) fresh-nonce 成功、(b) stale-nonce fenced 不变。
- 评审：A PASS（3 NIT：end-of-RX 自愈 mint 计数不断言、UCN_ERR_STATE 不在 deliver 允许集（响亮 bug 捕捉器）、libucn_cluster.a 缺索引为构建环境产物已 ranlib 修复）+ C PASS（2 NIT：backup_resync 行号自相矛盾与 consider_head_offer 陈旧约 74 留待下次刷新、OP 记录待补）。
- 验证：FULL + ASan 全绿（observed 35/0）；Golden 37 行逐字节不变；cluster_bytes 1096；功能 diff 仅 HEAD_STEPDOWN 重构（其余全为注释/行号刷新）；生产调用点仍 6 site/7 call + test hook。
- 结论：用户 MAJOR 关闭，01-04b 全部 6 site 接线完成。待用户终审后进入 01-04c（MEMBER/GRACE 组）。已提交将推送。

### OP-198｜CLV2-01-04c.5 完成：HEAD_STEPDOWN MEMBER 子分支接线（b.6 教训应用到 MEMBER）

- 时间：2026-08-16。
- 范围：HEAD_STEPDOWN 处理器（receive_inner）MEMBER 子分支——MEMBER_ACTIVE / MEMBER_TAKEOVER_GRACE -> DETACHED_OBSERVE（STEPDOWN_ORDERED）。BACKUP 子分支保持 Current 原顺序（先 nonce 后 backup_clear_sync，01-04e），未触碰。
- 接线（b.6 fail-closed 教训应用到 MEMBER）：先按 role 分支；MEMBER 子分支从 PRE-CALL 状态推导 old phase（role==MEMBER：grace_deadline_ms!=0 ? MEMBER_TAKEOVER_GRACE : MEMBER_ACTIVE），cluster_transition(old_phase→DETACHED_OBSERVE, STEPDOWN_ORDERED) 先行——set_detached() 写 role=DETACHED 会违反 pre-transition derive 纪律；成功后才消费 last_stepdown_nonce + set_detached() 原顺序 + post-commit derive 断言；失败返回 UCN_ERR_STATE（fence 未消费、不 detach、后续合法 STEPDOWN 仍可接受）。epoch/term/nonce fencing 原样；reason 显式 STEPDOWN_ORDERED（BEST-EFFORT fallback 会铸 RESET/GRACE_TIMEOUT，语义错）。
- 测试（test-first）：cluster_test_member_stepdown_shadow_transition 三场景——(a) MEMBER_ACTIVE fresh nonce → shadow==DETACHED_OBSERVE、reason==STEPDOWN_ORDERED、fence 推进、set_detached 效果；(b) grace 中 MEMBER（old=GRACE）同样 → DETACHED_OBSERVE、grace 清零；(c) shadow 失同步（shadow=DETACHED_OBSERVE vs role=MEMBER 推导 MEMBER_ACTIVE）→ transition 在 shadow==old 检查失败（先于 derive 检查）→ UCN_ERR_STATE、fence 未消费、role/epoch 不变、end-of-RX sync 回对齐 MEMBER_ACTIVE、无 STEPDOWN_ORDERED 记录（镜像 b.6 case (c) 模式）。既有测试全绿：cluster_test_join_txid_and_stepdown_nonce 的 MEMBER 段补 shadow 对齐（迁移后该路径 fail-closed 校验 shadow==old，真实 join 流在 end-of-step/RX sync 处已对齐）。
- 验证：FULL 全绿（observed 35/0）；Golden 37 行逐字节不变（committed blob 为 LF；WSL 下工作树 CRLF 引用按 GOLDEN CHECK 规范化为 LF，hash-object 6ed2ada4 == HEAD blob，无再生成）；cluster_bytes 1096；-Werror 零告警；功能 diff 仅 HEAD_STEPDOWN MEMBER 子分支重构（其余为注释/行号刷新）。
- 结论：01-04c MEMBER stepdown 分支完成。待用户终审后进入 01-04c 其余 site（grace arm/timeout、MEMBER refresh、better-head LEAVE）。已提交将推送。

### OP-199｜CLV2-01-04c 完成：MEMBER/GRACE 组全部接线（点并行第二批）

- 时间：2026-08-16。
- 范围：01-04c 五小点按用户顺序（c.1 grace arm、c.2 refresh、c.3 grace timeout、c.4 better-head、c.5 MEMBER stepdown）3 点并行（wt/c13=2272813：c.1+c.3；wt/c24=c126345：c.2+c.4；wt/c5=11730ea：c.5；基线 f5dae34），统一合并（13da19b/1caa918/425144b，无冲突）→ 统一门禁 → 统一双评审全 PASS。
- c.1：step_inner grace arm——transition(MEMBER_ACTIVE→MEMBER_TAKEOVER_GRACE, HEAD_LEASE_EXPIRED) 先，stats++ 与 deadline 写后置；apply_legacy(GRACE) 的 deadline 表达式与 site 逐字节一致（幂等无冲突）。
- c.3（用户标注最危险点）：grace timeout——transition(GRACE→RECOVERY_OBSERVE, GRACE_TIMEOUT) 先（apply_legacy 写 eligible=true），site 的 eligible=true 重写与 set_detached 后置（transition 前绝不写 eligible，pre-derive 保证）。
- c.2：refresh——同簇同 term offer 且处于 grace 时 transition(GRACE→MEMBER_ACTIVE, HEAD_LEASE_RENEWED) 先，lease/grace=0 后置；MEMBER_ACTIVE 入边无 transition。
- c.4：better-head——LEAVE 发送严格最先、stats.head_switches++ 第二、transition(MEMBER_ACTIVE|GRACE→JOIN_PENDING, JOIN_INITIATED) 第三、begin_join_prepare_fields 最后（用户禁止为整洁改 LEAVE 时序——已保持）。
- c.5：HEAD_STEPDOWN MEMBER 子分支——pre-call 按 grace 状态派生 old（GRACE|ACTIVE），transition 先，成功后才消费 nonce + set_detached（b.6 fail-closed 教训推广）；BACKUP 子分支保持 legacy（01-04e）。
- 测试（每点 test-first + reason 钉死 + fail-closed pin）：cluster_test_lease_grace_reasons（HEAD_LEASE_EXPIRED/GRACE_TIMEOUT）、member_offer_grace_refresh_wiring（HEAD_LEASE_RENEWED + stale-shadow pin）、member_better_head_switch_wiring（JOIN_INITIATED + LEAVE 时序 + ACTIVE/GRACE 双变体）、member_stepdown_shadow_transition a/b/c（STEPDOWN_ORDERED + GRACE 变体 + fence-not-consumed fail-closed）。
- 评审：统一双评审 A PASS（3 NIT：grace/refresh 幂等冗余、显式 reason 语义正确、step 传播 UCN_ERR_STATE 为响亮 bug 捕捉器；13 调用点精确计数=原 7+新 6）+ C PASS（2 NIT：backup_resync 行号约 101 陈旧留待下次、统一 OP 待补）。
- 验证：Golden 37 行逐字节不变（blob 一致）；FULL + ASan 全绿（35/0）；cluster_bytes 1096；Debug 全断言活跃；生产调用点 13 个（7 site/8 call + 6 新）。
- 限制/下一步：01-04d（HEAD ladder：assign_backup、sweep-done、handle_backup_ready、remove_member/expire_members、backup_resync、begin_ordered_stepdown——每 site 自带 node_id/ready/assign_pending 写 + post-derive 断言；STABLE→ASSIGNING 复合对保持复合）待用户终审 01-04c 后启动；worktree wt/c13/c24/c5 待清理。已提交将推送。

### OP-200｜CLV2-01-04d.0 完成：cluster_transition_preflight 框架扩展（preflight 模式前置）

- 时间：2026-08-16。
- 背景：用户终审 01-04c 时建议为 d 组不可逆 site（remove_member/expire_members 先删成员再清 phase 字段）引入 preflight 模式（先校验→执行 Current 不可逆 side effects→commit），避免 b.6 类半提交。本点为纯框架扩展，零行为变更。
- 代码：提取 cluster_transition_validate()（完整校验链零写入：NULL→ARGUMENT；bounds+shadow==old+DIRECT→fail-closed；legacy valid+derive==old→双模式 fail-closed）；cluster_transition_preflight()（纯校验包装，CLV2_01_04_UNUSED，永不 commit，供 d 组在不可逆 side effects 之前调用）；cluster_transition() 重构为 validate-first + commit 原样（行为逐字节一致，17×17 扫描全绿）。RULE 注释保持前置声明+定义两处原文。
- 测试：新 hook ucn_cluster_test_transition_preflight()（UCN_CLUSTER_ENABLE_TEST_HOOKS）；cluster_test_transition_preflight 三场景 memcmp 零写入证明（合法对 UCN_OK 零写、非法对 UCN_ERR_STATE 零写、shadow 失同步 UCN_ERR_STATE 零写）。
- 评审：双评审 A PASS（3 NIT：archive-index 构建环境产物建议 CI 加 ar s、preflight 生产未引用属 d.0 预期、preflight 于 irreversible 的实际使用随 d.4 到来）+ C PASS（2 NIT：OP 记录待补、archive-index 同上）。
- 验证：Golden 37 行逐字节不变；FULL + ASan 全绿（35/0）；cluster_bytes 1096；-Werror 零告警；无新生产接线、无新公共 API。
- 下一步：01-04d 四点并行（d.1+d.2 assign/sweep、d.3 handle_backup_ready、d.4 remove_member/expire_members preflight 首战、d.5+d.6 resync/stepdown）。已提交将推送。

### OP-201｜CLV2-01-04d.4 完成：remove_member/expire_members Backup 逐出 preflight 首战

- 时间：2026-08-16。
- 背景：d.0 框架的 preflight 模式首个不可逆 site 接线（用户明确要求）：被逐成员恰为当前 Backup（backup_node_id==node）时，先 preflight 零写校验、再 transition(HEAD_STABLE|HEAD_BACKUP_SYNCING|HEAD_BACKUP_ASSIGNING→HEAD_NO_BACKUP, BACKUP_LOST)、后跑 Current 不可逆 side effects（memset slot、node_id=0、ready=false、backup_resync）；非 Backup 逐出保持 legacy 路径零 transition。
- 代码：remove_member 增 now_ms 参数（LEAVE 调用点同步）；old_phase 由 PRE-CALL 状态派生（ready→STABLE / assign_pending→ASSIGNING / 其余→SYNCING，即 cluster_phase_from_legacy_state 的 HEAD 阶梯）；preflight != UCN_OK 提前 return（slot 与 backup 字段零触碰）；transition 先于 phase-relevant 清写（d.0 derive 检查要求 commit 时 legacy 仍派生 old_phase，backup_node_id=0 若先行会被判 pre-mutated——apply_legacy(NO_BACKUP) 写 role+node_id=0+ready=false，site 自身清写幂等）；post-commit Debug assert derive==NO_BACKUP。expire_members 同型：先只读探测 Backup 是否到期，Backup 到期走 preflight+transition+原序逐出循环，非 Backup 到期走 legacy；transition 拒绝时整趟逐出零发生（无半提交）。backup_resync 体未动（d.5 职责；remove_member 清 ready 后调用使其提前返回，跨点兼容）。
- 测试（test-first）：新 hook ucn_cluster_test_remove_member/ucn_cluster_test_expire_members（直驱静态 site，无网络 tick、不录 observed pair）；cluster_test_remove_member_backup_loss 覆盖 (a) STABLE 逐出 Backup→shadow==NO_BACKUP、reason==BACKUP_LOST、slot 释放、node_id=0/ready=false；(b) SYNCING 与 ASSIGNING 同型；(c) 非 Backup 逐出→零 transition（shadow/count 不动）；(d) shadow 失同步+Backup 逐出→preflight UCN_ERR_STATE 且 memcmp 零写入（slot 未释放）。observed pairs 保持 35（STABLE→NO_BACKUP/ASSIGNING→NO_BACKUP 未新增观察，直驱 hook 不经过 audit）。
- 验证：Golden 37 行逐字节不变（blob 6ed2ada 一致，已按 GOLDEN CHECK 将工作区文件归一 LF + .gitattributes eol=lf 固化）；FULL 全绿（All UCN tests passed、35/0）；cluster_bytes 1096；-Werror 零告警（-Wall -Wextra -Wpedantic）；Lxxxx 引用刷新（remove_member L2085 / expire_members L4373 / backup_resync L4318）。
- 评审：待统一双评审。已提交将推送。

### OP-202｜CLV2-01-04d 完成：HEAD ladder 组全部接线（点并行第三批 + preflight 首战）

- 时间：2026-08-16。
- 范围：01-04d 六点按 4 点并行（wt/d12=c03b914：d.1 assign_backup + d.2 sweep-done；wt/d3=02b8a84：d.3 handle_backup_ready；wt/d4=f163eaf：d.4 remove_member/expire_members；wt/d56=437460c：d.5 backup_resync + d.6 begin_ordered_stepdown；基线 8d9e846），统一合并（fe1b9df/8810817/4de563a/ca742de，冲突为注释行号+测试插入+一处函数边界重组事故已修复）→ 统一门禁 → 统一双评审全 PASS。
- d.1/.2：assign_backup 的 NO_BACKUP→ASSIGNING（BACKUP_ASSIGNED）+ sweep-done 的 ASSIGNING→SYNCING（BACKUP_SYNC_STARTED）；apply_legacy(ASSIGNING) 扩展武装 assign_pending=true（相位定义不变量，所有入边公共）；两 transition 均 shadow 守卫（失同步时 legacy 路径 + 末步 sync 对齐，观察对保持 35）。
- d.3：handle_backup_ready（ASSIGNING|SYNCING→STABLE, SNAPSHOT_READY）+ 重复 READY 于 STABLE 的幂等守卫（自环非 DIRECT 不达 entry point）。
- d.4：remove_member/expire_members——preflight 模式首战（评审 A 裁决：用户字面顺序不可行因 validate 在 commit 时重 derive；实际顺序 preflight→commit→幂等 site 清理行为等价且满足意图——校验失败零副作用无半提交）；非 backup 移除保持 legacy；新 hooks + memcmp 零写入失败路径测试。
- d.5：backup_resync（STABLE-origin→SYNCING, RESYNC_STARTED；非 STABLE pre-call 无 transition——d.4 兼容）；评审 A 的 MINOR（post-assert 窗口：periodic re-assign 在 ready=true 时武装 sweep 导致 derive 落 ASSIGNING）已按建议放宽为 SYNCING||ASSIGNING 并修正注释。
- d.6：begin_ordered_stepdown（HEAD_*→STEPPING_DOWN, STEPDOWN_ORDERED；RECOVERY_HEAD 源保持 legacy 至 01-04f）。
- 交叉修复：d.4 测试 (c)/(c-expire) 原断言 shadow 保持 STABLE——合并 d.5 后 legacy backup_resync 正确地产生 STABLE→SYNCING transition（Current 语义：非 backup 移除也会触发 resync）；断言更新为 SYNCING/RESYNC_STARTED/count=1（无 BACKUP_LOST）。合并重组事故（sweep 测试函数闭合丢失导致嵌套函数编译错）已修复。
- 评审：统一双评审 A PASS（1 MINOR 已修 + 3 NIT；23 个生产调用点精确计数=原 13+新 10；d.4 顺序张力裁决、交叉修复正确性裁决、shadow 守卫无分歧确认）+ C PASS（2 NIT + .gitattributes 单条 golden LF 规则确认最小正确）。
- 验证：Golden 37 行逐字节不变；FULL + ASan 全绿（35/0）；cluster_bytes 1096；-Werror 零告警；生产调用点恰 23 个；无 01-04e/f 越界。
- 限制/下一步：01-04e（BACKUP/TAKEOVER 组：handle_backup_assign、handle_backup_member_sync、start_takeover、complete_takeover、takeover timeout——F1 锚点 backup_clear_sync 必须保留 site 侧清理）待用户终审 01-04d 后启动；worktree wt/d12/d3/d4/d56 待清理。已提交将推送。

### OP-203｜CLV2-01-04d.7 完成：HEAD-Ladder Closure（用户 3 MAJOR 关闭）

- 时间：2026-08-16。
- 背景：用户终审 01-04d（d992e37）AUDIT HOLD 3 MAJOR——HEAD ladder 仍有 3 个 phase-changing site 未接线（start_backup_assignment_cycle、queue_backup_assignment_for_member、handle_backup_reject）依赖 end-of-step shadow_sync 补铸；backup_resync 的 armed-sweep 窗口依赖补铸；sweep-done 最后一帧 send-then-transition 失败导致 pending=true/remaining=0 永久卡死。按 01-04d.7 七项收口执行。
- ①三个旁路 site 显式接线：cycle 与 queue 的 SYNCING→ASSIGNING（BACKUP_ASSIGNED，transition 先于 pending 写；ASSIGNING 自武装幂等无 transition；STABLE ready-precedence 保持 legacy）；handle_backup_reject 显式链（HEAD_BACKUP_*→NO_BACKUP via BACKUP_LOST + 重选 NO_BACKUP→ASSIGNING via BACKUP_ASSIGNED——count==2 两段显式 transition，无 stale-shadow fallback）。
- ②backup_resync 动态目标：pre-call 按 backup_assign_pending 分派 target（ASSIGNING:SYNCING），transition(STABLE→target, RESYNC_STARTED) 先于 ready=false；post-assert 恢复严格 derive==target（撤销 d.5 的 SYNCING||ASSIGNING 放宽）。
- ③STABLE→ASSIGNING 升为 DIRECT（真实 site=backup_resync armed-sweep 分派）；OBSERVED 复合清单减为 3（BACKUP_READY→HEAD_NO_BACKUP、STEPPING_DOWN→MEMBER_ACTIVE/DETACHED_OBSERVE）；观察对保持 35。
- ④sweep-done 最后一帧 preflight：remaining==1 时先 preflight(ASSIGNING→SYNCING)——失败零副作用（不发帧、cursor/remaining/pending 不动，下一步重试）；成功顺序 preflight→send→transition→cursor/remaining/pending 提交。永久卡死消除。
- ⑤写点审计注释：枚举全部 backup_node_id/backup_ready/backup_assign_pending 写点并分类（transition commit / 显式后写 / phase 不变 / BACKUP 侧 / 01-04e complete_takeover）——评审 A 独立 grep 复核确认真实；评审 C 的 MINOR（注释漏列 handle_backup_reject 幂等清理）已补两处。
- ⑥测试：cluster_test_head_ladder_closure 四场景（cycle+自武装、reject 链 count==2、armed-sweep resync 直转、最后一帧 preflight 失败零副作用+恢复）；2 个新 hook；pre-derive assert 改 CLV2_01_04_ASSERT_FAIL 门控（生产行为不变，测试可走 release 路径）；既有 reject 测试补 shadow 对齐。
- 评审：双评审 A PASS（3 NIT：complete_takeover 遗留 01-04e、READY-mid-sweep 守卫一致、自武装依赖 shadow 对齐；显式声明——HEAD ladder 无任何 phase 变化依赖 shadow_sync 铸币）+ C PASS（1 MINOR 已修 + 2 NIT）。
- 验证：Golden 37 行逐字节不变；FULL + ASan 全绿（35/0）；cluster_bytes 1096；-Werror 零告警；98 条行号刷新零陈旧。
- 结论：用户 3 MAJOR 关闭，HEAD ladder 单入口化真正闭环。待用户终审后进入 01-04e（BACKUP/TAKEOVER 组）。已提交将推送。

### OP-204｜CLV2-01-04d.7.1 完成：Shadow-Guard Closure（shadow 绕过 transition 门禁的通用缺陷关闭）

- 时间：2026-08-16。
- 背景：用户终审 d.7（fe1c561）发现同根因 MAJOR——5 个 site 用 shadow_phase==old 决定是否调用 transition，而 shadow 不一致恰应由 transition 自己验证；shadow 损坏时恰好绕过检测门，回落到 legacy bool + end-of-step shadow_sync 铸币（与 d.7 声明冲突）。通用规则：Legacy/event 决定该发生什么迁移；cluster_transition()/preflight 验证 Shadow 是否同意；绝不允许 caller 用 Shadow 决定跳过 transition。
- 修复（用户 A-E 精确形态）：assign_backup 选中 candidate 后无条件 transition(NO_BACKUP→ASSIGNING)（入口 node_id==0 保证 legacy derive NO_BACKUP；shadow 错则 fail-closed 于 node_id/gen 写前；删除过时 still-legacy 注释）；cycle/queue 的 needs_transition = legacy pre_phase==SYNCING（去 shadow 条件；STABLE ready-precedence 幂等、ASSIGNING 自武装）；sweep last-frame 的 transition_required 由 pre-send legacy 派生（preflight 无条件、失败零副作用；post-send commit 与 loop-exhausted 均按 transition_required 而非 shadow）。RULE 注释原文置顶于写点审计块。
- 测试（镜像对称 5 个，覆盖上一轮盲区的 shadow-desync 方向）：(d) last-frame preflight 失败不发帧+恢复后显式 SYNCING；(e) cycle 不武装；(f) queue 不武装；(g) assign_backup 不提交 node_id/gen；(h) loop-exhausted 不清 pending；全部 knob off 走 release 路径断言零副作用。旧 d.7 case (d)（legacy 错/shadow 对）被替换——其前提（node_id==0 mid-sweep）在真实 FSM 不可达，保护由新 (d) 与 (g) 共同覆盖。2 个新 hook。
- 全局检查（交付证据）：grep 生产 shadow_phase 仅剩框架内使用（validate 的 shadow==old 检查、shadow_sync 的变更检测、commit 写、init 播种）——零 caller-side shadow 跳过守卫。评审 A 独立复证。
- 评审：双评审 A PASS（3 NIT：旧 (d) 前提不可达、自武装依赖 assign_backup 已同步 shadow、STABLE-ready-precedence 无 transition 符合规则；显式声明 Shadow-Guard 规则全站成立）+ C PASS（1 NIT：OP 待补 + 行号漂移留待下次）。
- 验证：Golden 37 行逐字节不变；FULL + ASan 全绿（35/0）；cluster_bytes 1096；-Werror 零告警；正常路径等价（各调用点经显式前序 transition 保证 shadow==old）。
- 结论：用户 MAJOR 关闭，Shadow-Guard 规则成为 01-04e/f 通用纪律。待用户终审后进入 01-04e（BACKUP/TAKEOVER 组）。已提交将推送。

### OP-205｜CLV2-01-04e.2 完成：handle_backup_member_sync SYNC_END 单入口接线（SNAPSHOT_READY + M01.0.2 接管优先）

- 时间：2026-08-16。
- 背景：01-04e（BACKUP/TAKEOVER 组）首点（用户并行指派 e2）。CURRENT：Backup 应用 Type12 帧——SYNC_BEGIN 清 mirror+syncing=true（保持 SYNCING）、数据帧更新 mirror（保持 SYNCING）、SYNC_END 且 backup_covers_all_members 通过 → syncing=false+ready=true+发 BACKUP_READY（→BACKUP_READY）；DELTA 缺口 → ready=false+syncing=true+RESYNC_REQ（保持 SYNCING 或重入）。Shadow-Guard 规则（d.7.1）：Legacy/event 决定发生什么迁移，cluster_transition()/preflight 验证 Shadow 是否同意，绝不用 shadow 决定跳过。
- 代码：仅 SYNC_END+全覆盖路径接线——pre-call 按 cluster_phase_from_legacy_state 派生检查（legacy 派生 BACKUP_SYNCING 时无条件调用 cluster_transition(BACKUP_SYNCING→BACKUP_READY, SNAPSHOT_READY)，先于 site 的 syncing=false/ready=true 写；失败 UCN_ERR_STATE fail-closed 零写不置 ready；post-commit Debug assert derive==BACKUP_READY）；site 原序幂等写+send_backup_ready 保留。SYNC_BEGIN/缺口/数据帧路径零 transition（legacy body 原样）。M01.0.2 接管优先：takeover_active 节点 legacy 派生 BACKUP_TAKEOVER（无 SYNCING→READY 边）→ 不调用 transition，legacy body 仍按 CURRENT 应用同步帧（相位保持 BACKUP_TAKEOVER）。注：membership_sequence 写（L3053）先于 SYNC_END 分支是既有顺序（非相位相关，derive 不读 seq），拒绝路径下 seq 会前移——与 d.3 的零写证明对象（相位字段 ready/assign_pending/node_id）一致。
- 测试（test-first，直驱 ucn_cluster_receive 无 tick 不录 observed pair）：新 cluster_test_backup_sync_transition 五场景——(a) SYNC_END 全覆盖→shadow==BACKUP_READY、reason==SNAPSHOT_READY、count==1、ready=true、BACKUP_READY 入队；(b) SYNC_BEGIN→保持 SYNCING 零 transition；(c) DELTA 缺口→保持 SYNCING+RESYNC_REQ 入队零 transition；(d) SYNC_END shadow 失同步（shadow 声称 READY、legacy 派生 SYNCING）→无条件调用、validate 拒绝→receive==UCN_ERR_STATE、ready 未置、syncing 未清、末 RX 镜像同步回 BACKUP_SYNCING；(e) M01.0.2 迟 SYNC_END 于接管中（READY→TAKEOVER 经真实 DIRECT 边起接管，迟 BEGIN 重入 syncing 组合后，迟 END 全覆盖→不 transition、legacy body 应用、takeover_active 保持、shadow 保持 BACKUP_TAKEOVER、count 不变）。
- 验证：先仅加测试→(d) 断言失败（无迁移时 receive 返回 OK 而非 ERR_STATE）证明测试钉住新行为；接线后 FULL 全绿（All UCN tests passed、OBSERVED-PAIRS count=35 零违例）；Golden 37 行逐字节不变（git diff tests/golden 空；blob 6ed2ada 与 HEAD 一致、LF/eol=lf，无再生成）；cluster_bytes 1096（无结构变更）；clean 全量重建 -Werror 零告警；Lxxxx 引用刷新（L2980/L3069/L3091-3092/L3008-3012/L3026-3037/L3031-3032/L3044-3045）。
- 评审：待统一双评审。已提交将推送。

### OP-206｜CLV2-01-04e 完成：Takeover 生命周期组（e.3/e.4/e.5/e.6）接线（wt/e34 点并行）

- 时间：2026-08-16。
- 范围：本点（e34）独占接管生命周期 4 个 site：e.3 start_takeover（BACKUP_READY→BACKUP_TAKEOVER, TAKEOVER_STARTED）、e.4 complete_takeover（BACKUP_TAKEOVER→HEAD_NO_BACKUP, TAKEOVER_QUORUM，人类 e-group focus 3 最高风险 site）、e.5 step 接管超时（BACKUP_TAKEOVER→DETACHED_OBSERVE, TAKEOVER_TIMEOUT）、e.6 handle_head_takeover（BACKUP_SYNCING/BACKUP_READY/BACKUP_TAKEOVER/GRACE→MEMBER_ACTIVE, TAKEOVER_STARTED）。全部无条件 transition 先行 + fail-closed + post-commit derive assert；Shadow-Guard 规则（legacy 决定迁移、validate 验 shadow、绝不 shadow 跳过）全站成立。
- e.3：transition 先于 takeover_active 写；apply_legacy(BACKUP_TAKEOVER) 只写 role+takeover_active，绝不触碰 syncing/ready（M01.0.2 组合保持可表达）；deadline/cursor/ack 全部 site-owned 原序。e.4（F1 锚点）：apply_legacy(HEAD_NO_BACKUP) 只写 role+node_id=0+ready=false；takeover_active/syncing/primary/known_backup_*/term/head/deadlines/cursors/stats 全部保留在 site 原序（idempotent 重叠无害）；失败路径零清除（d 测试镜像对称验证）；backup_generation 不动（review C G1）。e.5：transition 先于 stats++/backup_clear_sync；recovery_eligible 不置位（与 Current 一致）；失败返回 UCN_ERR_STATE 且镜像原封。e.6：old_phase 由 pre-call legacy（role+flags）派生，绝不用 shadow；四入边无条件 transition；全清除集 + epoch refresh 留在 site（F1 锚点）；plain MEMBER_ACTIVE 无自环不 transition，JOIN_PENDING/RECOVERY_HEAD 入边留 01-04b/f site（端末 RX sync 对齐）。
- 测试：cluster_test_takeover_lifecycle_wiring 覆盖 (a) start_takeover 租约过期→BACKUP_TAKEOVER/TAKEOVER_STARTED/takeover_active=true；(b) M01.0.2 迟 Type12 组合保持可表达（既有 late-sync 测试保持绿）；(c) complete_takeover 全清除集逐字段断言+generation 不动；(d) complete_takeover shadow-desync 零清除（镜像对称）；(e) 超时→DETACHED_OBSERVE/TAKEOVER_TIMEOUT/eligible 不置位+backup_clear_sync 全效果；(f) handle_head_takeover 自 BACKUP_READY/BACKUP_TAKEOVER(M01.0.2 组合)/GRACE 三入边→MEMBER_ACTIVE 全清除；(g) 每 site shadow-desync 变体 fail-closed。2 个新 hook（ucn_cluster_test_start_takeover/complete_takeover，按 shadow_transition_count 报告成败）。既有 cluster_test_takeover_self_vote 手工构造 BACKUP_READY 状态补 shadow 播种（真实 FSM 经 step 到达 READY 时 shadow 恒同步；测试直构状态必须同样播种——与既有 wiring 测试同纪律）。
- 人类 NIT：(a) cluster_transition_validate 注释替换为「migrated site 可做 caller-owned post-transition 写，仅当保持已提交的新 phase；migrated phase 变化不得依赖 shadow_sync 铸币」；(b) last-frame sweep preflight 补 preflight→commit 窗口注释「MUST NOT invoke a callback capable of mutating Cluster phase state」(M02 note)。
- 验证：Golden 37 行逐字节不变（blob 6ed2ada 一致，tests/golden diff 空）；FULL + ASan 全绿（observed 35/0）；cluster_bytes 1096；-Werror 零告警；git diff --check 干净；Lxxxx 引用刷新（start_takeover L3293/takeover=true L3315、complete_takeover L3226/role L3247/node_id L3257、handle_head_takeover L3504/role L3571/grace L3578、timeout branch L5156、backup_clear_sync L2738、set_detached L2008）；写点审计 complete_takeover 分类由「legacy (01-04e)」更新为「explicit (01-04e.4)」。
- 限制/下一步：01-04e.1/.2（handle_backup_assign、handle_backup_member_sync）与 01-04f（recovery 组 + BACKUP missed-heartbeat eligible 路径 + JOIN_PENDING/RECOVERY_HEAD→MEMBER_ACTIVE 入边）留待点并行其余 agent / 用户终审后启动。

### OP-207｜CLV2-01-04e 完成：BACKUP/TAKEOVER 组统一合并、交叉点修复与统一双评审（e1+e2+e34 点并行收口）

- 时间：2026-08-16。
- 背景：01-04e（BACKUP/TAKEOVER 组）三点并行（用户指派）已各自完成——e1 handle_backup_assign（MEMBER_ACTIVE/TAKEOVER_GRACE/JOIN_PENDING→BACKUP_SYNCING, BACKUP_ASSIGNED）、e2 handle_backup_member_sync SYNC_END（BACKUP_SYNCING→BACKUP_READY, SNAPSHOT_READY, M01.0.2 接管优先）、e34 接管生命周期（e.3 start_takeover / e.4 complete_takeover / e.5 超时 / e.6 handle_head_takeover）。各点独立 worktree 自验绿后按 83c8663（e1）→ 263170c（e2，注释引用冲突）→ c8047f3（e34，OP-doc 冲突：OP-205=e2、OP-206=e34）顺序合并。
- 合并树断言（交叉点真实缺陷）：统一门禁首次运行在 cluster_transition_validate（ucn_cluster.c:1377）触发「illegal or mismatched phase transition」assert——gdb 定位为 handle_backup_assign 调用（test_cluster.c:3547），shadow=1（DETACHED_OBSERVE）≠ old=MEMBER_ACTIVE。根因：e2 测试 cluster_test_backup_sync_transition 以 pre-e1 基座编写（当时 handle_backup_assign 无 transition），手工构造 MEMBER legacy 未播种 shadow；e1 接线后无条件 transition 的 validate 门拒绝陈旧镜像。修复 d66b0ca：按兄弟 staging 纪律（cluster_test_backup_assign_transition、既有 e.2 staging、takeover_self_vote 同款）补 shadow_phase=MEMBER_ACTIVE 播种——真实 FSM 经逐步 transition 到达时 shadow 恒同步，测试直构状态必须同样播种；生产 validate 未动，去迁移仍会红。
- 统一门禁（修复后、含 f4）：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；Golden 37 行逐字节不变（blob 6ed2ada、tests/golden diff 空）；cluster_bytes 1096；git diff --check 干净；-Werror 零告警。另修复双检出 CRLF 伪差异（core.autocrlf=true，221 文件 M 噪声收敛为真实 6 行变更）。
- 统一双评审：A（协议语义）PASS——六 site 与 3d69799 逐字段等价（e.4 全 20 写原序、backup_generation 不动 G1；e.5 recovery_eligible 不置位；e.6 old_phase 派生自 pre-call legacy 绝不用 shadow）；Shadow-Guard 全站成立；M01.0.2 组合可表达且不被清除；无 shadow_sync 铸币依赖。3 NIT（GRACE 入边 reason 记 TAKEOVER_STARTED 为 shadow 簿记、e1 fail-closed 路径已写 known_backup_* 为 Current 行为、OP 文件非 UTF-8 名与未跟踪副本的整理项）。C（一致性/测试）PASS——test-first 纪律成立（每子例断 shadow+reason+count+site 字段，去 transition 即红）；镜像一致性（takeover_active 优先派生、M01.0.2 ready&&syncing 非法而 takeover_active&&syncing 合法）；覆盖扫描唯一缺口：e.6 BACKUP_SYNCING 入边无消息级测试。C MINOR 1 已关闭：新增 (f4) 子例（619463b）——syncing Backup 被更高 Term Head 公告切换→MEMBER_ACTIVE 全清除集，52 行测试钉住该边。
- 结论：01-04e（BACKUP/TAKEOVER 组）全部 6 site 接线完成并通过统一门禁与双评审。待用户终审签字后进入 01-04f（recovery 组：declare_recovery_head/start_recovery_backoff/stepdown_recovery_head/handle_recovery_declare、RECOVERY_HEAD→STEPPING_DOWN、BACKUP missed-heartbeat eligible 路径、JOIN_PENDING/RECOVERY_HEAD→MEMBER_ACTIVE 入边）。已提交将推送。

### OP-208｜CLV2-01-04e.7 完成：BACKUP Residual-Edge Closure（人类终审 HOLD 关闭，评审方法升级为 site-scan）

- 时间：2026-08-16。
- 背景：人类终审 1cc9dc6 发 AUDIT HOLD（2 MAJOR + 2 NIT）：评审覆盖单元是"指定 6 site"，而里程碑门禁应是"所有 BACKUP/TAKEOVER Phase-changing site 无遗漏"——READY→SYNCING（fresh BEGIN/DELTA gap/seq gap）与 SYNC 失败→DETACHED 仍靠 end-of-RX cluster_shadow_sync() 补铸（d.7.1 禁止模式），且现有测试已亲手暴露（fresh BEGIN 后手工重置 shadow/count 掩盖）；backup_challenge→ELECTION、higher-Term offer→JOIN_PENDING、HEAD_STEPDOWN BACKUP→DETACHED 三个 BACKUP exit site 未迁。执行 01-04e.7 点并行（4 点独立 worktree：e71/e72/e73/e74）。
- 四点接线（全部无条件 transition 先行 + fail-closed 零 phase 写 + post-commit derive assert；Shadow-Guard 全站成立；M01.0.2 组合保持可表达、takeover 中迟 Type12 绝不为 phase 原因拒绝）：
  - e71（b5430eb，MAJOR 1）：handle_backup_member_sync 残余边——READY→SYNCING（RESYNC_STARTED）三条重入路径（fresh SYNC_BEGIN / DELTA gap / snapshot seq gap）于 site 写前提交；DETACH（(SYNCING|READY|TAKEOVER)→DETACHED_OBSERVE, PRIMARY_LOST）两失败路径（coverage 失败 END / mirror 分配 NO_SPACE）用 d.4 preflight 模式（preflight 零写→stats/send→commit→幂等 backup_clear_sync）。旧测试手工重置删除，改直接断 shadow/reason/count；TAKEOVER pre-state 用 PRIMARY_LOST（TAKEOVER_TIMEOUT 是谎言——同步流失败非超时），绝不因怕边而拒 TAKEOVER。
  - e72（a55ae55，MAJOR 2.A）：backup_challenge→ELECTION（ELECTION_STARTED），单个派生调用点覆盖 BACKUP_SYNCING/READY/TAKEOVER 三源；TAKEOVER→ELECTION 按真实可达性接（takeover-active Backup 可收同 Primary 同 cluster 同 term ADVERTISE）；void→ucn_result_t fail-closed；site 清场保留（members[]/backup_generation 存活）；新增 hook ucn_cluster_test_backup_challenge（desync 零写证明必需——RX 驱动会让 end-of-RX sync 重对齐镜像）。
  - e73（a973659，MAJOR 2.B）：consider_head_offer higher-Term BACKUP→JOIN_PENDING（JOIN_INITIATED）于任何 site 写前提交；拒绝零写（takeover 保持武装、不 join、identity 保留）；backup_clear_sync+begin_join 原序 F1。
  - e74（593c59b，MAJOR 2.C）：HEAD_STEPDOWN BACKUP 分支→DETACHED_OBSERVE（STEPDOWN_ORDERED）按 b.6 nonce-fence 纪律——transition 先、成功后消费 last_stepdown_nonce；拒绝时 fence 不动（BACKUP 保持、后续合法 STEPDOWN 仍可接受）；删除"not migrated yet (01-04e)"陈旧注释。
  - 6ed0c6b：BACKUP-side phase-defining write audit 表（人类 item 5）——backup_ready/backup_syncing/backup_takeover_active/role-leaving-BACKUP 每写点分类为 explicit transition 或 phase 不变。
- 人类 NIT 关闭：NIT-1（e.1 "nothing committed" 注释精度）1b41998——known_backup_* 在 transition 前按 Current 为每个分配接收者写入（同 c.4 LEAVE-before-transition），fail-closed 只保 phase-relevant 字段；NIT-2（e.6 TAKEOVER_STARTED 语义宽）人类与评审 A 一致同意不阻塞 M01，仅 shadow 簿记。
- 统一门禁（含全部关闭项）：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；Golden 37 行逐字节不变（blob 6ed2ada）；cluster_bytes 1096；git diff --check 干净；-Werror 零告警。
- 统一双评审（评审方法升级：site-scan，非函数名扫描）：A（协议语义）PASS——site-scan 逐写点验证（backup_ready/syncing/takeover_active/role-leaving-BACKUP 全显式或 phase 不变，审计表与写点清单一致）；语义与 1cc9dc6 等价（新边全在 DIRECT）；M01.0.2 四 site 可表达；RX shadow_sync 不再铸任何新 BACKUP 对。1 MINOR（step 侧 missed-heartbeat BACKUP_SYNCING→RECOVERY_OBSERVE 仍由 end-of-STEP mint——目的地为 Recovery 域、人类 01-04f 清单明列"BACKUP missed-heartbeat eligible 路径"，按人类门禁"所有未迁移 Phase 写应只剩 Recovery"正确顺延 01-04f，非 e.7 失败）+ 2 NIT（DIRECT 矩阵 READY→SYNCING 注释陈旧已修 4382336、Lxxxx 引用漂移 best-effort）。C（一致性/测试）PASS——test-first 验证：stepdown/challenge/takeover-interrupted 判别强（显式 reason 异于 mint fallback、hook 驱动零写证明最强）；旧掩盖全清（417 处剩余 shadow 赋值全为预 RX staging/故意 desync/文档化基线）；M01.0.2 四 site 全覆盖；delta_gap_resync staging 修复同 d.7 MAJOR 1C 类。1 MINOR（三处 e71 子 site 缺判别测试：DELTA-gap 重入/seq-gap 重入/alloc-fail detach 的 happy path 被 mint 掩盖——end-of-RX sync 复现相同 shadow/reason/count，去 transition 无测试红）——已关闭 a78adab：新增 desync 兄弟子例，断 UCN_ERR_STATE + 零 site 写，含两次 READY 重完场 staging。
- 结论：01-04e 全部残余 BACKUP/TAKEOVER Phase-changing site 显式化完成，通过统一门禁与 site-scan 双评审。按人类终审门禁，未迁移 Phase 写现仅剩 Recovery 域（01-04f 清单）。待用户终审签字后进入 01-04f。已提交将推送。

### OP-209｜CLV2-01-04f 完成：RECOVERY / STEPPING_DOWN 显式 Phase 闭环（点并行 f1-f6，全文件断言达成）

- 时间：2026-08-16。
- 背景：人类终审 2b9446a 签 01-04e PASS 并批准 01-04f，明确闭环范围——Recovery（RECOVERY_OBSERVE→RECOVERY_ELECTION / RECOVERY_ELECTION→RECOVERY_HEAD / RECOVERY_HEAD→RECOVERY_OBSERVE / RECOVERY_HEAD→STEPPING_DOWN / RECOVERY_*→JOIN_PENDING / MEMBER_ACTIVE / BACKUP_SYNCING→RECOVERY_OBSERVE / handle_recovery_declare 入边）+ STEPPING_DOWN→JOIN_PENDING 必须补入 + JOIN_PENDING/RECOVERY_HEAD→MEMBER_ACTIVE 入边。目标全文件断言：除 init/reset 与 Phase-preserving writes 外，不再有 production phase change 绕过 cluster_transition()。执行点并行（5 点独立 worktree f1-f5 + 评审 MINOR 关闭 f6）。
- 五点接线（全部无条件 transition 先行 + fail-closed 零 phase 写 + post-commit derive assert；Shadow-Guard 成立；M01.0.2 组合保持可表达）：
  - f1（d058663 + 退化守卫 9599b6e）：step 侧 Recovery 生命周期——RECOVERY_OBSERVE→RECOVERY_ELECTION（RECOVERY_BACKOFF，start_recovery_backoff 前；backoff 由调用方提供，ITEM 4 绝不自动铸币）、RECOVERY_ELECTION→RECOVERY_HEAD（RECOVERY_WIN，declare_recovery_head 前）、RECOVERY_HEAD→RECOVERY_OBSERVE（RECOVERY_TTL_EXPIRED，stepdown_recovery_head 前；eligible 保持 true）。**退化配置守卫**（点 agent 发现 + 编排者复核确认）：node_id % recovery_backoff_max_ms == 0 时 compute_recovery_backoff 得 0、ucn_deadline_from_now(now,0) 因 duration 非法返回 0，RECOVERY_ELECTION 派生要求非零 backoff——无条件 transition 会触发 derive assert/shadow-flap；守卫按 legacy 侧判别（计算 backoff==0 则不 transition），保留旧 RECOVERY_OBSERVE 自旋（site 的 nonce bump 保留），绝不读 shadow；回归子例 (f) 钉住。
  - f2（f1e5ef2）：BACKUP_SYNCING→RECOVERY_OBSERVE（PRIMARY_LOST，missed-heartbeat eligible 分支，stats++/eligible/backup_clear_sync 前）+ STEPPING_DOWN→JOIN_PENDING（STEPDOWN_COMPLETE，stepdown deadline，clear_members 前）。评审 A e.7 MINOR 关闭。
  - f3（f069f7c）：consider_head_offer RECOVERY_*→JOIN_PENDING（JOIN_INITIATED，begin_join_prepare_fields 前；RECOVERY_OBSERVE/ELECTION 由 armed backoff 派生）+ begin_ordered_stepdown RECOVERY_HEAD→STEPPING_DOWN（STEPDOWN_ORDERED）；2 个新 hook（ucn_cluster_test_consider_head_offer / begin_ordered_stepdown，count delta 报告成败，RX 驱动无法验零写必需）。
  - f4（8bebfca）：handle_recovery_declare——RECOVERY_HEAD 让位→MEMBER_ACTIVE（RECOVERY_YIELDED，stepdown_recovery_head 前）+ 普通 join pre_phase→MEMBER_ACTIVE（RECOVERY_WIN，覆盖 RECOVERY_OBSERVE/ELECTION/DETACHED_OBSERVE/BACKUP_SYNCING/READY/TAKEOVER+M01.0.2/MEMBER_TAKEOVER_GRACE；MEMBER_ACTIVE self 不 transition）；phase_committed 标志防让位路径二次 transition（stepdown 后 legacy 变 DETACHED 而 shadow 已 MEMBER_ACTIVE）。修复真实标注 bug：BACKUP join 原被 end-of-RX mint 成 TAKEOVER_STARTED，现显式 RECOVERY_WIN。
  - f5（c38ce51）：handle_head_takeover RECOVERY_HEAD→MEMBER_ACTIVE（RECOVERY_YIELDED，recovery 清场+epoch refresh 前）。
  - f6（5fb13b4，评审 A MINOR 1 关闭）：handle_head_takeover JOIN_PENDING→MEMBER_ACTIVE（TAKEOVER_STARTED）——人类 01-04f 计划明列 "JOIN_PENDING/RECOVERY_HEAD→MEMBER_ACTIVE 入边"（RECOVERY_HEAD 由 f5、JOIN_PENDING 是本 gap）；JOIN_PENDING→MEMBER_ACTIVE DIRECT 边的第二个生产 site（01-04b join-accept 是另一个），end-of-RX mint 消除；happy + desync 兄弟测试。
- 统一门禁（含 f6）：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；Golden 37 行逐字节不变（blob 6ed2ada）；cluster_bytes 1096；git diff --check 干净；-Werror 零告警。
- **全文件断言达成**（人类目标）：编排者 site-scan 复核 10 个 phase 字段全部 157 处写点，生产代码中所有 role/recovery_*/backup_*/grace 写点分类为 (A) 显式 transition/preflight 提交（apply_legacy）、(B) transition 后幂等 site 写、或 (C) phase-preserving；评审 A 独立重建写点清单后一致（仅 f6 前有 1 处字面例外——handle_head_takeover JOIN_PENDING 入边，已由 f6 关闭）。grep 确认 "not migrated yet" 标记清零；剩余 "end-of-RX sync re-aligns" 仅出现在 fail-closed 拒绝路径（拒绝后镜像重对齐到未变 phase，不铸新对）。
- 统一双评审（site-scan 方法延续）：A（协议语义）PASS——独立重建 10 字段写点清单全部 (A)/(B)/(C)；DIRECT/OBSERVED 矩阵 base-vs-HEAD 逐字节一致（f 边早已声明、现在真正实现）；语义与 c2bbf10 等价；M01.0.2 三处钉住（f2 A3a takeover-active→e.5 超时、f4 (c) BACKUP_TAKEOVER join、f5 (c) 入边）；退化守卫只读 config 不读 shadow；phase_committed 必要且无漏迁。1 MINOR（handle_head_takeover JOIN_PENDING 入边仍 mint——人类 01-04f 范围，已由 f6 关闭）+ 2 NIT（no-quorum 自写矩阵引用已补 653a183、hook count-delta 报告法正确）。C（一致性/测试）PASS——desync 兄弟判别测试全钉（mint-masking 认知明确、happy path 单独不足、判别在 desync 兄弟）；f1 退化 (f) 钉住守卫；phase_committed 覆盖（desync (f1) 证明 still-RECOVERY_HEAD 可让位后续者）；M01.0.2 三处全覆盖；新测试区 111 处 shadow/count 赋值全为预调用 staging 无掩盖。无 MAJOR/MINOR。
- 结论：01-04f 全部 Recovery/SteppingDown Phase-changing site 显式化完成，通过统一门禁与 site-scan 双评审；人类目标全文件断言（无 production phase change 绕过 cluster_transition()）达成。M01 显式 Phase 迁移主链全部闭合（01-01..01-09 中 01-04a..f）。待用户终审签字后进入 M02 模块拆分。已提交将推送。

### OP-211｜CLV2-M02 开始：02-01 Cluster 私有头 + 02-02 Codec 模块提取（纯结构性重构第一步）

- 时间：2026-08-16。
- 背景：人类终审 ab53b31 签 M01 正式闭环并授权 M02（纯结构性重构：cluster_transition() 语义、DIRECT matrix、Legacy derive、各 site before/transition/after 顺序一律不顺手优化；不得破坏 transition→site-cleanup 原子窗口；禁止动态内存；Core 不反向依赖 Extended）。M02 按顺序 OP 里程碑推进（拆分同一 6216 行文件时点并行冲突风险过高）：OP-211 codec → OP-212 fsm → OP-213 membership → OP-214 backup/takeover → OP-215 recovery/merge → OP-216 storage+CMake。
- 02-01（ucn_cluster_internal.h）：Cluster 私有头建立——声明模块布局（02-03 fsm / 02-04 membership / 02-05 backup+takeover / 02-06 recovery+merge 后续追加接口）；仅 src/extended 使用，Core 绝不 include；无动态内存。
- 02-02（ucn_cluster_codec_v3.c）：Wire v5 32B（Format v3）codec 从 ucn_cluster.c 原样移出——read/write_be、role/message_type/node_id/flags/message_is_valid、encode/decode_trailing_12b、ucn_cluster_message_encode/decode。**字节级验证**：移出前捕获 L29-350 基线 md5=41aeae9de5aa574db3559497b0c4f58e，移出后 body 与基线 diff 仅 1 处尾部空行（LF 规范化，内容逐字节一致）；codec 完全自包含（内部 helper 保持 file-static，公共两函数由 ucn_cluster.h 声明）。CMake：codec 加入 ucn_cluster 库 + ucn_tests 自身副本编译。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；cluster_bytes 1096；golden 37 行逐字节不变；-Werror 干净（diff --check 后修 LF 规范化 c208b07）；Core 依赖方向检查通过（src/core 无 cluster include）。
- 备注：最初委派的点 agent 跨多轮零进度（无构建目录、无文件修改），已中断并由编排者直接接管完成提取（机械移动 + 字节校验，风险可控）。
- 结论：02-01/02-02 完成，codec 拆分机制验证可行（字节级等价 + 全 profile 门禁）。下一步 02-03 FSM 提取。已提交将推送。

### OP-212｜CLV2-M02 02-03 完成：FSM 模块提取（transition 框架 + 7 个共享 helper 经 internal header 暴露）

- 时间：2026-08-16。
- 背景：延续 OP-211（codec 已提取）。02-03 把显式 Phase transition 框架从 ucn_cluster.c 移出为 ucn_cluster_fsm.c——legacy derive、reason 表（diff + RX hint）、shadow mirror、DIRECT/OBSERVED 矩阵、cluster_transition/validate/preflight/apply_legacy、test hooks。这是最复杂的一次提取：框架被全文件调用，且内部有 decl+def 对与 CLV2_01_04_UNUSED 宏生命周期。
- 字节级验证：移出前捕获 FSM 区域（原 L29-1210）基线 md5=3e1377c381a91c1aa36b56e51e2c8f72（重算 29-1210 精确范围后），移出后 diff 空（逐字节一致，仅 4+3 处 static 去除）。
- internal header 暴露（02-03 核心）：ucn_cluster_internal.h 新增 7 个共享 helper 声明——cluster_phase_from_legacy_state / cluster_legacy_state_is_valid / cluster_transition / cluster_transition_preflight（4 个主要入口）+ cluster_now / cluster_rx_reason_from_type / cluster_shadow_sync（3 个经编译错误发现同样被 ucn_cluster.c 调用的共享函数）。全部仅去 static、函数体不动。矩阵/reason 表/shadow_sync/apply_legacy/validate/test hooks 保持 fsm 模块内 file-static。
- CMake：fsm 加入 ucn_cluster 库 + ucn_tests 自身副本（test hooks 在 UCN_CLUSTER_ENABLE_TEST_HOOKS 下编译）。include 路径：cluster 模块内相对引用 ucn_cluster_internal.h（src/extended 未加入 include path）。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；cluster_bytes 1096；golden 逐字节不变；-Werror 干净（diff --check 零告警）；Core 不反向依赖（internal header 仅 src/extended 使用）。
- 备注：委派点 agent 再次跨多轮零进度（无构建目录/无文件修改），中断后由编排者直接接管完成（含 3 个额外共享函数的编译错误驱动发现——这是直接接管优于 agent 的关键收益）。
- 结论：02-03 完成。下一步 02-04 membership 提取。已提交将推送。

### OP-213｜CLV2-M02 02-04 完成：Membership 模块提取（member 表 + Join/Keepalive/Leave handlers）

- 时间：2026-08-16。
- 背景：延续 OP-211/212（codec + FSM 已提取）。02-04 把 member 表与 Join/Keepalive/Leave 处理器从 ucn_cluster.c 移出为 ucn_cluster_membership.c——13 个函数：member_count_u16、available_capacity、find_member、allocate_member、remove_member、clear_members、handle_join_request、send_join_reply、handle_join_accept、handle_keepalive、expire_members、send_join_request、send_keepalive。
- 依赖分析（python 脚本逐函数 brace 匹配）：membership handlers 与 backup 域交错（handle_join_request 调 assign_backup/queue_backup_assignment_for_member；remove_member/expire_members 调 backup_resync；handle_join_accept 调 cluster_transition）。因此 02-04 提取为"handler 组移出 + 共享 core 留在 ucn_cluster.c 经 internal header 暴露"的模式。
- internal header 暴露：13 个 membership 入口（de-static only）+ 6 个 ucn_cluster.c helper（send_cluster_message / send_message / next_nonce / backup_resync / assign_backup / queue_backup_assignment_for_member）。send_message 末参为 uint16_t capacity（初稿误写 uint8_t，编译后修正签名）。fsm 侧已暴露的 cluster_transition / cluster_transition_preflight / cluster_phase_from_legacy_state 供 membership 调用。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；cluster_bytes 1096；golden 逐字节不变；-Werror 干净（diff --check 零告警，含 LF 规范化 71c1c05）；Core 不反向依赖。
- 备注：委派点 agent 第三次跨多轮零进度（无构建目录/无文件修改），中断后编排者直接接管完成（机械移动 + 逐函数依赖分析 + 编译错误驱动修正）。
- 结论：02-04 完成。下一步 02-05 backup/takeover 提取（send_join_reply 等仍引用的 backup 域函数将随之移出，届时从 internal header 移走暴露、收口跨模块面）。已提交将推送。

### OP-214｜CLV2-M02 02-05 完成：Backup/Takeover 模块提取（backup 生命周期 + takeover prepare/ACK/complete）

- 时间：2026-08-16。
- 背景：延续 OP-211..213（codec/FSM/membership 已提取）。02-05 把 Backup 与 Takeover 生命周期从 ucn_cluster.c 移出为 ucn_cluster_backup.c——30 个函数：backup_allocate_mirror、backup_covers_all_members、backup_clear_sync、send_backup_assign、assign_backup、handle_backup_assign、send_backup_ready、handle_backup_ready、handle_backup_member_sync、handle_primary_heartbeat、send_backup_resync_req、send_backup_reject、handle_backup_resync_req、handle_backup_reject、complete_takeover、start_takeover、send_takeover_prepare_step、send_takeover_announce_step、handle_takeover_prepare、handle_takeover_ack、handle_head_takeover、backup_control_spacing_ms、start_backup_assignment_cycle、queue_backup_assignment_for_member、send_backup_assignment_step、send_backup_heartbeat、send_backup_delta_step、backup_sync_spacing_ms、send_backup_snapshot_step、backup_resync。
- 边界（编排者复核）：区域 1 = L1043-2214（backup_allocate_mirror 至 handle_head_takeover 结尾 return UCN_OK; }——首次提取曾误切 handle_head_takeover 于 L2189 中部，重置后按真实边界 L2214 重提）；区域 2 = L2992-3625（backup_control_spacing_ms 至 backup_resync 结尾，send_next_advertisement 前）。**关键修正**：首次删除脚本按正序删两区域导致行号漂移删错内容（区域 2 命中 recovery 段），重置 ucn_cluster.c 后按**逆序**删除成功。
- internal header 暴露：backup/takeover 入口（de-static only）+ find_candidate / consider_head_offer（编译错误驱动发现 assign_backup/backup 代码调用）；set_detached / find_peer / backup_clear_sync 与剩余 ucn_cluster.c 共享。旧 static 前向声明清理（start_backup_assignment_cycle/send_backup_assign/resync_req/reject 已移出），补 send_takeover_prepare_step/announce_step 本地声明。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；cluster_bytes 1096；golden 逐字节不变；-Werror 干净（diff --check 零告警）；Core 不反向依赖。M01.0.2 takeover_active && backup_syncing 语义未动。
- 备注：委派点 agent 第四次零进度后编排者直接接管；backup 提取是最大单次（1744 行），经历边界误切与删除顺序 bug 两次修正，最终字节级等价 + 全 profile 门禁。
- 结论：02-05 完成。下一步 02-06 recovery/merge 提取（剩余 ucn_cluster.c 含 recovery 域 + head offer/stepdown/score switch + receive dispatch + step + public facade）。已提交将推送。

### OP-215｜CLV2-M02 02-06 完成：Recovery + Merge/Head-offer 模块提取（剩余域收口）

- 时间：2026-08-16。
- 背景：延续 OP-211..214。02-06 分两步完成：(1) ucn_cluster_recovery.c——compute_recovery_backoff、recovery_quorum_met、send_recovery_declare、start_recovery_backoff、declare_recovery_head、stepdown_recovery_head、handle_recovery_declare、handle_recovery_ack（L1046-1360，315 行）；(2) ucn_cluster_merge.c——find_candidate、allocate_candidate、observe_candidate、candidate_better、score_improves_by、send_head_stepdown、begin_ordered_stepdown、backup_challenge、consider_head_offer（541 行）。全部逐字节移出（de-static only）。
- internal header 暴露：recovery 8 入口 + merge 入口（observe_candidate/begin_ordered_stepdown/backup_challenge/allocate_candidate/candidate_better/send_head_stepdown）+ 核心 helper（cluster_transmit、begin_join_prepare_fields、begin_join）。merge 模块内序与前文件不同，补 2 个模块内前向声明（score_improves_by/send_head_stepdown）——仅声明、函数体未动。allocate_candidate 签名核对（ucn_node_id_t, ucn_node_id_t, uint32_t）。
- 拆分效果：ucn_cluster.c 从 6216 行降至 **1667 行**；6 个模块文件 + internal header：codec/fsm/membership/backup/recovery/merge。剩余 ucn_cluster.c = public facade（init/set_head_score/sync_neighbors/receive/receive_inner/step/step_inner/get_view/get_member_summary_at）+ 共享 infra（config/token_bucket/transmit/send_message/next_nonce）+ 选举（start/complete_election）+ test hooks。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；cluster_bytes 1096；golden 逐字节不变；-Werror 干净（diff --check 零告警）；Core 不反向依赖。
- 备注：委派点 agent 第五次零进度后编排者直接接管；merge 提取经历函数内序重排（前向声明）与 allocate_candidate 签名核对两次编译修正。
- 结论：02-06 完成。下一步 02-07（storage header 分离）+ 02-08（CMake/profile 验证）收口 M02。已提交将推送。

### OP-216｜CLV2-M02 02-07/02-08 收口：Storage 契约文档化 + 按需 Extended 静态库验证（M02 完成）

- 时间：2026-08-16。
- 02-07（storage header 分离）范围决策：任务表字面为"将可变内部存储布局从主 public API 分离，应用禁止直接读写内部字段"。经评估，**字面实现（opaque ucn_cluster_t + storage header）是 API 契约变更而非纯结构重构**——会破坏 test fixture 的 57 处直接字段访问 + 全部 6 个模块对 ucn_cluster_t 字段的读写，且违背 M02"禁止顺手优化/禁止语义变化"的硬约束。M02-consistent 完成方式：ucn_cluster_internal.h 头部已明确编码"Cluster internal fields 不是 public API；应用通过 ucn_cluster.h 类型/API + view 函数访问"契约（Extended-only include、Core 禁止引用）。opaque 化本身留待 M02 之后的 API 收口阶段（需要应用层适配，超出纯结构范围）。
- 02-08（CMake/profile 验证）完成：ucn_cluster / ucn_cluster_federation / ucn_transfer 均 EXCLUDE_FROM_ALL（按需链接，唯一非测试消费者 ucn_cluster_sim 显式 add_executable）；Cluster-OFF 全量构建通过（ucn_tests 编译无 cluster 源，证明 Core-only 产品不拉取 Cluster 模块）；NANO 全绿且 cluster_bytes 1096 不变（无 per-profile 增肥）；FULL/ASan/LITE/NANO 全绿；golden 逐字节不变；observed 35/0；-Werror 干净。
- **M02 最终结构**：ucn_cluster.c 6216 → 1667 行（public facade + receive dispatch + step + election + shared infra + test hooks）；6 个模块 + internal header：codec（338）/fsm（1207）/membership（427）/backup（1834）/recovery（337）/merge（569）+ internal.h（249）。全部字节级等价（函数体逐字未动，仅 de-static + 前向声明）。
- 门禁（M02 全量）：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；cluster_bytes 1096；golden 37 行逐字节不变（blob 6ed2ada）；-Werror 干净；Core 不反向依赖 Extended（src/core 无 cluster include）；禁止动态内存遵守（无 malloc/free 引入）。
- 结论：CLV2-M02 模块拆分完成（02-01..02-08 收口）。人类约束全部遵守：cluster_transition() 语义、DIRECT matrix、Legacy derive、各 site before/transition/after 顺序零改动；transition→site-cleanup 原子窗口未插入任何 callback/provider/async/early-return；Golden/Wire/资源结果一致。待用户终审签字后进入 M03（Epoch 分类 + Authority Offer + 安全 Serial Helper）。已提交将推送。

### OP-217｜CLV2-M02.1 Scope-Reconciliation Closure（人类终审 fa642df：代码 PASS + 纯文档对齐）

- 时间：2026-08-16。
- 终审：人类按 M02"纯结构性重构"门禁审 fa642df——production semantic regression 0、Wire/Golden regression 0、Core→Extended 反向依赖 none、动态内存 none、transition/site atomic-window regression none；**代码实现 PASS**；02-07 opaque 化延期 APPROVED。唯一 HOLD：Roadmap 文字与实际完成范围不一致（1 documentation closure），要求 M02.1 纯文档对齐（不碰 .c/.h/CMake，无需重跑 FULL/ASan）。
- 对齐项（Roadmap 任务表 + 本记录）：(1) **M02 audit range** 更正为 `ab53b31...fa642df` 完整 21 commits（此前 13a25ee..fa642df 为 16，因 13a25ee 已是 OP-211 收尾）；(2) **02-03** 改为实际架构——FSM owns transition/derive/invariants/matrix，Step/RX orchestration（step_inner/receive_inner/election dispatch）保留 facade，domain module 保留自己的 transition site（比原"Phase 写只存在 FSM"更合理）；(3) **02-05** Backup+Takeover 合并为单模块 ucn_cluster_backup.c（已批准，耦合紧密）；(4) **02-07** internal storage contract DONE（ucn_cluster_internal.h 仅 Extended 使用、public read-only view 为应用边界），真正 opaque/storage split **DEFER → CLV2-14-02**；(5) **02-08** whole-Cluster EXCLUDE_FROM_ALL DONE（Core-only 零成本，libucn_cluster.a absent 验证），per-module Feature trim **deferred**（等安全可裁剪边界）；(6) **M02 milestone 行** 改为"02-01..02-06 + approved scope adjustments 02-07/08"，不再宣称字面 02-01..08 全完成。
- 验证：仅 docs diff——`git diff --check` 干净；`git diff --name-only` 仅 docs/（任务表 + 01-项目操作记录）。未跑 FULL/ASan（无代码变更）。
- 结论：M02.1 Scope-Reconciliation 完成。人类终审要求全部落实，正式签 M02 CLOSED；权限进入 M03（Epoch 分类 + Authority Offer + 安全 Serial Helper）。已提交将推送。

### OP-218｜CLV2-M03 开始：03-01 Epoch 值类型 + 纯函数 Comparator（纯基础设施，M03 第一批唯一项）

- 时间：2026-08-16。
- 背景：人类终审 6b5f382 签 M02 CLOSED 并批准 M03，明确建议"第一批只做 CLV2-03-01，完成后停下来审 comparator，再进入 active_epoch 和 Authority/Merge 语义修改"。03-01 定义为纯基础设施：新增 ucn_cluster_epoch.[ch]，**不改变任何生产判断逻辑**（Current business code 照常工作，Golden 必须完全不变）。
- 实现：include/ucn/ucn_cluster_epoch.h（public，值类型）+ src/extended/cluster/ucn_cluster_epoch.c。
  - `ucn_cluster_epoch_t { cluster_id, term, head_node_id }`（cluster_id/term 为 uint32_t，head 为 ucn_node_id_t）；
  - `ucn_cluster_epoch_relation_t`：UNKNOWN/SAME/LOWER/HIGHER/CONFLICT/FOREIGN；
  - `ucn_cluster_epoch_compare()` 纯函数 + `is_same_cluster()`/`is_foreign()` 便捷谓词。
- **关系语义（人类终审冻结）**：same cluster → Term 可比较（LOWER/HIGHER）；same cluster + same term → head 相同 SAME、head 不同 CONFLICT；foreign cluster → Term **永不**比较 → FOREIGN。**比较域截断先行**：`cluster_id` 不同即返回 FOREIGN——绝不先比 term 再看 cluster_id（Cluster A term 2 不得因 Cluster B term 100 让位；这正是 03-03 语义修正的数学基础，comparator 层先钉死）。
- 测试（test-first，tests/test_cluster.c 新增 cluster_test_epoch_comparator + property_sanity）：(a) SAME；(b) LOWER/HIGHER 反对称；(c) CONFLICT 对称；(d) **关键 FOREIGN 截断**——同簇 term 2 vs 异簇 term 100 必须 FOREIGN 非 HIGHER、对称性、同 term 数异簇仍 FOREIGN；(e) NULL 安全；(f) term 0 边界（LOWER）。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；**Golden 逐字节不变（git diff tests/golden 空——纯基础设施未触生产路径）**；cluster_bytes 1096；-Werror 干净；diff --check 零告警。
- 结论：03-01 完成。按人类指令**在此停止审计**——先确认 "Epoch relation 的数学定义正确"，再让 comparator 参与 03-02 active_epoch 收拢与 03-03 Authority/Merge 语义修改。已提交将推送。

### OP-297｜CLV2-06-02 代码完成：Primary Member Table 自审

- 时间：2026-08-22。
- 修改：新增固定 `ucn_cluster_member_table_t` 和 table validity/count API；`ucn_cluster_t.members[]` 收敛为 `primary_members.slots[]`。Membership helper 统一改名为 `primary_member_*`，所有 Head/Recovery Runtime、Backup committed mirror、Merge、Facade、Federation 和测试访问点均使用显式 primary table 路径；没有增加 staging/voter table。
- 自审修正：发现初版模型测试固定访问 `slots[3]`/`slots[7]`，对裁剪成员表存在越界风险；已改为 `slots[0]` 与独立 invalid-table，并在 `UCN_CLUSTER_MAX_MEMBERS=6` 的用户配置构建中复验通过。
- 验证：Windows Full/Lite/Nano CTest 各 `4/4`、Service OFF `27/27`、用户配置 `5/5`、配置契约 `7/7`；WSL ASan/UBSan `4/4`、`-fanalyzer -Wall -Wextra -Werror` `4/4`；64-node clean simulation 仍在 `8920 ms` 收敛，Host `object_bytes=1392`。
- 隔离/资源：旧 `cluster->members[]` 生产访问为零；production Cluster 无 v4 引用、Adapter 无 Cluster 引用；status/voting 未进入控制判定。相对 06-01 的 Host `ucn_cluster_t` 增量为 `+0 B`。
- 结论/限制：`CLV2-06-02` 进入 **CODE COMPLETE / AUDIT HOLD（待外部审计）**，自审报告为 `UCN_V5_Cluster_M06_02_成员表封装自审报告_2026-08-22.md`。M05 顶层 HOLD 不解除，不开始 06-03；不得启用 production v4 RX/TX/FSM、Voter quorum、Authority、资格选择或实机互通声明。

### OP-296｜CLV2-06-02 启动：Primary Member Table 结构封装

- 时间：2026-08-22。
- 前置：`CLV2-06-01` 已获受限外审 GO；M05 顶层仍为 `AUDIT HOLD`。本项只处理成员表的固定内存结构与命名边界，不启动任何 v4 控制面。
- 现场问题：`ucn_cluster_t.members[]` 目前同时表示 Head Runtime 成员与 Backup 同步 mirror，数组名称没有标注所属表或后续 staging 边界，容易在 M07/M09 扩展时混用。
- 计划：新增 `UCN_V5_Cluster_M06_02_成员表封装实施计划_2026-08-22.md`，以 `ucn_cluster_member_table_t primary_members` 取代裸数组；Head/Recovery Head 将其视作 Runtime table，Backup 将其视作 committed mirror。所有生产访问改为显式 slot 路径，现有 v3 控制面行为保持不变。
- 限制：不增加 staging/voter table，不读取 status/voting 作权限、容量、选路、Backup 候选或 Takeover 决策；不接入 production v4 RX/TX/FSM、Authority、Adapter 或 encoder。代码完成后先进行完整自审，仍需等待独立外审。

### OP-295｜CLV2-06-01 外部复审 GO：成员状态值模型受限签字

- 时间：2026-08-22。
- 外部复审：成员状态、4×4 转换表、record 合法域和 legacy bridge 均获 **GO（受限范围）**，未发现 P0/P1/P2；复审确认新字段未被现有权限、选路、容量、Backup 候选或 Takeover 逻辑读取。
- 独立证据：Full `30/30`、Lite `30/30`、Nano `20/20`、Service OFF `27/27`、WSL ASan/UBSan `20/20`、`-fanalyzer` `7/7` 均通过；64-node clean simulation 为 `converged_ms=8920`，Host `object_bytes=1392`。
- 结论/限制：`CLV2-06-01` 更新为 **DONE / 外部复审 GO（受限范围）**；M06 仍继续进行后续子任务，M05 顶层继续 `AUDIT HOLD`。本签字不授权 production v4 Join/RX/TX/FSM、Voter quorum、Authority、资格选择或实机互通声明。

### OP-294｜CLV2-06-01 代码完成：成员状态值模型自审

- 时间：2026-08-22。
- 修改：新增 `include/ucn/ucn_cluster_membership.h`，定义固定 `NONE/PROVISIONAL/COMMITTED/REMOVING` 状态、紧凑 member record 和 record/transition 校验 API；`ucn_cluster.h` 复用该公共值类型。现有 Head Join、Backup mirror snapshot/delta、Recovery ACK 的创建路径统一通过内部 legacy bridge 写入 `COMMITTED + voting=true + wire=v3 + capabilities=0` 以及 joined/keepalive 时间；非法 `0`/broadcast Node ID fail-closed 且保持 slot 不写回。
- 测试：新增独立 `ucn_cluster_membership_model_tests`，覆盖全部 4×4 状态转换、非法 record、legacy 初始化/keepalive 与非法 identity 无写回；既有 Cluster 三节点 Join→Backup failover→Recovery 回归增加真实 metadata 断言。实际新构建后 Windows Full `30/30`、Lite `30/30`、Nano `20/20`、Service OFF `27/27`；WSL ASan/UBSan `20/20`、`-fanalyzer -Wall -Wextra -Werror` `7/7` 均通过。64-node clean simulation 仍收敛（8920 ms）。
- 资源/隔离：Host x64 Debug `ucn_cluster_t` 从 M04/R23 `1200 B` 到 `1392 B`（`+192 B`，默认 16 槽每槽 +12 B）；无动态分配。自审扫描确认 production `ucn_cluster.c` 无 v4 引用、Adapter 无 Cluster 引用；`git diff --check` 无空白错误（仅既有 CRLF 提示）。
- 结论/限制：`CLV2-06-01` 仅进入 **CODE COMPLETE / AUDIT HOLD（待外部审计）**。M05 顶层 HOLD 不解除；本项未让 status/voting 参与生产资格/Authority，未接入 v4 RX/TX/FSM，也不构成实机验证或协议整体放行。

### OP-293｜CLV2-06-01 启动：成员状态值模型与 legacy-v3 元数据桥

- 时间：2026-08-22。
- 前置判断：05-01..12 已完成各自受限外审，满足 M06-01 所需的 frozen codec/contract 基础；但 M05 顶层仍为 **AUDIT HOLD**。因此本项只建立不产生网络权限的成员数据模型，不能启动 production v4 RX/TX/FSM、Authority、Join 准入或实机兼容性宣称。
- 现场问题：当前 `ucn_cluster_member_t` 只有 `occupied/node_id/lease/nonce`，所有 v3 Join、Backup mirror、Recovery mirror 在语义上混为“已可参与当前控制面”的成员；尚不能表达 provisional、removing、wire version、capability 或 voting metadata。
- 计划：新增公共值模型 header，固定 `NONE/PROVISIONAL/COMMITTED/REMOVING` 状态、record 合法域和显式转换表；扩展 Member record；在既有 v3 Join、Backup mirror、Recovery mirror 的**已存在**写入点标记 legacy `COMMITTED + voting + wire v3 + capability=0`，记录 joined/keepalive 时间。当前成员循环/Backup/Takeover 行为不读取这些新字段。
- 验证边界：新增模型单测覆盖状态/非法 record/转换/legacy 初始化；现有 Cluster 回归验证 v3 行为未变。完成后仅作自审和外审准备，M06 仍不允许 v4 production 接线。

### OP-292｜CLV2-05-12 外部复审 GO：Host 双格式兼容门禁受限签字

- 时间：2026-08-22。
- 外审结论：05-12 的 P1 已完整关闭，签字 **DONE / 外部复审 GO（受限范围）**。默认关闭 encoder 现在对完整 40 B 哨兵执行 `memcmp`，外审确认失败返回时 output 完全不写回；Host 双格式门禁继续覆盖 `v3 → v4` 交替、32/40 B 版本/长度混搭与 31/39/41 B 拒绝，并完整验证 dispatch output 不变。
- 外部复核：`UCN_CLUSTER_WIRE_V4_ENCODER_ENABLED=1` 仅存在于两个独立测试 target，未泄漏到 production `ucn_cluster`；production Cluster 与 Adapter 仍未发现 v4 API 接线。外审实际复跑 Windows Full `3/3`、WSL ASan/UBSan `26/26` 通过；`git diff --check` 通过（仅既有 CRLF 提示）。
- 状态：任务表已将 05-12 标为 DONE，05-01..12 均获各自**受限**签字。
- 限制：M05 顶层仍为 **AUDIT HOLD**；本签字不授权 production v4 RX/TX/FSM、Authority、资格或任何真实设备兼容性/完成宣称。

### OP-291｜CLV2-05-12 外审 P1 整改完成：默认 encoder 完整 output 无写回

- 时间：2026-08-22。
- 修改：`test_cluster_wire_v4_encoder_closed()` 在填充 40 B `0xA5` 哨兵后保存完整 `expected_output`，调用默认关闭的 encoder 后用 `memcmp(output, expected_output, sizeof(output))` 精确断言全部 40 B 未改变；删除原本只检查首尾的不足断言。
- 定向核对：静态检查确认完整 `memcmp` gate 存在；production `ucn_cluster.c` 的 v4 refs=0、generic Adapter 的 Cluster refs=0、新 Host test 的 runtime init/step/receive/send calls=0，encoder public default=0；`git diff --check` 通过（仅既有 CRLF 提示）。
- 验证：Windows GCC Full `29/29`、Lite `29/29`、Nano `19/19`；WSL ASan/UBSan `19/19`；WSL `-fanalyzer -Wall -Wextra -Werror` `6/6`。所有构建均实际重编 `test_cluster_wire_v4_encoder_closed.c` 后执行。
- 结论/限制：P1 的输出完整性覆盖已整改，05-12 回到 **CODE COMPLETE / AUDIT HOLD（外审 P1 已整改，待复审）**。未改 encoder、production Cluster RX/TX/FSM、Adapter、资格或 Authority；M05 顶层继续 **AUDIT HOLD**，等待外部复审。

### OP-290｜CLV2-05-12 外审 P1 整改启动：默认 encoder 输出完整性

- 时间：2026-08-22。
- 外审核实：05-12 的 Host 双格式 dispatcher、测试宏隔离、长度/版本错配的完整 dispatch output 无写回均正确；但默认 `test_cluster_wire_v4_encoder_closed()` 只检查 40 B output 的首、尾字节，却在自审报告中表述为“不写 output”。中间字节被意外改写时会漏检，P1 成立。
- 整改：任务回退为 **REMEDIATION IN PROGRESS / AUDIT HOLD**；新增 `UCN_V5_Cluster_M05_12_外审P1整改计划_2026-08-22.md`，将默认 encoder 关闭回归改为保存完整哨兵副本并用精确 `memcmp` 断言 40 B 全部不变。只改测试/台账，禁止修改 encoder、production Cluster RX/TX/FSM、Adapter、资格或 Authority。
- 后续：完成后重跑受影响构建矩阵与生产隔离扫描；再次自审后仍只能等待外部复审，M05 顶层继续 **AUDIT HOLD**。

### OP-289｜CLV2-05-12 代码完成：Host 双格式兼容门禁自审

- 时间：2026-08-22。
- 实现：新增独立 `ucn_cluster_wire_v4_host_dual_stack_tests`。测试用固定合法 32 B/v3 ADVERTISE 与本 target 局部开启 encoder 后生成的合法 40 B/v4 ADVERTISE，按 `v3 → v4 → v3 → v4` 连续调用 `ucn_cluster_wire_decode()`；每次逐字段断言归属格式和 body，固定 dispatcher 无 sticky format 状态。`32/v4`、`40/v3`、版本改写和 `31/39/41 B` 一律拒绝，并逐字节断言 dispatch output 无写回。
- 构建隔离：Host target 只直接编译两个 codec、链接 `ucn_core`，没有链接或调用 `ucn_cluster` runtime/FSM；默认 `ucn_tests` 的 `test_cluster_wire_v4_encoder_closed()` 继续证明生产编译中 encoder 返回 `UCN_ERR_CONFIG` 且不写 output。未改 RFC4、v3 production Wire、`ucn_cluster_t`、runtime View/Stats、Adapter、资格或 Authority。
- 自审验证：Windows GCC CTest Full `29/29`、Lite `29/29`、Nano `19/19`；WSL ASan/UBSan `19/19`；WSL `-fanalyzer -Wall -Wextra -Werror` `6/6`。静态复扫：production `src/extended/ucn_cluster.c` 的 v4 refs=0、generic Adapter 的 Cluster refs=0、新 Host test 的 runtime init/step/receive/send calls=0、public encoder default=0；`git diff --check` 通过（仅既有 CRLF 提示），新增源码/文档尾随空白=0。
- 结论/限制：`CLV2-05-12` 更新为 **CODE COMPLETE / AUDIT HOLD（待外部审计）**，详细证据见 `UCN_V5_Cluster_M05_12_兼容门禁自审报告_2026-08-22.md`。该自审没有实现 RX Owner、mixed-version 准入、Head/Backup/Voter 资格、Authority、生产 v4 TX 或实机测试；M05 顶层保持 **AUDIT HOLD**。

### OP-288｜CLV2-05-12 启动：Host 双格式兼容门禁与生产隔离复核

- 时间：2026-08-22。
- 授权/前置：05-11 已获受限外部 GO；M05 顶层仍为 **AUDIT HOLD**。05-12 只验证固定 Wire v3/v4 codec 的 Host 双格式共存与构建隔离，不能将该验证解释为真实 RX Owner、Member/资格、Authority 或硬件互通。
- 现场核对：默认 `ucn_tests` 已有 encoder-closed 回归，验证正常库中的 v4 encode 返回 `UCN_ERR_CONFIG` 且不写 output；现有 `ucn_cluster_wire_v4_codec_tests` 是定向 codec target，但未单独命名、覆盖“v3/v4 交替交付且无 sticky format”的兼容门禁。
- 计划：新增实施计划与 Host-only `ucn_cluster_wire_v4_host_dual_stack_tests`。该 target 自行编译 v3/v4 codec、仅在本 target 开启 test encoder，交替严格 dispatch 32 B/v3 与 40 B/v4；长度/版本混搭、短长帧必须拒绝且 caller output 不写回。它只链接 `ucn_core`，不链接或调用 `ucn_cluster` runtime/FSM。
- 边界：不修改 RFC4、v3 codec/生产消息、`ucn_cluster_t`、public runtime View/Stats、Cluster RX/TX/FSM、Adapter、资格或 Authority；不启用 production encoder。完成代码后先自审和跑构建矩阵，再等待外部审计。

### OP-287｜CLV2-05-11 外部复审 GO：隔离 Versioned Diagnostics 受限签字

- 时间：2026-08-22。
- 外审结论：05-11 获 **DONE / 外部复审 GO（受限范围）**。诊断仍严格复用 05-08：v3 伪造 offer 或携带 required v4 bit 仍返回 `UCN_ERR_ARGUMENT`，不会被诊断层降格；普通兼容性拒绝由 `view.compatibility=UCN_ERR_STATE` 和确定性 reason 表达，非法 input 不写 view。
- 统计/测试：stats 只经显式 record 改变且全部饱和；定向覆盖 Strict/legacy、v4 缺/非法/能力不足 offer、无写回、全部 reason 分桶与饱和。外审独立复跑 Windows Full `2/2`、WSL ASan/UBSan `25/25`，`git diff --check` 通过（仅既有 CRLF 提示）。
- 边界：production Cluster/FSM、generic Adapter 均没有 v4 或 diagnostic 引用，v4 encoder 仍默认关闭。`COMPATIBLE_*` 不是 Member/Voter/Backup/Head/Authority 授权。
- 状态：M05 顶层继续 **AUDIT HOLD**；05-12 为下一受限项，尚未开始、不得提前接入 production v4 RX/TX/FSM。

### OP-286｜CLV2-05-11 代码完成：隔离 Versioned Diagnostics 自审

- 时间：2026-08-22。
- 实现：在 private v4 semantic/codec 层新增 caller-owned input、diagnostic view、reason 和 saturating stats。view 记录 peer Node ID、检测 format、offer、policy、请求类和 required bits；reason 解释 Strict v4 拒绝 v3、legacy 仅 non-voting、v4 缺/非法 offer、v4 capability 不足。`UCN_OK` 仅表示 wire precondition，绝不授权真实 Member/Voter/Backup/Head。
- 合同修正：自审先发现 v3+required v4 bit 被错误降格为普通拒绝；已回到 05-08 冻结语义：该组合仍为 `UCN_ERR_ARGUMENT`、view 不写回，调用方最多记录 `INPUT_INVALID`。首次编译还发现 C 标识符换行拆分，已修复后重新构建；旧二进制 CTest 结果未作为本轮证据。
- 验证：Windows GCC Full `28/28`（显式重编 `ucn_cluster` + focused codec）；WSL ASan/UBSan `18/18`；WSL `-fanalyzer -Wall -Wextra -Werror` `5/5`。测试覆盖 v3 Strict/legacy、v4 missing/invalid/insufficient/sufficient offer、所有 stats reason 分桶、饱和计数和 invalid input 不写回。
- 隔离：`ucn_cluster_t`、public runtime View/Stats、production `ucn_cluster.c`、generic Adapter、v3 32 B 和 default-disabled v4 encoder 均未改；静态扫描生产 Cluster v4 refs=none、Adapter Cluster refs=none；`git diff --check` 通过（仅既有 CRLF 提示）。
- 状态：`CLV2-05-11` 为 **CODE COMPLETE / AUDIT HOLD（待独立审计）**，详细自审见 `UCN_V5_Cluster_M05_11_版本诊断自审报告_2026-08-22.md`。不得进入 05-12 或解除 M05 hold，等待外部复审。

### OP-285｜CLV2-05-11 启动：隔离 Versioned Diagnostics

- 时间：2026-08-22。
- 授权/边界：05-10 已获受限外审 GO，M05 仍为 **AUDIT HOLD**。05-11 只建立可解释 mixed-version compatibility precondition 的 caller-owned 诊断对象；不得接入 production v4 RX/TX/FSM、Cluster Member/资格/Authority 或 v4 encoder。
- 现场核对：`ucn_cluster_view_t` / `ucn_cluster_stats_t` 当前只投影 v3 runtime；仓库没有 v4 RX Owner。把未接收的 peer 版本/能力直接写入 `ucn_cluster_t` 会制造错误的生产完成假象，因此先建立独立 diagnostic view/reason/stats 合同，未来 Owner 完成自身 admission 后再调用。
- 计划：新增 `UCN_V5_Cluster_M05_11_版本诊断实施计划_2026-08-22.md`，固定 v3 Strict/legacy、v4 offer 和 required-bit 的 reason 优先级；复用 05-08 的单一 compatibility helper，不复制策略。任务表更新为 **IN PROGRESS / AUDIT HOLD**。

### OP-284｜CLV2-05-10 外部复审 GO：RFC4 全字段 Parser 与 Fuzz 受限签字

- 时间：2026-08-22。
- 外审结论：撤销初版 P1。独立 RFC4 fixture 已逐 Type 固定 Role、flags 与 P0..P5；所有定义 Role、全部 256 个 flags、每个字段确定性反例均经 raw decode、strict dispatch、semantic parser 验证，失败 output 不写回。Type 12 marker 零尾、同簇 `HANDOVER_READY` 的 Backup 分支及 seed `0xC15A4E5D` 的 4096 次 fuzz 均获确认。
- 外部验证：Windows Full 2/2、WSL ASan/UBSan 25/25 通过；`git diff --check` 无空白错误（仅既有 CRLF 提示）。生产 Cluster/FSM、generic Adapter 仍无 v4 引用，encoder 默认关闭。
- 状态：`CLV2-05-10` 标记 **DONE / 外部复审 GO（受限范围）**。M05 整体继续 **AUDIT HOLD**，不授权 production v4 RX/TX/FSM、Authority 或实机完成声明；下一受限项为 05-11 Versioned diagnostics。

### OP-283｜CLV2-05-10 外审 P1 整改完成：独立 RFC4 全字段合同矩阵

- 时间：2026-08-22。
- 实现：在 `tests/test_cluster_wire_v4_codec.c` 新增 `v4_rfc4_fixture_t`，每一行固定 Type、允许 sender Role、flags contract 与 P0..P5 字段类别。测试遍历所有现有 Role 枚举、所有 256 个 flag 值，并对每个受 codec 约束的字段分别生成 zero/serial 外域/broadcast/duration/score/offer/reason/count/descriptor 等负例；每项均断言 raw decode、strict dispatch、semantic parser 三层拒绝且 caller output 不写回。Type 12 marker P3..P5 和 Type 27 同簇 Backup READY 均有专门模式回归。
- 边界核实：P5 CRC/bitmap raw carrier 的 quorum 语义依旧留给 M10；selected offer 是否在双方交集内需要 peer offer context，raw codec 只验证其 structural bits，未错误收紧。生产 Cluster/FSM、Adapter、v3 和 encoder 默认值没有修改。
- 验证：Windows GCC Full 28/28；WSL ASan/UBSan 18/18；WSL `-fanalyzer -Wall -Wextra -Werror` 5/5，全部通过。
- 结论：05-10 更新为 `CODE COMPLETE / AUDIT HOLD（外审 P1 已整改，待复审）`。M05 总体继续 **AUDIT HOLD**，等待外部复审后才允许进入下一项。

### OP-282｜CLV2-05-10 外审 P1 核实：全量字段覆盖不足，回退整改

- 时间：2026-08-22。
- 核实：外审指出原矩阵对每 Type 只破坏一个 `required_nonzero_word`、至多一个 zero-tail，并只用 `DISABLED` 测 Role；该判断成立。随机 fuzz 不应替代确定性的 RFC4 字段覆盖，因此原 `CODE COMPLETE` 自审结论回退。
- 整改：新增 `UCN_V5_Cluster_M05_10_外审P1整改计划_2026-08-22.md`。下一轮测试使用独立 RFC4 fixture table 记录每 Type 的 Role 集、flags 合同及 P0..P5 字段规则；每条规则分别构造 raw/dispatch/semantic 三层无写回反例，Type 12/27 的模式分支单列。
- 边界：不修改 parser 宽严语义、RFC4、v3、Core Wire、pending 或任何 production Cluster RX/TX/FSM；M05 保持 **AUDIT HOLD**。

### OP-281｜CLV2-05-10 代码完成：Type 1..33 负向矩阵与固定 Seed Codec Fuzz 自审

- 时间：2026-08-22。
- 实现：`tests/test_cluster_wire_v4_codec.c` 新增独立 raw-frame fixture，不借测试专用 v4 encoder 产生负向输入。Type `1..33` 每类均从合法 raw 基线开始，分别注入非法 Role、组合 flags、保留 flag、Cluster/Head 的 zero 与 broadcast、Term zero/超 serial 上界、必填 word zero 及适用的 non-zero zero-tail；Type 12 另覆盖 BEGIN marker 零尾。未知 version、未知 Type 亦严格拒绝。
- Fuzz：固定 seed `0xC15A4E5D` 执行 4096 次 bounded mutation，轮转全部 Type、随机原始 40 B、1..3 处篡改及 39/41 B 长度。decode/strict dispatch 成功时必须得到仍合法的 v4 raw + semantic；失败时 raw、dispatch、semantic caller output 都以 sentinel + `memcmp` 断言逐字节不变。
- 自审：Windows GCC Full 28/28；定向 codec 通过；WSL ASan/UBSan 18/18；WSL `-fanalyzer -Wall -Wextra -Werror` 5/5。`src/extended/ucn_cluster.c` 的 v4 refs=none，generic Adapter Cluster refs=none，encoder 生产默认仍为 0；`git diff --check` 通过（仅既有 CRLF 提示）。
- 结论/限制：05-10 标为 `CODE COMPLETE / AUDIT HOLD（待独立审计）`。本项没有调用 pending helper、没有 Cluster runtime state，也没有接入 production RX/TX/FSM、资格、Authority 或 v4 发送；M05 整体继续 **AUDIT HOLD**。

### OP-280｜CLV2-05-10 启动：隔离 v4 Codec 负向 Parser 与 Bounded Fuzz

- 时间：2026-08-22。
- 授权：05-09 已获外部复审 **GO（受限范围）**，现在进入 05-10。新建 `UCN_V5_Cluster_M05_10_负向Parser与Fuzz实施计划_2026-08-22.md`；本项只增加 deterministic negative matrix 和 fixed-seed bounded mutation fuzz，证明 Type 1..33 的 codec 失败无副作用。
- 边界：不修改 RFC4、v3 production codec、Core Wire、pending admission、Cluster RX/TX/FSM、资格、Authority 或 default-disabled v4 encoder。M05 继续 **AUDIT HOLD**。

### OP-279｜CLV2-05-09 外部复审 GO：消息长度与 Carrier 容量受限签字

- 时间：2026-08-22。
- 外部复审确认：Stream、CAN-FD、Classic CAN 对完整 40 B 序列精确 `memcmp`；CAN-FD 明确验证 57→64 B 的 7 B zero padding，篡改后 endpoint 不交付且 `fd_padding_errors` 增加。生产 v3 仍严格 32 B，v4 40 B 仅为承载容量常量，encoder 默认关闭，production Cluster/FSM 与 generic Adapter 均无 v4 接线。
- 外部复跑：Windows Full 2/2、WSL ASan/UBSan 25/25 通过；`git diff --check` 无空白错误，仅既有 CRLF 提示。
- 状态：`CLV2-05-09` 标为 **DONE / 外部复审 GO（受限范围）**；不授权 v4 生产 RX/TX/FSM、实机传输宣称或 M05 整体放行。未提交或推送。

### OP-278｜CLV2-05-09 外审 P1 整改完成：完整 payload 与 CAN-FD padding 逐字节门禁

- 时间：2026-08-22。
- 整改：Stream/CAN 的接收 fixture 现在保存 `UCN_CLUSTER_WIRE_MAX_MESSAGE_BYTES` 上界内的完整 payload 副本和捕获标记。40 B Stream、CAN-FD、Classic CAN 成功路径均对全部 40 B 做 `memcmp`，不再只看第 0 字节。
- CAN-FD 专项：40 B payload + 17 B W0 header 固定为 57 B；测试遍历 57..63 共 7 个 padding 字节并断言全零。随后重新编码同长度 frame、将最后一个 padding 篡改为 `0x7E`，验证 source 只增加 `fd_padding_errors`，endpoint 交付次数保持不变。
- 自审：Full 28/28、Lite 28/28、Nano 18/18、MSVC Debug Full 15/15、配置契约 28/28、WSL ASan/UBSan 18/18、`-fanalyzer -Werror` 5/5 通过；`git diff --check` 通过（仅既有 CRLF 提示）。生产 `ucn_cluster.c` 仍无 v4 API，encoder 默认 0。
- 状态：05-09 重新标为 `CODE COMPLETE / AUDIT HOLD（外审 P1 已整改，待复审）`；M05 总体继续 **AUDIT HOLD**。未提交或推送。

### OP-277｜CLV2-05-09 外审 P1：完整 payload 与 CAN-FD padding 覆盖整改启动

- 时间：2026-08-22。
- 外审结论确认：05-09 的长度合同、v3 生产 32 B 边界和 v4 production isolation 均成立，但 Stream、CAN-FD、Classic CAN 的 40 B 正向回归仅检查 payload 长度和第 0 字节，不足以证明 40 B 内容完整性；CAN-FD 未逐字验证 57→64 的 7 B zero padding，也未在本任务中覆盖非零 padding 的拒绝。
- 整改任务：新增 `UCN_V5_Cluster_M05_09_外审P1整改计划_2026-08-22.md`；将两类接收 fixture 扩为完整有界 payload 捕获，三条 Carrier 都改用精确 `memcmp`；CAN-FD 增加零尾遍历和篡改零尾后的无 endpoint 交付断言。
- 边界：只改测试夹具和测试台账，不修改 v3 Cluster codec、Cluster endpoint/生产 RX/TX/FSM、v4 encoder 或 Adapter 生产实现。05-09 回退为 `REMEDIATION IN PROGRESS / AUDIT HOLD`，M05 总体仍为 **AUDIT HOLD**。

### OP-276｜CLV2-05-09 代码完成：v3/v4 消息长度合同与 Carrier 容量自审

- 时间：2026-08-22。
- 实现：`UCN_CLUSTER_WIRE_V3_MESSAGE_BYTES=32 B` 成为 current production wire 的明确所有者，原 `UCN_CLUSTER_MESSAGE_BYTES` 保持严格同值 alias；v4 header 新增仅供 dual-version transport sizing/测试使用的 `UCN_CLUSTER_WIRE_MAX_MESSAGE_BYTES=40 B`，并设 alias/max 编译期合同。没有把 v3 decoder 改为 40 B，也没有改变 Core Wire、Node/Adapter 生产接口或 Cluster endpoint。
- Carrier 证据：新增真实普通 UCN Frame 回归（不调用 Cluster v4 API）：Stream 经 COBS/source/Adapter Queue/endpoint 完整交付 40 B；CAN-FD 将 57 B W0 Frame 填充到 64 B DLC 后完整交付；Classic CAN 将同一 57 B Frame 分成固定 12 段并重组完整交付。较长 UCN header 超出 CAN-FD direct 64 B 的情形没有被掩盖，既有 `UCN_ERR_TOO_LARGE` 仍是 fail-closed 行为。
- 隔离自审：`src/extended/ucn_cluster.c` 无 `ucn_cluster_wire_v4` 引用；generic Adapter 目录无 Cluster 头/常量依赖；`UCN_CLUSTER_WIRE_V4_ENCODER_ENABLED` 仍默认 `0`；未修改 Cluster FSM、资格、quorum、Authority、Config、生产 RX/TX 或 `ucn_cluster_t`。
- 验证：Windows GCC Full 28/28、Lite 28/28、Nano 18/18；MSVC Debug Full 15/15（仅既有 C4819 编码警告）；配置契约 28/28；WSL ASan/UBSan 18/18；WSL `-fanalyzer -Wall -Wextra -Werror` 5/5；`git diff --check` 通过（仅既有 CRLF 提示）。
- 文档：新增 `UCN_V5_Cluster_M05_09_消息长度迁移自审报告_2026-08-22.md`，任务表更新为 `CODE COMPLETE / AUDIT HOLD（待独立审计）`。M05 整体继续 **AUDIT HOLD**；未提交或推送。

### OP-275｜CLV2-05-09 启动：消息长度合同与 Carrier 容量迁移

- 时间：2026-08-22。
- 前置状态：05-01..05-08 均已获各自受限外审 GO，M05 仍为 **AUDIT HOLD**。本项只准备 v4 未来 40 B Cluster control payload 的底层承载，不能开启 v4 生产收发。
- 现场核对：Current production Cluster 的 `UCN_CLUSTER_MESSAGE_BYTES` 是 32 B v3 精确长度；Stream/CAN Source、Adapter Queue 使用通用 `UCN_MAX_FRAME_BYTES`，没有将 Cluster 控制面硬编码为 32。最短 W0 UCN header 为 17 B，故 40 B payload 编码为 57 B：可装入 CAN-FD 64 B DLC（7 B canonical zero padding），Classic CAN 需 12 个 Carrier segment，Stream 可直接 COBS 承载。
- 计划：新增 `UCN_V5_Cluster_M05_09_消息长度迁移实施计划_2026-08-22.md`，先分离 v3 exact / v4 exact / dual-version max 常量，再补 Stream、CAN-FD、Classic CAN 的真实 source/queue/endpoint 回归，并确认过长 CAN-FD 仍 fail-closed。
- 明确非目标：不改 RFC4、Core Wire W0-W3、Cluster FSM/endpoint、Node/Adapter 生产行为、v4 encoder 或 public eligibility；generic Adapter 不引入 Cluster 头依赖。完成后仍须独立审计，M05 保持 AUDIT HOLD。

### OP-274｜CLV2-05-08 外部复审 GO：Mixed-version policy 受限签字

- 时间：2026-08-22。
- 外部复审结论：`CLV2-05-08` 获 **DONE / 外部复审 GO（受限范围）**。Strict v4 下，v3 的 non-voting、Voter、Backup、Head 均拒绝；explicit legacy 只放行 `v3 + non-voting + required_bits=0`，v3 携带 offer 或任一 v4 capability 均参数拒绝。v4 只验证合法 offer 和调用方显式所需的 capability，不产生真实资格或 Authority。
- 生产边界复核：`src/extended/ucn_cluster.c` 仍无 v4 API 调用；v4 encoder 仍默认关闭。该策略没有接入 Member、Config、quorum、Head/Backup、RX/TX/FSM 或 Adapter。
- 外部验证：Windows Full CTest `2/2`、WSL ASan/UBSan CTest `25/25` 通过；`git diff --check` 无空白错误，仅既有 CRLF 提示。
- 台账：任务表将 05-08 标为外部 GO，M05 顶层状态同步为 `05-01..08 已完成受限签字`；自审报告补记外审结论。
- 限制：该签字不放行 M05 整体。后续仍不得接入 production RX/TX/FSM、真实 eligibility 或 v4 发送；M05 保持 **AUDIT HOLD**。未提交或推送。

### OP-273｜CLV2-05-08 代码完成：私有 mixed-version precondition 自审

- 时间：2026-08-22。
- 实现：在 private semantic header/isolated codec 新增 `mixed_version_policy`、`peer_contract_class` 和纯函数 `ucn_cluster_wire_v4_mixed_version_peer_is_compatible()`。Strict v4 对所有 v3 class 拒绝；explicit legacy 只允许 `v3 + NON_VOTING_MEMBER + required_bits=0`；v3 携带 v4 offer 或 required bit 作为参数错误拒绝。v4 只接受合法 RFC4 offer，且仅检查调用方明确提供的 required bit 子集。
- 安全边界：该函数无 output/state、无 I/O、无时钟、无 Cluster object 访问；`UCN_OK` 仅代表 mixed-version wire 前置条件，不代表真实 Member/Voter/Backup/Head/Authority。未把 capability bit 硬编码为角色授权，M06/M08/M10/M11/M13 继续拥有 member/config/quorum/epoch/persistence 的最终决策权。
- 测试：focused codec PASS；Windows GCC Full/Lite `28/28`、Nano `18/18`、MSVC Debug Full `15/15`；WSL ASan/UBSan `18/18`、`-fanalyzer -Wall -Wextra -Werror` `5/5` 全通过。`git diff --check` 无空白错误（仅既有 CRLF 提示）；`src/extended/ucn_cluster.c` 无 v4 引用，encoder 默认仍为 `0`。
- 文档：新增实施计划和自审报告；任务表更新为 `CODE COMPLETE / AUDIT HOLD（待独立审计）`。未修改 RFC4、public API、CMake、v3 production path、v4 production encoder 或生产 RX/TX/FSM；未创建提交或推送。
- 结论：本项可送独立审计，但 M05 整体继续 **AUDIT HOLD**；在签字前不得启动 M06 或开启 v4 生产发送。

### OP-272｜CLV2-05-08 启动：私有 Mixed-version policy 合同与测试矩阵

- 时间：2026-08-22。
- 前置状态：05-01..05-07 均已获得各自的受限外部 GO；但 M05 仍为 **AUDIT HOLD**，不得接入 production RX/TX/FSM、Head/Backup eligibility 或 v4 发送。
- 授权范围：开始 `CLV2-05-08`，先建立 `UCN_V5_Cluster_M05_08_混合版本策略实施计划_2026-08-22.md`。仅实现 private、无状态的 wire-version/capability 前置条件：Strict v4 拒绝一切 v3 请求；explicit legacy 仅允许 v3 的 non-voting legacy class；v4 的 required capability 由未来 owner 显式传入。
- 明确非目标：不修改 RFC4、public API、v3 生产消息或默认关闭的 v4 encoder；不读取/修改 Member、Config、Epoch、quorum 或 Authority 状态；不将 helper 接入真实 Join、Voter、Backup、Head 或任一生产 RX/TX/FSM 路径。M06/M08/M10/M13 才拥有这些权限。
- 后续步骤：完成 private helper 和拒绝/降级矩阵后，先做本地完整自审与构建矩阵；届时只可标记 `CODE COMPLETE / AUDIT HOLD（待独立审计）`，不能自行放行 M05。

### OP-271｜CLV2-05-06 外部复审 GO：冻结字段顺序 P1 关闭

- 时间：2026-08-22。
- 外部复审结论：05-06 的字段顺序 P1 已有效关闭。fixture 独立于 `make_valid_frame()`，Type 20..33 的每个可写 P 字段取值可区分且满足 raw gate；`raw → semantic` 逐项检查具名字段，`semantic → raw` 精确检查 P0..P5，parser/builder 同时交换字段也不能绕过。
- 状态同步：`CLV2-05-06` 标记为 **DONE / 外部复审 GO（受限范围）**；05-07 既有受限 GO 保持有效。M05 仍为 **AUDIT HOLD**，不得据此接入 production RX/TX/FSM、Head/Backup eligibility 或 v4 发送。
- 外部验证：报告记录 Windows Full CTest `2/2`、WSL ASan/UBSan CTest `25/25` 通过；`git diff --check` 无空白错误，仅 CRLF 提示。
- 文档：任务表与 05-06 整改计划/自审报告均已回写外审签字。无代码修改、无提交或推送。

### OP-270｜CLV2-05-06 外审 P1 整改完成：独立 Type 20..33 字段顺序冻结

- 时间：2026-08-22。
- 修改：将原 `test_extended_type_registry_contract()` 的低区分 `make_valid_frame()` 样本替换为独立 Type 20..33 fixture table。每条 fixture 指定 type/role/flag/payload length/zero-tail 和合法、可区分的 P0..P5；测试不再从被测 helper 生成 raw 输入。
- 新门禁：新增 `extended_type_semantic_fields_match()`，对每个 Type 分别断言 `semantic_from_frame()` 后的具名 payload 字段；再将同一 semantic message builder 成 raw frame，逐项断言 Common Header 和精确 P0..P5。故 parser 或 builder 任一侧字段对调都会失败；两侧同时对调仍会在具名字段断言失败。RFC 强制 zero-tail 保持零，并保留其非零拒绝/失败无写回测试。
- 复核：05-07 外审 `GO（受限范围）` 已同步入任务表；05-06 仍为 P1 整改后的 `CODE COMPLETE / AUDIT HOLD（待独立复审）`，M05 继续 AUDIT HOLD。
- 验证：Windows GCC Full/Lite `28/28`、Nano `18/18`、MSVC Debug Full `15/15`；WSL ASan/UBSan `18/18`、WSL `-fanalyzer -Wall -Wextra -Werror` `5/5` 全通过。`git diff --check` 无空白错误（仅既有 CRLF 提示）。未创建提交或推送。
- 文档：新增 `UCN_V5_Cluster_M05_06_字段顺序外审P1自审报告_2026-08-22.md`；05-06 计划与任务表同步。未改 RFC4、codec 生产逻辑、public API、v4 encoder 或生产 RX/TX/FSM。

### OP-269｜CLV2-05-06 外审 P1：冻结字段顺序整改启动

- 时间：2026-08-22。
- 外审结论：05-07 获受限 GO；05-06 原有“不可变 registry”测试未逐字段固定 RFC4 Type 20..33 的 P0..P5 映射。它只固定 ID/长度/role/flag/zero-tail，再做 raw→semantic→raw 往返；构造样本重复 `1`，无法检测 parser 与 builder 同时交换字段。
- 整改范围：只重写 focused `test_extended_type_registry_contract()`：采用独立的、每字段可区分且合法的 fixture table；逐项断言 raw→semantic 命名字段与 semantic→raw P0..P5。禁止修改 RFC4、codec 业务实现、public API、encoder、生产 RX/TX/FSM。
- 台账：新增 `UCN_V5_Cluster_M05_06_字段顺序外审P1整改计划_2026-08-22.md`；任务表 05-06 改为 `REMEDIATION IN PROGRESS / AUDIT HOLD`。M05 整体继续 AUDIT HOLD，未提交或推送。

### OP-268｜CLV2-05-07 代码完成：Capability / Wire Offer 私有语义自审

- 时间：2026-08-22。
- 实现：在 private semantic header 定义六个 RFC4 capability bit、`WireOffer` 与 `SelectedWireOffer` 固定值对象；codec 新增严格 raw-word 转换、required-capability 与 common-intersection pure helper。所有转换复用既有 RFC4 validator，失败不写 caller output；合法双方 offer 选择最高共同 format 与共同 capability 集。
- 自审修正：复读 RFC4 发现 `HEAD_DECLARE.P3` 同样是 `wire_offer` owner，初始计划遗漏但实现 generic helper 不受影响；已补进定向回归、计划和任务表。另确认合法 v4 offer 必须都包含 format 4，故“合法 range 无交集”不可构造；非法 range 由 validator fail-closed。
- 测试：codec 定向 PASS；Windows GCC Full/Lite `28/28`、Nano `18/18`、MSVC Debug Full `15/15`；WSL ASan/UBSan `18/18`、WSL `-fanalyzer -Wall -Wextra -Werror` `5/5` 全通过。`git diff --check` 无空白错误（仅既有 CRLF 提示）；production 复扫无 helper 调用，v4 encoder 仍 default-disabled。
- 文档：新增 `UCN_V5_Cluster_M05_07_Capability语义自审报告_2026-08-22.md`；任务表将 05-07 标为 `CODE COMPLETE / AUDIT HOLD（待独立审计）`，并明确 05-06、05-07 都没有获得外审签字。
- 结论/限制：可送 05-07 独立审计；M05 总体继续 **AUDIT HOLD**。未接入 Head/Backup eligibility、生产 RX/TX/FSM、Adapter、v4 发送、Authority、quorum 或 mixed-version policy；未创建提交或推送。

### OP-267｜CLV2-05-07 启动：Capability / Wire Offer 私有语义实施计划

- 时间：2026-08-22。
- 前置状态：05-01..05-05 已获受限外审 GO；05-06 的 Type registry/parser 自审已完成、仍待独立审计。用户指示“继续”，授权本项以完全隔离方式与 05-06 外审并行；05-06 不被标记 GO，M05 整体继续 **AUDIT HOLD**。
- 授权范围：只在 private codec 把冻结 RFC4 `wire_offer` / `selected_wire_offer` 的 word 解析、写出、required-capability 和 common-intersection 收敛为 typed pure helper。ADVERTISE.P3、JOIN_REQUEST.P1、HEAD_DECLARE.P3、JOIN_ACCEPT.P4 的现有字节不变；六个 capability bit 与全部保留位严格按 RFC4。
- 明确非目标：不接入 Head/Backup 选择、Join、Advertise、production RX/TX/FSM、Adapter 或 v4 encoder；不把 capabilities 当成当前节点的 Authority 或 voter 准入。M06/M08 之后才拥有真实 eligibility/FSM 接线权。
- 计划：新增 `UCN_V5_Cluster_M05_07_Capability语义实施计划_2026-08-22.md`，先完成 private type/word 合同，再做纯交集与负向回归；每个子项后自审，最终仍送独立审计。未创建提交或推送。

### OP-266｜CLV2-05-06 代码完成：冻结 Type 注册表 / Parser 一致性自审

- 时间：2026-08-21。
- 核对：逐项复核 RFC4、public enum、private named semantic payload、`semantic_from_frame()`、`semantic_to_frame()`、payload-size table 和 raw structural gate。Type 20..25 Config、26..29 Handover/Withdraw、30..32 Rekey、33 Certificate 的 numeric ID、P0..P5、role、flag、zero-tail 与既有 private helper 一致；没有发现需要修改冻结 Wire 或 codec 业务逻辑的问题。
- 修改：只在 `test_cluster_wire_v4_codec.c` 新增 `test_extended_type_registry_contract()`。它固定 Type 20..33 精确编号、每个 payload word 数、Config Commit/Abort 与 Head Withdraw 的 P5=0、Config Member/Certificate 合法 flag、Config/Rekey ACK 双角色，以及同 Cluster READY 必为 Backup 的规则。semantic parser 对非法 tail 的失败无写回也被断言。
- 验证：定向 codec PASS；Windows GCC Full/Lite `28/28`、Nano `18/18`、MSVC Debug Full `15/15`；WSL ASan/UBSan `18/18`、WSL `-fanalyzer -Werror` `5/5` 全通过。`git diff --check` 无空白错误（仅既有 CRLF 提示）；production `src/extended/ucn_cluster.c` 无 v4 引用，encoder 仍 default-disabled。
- 文档：新增 `UCN_V5_Cluster_M05_06_Type注册表自审报告_2026-08-21.md`，计划和任务表同步为 `CODE COMPLETE / AUDIT HOLD（待独立审计）`。
- 结论/限制：05-06 可送外审；M05 继续 **AUDIT HOLD**。没有新增 Wire 语义、public API、production RX/TX/FSM、Authority 或提交/推送，未开始 05-07。

### OP-265｜CLV2-05-05 外部复审 GO：Type 8/33 私有语义范围签署通过

- 时间：2026-08-21。
- 外部复审结论：`CLV2-05-05` 获 **GO（受限范围）**，无 P0/P1/P2。复审确认 Type 8/33 private 对象完整携带 proposed Epoch 与 certificate key，两个对象均有 `<= 40 B` 上限；admission relation 严格绑定 outer source、Epoch、generation、snapshot、txid、Stable anchor 与 Joint `C_old/C_new`。
- 边界复核：CRC、VoteId、voter order、bitmap 上界和 quorum 仍明确归 M10；未出现生产 RX/TX/FSM 调用，v4 encoder 仍 default-disabled。外部独立 GCC Full CTest `28/28`、`git diff --check` 均通过（仅既有 CRLF 提示）。
- 状态：任务表将 05-05 标记 **DONE / 外部复审 GO（受限范围）**；M05 仍为 **AUDIT HOLD**。授权仅进入受限 05-06：核对冻结的 Type 20..33 enum、parser/semantic builder table 与 RFC4 一致性，不新设 Wire 语义、不接生产路径、不赋予 Takeover Authority。新增 `UCN_V5_Cluster_M05_06_Type注册表核对计划_2026-08-21.md`，先完成 enum/parser/gate 审计与不可变回归计划，再决定最小代码变更。
- 提交：本条无新增业务代码、无提交或推送。

### OP-264｜CLV2-05-05 代码完成：Takeover / Certificate 私有语义自审

- 时间：2026-08-21。
- 实现：在 private `ucn_cluster_wire_v4_semantic.h` 新增 `ucn_cluster_wire_v4_takeover_t`、`ucn_cluster_wire_v4_takeover_fragment_t`、OLD/NEW set enum 与 `<= 40 B` 编译期上限；在隔离 codec 实现 Type 8/33 各自的双向 builder。Type 8 总是写完整 proposed Epoch、generation、snapshot、anchor Config、txid、required set 和 CRC carrier；Type 33 总是写同一 Epoch/key、fragment Config、set、index/count 和 bitmap。所有 raw 写出仍复用既有 semantic/raw structural gate。
- 关联约束：新增 private `fragment_matches_admission()`。它先以临时 raw frame 验证两个私有对象的 RFC4 合法域，再要求 outer source=proposed Head、完整 Epoch/generation/snapshot/txid 相同；Stable 固定 anchor=`C_old`/OLD set，Joint 固定 anchor=`C_new` 且 OLD/NEW 分别匹配 admission 的 `C_old/C_new`。该 helper 不保存 state、不创建 pending、不计算 CRC/quorum/Authority。
- 测试：覆盖 Type 8/33 complete round-trip；Stable 双片必须两片齐全；Joint OLD/NEW 双 set 只在两边到齐后 complete；伪造 term/generation/snapshot/txid/Config/set、descriptor 越界、invalid set、缺 term、错 Type 和 output 不写回。伪造 raw fragment 进入 pending cache 时保持已有 slot/mask 不变。
- 验证：定向 codec PASS；Windows GCC Full/Lite `28/28`、Nano `18/18`、MSVC Debug Full `15/15`；WSL ASan/UBSan `18/18`、WSL `-fanalyzer -Werror` `5/5` 全通过。`git diff --check` 无空白错误（仅既有 CRLF 提示）；私有 API 仅被 codec 与 focused test 引用，`src/extended/ucn_cluster.c` 无 v4 调用，encoder 仍 default-disabled。
- 文档：新增 `UCN_V5_Cluster_M05_05_Takeover语义自审报告_2026-08-21.md`，实施计划与任务表更新为 `CODE COMPLETE / AUDIT HOLD（待独立审计）`。
- 结论/限制：05-05 可提交独立审计；M05 整体继续 **AUDIT HOLD**。未接入生产 RX/TX/FSM、真实 RX owner、CRC/voter-order/quorum/Authority，未开始 05-06，未创建提交或推送。

### OP-263｜CLV2-05-05 启动：v4 Takeover / Certificate 私有语义实施计划

- 时间：2026-08-21。
- 外部复审：`CLV2-05-04` 已签署 **GO（受限范围）**。确认完整 Snapshot 绑定、marker/member structural gate、失败无写回和生产隔离均通过；独立 GCC Full CTest `28/28` 通过。
- 授权范围：进入受限 `CLV2-05-05`，只在 private codec 内为冻结 RFC4 的 Type 8 `HEAD_TAKEOVER` 和 Type 33 `TAKEOVER_CERTIFICATE` 建立完整 Takeover/CertificateFragment 对象、双向 builder 与无状态关联检查。对象必须完整携带 proposed Epoch、generation、snapshot、Config identity、txid、required set、CRC carrier、descriptor 与 bitmap，并借已有 admission context 表达 Stable/Joint 的 `C_old/C_new` 关系。
- 明确非目标：不改 RFC 字节或 public pending cache 合同；不做 CRC、voter order、VoteId、quorum、persistence、Authority 或任何生产 RX/TX/FSM 接线；v4 encoder 继续 default-disabled。M10 才拥有完整证书验证与 Authority 接受。
- 计划：先建私有固定大小对象与 builder，再建 Stable/Joint 关联检查，最后覆盖伪造 key/Config/set、缺片/descriptor 越界、output/slot 无副作用并完成矩阵和生产路径复扫。新增 `docs/07-Cluster簇/02-里程碑/M05/UCN_V5_Cluster_M05_05_Takeover语义实施计划_2026-08-21.md`；任务表将 05-04 记为外审 GO、05-05 记为 `IN PROGRESS / AUDIT HOLD`。
- 结论/限制：本条只是启动记录，尚未完成代码或测试；M05 整体继续 **AUDIT HOLD**，未创建提交或推送。

### OP-262｜CLV2-05-04 代码完成：完整 Snapshot 语义 helper 自审

- 时间：2026-08-21。
- 实现：在 private `ucn_cluster_wire_v4_semantic.h` 新增 `ucn_cluster_wire_v4_snapshot_t`（完整 Epoch、backup generation、snapshot ID、membership sequence、kind、成员三元组）及 `snapshot_from_frame()` / `snapshot_to_frame()`；实现位于隔离 `ucn_cluster_codec_v4.c`，经过既有 semantic builder 和 raw structural validator。写出固定 Type 12 + HEAD role；从非 Type 12 或非法 raw 读取、非法 kind/marker/member tail/serial/duration/不完整 Epoch 写出均 fail-closed 且 output 不写回。
- 资源/边界：Snapshot 是 private transient object，有 `<= 40 B` 编译期上限，不在 public include path、不嵌入 `ucn_cluster_t`、不动态分配。它不保存 snapshot、不推进 sequence、不发送消息；`src/extended/ucn_cluster.c` 无 Snapshot API 调用，v4 encoder 仍 default-disabled。
- 测试：新增完整字段 round-trip、合法 `UINT32_MAX-1` ID/nonce、serial threshold、最大安全 lease、BEGIN/END/DELTA、marker 非零 tail、缺失 cluster/term/head、超域 serial/duration、invalid kind、错 Type 及失败无写回。最终 Windows GCC Full/Lite 为 28/28、Nano 18/18、MSVC Debug Full 15/15；WSL ASan/UBSan 18/18、`-fanalyzer -Werror` 5/5；`git diff --check` 无空白错误（仅既有 CRLF 提示）。
- 文档：新增 `UCN_V5_Cluster_M05_04_Snapshot语义实施计划_2026-08-21.md` 与 `UCN_V5_Cluster_M05_04_Snapshot语义自审报告_2026-08-21.md`；任务表同步 05-03 外审 GO、05-04 为 `CODE COMPLETE / AUDIT HOLD（待独立审计）`。
- 结论/限制：可提交 05-04 独立审计；M05 整体继续 **AUDIT HOLD**。不接入生产 RX/TX/FSM，不开始 05-05+，未创建提交或推送。

### OP-261｜CLV2-05-04 启动：v4 Snapshot 语义与完整绑定

- 时间：2026-08-21。
- 外部复审：`CLV2-05-03` 已签署 **GO（受限范围）**。确认 Type `1..33` 的语义字段、清零 builder、私有固定资源及生产隔离均无 P0/P1/P2；独立 GCC Full CTest `28/28` 通过。
- 授权范围：进入受限 `CLV2-05-04`，仅在 private codec 内把 RFC4 Type 12 `BACKUP_MEMBER_SYNC` 收敛成完整 Snapshot 对象。必须同时携带 Common Header Epoch、generation、snapshot ID、sequence、成员三元组和 marker/delta kind；不改 RFC 字节、不接入生产 RX/TX/FSM、不启用 v4 发送。
- 计划：先建立 `Snapshot ↔ semantic/raw` 无副作用 helper，再覆盖合法 `UINT32_MAX-1` ID/nonce、serial threshold、最大安全 lease 以及错误 marker/超域 serial/duration/错 Type 的失败无写回；完成后做构建矩阵和生产路径复扫，仍待独立审计。
- 文档：新增 `docs/07-Cluster簇/02-里程碑/M05/UCN_V5_Cluster_M05_04_Snapshot语义实施计划_2026-08-21.md`，任务表同步将 05-03 记为外审 GO、05-04 记为 `IN PROGRESS / AUDIT HOLD`。未创建提交或推送。

### OP-260｜CLV2-05-03 代码完成：private 语义 Payload / Type-specific Builder 自审

- 时间：2026-08-21。
- 实现：新增 private `src/extended/cluster/ucn_cluster_wire_v4_semantic.h`，定义 Common Header + Type `1..33` 的 named payload union；`ucn_cluster_wire_v4_semantic_from_frame()` 和 `ucn_cluster_wire_v4_semantic_to_frame()` 在 codec 内显式完成 raw frame/semantic message 转换。builder 先清零临时 raw frame，只写 active Type 字段，最终复用既有 structural validator；没有 `memcpy words[6]` 或 generic payload fallback。
- 边界/资源：semantic header 不在 public include path；CMake 只给专用 codec test 加 private include directory。payload union 有编译期 `<= 24 B` 上限，不嵌入 `ucn_cluster_t`、不动态分配。public v4 dispatch 仍输出 raw frame，生产 encoder 仍 default-disabled。
- 测试：Type `1..33` raw→semantic→raw；9 条 frozen RFC vector 同样回环；存在的 inactive union tail 写入 `0xA5` 不影响 raw；Type12 marker 零尾保持；非法 raw/semantic output 不写回。Windows GCC Full/Lite 各 28/28、Nano 18/18、MSVC Debug Full 15/15、WSL ASan/UBSan 18/18、WSL `-fanalyzer -Werror` 5/5 通过。
- 复扫：semantic API 只由 codec 和 focused test 使用；`src/extended/ucn_cluster.c` 没有调用。更新 public header 注释与 M05-02 报告，避免仍将 05-03 typed builder 描述为未实现。
- 结论/限制：`CLV2-05-03` 为 **CODE COMPLETE / AUDIT HOLD（待独立审计）**；M05 继续 AUDIT HOLD。未接入生产 RX/TX/FSM、真实 RX owner、quorum/CRC、Capability/Adapter 或任何后续里程碑；未创建提交或推送。

### OP-259｜CLV2-05-03 启动：语义 Payload / Type-specific Builder 实施计划

- 时间：2026-08-21。
- 授权与范围：05-02 已获外部 GO，开始受限的 `CLV2-05-03`。本项只在隔离 v4 codec 内处理 raw `words[0..5]` 的字段所有权，不改变 RFC4/v3/生产 FSM，也不启用发送。
- 计划：新增 private tagged semantic message（Header + Type-specific payload union），为 Type `1..33` 建立 raw→semantic 与 semantic→zeroed raw 的显式 builder；后者最终复用既有 raw structural validator。测试将覆盖全 Type 回环、inactive storage 污染不影响 Wire、invalid payload fail-closed 与生产调用点复扫。
- 文档：新增 `docs/07-Cluster簇/02-里程碑/M05/UCN_V5_Cluster_M05_03_语义Payload与Builder实施计划_2026-08-21.md`；任务表将 `CLV2-05-03` 标为 `IN_PROGRESS`。
- 结论/限制：本条只是启动记录，尚未完成代码或测试；M05 整体继续 **AUDIT HOLD**。

### OP-258｜CLV2-05-02 外部复审 GO：隔离 Codec 范围签署通过

- 时间：2026-08-21。
- 外部复审：R06-B P1 已签署闭环。复审确认 Type33 的 admission、pending key、frozen Config-set 核验均在 lazy expiry 前完成；三种 `now == deadline_ms` 拒绝路径保持 slot/deadline/mask 不变，只有显式 timer API 才释放 slot；RFC4、公共接口和实现一致。
- 验证证据：外部独立执行 Windows GCC Full CTest `28/28` 通过；`git diff --check` 无空白错误，仅既有 CRLF 提示。
- 状态：`CLV2-05-02` 标记为 **DONE / 外部复审 GO（受限范围）**，R06/R06-B 均为外部 GO。该签字只允许继续受限的 05-03；生产 RX/TX/FSM、真实 RX owner、quorum/CRC/FSM、M07/M10/M11/M13、实机和掉电验证仍未获授权，M05 整体保持 **AUDIT HOLD**。本项无新增代码、无提交或推送。

### OP-257｜CLV2-05-02 外审 P1 R06-B 整改：deadline 边界不得由未准入分片驱逐 slot

- 时间：2026-08-21。
- 核实：外审结论成立。`pending_accept_fragment()` 原先在 source/frozen-Config/key 校验前执行 lazy `pending_expire()`；因此在 `now_ms == deadline_ms`，结构合法但未准入的 Type33 能清空唯一 pending slot，违反 RFC4 的“准入不匹配不得清空 slot”合同。
- 修改：将 lazy expiry 移到 admission context、pending key、required set 与 frozen Config ID 的全部只读校验之后。未匹配候选现在直接拒绝并保持 slot；公开 `ucn_cluster_wire_v4_pending_expire()` 仍是未来 RX owner 的周期 timer 回收入口。头文件和 RFC4 同步写明 deadline 边界优先适用无副作用准入规则。
- 测试：在 `now == deadline` 分别覆盖 outer source mismatch、`frozen_config_admitted=false`、fragment Config mismatch，均断言 `occupied`、`deadline_ms`、fragment mask 不变；随后显式 expire 才释放 slot。Windows GCC Full 28/28、Lite 28/28、Nano 18/18、MSVC Debug Full 15/15、WSL ASan/UBSan 18/18、WSL `-fanalyzer -Werror` 5/5 通过。
- 结论/限制：R06-B 整改完成，`CLV2-05-02` 继续 **AUDIT HOLD / 待外部复审**；没有接入生产 RX/TX/FSM，也没有开始任何后续 M05 子项。

### OP-256｜CLV2-05-02 外审 P1 整改：Certificate pending 准入上下文

- 时间：2026-08-21。
- 外部审计：确认严格 `32 B/v3`、`40 B/v4` 分派、default-disabled encoder、Type 1..33/READY role 与 v3 生产隔离均正确；但判定 **NO-GO / M05 AUDIT HOLD**。P1 为 pending helper 只接收 raw frame/time，无法落实 RFC 所需的 outer source 与 frozen Config 准入，结构合法的伪造 Type 8 可能占用唯一 slot。
- 整改：新增非 wire 的 `ucn_cluster_wire_v4_certificate_admission_t`，固定携带 `outer_source`、`source_admitted`、`frozen_config_admitted`、`C_old` 和可选 `C_new`。`pending_begin()` 与 `pending_accept_fragment()` 均强制接收该 context；缺失/未准入 context、source 与 Head 不同、Stable `C_old != Type8.P2`、Joint `C_new != Type8.P2`、或 Type33 OLD/NEW Config 与 `C_old/C_new` 不符，均返回 `UCN_ERR_STATE`，不占用、不替换、不清空、不延长已有 slot。
- 保留边界：这些 admission fields 必须由未来 v4 RX gate 在 transport/security/source/frozen-Config 检查后构造，绝不来自 wire。当前仍未有 v4 RX gate/ConfigState/FSM；helper 只是把该前置条件变成不可遗漏的公开 API 合同，不能替代 M07/M10 的 voter/quorum/CRC 业务验证。
- 测试：新增/扩展 source mismatch、`frozen_config_admitted=false`、Stable Config mismatch、Joint `C_old/C_new` 双 set 的拒绝与 slot/mask 不变回归；原有 slot-full、重复冲突、超时、Epoch reset 继续覆盖。
- 验证：Windows GCC Full `28/28`、Lite `28/28`、Nano `18/18`；WSL ASan/UBSan Full `18/18`；WSL GCC `-fanalyzer -Wall -Wextra -Werror` `5/5`；`git diff --check` 通过（仅既有 CRLF 提示）。
- 结论/限制：R06 整改代码已完成，`CLV2-05-02` 仍为 **AUDIT HOLD / 待外部复审**。没有接入任何生产 RX/TX/FSM，未授权后续 M05 子项仍不开始。

### OP-255｜CLV2-05-02 代码完成：隔离 v4 Codec、严格分派与自审

- 时间：2026-08-21。
- 实现：新增 `include/ucn/ucn_cluster_wire_v4.h` 和 `src/extended/cluster/ucn_cluster_codec_v4.c`。v4 是独立 `40 B` raw frame（16 B Common Header + 6 个网络序 u32），不替换既有 v3 `ucn_cluster_message_t`。`ucn_cluster_wire_detect_format()` 只接受精确 `(32 B, v3)` 与 `(40 B, v4)`；泛型 decoder 只按该唯一分派调用对应 decoder，不能回退猜测或降级解释。
- 安全边界：生产 `ucn_cluster` archive 仅编译 v4 decoder；`UCN_CLUSTER_WIRE_V4_ENCODER_ENABLED=0` 时 encoder 返回 `UCN_ERR_CONFIG` 且不改输出。唯一打开 encoder 的是独立测试 target，用于冻结字节比较。`rg` 复扫确认新 v4 APIs 只被其测试调用，现有 `src/extended/ucn_cluster.c` 仍只使用 v3 / `UCN_CLUSTER_MESSAGE_BYTES=32` 收发路径，没有 v4 RX/TX 或 FSM 接线。
- Codec 合同：固定 Type `1..33`、role、flags、zero/reserved 字段、ID/serial/duration/wire-offer 域的结构性校验；失败不修改 decode 输出。Type 27 按 RFC4 以 target Cluster ID 判别：同 Cluster 仅 BACKUP，跨 Cluster 仅 HEAD。补正首轮自审发现的 Type 12 `BACKUP_MEMBER_SYNC` HEAD role 白名单遗漏，并用 RFC vector 覆盖。
- Certificate pending：新增无动态分配的独立 1-slot helper，完整 Type 8 key 绑定；fragment 先到拒绝、不同 key 满载拒绝不替换、相同片幂等/冲突释放、固定 1000 ms 超时与 active Epoch 改变释放。它没有 Authority、发送、持久化或 Cluster FSM 副作用；未来获授权的 v4 RX owner 才能每 Cluster 持有一个实例。
- 测试：新增 production-closed encoder 回归及独立 `ucn_cluster_wire_v4_codec_tests`。后者覆盖 Type 1..33 round-trip、9 条 RFC 正向 vector 的逐字节 decoder→test-encoder golden 比对、v3 dispatch、错误长度/版本/type/role/flags/reserved 字段输出不变、同/跨 Cluster READY 角色负例、以及 pending cache 的先到、满载、冲突、重复、超时、Epoch 变化。
- 验证：Windows GCC Full `28/28`、Lite `28/28`、Nano `18/18`；WSL ASan/UBSan Full `18/18`；WSL GCC `-fanalyzer -Wall -Wextra -Werror` `5/5`；所有新增编译均以 `-Werror` 通过；`git diff --check` 通过（仅仓库既有 CRLF 转换提示）。
- 自审材料：逐项范围、生产调用点、R06 缓存合同和测试证据见 `docs/07-Cluster簇/02-里程碑/M05/UCN_V5_Cluster_M05_02_隔离Codec自审报告_2026-08-21.md`。
- 自审结论/限制：`CLV2-05-02` 为 **CODE COMPLETE / 待外部审计**，M05 仍在进行中。没有改变 Core W0-W3、`UCN_PROTOCOL_VERSION`、`UCN_CLUSTER_FORMAT_VERSION=3` 或现有生产 v3 收发；没有实现能力协商、混合版本准入、Type-specific builder、Adapter 40 B 迁移、证书 quorum/CRC 业务验证、Config/Handover/Rekey FSM，也没有进行实机或掉电测试。外部审计前不得把 v4 称为可发送/可接管 Authority 的生产协议。

### OP-254｜CLV2-M05-01 外部冻结复审 GO：RFC4 冻结并授权隔离 05-02

- 时间：2026-08-21。
- 外部结论：RFC4 的最终冻结复审为 **GO（仅 RFC / CLV2-05-01）**。已确认同簇 READY 只能由已确认 Backup 发、跨簇 Merge 才由 Head 发；A/B/成员的 READY/Stepdown 验证职责一致；10 条 vector 均为 `40 B`；Certificate canonical 为 `74 B`、CRC=`0x12D221F9`。当前源码仍是 v3/32 B/Type 1..19，未提前引入 v4 Codec、常量或 FSM。
- 文档冻结：`UCN-CL-WIRE-4-RFC4` 状态改为 **FROZEN**；今后任意 v4 字节语义修改必须升 Cluster Format，不能覆写 RFC4。
- 任务状态：`CLV2-05-01` 标记 DONE / 外部冻结复审 GO；M05 转为 IN_PROGRESS；仅 `CLV2-05-02` 标记 IN_PROGRESS。R01..R05、R07、R08 标记外部 PASS；R06 作为 05-02 的静态 Certificate-pending cache 实现门禁保留 IN_PROGRESS。
- 授权边界：05-02 只能实现隔离 v4 codec、严格 32B/v3 与 40B/v4 分派、golden/negative tests；v4 encoder 默认关闭，不接入生产发送或 Cluster FSM。M07/M10/M11/M13、实机掉电和 MCU 资源门禁不因此获得授权。
- 结论：开始审查现有 v3 codec 与构建结构，随后按上述最小范围实施；本条不代表 v4 已上线或生产可发送。

### OP-253｜CLV2-M05-01 外部审计整改：RFC4 同簇 BACKUP READY 与 Stepdown 验证责任

- 时间：2026-08-21。
- 背景：外部复审确认 RFC3 的 Anchor/CRC、双模式 Handover 字段与 Certificate pending slot 已关闭，但发现 P0：同簇 Planned Transfer 中 B 仍是 Backup，却被 RFC3 要求用 `HEAD` 发送 READY；另有 P1：A→B 单播 READY 被错误要求成员匹配。审计同时确认源码仍是 v3/32 B/Type 1..19，本轮不得提前实现 M05 Codec/FSM。
- 修改：将 `UCN_Cluster_Wire_v4.md` 更新为 `UCN-CL-WIRE-4-RFC4`。Type 27 的 sender role 按模式固定：跨 Cluster Merge 只能由 target `HEAD` 发 READY；同 Cluster Planned Transfer 只能由 old Config 中已确认的 target `BACKUP` 发 READY，outer source 始终精确等于 target Head ID。
- 修改：同簇 Backup READY 仅证明 ready-to-commit，不代表 Authority。B 在收到 Commit、持久化 target Epoch 成功、reload/prove durable state、并完成正式 Head 转换前，禁止发送 `ADVERTISE`、`HEAD_DECLARE`、`HEAD_TAKEOVER`、`PRIMARY_HEARTBEAT` 或任何 Head Authority 写入；失败时不得提升。
- 修改：分离验证责任：A 只在本地精确匹配 READY 后撤权并发送 Stepdown；B 只在接收 Commit 时匹配自己发送过的 READY；成员只验证 old Authority、Stepdown txid/nonce、完整 target Epoch 与对应模式，不得要求见过不可见的 READY。
- Vector：新增 8.6 同 Cluster `PREPARE(HEAD) → READY(BACKUP) → STEPDOWN(HEAD)` 三条 40 B 正向 vector，以及仅把 READY role `0x06` 改为 `0x05` 的 40 B 负向 vector；后者必须在任何 Authority/FSM 副作用前拒绝。
- 任务表：M05/05-01 更新为 RFC4 `AUDIT HOLD`；新增 R07（mode-bound READY role，P0）及 R08（三方验证责任，P1）。R01..R06 仍保留并随 RFC4 最终复审，不构成 05-02 授权。
- 自审：现有 6 条加新增 4 条共 10 条 vector 均为 `40 B`；Anchor Certificate canonical CRC 仍独立验证为 `0x12D221F9`；RFC3 的“READY 一律 HEAD”“成员必须匹配 READY”等现行规范表述已清除；`git diff --check` 通过（仅 Git 的既有 CRLF 转换提示）。本轮仅修改 RFC、任务表和操作记录，未改 M05 常量、Codec 或 Cluster FSM。
- 结论/限制：M05 继续 **AUDIT HOLD**；RFC4 须重新进行最终冻结外部复审。通过前不得冻结 RFC、提交本轮候选、进入 `CLV2-05-02` 或发送 v4 帧。

### OP-252｜CLV2-M05-01 外部审计整改：RFC3 Anchor Certificate、双模式 Handover 与固定 Pending Cache

- 时间：2026-08-21。
- 背景：外部复审确认 RFC2 的 R01..R03 P0 已关闭（双 Epoch Handover、Winning Head `HANDOVER_READY`、Type 33 双 quorum 分片、统一 `wire_offer` 与原六条 40 B vector 均通过），但维持 **NO-GO**：Type 8 `P2` 未绑定 Certificate，且“target Cluster 必须不同”会阻断 M11-09 同 Cluster Planned Leadership Transfer；另要求给 Certificate-pending cache 固定资源/满载规则。
- 修改：将 `UCN_Cluster_Wire_v4.md` 更新为 `UCN-CL-WIRE-4-RFC3`。Type 8 `P2` 改为唯一的 `certificate_anchor_config_id`：Stable 必须等于 `C_old`，Joint 必须等于 `C_new`；该值须同时匹配 frozen Config、对应 Type 33 `P2`，并加入 Canonical Certificate CRC 输入。8.5 vector 的 canonical 输入由 `70 B` 变为 `74 B`，CRC 更新为 `0x12D221F9`。
- 修改：Handover 明确为互斥两种模式。跨 Cluster Merge Handover 仅精确绑定 old/target identity/config、禁止跨簇 Term 数值比较；同 Cluster Planned Leadership Transfer 强制 `target_term=checked_next_term(old_term)`、target Head 不同、Config 不变且 target 是合格 Backup。后者的证明是 old authoritative Head 的完整 Handover 事务，不得伪装成或混用 Type 8/33 Takeover；无 old Authority 时只能走独立 Takeover。
- 修改：Certificate-pending cache 固定为每 `ucn_cluster_t` 一个 slot（`UCN_CLUSTER_TAKEOVER_PENDING_MAX=1`）。只允许 Type 8 先建 slot；先到 fragment 丢弃；slot 满时不同 key 拒绝、不替换、不淘汰；重复冲突清空并拒绝；成功、失败、old Epoch 改变或固定 `1000 ms` deadline 才释放。全程不得产生 Authority 副作用。
- 任务表：将 M05/05-01 更新为 RFC3 `AUDIT HOLD`；R01..R03 标为外部确认 PASS 但仍随 RFC3 复审；新增 R04（Anchor/CRC）、R05（双模式 Handover）与 R06（静态 cache 的 M05-02 实现门禁）。
- 自审：6 个规范帧均为 `40 B`；更新后的 canonical 输入独立重算为 `74 B / CRC-32/ISO-HDLC=0x12D221F9`；RFC2 标识、旧 `primary config` 语义、跨 Cluster-only 限制及旧 CRC 均已从现行 RFC/任务表清除；`git diff --check` 通过（仅 Git 的既有 CRLF 转换提示）。本轮仍只有文档/任务/记录修改，未改 v4 常量、Codec 或 Cluster FSM。
- 结论/限制：M05 继续 **AUDIT HOLD**；RFC3 须重新外部复审。通过前不得冻结 RFC、提交本轮候选、进入 `CLV2-05-02` 或发送 v4 帧。

### OP-251｜CLV2-M05-01 外部审计整改：RFC2 双 Epoch Handover 与可验证 Takeover Certificate

- 时间：2026-08-21。
- 背景：外部审计对 RFC1 作出 **NO-GO**：Type 26..29 没有完整 old/target Epoch，且把 `HANDOVER_READY` 错置为 Backup；Type 8 单一 32-bit bitmap 覆盖不了 `MAX_MEMBERS + 1` 或 Joint Config；Type 7 的 capability/range 编码也与统一 `wire_offer` 相矛盾。审计确认本轮没有提前修改 M05 编解码、常量或 FSM。
- 修改：将 `UCN_Cluster_Wire_v4.md` 提升为 `UCN-CL-WIRE-4-RFC2`。Handover Type 26..29 统一以 Common Header 表示 Losing Head A 的 old Epoch，以 `P1..P3` 表示完整 target Epoch B，`P0` 为 txid；`HANDOVER_READY` 改为 Winning Head B 发送且 outer source 必须等于 target Head。`HEAD_STEPDOWN` 携带完整 target Epoch 与 nonce。
- 修改：新增 Type 33 `TAKEOVER_CERTIFICATE`。Type 8 改为只引用 Certificate；Type 33 以 OLD/NEW Config flags、index/count 和 32-bit bitmap word 分片。证书按包含 Head 的 Node ID 升序 voter set 计算，稳定配置验证单 quorum、Joint Config 同时验证 old/new quorum；定义完整 VoteId、canonical CRC-32/ISO-HDLC、缺片/冲突/越界/CRC 错误的 fail-closed 行为。当前 `MAX_MEMBERS=32` 的 33 voter 边界至少使用两片。
- 修改：Type 7 `HEAD_DECLARE.P3` 与 ADVERTISE/JOIN_REQUEST 使用相同的 `wire_offer`；P4/P5 归零。新增 Handover 双 Epoch 与 Certificate golden vector，并将 R01..R03 登记到任务表。
- 自审：逐条比对 Target FSM Head-to-Head Handover 序列、M06 `MAX_MEMBERS + 1` voter bitmap 要求、M10 Frozen Config/VoteId/Joint quorum 要求及现有 CRC32 实现；确认所有新内容仍为 RFC 文档，工作区没有新增 M05 编解码实现文件。6 个规范帧均为 `40 B`；Certificate canonical 输入为 `70 B`，以 CRC-32/ISO-HDLC 重算得到 `0xA73F66B9`；`git diff --check` 通过（仅 Git 的既有 CRLF 转换提示）。
- 结论/限制：M05 进入 **AUDIT HOLD**；RFC2 仍须新的外部复审，之前不得冻结 RFC、不得进入 `CLV2-05-02`，也不得实现或发送任何 v4 帧。

### OP-250｜CLV2-M05-01：Cluster Wire v4 RFC 草案与自审

- 时间：2026-08-21。
- 操作：在 M04 软件范围获外部复审放行后，仅启动 `CLV2-05-01`，新增 `docs/07-Cluster簇/00-规范与状态机/UCN_Cluster_Wire_v4.md`；没有修改任何 Cluster 常量、v3/v4 编解码、发送路径、Core Frame 或 FSM。
- RFC 内容：提出固定 `40 B = 16 B Common Header + 6 × u32` 布局、big-endian、Type `1..19` 延续与 Type `20..32` 新增编号、严格 role/flag/零字段合同、Capability bitmap、`32 B/v3` 与 `40 B/v4` 的长度加版本双分派，以及 ADVERTISE、Snapshot DELTA、REKEY_COMMIT 三个规范性 golden vector。
- 自审：逐项与当前 `UCN_CLUSTER_FORMAT_VERSION=3`、`UCN_CLUSTER_MESSAGE_BYTES=32`、既有 Type `1..19`、实际发送角色及 M05 任务表交叉核对。自审曾发现并立即修正 `HEAD_TAKEOVER` 的发送角色：它是晋升后的 `HEAD` 声明而不是 `BACKUP`；同时把 Join、Takeover、Snapshot、Recovery、Rekey payload 对齐任务表的既定字段需求。确认 RFC 明确禁止目前尚不存在的 v4 encoder/decoder/Join Epoch install 提前接入，且 Core `UCN_PROTOCOL_VERSION` 不受影响。
- 结论/限制：`CLV2-05-01` 为 **RFC 草案完成 / 自审 PASS / 待评审冻结**；M05 保持 `IN_PROGRESS`，只有 RFC 评审确认后才能进入 `CLV2-05-02` 并开始 Codec 代码与 golden-vector 测试。M07/M13 的 `CLV2-07-00` 持久化来源区分前置约束保持有效。

### OP-249｜CLV2-M04 外部复审 GO：软件范围结案与 M07/M13 前置约束

- 时间：2026-08-21。
- 背景：外部复审复跑 R23，确认 legacy Config/Rekey `PREPARED` 的真实 Record v1 encode/decode 后，启动会完成一次 durable `LEGACY_PREPARED_ABORT`，将 transaction 从 `PREPARED` 清为 `NONE` 并将 incarnation `1 → 2`；普通 `REPLAY_INCARNATION` 仍拒绝 `PREPARED`，且迁移不允许夹带 Epoch、Vote、已提交 Config/Rekey 或 Tombstone 修改。未发现新的 P0/P1。
- 外部验证：Windows GCC Full `27/27`、Lite `24/24`、Nano `14/14`、Service OFF `24/24`；WSL ASan/UBSan `24/24`、`-fanalyzer -Werror` `24/24`；旧漏洞独立探针重编译后其“启动必须失败”断言失效。外部复审未能在本机独立复跑 MSVC，故本条不把 MSVC 作为外部审计证据。
- 文档：任务表将 M04 标为 **DONE（软件范围）**，修正 Record 源码 SHA256 为 `75D2745C33CECB8D03553C890D655C525D3816E0550E774AE57B1C9BF3BB4508`，并登记 `CLV2-07-00`：未来 M07/M13 在重新开放真实 `PREPARED` 前必须通过 schema 或不可伪造迁移标记区分 legacy Record；R23 迁移只可清理旧格式记录，绝不能误清理新事务。
- 结论/限制：M04 软件审计解除 `AUDIT HOLD`，授权开始 M05；本工作区仍未提交/推送。板级 Flash Provider、真实掉电、目标 MCU 栈与资源测量仍未验证，不得称为生产发布或实机验收完成。

### OP-248｜CLV2-M04 R23：legacy Record v1 PREPARED 启动迁移整改与自审

- 时间：2026-08-21。
- 背景：外部复审确认 R20–R22 本身通过，但发现 P0：R20 前已由旧代码落盘、且仍满足 Record v1 canonical 校验的 Config/Rekey `PREPARED`，会被每次 REQUIRED 启动必需的 `REPLAY_INCARNATION` 拒绝，导致 init 清空对象并永久无法启动。禁止未来 Hook 写入不能修复已经存在的记录。
- 实现：
  1. 增加唯一的 `UCN_CLUSTER_PERSIST_OPERATION_LEGACY_PREPARED_ABORT`。它不是公开的 Config/Rekey 协议 Abort：只允许存在一个合法 `PREPARED` transaction 的已加载快照使用，严格保持 Epoch、Vote、committed Config/Rekey、Tombstone 与其余字段不变，只将 Prepared payload canonical 清零为 NONE，并在同一 request 中严格递增 `boot_incarnation`。
  2. `ucn_cluster_init()` 在 load 完整 Record 后、建立任何 timer/role/发送路径前识别该旧状态，使用上述 operation 替代 generic `REPLAY_INCARNATION`。Provider sync 完成仍 reload/journal prove；PENDING 保持全局启动 gate，poll 成功后才设置新 incarnation。写入、PENDING、reload proof 任一步失败均保留 fail-closed，不会继续旧 FSM。
  3. 保持 `TOMBSTONE_COMMIT` 独立 operation 非法；普通 `REPLAY_INCARNATION` 仍拒绝 PREPARED，非法 authority mutation 也被 migration validator 拒绝，避免借兼容迁移放宽其它状态转换。
- 新增回归：
  1. Config finalized PREPARED 与 Rekey finalized PREPARED 均先执行真实 280 B Record encode/decode，再分别走同步和异步 Provider 的 restart/init；断言最终 phase=NONE、incarnation 9→10、journal=LEGACY_PREPARED_ABORT、零 Control TX。
  2. admission 负向断言 generic Replay 仍拒绝 PREPARED，migration 不得改 Epoch；两类 PREPARED 均可通过唯一迁移操作。
  3. 双槽 Host Provider 对 Config/Rekey 分别注入 torn write：首次 init fail-closed 且旧 PREPARED slot 仍可读取；去除故障后二次 init 成功完成原子迁移。
- 定向证据：Windows GCC Full `ucn_tests` 通过；`m04_external_legacy_prepared_boot_probe` 输出 `state_valid=1 encode=0 decode=0 generation=7 init=0 submit_calls=1 durable_phase=1(NONE) durable_boot=2 cluster_enabled=1`。该探针原以“init 必须失败”为 exit 0 条件，现非零退出表示旧漏洞条件失效。
- 全量复验：Windows MSVC Debug Full `14/14`、GCC Full `27/27`、Lite `24/24`、Nano `14/14`、Full Service OFF `24/24`；WSL ASan/UBSan `24/24`、WSL `-fanalyzer -Werror` `24/24` 均通过。此前 R17–R22 的 Prepare/reentrancy/Vote replay/Link Down/发送失败外部旧漏洞探针也全部以非零退出，且正式回归覆盖其恢复路径。
- 文档：同步任务表、Current FSM、M04 自审报告与资源记录；Host Full `cluster_bytes` 维持 `1200 B`，R23 不新增 `ucn_cluster_t` 字段。
- 结论/限制：R23 已完成本地代码整改与定向自审，但 **M04 继续 AUDIT HOLD**，不得提交、推送、进入 M05 或宣称实机 Flash/掉电验证完成；必须再次外部复审整个 M04 候选工作区。

### OP-247｜CLV2-M04 外部复审 R20..R22 整改与全量自审：仍为 AUDIT HOLD

- 时间：2026-08-21。
- 背景：外部复审确认 R17 Rekey fence、R18 Provider I/O gate 通过，并发现三个未覆盖分支：R20（public Config/Rekey Prepare 可落盘但 Commit 不开放，重启会被 `REPLAY_INCARNATION` 拒绝）、R21（重启恢复的 exact durable Vote 重放 ACK 绕过 retry dispatcher）、R22（async Vote durable 后 `LINK_DOWN` 被误计为 persistence fault）。本轮不提交、不进入 M05。
- R20：采用 M07/M13 前的保守 API 收缩，而不是伪造 Prepare recovery。四个 public Config/Rekey Hook（Prepare/Commit）均在 Provider I/O 前返回 `UCN_ERR_CONFIG`；不再产生 orphaned `PREPARED`。同步 Provider 与配置为 async 的 Provider 均覆盖：调用无 submit/pending/Record 写入，随后 reset/init 成功、boot incarnation 递增、两个 transaction 均为 NONE。M07/M13 将来必须连同 Resume/Abort/Commit 与 runtime continuation 一起重新开放。
- R21：REQUIRED 模式下的 exact durable Vote replay 改为调用 `send_takeover_ack_after_persistence()`，不再直接 `send_takeover_ack()`。所以新 Vote 和重启恢复 Vote 的 `NO_SPACE/LINK_DOWN` 都设置同一个 retry descriptor；下一 Step reload/prove Vote 后只允许该 ACK 重发。
- R22：定义 Cluster TAKEOVER_ACK 的可重试本地传输结果为 `UCN_ERR_NO_SPACE`（队列背压）与 `UCN_ERR_LINK_DOWN`（直接 bearer 临时不可用）。这两类结果不会调用 `cluster_persistence_fail_closed()`，不会增加 `persistence_failures`；其他无法重新证明 durable Vote/Epoch 的错误仍 fail-closed。新增异步 Vote → Link Down → retry → success 回归。
- 外部探针复跑：`m04_external_prepare_restart_probe` 输出 `prepare=-2 / committed=0 / init2=0 / second_enabled=1`；replay-backpressure probe 输出 `retry=1 / faulted=0 / persistence_failures=0`；Link Down probe 输出 `retry=1 / faulted=0 / failure=0 / persistence_failures=0`。三者均以“漏洞仍存在”作为 exit 0 条件，现分别非零退出；正式回归额外验证 bearer/队列恢复后的下一 Step ACK 成功。
- 测试与自审：Windows GCC Full `27/27`、Lite `24/24`、Nano `14/14`、Service OFF `24/24`；WSL ASan/UBSan `24/24`、WSL `-fanalyzer -Werror` `24/24`。生产源码复扫确认 Config/Rekey 四个 Hook 没有 Provider 调用；REQUIRED durable replay 仅走 common ACK dispatcher。Host Full `cluster_bytes=1200 B`，无结构体增量。
- 结论：R20–R22 已完成本地代码整改与自审；**M04 继续 AUDIT HOLD，等待外部复审**。不得提交、推送、进入 M05，亦不得称为生产或实机掉电验证完成。

### OP-246｜CLV2-M04 最终外部审计 R17..R19 整改与再次全量自审：仍为 AUDIT HOLD

- 时间：2026-08-21。
- 背景：最终外部审计以三个未覆盖的时序问题否决 M04：R17（Rekey Commit durable successor 后旧 RAM Head 仍可发送 retired Epoch）、R18（Provider `load/submit/poll` 调用前没有重入门）、R19（durable Vote 后首次 `TAKEOVER_ACK` 队列背压被错误归为 persistence failure）。按用户要求不提交、不进入 M05，先将三项转为任务和正式回归。
- R17：Config/Rekey 的真正 runtime/wire continuation 分属 M07/M13，当前不能以通用 `ACTION_NONE` 假装完成。因此两个 public Commit API 都改为**Provider I/O 前**返回 `UCN_ERR_CONFIG`，保持 Record、runtime Epoch 与 wire 均不变；Prepare 仍可安全持久 staging。此为有意的 fail-closed API 收缩，不提前实现 M07/M13。
- R18：新增 `persistence_io_active` 动态门。`ucn_cluster_init()` 的同步 `load` 以及运行期每一次 Provider `load/submit/poll` 都在 callback 调用**前**置门、返回后清门。门内同步重入的 Step/RX/递归 poll 返回 `UCN_ERR_STATE`，不能解析、推进或发送。新增 init load、运行 load、submit PENDING 前与 poll 内四类重入回归，全部断言零 Control TX。
- R19：新增有界 durable ACK retry descriptor。Vote 已由 reload proof 证实后，首次 `send_takeover_ack` 若返回 `UCN_ERR_NO_SPACE`，不触发 persistence fault；普通 TX/RX 维持 fence，下一次 Step 重新 load/prove 同一 Vote 后仅重发该 ACK。新回归验证第一次 NO_SPACE 后 `faulted=false`、下一 Step 发送成功、真实 Provider failure 仍保持 fail-closed。
- 对抗探针：外部 Rekey/reentrancy probe 因 Config Commit 现 fail-before-I/O 而在旧 setup 阶段退出 `3`；外部 Vote probe 观察 `durable_vote=1`、`faulted=0`、`persistence_failures=0`，其旧“漏洞存在才返回 0”的断言退出 `4`。这些非零退出码是旧漏洞期望不再成立，正式 `test_cluster_persist` 已补正向 retry 成功断言。
- 全量门禁：Windows GCC Full `27/27`、Lite `24/24`、Nano `14/14`、Service OFF `24/24`；WSL ASan/UBSan `24/24`、WSL `-fanalyzer -Werror` `24/24`。Host Full `cluster_bytes=1200 B`（相对 M03 +64 B），资源记录已同步。最终源码扫描确认 Provider `load/submit/poll` 只保留在带 I/O gate 的 Bridge 三个调用点、Cluster 直发仍只有 `cluster_transmit()`；`git diff --check` 与变更/未跟踪文本尾随空白扫描均无错误（仅 CRLF 提示）。
- 边界更正：Recovery Head 创建已接 persistence，不等于接收端 Recovery Join/`RECOVERY_ACK` 已持久化；该接收安装仍是 M05 前的 RAM 路径。M04 不能再表述为“所有 Recovery 均已持久化”。
- 结论：R17..R19 已完成本地代码整改与自审，但 **M04 继续 AUDIT HOLD，等待外部复审**。不创建提交、不推送、不进入 M05，也不得宣称生产或实机掉电验证完成。

### OP-245｜CLV2-M04 04-01..10 全量交叉自审完成：CODE COMPLETE / AUDIT HOLD

- 时间：2026-08-21。
- 范围：按用户“整个 04 完成后再外部审”的要求，对 04-01..10 做最终交叉自审，而不是把 04-05..10 的单项自测误作里程碑签字。04-01/02 的 R01..R16 和 04-03 已有历史独立复审 PASS；本次重新审查 04-04..10 及它们和现有 Current FSM 的连接。
- **发送/状态机复扫**：检索 `config.send`、`send_message`、`send_cluster_message`、`cluster_transmit` 与所有 Cluster Control type。Cluster 只有 `cluster_transmit()` 直调 `cluster->config.send`；所有路径都先经 persistence gate，且 gate 位于 token bucket 前。RX 在 parse/peer/replay/ACK 前冻结，step 先 poll、再以 `operation_id + operation + fingerprint` reload proof 解开 continuation。Federation 的独立 send 不在 Cluster M04 promise 范围。
- **Provider/崩溃复扫**：检查 submit/poll 的合法 completion、同步 Provider 的 PENDING 拒绝、terminal reload proof、failure sticky、Factory/READY/boot incarnation、double-slot generation/CRC/torn write 和 retry idempotence。检查中发现 `VOLATILE_TEST` 下 Config/Rekey Hook 会缺少 staging 载体，已改为明确 `UCN_ERR_CONFIG`，并新增回归；不保留“Prepare 假成功、Commit 才丢失”的接口陷阱。
- **全量门禁**：Windows GCC Full `27/27`、Lite `24/24`、Nano `14/14`、Full Service OFF `24/24`；新鲜 Windows GCC Full CMake 配置 `24/24`；WSL ASan/UBSan `24/24`；WSL GCC `-fanalyzer -Werror` `24/24`。`git diff --check` 无空白错误（仅 CRLF 转换提示）。Host x64 Debug Full `cluster_bytes=1192 B`（M03 后 `+56 B`）。
- **未覆盖项**：本机没有可用 VS2022 实例，本轮不把 MSVC 构建当作证据；没有真实 MCU Flash Provider、掉电切断、擦写寿命或 task stack 测量。Config/Rekey 的 M07/M13 wire/FSM、M05 Join Epoch install 也没有被提前实现。
- 文档：任务表、CURRENT FSM、资源记录和独立 [M04 自审报告](../07-Cluster簇/02-里程碑/M04/UCN_V5_Cluster_M04_自审报告_2026-08-21.md) 已同步。报告包含实现对照、反例、门禁和给外部审计的重点。
- 结论：**M04 是 CODE COMPLETE / AUDIT HOLD，交由外部审计。** 不创建提交、不推送、不进入 M05；即使软件门禁全绿，也不得称为生产/实机掉电验证完成。

### OP-244｜CLV2-M04 04-10：双槽 Host Fake 与崩溃矩阵自审 PASS（纳入 M04 外部审计）

- 时间：2026-08-21。
- 实现：`test_cluster_persist.c` 新增双槽 Host fake，但物理内容仍由公开 Record v1 codec 编码/解码；fake 只模拟原子槽选择和板级 Provider 必须面对的故障切点。覆盖 write-before 失败、半写（torn）、新槽完整写完后调用者故障、CRC 损坏和重启 load 选择。
- 自审：每个故障切点均重新 load。写前、半写、CRC 损坏必须保留旧 Epoch；完整写后故障必须恢复新 Epoch，原请求 retry 只能 idempotent，不能出现 half-old logical state。该矩阵以真实 `ucn_cluster_persist_record_encode/decode` 驱动，而非直接复制内存快照。
- 边界：这是 Host 软件故障注入，不是 MCU Flash 页擦除、断电保持、磨损或 RTOS 时序的实机证据；板级 Provider 与掉电矩阵仍留给后续实机验证。

### OP-243｜CLV2-M04 04-09：受控启动 incarnation 自审 PASS（纳入 M04 外部审计）

- 时间：2026-08-21。
- 实现：REQUIRED `ucn_cluster_init()` 在普通 Cluster 行为前请求 `REPLAY_INCARNATION`；Factory 首次 boot durable incarnation 为 1，之后每次受控初始化严格递增。异步完成前保持 persistence pending，因此不得发送或进入可承诺角色；完成后才把 durable 值投影到 `cluster_id_incarnation`。
- 自审：单测覆盖 Factory `1`、第二次 boot `2`、PENDING 的零发送门和 nonce 在新对象从小值重启但 replay 域已改变。Record transition validator 保持“仅 REPLAY_INCARNATION 可改 incarnation”的既有 P0 合同。
- 边界：这不要求每帧写 Flash；它只定义 `(node_id, incarnation, nonce)` 的 boot 域，不替代硬件随机源或产品级安全 nonce 方案。

### OP-242｜CLV2-M04 04-08：统一 fail-closed 处置自审 PASS（纳入 M04 外部审计）

- 时间：2026-08-21。
- 实现：Provider 返回 FAILED、非法 completion、缺少 async `poll` 或报告 COMMITTED 后 reload 仍看不到相同 completed journal 时，Bridge 立即清 pending、记录 failure/stat，并设 sticky fault。正常 Head/Recovery Head 走既有 `TERM_CONFLICT_WAIT` wire-silent containment；Member/Backup 由 progress/TX 总门冻结。
- 自审：生产源码扫描确认 Cluster 的 `config.send` 只有 `cluster_transmit()` 一个入口，全部 `send_message`/手工编码帧最终经此入口；其持久化 gate 位于 token bucket 前。单测分别覆盖 claimed-commit-not-visible、Head Config poll fail、Member Vote poll fail 与 Backup Takeover poll fail，均断言没有 ACK/Head promotion/发送泄漏。
- 边界：在 FSM 自身已损坏而 containment transition 被拒绝的极端诊断状态下，全局 TX/RX gate 仍先阻断；不能把该 Host 覆盖替代为硬件断电证明。

### OP-241｜CLV2-M04 04-07：Config/Rekey Provider Hook 自审 PASS（纳入 M04 外部审计）

- 时间：2026-08-21。
- 实现：公开 `ucn_cluster_persist_config_prepare/commit()` 与 `ucn_cluster_persist_rekey_prepare/commit()`。四个 Hook 都经同一 `load → finalize/admit → submit → reload journal` Bridge，并复用 Record v1 的严格 transaction validator；Config/Rekey PREPARED 并发和错误 txid/阶段在 Provider I/O 前拒绝。
- 自审：覆盖 Config PENDING→poll completion、Config Commit、重复 Commit 的 owner ordering 拒绝，以及 Rekey Prepare/Commit 后 successor Epoch、committed rekey、Tombstone 的原子状态。没有引入 M07/M13 的 wire 消息、quorum 或运行期 Config FSM，故不会把存储 hook 误称完整 Config/Rekey 协议。

### OP-240｜CLV2-M04 04-06：Takeover Vote persist-before-ACK 自审 PASS（纳入 M04 外部审计）

- 时间：2026-08-21。
- 实现：`TAKEOVER_PREPARE` 的 REQUIRED Member 先 load current Record，验证 durable active Epoch 与 RAM authority 完全相同，再持久化完整 VoteId `(cluster, term, candidate, backup_generation)`；仅经过 reload journal match 后才发 `TAKEOVER_ACK`。相同 durable VoteId 允许重发丢失 ACK，候选或 generation 冲突拒绝。
- 自审：覆盖同步/异步 Vote、PENDING 零 ACK、精确 durable 重放、冲突 Vote、RAM/durable Epoch 不匹配和 poll 失败；错误路径不会将普通 owner 顺序错误伪装为 Flash 设备故障。`VOLATILE_TEST` 保留既有模拟行为，不能作为 durable ACK 证据。
- 边界：M05 尚未定义 Join Epoch install；因此新加入但未取得 durable active Epoch 的 REQUIRED Member 选择拒绝 ACK，这是 safety-first 的可用性限制。

### OP-239｜CLV2-M04 04-05：Epoch/Head 承诺门自审 PASS（纳入 M04 外部审计）

- 时间：2026-08-21。
- 实现：新增有界 `ucn_cluster_persistence_runtime.c`。Bridge 不缓存完整 280 B Record，只保存一个 pending descriptor；每次 Epoch 承诺走 `submit → PENDING/poll → authoritative reload → completed journal match`。Election/Recovery、Backup score challenge 和 Takeover Head promotion 仅在 matching durable Epoch 后执行各自 continuation。
- 自审：覆盖 Election PENDING、伪 COMMITTED 但 reload 不可见、Backup challenge PENDING、Takeover PENDING、Recovery 新 Cluster PENDING；断言 pending 时原 role/phase 保持且没有 Advertise/Head promotion。Full target `ucn_tests` 在本项和后续反例加入后通过。
- 资源：Bridge 把完整 Record 留在 Provider/调用栈而非 `ucn_cluster_t`；最终对象趋势与目标 MCU 栈测量要求已单列在 `docs/results/cluster_m04_resource_delta.md`。

### OP-238｜CLV2-M04 04-04：同步 load-before-init 候选实现（待独立复审）

- 时间：2026-08-21。
- 前置签字：独立复审已确认 04-03 GO；默认 REQUIRED、显式 VOLATILE_TEST、Provider 合同和 view/stats 可见性均通过，授权只进入 04-04。04-05/06 的 Election、Takeover、ACK、Advertise 与 Commit 接线仍冻结。
- 实现：`ucn_cluster_init()` 在配置归一和 Provider 合同校验后、但在读取时钟、建立角色/Phase、计时器、Token Bucket 或任意发送路径前，针对 REQUIRED 同步调用 `provider->load()`。Factory Empty 成功后才继续形成 Detached 初始状态；READY 的 validated Record 仅恢复 Current FSM 已有的 M03 safety inputs：`last_cluster_id/max_seen_term/last_stable_head`、三字段 `(cluster_id, term, backup_generation)` Vote replay key，以及 durable `cluster_id_incarnation`。完整持久 VoteId 的 `voted_for_node_id`、Config/Rekey/Tombstone 虽已由 Record v1 验证，却因 Current FSM 尚无完整且安全的消费模型，继续由 Provider 保持 authoritative，留给 04-06/04-07/M07/M13 接线，避免半成品 RAM mirror 被旧状态迁移覆写。
- 失败语义：Provider 返回错误、全零或其他非法 load result 时，`ucn_cluster_init()` 清零整个 Cluster 并 fail-closed；VOLATILE_TEST 不调用 load，诊断状态为 NOT_APPLICABLE。新增 Factory、READY、CRC 错误、非法零结果和 VOLATILE 不 load 回归；view/stats 均回显恢复状态。
- 资源：Host x64 Debug FULL 的 `cluster_bytes` 从 M03 `1136 B` → 04-03 `1144 B` → 当前 04-04 `1152 B`；本轮增加 `8 B`（累计 M03 后 `+16 B`）。这是 Host 结构体观测，不等同于 MCU 最终 RAM 结论，详见 `docs/results/cluster_m04_resource_delta.md`。
- 门禁：Windows GCC Full `27/27`、Lite `24/24`、Nano `14/14`、Full Service OFF `24/24`；WSL ASan/UBSan `24/24`、WSL `-fanalyzer -Werror` `24/24` 均通过。未进行实机 Flash/掉电验证，也没有发送承诺帧。
- P2 记录更正：独立审计快照与工作区对第 10 行的表述不一致；为避免歧义，本轮把 `ucn_cluster_persist.h` 改写为“04-02 codec implements ...”，并同步更正 OP-237 的旧描述。
- 结论/限制：04-04 为 **CODE COMPLETE / 待独立复审**，不是 production persistence 闭环；不得跳过独立审计直接开始 04-05。

### OP-237｜CLV2-M04 04-03：显式持久化运行模式配置门禁（独立复审 PASS）

- 时间：2026-08-21。
- 前置签字：独立复审已对 04-01/02 作出 GO，确认 Record v1 当前源码 SHA256 为 `0E5C669CED71BD5D4F66E68436CDBBADED870817131A3B190158256D3D92277C`；R01..R16 的持久化合同与 Tombstone fail-closed 边界已通过。本轮进入 04-03，后续独立复审已对本项签署 GO。
- 实现：`ucn_cluster_config_t` 在尾部追加 `persistence_mode` 与 Provider 指针。零值默认 `REQUIRED`，缺少兼容 `ucn_cluster_persist_provider_t` 时 `ucn_cluster_init()` 返回 `UCN_ERR_CONFIG`；`VOLATILE_TEST` 必须由测试/模拟显式选择。`ucn_cluster_view_t` 和 `ucn_cluster_stats_t` 回显该模式，避免把无持久化模拟误读为 production 安全运行。公共 Cluster header 仅前置声明 Provider，真正 Provider/Record API 继续由 `ucn_cluster_persist.h` 提供。
- 边界（04-03 完成时）：本项本身不调用 Provider `load()`、不恢复任何 Record 状态、不改 Election/Takeover/ACK/Advertise，也不引入持久化完成事件；回归曾断言兼容 Provider 的 `load_calls == 0`。因此 04-03 本身不是 load-before-init，更不是 persist-before-promise 生产闭环；随后 04-04 才在独立授权范围内开始同步 load。
- 测试：Windows GCC Full `27/27`、Lite `24/24`、Nano `14/14`、Full Service OFF `24/24` CTest 全部通过；WSL ASan/UBSan `24/24`、WSL `-fanalyzer -Werror` `24/24` 通过。新增回归覆盖默认/非法模式/无 Provider/不兼容 Provider 均拒绝，兼容 Provider 允许初始化但不 load，以及 view/stats 的 REQUIRED/VOLATILE_TEST 可见性；既有 Cluster 测试与模拟器均显式标记 `VOLATILE_TEST`。
- 文档：任务表将 04-01/02 和 04-03 标为独立复审 PASS；Current FSM 同步真实边界。独立审计同时指出第 10 行注释与先前记录不一致；该 P2 已由 OP-238 的无歧义句式修正。
- 结论/限制：04-03 已独立签署完成。工作区保持未提交；不把此次软件回归或 Provider 指针存在本身当成真实 Flash、掉电恢复或对外承诺安全证据。

### OP-236｜CLV2-M04 04-01/02 五次 NO-GO 整改：Retired Tombstone 不可被新簇创建抹除

- 时间：2026-08-21。
- 决策：接受对当前工作区的独立复审。此前 `CLUSTER_CREATE_COMMIT` 为新 Cluster 清 snapshot 时删除了 committed Rekey、Rekey transaction 和 Tombstone，因而可在 `A→B` Rekey 后复活 retired A，或以创建 C 的方式丢弃 A 的退休证据；这是 P0。`CLV2-04-03/04-04` 与所有 FSM/wire 接线继续冻结。
- 整改：将 M04 的策略明确为**保守 fail-closed**，不在当前 280 B Record v1 中伪造不完整的 retired-identity 集合。`CLUSTER_CREATE_COMMIT` 若 source 含 committed Rekey、非 NONE Rekey transaction 或 Tombstone，直接拒绝；其 ordinary-state 清理 helper 只清 Vote 与 Config，代码不再清除 Rekey/Tombstone。以后只有 M12 定义持久 lineage/retired identity 约束后才可重新评估从这类状态创建新簇。
- 测试：`test_cluster_persist` 在真实 `A→B` Rekey Commit 后依次尝试重建 retired A、创建 fresh C、编码/解码重启后创建 fresh C，均要求 `ADMISSION_REJECTED`；同时断言 source/reloaded Tombstone 仍为 `retired=A, replacement=B` 且 committed Rekey 仍有效。实际回归：MSVC Debug Full 14/14；Windows GCC Full/Lite/Nano 各 14/14；配置契约 27/27；Service OFF 24/24；WSL ASan/UBSan 24/24；WSL `-fanalyzer` 24/24。
- 结论/限制：R16 已保存到 `E:\File\MESH\UCN` 当前工作区，但 M04 仍为 **AUDIT HOLD**。这是 M04 的安全收敛策略，不是 M12 lineage、也不是已接入真实 Flash 或 Cluster FSM 的 production 功能；本轮未创建 Git 提交或推送。

### OP-235｜CLV2-M04 04-01/02 四次 NO-GO 整改：新簇创建与 Replay-only incarnation

- 时间：2026-08-21。
- 决策：接受独立复审新增的两项 P0；`CLV2-04-03/04-04`、Cluster FSM 初始化、Election/Takeover/ACK/Advertise 的真实发送接线继续冻结。本轮仅改 04-01/02 的持久化转换契约、Host fake 与软件回归。
- 整改：
  1. 新增 `CLUSTER_CREATE_COMMIT`，使普通 Detach、Recovery 或首轮分配出的**新** Cluster ID 可用独立原子转换持久化为 Term 1。普通 `EPOCH_COMMIT` 继续只允许同 Cluster identity 的 exact-next Term；不得再以普通 Epoch 变更身份。
  2. 新簇创建必须使用非零/非 broadcast 的有效 Epoch，Term 固定 1，且与已有 parent Cluster ID 不同。转换保留 node 的 `boot_incarnation` 和新的操作 journal，清除 parent 的 Vote、Config、Config/Rekey transaction、committed Rekey 与 Tombstone，避免旧簇 quorum/retirement 语义继承到新身份。运行时 FSM 已 Detach 是调用前提；本 operation 不发送任何协议帧。
  3. `boot_incarnation` 被收敛为 `REPLAY_INCARNATION` 的唯一修改路径：该操作严格递增且不得同时修改 Epoch；所有其他 operation 必须完全保持已持久值。Factory Empty 必须先持久 Replay domain，随后才能创建首个 Cluster 或前进 Term。
  4. 修正公共头注释重复单词，并把 `TOMBSTONE_COMMIT` 的诊断保留枚举后移；该 Provider v1 API/Record v1 尚未发布，不涉及已部署节点的 wire 兼容承诺。
- 测试：新增 Factory `Replay → Cluster Create → Epoch`、普通 Epoch 在 Factory Empty 失败、Detach 后新 ID/Term 1、Record reload 后再次新建、父 ID 重用/错误 Term/残留父 Config 拒绝、Epoch 内 incarnation 回退或前进拒绝、Replay 单独递增与 Replay 携带 Epoch 变化拒绝。实际回归：MSVC Debug Full 14/14；Windows GCC Full/Lite/Nano 各 14/14；配置契约 27/27；Service OFF 24/24；WSL ASan/UBSan 24/24；WSL `-fanalyzer` 24/24。
- 结论/限制：R14/R15 代码已整改，但 M04 仍为 **AUDIT HOLD**，必须等待独立复审重新签字。没有真实 Flash Provider、掉电测试或 Cluster FSM persist-before-promise 接线；不得据此宣称 04-03 或生产恢复已经完成。本轮未创建 Git 提交或推送。

### OP-234｜CLV2-M04 04-01/02 三次 NO-GO 整改：Vote 冲突、Epoch 回退与 Config 序列复用

- 时间：2026-08-21。
- 决策：接受独立对抗探针的第三次 NO-GO。`CLV2-04-03/04-04`、所有 Cluster FSM 初始化和网络控制帧接线继续冻结；本轮只改持久化状态转换、Record/state 校验、Host fake 回归和文档。
- 整改：
  1. Vote transition 改为**每个 Active Epoch 最多一个 durable VoteId**。已存在当前 Epoch Vote 时，candidate 或 `backup_generation` 改变均拒绝；相同持久请求继续由 completed-operation journal 走幂等路径，只有 Epoch 严格推进后才允许新 Vote。
  2. `active_epoch` 与 `max_epoch` 在有效持久状态中必须是同一完整 Epoch。普通 `EPOCH_COMMIT` 对已有 identity 只接受同 Cluster 的 exact-next Term；拒绝 Term 回退、跳号、同 Term 换 Head、Active/Max 不一致和以普通 Epoch Commit 更换 Cluster identity。Factory/首次已知 authority 写入仍须由空状态进入，后续新 identity 留给明确的 Rekey/Recovery 接线。
  3. Config reference 的 `config_id` 与 `generation` 纳入 no-wrap serial 域；Config Prepare 的首次 txid/config_id/generation 固定为 1，后继事务三者均须精确 `+1`。因此旧 txid、旧 Config、跳号和重启后旧 Prepare replay 都不能重新成为合法事务。
  4. 修正公共 Header 两处注释：7 B reserved 属于 264 B payload；Tombstone replacement 必须等于非零 Rekey successor Cluster ID。
  5. `test_cluster_persist` 新增对抗回归：Vote 换 generation/换 candidate、Epoch 5→2/5→7/same-term-head-change/cross-cluster/active-max-mismatch、Config txid reuse/skip、Config ID/generation rollback/skip，以及 Record reload 后旧 Prepare replay；同时保留 5→6、连续 Config、Epoch 推进后新 Vote 等正例。
- 验证：MSVC Debug Full 14/14；Windows GCC Full/Lite/Nano 各 14/14；配置契约 27/27；Service OFF 24/24；WSL ASan/UBSan 24/24；WSL GCC `-fanalyzer` 24/24；`git diff --check` 通过。
- 结论/限制：R11/R12/R13 的代码整改与软件回归完成，但 M04 继续为 **AUDIT HOLD**。尚未有独立复审签字，故不得开始 04-03，也不得声称已把这些持久化规则接入真实 Cluster FSM、Flash Provider 或硬件掉电恢复。本轮未创建 Git 提交或推送。

### OP-233｜CLV2-M04 04-01/02 二次 NO-GO 整改：完整 VoteId、事务转换与 Rekey successor

- 时间：2026-08-21。
- 决策：接受二次复审的 NO-GO；不进入 `CLV2-04-03`，04-03/04-04 与所有 Cluster FSM 接线继续冻结。本轮只整改 Provider/Record v1、Host fake 和测试，不对 Election、Takeover、ACK、Advertise 或 init 路径作任何改动。
- 整改：
  1. `last_vote` 扩展并编码完整 VoteId：`(cluster_id, term, voted_for_node_id, backup_generation)`；`backup_generation` 进入 serial 合法域检查。Record round-trip 后保留该 generation；已持久化的同一完整 VoteId 再次提交会拒绝，不同 generation 可被清楚地区分。
  2. Rekey reference 扩展为 predecessor epoch、predecessor committed-config 和 successor epoch（新 Cluster ID、Term 固定为 1、Head ID）。Record v1 因尚未发布的字段需求从 200 B 调整为 280 B；所有字段仍为显式 big-endian、CRC32、canonical 编码，未持久化 C struct image。
  3. `request_admit()` 不再仅检查 operation ID/fingerprint；新增由 `committed_state + operation + next_state` 驱动的转换验证。Config Commit 必须匹配同 txid 的 PREPARE staging Config；Rekey Commit 必须匹配同 txid 的 staging successor，并在同一快照更新 active/max Epoch、committed Rekey、transaction journal 和 Tombstone。每类 operation 只能改写自己的许可字段。
  4. 禁止 Config/Rekey 双 PREPARE；Rekey PREPARE 必须绑定当前 active Epoch 与 committed Config；Tombstone 不再允许 standalone operation，而只能由匹配 Rekey Commit 原子生成。同步/异步 Host fake 在 Factory Empty 也执行同一 admission 检查，避免测试 Provider 绕过状态机。
  5. 回归新增 Prepare-A/Commit-B、txid conflict、双 PREPARE、successor mismatch、standalone Tombstone、VoteId generation restart round-trip/duplicate 与越阈值等协议级反例；保留零值、canonical、PENDING 多轮 poll 与无 poll fail-closed 测试。
- 验证：MSVC Debug Full 14/14；Windows GCC Full/Lite/Nano 各 14/14；配置契约 27/27；Service OFF 24/24；WSL ASan/UBSan 24/24；WSL GCC `-fanalyzer` 24/24；`git diff --check` 通过。
- 结论/限制：R04/R06 的本轮代码整改和软件回归已完成，R07..R10 已写入任务表；**M04 仍为 AUDIT HOLD**，未获得独立复审签字前不得开始 04-03，也不得宣称已经实现运行期 persist-before-promise、真实 Flash 原子掉电恢复或重启后的 FSM 接线。本轮未创建 Git 提交或推送。

### OP-232｜CLV2-M04 04-01/02 审计 NO-GO 整改：零值安全、canonical Record 与可恢复事务

- 时间：2026-08-21。
- 决策：接受复审的 NO-GO。7ba510a M03 候选提交仍为独立基线；04-01/02 的未提交初稿不进入 Cluster FSM，04-03/04-04 继续冻结。此前 OP-230/231 记录的是初稿完成，不构成 M04 安全签字。
- 整改：
  1. load 与 completion 枚举新增 INVALID=0；全零 completion 和全零 load result 均无效，Factory Empty 必须同时携带完整零 snapshot。新增 Provider admission helper：没有 poll() 的同步 Provider 返回 PENDING 时，Owner 必须 fail-closed。
  2. Epoch 统一拒绝 0/broadcast Cluster/Head ID，Term、Config/Rekey generation、incarnation、transaction ID 与 operation ID 统一限制在 UCN_CLUSTER_SERIAL_ROTATION_THRESHOLD 内；越域编码、解码或 admission 都失败。
  3. Record v1 在尚未发布/接入 FSM 前重定为固定 200 B canonical byte layout。encode 只写有效语义字段；decode 要求所有 absent 字节为零并在返回前零化。脏内存不再影响 CRC/物理记录，带有效 CRC 的脏物理记录被拒绝。
  4. Snapshot 增加 Config/Rekey transaction phase、txid、staging 与 committed reference；Tombstone 绑定 Rekey txid；completed-operation journal 同时保存 ID、operation、canonical fingerprint。Prepare、Commit、未完成 PENDING 和重启重试拥有明确可区分状态；相同 ID 只有 operation 与 canonical state 同时一致才幂等。
  5. Host fake/public-header tests 增加全零、无 poll PENDING、多轮 poll、广播/越域 serial、canonical encode/decode、Prepare/Commit/重启观察、相同 ID 冲突和同步/异步失败负向回归。
- 验证：Windows MSVC Debug Full、GCC Full/Lite/Nano CTest 均 14/14，Full Service OFF 24/24，配置契约 27/27；WSL ASan/UBSan 24/24，WSL GCC -fanalyzer 24/24；git diff --check 通过。
- 结论/限制：R01..R06 代码与 Host 回归已完成，但 M04 仍为 **AUDIT HOLD**，必须经独立复审签字才能开始 04-03；本记录不声称已经做到生产路径的 persist-before-promise、真实 Flash 掉电原子性或 FSM 启动恢复。

### OP-231｜CLV2-M04 04-02 完成：固定 CRC32 持久化记录编解码与无回绕 generation

- 时间：2026-08-21。
- 目标：完成 `CLV2-04-02` 的物理记录 schema，保证逻辑安全快照可由不同 Flash/FRAM/Host Provider 以相同字节布局保存和恢复；本项不绑定任何存储 SDK，也不接入生产 FSM。
- 实现：
  1. 在 `ucn_cluster_persist.h` 冻结 Record v1：固定 128 B，前 16 B 为 big-endian `magic/schema_version/record_size/record_generation/CRC32`，随后 110 B 为 active/max Epoch、Vote、Config reference、Rekey reference、Tombstone、boot incarnation，末尾 2 B 保留且必须为零。绝不持久化 C struct image，避免 ABI、对齐和 bool 表示差异。
  2. 新增 `src/extended/cluster/ucn_cluster_persist.c`。CRC32 覆盖完整记录且将 CRC 字段视作零；decode 先区分全零/全 FF factory-empty（`UCN_ERR_NOT_FOUND`），然后依次校验 magic、schema、size、nonzero generation、CRC、reserved、bool 编码和 Epoch/ID 不变量。损坏记录不会写入输出 state。
  3. generation 只允许单调增加，`UINT32_MAX` 后返回 `UCN_ERR_EXHAUSTED`，禁止静默回绕；提供比较 helper 供未来双槽 Provider 选择较新有效记录。物理双槽 I/O、选择策略和掉电矩阵仍由 04-10 实现。
  4. 扩展 Host fake 测试：完整 Record round-trip、CRC 破坏、未知 schema、全零/全 FF factory-empty、无回绕 generation 和非法 Epoch 编码；codec 无动态内存。
- 验证：Windows GCC Full CTest 14/14；随后 Full/Lite/Nano、配置契约和 WSL ASan/UBSan 在本轮最终门禁继续覆盖。
- 结论/限制：04-02 完成的是可审计的物理字节格式，而非“已经掉电安全”。下一项 `CLV2-04-03` 把 Provider 与 `REQUIRED`/`VOLATILE_TEST` 配置接入 Cluster；之后 04-04 才能在 init 前 load 并恢复安全状态。

### OP-230｜CLV2-M04 04-01 完成：平台无关 Persistence Provider v1 与 Host fake 契约门禁

- 时间：2026-08-21。
- 目标：完成 `CLV2-04-01`，先冻结与 Flash、文件系统、RTOS 和具体 MCU SDK 无关的 Persistence Provider 公共契约；不提前修改 Election、Takeover、ACK 或 Advertise 的生产发送路径。
- 实现：
  1. 新增 `include/ucn/ucn_cluster_persist.h`。Provider v1 含同步 `load()`、提交完整下一逻辑状态的 `submit()` 和可选 `poll()`；`ucn_cluster_persist_provider_t` 有 `struct_size/api_version` 兼容边界。逻辑状态覆盖 active/max Epoch、last Vote、committed Config reference、Rekey reference、Tombstone 和 boot/replay incarnation；物理 magic/CRC/双槽格式明确留给 04-02。
  2. 固定完成语义：`COMMITTED` 仅代表状态已 durable，`PENDING` 必须返回非零 token、Provider 必须先复制完整 request，`FAILED` 必须返回非 `UCN_OK` 原因。Header 明确禁止 Cluster 在 `load()` 完成或 `COMMITTED` 之前发送任何新的 Advertise、ACK、Commit 承诺。
  3. 新增 `tests/test_cluster_persist.c` Host fake。验证 factory-empty load、同步 Epoch 提交、异步 Vote 在 poll 前对 load 不可见、异步失败不发布新 Epoch、同步失败不发布 Config；`tests/test_public_headers.c` 同时验证外部产品可实现该 v1 API。
- 验证：Windows GCC Full、Lite、Nano CTest 各 14/14；新增契约测试与既有 group=2/4/8 隔离模拟共同经过同一测试入口。
- 结论/限制：04-01 只完成 API 与假存储契约，尚未向 `ucn_cluster_config_t` 接入 Provider，也没有任何真实 Flash 编码、load 恢复或 persist-before-promise 发送拦截；下一项为 `CLV2-04-02` 物理记录 schema，随后才可执行 04-03/04-04 的配置与初始化接入。

### OP-229｜CLV2-M03 功能审计 GO：冻结候选基线并授权进入 M04 Persistence

- 时间：2026-08-21。
- 决策：M03 最终功能审计确认 GO，未发现新的 P0/P1/P2 阻断。M03 `03-01..03-09` 与复审整改 `R01..R07` 关闭；`R08` 依批准边界保持逻辑 `active_epoch` 读访问器，物理 storage/统一 commit 继续留在 `CLV2-14-02`。
- 基线：将所有 M03 源码、测试、Golden、模拟器、任务表、Current FSM、审计/设计文档组成独立候选提交；`.h743cmp/` 是本地比较输出，不纳入版本库。后续 M04 改动必须建立在该提交之上，避免混入 M03 审计基线。
- 台账：里程碑表更新为 M03 `DONE`、M04 `IN PROGRESS`，并登记 `CLV2-04-01` 为当前任务。M04 的唯一首要安全门禁为 persist-before-promise：未持久化成功不得发送 Advertise、ACK 或 Commit；`PENDING` 仅保持本地事务，`ERROR` fail-closed。
- 验证：沿用 M03 最终审计门禁：MSVC Debug Full 14/14、Windows GCC Full 14/14、GCC Lite/Nano 各 14/14、配置契约 27/27、WSL ASan/UBSan 24/24、WSL `-fanalyzer` 24/24、group=2/4/8 `m03_isolation=passed`、`git diff --check` 无空白错误。
- 结论/限制：本记录允许开始 M04 的 04-01 公共契约；并不表示 Flash schema、load/boot 恢复、异步 persist 事务或 crash matrix 已实现，它们分别由 04-02..04-10 闭环。

### OP-228｜CLV2-M03 复审核实与整改：Assignment、历史 Takeover、无回绕与真实隔离门禁

- 时间：2026-08-21。
- 背景：逐项复核 `UCN_V5_Cluster_M03与理想协议架构对照复审报告_2026-08-21.md`，不直接采纳文本结论。固定 `fast-fixed/impaired/group=2` 种子在修复前真实触发 `handle_backup_assign()` 的 Phase 断言；其余 P0/P1 和 `STEPPING_DOWN` P2 均在当前源码路径确认。
- 实现：
  1. `BACKUP_ASSIGN` 先按 Member/Backup 的 active Epoch 或 Join Pending 的 pending Epoch 验证 `cluster_id/term`，并拒绝 zero/耗尽 generation；foreign/stale 帧不再改 `known_backup_*`。self Assignment 仅在合法 transition 成功后原子提交 known-backup、Backup mirror 和角色结果，并清除旧 takeover 的 deadline/ACK/announce 遗留状态。
  2. Type 12 Backup Member Sync 的 `SYNC_BEGIN`、DELTA 和普通 snapshot 均拒绝 zero/耗尽 `membership_sequence`；连续性改用 `cluster_serial_next_checked()`，不再以裸 `+1` 接收回绕。
  3. `HEAD_TAKEOVER` 对普通角色只接受 active Cluster；Recovery Head 仅接受 `last_cluster_id` 指定且存在 `last_stable_head` 的历史稳定域，并以 `max_seen_term` 比较。foreign Cluster 的 Term 不再同 Recovery local Term 直接比较，且 sender 必须等于 message Head。
  4. Federation Locator cache 在租约有效时只允许同 identity 的 `(term, record_nonce)` 严格单调更新；延迟或 foreign Reply 不得回滚下一跳，租约过期后才可替换 identity。`STEPPING_DOWN` 收到同簇更高 Term 时重定向 pending Epoch 并立即进入 `JOIN_PENDING`。
  5. 模拟器新增逐 Group 的可执行 isolation 不变量，而非只回显命令行参数；CMake 固定纳入 fast impaired `group=2/4/8` 三组回归，只有断言真通过才输出 `m03_isolation=passed`。
- 文档：更新 M03 任务表 R01-R07 为“已修复、验证通过”，并同步 Current FSM 的 Assignment 原子性、Type 12 serial、Recovery historical takeover 与 `STEPPING_DOWN` 行为。`active_epoch` 的物理存储/统一 commit 仍明确留给 `CLV2-14-02`，本轮维持 M03 的逻辑读访问器边界。
- 验证：Windows GCC Full、Lite、Nano CTest 各 14/14；配置契约 27/27；fixed-seed `fast-fixed/impaired` group=2、4、8 均输出 `m03_isolation=passed`；WSL ASan/UBSan 24/24；WSL GCC `-fanalyzer` 24/24；`git diff --check` 通过。
- 结论/限制：报告中的 P0/P1 和应在 M03 关闭的 P2 已完成整改，M03 仍为 `AUDIT HOLD`，不据此提前进入 M04。group=2 的隔离断言允许同一 Group 在受损场景出现多个本地 Head；它验证的是 M03 “不得跨 Group 错误归属”，不是 M11 跨簇 Merge/Handover 的单 Head 收敛承诺。本轮未创建 Git 提交或推送。

### OP-227｜CLV2-M03 03-09 完成：可复现 Epoch / Provider 性质测试，M03 进入 AUDIT HOLD

- 时间：2026-08-20。
- 目标：完成 `CLV2-03-09`，把 03-01 的边界样例与 03-07 的 serial 耗尽样例扩展为可重放的性质门禁，证明 Epoch relation 和 Cluster ID Provider 对大量组合仍维持 M03 的安全规则。
- 实现：
  1. `tests/test_cluster.c` 增加固定 master seed 派生的 8 条 xorshift32 流，每流 1024 组随机 Epoch 对，并构造同 Cluster 的溢出安全三元 Term 链。逐例经公共 comparator 断言 FOREIGN 的域截断、LOWER/HIGHER 反对称、SAME/CONFLICT 对称和同域 Term 顺序传递。
  2. 增加 8×96 组 Provider 属性测试。每例随机父簇和 parent Term，经真实 `ucn_cluster_init() → ucn_cluster_step()` 路径注入四类回调输出：`0`、broadcast、parent reuse、合法 ID；前三类必须 `UCN_ERR_CONFIG` 且保持 Detached/round 0，合法输出必须形成 Candidate/Term 1/round 1。该测试不依赖私有 helper，因此同时覆盖调用排序和 FSM fail-closed 边界。
  3. 03-07 的 Election、Backup challenge、Takeover、generation、membership sequence 五类 exhaustion 测试继续保留；与本项共同成为 M03 的随机/确定性安全门禁。所有 seed 是编译期常量，失败可稳定复现。
- 验证：GCC Full、Lite、Nano 各 12/12；MSVC Debug 12/12；配置契约构建 25/25；WSL ASan/UBSan 22/22；WSL GCC `-fanalyzer` 22/22；`git diff --check` 通过。Full 资源输出保持 `cluster_bytes=1136`，本项只增加 Host 测试代码，无生产 RAM、Wire、动态内存或 Core→Extended 依赖增量。MSVC 仅保留仓库已有 C4819 UTF-8/GBK 编码警告。
- 结论：`03-01..03-09` 代码与测试任务全部完成，M03 转为 `AUDIT HOLD` 等待审阅；审阅确认后才可标记 DONE 并进入 `CLV2-04-01` Persistence Provider 公共接口。当前未创建 Git 提交或推送。

### OP-226｜CLV2-M03 03-08 完成：Cluster ID Provider / incarnation，新的簇身份不再绑定 Node ID

- 时间：2026-08-20。
- 目标：完成 `CLV2-03-08`。去除普通新簇和 Recovery 对 `cluster_id == local_node_id` 的永久假设，使同一节点的不同运行轮次可建立不同的 Cluster identity，并为 M04 持久化和 M13 Rekey 预留统一入口。
- 实现：
  1. `ucn_cluster_config_t` 末尾追加可选 `make_cluster_id`、`cluster_id_context`、`cluster_id_incarnation`；回调请求固定携带 purpose（Election/Recovery/Rekey）、本地 Node ID、父簇 ID/Term、incarnation 与单调 round，保持既有位置初始化的源代码兼容。
  2. Core 新增唯一 ID 分配/校验入口。它先预检 round，再调用产品 Provider 或确定性默认生成器，统一拒绝 `0`、`UCN_NODE_BROADCAST` 和非零父簇 ID 的重用；错误不会消耗 round 或修改 FSM。默认仅在 `incarnation == 0`、无父簇、`round == 1` 的普通 Election 保留 `local_node_id`，此后各 round/Recovery/非零 incarnation 都导出新 ID。
  3. Ordinary Election 与 Recovery Head 都在 transition 前分配 identity。Recovery Declare、Recovery Head 自身和加入它的成员统一使用 provider 分配出的 `cluster_id`，不再假设它等于 Head Node ID；public view 同步公开 `cluster_id_round`。
  4. 新增回归：验证 Provider 的完整请求参数、父簇 lineage、无历史候选失败后的第二轮 ID、非法 `0`/broadcast/parent 结果的 fail-closed 行为，以及 Recovery 连续两轮均调用 Provider 并获得新身份。补正 M03-07 旧记录语义：03-08 后普通 Detach 重建的是新 Cluster ID + Term 1，而不是同 identity 的 Term 回退；M13 quorum Rekey 仍未实现。
- 验证：GCC Full、Lite、Nano 各 12/12；MSVC Debug 12/12；WSL ASan/UBSan 22/22；WSL GCC `-fanalyzer` 22/22；`git diff --check` 通过；Cluster serial raw-wrap grep=none。Full 资源输出 `cluster_bytes=1136`（相对 03-07 的 1104 固定 +32 B，无动态分配）。MSVC 仅有仓库已有 C4819 UTF-8/GBK 编码警告。
- 边界：确定性默认值只保证**单个 Cluster 对象运行期**不复用；掉电后若仍使用 `incarnation=0`，首轮仍会得到 legacy Node ID。产品若要跨重启唯一性，必须提供 boot counter/RNG/安全存储 incarnation 或自定义 Provider；M04 负责持久化与掉电原子性。`UCN_CLUSTER_ID_PURPOSE_REKEY` 目前只是接口预留，不能据此宣称已经具备 quorum Rekey。
- 结论：03-08 完成，M03 仍为 IN_PROGRESS；下一项为 `CLV2-03-09` Epoch/Provider/serial 性质测试。本记录对应工作区，尚未创建 Git 提交或推送。

### OP-225｜CLV2-M03 03-07 完成：Cluster serial 阈值耗尽 fail-closed，禁止回绕复用

- 时间：2026-08-20。
- 目标：完成 `CLV2-03-07`。在 M13 的 quorum Rekey 尚未实现期间，Term、Backup generation 与 membership sequence 达到安全旋转阈值后必须显式停止；绝不允许 `UINT32_MAX → 1` 使旧控制帧重新具有看似“新”的身份。
- 实现：
  1. 公共 Cluster 配置增加 `UCN_CLUSTER_SERIAL_ROTATION_THRESHOLD`（默认 `UINT32_MAX - 1024`）及编译期范围断言；新增公共错误值 `UCN_ERR_EXHAUSTED=-17`。内部唯一递增入口 `cluster_serial_next_checked(current, next)` 对 `current >= threshold` 返回该错误，零值仍可作为未初始化 serial 分配首个 1。
  2. Term 的三条生成路径全部改为预检后提交：Detach 观察期的 Election、Backup score challenge、majority quorum takeover。预检失败时不发生 Phase transition、角色/Term 改写或控制发送；`ucn_cluster_step()`、challenge/test-hook、ACK completion 可见 `UCN_ERR_EXHAUSTED`。本项完成时普通 Re-election 仍复用同一 identity，故以 03-06 的 `max_seen_term` 作为下界；该临时语义已由后续 OP-226（03-08 的新 Cluster ID + Term 1）取代。
  3. Backup generation 与 membership sequence 同样使用该 helper：generation 耗尽时不提交新 Backup assignment；镜像 Delta/Snapshot 耗尽时 Owner Step 返回 `UCN_ERR_EXHAUSTED` 且不发送回绕帧。Cluster 目录的 `UINT32_MAX ? 1 : ...` 模式 grep 为零。
  4. 新增回归覆盖五类边界：Detach + 安全历史高 Term 的重选、Backup score challenge、quorum takeover、Backup generation assignment、已 READY Backup 的 membership mirror sequence；每例验证耗尽后不回绕、不 transition 或不发送。
- 验证：GCC Full 12/12；Lite 12/12；Nano 12/12；MSVC Debug 12/12；WSL ASan/UBSan 22/22；WSL GCC `-fanalyzer` 22/22；Cluster raw-wrap grep=none；`git diff --check` 通过。Full 资源输出仍为 `cluster_bytes=1104`；MSVC 仅保留仓库已有 `C4819` UTF-8/GBK 编码警告。
- 边界：这不是 Rekey 协议；它只在当前 v3/Current FSM 阶段建立**不回绕**的安全底线。达到阈值后的新 Cluster ID、迁移 quorum、persist-before-promise 与旧 Cluster tombstone 均仍归 M04/M13，不能将当前“停止”误认为已经完成自动恢复。
- 结论：03-07 完成，M03 仍为 IN_PROGRESS；下一项为 `CLV2-03-08` Cluster ID Provider/incarnation hook。本记录对应工作区，尚未创建 Git 提交或推送。

### OP-224｜CLV2-M03 03-06 完成：Detach 后保留运行期 Epoch 安全历史，拒绝旧 Term 重入

- 时间：2026-08-20。
- 目标：完成 `CLV2-03-06`。节点离开当前簇时，Active/Pending 状态可以清除，但不得遗失最近稳定簇的已见 Term 与 Head；否则延迟到达的旧 Term Head 广播可能让节点错误重新加入已过时的 Epoch。
- 实现：
  1. 在 `ucn_cluster_t` 新增 `last_cluster_id/max_seen_term/last_stable_head`，`ucn_cluster_view_t` 同步给出只读诊断快照。该历史当前只在 RAM 存活，明确不承诺跨重启；M04 将以 Persistence Provider 取代这一边界。
  2. `cluster_history_note_stable_epoch()` 只单调记录一个最近稳定 Cluster 域：更高 Term 才替换；相同 Term 不以不同 Head 覆盖已知 Head。Election 胜出、Join Accept、Backup Assign/Takeover、受认证的同簇 higher-Term offer 等确认点均调用它。Recovery 域与 Candidate 临时 Term 不得污染稳定历史。
  3. `set_detached()` 仅清 Active/Pending；若仍持有稳定 Active Epoch 仅作补记，绝不能倒退已提前看到的 higher-Term 历史。普通 `ADVERTISE/HEAD_DECLARE` 经 decode、来源认证和候选 replay admission 后，先检查历史：同历史簇的低 Term、或同 Term 异 Head 以 `UCN_ERR_REPLAY` 拒绝；同 Term 原 Head 可正常重新发起 Join。活跃节点正处于同 Epoch 双 Head 的情况特意放行给 03-05 `TERM_CONFLICT_WAIT`，不会被误判为 stale；foreign Cluster 仍绝不按 Term 比较。
  4. 新增真实 RX 回归：`HEAD_STEPDOWN → Detach → ADVERTISE` 依次验证 A/6 拒绝、A/7 异 Head 拒绝、A/7 原 Head 可 Join；另验证已先收到 A/8 并进入 Join Pending 时，旧 A/7 Stepdown 后历史仍为 A/8，旧广告继续被拒绝，并校验 public view。
- 验证：GCC Full 12/12；Lite 12/12；Nano 12/12；MSVC Debug 12/12；WSL ASan/UBSan 22/22；WSL GCC `-fanalyzer` 22/22；Full 资源输出 `cluster_bytes=1104`（原 1096，固定 +8 B）；Golden/规模场景同属各套 CTest 并通过；`git diff --check` 通过。MSVC 仅保留仓库已有 `C4819` UTF-8/GBK 编码警告。
- 边界：本项解决的是**不掉电的运行期** Detach/rejoin 回退；掉电、Flash 写入失败、incarnation、persist-before-promise 和 Term Rotation 仍分别归 M04 与 03-07/M13，不能据此宣称持久化安全已经完成。
- 结论：03-06 完成，M03 仍为 IN_PROGRESS；下一项为 `CLV2-03-07` checked serial helper + exhaustion fail-closed。本记录对应工作区，尚未创建 Git 提交或推送。

### OP-223｜CLV2-M03 03-05 完成：同簇同 Term 双 Head 进入本地安全等待

- 时间：2026-08-20。
- 目标：完成 `CLV2-03-05`。当已经持有本地 active Epoch 的节点，经来源认证、候选 replay 接纳后观察到“同 `cluster_id`、同 Term、不同 `head_node_id`”的普通 Head offer 时，禁止沿用旧的 Score/Node ID 评分仲裁；必须停止相关控制活动并进入安全等待，等待后续正式 Fence（M08）。
- 实现：
  1. 新增**仅本地**的 `UCN_CLUSTER_ROLE_TERM_CONFLICT`、`UCN_CLUSTER_PHASE_TERM_CONFLICT_WAIT` 和 `UCN_CLUSTER_REASON_TERM_CONFLICT`；Codec 的 wire-role 验证仍只允许 v3 已定义的 `DISABLED..RECOVERY_HEAD`，因此该安全状态不会被发送或被远端伪造。`cluster_bytes` 不变。
  2. 新增统一 `process_term_conflict()`，在 `ADVERTISE/HEAD_DECLARE` 的全局 RX priority 中置于 `process_higher_authority()` 之前；任何可持有本地 Epoch 的 Current Phase 命中 comparator `CONFLICT` 后，先走合法 transition，再撤销 advertise/keepalive/join/election/takeover deadline 与 Recovery/Backup takeover 活动。Score、Node ID、普通 role handler 不再有机会裁决冲突。
  3. `TERM_CONFLICT_WAIT` 的普通 `ucn_cluster_step()` 为控制静默；入站仅保留普通 Head offer 判定入口：同 Term 重复 offer 幂等保持等待，严格更高且同簇的 Term 才经 `HIGHER_AUTHORITY` 转 `JOIN_PENDING`。其余 lifecycle/voting/takeover 控制帧返回 `UCN_ERR_STATE`，避免绕过安全等待直接改写角色。
  4. 既有同 Term “更高评分 Head 切换”测试改为验证 TERM_CONFLICT；新真实 RX 回归覆盖 ELECTION、JOIN_PENDING、MEMBER_ACTIVE、MEMBER_TAKEOVER_GRACE、HEAD_NO_BACKUP、HEAD_BACKUP_ASSIGNING、HEAD_BACKUP_SYNCING、HEAD_STABLE、BACKUP_SYNCING、BACKUP_READY、BACKUP_TAKEOVER、RECOVERY_HEAD 共 12 个 Phase。每个 Phase 验证一次 transition、Step 零控制发送、普通 PRIMARY_HEARTBEAT 被拒绝、同 Term 重放不新增 transition；另验证严格更高 Term 才能离开安全等待。
- 验证：GCC Full 12/12；Lite 12/12；Nano 12/12；MSVC Debug 12/12；WSL ASan/UBSan 22/22；WSL GCC `-fanalyzer` 22/22；Full 资源输出 `cluster_bytes=1096`；Golden/规模场景同属各套 CTest 并通过；`git diff --check` 通过。MSVC 仅保留仓库已有 `C4819` UTF-8/GBK 编码警告。
- 边界：这不是 M08 的持久化 Fence、Quorum 证明或永久隔离；当前实现选择可用性更低但 fail-closed 的安全等待。跨簇 Merge/Handover 仍不在 03-05 范围，仍归 M11。
- 结论：03-05 完成，M03 仍为 IN_PROGRESS；下一项为 `CLV2-03-06` Detach safety history。本记录对应工作区，尚未创建 Git 提交或推送。

### OP-222｜CLV2-M03 03-04 完成：同簇高 Term 的全局 RX 优先入口 + 当前门禁修复

- 时间：2026-08-20。
- 目标：完成 `CLV2-03-04`，将受认证、已准入来源的普通稳定 Head offer（`ADVERTISE` / `HEAD_DECLARE`）在 decode、来源校验和候选 replay 接纳后、进入任何角色的普通 offer 逻辑前，统一交给 `process_higher_authority()`。`ucn_cluster_epoch_compare(local, remote) == LOWER` 是唯一命中条件，即**同簇且远端 Term 更高**；FOREIGN 在比较域截断处返回，不会因数值大的 Term 进入此入口。
- 实现：
  1. 新增内部 `process_higher_authority()` 与显式 `UCN_CLUSTER_REASON_HIGHER_AUTHORITY`。Head / Recovery Head 复用有序 stepdown；Member（含 grace）与 Backup（SYNCING / READY / TAKEOVER）统一转 `JOIN_PENDING`，然后按既有站点顺序清理 Backup mirror、建立 pending join。`consider_head_offer()` 的 test-hook 直调也先经过同一 gate，删除原 Backup 内部重复的裸 `term >` 分支，避免两套 higher-Term 行为漂移。
  2. 新增真实 RX 回归 `cluster_test_global_higher_authority_pre_dispatch()`：覆盖 MEMBER_ACTIVE、MEMBER_TAKEOVER_GRACE、四个 Head 子 Phase、三个 Backup 子 Phase、RECOVERY_HEAD 共 10 个 Current active Phase；每例断言 decode → replay admission → pre-dispatch 后仅发生一次 transition、原因均为 `HIGHER_AUTHORITY`，并验证 Head 类到 `STEPPING_DOWN`、Member/Backup 类到 `JOIN_PENDING`。既有 Backup 高 Term 测试同步改为验证该统一原因。
  3. Golden trace 仅接受本语义造成的两步变化：node4 在 `t=180` 收到同簇更高 Term 的 HEAD_DECLARE 先进入 JOIN_PENDING，随后 `t=181` 接收已验证的 HEAD_TAKEOVER 成为 Member；不是跨簇合并行为变化。
  4. 前置门禁修复：无成员时 Backup assignment 不再先进入 ASSIGNING 又立即清空 pending 而触发 debug derive assert；受损规模测试改为显式 `--expect-m03-isolation`，在 M03 阶段检查所有存活节点仍有 Head/Member 覆盖、控制预算和故障注入有效，**不再错误要求跨簇立即收敛为单 Head**（跨簇 Merge 属 M11）；MSVC 测试观察表改为函数式 DIRECT+3 个 compound 判断，消除“用另一静态表元素初始化静态表”的非标准扩展。
- 验证：GCC Full 12/12、Lite 12/12、Nano 12/12；MSVC Debug 12/12；WSL ASan/UBSan 22/22；WSL GCC `-fanalyzer` 22/22；`git diff --check` 通过。资源回归保持 `sizeof(ucn_cluster_t) == 1096`；未引入动态内存或 Core→Extended 依赖。
- 边界：`HEAD_TAKEOVER` 不是普通 offer；它仍必须先通过已知 Backup、generation、nonce 等独立证明后才提交 Member，因此不将其降级为普通 `process_higher_authority()` 路径。same-cluster/same-Term/different-Head 的当前 score 仲裁尚未替换，明确留给下一项 `03-05` 的 TERM_CONFLICT 安全等待。
- 结论：03-04 已完成，M03 维持 IN_PROGRESS；下一项执行 `CLV2-03-05`。本记录对应工作区，尚未创建 Git 提交或推送。

### OP-221｜CLV2-M03 03-03 审计 MINOR 关闭：测试注释 HIGHER→LOWER（comment-only，无逻辑改动）

- 时间：2026-08-20。
- 背景：人类终审 d6a0d22 签 03-03 协议语义 PASS（BLOCKER 0 / MAJOR 0 / MINOR 1），唯一 MINOR 是测试注释把 compare(local, remote) 实际返回 LOWER 的场景写成了 HIGHER（Group 3 顶部摘要 + Group 3 内部注释两处）；代码无方向性 BUG（classify_same_cluster_authority 内注释方向正确）。人类同时冻结命名规则：relation == LOWER ⇔ remote_is_higher_authority；严禁再写模糊的 "higher relation" 代指远端更高，防 03-04 process_higher_authority 实现时写反。
- 修改（tests/test_cluster.c，仅注释）：(1) 测试函数顶部 Group (3) 摘要改为 "-> LOWER ->（compare(local, remote)：remote A/3 IS the higher authority）-> 统一同簇 authority path -> surrender"；(2) Group 3 内部注释改写：relation == LOWER（local term 2 < remote term 3），remote_is_higher_authority == true，并显式警告 "Never write this as relation == HIGHER: HIGHER would mean the REMOTE is stale"。无任何断言/逻辑/常量改动。
- 门禁（复核）：FULL ucn_tests 编译 -Werror 干净 + 全量运行 All UCN tests passed（OBSERVED-PAIRS 34 零违例，与 d6a0d22 一致——comment-only 无行为差异）。
- 结论：03-03 审计 MINOR 关闭；03-03 正式 CLOSED。命名规则冻结（relation == LOWER ⇔ remote 更高权威；03-04 用 remote_is_higher_authority 命名显式化）。已提交将推送。
### OP-220｜CLV2-M03 03-03 完成：consider_head_offer 按 Epoch 关系分类——FOREIGN 绝不进入任何 authority 让位逻辑（M03 第一个协议语义变更）

- 时间：2026-08-20。
- 背景：人类终审 f943fb9 签 03-02 CLOSED（active_epoch 行为等价），批准 03-03：把 consider_head_offer() 拆为 classify_same_cluster_authority() / classify_foreign_cluster_merge()；删除 HEAD 跨簇直接按 Term 让位（A/2 vs B/100 → FOREIGN，绝不能进入 HIGHER-authority 让位逻辑）；4 组钉死测试；Golden 差异只允许归于 "foreign cluster Term 不再作为 authority superiority"。
- 03-03 实现（src/extended/cluster/ucn_cluster_merge.c，唯一语义 delta 在 HEAD 分支）：
  1. HEAD 分支不再比较裸 Term：local_epoch = ucn_cluster_active_epoch_get(cluster)；remote_epoch = {candidate->cluster_id, candidate->term, candidate->head_node_id}；relation = ucn_cluster_epoch_compare(&local_epoch, &remote_epoch)。
  2. relation == FOREIGN → classify_foreign_cluster_merge()：零 authority 动作——不让位、不比 Term、不进 score 仲裁、不改状态（跨簇 merge 协商是后续 OP，此处诚实 no-op 并注释）。
  3. 其余（SAME/LOWER/HIGHER/CONFLICT）→ classify_same_cluster_authority()：relation 语义注意 (local, remote)——LOWER 表示 remote Term 更高（本地低）→ 立即 begin_ordered_stepdown（原 candidate->term > cluster->term 的合法同簇版）；HIGHER 表示 remote 过时 → return（原 stale 分支）；SAME/CONFLICT（同簇同 term）→ 原 score/Node-ID/样本/任期仲裁原样保留。CONFLICT 明确不是 foreign merge，进入统一同簇路径（03-05 再做最终冲突处理）。
  4. 逐行对照：同簇 term/score 路径行为 1:1 等价，仅 FOREIGN 分支语义改变（新增 2 个 static 分类器 + HEAD 分支重写；其他所有分支零改动）。
- 测试（tests/test_cluster.c）：
  - 新增 cluster_test_epoch_classified_head_offer()（4 组钉死，经 ucn_cluster_test_consider_head_offer 白盒钩子）：(1) 本地 A/2 vs remote B/100 → FOREIGN，better_samples=200 不动、role 保持 HEAD、shadow_transition_count 0、head_switches 0（100>2 且 score 更优也不让位）；(2) 本地 A/100 vs remote B/2 → FOREIGN 反向同契约；(3) 本地 A/2 vs remote A/3 → LOWER（remote 更高）→ 统一同簇路径让位：STEPPING_DOWN、pending={1,3,head}、count=1；(4) 本地 A/2/head0 vs remote A/2/head1 → CONFLICT：先低 score（6200<7200）→ better_samples 归零（证明走了同簇 reset 分支而非 foreign no-op），再高 score（9500）+ samples=2 → 阈值+任期满足 → STEPPING_DOWN（证明 CONFLICT 进入统一同簇仲裁，绝非 foreign）。
  - 更新 cluster_test_full_head_term_convergence()（C07.7 P1）：原测试用 foreign cluster_id=2 的更高 Term 让位来证明 "capacity 0 不阻断收敛"——这恰恰钉死了 03-03 删除的行为。改为两段：先 foreign（A/1 vs B/2, capacity 0）→ 断言 role 保持 HEAD、count=0（不让位）；再同簇（A/1 vs A/2, capacity 0）→ 断言 STEPPING_DOWN + pending（capacity 0 不阻断同簇高 Term 收敛，P1 意图保留）。
- OBSERVED-PAIRS count 35→34 归因（关键证据，纯 foreign 让位删除）：消失的对是 13→1（STEPPING_DOWN→DETACHED_OBSERVE，spec 允许的 tick 复合，仅"不再被观察到"）。基线探针定位到 cluster_test_capacity_is_bounded：容量=1 的簇拒绝 head-capable 的 node1(7000) 入簇 → node1 自选为自身簇 2 的 HEAD；基线里 node0 的簇 1 ADVERTISE 落入 HEAD 分支同 term → score 路径跨簇让位（9000>7000，3 样本+任期后 STEPPING_DOWN）→ join 被拒 → DETACHED（t=128 观测到 13→1）。03-03 后：FOREIGN → 不让位 → node1 保持自身簇 2 的 HEAD（实测 t=140 role=HEAD ph=HEAD_NO_BACKUP clu=2）。即 foreign offer 连 score 路径也不进入——这正是 "foreign 不参与 authority" 的完整语义，Golden 差异判据 "foreign cluster Term 不再作为 authority superiority" 的唯一来源。13→1 仍在 SPEC 中（允许），violations 保持 0。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=34 零违例（35→34 归因如上）；Golden 逐字节不变（blob 6ed2ada 未动——canonical lifecycle 全同簇，无 foreign 让位路径）；cluster_bytes 1096（无 struct 变化）；-Werror 干净；git diff --check 零告警。
- 结论：03-03 完成——HEAD 分支由裸 Term 比较改为 Epoch 关系分类，FOREIGN 永不进入让位/score authority 逻辑，同簇路径 1:1 等价。按人类指令在此停止审计：4 组钉死测试 + observed 35→34 归因 + golden 逐字节证据齐备，等待人类复核 comparator 驱动的首个协议语义变更。已提交将推送。

### OP-219｜CLV2-M03 03-02 完成：active_epoch 读访问器（行为等价存储/访问统一）+ 03-01 双 NIT 关闭

- 时间：2026-08-16。
- 背景：人类终审 116df31 签 03-01 PASS（BLOCKER/MAJOR/MINOR 0，NIT 2），批准进入 03-02，并明确边界：**03-02 只做存储/访问统一、行为等价；foreign-first comparator 改变协议决策留到 03-03**（不要顺手把 `if (cluster_id == msg.cluster_id && msg.term > term)` 换成 comparator 从而改变 foreign 行为）。
- 03-02 设计（关键决策）：**不新增物理 `active_epoch` 字段**——那会使 ucn_cluster_t 变大、破坏 cluster_bytes=1096 硬门禁。行为等价方案：`ucn_cluster_active_epoch_get()` 读访问器返回由既有 `cluster_id/term/head_node_id` 三字段组装的逻辑 epoch（物理存储不变、struct 尺寸冻结）；NULL/Detached 返回 {0,0,0}；不承担有效性校验（comparator != validator）。ucn_cluster.h include epoch 头 + view API 区声明 getter。03-03+ 将用 comparator 驱动 Head-Offer/Merge/Higher-Authority 决策。
- 03-01 NIT 关闭：(1) **NIT-1** property_sanity 从 `TEST_ASSERT(true)` 空壳改为真实确定性断言——LOWER/HIGHER 反对称、SAME 双向稳定、CONFLICT 对称、FOREIGN 对称 + 跨簇域截断（term 2 vs term 100 双向 FOREIGN）；(2) **NIT-2** epoch header 明确写入 "COMPARATOR != VALIDATOR"——零/保留字段有效性由 03-08 状态/Provider 不变量负责，不得把 `compare != UNKNOWN` 当 "epoch is valid"。
- 门禁：FULL + ASan + LITE + NANO 全绿（All UCN tests passed）；OBSERVED-PAIRS count=35 零违例；**Golden 逐字节不变（行为等价验证）**；**cluster_bytes 1096 不变（物理存储未动——03-02 关键门禁）**；-Werror 干净；diff --check 零告警。
- 结论：03-02 完成（存储/访问统一，行为等价）。下一步 03-03（consider_head_offer 拆分为 classify_same_cluster_authority / classify_foreign_cluster_merge，删除跨簇直接按 Term 让位）——M03 第一个真正改变协议语义的点。已提交将推送。

### OP-210｜M01 显式 Phase 正式闭环（人类终审签字 ab53b31，全序列 01-01..01-04f PASS）

- 时间：2026-08-16。
- 终审：人类按"01-04f 是整个 M01 最后一道门"标准重审 ab53b31，签字 M01 正式闭环：BLOCKER 0 / MAJOR 0 / MINOR 0 / NIT 2；01-01 Phase model、01-02 Shadow mapping、01-03 Reason infra、01-04a Transition framework、01-04b DETACHED/ELECTION/JOIN、01-04c MEMBER/GRACE、01-04d HEAD ladder、01-04e BACKUP/TAKEOVER、01-04f RECOVERY/STEPPING_DOWN 全部 PASS；正常路径隐式 Phase mint NONE FOUND；Shadow-Guard 纪律 PASS（全站无 if(shadow_phase==X) transition(...) bypass，判等仅存于 shadow_sync/validate/commit 框架位置）；M01.0.2 组合保留；Golden 等价 PASS。M02 权限 APPROVED。
- 人类确认的关键判定：(1) zero-backoff 退化自旋（OBSERVE→nonce bump→OBSERVE）是 Current deficiency 而非 Target 设计，M01 保留之正确，留待 M12 Recovery backoff/lineage 真正解决；(2) f6（JOIN_PENDING→MEMBER_ACTIVE 第二生产 site）若不补 M01 会留下 RX-wrapper mint，已关闭；(3) handle_recovery_declare 的 phase_committed 是防同 handler 二次铸同一目标 Phase，非 Shadow 决定跳过；(4) M01.0.2 BACKUP_TAKEOVER+syncing 作为 Legacy pre-state 正确迁入 winner Member，未为简化 handler 清除 Current 可达状态；(5) 全文件"隐式 mint"门禁独立复核通过——role/recovery_eligible/recovery_backoff_deadline/backup_ready/backup_syncing/backup_takeover_active/head_grace_deadline/backup_node_id/backup_assign_pending/stepping-down completion 十类写点无正常路径满足"phase 改变→无 transition→靠 end-of-Step/RX mint"；残余 legacy/no-transition 注释对应 Phase-preserving 路径；not migrated yet 清零。
- NIT-1：zero-backoff 必须明确标为 Current-preservation 而非最终策略（M12 修）。NIT-2：M02 拆模块不得在"transition commit 后、site cleanup 完成前"的原子窗口插入 callback/provider call/async dispatch/early return——当前 Single Protocol Owner 结构下仅属内部实现顺序的瞬间状态不得变成外部可观察状态。两 NIT 均不阻塞 M01 closure。
- 门禁（终审复核）：clean rebuild FULL 22/22 + ASan/UBSan 22/22；LITE/NANO green；Observed pairs 35/0；Golden blob 不变；cluster_bytes 1096。
- 结论：CLV2-M01 显式 Phase 迁移正式冻结。M02 按纯结构性重构执行——cluster_transition() 语义、DIRECT matrix、Legacy derive、各 site before/transition/after 顺序一律不"顺手优化"。已提交将推送。
