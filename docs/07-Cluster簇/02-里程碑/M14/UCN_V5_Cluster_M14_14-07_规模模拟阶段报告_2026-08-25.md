# UCN V5 Cluster M14 / 14-07 规模模拟阶段报告

> 日期：2026-08-25  
> 状态：`PARTIAL / CURRENT-FSM MATRIX PASS / TARGET INTEGRATION BLOCKED BY M05`

## 1. 已完成矩阵

使用生产 `ucn_cluster` archive（无 test bridge）对 64/256/1000 Node、每簇 8 Node 执行：

- clean；
- impaired：丢包、延迟、重复、短时非对称；
- mobility：每簇一名 Member 离开后返回；
- score-shift；
- head-failover 的预期安全围栏。

完整机器可读结果见 `docs/results/M14/cluster_scale_matrix_2026-08-25.csv`。15 个规模门禁全部进入 CTest。

| Node | clean 收敛 | mobility 最终收敛 | score-shift 恢复 | impaired | 峰值/Node/1s |
|---:|---:|---:|---:|---|---:|
| 64 | 8920 ms | 23000 ms | 4920 ms | M03 隔离 PASS | 13 |
| 256 | 8920 ms | 23000 ms | 4920 ms | M03 隔离 PASS | 14 |
| 1000 | 8920 ms | 23000 ms | 4920 ms | M03 隔离 PASS | 14 |

控制消息总量随独立 8-Node group 数近似线性增长；本模型不存在跨组共享 Bearer，因此不能据此推导真实无线介质的全网吞吐。

## 2. 负向产品边界

所有 Primary 同时故障后，64/256/1000 三组结果均为 0 Head，且进程通过 `--expect-v3-authority-fence` 明确验证该结果。它证明生产 v3 Backup/Takeover 没有被重新放开；**不代表 failover 成功**。

## 3. 尚未完成的 Target 规模项

以下任务仍被 M05 的 production v4 RX/TX/FSM/Authority 冻结：

| 子场景 | 当前证据 | 14-07 状态 |
|---|---|---|
| Config churn / Joint 双 quorum | M07 定向与 bounded property | 缺生产 Wire/Owner 规模接线 |
| Backup churn / frozen snapshot | M09 定向模型 | 缺生产 v4 Backup Owner |
| Primary fault / M10 takeover | M10 实验事务 | 生产 v3 明确 fail-closed |
| Recovery lineage | M12 定向生命周期 | 缺生产 v4 恢复网络接线 |
| Rekey/no-wrap | M13 实验事务与持久化模型 | 缺 production commit/materialize |

为维持 M05 `AUDIT HOLD`，本项没有另造第二套伪 Wire/FSM 来产生“全绿规模结果”。只有未来生产集成经独立审计后，才能完成这些子场景并把 14-07 标为 DONE。

## 4. 自审结论

Current-FSM 规模矩阵与 v3 Authority 围栏自审通过；Target v2 churn 规模闭环未完成，14-07 必须保持 PARTIAL。该阻断不会阻止独立的 14-09 资源核算与 14-10 文档同步，但会阻止 14-11 最终签字和 14-12 发布。
