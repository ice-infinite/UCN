# CLV2-06-01：成员状态值模型自审报告

日期：2026-08-22  
结论：**DONE / 外部复审 GO（受限范围）**。

## 1. 实现范围

| 合同 | 实现与证据 | 自审 |
|---|---|---|
| 有界成员模型 | `ucn_cluster_membership.h` 定义 `NONE/PROVISIONAL/COMMITTED/REMOVING`，record 保持固定大小、无动态分配。 | PASS |
| 状态安全 | 公开纯函数拒绝无效枚举、非法转换、零/broadcast Node ID、v3 非零 capability、Provisional voting 和非 canonical 空槽。 | PASS |
| legacy-v3 桥 | Join、Backup mirror、Recovery ACK 的新槽均初始化为 `COMMITTED + voting + v3 + cap=0`；非法 identity 不写 slot。 | PASS |
| 旧运行语义 | 既有成员计数、容量、Backup、Takeover、选举循环仍只依据 `occupied`；本次未读取 `status/voting` 作资格或 Authority 决策。 | PASS |
| M05 隔离 | 新 public header 和 membership producer 不包含 v4 wire；production `ucn_cluster.c` 的 v4 引用为零、Adapter 的 Cluster 引用为零。 | PASS |

## 2. 回归与模拟

| 环境 | 实际结果 |
|---|---|
| Windows GCC Full / Service ON | CTest `30/30` PASS |
| Windows GCC Lite / Service ON | CTest `30/30` PASS |
| Windows GCC Nano / Service ON | CTest `20/20` PASS |
| Windows GCC Full / Service OFF | CTest `27/27` PASS |
| WSL GCC ASan/UBSan Full | CTest `20/20` PASS |
| WSL GCC `-fanalyzer -Wall -Wextra -Werror` Full | CTest `7/7` PASS |
| Full 64-node clean simulation | 收敛通过，`converged_ms=8920`、`object_bytes=1392` |

新增模型测试逐项覆盖完整 4×4 状态转换矩阵、record 合法域、legacy 初始化/keepalive、非法 identity 的无写回；既有 Cluster 集成回归额外验证真实 v3 Join、Backup failover mirror 和 Recovery member record 的 metadata。

## 3. 资源与隔离

- Host x64 Debug Full：`sizeof(ucn_cluster_t)=1392 B`；相对 M04/R23 的 `1200 B` 增加 `192 B`。
- 默认 `UCN_CLUSTER_MAX_MEMBERS=16`，故此次 record 元数据增量为每槽 `12 B`；未引入堆分配，也未把 Config/Certificate/完整 Wire v4 frame 放入 Cluster 对象。
- `git diff --check` 通过；输出仅为现有 CRLF 提示，无空白错误。

## 4. 未完成与外审边界

本项**没有**实现 v4 Join → Provisional、Provisional timeout、Runtime/Voter table 分离、voter set/quorum、Joint Config、Authority、生产 v4 RX/TX/FSM 或实机互通。M05 顶层仍为 `AUDIT HOLD`；本报告只申请 06-01 数据模型范围的外部审计，不申请任何生产协议放行。

## 5. 外部复审结果

2026-08-22 外部复审签署 **GO（受限范围）**，未发现 P0/P1/P2。独立复跑确认 Full `30/30`、Lite `30/30`、Nano `20/20`、Service OFF `27/27`、WSL ASan/UBSan `20/20`、`-fanalyzer` `7/7` 通过；64-node clean simulation 仍在 `8920 ms` 收敛，Host `object_bytes=1392`。

签字结论仅确认成员状态值模型和 legacy-v3 元数据桥。它不解除 M05 顶层 `AUDIT HOLD`，也不授权生产 v4 RX/TX/FSM、Authority、Voter quorum、资格选择或实机完成声明。
